# Handoff

## Objective
Teddy's terum-skills desktop app (`desktop/` inside `ryanliu-terum/terum-skills`, public, Apache-2.0). Teddy's directives in force, in his words where it matters:
1. M7 = "close the canvas gaps in the CLI (metadata + intelligent sync)": DONE as a document (see Progress).
2. "The real app should only be showing real data. If the inbox does not work it should not be there, visible just yet." and "Once it is covered by M7 implemented, they should show, since they should be connected." So every batch ships its CLI half AND its app half (adapter mapping, mock, served-surface un-hide) in one PR; a surface whose read model is still a gap stays hidden.
3. "Don't care about ryan. You can merge." Implementers merge their own PRs after their gates and the PR's CI checks are green. Never a direct push to main, never a hook bypass, never merge a draft.
4. "finish up the M7 and move to implementation": the implementation queue is LIVE (see Current State). Goal: a working app at the end of the queue, proven by the end-to-end review (headless frames leg here, native leg on Teddy's Windows ARM host).
Constraints: `.planning/` and the canvas stay local (never committed); Codex (gpt-6-astra) writes all code, Claude specs, gates, commits, pushes, opens and merges PRs; one gate battery at a time on this box (the queue serialises with flock); the 87 locked fidelity boards never move (a pixel change is a failure, never an unlock); every new CLI field is `?? null`, never undefined.

## Approach (settled; do not relitigate)
- **Batch lettering is fixed**: S7a..S7ag as in `routed-batches.json` / `batches-slim-routed.json` (S7b = people file, S7f = ls/search payload, S7k = status, S7af/S7ag = the two app-only plumbing batches that run first). The workflow synthesizer re-lettered these twice even when pinned, so its table was discarded and the document is GENERATED by `build-m7-doc.py` from the verified data. Never hand-edit the document; edit `apply-routing.py` (routing) or `build-m7-doc.py` (prose) and regenerate with `--force`.
- **Order**: S7af, S7ag, S7f, S7g, S7ad, S7k, S7d, S7c, S7b, S7e, S7h, S7i, S7j, S7ae, S7l, S7m, S7n, S7o, S7p, S7q, S7r, S7s, S7t, S7u, S7v, S7w, S7x, S7y, S7aa, S7ab. Skipped: S7z (MOOT, relabel), S7a (MOOT, frame mode), S7ac (DONE). Drafts, never merged by the queue: S7t (PF-02 needs Teddy's written yes), S7ab (Ryan's closed guard table). Every batch except S7af/S7ag depends on S7af and stacks on its branch until it merges; S7ad also stacks on S7ag (both edit lib.rs).
- **Teddy's decisions (2026-09-08, ledger in `review/2026-09-08-desktop-blank-map/m7-routing/decision-ledger-2026-09-08.md`)**: D-BM-1=B (S7ae ships progress frames), D-BM-2=A (tool presence only, offline), D-BM-3=A + rider (join target comes from the invite's generated wizard command; a joiner never types the team), D-BM-4=A (`declined` on `ls member`), D-BM-5=A (one S7k PR), D-BM-6=A (RM-44 subset), D-BM-7=A (`--runs`); A1 keep CP-30 as note-composer; A2 confirm team.json key casing before S7y (lowercase = MOOT); A3 relabel New skill to Connect (design walk); A4 derive RM-13 from the SKILL.md body; A5 hold PF-02 and the Follow tooltip's Inbox promise; A6 accept the TJ-02 interim; A8 the three refused strings stay design-side.
- **Claude's three calls under "finish it"** (recorded on the batches, reversible): S7j returns placed/removed skill NAMES beside counts; S7t's project heart is a derived chip (favorites stays `skillId[]`); S7c closes CP-D1 on the costing's merits (MC-11 only).
- **Critic's verdict on Teddy's premise**: QUALIFIED. Confirmed for the bulk (53/62 SIMPLE CLI rows need no new field; a committed field propagates with zero sync code). Contradicted for the greyed controls (10 of 12 FRAME_FEATURES flags are decisions/permissions/absent mechanisms, not metadata; the team schema is closed by `guard.ts:150-156`, not by layout). Quoted verbatim in the document's section 0.
- **Queue mechanics** (`m7-implement-queue-v2.js`): priority ready-queue, max 2 in flight, dependents `git merge origin/codex/m7-<dep>` onto unmerged dependency branches, a three-attempt spawn retry per batch (a root-batch spawn failure otherwise cascades to all 28 dependents), statuses `merged | pr-opened | draft-pr-opened | failed | skipped`, a final reviewer over every opened PR. Each implementer: spec at `<wt>/.planning/specs/m7-<id>.md`, worktree `~/Projects/terum-skills-codex/<id>` on branch `codex/m7-<id>`, `codex exec` (gpt-6-astra high, workspace-write, stdin prompt), gates (root battery; desktop battery incl. Playwright routes + 87-board fidelity with `TERUM_DESIGN_DIR`; `cargo check` for src-tauri; the REAL-DATA PROOF: rebuild the CLI, run `review/2026-09-08-desktop-blank-map/fixture.sh` against it, record `--frames <verb>` output, replay it through the adapter read model in a vitest asserting the served DTO carries the batch's fields), run record under `<wt>/.planning/codex-runs/m7-<id>/`, push with `-c credential.helper='!gh auth git-credential'`, `gh pr create`, `gh pr checks --watch`, `gh pr merge --merge`.
- **Delivery without Ryan**: main has no branch protection; teniroo has push. A machine build = bump `package.json` on main, dispatch `release.yml` with `dry_run: true`, download the `desktop-<suffix>` artifacts. The public `npx` release still needs the `npm` environment's only reviewer (ryanliu-terum). The `app` verb installs only from a GitHub Release; a bundle placed at `~/.terum/skills/app/<version>/` with `installed.json` skips the download (`app.ts:96`).

## Progress
**DONE**
- M7 document: `~/Projects/SSM/terum-skills/.planning/research/2026-09-08-m7-close-the-canvas-gaps.md` (95k words, 33 batch sections, 62/62 SIMPLE CLI rows placed once, 26 AD rows, 12 BM rows, section 10.1-10.8 routing + Appendix A row index + Appendix B demand list; 0 em-dashes outside section 10). Mirror + every input under `~/Projects/SSM/review/2026-09-08-desktop-blank-map/m7-routing/`.
- Decision ledger, memory files (`desktop-real-data-only`, `terum-skills-merge-and-release-authority`, `workflow-authoring-lessons` +2, `terum-skills-desktop-fold-in` updated), routing section 10.8 (connected surfaces), queue script with merge + real-data proof + spawn retry, all simulated clean (30 run, 0 dependency violations, retry path exercised).
- Codex quota probe: LAUNCH (6.0% weekly, refreshed 18:0x UTC). `gh api user` works; `gh auth status` self-check times out (harmless).
**IN FLIGHT**
- Implementation queue run `wf_b0d0920d-4ef` (task `wsdh0gscz`, monitor `bglu9f6vo`), launched 19:57 UTC after `/login`; S7ag's implementer started 19:58 UTC, S7af's expected alongside. Two earlier launches died: `wf_f236f6e0-640` (S7af spawn failed with no reason; cleaned up) and `wf_f68cb20e-e6c` (all six implementer attempts hit "You've hit your session limit · resets 3:50pm America/Los_Angeles"; Teddy ran `/login`).
**NOT STARTED**: e2e review legs; Teddy's bug findings; design-strings closure; the 11 new boards; the design-walk list (Connect relabel, TZ avatar/sidebar literals, CP-19's three strings, window floor, CP-03 strings, BootError rows).

## Dead Ends
- **Workflow synthesizer re-letters batches** even with an explicit "ids are FIXED, never re-letter" clause in both synth and revise prompts (two runs, both wrong). Do not re-run the M7 synthesis; the generator is the source of truth.
- **`Workflow({resumeFromRunId})` after `/clear`** reads only the CURRENT session's `subagents/workflows/<runId>/journal.jsonl`; after a clear that file does not exist and the resume silently re-runs everything from the extractors. Fix: `cat` the older sessions' journals for that runId into the new session's journal path first, then resume. Verify within a minute by checking the newest agent transcript's prompt size.
- **Spawn failures are journaled as `{"type":"failed","agentId":""}` with no reason.** The queue now retries three times; a session-limit death shows as `[implement:X] failed: You've hit your session limit · resets …` in the task notification's `<failures>` block and needs `/login` before relaunch.
- **The auto-mode classifier** intermittently times out ("claude-sonnet-5[1m] is temporarily unavailable"), blocking Bash; retry after a moment. It blocked one Bash heredoc that mentioned merge/release commands; use Write/Edit for such text. It did NOT block the queue's `Workflow(...)` call this time (three launches went through).
- **StructuredOutput `maxLength` caps** killed 16/37 confirm agents in the routing pass; never cap digest strings.

## Key Files
Scratchpad S = `/tmp/claude-1000/-home-teniroo-Projects-SSM/7f851f32-e149-4a13-9205-90f176fdcb4a/scratchpad`, SC = `/tmp/claude-1000/-home-teniroo-Projects-SSM/667cfcd7-cc2c-4c6e-8fc8-866a9b55b649/scratchpad`; everything mirrored under `~/Projects/SSM/review/2026-09-08-desktop-blank-map/m7-routing/` (R).
- `S/m7-implement-queue-v2.js` (also R): the live queue script. Must be Read with the Read tool in a new session before `Workflow({scriptPath})` accepts it.
- `SC/queue-args-final.json` (also R): the EXACT `args` the live run was launched with; pass it verbatim on any resume/relaunch so unchanged agents replay from cache.
- `S/batches-slim-routed.json`, `S/routed-batches.json`, `S/apply-routing.py` (regenerates both), `S/routing-section.md` (section 10 + Appendix B), `S/ad-rows.md`.
- `SC/build-m7-doc.py` (also R): regenerates the document; inputs `SC/synth-v2-pinned.json`, `SC/critic.json`, `SC/rows-catalogue.json`.
- `R/decision-ledger-2026-09-08.md`: every Teddy answer and Claude call with its consequence.
- `~/Projects/SSM/review/2026-09-08-desktop-blank-map/REPORT.md` + `fixture.sh`: the blank-map review and the offline fixture team the real-data proof uses.
- Repo `~/Projects/SSM/terum-skills` @ f8557c4 = origin/main; worktrees under `~/Projects/terum-skills-codex/<batchId>`.

## Current State
**Baseline:** branch `main` @ HEAD `f8557c4`, snapshot `2026-09-08 19:59 UTC`. `git status` shows 7 untracked `.planning/` files (six research/decision files + the new M7 document); nothing modified. No local `codex/m7-*` branches, no worktrees (the stale S7ag one from the killed run was removed at 19:59 UTC). No remote `codex/m7-*` branches and no open PRs as of 19:56 UTC.
- Background: queue run `wf_b0d0920d-4ef` (task `wsdh0gscz`) live; monitor `bglu9f6vo` emits one line per batch result/failure and "QUEUE DONE" on completion. Expect S7af and S7ag to take 1-2 h each (desktop battery), then S7f.
- Session limits have killed runs three times today (resets at 06:20, 11:20, 15:50 PT). If the task notification shows session-limit failures: `/login`, remove any empty worktree the dead implementer left (`git worktree remove --force ~/Projects/terum-skills-codex/<id>; git branch -D codex/m7-<id>` ONLY if `git -C <wt> status --porcelain` is empty and there is no run record), then relaunch with `Workflow({scriptPath: S/m7-implement-queue-v2.js, args: <SC/queue-args-final.json>})` (a fresh run; or `resumeFromRunId: 'wf_b0d0920d-4ef'` if merged batches exist and the journal is in this session's dir).

## Gotchas
- **A worktree that already exists makes the implementer's `git worktree add` fail**; always clear leftovers before a relaunch. A non-empty leftover (spec, run record, diff) is real work: inspect before deleting.
- **Implementers merge**: a merged batch changes origin/main under later batches; that is intended. Dependents merge the dependency's BRANCH only while it is unmerged.
- **S7k must put RM-41/RM-48/RM-S1 on `TeamStatus`**, not `StatusResult` (critic refuted the synthesis); its PR body must say every status payload row lights only on healthy clones until AD-01 (S7af) is merged.
- **Inbox stays hidden until S7r serves IB-01** from real data; do not "fix" a missing Inbox nav group on the real adapter.
- **`FRAME_FEATURES.roles` is ambiguous** (PF-03 label vs TJ-02 permission role); S7b decides from the seam and records it.
- The `.planning/` tree is NOT gitignored, only untracked: never `git add -A` in the main checkout.
- `gh pr edit --body-file` fails on this repo (Projects-classic GraphQL deprecation); use `gh api -X PATCH repos/ryanliu-terum/terum-skills/pulls/<n> --input body.json`.
- The robust-gate stop hook wants a passing runner after any script edit: `node --check` on the queue script needs the meta block stripped and the body wrapped in an async function (see `SC/queue-check-wrapped.js` recipe); `python3 -m py_compile` for the generator.

## Next Step
1. Watch the queue (monitor `bglu9f6vo`); report each batch's status and PR/merge to Teddy as it lands; on a `failed` batch read `<wt>/.planning/codex-runs/m7-<id>/NOTES.md` and the journal result before deciding to relaunch that batch with `args.only: ['<id>']`.
2. Once S7af AND S7f are merged: run the headless e2e frames leg here (fixture team via `review/2026-09-08-desktop-blank-map/fixture.sh` against a fresh build of origin/main; every read verb over `--frames`; never `sync --hook`, never `--help`), and record the served DTOs; then the native leg on Teddy's Windows ARM host after his `gh auth login` there (`npx -y terum-skills@latest app` needs a release; for a pre-release build, bump `package.json`, dispatch `release.yml` with `dry_run: true`, download the `desktop-arm64` artifact).
3. If the run dies (session limit): `/login`, clear empty leftovers, relaunch with the saved args (Current State), seeding this session's journal into the new session dir first if `/clear` happened.
4. When the queue completes: read the reviewer's findings, fix blockers via `args.only`, then hand Teddy the merged list, the two drafts (S7t needs his written yes on PF-02; S7ab waits on Ryan), and the design-walk list.
