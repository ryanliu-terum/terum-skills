## 10. Desktop blank map routed into M7 (2026-09-08)

Source: the desktop blank-map review of 2026-09-08 (REPORT.md, 29 claims verified two ways). The review folder lives outside this repo, so everything M7 needs from it is copied here: the demand list (its section 5.1) verbatim in Appendix B, the two verified bugs with a CLI half (its section 4, rows 1 and 13) as rows, and the batch order that lights the desktop soonest. Every row below was re-confirmed against `src/commands/*.ts` at f8557c4 (v0.1.6) and against the measured frames before it became a batch row: one confirm agent per row, two adversarial refuters on every new, moved or divergent row, one ordering pass. Where a refuter overturned a claim, only the corrected fact appears. "Before" shapes are the last line of the named frames file, real output of the built 0.1.6 CLI against the fixture team.

Three facts govern every row here and are stated once:

- **Frame mode is the only delivery.** The app reaches the CLI as a child process (`desktop/src-tauri/src/lib.rs:39-49`) and reads the verb's result frame `value` (`src/lib/frames.ts:24`, `:171-175`). An exported function, a `ConfigStore` read or a library import delivers nothing. Every costing verdict of the form "the app reads it itself" is void, which is why several costed rows below gain a reader line and why the nine uncatalogued asks exist at all.
- **The adapter drops `value` on a failing result** (`desktop/src/backend/tauri/run.ts:72-74`), and `status` returns failure-with-value whenever any configured team is unreadable (`src/commands/status.ts:100`). Every `status` payload row in this section therefore reaches the app only while every team's clone is healthy. That one-line adapter change ("carry `value` beside `error` on ok:false") is not an M7 batch (section 10.5) but is the first item of the adapter track, and every S7k PR body must say so.
- **Additive fields keep `hello.protocol` at 1** (`docs/frame-protocol.md:61-63`). A new verb must be added to `FRAME_VERBS` (`src/lib/frames.ts:32`) plus an `INVOCATIONS` entry and a stub in `src/__tests__/frames-cli.test.ts` (:23-26, :51), or the hello frame under-advertises it (as it already does for `app`).

### 10.1 Rows that already sit in a batch (confirmed at f8557c4)

Every costed id in the demand list already has a batch. The table records what the confirmation found and what changes as a result; "same" means the report's ask equals the costed delta, "subset" that the costing delivers more, "diverges" that frame mode reshapes the delivery.

| row | ask (verb) | batch | ask vs costing | verified change at f8557c4 | consequence |
|---|---|---|---|---|---|
| RM-30 | `description` on `LsSkill` and `SearchHit` (`ls`, `search`) | S7f | same | `ls.ts:18` and `:60` (`parsed.data.description`, parsed at `:53`); `search.ts:16` and `:58` (`skill.frontmatter.description`, matched at `:42`); zod already declares it (`schema.ts:112`). Before: seven-key rows in `frames/ls.jsonl`, nine-key hits in `frames/search.jsonl`, no description. | none; anchor of S7f. `search.ts:60` calls the exported `format` with a seven-key literal, so every REQUIRED key S7f adds to `LsSkill` extends that literal once. |
| RM-44 | structured `placement {id, team, version}` and `shared {id, team}[]` on `ls --local` rows | S7g | subset | `ls.ts:19` row type, `:135` literal from `entry.shared` / `entry.placement` (`local-skills.ts:8-9`), full 40-char version. Printed sentence at `:139` unchanged. Before: `frames/ls-local.jsonl` rows carry `state` prose only. | ship the subset in S7g; the costed `sharedState` producer (a refactor of the mutating `reconcileShared`, `connect.ts`) is a separate Ryan PR if the Sharing state chip must light. |
| RM-39 | `health` on `ls --local` rows | S7g | same | `local-skills.ts:9` gains `fingerprint` (in hand at `:86-88`); `ls.ts:19` gains `health?`; snapshots at `ls.ts:93/:118/:125` gain the record directories; a try/catch to `unknown` is load-bearing (a throw blanks the whole listing). | none. Tell Ryan the one semantic: for a PINNED placement, `health` means "the team's tree moved", which differs from sync's "your pinned tree moved". |
| RM-40 | `tracked: boolean` on the row | S7g | same | two lines, `ls.ts:19` and `:135`; the boolean is the const at `:131`. | none; the only optional row in S7g (RM-44 makes it derivable). Keep it. |
| RM-48 | `syncedAt` on `TeamStatus` | S7k | same | `hook.ts` gains `stampedAt()` beside `stampPath` (`:157`), mtime verbatim, no skew clamp; `status.ts:21` member, initialised null at `:38-41`, set first inside the per-team try (`:44`) so a broken clone still reports it. | none. |
| RM-S1 | `policy {publish, skill_license}` on `TeamStatus` | S7k | same | `status.ts:21`, `:40` null, `:70-71` from the `readTeam` already awaited. | none. |
| RM-22 | gh/git/cc presence on `status` | S7d, MOVED to S7k | diverges | the costed shape (an exported `gitState()` beside `ghState`, `auth.ts:21`) delivers nothing over frames; the frame-mode form is a `tools` member on `StatusResult`. NARROWED to presence: `tools: { git: { installed, version }, gh: { installed, version } }` from `git --version` / `gh --version` exit codes and stdout, computed before the try so it rides every return (`:100`, `:101`). NOT `ghState()`: `auth.ts:28` chains into `gh auth status`, a host round-trip, while §6 (`spec:347`) and §12 (`:498`) define `status` as offline with no host access; the SIGNED-OUT/SIGNED-IN three-state is a separate MEDIUM for Ryan. The `claude` third stays out (AGENTS.md:31 "shell out only to git and gh"; Ajay's file). | S7d loses RM-22 and no edge changes. RM-22 is untruthful under a GUI launch until BM-11 lands (bare `git`/`gh` spawns read the launcher's PATH), which is why S7ad precedes S7k in 10.4. RM-27 (S7q) is void as costed ("the app runs the probes itself"); its gh version string rides `tools.gh.version` here and S7q records that instead of re-costing. |
| RM-08 | `update` returns a report instead of void | S7e | diverges | the costed "one `export` keyword" is void; the payload the costing demoted is the delivery: `UpdateReport { running, thisCopy, launch, description, probe, probeError?, advice, lines }` at `update.ts:11-12`, advice hoisted so the value is complete even when `:32` prints none; `Result<void>` becomes `Result<UpdateReport>` (`:12`, `:35`). Printed lines byte-identical. Before: the result frame has no `value` key at all. | none. `update.ts:19` forces the probe (`force: true` bypasses the 24 h gate at `update.ts:147`), so MC-12's gesture-only app rule ships with it: never on mount, focus or a timer. |
| MC-10 | hook state (and the drawn toggle) | S7l | subset | the read must ride the `hook` verb's own result: `Result<HookState>` with `{ installed, settingsFile, command, timeout, async, changed }` read from the file's real entry (a new exported reader beside `hookInstalled`, `hook.ts:57`, using the module-private `readSettings` `:52` and `matchingEntry` `:23`), never `HOOK_ENTRY`'s constants. Positional `hook [on|off]`, never a `--hook` spelling (`src/index.ts:60` refuses any frame-mode argv containing `--hook`). Register after the M2 comment at `cli.ts:122`; `CliVerbs` at `:34`, `active` at `:37`. | keep the read OFF `status` (it would blank in the broken-clone state and collide with S7k). S7l shares `src/lib/hook.ts` with S7k at disjoint hunks (`:57-61` vs `:157-168`); check the merge. Add `hook` to `FRAME_VERBS` plus the three `frames-cli.test.ts` edits; §8 delta reconciles `:405`, `:407` (the offer rule governs the OFFER) and `:437` (removal list gains `hook off`). |
| MC-11 | config identity on the status result | S7c (write half) | diverges | the costed row is the WRITE half only (`login --set`, `cli.ts:51-53`, `login.ts`) and stays in S7c. Its stated reason for having no read half ("the adapter reads config.json directly", costing:297) is void; the read half is uncovered by every costed row and is BM-10 below, in S7k. | S7c keeps only the write half. Land S7c with or after S7k, never before: `login --set` without BM-10 leaves Settings writing values it cannot read back. |
| RM-01 | the SKILL.md body (`ls`) | S7p, MOVED to S7f | diverges | the costing's `SkillRecord.body` and `skillAtSource` edits are dead under frame mode; the delivery is one OPTIONAL key `body?: string` on `LsSkill` (`ls.ts:18`, filled at `:60` from `parseSkillFrontmatter`, which gains `body: source.slice(match[0].length)` at `schema.ts:153/:161`). Optional is load-bearing: `search.ts:60` builds a seven-key `LsSkill` literal and never holds the raw text. Spec delta in the §6 `ls` bullet (`:345`), not §5.3. Test the boundary with no leading newline, CRLF, a file ending at `---`. | S7p shrinks to [TJ-10, RM-13] and gains a dep on S7f (RM-13 derives its paragraph from the body). Payload-size caveat for Ryan: `ls` is the list call and now ships every skill's prose on one line; HYG6's 20,000-char size check is a warning, so there is no ceiling. |
| RM-02 | `grants` and `grantsHash` on `LsSkill` and `SearchHit` | S7f | same | `ls.ts:18/:60` from `parsed.grants` (`schema.ts:161`); `search.ts:16/:58` from `skill.grants`; `null` only for a malformed `allowed-tools` (absent normalises to the literal `none`, `schema.ts:127-134`); `search.ts:60` literal extended. | none. Do not carry `describeRaw(grants.raw)` for the malformed case; the app says "malformed". |
| RM-03 | installer handles per skill | S7f | same | `readme.ts` gains `Installer` and `installersById(people)` beside `installCounts` (`:40-44`), one entry per install RECORD (not deduped, the D38 mirror), carrying `displayName` because `readRoster` omits archived people (`skills.ts:79`); `installedBy?` on `LsSkill`, OPTIONAL. | none. Archived people's installs stay listed (they are historical installs; TJ-13 owns the filtering question). |
| EV-07 | hygiene findings as data (`validate`) | S7m (Ajay) | subset | no `--json` flag: the result frame already carries the value on both outcomes (`execute.ts:32/:34`, proven by `frames/validate.jsonl`). `ValidateResult` gains `assessment: HygieneAssessment` (both buckets, `hygiene.ts:9`) at `validate.ts:12`, `:46`, `:50`; counts retained. | every drawn hygiene row is the FAILING case, so the CLI half lights nothing until the adapter carries `value` on ok:false; and `cliValidate` (`tauri/index.ts:29`) is a plain `z.object` that strips unknown keys, unlike `cliSearch`. Both are adapter-track items paired with S7m. |
| EV-13 | a read-only receipt reader | S7n (Ajay) | subset | a `receipt <ref>` verb (`src/commands/receipt.ts`, registered beside `eval` at `cli.ts:117`, added to `FRAME_VERBS`); the reader module must not be named `receipts.ts` beside the existing `receipt.ts` (use `receiptStore.ts` or fold it into the command); the receipt travels in the result VALUE, never as printed JSON; "no receipt for this version" is ok:true with `receipt: null` (the drawn SkillDetailNoReceipt board), failure only for an unknown skill or version; no `refreshClone`. | ship `--runs` (three lines over the same directory listing), which pre-pays EV-16's CLI half and leaves EV-16 display-permission only. |
| RM-49 | `unresolved` on `LsSkill` and per-row degradation | S7f | subset | the costing also enumerates through `skillRecords` (already imported, `ls.ts:8`) with `problems` on `LsResult`, fixes the roster fail-whole at `:33`, and reports a folder whose frontmatter name disagrees with its directory. RM-49 lands FIRST inside S7f; every other S7f key then reads off the record. | none. |
| RM-38 | a per-skill last-changed date | S7f | same | `readme.ts` gains `latestChange()` after `latestTree` (`git log -1 --format=%cI -- skills/<name>`), returning `—` on EMPTY stdout (git exits 0 for a path with no history, measured), per-row `.catch` at `ls.ts:59`; `updated` on `LsSkill` and `SearchHit`. | adds a second git child per skill to the loop BM-08 bounds. |
| IB-02 | one honest Inbox `share` row | S7n (Ajay) | diverges | the costed IB-02 has no CLI surface and assumed the app reads the clone and people files; under frame mode its inputs must ride verbs. The `share` kind needs the global set and my installed ids (both on `ls` after RM-03's `installedBy`) plus my `declined` ids, which is BM-04 (S7f, Ryan). My handle comes from `status` (`frames/status.jsonl`, `teams[0].handle`). | IB-02 stays in S7n for the §12 cross-version-regression paragraph and the `eval` kind; its `ls` input is BM-04; the derivation is app-side and gated on S7f (and S7g for `alert·local`, costing:635). |
| TJ-04 | host admin permission (`ls --host`) | S7h | same | `cli.ts:94` gains `--host` (threaded into the member/project forms at `:96-97`); `ls.ts:17/:20/:25-26`; roster entries gain `hostRole?: 'admin' | 'member' | 'unknown'` and `LsResult` gains one top-level `host?: { checkedAt } | { unavailable }`; a member whose `github` is `''` is `unknown`, never Member. | the degraded path MUST return success with `host.unavailable` (gh absent, logged out, 401, 403, generic-git, offline): a failure Result loses the whole roster at the adapter. |
| TJ-05 | pending invitations (`ls --host`) | S7h | same | the same flag; `invitations?: { login, invitedBy, invitedAt }[]` on `LsResult`, from the read `team remove` already makes (`team.ts:115-117`); absent means "not asked". | one PR with TJ-04, one §6 paragraph. Ryan: the costing omits the section on a 403; the frame-mode-correct rule is "any host-read failure", or Share goes blank on a gh hiccup. |
| PF-03 | a `role` field on the people file | S7b | diverges (superset) | the costed row is the write half (`role: z.string().max(32).default('')` after `schema.ts:61`, `profile --role`, the shared guard change `guard.ts:10/:36`); it names no reader, so the field would be committed and invisible forever. S7b adds the reader line itself: `role: person.role` on the `ls` roster literal (`ls.ts:34`, and optionally `RosterEntry` at `skills.ts:66/:79`), folded into the ONE §5.2 paragraph so condition (5) holds. | do NOT flip `FRAME_FEATURES.roles` (it names TJ-02's Admin/Member model, S7ab); add a new additive key (`memberRole: true`) and say in the delta that the discipline label is outside `roles`. Add `profile` to `FRAME_VERBS` with the `frames-cli.test.ts` edits. |
| PF-04 | project members on the people file | S7b | diverges (superset) | the costed write half (`projects: z.array(z.string().min(1)).default([])` at `schema.ts:63`, `profile --project`, validated against `team.json` inside the safeWrite mutate per `uninstall.ts:97-98`) plus the missing read half: `projects` on `RosterEntry` (`skills.ts:66`, pushed at `:79`), which rides `status`, `team join` and `setup` results. Both `Person` literals in `team.ts` (`:407-417`, `:526`) must gain `projects` or tsc fails; `skills.test.ts:95` is an exact `toEqual`. | flip `FRAME_FEATURES.projectMembers` to true in the same PR (advertisement only; the shell discards hello). |
| RM-32 | `--team` on `sync` | S7j | same | `cli.ts:129` option threaded into both `active.sync` shapes; `sync.ts:26` arg; refusals beside `--prune` and `--hook`; `selected` computed between `:129` and `:130` (not `:118`, `skipped` is not declared until `:128`); every other configured team joins the same `skipped` map the fresh/locked/busy paths use. | worthless without RM-31 (a scoped sync of an unreachable team returns a bare error string). |
| RM-31 | per-team outcomes on a failed interactive sync | S7j | subset | half shipped at 55ef7f0 (`TeamOutcome[]` on `SyncResult`, `sync.ts:34-39`); what remains is two edits: drop the value ternary at `sync.ts:353` and the SECOND ternary at `:359` only (the message ternary is the §6 contract, asserted at `sync.test.ts:858`). | RM-18's item (a) is this exact edit; build it once under RM-31 and strike (a) from RM-18. Inert until the adapter carries `value` on ok:false and stops mapping sync's `placed` to `[]` on both arms (`tauri/index.ts:78`): adapter-track items paired with S7j. |
| RM-17 | truthful boot rows: progress events on `sync` | S7r (app-only) | diverges | the costed SIMPLE·app design polls `config.json`, the clone and `run/<team>.stamp`; the shell has no filesystem (`src-tauri/capabilities/default.json` has no fs permission; `lib.rs:137` exposes five commands, `read_app_state` hard-wired to `run/app.json`), so it is unbuildable. The producer is a CLI change: an OPTIONAL `progress?(step, current?, total?)` Prompter member (`prompt.ts:25`), its frame implementation beside `print` (`frames.ts:163`, the reserved `ProgressFrame` at `:23`), four `sync.ts` call sites (after `:131` team found, after `:147` fetched, inside `:310`'s placement loop with `index + 1` of `candidates.length`, after `:348` stamped), `features.progress` flipped and the all-false invariant at `frames.test.ts:31` rewritten. Cancellation is CLOSED end to end (`frames.ts:126`, `drive.ts:14`, `cli_kill`). | MEDIUM, not SIMPLE: it breaks condition (5) twice (`docs/frame-protocol.md:18` and the Prompter enumeration at spec §3 `:53/:60`), and the bar's "new Prompter member" clause must be answered (optional, called by `sync` alone, no other Prompter changes). The producer becomes its own batch S7ae (decision D-BM-1); the consumer stays app-only in S7r, keyed on frames ARRIVING, never on `features.progress` (the adapter discards hello). |

### 10.2 New rows: the uncatalogued asks, costed against the bar

Each row was costed on the six SIMPLE conditions at f8557c4, then attacked by two refuters. The edits are paste-ready for the batch spec; "lights" names what turns on once the adapter reads the verb.

**BM-01, `projects` on `LsResult` (`ls`). SIMPLE, batch S7f, Ryan, §6 `ls` bullet (`spec:345`).** No verb enumerates `team.json.projects`; every reader takes the name as input (`ls.ts:76`, `search.ts:41`, `install.ts:57`, `publish.ts:56`, `uninstall.ts:40`, `sync.ts:421`), and `LsResult` (`ls.ts:20`) has no such key in any measured frame. Edit: `projects?: readonly { name: string; skills: readonly string[]; remotes: readonly string[] }[]` on `LsResult` (`remotes` is REQUIRED by `teamSchema`, `schema.ts:48`, so it is not optional); built right after the parse at `ls.ts:32` with a codepoint sort (`readme.ts:47` is the precedent; `localeCompare` is locale-dependent and would not keep frames byte-stable); spread the parsed entry's passthrough keys so TJ-10's hand-maintained `description` (S7p) is not stripped; threaded into `showMember`/`showProject` (typed as the element array, not `LsResult['projects']`, so the member and project forms never emit an `undefined` key) and returned at `:45/:73/:81`. `projects` is absent exactly on the `--local` path (`ls.ts:28` returns before `team.json` is parsed at `:32`): omit, never `[]`. Multi-team: `selectTeam` throws with no `--team` (`config.ts:29`), so the app passes `--team` per team. Tests in `ls.test.ts`: the registry in name order including a project with no skills; absent on `--local`. Lights: the sidebar project rows (`Sidebar.tsx:13`'s literals), `/library/project/:name`, the Marketplace Projects board; per-project counts are one app-side intersection with `LsSkill.id`, no fan-out. RM-26 stays CLOSED (it assumed this enumeration).

**BM-02, `clonePath` on `TeamStatus` (`status`). SIMPLE, S7k, Ryan, §6 `status` bullet (`spec:347`).** `status.ts:45` computes `const clone = store.teamClone(team)` and drops it. Edit: `clonePath: string | null` on `TeamStatus` (`:17-22`), `null` in the detail literal at `:38-41` (the literal is built before `teamClone` can throw on a bad config key, `config.ts:92`), assigned at `:45-46` BEFORE `describeClone` at `:47` so absent/foreign/incomplete clones carry it too. Never inline `teamClone` into the literal: a throw there escapes the per-team catch and kills every remaining team, contradicting `spec:347`. Map to the design's `TEAMS[].clone` (a string, `fixtures/schema.ts:60`), not the CLI's `clone` state object. Tests: the clone-state block at `status.test.ts:119-133` plus a healthy case; the strict `toEqual` at `:102` is the zero-team case. Lights: the clone-path row in Settings ▸ Teams; there is no per-team Show-in-Finder control at f8557c4 (`SettingsContent.tsx:36` renders the value only), so that button is app/design work (AC-08).

**BM-03, `joinCommand` and `joinBlock` on `TeamStatus` (`status`). SIMPLE, S7k, Ryan, §6 `status` bullet.** The block already exists and is exported: `joinCommand` at `invite.ts:54`, `slackBlock` at `:56-58`, printed only inside the GitHub-gated `invite` (`:45`); `setup.ts:18` already imports it. Edit: split `slackBlock` into an exported `joinLines(ownerRepo): readonly string[]` plus the joining wrapper (byte-preserving, invite's stdout unchanged); `joinCommand: string | null; joinBlock: readonly string[] | null` on `TeamStatus`, null in the literal, set after `detail.repository` at `:46` from `githubOwnerRepo(binding.remote)` (`remote.ts`), null on a generic-git remote. Tests: byte-identical to `slackBlock('acme/team')` for a GitHub team; null for generic git. Lights: ShareInvite's copyable block and the Members screen on the success path; the Onboarding join block only once the adapter carries `value` on ok:false (a missing clone makes `status` fail-with-value).

**BM-04, `declined` on `ls member`'s value (`ls`). SIMPLE, S7f, Ryan, §6 `ls` bullet.** REDUCED from the report's "installed list": `installed` per member is RM-03's `installedBy` inverted (one entry per install record, already in S7f), and shipping both projections of the same people-file rows in one batch is the duplicate the inventory already avoids (rows 12 and 13). What no verb returns is the member's `declined` ids (`schema.ts:63`), read at seven sites and returned nowhere. Edit: `member?: { handle: string; declined: Person['declined'] }` on `LsResult` (`ls.ts:20`; add `Person` as a type-only import at `:15`, there is no exported `Scope` type), returned at `:73` from the `member` already found at `:66`. The printed `Installed:` line at `:72` stays. Tests: the member fixture at `ls.test.ts:60-74` with a `declined` id; unknown handle still fails. Lights: with `status` (my handle) and plain `ls` (global set plus `installedBy`), the Inbox `share` row and Onboarding's "later" list; three spawns. Alternative for Ryan (D-BM-4): `declinedBy?: readonly string[]` on `LsSkill` on the default `ls`, filled beside `installCounts` at `:51`, so the share row costs one spawn.

**BM-05, `name` on `UninstalledResult` (`uninstall-skill`). SIMPLE, S7i, Ryan, §6 `uninstall-skill` bullet (`spec:353`) only.** The install half is CLOSED at f8557c4: `InstalledResult.path` (`install.ts:142`) is `join(root, skill.name)` (`placer.ts:52/:78`), so `basename(path)` IS the name, exactly, and the adapter's `name: item.id` (REPORT §4 row 10) is fixable app-side. `UninstalledResult` (`uninstall.ts:16`) is `{ id, team, removed }` with nothing name-bearing. Edit: `name: string | null`, derived inside `uninstallMany` from the matched placement-ledger keys it already reads at `:83` (each key was written as `join(root, skill.name)`), the last path segment of the first match, `null` when `matching` is empty; the literal at `:88`; zero new I/O, no clone read. Tests: `uninstall.test.ts` (the deleted-upstream fixture at `:94` exercises the null arm). Adapter note: `cliUninstalled` is passthrough but must DECLARE the key as optional and fall back to `item.id`, or an older CLI breaks the read.

**BM-06, a result frame on a commander usage error (the bin). SIMPLE, S7d, Ryan, `docs/frame-protocol.md` (the ordering sentence at `:11` and the `result` row at `:19`).** `src/index.ts:69-72` catches `CommanderError`, sets the exit code and writes no result frame; commander runs in `exitOverride` (`cli.ts:40`) so every usage error throws before any action, and the app reports "exited with code 1 before reporting a result" (measured for 20 argv shapes). Edit: in `frames.ts` beside `isDecline`, `COMMANDER_NON_ERRORS = new Set(['commander.help', 'commander.helpDisplayed', 'commander.version'])` (those already wrote their text; rule 3 forbids them over frames) and `attemptedVerb(operands)`: the longest leading non-flag prefix naming a `FRAME_VERBS` entry (two words for the `team` forms), else the first operand, else `terum-skills`, never empty; in the catch, when `channel` exists and the error is a `CommanderError` whose code is not in the set, `channel.result({ verb: attemptedVerb(argv.slice(2)), ok: false, error: error.message, exitCode: 1 })`. Do NOT guard on `channel.closed` (stdin is already closed when the adapter and the harness run) and prefer a `reported` flag over re-emitting. `isDecline` false-positive: an unknown option literally named `--declined` would be stamped `declined: true`; suppress the stamp for commander errors. Tests: `frames.test.ts` (`attemptedVerb` cases), `bin.test.ts` or `frames-cli.test.ts` (an unknown option under `--frames` ends in exactly one result frame, exit 1; `--help` and `--version` still emit none).

**BM-07, stop the `--frames` and `--hook` scans at the first `--` (the bin). SIMPLE, S7d, Ryan, `docs/frame-protocol.md:7` (the position-independence paragraph gains "before the first `--`").** `index.ts:42-43` scan and filter the whole argv for `FRAMES_FLAG` and `:60` scans it for `--hook`, so `--frames search -- --hook` returns a `verb: "sync"` refusal frame and an operand literally named `--frames` is eaten. Edit: `const separator = process.argv.indexOf('--')`; scan and filter only the prefix; recompute `argv.indexOf('--')` after the filter (the separator shifts left by the removed matches) and slice `argv` to it before the `--hook` test at `:60`. Rule 2 (`:35`) stays literally true. Tests in `bin.test.ts` (an unconfigured HOME makes `search` return `success([])`): `--frames search -- --hook` ends in a `search` result, `search -- --frames` likewise. Coupled to BM-06 in one commit: alone, `sync -- --hook` regresses from a wrong-verb frame to none (`sync` has no `allowExcessArguments`, `cli.ts:129`). Inert in the app today (nothing writes `--`); it is what makes the adapter's argv reorder (REPORT §3 step 5) safe.

**BM-08, bound `ls`'s per-skill git fan-out (`ls`). SIMPLE, S7f, Ryan, §6 `ls` bullet, RECONCILED with RM-10.** `ls.ts:52` runs an unbounded `Promise.all` with one `latestTree` child per skill (`:59`); `search.ts:51-52` batches eight. The report's "142 + 2.46N ms" motive does not hold as a latency claim (followup-2.md:74/104: the slope is per child, peak concurrency with a real git is already 2); the defensible framing is a documented cap matching `search`, bounded process pressure when git is slow. But RM-10 (S7d, "export the batched `skillTrees` reader", costing:793) names the identical defect at the identical lines and its mechanism, ONE `git ls-tree <ref>:skills` for every version (`teamRepo.ts:284-286`), strictly dominates a chunk of eight. Under frame mode RM-10's export has no consumer except this call site. Resolution: RM-10 MOVES from S7d to S7f; `listSkills` consumes `skillVersions()` once (its call-site half is BM-08); the chunk-of-eight is the fallback only if Ryan declines RM-10. Written INSIDE RM-49's rewrite of `ls.ts:49-62` as one commit, and the bound must cover RM-38's second git call per skill (`git log -1`), or peak concurrency is 16, not 8. Tests: a runner spy asserting the peak in flight (the first injected-Runner team-path test in `ls.test.ts`; the three existing injections are `--local` runs that never reach `listSkills`).

**BM-09, the machine ledger on `StatusResult` (`status`). SIMPLE, S7k, Ryan, §6 `status` bullet.** RM-06 was costed CLOSED because "config.placements is readable via ConfigStore", an in-process route frame mode retired; RM-07 (S7j) exports the private `approved()` predicate (`sync.ts:364`), equally unreachable. No measured frame carries `placements`, `approvals` or `shared`. Edit: `ledger: { placements: { path, id, team, version, scope, placed_at }[]; approvals: { id, grants, approved_at }[]; shared: { id, source, team }[] }` on `StatusResult` (`:23`), declared beside `teams` at `:28` so all three returns carry a well-formed value, filled right after the config read at `:32` and BEFORE the `--team` selection at `:33` (machine-wide, never narrowed); `fingerprint` omitted. Distinct from RM-44: `ls --local` is scoped to the discovered roots (global plus the cwd project), the machine-wide count Settings ▸ This machine draws lives only in `config.json`. Consequences: RM-07's CLI half is superseded (the app compares `approvals[id].grants` with RM-02's `grantsHash`), so RM-07 moves to S7k as this row's consumer and S7j loses it; RM-06 flips CLOSED to app-side over BM-09 (S7r). Tests in `status.test.ts` via the injected `ConfigStore`: full field set with the 40-char version and the project scope; empty ledger on `emptyConfig()`. Privacy: `config.json` is machine-local and the reader is the local app.

**BM-10, the identity block on `StatusResult` (`status`), MC-11's read half. SIMPLE, S7k, Ryan, §6 `status` bullet.** `configSchema` declares `default_handle`, `email`, `display_name`, `github` (`schema.ts:95-98`); `setIdentity` (`auth.ts:176-180`) is their only writer; `status` loads the whole config at `:32` and returns none of it. Edit: `identity: { default_handle, email, display_name, github } | null` (each `string | null`; `?? null` is mandatory because `JSON.stringify` drops `undefined` and the app could not tell "unset" from "older CLI"; the outer `null` means login never ran), declared ABOVE the try beside `teams` at `:28` because `config` is out of scope at the outer catch's return (`:101`), filled after `:32`, on both returns. Existing assertion to update: `status.test.ts:102` is a whole-result `toEqual`. Name the field `identity` in both documents (the inventory rows 25-28 say `me`). Lights: Settings ▸ Account's three fields and the footer login, which today write only `localStorage`; on a zero-team machine `status` still succeeds (`teams: []`), so this reaches the boards where the complaint is worst.

**BM-11, the launch PATH on `app.json` (`app`), bug row 1's CLI half. SIMPLE, S7ad (new), Ryan, spec §4.2 (`:103`).** `AppState` (`app.ts:58`) is exactly `{ schema, node, entry, version, writtenAt }`, written at `:134`; the shell hands the CLI the inherited environment (`lib.rs:47-49`) while `runner.ts` spawns bare `git` and `gh`, and with gh off PATH `invite` returns "GitHub CLI (gh) is not installed" (measured). Edit: `path: string | null` after `entry` (`process.env.PATH ?? null`, precedent `index.ts:37`; Node reads Windows `Path` case-insensitively; the string is `;`-delimited there and must be replayed verbatim), an `AppArgs.path` test knob; `app.json` is machine-local run state, never an owner file, and `app` stays OUT of `FRAME_VERBS` (its seam is `read_app_state`, not a frame). Tests: extend the pinned block at `app.test.ts:83-103`. Spec: §4.2 has no `run/app.json` row at all today (PR #58's undocumented state); one tree line plus one paragraph. Inert until the adapter half lands: `bridge.ts:13` is a plain `z.object` on zod 4.5.4, which STRIPS unknown keys (verified), so `path` is silently discarded; the two adapter lines (`bridge.ts:13` accepts it, `cli_spawn` sets `command.env("PATH", …)`) are the adapter track's item paired with S7ad, and the launchd half stays unverified (no Mac here).

**BM-12, the join target on `app.json` (`app`, `setup`), the demand list's tenth uncatalogued row. SIMPLE, S7ad, Ryan, §4.2, GATED (decision D-BM-3).** `setup` prints "Continuing in the app. Join `<target>` there." and returns with no team work done (`setup.ts:113-115`); `app.ts:139` opens the app with no argument and `app.json` carries no target. Edit: `target?: string` on `AppArgs` and `AppState`, written at `:134` only when set (a later plain `app` run clears it, `writeState` renames over the file); `setup.ts:111` passes `target: args.target`. Drop the report's argv half: `open [appPath]` ignores trailing arguments without `--args`, and the webview cannot read argv anyway (no sixth Rust command). Consume-once rule in the same §4.2 paragraph: `app.json` persists and is read once per session (`tauri/index.ts:37-38`), a Dock relaunch a week later does not re-run `app`, so the shell must remember the consumed `writtenAt` in its prefs or every launch re-routes a joined user into the wizard; make `writtenAt` required in the bridge schema. Also spec debt this row must NOT absorb: §6.1 step 8 (`:388`) still says the app hand-off replaces the last line of the wizard, while the shipped code hands off before the role question (`setup.ts:107-118`); that belongs to the `app` verb's missing §6 bullet. Lights nothing today: `/onboarding` is unreachable and `backend.onboarding()` is a gap (`index.ts:63`), no screen calls `backend.setup`; it is the gating half of the joiner path (CP-21's completion-intent slot in S7r), and today's opt-in joiner is stranded.

### 10.3 The two bugs with a CLI half (REPORT.md section 4)

- **Row 1 (blocker), the GUI-launched app hands the CLI the launcher's environment.** CLI half is BM-11 above (S7ad). The adapter halves (`bridge.ts:13`, `cli_spawn` setting PATH) and the launchd verification are adapter-track work paired with S7ad; the CLI half is inert without them.
- **Row 13, a usage error emits no result frame and the argv scans ignore `--`.** CLI half is BM-06 plus BM-07 above (S7d, one commit). The adapter's own half (reorder every builder to verb, flags, `--`, positionals) is adapter-track work that BM-07 makes safe.

The other thirteen rows of section 4 (2 to 12, 14, 15) are adapter or shell work and are not M7 rows (section 10.5).

### 10.4 Batch order (the desktop lights soonest)

Priority order for the queue; a batch never precedes one it depends on, and the queue starts the first ready batches in this order with at most two in flight. Teddy's four pinned asks resolve to three batches (RM-30 and BM-01 share S7f); S7ad is hoisted to third because RM-22's tool row is untruthful under a GUI launch until BM-11 lands.

| # | batch | rows (after routing) | dependsOn | what lights when it lands (given the adapter reads the verb) |
|---|---|---|---|---|
| 1 | S7f | RM-49, RM-10, BM-08, RM-30, RM-02, RM-03, RM-38, RM-01, BM-01, BM-04 | none | Library cards: description, broken flag, "Recently updated"; Skill detail: description, SKILL.md tab, grants (retires the pre-answered consent at `SkillScreen.tsx:39`), users facepile; the sidebar project rows and `/library/project/:name`; Marketplace descriptions and "0 projects" dies; Search's `description` kills the author-email leak; the Inbox `share` inputs. |
| 2 | S7g | RM-39, RM-40, RM-44 | S7f | Settings ▸ This machine's placements table stops being prose; Library card health flags; `counts.Global` becomes trustworthy. |
| 3 | S7ad | BM-11, BM-12 (gated) | none | no pixel; two blockers die once the adapter halves land: every git/gh verb on a GUI-launched Mac, and the joiner hand-off has a carrier. |
| 4 | S7k | RM-41, RM-48, RM-S1, TJ-12, BM-02, BM-03, BM-09, BM-10, RM-22, RM-07 (consumer) | none | Footer identity and last sync; Settings ▸ Teams (policy, pending, clone path, categories), ▸ Sync, ▸ This machine's approvals and machine-wide ledger, ▸ Account's tool rows; the join block. Caveat: healthy clones only until the adapter carries `value` on ok:false. |
| 5 | S7d | RM-09, CP-35, BM-06, BM-07 | none | every long verb becomes diagnosable: usage errors end in a result frame, `--` is safe, declines are typed (four "cancelled" sites re-worded). |
| 6 | S7c | CP-D1, MC-11 (write half) | none | Settings ▸ Account writes reach `config.json` and read back through BM-10. |
| 7 | S7b | PF-03, PF-04, PF-07, PF-09 (+ reader lines, `profile` in `FRAME_VERBS`, `memberRole` key, `projectMembers` flip) | S7c | Share's role cell and Teams column; Marketplace people cards. |
| 8 | S7e | RM-08, MC-12, RM-29, CP-40, CP-37, CP-24, CP-39 | none | Settings ▸ Updates advice and `CLI_LATEST`. |
| 9 | S7h | TJ-04, TJ-05 | S7g, S7c, S7b | Share's admin column and "N invitations". |
| 10 | S7i | RM-19, RM-21, BM-05 | none | Marketplace member/project installs report per item with names; Windows home. |
| 11 | S7j | RM-31, RM-32, CP-S3, RM-18 (minus (a)) | S7f, S7i | per-team Sync buttons and outcomes (inert until the adapter maps `SyncResult`). |
| 12 | S7ae (new, MEDIUM, DEFERRED until D-BM-1; the queue skips it) | RM-17 producer | S7j | the Onboarding boot card's phases, once S7r's consumer renders arriving progress frames. |
| 13 | S7l | MC-10 | S7k (ordering only; disjoint `hook.ts` hunks) | Settings ▸ Sync's hook row and toggle. |
| 14 to 28 | S7m, S7n, S7o, S7p (now TJ-10, RM-13; dep + S7f), S7q, S7r (+ RM-06 re-opened, dep + S7k), S7s, S7t, S7u, S7v, S7w, S7x, S7y, S7z, S7aa, S7ab | unchanged | unchanged. S7q, S7r, S7w and S7x carry no CLI code and no approver: they are the natural second lane while the Ryan chain runs, and S7r holds every consumer these CLI rows need (the four `COUNTS` literals, the footer, the `ChangeSource` consumer). |

Skipped by the queue: S7a (MOOT, the exports map), S7ac (DONE, the `app` verb shipped as c487048 and PR #58; BM-12 is its named residue, not a reopening) and S7ae (DEFERRED until D-BM-1 is answered). Gated batches (S7ab, S7t) open draft PRs.

### 10.5 Excluded from M7, and where each goes instead

- SUPERSEDED BY 10.8 (Teddy, 2026-09-08 17:40 UTC: the adapter halves are M7 rows now). Kept for the record: the adapter work in `desktop/src/backend/tauri` (the review's basic set, steps 1 to 4: implement `status()`, the query lifecycle, `library()` with its pixel-neutral screen half, `skill()`; the adapter halves of steps 5, 6, 7, 12 and 13; and bugs 2 to 12, 14 and 15 of its section 4) goes to the adapter track: a spec of its own under `desktop/` (Codex batches in the desktop tree, gated by the desktop battery, `desktop/AGENTS.md` invariants binding), sequenced after the end-to-end review's CLI leg. Its first three items are the ones every payload row above depends on: carry `value` beside `error` on an ok:false result frame (`run.ts:72-74`), add `path` and `target` to `appStateSchema` (`bridge.ts:13`) with `cli_spawn` setting PATH, and declare the new keys on the closed zod objects (`cliValidate`, `cliInstalled`, `cliUninstalled`) instead of relying on passthrough.
- PARTLY SUPERSEDED BY 10.8 (the four `COUNTS` literals are AD-03; the rest stands). The React screen fixes listed with those steps (the four hard-coded sidebar `COUNTS` literals at `InboxScreen.tsx:67`, `ShareScreen.tsx:32`, `LibraryScreen.tsx:18`, `MarketplaceScreen.tsx:66`; the Skill error board's fabricated crumbs and diagnosis at `SkillScreen.tsx:41` and the `actionError`/load-error split at `:33`; the "passed on share, 12 days ago" caption at `:26`; `Analytics.tsx:10`'s provenance default; the `reports.tsx` literals; `InboxScreen.tsx:21`'s recipient; the `white-space:nowrap` `ErrorLine`; gating the disable Switch on `disablePerMachine`) go to the shell-hygiene track: pixel-neutral edits only, bundled with the design-strings closure (D16, strings first). Anything among them that moves pixels on a locked board goes through the design walk instead (canvas patch, re-render, Codex spec, re-lock), never through an M7 PR.
- The two design calls stay with the design walk: (a) the `TZ` avatar and the `Terum / SSM / MRF` sidebar literals are the canvas's own (`build.py:247-249`, `:265`) on 75 of the 87 locked boards, so removing them is a canvas change plus a re-lock, Teddy's call; (b) the Onboarding `BootError` board's invented rows (`OnboardingScreen.tsx:79,83`) need an absent state that is not drawn (D9/D10 board work with Teddy) before the screen logic can stop inventing them.
- **Teddy's rule, 2026-09-08 (shell-hygiene track, first item, before the native e2e leg): the real app shows only real data; a surface whose read model is a gap on the real adapter is hidden, not stubbed, and the Inbox is the first case.** Today `desktop/src/components/domain/Sidebar.tsx:14` draws the Inbox group (Inbox, Pushes, Updates, Alerts) unconditionally, while every real-adapter read model, `inbox()` included, returns the typed gap at `desktop/src/backend/tauri/index.ts:32` (`ok:false`, "not available from terum-skills yet"). Mechanism: the adapter declares which read models it serves (a `surfaces` block beside `Capabilities`, `types.ts:8`, or a per-model probe), and the shell, which already queries `capabilities` at `Shell.tsx:15`, omits a nav group whose model is a gap; the mock keeps every surface, so the 87 locked boards do not move. Consequence: a nav entry reappears on the real adapter only when the adapter track maps that model, never from a CLI batch alone (S7f lights Library and Skill detail, S7k Settings and the footer, S7n/S7o/S7m the Inbox's derivable rows; the Inbox feed itself, IB-01, stays app-only in S7r). Not an M7 row.
- Batch S7a (the exports map, RM-11) stays retired by Ryan's frame-mode decision (80054f4): the app reaches the CLI as a child process over frames, and CP-19's inventory rides the hello frame's verbs and features.

### 10.6 Decisions this routing adds (for Teddy, with the lean)

- **D-BM-1, RM-17.** (A) drop the mid-flight phases: the boot card shows an indeterminate state and the truthful end state from `SyncResult`, zero cost, the drawn "4 of 7" is deleted or stays fabricated; or (B) ship the producer as the MEDIUM batch S7ae and ask Ryan to waive condition (5), since `docs/frame-protocol.md:63` already pre-authorised a verb starting to emit `progress` as additive and the §3 edit is one clause adding an optional member. Lean: B.
- **D-BM-2, RM-22's third state.** Presence only now (`--version` exit codes, offline, one §6 clause), with the SIGNED-OUT/SIGNED-IN state re-filed as a MEDIUM that changes `status`'s defining property (`gh auth status` is host access; §6 `:347`, §12 `:498`). Lean: presence now; the three-state is Ryan's to reopen.
- **D-BM-3, BM-12.** Land the target field with BM-11 in S7ad now (4 CLI lines, the §4.2 paragraph that also documents `app.json` for the first time, the consume-once rule), accepting it is inert until S7r's onboarding route consumes it; or freeze `app.json` until the route exists, in which case `setup.ts:113-115` should stop offering the app to a joiner at all, because today's yes strands them. Lean: land it now, gated in the S7ad spec on this answer.
- **D-BM-4, BM-04's shape.** `declined` on `ls member` (spec-conformant, the Inbox `share` row costs three spawns) or `declinedBy?` on `LsSkill` on the default `ls` (one spawn). Lean: `ls member`; Ryan may prefer the other.
- **D-BM-5, S7k's size.** One PR with ten rows on one file and one §6 paragraph, or two PRs (per-team members; machine-level `identity`, `ledger`, `tools`). Lean: one PR; every row edits `status.ts:17-23` and the same test helper, and a split doubles Ryan's reviews of the same lines.
- **D-BM-6, RM-44's scope.** The subset (two members, three lines) in S7g now, the `sharedState` producer that refactors the mutating `reconcileShared` as a later Ryan PR for the state chip. Lean: subset now.
- **D-BM-7, EV-13's `--runs`.** Ship it (three lines over the same listing, EV-16's CLI half pre-paid) or the latest receipt only. Lean: ship it.

### 10.7 Corrections from the second refutation pass (binding on the batch specs)

Thirteen rows lost their refute stage to a schema cap in the first pass and were refuted afterwards (26 agents, none failed). Anchor slips were fixed in place above; these are the material corrections an implementer must apply.

- **RM-39.** Gate the placed-copy snapshot on the entry's inspection: a top-level symlink at a ledger path is still a row (`local-skills.ts:88-94`, `ls.ts:131-135`) and `snapshotSkillDirectory` would read THROUGH the link, which `spec:345` forbids; return no health for `inspection.kind === 'rejected'` (or mirror sync's `inspect(path, true)` gate at `sync.ts:250`). Drop `placement-missing` from the enum: a ledger placement whose folder is gone produces no row at all (`local-skills.ts:74`), so it is a MISSING ROW the S7g paragraph must name, not a health value. Split "the skill id is no longer in the clone" (`gone-from-repo`, from `snapshot.ids` at `ls.ts:125`) from `unknown` (unreadable clone). Use `spec:355`'s own vocabulary: `up-to-date`, not `ok`. `ls.test.ts:209` already pins "no git on `--local`"; do not write a runner-injection test for `showLocal` (it never receives a runner).
- **RM-08.** `src/__tests__/cli.test.ts:266` stubs `update` as `success(undefined)` under `CliVerbs`, so it must return an `UpdateReport`; wire the adapter through `read()` with a `.passthrough()` schema replacing the gap at `tauri/index.ts:84-85` (adapter track); CP-40 (same batch) deletes `COMMUNITY_URL` in `constants.ts` while `update.ts:7` keeps importing `RELEASE_PROBE_POLICY` from it.
- **RM-02.** The `allowed-tools` key is `schema.ts:115`; search's all-teams-failed gate is `search.ts:76`; per-row degradation is `skills.ts:34-36`; the malformed-grant shape is asserted at `schema.test.ts:25`.
- **EV-07.** The seam `ValidateResult` (`desktop/src/backend/types.ts:53`) is implemented by both backends, so the mock (`mock/index.ts:65`, pinned by a strict `toEqual` at `mock.test.ts:30`) changes too (adapter track). Warnings-only skills DO succeed (`validate.ts:50`, `hygiene.ts:95` HYG6), so the CLI half lights the warn row on ok:true; the error rows still wait for the adapter's ok:false value carry, so S7m depends on that adapter item. `validate.test.ts` is 55 lines: test (a) extends `:13-23` inside the describe, test (b) the top-level `it.each` at `:39-55`. Declare `let assessment: HygieneAssessment` at `validate.ts:39`.
- **TJ-05 (with TJ-04).** Register `--host` ONCE on the `ls` chain with TJ-04's wording and no default value (`cli.test.ts:44/:124/:125` assert the forwarded args by exact equality); export `paginatedItems` (`team.ts:147`, module-private today); place the invitations read AFTER the member/project early returns at `ls.ts:38-39`; update the docblock at `ls.ts:22`. The costing already enumerates the host-failure cases; the change is one never-throw rule covering all of them.
- **RM-31.** `sync.test.ts:859` asserts `expect(result.value).toBeUndefined()` on exactly the path that gains a value; invert it to a `toMatchObject({ hook: false, teams: expect.any(Array) })`. Locals are `placed :91`, `changed :92`, `teams :93`; the stamp loop is `:335-350` with `writeStamp` at `:348`; `execute.ts` gate at `:28`, `isHookSync` at `:49-50`; `unreachable` is pushed at `sync.ts:151`.
- **BM-03.** One file, not two: `detail.joinBlock = slackBlock(ownerRepo).split('\n')` round-trips exactly (every element of the array at `invite.ts:57` is newline-free), so no `joinLines` refactor and no overlap with S7o's `invite.ts` edits. Amend ONLY the §6 `status` bullet (`:347`); §12 (`:498`) is a separate top-level section and touching it breaks condition (5). The fixed-portable-forms rule is `spec:29`, §3. The unreadable guard is `status.ts:98`.
- **BM-04.** RM-49 (same batch) makes `problems` a REQUIRED member of `LsResult` and rewrites all four success sites including `:73`, so BM-04 is an ADDITION to whatever literal `:73` holds after RM-49, never a replacement. After RM-49 tolerates a bad people file, `ls member <handle>` for that person hits "No member named" (`ls.ts:67`); the merged S7f paragraph must say the roster's `problems` entry is the tell. `selectTeam` throws, it never prompts (`config.ts:23-29`). `Person` is `schema.ts:65`; `installedSchema` closes at `:41`.
- **BM-06.** Discriminate on the exit code, not on a code set: commander 15.0.0 raises code `commander.help` with `exitCode 1` for a bare `terum-skills` and a bare `team` (probed against the repo's `node_modules/commander`), so emit the frame for every `CommanderError` with `exitCode !== 0` (and every non-commander throw), suppress only `exitCode === 0`. For `commander.help` the message is the literal placeholder `(outputHelp)` (`command.js:2647`); substitute a written one-liner. The adapter accepts an empty `verb` (`tauri/frames.ts:18,:48`), so `attemptedVerb`'s fallback is a quality choice. Add `['--frames']` and `['--frames','team']` to the bin tests. `attemptedVerb` breaks at the first dash, so `--frames -- search -x` reports `terum-skills`; that is BM-07's half, out of this row.
- **BM-07.** A `--frames` that appears only after `--` no longer enters frame mode (`sync -- --frames` falls to the plain branch and raises `commander.excessArguments`): state it in the `docs/frame-protocol.md:7` paragraph and test it (`[bin,'search','--','--frames']` yields plain text, no frames). The filter drops EVERY pre-`--` occurrence (safer than followup-1's "first match only"); say so. Costing drift: `index.ts:38-40` is now `:46-48`, `:43` is `:51`, `:37` is `:45`. The `--hook` scan lives inside `if (channel)` (`:58-60`), so plain `sync --hook` and §8 are untouched.
- **BM-09.** Two existing assertions change: `status.test.ts:102` (whole-result `toEqual`, add `ledger: { placements: [], approvals: [], shared: [] }`) and the `status` stub at `src/__tests__/cli.test.ts:21` (typed by `CliVerbs`). Fold the sentence into the ONE §6 `status` paragraph RM-41/RM-48/RM-S1/BM-02/BM-03/BM-10/RM-22 share; never touch §12 `:498` (extend the `query()` helper in `status.test.ts` instead). Copy the scope arm explicitly (`scopeSchema` arms are passthrough). `config.shared` is created at `connect.ts:217`. Inventory rows 30, 31 and 33 (32 is RM-41's `pending`). RM-06 is re-opened as an app-side row that consumes `ledger`: it joins S7r, and S7r gains a dependency on S7k.
- **BM-10.** The same two assertions (`status.test.ts:102` adds `identity: null`; `cli.test.ts:21` adds `identity: null`); `identity` stays REQUIRED (an optional member reintroduces the undefined-is-dropped hole). Keep `github: ''` as "answered `-`, no login" and `null` as never asked (`auth.ts:81-91`). Add the failure-path test for an unconfigured `--team` (`config.ts:25` throws after the assignment at `:32`). Do not add the `login.test.ts` round-trip case (S7c edits that file; separate PRs). The plain `z.object` at `tauri/index.ts:29` STRIPS rather than throws; write the status schema `.passthrough()` and state the failure mode as silent loss.
- **BM-11.** Assert `state.path` against `process.env.PATH ?? null`; the read side (`app.ts:185` casts the JSON) should type `path?: string | null` or the §4.2 paragraph must say files older than this release omit it; the adapter follow-on is three edits, not two (`bridge.ts:13`, `bridge.ts:34` passing `path` to `cli_spawn`, `tauri/README.md:11`), plus `lib.rs:48`. Fork for Ryan under D1: the recorded PATH is only as fresh as the last `terum-skills app` run; resolving PATH at spawn time on the Rust side (a login-shell probe) needs no CLI field at all.

### 10.8 Connected surfaces (Teddy, 2026-09-08 17:40 UTC): the adapter halves fold into M7

Teddy, in-thread: "The real app should only be showing real data. If the inbox does not work it should not be there, visible just yet." and, on when a surface returns: "Once it is covered by M7 implemented, they should show, since they should be connected." This section is binding over 10.5's first two bullets and over every "adapter track" reference above; the M7 document's own definition row (every SIMPLE row shipped with its app half on `desktop/src/backend/tauri/` and `mock/`; the ten `gap()` read models retire) already said the same thing, and the queue now implements it.

**The rule.** A batch is done when its surface shows real data end to end: the CLI half, the adapter mapping of the verb's result frame `value` into the seam DTO (every new key declared on the closed zod objects, every new field `?? null`), the mock where the seam changes, and the served-surfaces flag flipped for any read model the batch completes. The real app never renders a design constant, a sample value or a fabricated caption; an undelivered field is null and its slot renders empty; a surface whose read model is still a gap stays hidden from the sidebar. Everything stays pixel-neutral on the mock, so the 87 locked boards do not move. A surface reappears in the PR that first serves its read model from real data, with later batches filling fields that were null.

**Two new app-only batches, first in the queue.** S7af, adapter plumbing and the real-data rule (AD-01 to AD-10), and S7ag, the Rust child lifecycle (AD-11 to AD-13). Every other batch that is not MOOT or DONE depends on S7af and stacks on its branch until it merges; S7ad also stacks on S7ag because both edit `lib.rs`. The review's basic set (REPORT.md section 3) and its section-4 bugs are placed as AD rows: steps 1 and 12 (`status()`, `settings()`) ride S7k as AD-20 and AD-24; steps 3 and 4 (`library()`, `skill()`) ride S7f as AD-21 and AD-22; step 10's mapping rides S7g as AD-23; step 13 (`roster()`, `catalog()`) rides S7b as AD-25 and AD-26; bug 13's argv order rides S7d as AD-17; bugs 1 and 14 ride S7ad as AD-19 and AD-18; bugs 2 and 10 ride S7i as AD-14 and AD-16; bug 8 rides S7m as AD-15. The Inbox has no honest step today (nothing is derivable without IB-02, RM-02 and RM-30); it returns when S7r serves the feed. Onboarding stays unreachable until S7r's routing rule and a screen that calls `backend.setup` exist.

**Order (replaces 10.4's numbering; the queue's args carry it):** S7af, S7ag, S7f, S7g, S7ad, S7k, S7d, S7c, S7b, S7e, S7h, S7i, S7j, S7ae, S7l, S7m, S7n, S7o, S7p, S7q, S7r, S7s, S7t, S7u, S7v, S7w, S7x, S7y, S7aa, S7ab; skipped S7z (MOOT, A3), S7a (MOOT), S7ac (DONE); drafts S7t (PF-02 gated) and S7ab (Ryan-gated). Simulated: 30 batches, 151 rows placed once each, zero dependency violations.

**Gates.** Every batch with an app half runs the desktop battery (typecheck, lint, vitest, build, Playwright routes, then the 87-board fidelity spec with `TERUM_DESIGN_DIR`), serialised with the root battery under one lock; a Rust change adds `cargo check` per `.planning/research/2026-09-07-desktop-app-tauri-build.md`. Slower per batch than CLI-only PRs, and correct.

**What stays out of M7 (10.5 still applies to these):** the React screen fixes that are not data (the Skill error board's fabricated crumbs and diagnosis, the caption literals, `Analytics.tsx:10`'s provenance default, the `reports.tsx` literals, the `ErrorLine` wrapping, the disable Switch gating) remain shell hygiene with the design-strings closure, except the four sidebar `COUNTS` literals, which are fake data and die in S7af (AD-03); the `TZ` avatar and sidebar literals and the BootError rows stay with the design walk; S7a stays retired.

**AD rows (the delta an implementer applies; file:line from REPORT.md sections 3 and 4, verified 2026-09-08 against 898a795 / f8557c4):**

- **AD-01.** carry `value` beside `error` on an ok:false result frame (`desktop/src/backend/tauri/run.ts:72-74`); `status` fails-with-value when any team is unreadable (`status.ts:100`), so without this every status payload row lights only on healthy clones
- **AD-02.** the query lifecycle (report step 2): `providers.tsx:11` policy, `({signal})` on every read, no refetch storm on minimise/restore (measured +1 fetch per key per cycle; Settings, Teams = 10 node processes and 15 git children at N=3); a stale board is a previously true board and must not be presented as current
- **AD-03.** the served-surfaces declaration and the real-data rule: the adapter declares which read models it serves (a `surfaces` block beside `Capabilities`, `desktop/src/backend/types.ts:8`); `Sidebar.tsx:14` omits a nav group whose model is a gap (Inbox first) and `Shell.tsx:15` already queries capabilities; the mock declares every surface so the 87 locked boards do not move; delete the four hard-coded sidebar `COUNTS` literals (`InboxScreen.tsx:67`, `ShareScreen.tsx:32`, `LibraryScreen.tsx:18`, `MarketplaceScreen.tsx:66`) so a count comes only from `status` or is omitted (`Sidebar.tsx:8` draws the span only when present)
- **AD-04.** report bug 12: `read()` (`tauri/index.ts:47-53`) collects print frames and appends them to a failing `Result.error`, as `run.ts:57` already does for stderr
- **AD-05.** report bug 15: one `useEffect` in `providers.tsx` does `backend.subscribe(source => queryClient.invalidateQueries(...))` so `ChangeSource` (`tauri/index.ts:39-44,91`) has a consumer and counts refresh after install, uninstall and sync
- **AD-06.** report bug 3 and step 5: pass `--team` on every verb, `ref` becomes `<team>/<name>` from a search hit, optional `team` on `ValidateArgs` / `InviteArgs` / `EvalArgs` (`tauri/index.ts:70,72,75,79,82,83,100`); no CLI change
- **AD-07.** report bug 4: `cliConnect.optional()` (`tauri/index.ts:22,75`); bare `connect` legitimately succeeds with no value
- **AD-08.** report bug 9: drop publish's `?? 'main'` third arm and carry `changed` (`tauri/index.ts:76`) so a no-op publish never reads as a real one
- **AD-09.** report bug 11: `(args.ref || args.cwd)` for validate (`tauri/index.ts:83`); today `{ref:'', cwd}` sends `['validate','']` and emits 47 findings over `.git/**`
- **AD-10.** report step 7: widen the `SearchHit` mapping (`team`, `category`, `author`, `installs`, `latest`, `endorsed`, `unresolved`; `description:''` until RM-30) and stop rendering the author's email in the results row
- **AD-11.** report bug 5 (Rust): hold and `join()` the two reader `JoinHandle`s before emitting `Exit` (`src-tauri/src/lib.rs:59,69,76-87`); `run.ts:48` stops dropping events after the first settle; `bridge.ts:31` stops calling `unlisten()` on the exit event
- **AD-12.** report bug 6 (Rust): `cli_kill` (`lib.rs:100-108`) uses a bounded poll after closing stdin, then kills the process group, so cancel no longer orphans a `git clone`
- **AD-13.** report bug 7 (Rust): a small child queue with a 'too many pending' error (`lib.rs:15`); pairs with BM-08's one git child per listing
- **AD-14.** report bug 2: key the install argv off `kind` using `ref` (`tauri/index.ts:72`; `MarketplaceScreen.tsx:37` sends `{ref, kind}`) so member and project installs stop building a bare skill install
- **AD-15.** report bug 8: parse `executionStatus` (`eval.ts:53,256`) and translate `failed` into ok:false or carry it on the seam (`tauri/index.ts:82`); a failed or partial eval must never report success
- **AD-16.** report bug 10: install / uninstall-skill map `name`, not `item.id`, into the DTO's `name` (`tauri/index.ts:72,73`), and the unit test stops feeding `{id:'deploy-check'}`; BM-05's app half
- **AD-17.** report bug 13's adapter half: every argv builder orders verb, flags, `--`, positionals (`tauri/index.ts:70-83,96-103`); safe once BM-06 and BM-07 land in the same PR
- **AD-18.** report bug 14: a truncated `app.json` surfaces the `NO_STATE` guidance, not a raw `SyntaxError` (`bridge.ts:45-46`), and `tauri/index.ts:37-38` stops memoising the failure for the whole session
- **AD-19.** report bug 1's adapter half (BM-11 / BM-12): `bridge.ts:13` accepts `path` and `target` with `writtenAt` required; `cli_spawn` sets `command.env("PATH", ...)` (`lib.rs:48`); the onboarding route's consume-once rule is S7r's
- **AD-20.** report step 1: implement `backend.status()` from `status` (`teams[0].{team,handle,repository,members[].displayName,memberCount,sharedSkills}`, `version`), `ls --local` (`counts.Global`) and `host_platform()` (`machine.os`); omit `counts.Pushes/Updates/Alerts` and every project key until real; `gh_login`/`me.email`/`clone`/`last_sync`/`stamp` empty until their rows land; flips the status surface
- **AD-21.** report step 3: implement `backend.library({scope})` from `ls`, `ls project <name>`, `ls --local` (`installed`) and `status` (`origin`) with the pixel-neutral screen half; honest defaults (`desc:''` until RM-30, `size:'—'`, `tokensK:0`, `wlt:null`, `summary:null`, `favorite:false`); flips the Library surface
- **AD-22.** report step 4: implement `backend.skill({ref})` from `ls` + `ls --local` + `status` + `validate` (bare pass/fail); never default `favorites`, `lines`, `files`, `grants`, per-check hygiene rows or any synthesized receipt; `receipt:null` lands the evals tab on the drawn 'No receipt yet' board; flips the Skill detail surface
- **AD-23.** report step 10's adapter half: map `placement {id,team,version}`, `shared {id,team}`, `tracked`, `health` from `ls --local` rows into Settings, This machine and into `installed`/`version` for steps 3 and 4 (no regex over prose)
- **AD-24.** report step 12: implement `backend.settings()` from `ls --local` + `status` + board constants with the honest defaults (`PLACEMENTS[].state`/`placed` '—', `APPROVALS []`, `QUARANTINE []`, `CLI_LATEST '—'`, `HOOK.installed false`); stop folding `catalog()` into settings; flips the Settings surface
- **AD-25.** report step 13: implement `backend.roster()` from `status` members (+ PF-03 role, PF-04 projects when this batch lands); `joined`/`lastSeen` '—' until PF-06 exists; flips the Share surface
- **AD-26.** report step 13: implement `backend.catalog()` from `ls` + `ls member <handle>` + `ls --local` + `status` (Top rated, People, Categories; `projects:[]` until BM-01, `bulkInstall:{}`); flips the Marketplace surface

### Appendix B. Desktop demand list (2026-09-08 blank map)

Copied verbatim from the review's section 5.1 ("SIMPLE, a field on an existing verb"). The "row" column is the costing id where one exists; "uncatalogued" rows are new asks that exist only because frame mode replaced in-process import, and section 10.2 costs and places each (BM-01 to BM-12; the usage-error row is BM-06 plus BM-07; the PR #58 row is BM-12; the last row is BM-09).

| want | change | verb | row |
|---|---|---|---|
| every card's description | `description` on `LsSkill` and `SearchHit` | `ls`, `search` | **RM-30** |
| project rows, the project scope, the Marketplace Projects board | `projects: {name, skills}[]` on `LsResult`, from the team object parsed at `ls.ts:32` | `ls` | uncatalogued |
| the placements table, Sharing, card health flags | real fields on `ls --local` rows instead of prose (`ls.ts:106-109`) | `ls --local` | **RM-44** / **RM-39** / **RM-40** |
| last sync on Teams and Sync | `stampedAt` on `TeamStatus` | `status` | **RM-48** |
| team policy rows | `policy {publish, skill_license}` (zod line exists at `schema.ts:50`) | `status` | **RM-S1** |
| the clone path row | `clonePath` (`status.ts:45` computes and drops it) | `status` | uncatalogued |
| the two green-check rows that today assert an unmade probe | gh/git/cc presence (`ghState` already exported) | `status` | **RM-22** |
| Settings ▸ Updates | update advice as a value; `update` returns `Result<void>` today (measured: no `value` key) | `update` | **RM-08** |
| Settings ▸ Sync | session-start hook state (`hookInstalled` already exported, `hook.ts:57`) | new small verb | **MC-10** |
| the identity fields that today write only `localStorage` | config identity on the status result | `status` | **MC-11** |
| SKILL.md tab | return the text read at `ls.ts:53` and thrown away | `ls` | **RM-01** |
| the consent list, the Quality tab chips | `grants` from `parsed.grants.normalized` (`schema.ts:165`) | `ls` | **RM-02** |
| the uses popover and facepile | installer handles behind `installCounts` | `ls` | **RM-03** |
| the six drawn hygiene rows | `validate --json` emitting `HygieneFinding{code,path,line?,message}` | `validate` | **EV-07** |
| every verdict, lift and the Evaluated meter | a read-only `receipt <ref>` verb over `evals/<id>/<version>/<runId>.json` | new | **EV-13** |
| the card's broken flag | `unresolved` on `LsSkill`, mirroring `SearchHit` | `ls` | **RM-49** |
| "Recently updated" sort | a per-skill last-changed date | `ls` | **RM-38** |
| one honest Inbox `share` row | `installed`/`declined` ids on `ls` | `ls` | **IB-02** |
| the Share admin column | host admin permission | `ls --host` | **TJ-04** |
| the "N invitations" clause | pending GitHub invitations (`team.ts:115-117` already reads them) | `ls --host` | **TJ-05** |
| the member role cell | a role field on `people/<handle>.json` | `ls` / `status` | **PF-03** |
| the member Teams column | project members | `status` | **PF-04** |
| the drawn per-team Sync button | `--team` on `sync` | `sync` | **RM-32** |
| per-team outcomes on a failed app-driven sync | delete the `args.hook ?` guards at `sync.ts:353,359` | `sync` | **RM-31** |
| a name instead of a UUID on the install DTO | `name` on `InstalledResult`/`UninstalledResult` | `install`, `uninstall-skill` | uncatalogued |
| "what this person installed" | return `ls member`'s already-computed installed list (`ls.ts:72`) | `ls member` | uncatalogued |
| the Onboarding join block | `joinCommand`/`joinBlock` on `TeamStatus` | `status` | uncatalogued |
| a joiner landing in the wizard (PR #58) | a target field on `app.json` plus one argv passthrough (`app.ts:134,139`) | `app` | uncatalogued |
| truthful boot rows | progress events the adapter can report | `sync` | **RM-17** |
| a result frame on a usage error, and `--`-safe argv scans | `src/index.ts:43,60,69-71` | all verbs | uncatalogued |
| Library and Marketplace cost at scale | batch `ls`'s per-skill `git rev-parse` fan-out the way `search.ts:52` already batches 8 (measured: `ls` is 142 + 2.46N ms, N git children) | `ls` | uncatalogued |
| Settings ▸ This machine's approvals half | `config.placements`/`approvals`/`shared` on the status result. RM-06 was costed CLOSED only because it assumed in-process `ConfigStore` access, which frame mode retired | `status` | uncatalogued (not RM-06 as written) |
