{
 "document": "# M7 \u2014 close the canvas gaps in the CLI (metadata + intelligent sync)\n\n## 0. The answer in five lines\n\n1. **Can M7 make the canvas fully functional? Not on its own.** Every SIMPLE cli/both row shipped flips 2 of the CLI's 12 capability flags (`roles`, `projectMembers`). The rest stay false: `favorites`/`follow` need one ledger sentence retiring D38, `perCase`/`liftOnCards`/`runEvalInApp` one Ajay paragraph, `disablePerMachine` is HARD, `progress` has no emitter, and `lastSeen`/`installScope`/`inviteScoping` are refused or deferred (frames.ts:22-23, :39-42).\n2. **What it is:** 29 batches, S7a to S7ac, over the 62 SIMPLE CLI-side rows and the routed MEDIUMs. S7p is MOOT (frame mode replaced the exports map), S7a is moot in part, and S7ac is DONE (c487048).\n3. **From Ryan:** 44 costed rows (45 less RM-11, already routed) across 17 batches, two widenings, one gated ask (S7ab reopens the table guard.ts:150-156 closes at rows c/d/e).\n4. **From Ajay:** 9 rows across 5 batches, as one message and three PRs; the leverage is S7v's single \u00a712 paragraph, which unblocks five rows and costs no engine code.\n5. **From Teddy:** 57 design-side string rows (his standing call, BRIEF.md:28/:33), the eight forks in section 6, and the board debt in S7z and S7x.\n\n## 1. Definition row and exit criterion\n\n|**M7: close the canvas gaps in the CLI (metadata + intelligent sync)**|every SIMPLE `cli`/`both` row shipped (payload, flag, verb or spec delta merged on `ryanliu-terum/terum-skills`) and its board surface real: from `desktop/src/backend/tauri` where the adapter can reach it, from `mock/` where the row is app-side; every MEDIUM, HARD, CLOSED, DEFERRED and design-side row has a recorded disposition; `npm run check` and `check:design` green, 87/87 locked|\n\n## 2. What \"metadata + monorepo + intelligent sync\" concretely means\n\nRight for reads, wrong for writes. `safeWrite` fetches and hard-resets to `origin/main` before re-running its mutation, and `refreshClone` runs per team (sync.ts:147), so **a new committed field is read by every other machine on its next sync with zero sync code**. What sync lacks is a writer for anything new, a names payload, and any notion of a machine.\n\n**Where sync writes.** People files at install.ts:131-140, uninstall.ts:92-107, sync.ts:385-394 (adopt) and :398-405 (decline), all four rewriting only `installed`/`declined`, plus two writers a verifier found: team.ts:407-418 (join, an object literal) and team.ts:524 (a bare `writeFile` outside safeWrite).\n\n**Per field.** `role`, `bio`, `display_name` and `projects` are owner-written through one verb under guard row b (guard.ts:49 is a whole-file allow with no field check, which is why they cost no guard code on an existing action); sync must never refresh them, or a rename made elsewhere is reverted on the hook. `declined` is append-only and id-keyed. `favorites`/`following` ride the same passthrough but must be `.optional()`: the one additive config field that shipped is `app: appChoiceSchema.optional()` (schema.ts:91-94), and a `.default` materialises the key into every people file on the next unrelated write. team.json fields are refused (`guardTeam` admits three shapes and throws, guard.ts:150-156); TJ-10 works only because `projects[]` is passthrough. Receipts are passthrough with `schema_version: z.literal(1)`, so additions are free and a version bump is never the mechanism.\n\n**Conflict rule.** Mutations are re-applied against a freshly reset tree, so a writer reads its pre-image inside the mutation (sync.ts:387-389); capturing it outside turns field-level last-writer-wins into whole-document.\n\n**Four derivations refused.** Install counts stay derived at read time (readme.ts:40). No inbox event log: the change signal is `run/<team>.stamp` (writeStamp sync.ts:414, called :348), already modelled as `ChangeSource 'stamp'`. No machine registry: `hostname()` appears once, in an uncommitted lock. No lastSeen heartbeat: it would push on every session start, so PF-05 becomes a `git log` derivation.\n\n**The stamp invariant.** A team is stamped only when `!incomplete.has(team) && pendingLeft === 0` (sync.ts:344-348), so undone work no later run can clear withholds the stamp forever and refetches at every session start. Hence CP-S3's absent clone is a SKIP, and `sync --team` is a skip-set, not a filter: `reconcileOrphans` excludes only via `skipTeams` (:373) and otherwise prompts \"Adopt orphaned placement at <path>?\" (:382), writing a permanent team-readable decline on a no (:403).\n\n**D20.** Three surfaces render app-local state as team truth: the role chip (ShareScreen.tsx:45), Follow, the heart. Each batch landing a committed field deletes its pref in the same PR.\n\n**One widening beyond the costing:** sync returns counts where the seam wants names, so `Backend.sync()` returns empty lists (a dead ternary, tauri/index.ts:78). The names exist one line before the projection to counts. Ryan approves that as a widening, not as RM-31.\n\n## 3. Where M7 sits now that M5 and M6 have shipped\n\nThe M6 prerequisite is **not** a library entry. `package.json` declares only `bin`, `files`, `version`; the app spawns `node <entry> --frames <verb>` (lib.rs:42-48) and finds the CLI in `~/.terum/skills/run/app.json`. So RM-11 is off every critical path, and **every widened Result member reaches the app the moment its PR merges**: execute.ts:32 puts the verb's value on the result frame.\n\nTen read models still return a hard-coded gap, the M7 backlog the adapter wrote itself (tauri/index.ts:61-69, :85):\n\n|gap()|unblocked by|\n|---|---|\n|`status`|S7d (+ S7k categories, S7s storage)|\n|`library`, `catalog`|S7b, S7c (S7l filters over them)|\n|`skill`|S7n + S7b + S7c + S7j + S7k|\n|`receipts`|S7k (EV-13), S7u (per_case)|\n|`inbox`|S7m (+ S7c, S7j, S7k)|\n|`roster`|S7i (+ S7a's committed fields)|\n|`update`|S7g (RM-08 must return a value)|\n\nTwo adapter facts define \"real\": a failing result frame drops its `value` (run.ts:72-79), so any row whose payload rides a failure (RM-47, RM-19) needs that pass-through first; and one malformed row fails a whole call, since every result goes through one `schema.parse` (tauri/index.ts:44).\n\n**First end-to-end test.** Only three platforms have a build: `aarch64.app.tar.gz`, `x64.app.tar.gz`, `arm64-setup.exe` (platform.ts:15-33), built by release.yml's `desktop` matrix before the npm approval behind an artifact-exists gate. So the first real run is on the Windows ARM host: `terum-skills app` installs the NSIS build silently, launches it, and the app spawns `--frames status`, which today renders the GAPS.md error board. The first batch that changes a pixel there is S7d. macOS cannot be walked by hand here, so it is proven by the CI runner (`macos-latest`, `aarch64-apple-darwin`) plus the checksum-verified install (app.ts:96-126). WSL and Linux get an honest one-liner and exit 0 (D3).\n\n## 4. The batches\n\n|id|title|rows|owner file|CLI change|app half|approver|size|dependsOn|\n|---|---|---|---|---|---|---|---|---|\n|S7a|Retired: exports map|RM-11 (moot)|none|none|none|Ryan|0|none|\n|S7b|ls/search payload|RM-49, RM-30, RM-38, RM-02, RM-03, RM-28, RM-51, RM-04, EV-06, RM-50, RM-12, CP-19 (moved)|none|LsSkill/LsResult fields; per-row degrade; frames inventory|un-gap library, catalog|Ryan|L|S7c|\n|S7c|Local and placement payload|RM-39, RM-40, RM-44, RM-05, RM-06, RM-46, CP-26, CP-25|none|health, tracked, refs, sharedState|Sharing, health dots, quarantine|Ryan|M|S7b|\n|S7d|status payload|RM-41, RM-48, RM-S1, CP-20, MC-03, MC-07, CP-43, CP-21|none|pending, syncedAt, policy|un-gap status; failing value|Ryan|M|none|\n|S7e|Results carry the run|RM-31, RM-18, CP-S3, RM-07, RM-32, RM-17, RM-33, CP-39|none|sync --team, absent-clone skip, notices|onboarding phases; run queue|Ryan|L|S7b|\n|S7f|Batch isolation, Windows home|RM-19, RM-21|none|isolate batches; placementHome|pass member/project|Ryan|S|none|\n|S7g|Machine lifecycle|MC-10, RM-29, MC-12, RM-08, AC-17, MC-08|none|hook verb; update returns a value|un-gap update|Ryan|M|none|\n|S7h|Identity, zero-team spelling, docs|MC-11, CP-37, CP-40, CP-24|config.json|login --set; getStartedLines; README|login seam method|Ryan|M|none|\n|S7i|Host reads, invitations|TJ-04, TJ-05, TJ-11, TJ-10, PF-06|none|ls --host; invite --cancel; joined|un-gap roster|Ryan|M|S7c|\n|S7j|Hygiene as data|EV-07, EV-17, IB-05|none|assessment; validate --json; HYG7|widen cliValidate|Ajay|S|none|\n|S7k|Eval results as data|EV-08, EV-11, CP-02, EV-13, EV-04, EV-14, EV-18, IB-07, CP-16, TJ-12|none|summary; escalation flag; receipt verb|un-gap receipts|Ajay (+Ryan: TJ-12, CP-16)|L|S7d, S7j|\n|S7l|Filtering is not ranking|CP-11, CP-15, CP-28|none|one \u00a712 paragraph|facets; defaults off|Ajay + Teddy|S|S7b, S7c, S7k|\n|S7m|Inbox as a derivation|IB-06, IB-02, IB-08, IB-01, IB-04, IB-09, IB-10, MC-02, CP-41, CP-S4|none|endorsements verb|un-gap inbox; no log|Ryan|M|S7c, S7d, S7j, S7k|\n|S7n|Skill body, project context|RM-01, MC-01|none|body on SkillRecord|finish the cwd half|Ryan|S|none|\n|S7o|A cancel is not an error|RM-09, RM-10, CP-35, RM-22, RM-34, RM-35, RM-36, CP-30|none|typed decline; three readers; invite --project|carry declined|Ryan|M|none|\n|S7p|Retired: packaging|RM-11, RM-13|none|none|RM-13 re-homed|Ryan|0|none|\n|S7q|Records, not code|AC-06, AC-08, AC-07, CP-14, CP-D1, CP-13, RM-27, RM-45, RM-47|none (2 refusals)|none|openUrl; clone-state surfaces|none (Teddy: copy)|M|S7d|\n|S7r|App-only chrome|AC-01, AC-02, AC-03, AC-05, AC-10, AC-12, AC-13, AC-14, AC-15, AC-16|none|none|theme, keymap, clock, contrast|none|L|none|\n|S7s|Storage and quarantine|MC-S1, CP-S1|none|status --storage; sidecar|real bytes, real reasons|Ryan|M|S7d|\n|S7t|Favorites and following|PF-01, PF-02|people/<handle>.json|two fields; two flags|delete three prefs|Ryan + Teddy (gated)|M|S7a's verb|\n|S7u|Receipt additions, pruning|EV-03, CP-23, MC-09|none|per_case; incumbent; prune-evals|per-case tables|Ajay|M|S7k|\n|S7v|The Ajay \u00a712 paragraph|EV-01, EV-02, EV-09, EV-16, EV-19|none|four \u00a712 texts, \u00a75.4, two flags|lift, forecast, rail, roll-up|Ajay|S|S7k|\n|S7w|Change notification, setup|RM-23, RM-24|none|none|native watch; step map|none|L|native surface|\n|S7x|Reflow, axe gate|AC-04, AC-18|none|none|width assertions; axe job|none (Teddy: floor)|L|S7r|\n|S7y|Project key case|RM-15|none|one resolver, eight sites|none|Ryan|S|S7b, S7f|\n|S7z|The `new` verb|RM-37|none|new verb; FRAME_VERBS|seam method; 3 boards owed|Ryan + Teddy|S|none|\n|S7aa|MC-01 as a MEDIUM|MC-01|none|--cwd on three verbs|already shipped|Ryan|S|S7c|\n|S7ab|Permission roles (gated)|TJ-02|team.json|roles map; fourth guard arm|role menu|Ryan (gated)|M|S7i|\n|S7ac|Done: npx to app|none|none|shipped|NO_STATE wording|done|0|none|\n\n**S7a (retired in part).** RM-11 is superseded by frame mode (80054f4). **CP-19 moves to S7b**, re-specified as the hello frame's verbs/features inventory (frames.ts:32, :39-42); cut every dependsOn edge on RM-11. One caveat: the retirement instruction names RM-11, which FINAL DESIGN places in S7p, so S7a's four other rows (PF-07, PF-03, PF-04, PF-09) are **not** retired. They are the `profile` and `decline` verbs plus the one guard action S7b, S7i and S7t depend on, and they still need this batch.\n\n**S7b.** `format` is exported (ls.ts:84) and `search` calls it with a seven-key literal (search.ts:60), so new members are optional or typecheck breaks; RM-49's people half needs a per-file catch at ls.ts:33. Four of its rows are CLOSED or DEFERRED app-side, so five carry CLI change.\n\n**S7c.** dependsOn **S7b**, not S7a. The producer belongs in lib/skills.ts beside `canonicalDigest`, and must digest repaired bytes: the reconciler writes before it digests (connect.ts:270 then :276). CP-25's confirm is a CLI edit (connect.ts:365/:373), and the stamped team set must not change.\n\n**S7d.** Its `cliChanges` field is S7o's decline inventory; S7d's own change is three members on StatusResult (status.ts:16-23). Blocking: an unreadable clone returns `failure(..., {version, teams})` (status.ts:99) and the adapter drops that value. MC-03 needs a new Rust command.\n\n**S7e.** RM-31 is half DONE (TeamOutcome shipped in 55ef7f0, sync.ts:34-39); the interactive-failure payload is ruled against in a LOCKED spec (sync-verdict.md:26), so it is a reopen, not a build. `--team` and the absent clone each need a new `skipped` reason.\n\n**S7f.** RM-21 is a real bug (install.ts:241's POSIX test cannot match on Windows). The ls.ts text in its fields belongs to S7b. Add SafeWriteExhausted, and fix MarketplaceScreen.tsx:37, which never sets member/project so tauri/index.ts:72 falls through to the single-skill form.\n\n**S7g.** RM-08's export buys nothing without a library: `update` returns `Result<void>` (update.ts:12, :35) and the adapter says so (tauri/index.ts:84). MC-12 is cheaper than costed (`noUpdateCheck` is never read in update's run), and MC-08 contradicts shipped prefs, so it is a decision.\n\n**S7h.** MC-11 needs a branch inside login.ts, so five files (schema anchors :92-94, passthrough :101). CP-24 is red at HEAD: `app` is non-hidden (cli.ts:132) and undocumented, and the gate needs a `.hidden` exclusion.\n\n**S7i.** PF-06 must move CLI-side (the shell cannot run git). D6 shipped the authenticated user's reader (team.ts:496); the repo-wide list and DELETE are team.ts:115/:132, and `paginatedItems` (:147) must be lifted. Degrade per read: collaborators is push-readable, invitations admin-only.\n\n**S7j.** `cliValidate` is the only closed zod object (tauri/index.ts:29), so an added assessment is silently stripped: widen it, the seam and the mock. The Quality tab renders `skill()`, not `validate()`, and HYG7 falsifies two \"6 checks\" strings.\n\n**S7k.** Add **dependsOn S7d**: `categories` appends to fields S7d creates. The seam `Receipt` is the fixture receipt (types.ts:15) and requires `per_case`, so `receipts()` needs EV-03 or optionality. The `receipt` verb must join CliVerbs and FRAME_VERBS and ask nothing.\n\n**S7l.** Not zero-cost: the defaults live in the generated fixture, so it is a build.py change plus a regeneration, it moves a locked board (FIDELITY.md:43) and breaks a green test. `ReceiptSummary` has no provenance, so the mismatch rule waits.\n\n**S7m.** The app has no filesystem and no watcher (five commands, lib.rs:137), so the recompute arrives over frames or gains a native capability. `endorsements` cannot expose `openEndorsements` (name-anchored, publish.ts:190): it is one repo-wide ls-remote plus a `gh pr list`.\n\n**S7n.** Three files: schema.ts, skills.ts, install.ts:163 (`skillAtSource`'s spread); the boundary is `source.slice(match[0].length)`. MC-01 is double-owned with S7aa with the opposite ruling; pick one. The seam also wants parsed blocks and the raw frontmatter.\n\n**S7o.** RM-10, RM-22 and CP-35 were costed as exports for an importer that no longer exists; re-point each at `result.value` or mark it CLI-internal. RM-09 is half shipped (frames.ts:44-47) and the adapter drops it, so the rest is types.ts:6 plus run.ts, drive.ts and the mock; four decline strings say \"cancelled\" and miss `isDecline`.\n\n**S7p (retired).** All four packaging edits are absent and nothing needs them. RM-13 is re-homed: render the body's first paragraph with `description` as fallback (an S7n consumer), or take the costed design route.\n\n**S7q.** Keep the two refusals on record; window chrome and clipboard are DONE, not records. `decorations: true` on every platform, so the drawn-controls arm paints dead buttons beside real ones, and anchors already compute URLs, so AC-08 is routing them through a new `openUrl` method.\n\n**S7r.** Five rows are shipped or nearly: fonts bundled, `icon-paths.ts` present, window-state registered (lib.rs:140), a theme boot script with no system arm (index.html:5), the isTauri tree test. AC-12 is remedial (app.css:57 sets it already) and narrowing it moves locked pixels.\n\n**S7s.** MC-S1 is costed as one **flag** on status, not an unconditional block; unflagged it walks 312 MB on every adapter read. `moveToQuarantine` is not the single funnel (leave.ts:100-103 moves the clone without it), and CP-S1 is design-costed.\n\n**S7t.** `skillIdSchema` is a uuid, so `favorites` cannot hold `favorite-project:<key>`: a third ruling for Teddy. Four write sites (add SkillScreen.tsx:35), no Backend method; two green tests assert the prefs it deletes.\n\n**S7u.** CP-23's constraint is wrong: `materializeIncumbent` yields a 40-hex tree on both branches (eval.ts:333-335), so null-on-fallback is a product call; record the source instead. It returns a directory, so threading the tree changes eval.ts; `--prune` is taken by sync.\n\n**S7v.** Not zero code: `liftOnCards` and `runEvalInApp` are false and the seam has no matching keys. The paragraph must amend four \u00a712 texts (:480, :493, :495-496, :497-498) plus \u00a75.4, or \u00a712 contradicts itself.\n\n**S7w.** Half of RM-23 shipped: subscribe fires on the app's own successful runs (tauri/index.ts:39-45); external detection needs a native watcher, so it is not app-only. RM-24's step map is 11 to 6, and the gh offer is no longer asked over frames (D5).\n\n**S7x.** The shipped floor is 960x600 (tauri.conf.json:18-19), not D-W1's 1200x720, and below ~1150 two undrawn screens become mandatory: raise the floor, or add them to scope. The three boards wanting a second baseline have locked oracles already.\n\n**S7y.** Eight key lookups, not five (add install.ts:235, publish.ts:170, sync.ts:422). The app renders `.name`, not `.key`, so deleting the invented `name` lowercases the boards.\n\n**S7z.** dependsOn S7p is retired, so this is independent. The CTA already calls `openInEditor` (LibraryScreen.tsx:17), so the batch replaces working behaviour; add `new` to FRAME_VERBS plus a Backend method.\n\n**S7aa.** Its stated risk is false: the app already sets the child's cwd (lib.rs:44-46), so `--cwd` is convenience, not the mechanism. `uninstall-skill` declares `cwd` and never reads it, so one of the three is dead.\n\n**S7ab.** dependsOn **S7i**, not S7h. It is a three-condition break: a team.json field, a fourth \u00a76.0 row, and a new `GuardAction` member. Use `.optional()`, or the first publish writes `\"roles\": {}` into every repo.\n\n**S7ac (done).** The `app` verb (c487048) and setup's first question (a3de58a) shipped under D1/D3/D4/D7/D8. One residual it predicted: `uninstall` sweeps run, cache, teams, quarantine (uninstallMachine.ts:135) and never `app/`. Fold that one-file fix into S7g.\n\n## 5. Dispositions\n\n**MEDIUM, build now:** CP-39 (rider on S7e); EV-03, CP-23, MC-09 (S7u); MC-S1, CP-S1 (S7s); PF-01 (S7t, gated); AC-04's D-W1 half and AC-18's five clauses (S7r); RM-23, RM-24 (S7w); EV-02, EV-09, EV-16, EV-19 (S7v, one paragraph).\n**MEDIUM, later:** AC-04's reflow table and AC-18's axe gate (S7x); MC-01 (S7aa).\n**HARD (3):** MC-05 per-machine disable, disclose only. IB-03 off-target telemetry, **cut** (a scope non-goal blocks it before engineering). PF-08 opt-out of install records, design-side, and it needs the GAPS.md line it lacks; unlike PF-05, disclosure does not close it.\n**CLOSED (9), no work:** CP-07, CP-38 (premise false, not a commit), EV-05, EV-10, EV-12, MC-04, MC-06, RM-20, TJ-O1.\n**DEFERRED (5):** CP-O3 (fold Ryan's one-line community.ts change into S7h), EV-15, RM-25, RM-42 (needs a ruling line), TJ-08 (un-defer candidate: the launcher exists).\n**The 57 design-side rows, by board family, all Teddy's calls:** Library and cards (RM-13, RM-14, RM-16, RM-26, RM-37, CP-06, CP-10, CP-45, CP-46); Skill detail and eval report (AC-11, CP-01, CP-04, CP-17, CP-23, CP-29, CP-44, EV-01, EV-20, EV-21, RM-43, TJ-13); Marketplace (RM-15, CP-03, CP-12, CP-27, CP-34, TJ-03, TJ-06); Share and invite (CP-05, CP-18, CP-30, CP-31, PF-02, PF-05, TJ-01, TJ-02, TJ-07, TJ-09, TJ-O2); Inbox (CP-09, CP-22, CP-36); Settings (CP-08, CP-19, CP-32, CP-33 blocked, CP-S1, CP-S2, CP-S4, CP-S5, CP-S6, TJ-12); Onboarding (CP-O1, CP-O2, CP-O4, RM-O1); chrome (AC-09).\n\n## 6. Decisions Teddy must make\n\n1. **Scope of \"fully functional\".** M7 flips two flags; if the greyed controls must light, PF-01, PF-02 and the four eval MEDIUMs come with it.\n2. **CP-30 vs TJ-01.** Invite scoping is dropped; is the selector kept as a note composer? If not, S7o's `--project` comes out.\n3. **RM-15.** The resolver pays off only if the live `team.json` keys are capitalised; otherwise the boards go lowercase.\n4. **RM-37.** Three boards owed (name, description, scope, what happens next), or relabel the CTA to Connect.\n5. **PF-02.** A committed follow list publishes an asymmetric social graph to everyone with repo read, permanently, in git history.\n6. **PF-01's project heart.** `favorites` is uuid-typed, so project favourites are a derived chip or a wider field.\n7. **TJ-02.** Ryan is asked to reopen a closed table; is the read-only, unknown-capable chip an acceptable long interim?\n8. **The window floor.** Keep 960x600 and draw two screens, or raise it to 1200x720.\n\n## 7. Approvals and PR cadence\n\n**Ryan:** 44 rows across the 17 batches section 4 marks Ryan, plus TJ-12 and CP-16 inside S7k, two widenings (names-not-counts; `categories`) and one gated ask. RM-11 is excluded: already routed to a Mac teammate.\n**Ajay:** 9 rows across 5 batches (S7j, S7k, S7l, S7u, S7v), as **one message, three PRs**: the zero-code \u00a712 paragraph (S7v); the additive payloads (EV-07, EV-08, EV-11, CP-02); the new predicate and print format (EV-17, IB-05).\n**Cadence.** One batch, one PR, one Codex run, titled `Merge codex/S<n>: <spec title>`, at most two Ryan PRs in flight. Send S7v first: it costs nothing and gates the evals surface. Open Ryan's queue with a small file-disjoint batch (S7f or S7n) while the read-model chain (S7b to S7c to S7i) is specced, and hold S7ab until every ungated batch merges. `src/cli.ts` is touched by nine batches: registrations are region-disjoint appends, never a reformat.\n\n## 8. First spec outline, ready for /codex-spec\n\n**S7f: batch isolation for install member/project, and the Windows global-placement bug (RM-19, RM-21).** Small, untouched by the delta, breaks no SIMPLE condition, and independent on the CLI side once its questionable S7e edge is dropped.\n\n1. **Pins.** Base f8557c4. Member loop install.ts:47-53, project loop :55-62, catch :77, pending written :93-95 and cleared :141, `placementHome` :241 and its only call site :106, `failure(error, value?)` result.ts:6, the isolation pattern at sync.ts:308-320.\n2. **RM-19.** Isolate per item: an unplaceable skill is named, the batch continues, and `ok:false` carries the successful `InstalledResult[]`. Batch-fatal classes stay fatal: PromptClosedError, RemoteAccessError, SafeWriteExhausted (teamRepo.ts:53, thrown :178).\n3. **RM-21.** Replace the POSIX suffix test with basename/dirname (the idiom at placer/agent-paths.ts:24); say whether `placementHome` is exported or tested through `installOne`.\n4. **App half.** run.ts's failing branch must map `frame.value` and finish `{ok:false, error, value}`, and types.ts:6's result arm must carry it, or seven successes of eight reach no screen. Fix MarketplaceScreen.tsx:37 to pass member/project, and decide whether a screen renders partial success or the error string names it.\n5. **Sync rule.** Unchanged: pending is written before placement and replayed at sync.ts:161-186, and a team with pending is left unstamped, so extra entries self-heal. Say in the PR that isolation leaves one pending entry per failed item.\n6. **Tests.** install.test.ts: one foreign folder in an eight-skill batch leaves seven placed and the Result carries them; each fatal class aborts; `placementHome` resolves for a Windows-shaped root and every pinned POSIX root (install.test.ts:126-136 and sync.test.ts:353-359 stay green). desktop: a run.test.ts case for a failing result carrying a value.\n7. **Out of scope.** The ls.ts text inherited in its fields (that is S7b), retention policy, any new flag.\n\n## 9. NOT VERIFIED / open\n\n- **The retirement instruction names RM-11 as S7a's row**, but FINAL DESIGN puts RM-11 in S7p and gives S7a four people-file rows; section 4 keeps the instructed table cell and records those four as live.\n- **Section 8's instruction names S7f but describes \"the ls / search payload\", which is S7b's content.** Ids are fixed, so the outline is S7f's rows; the ls.ts text inside S7f's own fields is misfiled and belongs to S7b.\n- **S7f's dependsOn S7e** rests on RM-18's notices editing install.ts; nothing at f8557c4 emits notices there. Confirm before treating S7f as blocked.\n- **Teddy's Windows host being ARM64** is a memory note, not re-verified; if it is x64 there is no build (platform.ts:15-33) and no end-to-end run from any machine in hand.\n- **Not re-read this session:** the eval-engine spec's \u00a712 line numbers, the design rows' build.py sites, the app repo's tests. Every CLI anchor above was read at f8557c4.\n- **Neither ask has been sent** as of the last record.\n- **`desktop/GAPS.md` is still headed \"pinned at b5c0507\"**; re-anchor it at 0.1.6 in the first batch that touches it. PF-08 has no GAPS.md line.\n- **build.py line numbers in the costing are snapshot citations** against a drifted file; re-grep by content.\n\n## Appendix: row to batch index\n\n**S7a** RM-11 (moot), CP-19 (moved to S7b); PF-07, PF-03, PF-04, PF-09 live. **S7b** RM-49, RM-30, RM-38, RM-02, RM-03, RM-28, RM-51, RM-04, EV-06, RM-50, RM-12, CP-19. **S7c** RM-39, RM-40, RM-44, RM-05, RM-06, RM-46, CP-26, CP-25. **S7d** RM-41, RM-48, RM-S1, CP-20, MC-03, MC-07, CP-43, CP-21. **S7e** RM-31, RM-18, CP-S3, RM-07, RM-32, RM-17, RM-33, CP-39. **S7f** RM-19, RM-21. **S7g** MC-10, RM-29, MC-12, RM-08, AC-17, MC-08. **S7h** MC-11, CP-37, CP-40, CP-24. **S7i** TJ-04, TJ-05, TJ-11, TJ-10, PF-06. **S7j** EV-07, EV-17, IB-05. **S7k** EV-08, EV-11, CP-02, EV-13, EV-04, EV-14, EV-18, IB-07, CP-16, TJ-12. **S7l** CP-11, CP-15, CP-28. **S7m** IB-06, IB-02, IB-08, IB-01, IB-04, IB-09, IB-10, MC-02, CP-41, CP-S4. **S7n** RM-01, MC-01. **S7o** RM-09, RM-10, CP-35, RM-22, RM-34, RM-35, RM-36, CP-30. **S7p** RM-11, RM-13. **S7q** AC-06, AC-08, AC-07, CP-14, CP-D1, CP-13, RM-27, RM-45, RM-47. **S7r** AC-01, AC-02, AC-03, AC-05, AC-10, AC-12, AC-13, AC-14, AC-15, AC-16. **S7s** MC-S1, CP-S1. **S7t** PF-01, PF-02. **S7u** EV-03, CP-23, MC-09. **S7v** EV-01, EV-02, EV-09, EV-16, EV-19. **S7w** RM-23, RM-24. **S7x** AC-04, AC-18. **S7y** RM-15. **S7z** RM-37. **S7aa** MC-01 (also S7n). **S7ab** TJ-02.\n\n**Named in an app-half column, not a rows column:** PF-05 (app half of S7b); AC-04's D-W1 half and AC-18's five clauses (app half of S7r). Others sit in their batch's rows column.\n\n**Design-side, no batch (57):** listed by board family in section 5. Twelve keep a design half with Teddy while a batch touches them: CP-19, CP-23, CP-30, CP-S1, EV-01, PF-02, PF-05, RM-13, RM-15, RM-37, TJ-02, TJ-12. **Cut:** CP-42, IB-03. **HARD, design-side:** MC-05, PF-08. **CLOSED:** CP-07, CP-38, EV-05, EV-10, EV-12, MC-04, MC-06, RM-20, TJ-O1. **DEFERRED:** CP-O3, EV-15, RM-25, RM-42, TJ-08.",
 "definitionRow": "| **M7: close the canvas gaps in the CLI (metadata + intelligent sync)** | every SIMPLE `cli`/`both` row shipped (payload, flag, verb or spec delta merged on `ryanliu-terum/terum-skills`) and its board surface real: from `desktop/src/backend/tauri` where the adapter can reach it, from `mock/` where the row is app-side; every MEDIUM, HARD, CLOSED, DEFERRED and design-side row has a recorded disposition; `npm run check` and `check:design` green, 87/87 locked |",
 "decisionsForTeddy": [
  "Scope of \"fully functional\": M7 as scoped flips only two of the twelve capability flags (roles, projectMembers). If the greyed controls must light, PF-01, PF-02 and the four eval MEDIUMs (EV-02, EV-09, EV-16, EV-19) come with it.",
  "CP-30 vs TJ-01: invite scoping is dropped, so is the drawn selector kept as a note composer? If not, S7o's `invite --project` comes out.",
  "RM-15: the case-insensitive resolver pays off only if the live team.json keys are capitalised (Terum/SSM/MRF). Confirm against a real repo, or the boards go lowercase instead.",
  "RM-37: keeping the New skill CTA owes three boards (name, description, scope, what happens next), or relabel the CTA to Connect, which is already a picker.",
  "PF-02: a committed follow list publishes an asymmetric social graph to everyone with repo read, permanently, in git history.",
  "PF-01's project heart: `favorites` is uuid-typed (skillIdSchema), so project favourites are either a derived chip or a widened field.",
  "TJ-02: Ryan is being asked to reopen a table his spec closes at rows c/d/e. Is the read-only, chevron-less, unknown-capable host-derived chip an acceptable long interim?",
  "The window floor: keep the shipped 960x600 and draw the two undrawn sub-1150 screens, or raise minWidth/minHeight to D-W1's 1200x720 in one line."
 ],
 "firstSpecOutline": "S7f: batch isolation for install member/project, and the Windows global-placement bug (RM-19, RM-21). Chosen because it is small, untouched by the 45-commit delta, breaks no SIMPLE condition, and is independent on the CLI side once its questionable S7e edge is dropped. NOTE: the instruction described this slot as \"the ls / search payload\", which is S7b's content; batch ids are fixed, so this outline is S7f's rows.\n\n1. Pins. Base f8557c4. Member loop install.ts:47-53, project loop :55-62, the outer catch that returns failure with no value :77, pending written :93-95 and cleared :141, placementHome :241 with its only call site :106, failure(error, value?) at result.ts:6, and the isolation pattern to copy at sync.ts:308-320.\n2. RM-19. Isolate per item: one unplaceable skill is reported by name and the batch continues; the Result is ok:false and carries the successful InstalledResult[]. Batch-fatal classes stay fatal: PromptClosedError, RemoteAccessError, and SafeWriteExhausted (teamRepo.ts:53, thrown :178).\n3. RM-21. Replace the POSIX suffix test with basename/dirname, the platform-correct idiom already at placer/agent-paths.ts:24; state whether placementHome is exported or exercised through installOne.\n4. App half. run.ts's failing-result branch must map frame.value and finish {ok:false, error, value}, and types.ts:6's result arm must carry it, or seven successes out of eight reach no screen. Fix MarketplaceScreen.tsx:37 to pass member/project so tauri/index.ts:72 stops falling through to the single-skill form. Decide whether a screen renders partial success or the error string names what landed.\n5. Sync rule. Unchanged: pending is written before placement and replayed at sync.ts:161-186, and a team with any pending entry is left unstamped, so extra entries self-heal. Say in the PR that isolation leaves one pending entry per failed item.\n6. Tests. install.test.ts: one foreign folder in an eight-skill project batch leaves seven placed and the Result carries them; each of the three fatal classes aborts the batch; placementHome resolves for a Windows-shaped root and every POSIX root already pinned (install.test.ts:126-136 and sync.test.ts:353-359 must stay green). desktop: a run.test.ts case for a failing result that carries a value.\n7. Out of scope. The ls.ts text inherited in this batch's fields (that is S7b), retention policy, and any new flag.",
 "notVerified": [
  "The retirement instruction names RM-11 as S7a's row, but FINAL DESIGN puts RM-11 in S7p and gives S7a four people-file rows (PF-07, PF-03, PF-04, PF-09). Section 4 keeps the instructed table cell and records those four as live so they are not lost from the plan.",
  "Section 8's instruction names S7f but describes \"the ls / search payload\", which is S7b's content. Ids are fixed, so the outline is S7f's rows; the ls.ts text inside S7f's own fields is misfiled and belongs to S7b.",
  "S7f's dependsOn S7e rests on RM-18's notices editing install.ts; nothing at f8557c4 emits notices there. Confirm before treating S7f as blocked.",
  "Teddy's Windows host being ARM64 is a memory note, not re-verified this session; if it is x64 there is no build (platform.ts:15-33) and no end-to-end run from any machine in hand.",
  "Not re-read this session: the eval-engine spec's section 12 line numbers, the 57 design rows' build.py sites, and the app repo's test suite. Every CLI anchor cited was read at f8557c4 in the read-only checkout.",
  "Neither the Ryan bundle nor the Ajay bundle has been sent, as of the last record.",
  "desktop/GAPS.md is still headed \"pinned at b5c0507\" and unchanged by the delta; re-anchor it at 0.1.6 in the first batch that touches it. PF-08 still has no GAPS.md line.",
  "build.py line numbers in the costing are snapshot citations against a file that has since drifted; re-grep by content before applying any design string.",
  "Approval arithmetic (44 Ryan rows, 9 Ajay rows, 57 design rows) is carried from the costing's own counts, not recomputed row by row this session."
 ]
}