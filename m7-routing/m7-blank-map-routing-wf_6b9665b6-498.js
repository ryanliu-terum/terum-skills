export const meta = {
  name: 'm7-blank-map-routing',
  description: 'Verify the desktop blank-map demand list against terum-skills f8557c4 and the measured frames, cost the uncatalogued asks against the SIMPLE bar, refute adversarially, and check the desktop-first batch order',
  phases: [
    { title: 'Confirm', detail: 'one agent per row: field absent today? before-shape from frames, costing delta vs report ask, batch home, bar costing for new rows' },
    { title: 'Refute', detail: 'two lenses per new/moved/divergent row: code-reality and bar-and-scope' },
    { title: 'Order', detail: 'one agent: dependsOn vs the desktop-first order, what lights after each batch' },
  ],
}

const REPO = '/home/teniroo/Projects/SSM/terum-skills'
const REVIEW = '/home/teniroo/Projects/SSM/review/2026-09-08-desktop-blank-map'
const COST = REPO + '/.planning/research/2026-09-07-desktop-app-gap-costing.md'
const SPEC = REPO + '/.planning/specs/2026-09-02-phase-1-build.md'

const BAR = `SIMPLE means ALL of: (1) at most one new field in ONE owner file — people/<handle>.json (guard row (b), the actor's own file) or config.json (via ConfigStore) — or no new field at all; (2) at most one new verb OR one new flag on an existing verb; (3) at most one guard change, and only of the shape "add an existing GuardAction to PEOPLE_ACTIONS" or "add one action string to GuardAction for a row-(b)-shaped self-write" — never a new team.json row (the §6.0 table is closed: rows c/d/e only); (4) tests that fit the existing vitest layout; (5) a one-paragraph spec delta to ONE section; (6) exactly one approver — Ryan (phase-1 spec, ledger, guard, schema) or Ajay (eval-engine spec, receipts, hygiene). ALSO SIMPLE: a purely app-side derivation over committed files that passes the D20 honesty test; a design-side string fix; an app rule. NOT SIMPLE: anything needing a collector, an event log, a machine registry, a change to WHAT the eval engine computes, a team.json schema change, a privacy call, a hook write path that commits on every session start, a new Prompter member threaded through every verb, or a mechanism that does not exist. MEDIUM = a bounded additive change that breaks exactly one SIMPLE condition (stated per row). HARD = a new subsystem or mechanism. CLOSED = already true at the pin. In-process Result/interface fields (LsSkill, LsResult, TeamStatus, StatusResult, SearchHit, ValidateResult, InstalledResult, AppState in run/app.json) are NOT owner-file fields: condition (1) is satisfied by them.`

const BATCHES = `S7a MOOT (RM-11 exports map superseded by frame mode; CP-19 re-placed) | S7b [PF-03,PF-04,PF-07,PF-09] people-file metadata + one guard change, dep S7c, Ryan | S7c [CP-D1,MC-11] config.json label + login --set (MC-11 = the WRITE half), dep [], Ryan | S7d [RM-09,RM-10,RM-22,CP-35] typed decline (frames.ts isDecline) + readers, dep [], Ryan | S7e [RM-08,MC-12,RM-29,CP-40,CP-37,CP-24,CP-39] one-line corrections incl. update returns a report, dep [], Ryan | S7f [RM-30,RM-02,RM-03,RM-38,RM-49] ls/search skill payload, dep [], Ryan | S7g [RM-39,RM-40,RM-44] ls --local structured rows, dep S7f, Ryan | S7h [TJ-04,TJ-05] ls --host admin + invitations, dep S7g,S7c(+S7b), Ryan | S7i [RM-19,RM-21] install per-item isolation + Windows home, dep [], Ryan | S7j [RM-31,RM-32,CP-S3,RM-07,RM-18] sync result, --team, missing clone, approvals predicate, dep S7f,S7i, Ryan | S7k [RM-41,RM-48,RM-S1,TJ-12] status read model: pending, syncedAt, policy, categories, dep [], Ryan | S7l [MC-10] hook on|off verb, dep S7k, Ryan | S7m [EV-07,EV-17,EV-08,EV-11,IB-05,CP-02,CP-S4] eval/validate payloads, dep [], Ajay | S7n [EV-13,CP-11,CP-15,IB-02] receipt reader + §12 UI contract, dep S7m, Ajay | S7o [IB-06,TJ-11,CP-30] endorsements list, invite --cancel, invite --project, dep S7h, Ryan | S7p [RM-01,TJ-10,RM-13] SKILL.md body + project fields, dep S7b,S7c,S7i, Ryan | S7q [AC-06,AC-07,AC-08,CP-13,CP-14,IB-08,RM-27,RM-45,RM-47] no CLI code / flag wiring, dep [], none | S7r app-only [AC-01,AC-02,AC-03,AC-05,AC-10,AC-12,AC-13,AC-14,AC-15,AC-16,MC-02,MC-03,MC-08,IB-01,IB-04,IB-09,CP-21,CP-25,CP-41,RM-12,RM-17,RM-33,RM-34,RM-35], dep [], none | S7s [MC-S1,CP-S1] storage + quarantine, dep S7k | S7t [PF-01,PF-02] favorites+following gated, dep S7b | S7u [EV-03,CP-23,MC-09] receipt additions, dep S7m, Ajay | S7v [EV-01,EV-02,EV-09,EV-16,EV-19] the §12 paragraph, dep S7n, Ajay | S7w [RM-23,RM-24] app seam mechanics, dep S7r | S7x [AC-04,AC-18] reflow/axe, dep S7r | S7y [RM-15] case-insensitive project keys, dep S7h,S7j | S7z [RM-37] new verb, dep S7p | S7aa [MC-01] --cwd on three verbs (MEDIUM), dep S7c,S7j | S7ab [TJ-02] roles model, Ryan-GATED, dep S7h | S7ac [] launch handoff, DONE (app verb c487048 shipped).`

const CONTEXT = `
CONTEXT (settled; do not re-litigate):
- Today is 2026-09-08. Teddy's terum-skills desktop app (desktop/ inside the CLI repo) drives the CLI over FRAME MODE: the shell spawns \`node <entry> --frames <verb> ...\` (desktop/src-tauri/src/lib.rs:39-49) and the adapter reads JSON frames from stdout (desktop/src/backend/tauri/run.ts). The ONLY way a value reaches the app is the verb's result frame \`value\` (src/lib/frames.ts:24, :171-175). Exported functions, ConfigStore reads and library imports are NOT reachable from the app (package.json ships bin-only; the Rust side exposes only cli_spawn/cli_write/cli_kill/read_app_state/host_platform). Any costing verdict of the form "CLOSED because the app reads it through ConfigStore / imports the reader" is void under frame mode. Two adapter facts that matter: run.ts drops \`value\` when the result frame has ok:false; the adapter's zod schemas for search/validate are .passthrough(), so additive fields are safe; hello.protocol stays 1 for additive changes (docs/frame-protocol.md:61-63).
- The CLI repo is ${REPO}, checked out at origin/main f8557c4 (v0.1.6). READ ONLY: never write there, never run git mutations, npm, or tests. Use grep/sed/cat/awk. Cite every claim as file:line AT f8557c4 with a verbatim excerpt. Known f8557c4 anchors: src/commands/ls.ts:18 LsSkill, :20 LsResult, :28 --local early return, :32 team.json parsed, :52 Promise.all over names, :53 parseSkillFrontmatter, :59 latestTree per skill, :60 the four fields, :72 prints Installed, :73 returns authored, :135 local row literal, :151 local return; src/commands/status.ts:17-22 TeamStatus, :23 StatusResult, :32 config read, :45 clone computed, :70 readTeam, :85-86 staleLine/stale, :100 failure-with-value when any team unreadable; src/commands/search.ts:15-19 SearchHit, :42 description matched, :52 batches of 8, :58 hit literal; src/index.ts:42-43 --frames scan+filter, :60 --hook scan, :69-72 CommanderError catch (no result frame); src/lib/frames.ts:22-23 ProgressFrame reserved, :24 ResultFrame, :32 FRAME_VERBS (20, no app), :39-42 FRAME_FEATURES (12 false); src/commands/install.ts:36 InstalledResult {id,team,path,version}; src/commands/uninstall.ts:16 UninstalledResult {id,team,removed}; src/commands/app.ts:58 AppState {schema,node,entry,version,writtenAt}, :134 writeState, :139 exec/open; src/commands/setup.ts:113-115 the "Continuing in the app. Join <target> there." hand-off; src/lib/schema.ts:48 projects, :50 policy, :93-104 configSchema (default_handle :95, email :96, display_name :97, github :98, shared :100, approvals :101, pending :102, placements :103); src/lib/hook.ts:57 hookInstalled, :157 stampPath, :165 stampIsFresh; src/lib/auth.ts:16 GhState, :21 ghState; src/commands/validate.ts:12 ValidateResult; src/commands/update.ts returns Result<void>; src/commands/invite.ts ~:49-54 slackBlock (verify). Re-grep anything you cite; these are hints, not proof.
- Ground truth for today's result shapes: real frames from the built 0.1.6 CLI against a fixture team (acme, handle seed, 3 members, 3 skills, 1 placed skill, 1 project) in ${REVIEW}/frames/{status,ls,ls-local,ls-member,ls-project,search,validate}.jsonl; the LAST line of each file is the verb's result frame and its value is the verb's result. The on-disk inventory (what the clone and config.json hold that no verb returns, with the seam field each would fill) is ${REVIEW}/reports/cli-inventory.md. The report that produced these rows is ${REVIEW}/REPORT.md: section 5.1 (lines 269-305) is the demand list, section 4 (lines 238-256) the verified bugs, section 2 (lines 27-203) the per-screen tables saying which fields each screen needs.
- The gap costing is ${COST}: §1 table lines 55-257 (one row per line, grep '^| <id> |'), §2 paste-ready deltas lines 258-1219 (grep '#### <id> '), §3 app/design rows 1220-1333, §4 MEDIUM/HARD 1334-1447. Its line anchors cite b5c0507 or 85cc276 and MAY HAVE DRIFTED (the app verb c487048 pushed src/cli.ts registrations down by one; install.ts/connect.ts/uninstallMachine.ts moved); re-grep at f8557c4 before quoting.
- THE BAR (verbatim policy every verdict is judged against): ${BAR}
- M7 batches as they stand (winner design + amend, after the batch verifications; a batch = one Codex run + one PR; approver in the name): ${BATCHES}
- Approvers and spec anchors: Ryan owns the phase-1 spec ${SPEC} (§6 "Command behavior" from line 281: one bullet per verb; §6.0 safeWrite at 301; §6.1 setup at 375; §5.1 team.json 152; §5.2 people file 170; §5.3 SKILL.md 196; §5.4 config.json 236; §4.2 local state 103; §8 hook 403), the ledger, guard, schema, and docs/frame-protocol.md. Ajay owns .planning/specs/2026-09-04-eval-engine.md, receipts, hygiene. Tests live in src/commands/__tests__/<verb>.test.ts, src/lib/__tests__/<lib>.test.ts, src/__tests__/{bin,cli,frames-cli,cli-hints,...}.test.ts (all exist; list them if you cite one).
- Your final structured output is the only thing read. Facts with anchors; no padding; never invent a line number.
`

const ROWS = [
  // ---- costed rows from REPORT §5.1 (confirm batch home, field absence, before-shape, ask-vs-costing) ----
  { id: 'RM-30', kind: 'costed', batch: 'S7f', verb: 'ls, search', ask: '`description` on LsSkill and SearchHit (every card\'s description). Value already parsed at ls.ts:53 and matched at search.ts:42.', costing: '§1 line 82, §2 line 905' },
  { id: 'RM-44', kind: 'costed', batch: 'S7g', verb: 'ls --local', ask: 'real fields on ls --local rows instead of prose state (ls.ts:94-110): placement {id, team, version}, shared {id, team}[]; the placements table, Sharing, card health flags.', costing: '§1 line 86, §2 line 1003' },
  { id: 'RM-39', kind: 'costed', batch: 'S7g', verb: 'ls --local', ask: 'health on ls --local rows: ok | update-available | local-changed | ... (fingerprints exist, compared only inside the mutating sync loop).', costing: '§1 line 85, §2 grep' },
  { id: 'RM-40', kind: 'costed', batch: 'S7g', verb: 'ls --local', ask: 'tracked: boolean on the row pushed at ls.ts:135 (the boolean exists at ls.ts:131).', costing: '§1 line 117, §2 line 975' },
  { id: 'RM-48', kind: 'costed', batch: 'S7k', verb: 'status', ask: 'stampedAt / syncedAt on TeamStatus (last sync on Teams and Sync) from run/<team>.stamp mtime; only the boolean stale returns today (status.ts:86).', costing: '§1 line 87, §2 line 1045' },
  { id: 'RM-S1', kind: 'costed', batch: 'S7k', verb: 'status', ask: 'policy {publish, skill_license} on TeamStatus (zod line exists at schema.ts:50; readTeam already called at status.ts:70).', costing: '§1 line 88, §2 line 1073' },
  { id: 'RM-22', kind: 'costed', batch: 'S7d', verb: 'status', ask: 'gh/git/cc presence on the status result (the two green-check rows that assert an unmade probe): ghState already exported (auth.ts:21), git never probed, claude version only inside eval preflight. The costing shaped this as an EXPORTED gitState() beside ghState(); under frame mode an export is unreachable, so the frame-mode form is a `tools` member on StatusResult. PROPOSED MOVE: from S7d (frames/decline batch) to S7k (status read model), leaving S7d with RM-09/RM-10/CP-35. Assess the move.', costing: '§1 line 81, §2 line 863', proposedMove: 'S7k' },
  { id: 'RM-08', kind: 'costed', batch: 'S7e', verb: 'update', ask: 'update advice as a value: update returns Result<void> today (measured: no value key); all eight values are locals at update.ts:16-33.', costing: '§1 line 74, §2 line 765' },
  { id: 'MC-10', kind: 'costed', batch: 'S7l', verb: 'new small verb (hook [on|off]) or a read field on status', ask: 'session-start hook state (hookInstalled already exported, hook.ts:57) for Settings ▸ Sync; the design also draws a toggle. S7l builds a hook verb; the cheaper READ half alone would be a field on status.', costing: '§1 line 110, §2 line 681' },
  { id: 'MC-11', kind: 'costed', batch: 'S7c', verb: 'status (read half) / login --set (write half)', ask: 'config identity (default_handle, email, display_name, github) on the status result so the identity fields stop writing only localStorage. The costing\'s MC-11 is the WRITE half (`login --set`, in S7c); the inventory (rows 25-28) says the READ half has no row. The read half is costed separately as BM-10 (proposed S7k). Confirm S7c keeps only the write half and that nothing at f8557c4 returns the identity block.', costing: '§1 line 67, §2 line 292' },
  { id: 'RM-01', kind: 'costed', batch: 'S7p', verb: 'ls', ask: 'the SKILL.md body (text read at ls.ts:53 and thrown away; parseSkillFrontmatter has match[0].length in hand at schema.ts:153-161).', costing: '§1 line 112, §2 line 709' },
  { id: 'RM-02', kind: 'costed', batch: 'S7f', verb: 'ls, search', ask: 'grants (+ grantsHash) from parsed.grants.normalized (schema.ts:161) on LsSkill and SearchHit: the consent list and the Quality tab chips.', costing: '§1 line 72, §2 line 723' },
  { id: 'RM-03', kind: 'costed', batch: 'S7f', verb: 'ls', ask: 'installer handles behind installCounts (installedBy?: Installer[] on LsSkill) for the uses popover and facepile.', costing: '§1 line 73, §2 line 737' },
  { id: 'EV-07', kind: 'costed', batch: 'S7m', verb: 'validate', ask: 'validate emitting HygieneFinding{code,path,line?,message}[] on ValidateResult (today {name, findings: count, warnings: count}, frames/validate.jsonl) for the six drawn hygiene rows. Note: validate already runs over frames as the adapter\'s read path and must stay prompt-free.', costing: '§1 line 63, §2 line 555' },
  { id: 'EV-13', kind: 'costed', batch: 'S7n', verb: 'new read verb receipt <ref> (or a field)', ask: 'a read-only receipt reader over evals/<id>/<version>/<runId>.json (every verdict, lift, the Evaluated meter). Under frame mode it must be a verb registered in FRAME_VERBS (or a field on an existing verb), never an exported latestReceipt().', costing: '§1 line 66, §2 line 597' },
  { id: 'RM-49', kind: 'costed', batch: 'S7f', verb: 'ls', ask: 'unresolved: boolean on LsSkill mirroring SearchHit (ls.ts:59 degrades to latest "—" with no flag) plus per-row degradation instead of fail-whole at ls.ts:54.', costing: '§1 line 121, §2 line 1059' },
  { id: 'RM-38', kind: 'costed', batch: 'S7f', verb: 'ls', ask: 'a per-skill last-changed date (updated on LsSkill/SearchHit, a latestChange() sibling of latestTree) for the "Recently updated" sort.', costing: '§1 line 84, §2 line 947' },
  { id: 'IB-02', kind: 'costed', batch: 'S7n', verb: 'ls', ask: 'one honest Inbox share row: MY installed/declined ids on ls (the share kind = team.json.global minus my installed[] minus my declined[]). The costing\'s IB-02 is "kind-by-kind derivation of the seven Inbox kinds" (recommended both, approver none on the recommended route) and it sits in S7n, an Ajay batch. Under frame mode the app cannot read my people file, so the derivation\'s INPUT must ride a verb. BM-04 (installed + declined on the `ls member` result, proposed S7f) would supply it. Assess whether IB-02\'s ls input belongs with BM-04 and whether S7n is still the right home for the rest.', costing: '§1 line 105, §2 line 625' },
  { id: 'TJ-04', kind: 'costed', batch: 'S7h', verb: 'ls --host', ask: 'host admin permission per member via one --host flag on ls (team.ts:106-109 reads collaborators?permission=admin today for remove).', costing: '§1 line 89, §2 line 1087' },
  { id: 'TJ-05', kind: 'costed', batch: 'S7h', verb: 'ls --host', ask: 'pending GitHub invitations (team.ts:115-117 already reads them, filtered to one login) via the same --host flag.', costing: '§1 line 90, §2 line 1101' },
  { id: 'PF-03', kind: 'costed', batch: 'S7b', verb: 'ls / status', ask: 'a role field on people/<handle>.json (the member role cell; a self-declared discipline label, NOT the Admin/Member permission).', costing: '§1 line 68, §2 line 308' },
  { id: 'PF-04', kind: 'costed', batch: 'S7b', verb: 'status', ask: 'project members: projects[] on people/<handle>.json (the member Teams column, facepiles).', costing: '§1 line 69, §2 line 322' },
  { id: 'RM-32', kind: 'costed', batch: 'S7j', verb: 'sync', ask: '--team on sync (cli.ts:129 registers only --hook and --prune) for the drawn per-team Sync button.', costing: '§1 line 83, §2 line 933' },
  { id: 'RM-31', kind: 'costed', batch: 'S7j', verb: 'sync', ask: 'delete the args.hook ? guards at sync.ts:353 and :359 so a failed interactive sync still carries per-team outcomes.', costing: '§1 line 116, §2 line 919' },
  { id: 'RM-17', kind: 'costed', batch: 'S7r', verb: 'sync', ask: 'truthful boot rows: progress events the adapter can report. The costing priced RM-17 as SIMPLE·app (poll three machine-local artifacts, NO CLI change) and it sits in S7r (app-only). The frame protocol reserves a `progress` frame that no verb emits (frames.ts:22-23; features.progress false) and the handoff says the 12 false features are what M7 flips. Under frame mode the app cannot stat config.json or run/<team>.stamp (no fs access), so the costed polling design may be unbuildable. Assess: does the app-side polling still work over frames? If not, is "sync emits progress frames per phase and flips features.progress" SIMPLE by the bar (which conditions, which spec section(s): docs/frame-protocol.md and/or §6 sync), and which batch (S7j edits sync.ts) would take it? This is a DECISION for Teddy; give the facts and a lean.', costing: '§1 line 160, §3 line 1261' },
  // ---- the nine uncatalogued asks from REPORT §5.1, the two bugs from §4, and the read halves frame mode reopened ----
  { id: 'BM-01', kind: 'new', batch: 'S7f', verb: 'ls', ask: '`projects?: {name: string; skills: readonly string[]; remotes?: readonly string[]}[]` on LsResult, from the team object already parsed at ls.ts:32 (schema.ts:48). OPTIONAL because ls --local returns at ls.ts:28 before team.json is parsed; omit rather than [] on the local path. Lights: sidebar project rows, the Library project scope, the Marketplace Projects board, per-project counts by fanning out ls project <name> (RM-26 was CLOSED on `ls project` existing; no verb ENUMERATES projects: ls.ts:76, install.ts, publish.ts, sync.ts:421, uninstall.ts, search.ts:41 all take the name as input). Report §5.1 line 274; inventory rows 3-4.', costing: 'uncatalogued' },
  { id: 'BM-02', kind: 'new', batch: 'S7k', verb: 'status', ask: '`clonePath: string` on TeamStatus: status.ts:45 computes `const clone = store.teamClone(team)` and drops it. Lights: the clone path row in Settings ▸ Teams and the Show-in-Finder action. Report §5.1 line 278.', costing: 'uncatalogued' },
  { id: 'BM-03', kind: 'new', batch: 'S7k', verb: 'status', ask: '`joinCommand: string` and `joinBlock: string[]` on TeamStatus, composed the way invite.ts already composes the Slack-ready join block (verify the function name and lines; export it if private). Lights: the Onboarding join block, ShareInvite\'s copyable block. Report §5.1 line 299. Check: does the block need anything status does not have (the remote is binding.remote; the invocation form is args.form)?', costing: 'uncatalogued' },
  { id: 'BM-04', kind: 'new', batch: 'S7f', verb: 'ls member <handle>', ask: 'return the installed list `ls member` already computes and prints at ls.ts:72 (`installed: {id, name, version, scope, since}[]` from member.installed, schema.ts:36-41) and `declined: readonly string[]` (schema.ts:63) on the LsResult of kind member (today value.skills = the AUTHORED list, ls.ts:73, and the print frame contradicts it: frames/ls-member.jsonl). Lights: "what this person installed" on MarketplacePerson; my own installed/declined feed IB-02\'s share rows and Onboarding\'s "later" list. Also assess: is per-skill installedBy (RM-03, same batch) the same information inverted, and should the app use one or both? Report §5.1 line 298; inventory rows 12-13.', costing: 'uncatalogued' },
  { id: 'BM-05', kind: 'new', batch: 'S7i', verb: 'install, uninstall-skill', ask: '`name: string` on InstalledResult (install.ts:36; installOne resolves the SkillRecord so the name is in hand — verify where) and `name: string | null` on UninstalledResult (uninstall.ts:16; uninstallOne has only id+scope — verify whether the name is resolvable from the clone without a new read). The seam\'s InstalledResult/UninstalledResult carry `name` (desktop/src/backend/types.ts:32,34) and the adapter aliases name to item.id today (REPORT §4 row 10). One row touching two §6 bullets (install, uninstall-skill) in ONE section: say whether condition (5) holds. Report §5.1 line 297.', costing: 'uncatalogued' },
  { id: 'BM-06', kind: 'new', batch: 'S7d', verb: 'all verbs (the bin)', ask: 'emit a result frame on a commander usage error: src/index.ts:69-72 catches CommanderError, sets the exit code, and writes NO result frame, so a leading-dash ref becomes "exited with code 1 before reporting a result" in the app (REPORT §4 row 13, measured for 20 argv shapes). Fix: in the catch, when `channel` exists and the error is a commander usage error (not help/version — name the commander error codes), write `channel.result({ verb: <the verb commander was parsing, or the first non-flag argv token>, ok: false, error: <commander message>, exitCode: 1 })`. Spec: docs/frame-protocol.md line 11 ("exactly one result") and line 36 (never --help/--version). Tests: src/__tests__/frames-cli.test.ts or bin.test.ts (verify which drives the bin). Report §5.1 line 302.', costing: 'uncatalogued' },
  { id: 'BM-07', kind: 'new', batch: 'S7d', verb: 'all verbs (the bin)', ask: 'stop the --frames and --hook argv scans at the first `--`: src/index.ts:42-43 (`process.argv.includes(FRAMES_FLAG)` and the filter) and :60 (`argv.includes(\'--hook\')`) scan the whole argv, so `search -- --hook` returns a verb:"sync" refusal frame and a positional literally named --frames is eaten (REPORT §4 row 13; followup-1.md). Fix: scan only argv before the first `--`; filter FRAMES_FLAG only before it. docs/frame-protocol.md line 7 says the flag is position-independent; the paragraph gains "before the first --". Same batch as BM-06 (same file, same doc). Report §5.1 line 302.', costing: 'uncatalogued' },
  { id: 'BM-08', kind: 'new', batch: 'S7f', verb: 'ls', ask: 'batch ls\'s per-skill `git rev-parse` fan-out: ls.ts:52 runs `Promise.all(names.map(...))` with one latestTree child per skill at once (ls.ts:59), while search.ts:52 already batches 8 at a time; measured ls = 142 + 2.46N ms with N concurrent git children (26 at N=100). No field, no verb, no flag: a loop shape change in listSkills. RM-49 (same batch) already rewrites this fail-whole map into per-row degradation, so the chunking rides that edit. Tests: src/commands/__tests__/ls.test.ts (a runner spy asserting at most 8 in flight). Spec: none or the §6 ls bullet. Report §5.1 line 303.', costing: 'uncatalogued' },
  { id: 'BM-09', kind: 'new', batch: 'S7k', verb: 'status', ask: '`ledger` on StatusResult (machine-level, beside version/teams): `placements: {path, id, team, version, scope, placed_at}[]`, `approvals: {id, grants, approved_at}[]`, `shared: {id, source, team}[]` from the config already read at status.ts:32 (schema.ts:100-103). RM-06 (Placements view) was costed CLOSED only because "config.placements is readable via ConfigStore", an in-process route frame mode retired; RM-07 (S7j) exports the approved() predicate, equally unreachable — BM-09 supersedes RM-07\'s CLI half (the app compares approvals[id].grants to RM-02\'s grantsHash). Distinct from RM-44: ls --local is scoped to the discovered roots (global + the cwd project), the machine-wide ledger (Settings ▸ This machine\'s "N in project checkouts", the approvals half) lives only in config.json. Privacy: config.json is machine-local and the reader is the local app. Report §5.1 line 304; inventory rows 30-33.', costing: 'uncatalogued (RM-06 reopened)' },
  { id: 'BM-10', kind: 'new', batch: 'S7k', verb: 'status', ask: '`identity: {default_handle, email, display_name, github} | null` on StatusResult (schema.ts:95-98, config read at status.ts:32): the READ half of MC-11, whose costed row is the write half (`login --set`, S7c). Lights: Settings ▸ Account\'s three identity fields and the footer login, which today write only localStorage. Inventory rows 25-28 ("the read half has no row").', costing: 'uncatalogued (MC-11 read half)' },
  { id: 'BM-11', kind: 'new', batch: 'S7ad (new: app.json fields)', verb: 'app', ask: 'REPORT §4 row 1 (blocker): the GUI-launched app hands the CLI the inherited environment (lib.rs:47-49) while runner.ts spawns bare `git`/`gh`; measured with gh off PATH, invite returns "GitHub CLI (gh) is not installed". CLI half: `terum-skills app` records the launch PATH as ONE app.json field (AppState at app.ts:58, written at :134): `path: string | null` = process.env.PATH ?? null. The adapter halves (bridge.ts:13 accepts it; cli_spawn sets command.env("PATH", …)) are adapter work, excluded from M7. app.json is machine-local state (not an owner file). Approver Ryan (decision walk D1 is his). Tests: src/commands/__tests__/app.test.ts.', costing: 'uncatalogued (bug 1, CLI half)' },
  { id: 'BM-12', kind: 'new', batch: 'S7ad (new: app.json fields)', verb: 'app, setup', ask: 'REPORT §5.1 line 300 (the row the user\'s enumeration did not name; it is in the report\'s table): a joiner landing in the wizard. setup prints "Continuing in the app. Join <target> there." and returns with no team work done (setup.ts:113-115); app.ts:139 execs the app with []; app.json carries no target (bridge.ts:13). CLI half: `target?: string` on AppState written by app when setup hands off a target (verify how the target reaches app.run: AppArgs has no target today), plus nothing else on the CLI side (the app boots to Library and no screen calls backend.setup — shell work, excluded). S7ac (launch handoff) is DONE per the delta; this is the residue. Cost it, and say whether it is worth landing before the shell has an onboarding route.', costing: 'uncatalogued (PR #58 residue)' },
]

const CONFIRM_SCHEMA = { type: 'object', properties: {
  id: { type: 'string' }, kind: { type: 'string' },
  fieldAbsentToday: { type: 'boolean', description: 'true when nothing at f8557c4 returns the asked value over frames' },
  whatExistsToday: { type: 'string', maxLength: 1500 },
  beforeShape: { type: 'string', maxLength: 1200, description: 'the relevant slice of the measured result frame (frames/*.jsonl), quoted' },
  anchors: { type: 'array', items: { type: 'object', properties: { file: { type: 'string' }, line: { type: 'string' }, excerpt: { type: 'string', maxLength: 400 } }, required: ['file', 'line', 'excerpt'] } },
  askVsCosting: { type: 'string', enum: ['same', 'subset', 'diverges', 'n/a'] },
  divergence: { type: 'string', maxLength: 1500 },
  batchHomeOk: { type: 'boolean' }, batchHomeNote: { type: 'string', maxLength: 1500, description: 'why this batch is / is not the right home under frame mode; name the better batch if not' },
  verdict: { type: 'string', enum: ['SIMPLE', 'MEDIUM', 'HARD', 'REFUSE', 'ALREADY-PLACED', 'CLOSED'] },
  conditions: { type: 'string', maxLength: 2500, description: 'new rows: the six bar conditions one by one with evidence; costed rows: which conditions the frame-mode re-shaping touches' },
  edit: { type: 'string', maxLength: 2500, description: 'the exact minimal edit: file:line, type change, where each value comes from' },
  tests: { type: 'string', maxLength: 1200 }, specSection: { type: 'string', maxLength: 600 }, approver: { type: 'string', maxLength: 200 },
  collisions: { type: 'string', maxLength: 1200, description: 'existing rows or batches this duplicates or must be reconciled with' },
  risks: { type: 'string', maxLength: 1500 },
  decisionForTeddy: { type: 'string', maxLength: 1500, description: 'only when a human call is needed; give the lean' },
}, required: ['id', 'kind', 'fieldAbsentToday', 'whatExistsToday', 'beforeShape', 'anchors', 'askVsCosting', 'batchHomeOk', 'batchHomeNote', 'verdict', 'conditions', 'edit', 'tests', 'specSection', 'approver', 'collisions', 'risks'] }

const REFUTE_SCHEMA = { type: 'object', properties: {
  id: { type: 'string' }, lens: { type: 'string' },
  refuted: { type: 'boolean', description: 'true when a MATERIAL sub-claim of the confirmation is wrong (anchor, existence, verdict, edit, home)' },
  corrections: { type: 'array', items: { type: 'string', maxLength: 1200 } },
  verdictShouldBe: { type: 'string', maxLength: 300 },
  evidence: { type: 'array', items: { type: 'object', properties: { source: { type: 'string' }, excerpt: { type: 'string', maxLength: 400 } }, required: ['source', 'excerpt'] } },
}, required: ['id', 'lens', 'refuted', 'corrections', 'verdictShouldBe', 'evidence'] }

const confirmPrompt = (r) => `${CONTEXT}
YOUR ROW: ${r.id} (${r.kind}). Verb: ${r.verb}. Batch it sits in / proposed home: ${r.batch}${r.proposedMove ? ' → proposed move to ' + r.proposedMove : ''}. Costing pointer: ${r.costing}.
THE ASK (from the blank-map report): ${r.ask}

TASK. Work only from the repo at f8557c4, the frames, the inventory, the report and the costing. Steps:
1. Confirm whether anything at f8557c4 returns the asked value over frames (grep the Result types and the frames files). Quote the measured "before" shape from the frames file for this verb (the last line's value, sliced to what matters).
2. For a costed row: grep '#### ${r.id} ' in the costing, read its Field / Verb or flag / Guard / Tests / Spec section / Spec delta lines, and say whether the report's ask is the SAME change, a SUBSET, or DIVERGES (say how). Note any line drift between the costing's anchors and f8557c4. Say whether the batch is a coherent home under frame mode (its delivery must be a verb's result value; name the verb and file) and, if the row proposes a move, assess the move on: same file, same approver, same spec paragraph, and dependency consequences for the batches it leaves and joins.
3. For a new row: cost it against THE BAR, all six conditions with evidence; give the exact minimal edit (file:line at f8557c4, the type change, where each value comes from — it must already be in scope or one call away), the tests in the existing layout (file + case names), the ONE spec section and the paragraph it needs, the approver, and whether the proposed batch is the right home (same file, same approver, same section) or name a better one. List collisions with existing costed rows (ids) and say how they reconcile.
4. Say what the desktop lights when it lands (which screens/fields from REPORT §2), in one line.
5. Risks: anything that silently defeats the row under frame mode (e.g. run.ts drops value on ok:false; status returns failure-with-value when any team is unreadable; the adapter's zod schema; FRAME_VERBS omission for a new verb; a prompt inside a verb the adapter drives through read()).
Do not propose new mechanisms. If the honest answer is REFUSE or CLOSED, say so with the evidence.`

const refutePrompt = (r, c, lens) => `${CONTEXT}
ADVERSARIAL VERIFIER, lens "${lens.key}". ${lens.ask}
THE ROW: ${JSON.stringify({ id: r.id, kind: r.kind, batch: r.batch, proposedMove: r.proposedMove || null, verb: r.verb, ask: r.ask })}
THE CONFIRMATION UNDER TEST: ${JSON.stringify(c).slice(0, 9000)}
Try to REFUTE it. Re-derive every anchor at f8557c4 yourself (grep/sed), re-read the frames file, re-read the costing entry. refuted=true only for a MATERIAL error (a wrong line that changes the edit, a value not actually in scope, a bar condition wrongly judged, a duplicate of an existing row, a wrong batch home, a frame-mode delivery hole). List corrections as imperative sentences an implementer applies; each with its evidence. If everything holds, refuted=false and say what you checked.`

const LENSES = [
  { key: 'code-reality', ask: 'Attack the code claims: does each anchor exist at f8557c4 as quoted? Is the asked value truly absent from every Result type and every frames file? Is the minimal edit really minimal — is each source value in scope at that line or one already-exported call away, or does it need a new read, a new git child, a new Prompter question, or a new import chain? Would the edit change the printed (terminal) output or an existing test expectation? Does a new verb need a FRAME_VERBS entry? Would a value be dropped by run.ts on a failing result?' },
  { key: 'bar-and-scope', ask: 'Attack the costing and the placement: go through the six bar conditions and try to break one (owner-file field, second verb/flag, guard, tests outside the layout, two spec sections, two approvers). Try to show the row duplicates or collides with an existing costed row or batch (name ids), that the batch home is wrong (different file, approver or spec paragraph; a dependency it creates or breaks), or that the row is CLOSED/REFUSED by an existing decision (search the costing §1 table and the decisions the CONTEXT names). Check the spec section you would amend actually exists at the cited line.' },
]

phase('Confirm')
const confirmed = await pipeline(ROWS,
  (r) => agent(confirmPrompt(r), { label: `confirm:${r.id}`, phase: 'Confirm', schema: CONFIRM_SCHEMA, effort: 'high' }),
  async (c, r) => {
    if (!c) return { row: r, confirm: null, refutations: [] }
    const contested = r.kind === 'new' || !!r.proposedMove || c.askVsCosting === 'diverges' || !c.batchHomeOk || !!c.decisionForTeddy
    if (!contested) return { row: r, confirm: c, refutations: [] }
    const refutations = (await parallel(LENSES.map((l) => () => agent(refutePrompt(r, c, l), { label: `refute:${r.id}:${l.key}`, phase: 'Refute', schema: REFUTE_SCHEMA, effort: 'high' })))).filter(Boolean)
    return { row: r, confirm: c, refutations }
  })

const rows = confirmed.filter(Boolean)
log(`Confirmed ${rows.filter((x) => x.confirm).length}/${ROWS.length}; refuted rows: ${rows.filter((x) => x.refutations.some((f) => f.refuted)).map((x) => x.row.id).join(', ') || 'none'}`)

phase('Order')
const ORDER_SCHEMA = { type: 'object', properties: {
  order: { type: 'array', items: { type: 'string' }, description: 'every batch id, first to last, as the M7 document\'s table should list them' },
  violations: { type: 'array', items: { type: 'string', maxLength: 600 } },
  lightsAfter: { type: 'array', items: { type: 'object', properties: { batch: { type: 'string' }, lights: { type: 'string', maxLength: 700 } }, required: ['batch', 'lights'] } },
  moves: { type: 'array', items: { type: 'string', maxLength: 500 }, description: 'row moves between batches the evidence supports, as imperative sentences' },
  notes: { type: 'string', maxLength: 2500 },
}, required: ['order', 'violations', 'lightsAfter', 'moves', 'notes'] }
const slim = rows.map((x) => ({ id: x.row.id, batch: x.row.batch, proposedMove: x.row.proposedMove || null, verdict: x.confirm && x.confirm.verdict, batchHomeOk: x.confirm && x.confirm.batchHomeOk, batchHomeNote: x.confirm && (x.confirm.batchHomeNote || '').slice(0, 500), collisions: x.confirm && (x.confirm.collisions || '').slice(0, 400), refuted: x.refutations.filter((f) => f.refuted).map((f) => ({ lens: f.lens, verdictShouldBe: f.verdictShouldBe, corrections: f.corrections.slice(0, 4).map((s) => s.slice(0, 400)) })) }))
const order = await agent(`${CONTEXT}
YOU ORDER THE M7 BATCHES SO THE DESKTOP LIGHTS SOONEST. Teddy's instruction, binding: description on LsSkill and SearchHit first (RM-30), then projects on LsResult (BM-01), then structured fields on ls --local rows (RM-44), then the new TeamStatus members (RM-48, RM-S1, clonePath BM-02, RM-22, MC-11's read half BM-10); everything else keeps its current order (S7a..S7ac as listed in CONTEXT, with S7a MOOT and S7ac DONE skipped). The proposed placements and moves, with their verification status: ${JSON.stringify(slim)}. Proposed new batch S7ad = [BM-11, BM-12] (app.json fields), approver Ryan, no dependsOn.
Rules: a batch must never be listed before a batch in its dependsOn (the queue runs batches in listed order among those whose dependencies are satisfied, max 2 at once, so the list order is the priority). Frame-mode bug fixes that block long verbs (BM-06/BM-07 in S7d, BM-11 in S7ad) are blockers on the Mac and should sit right after the four desktop-lighting batches unless a dependency forbids it. Use REPORT §2 (lines 27-203) to say, for each of the first eight batches in your order, which screens or fields light when it lands (be concrete: board names and fields). Report every dependsOn violation you had to resolve and any row move the evidence supports (e.g. RM-22 to S7k, IB-02's ls input to BM-04). Output the full order.`, { label: 'order', phase: 'Order', schema: ORDER_SCHEMA, effort: 'high' })

return { rows, order }