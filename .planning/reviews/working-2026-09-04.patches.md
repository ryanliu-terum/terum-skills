# ultrareview proposed patches — NOT YET ADJUDICATED

Inputs to adjudication, never a substitute for it (root CLAUDE.md: agent findings are hypotheses).

## Proposed patches — `/Users/ryanliu/Documents/Terum/terum-codex/m1-plumbing` (working tree, nothing applied)

16 confirmed findings, 20 edits across 10 files (8 source, 2 test-support). Ordered by finding severity.

---

### 1. CRITICAL — CLI hangs forever after every command (`src/lib/prompt.ts:18`, `src/index.ts:22`)

Also fixes the related HIGH "questions never settle on stdin EOF". One rewrite covers both: create the readline interface lazily, reject on `'close'` instead of leaving the promise pending, and expose `close()` on a **new** `TerminalPrompter` type so the verb-facing `Prompter` interface (and `ScriptedPrompter implements Prompter`) stays untouched.

```diff
-import { createInterface } from 'node:readline/promises';
+import { createInterface, type Interface } from 'node:readline/promises';
+
+/** Only index.ts sees close(); verbs stay on Prompter. */
+export interface TerminalPrompter extends Prompter { close(): void; }
+
+/** stdin lives behind the Prompter boundary, so the TTY probe is exported from here. */
+export function stdinIsTTY(): boolean { return Boolean(stdin.isTTY); }

-export function terminalPrompter(): Prompter {
-  const terminal = createInterface({ input: stdin, output: stdout });
+export function terminalPrompter(): TerminalPrompter {
+  let terminal: Interface | undefined;
+  let ended = false;
+  const open = (): Interface => { … terminal.once('close', () => { ended = true; }); return terminal; };
+  // readline never settles question() on EOF → node exits 13 with no error message.
+  const ask = async (question: string): Promise<string> => { … race question() against 'close' … };
   return {
-    async confirm(question) { const answer = await terminal.question(`${question} [y/N] `); … },
+    async confirm(question) { return /^(y|yes)$/i.test((await ask(`${question} [y/N] `)).trim()); },
     …
+    close() { terminal?.close(); terminal = undefined; },
   };
 }
```

```diff
-import { terminalPrompter } from './lib/prompt.js';
+import { Prompter, terminalPrompter } from './lib/prompt.js';
-async function execute(invoke: (io: ReturnType<typeof terminalPrompter>) => …): Promise<void> {
+async function execute(invoke: (io: Prompter) => …): Promise<void> {
   const io = terminalPrompter();
-  const outcome = await invoke(io);
-  if (!outcome.ok) { io.print(outcome.error ?? 'Command failed'); process.exitCode = 1; }
+  try {
+    const outcome = await invoke(io);
+    if (!outcome.ok) { io.print(outcome.error ?? 'Command failed'); process.exitCode = 1; }
+  } finally {
+    io.close();   // a live interface refs stdin and keeps the loop alive forever
+  }
 }
```

---

### 2. CRITICAL — `team create` remote has no owner (`src/commands/team.ts:39-42`)

Folded into one `create()` rewrite that also closes two HIGHs on the same function (partial-state-on-validation-failure, orphaned repo with no rollback):

```diff
+    const stored = args.remote ? normalizeRemote(args.remote) : undefined;   // parse BEFORE any side effect
+    const clone = store.teamClone(args.name);
+    await requireEmptyClone(clone); ownedClone = clone;                      // make a retry possible
-      const repo = args.org ? `${args.org}/${args.name}` : args.name;
-      const created = await runner.run('gh', ['repo','create',repo,'--private'], …);
-      remote = `https://github.com/${repo}.git`;
+      // gh repo create <name> creates under the AUTHENTICATED USER.
+      const owner = args.org ?? (await resolveOwner(runner, token, identity.github));
+      const repo = `${owner}/${args.name}`;
+      remote = await createGitHubRepo(repo, Boolean(args.org), token, runner);
+      createdRepo = repo;
-    await bootstrap(remote, clone, args.name, identity, runner);
+    await bootstrap(remote, clone, args.name, identity, runner, token);
-  } catch (error) { return failure(…); }
+  } catch (error) {
+    if (ownedClone) await rm(ownedClone, { recursive: true, force: true });
+    if (createdRepo) { /* gh repo delete --yes, else print the manual-cleanup URL */ }
+    return failure(…);
+  }
```

`createGitHubRepo` prefers the URL `gh` prints, falls back to `https://github.com/<owner>/<name>.git`, and uses the REST API when `gh` is absent (spec §6 promises the PAT path works with no gh).

---

### 3. HIGH — Prompter-boundary lint rules are bypassable (`eslint.config.js:13`)

```diff
-'no-restricted-imports': ['error', { paths: [{ name: 'node:readline' }, { name: 'node:readline/promises' }] }],
+'no-restricted-imports': ['error', { patterns: [
+  { group: ['readline', 'readline/promises', 'node:readline', 'node:readline/promises'], … },
+  { group: ['process', 'node:process'], importNames: ['stdin','stdout','stderr'], … },  // the prompt.ts idiom
+  { group: ['console', 'node:console'], … },
+]}],
 'no-restricted-properties': ['error',
   { object: 'process', property: 'stdin', … }, { object: 'process', property: 'stdout', … },
+  { object: 'process', property: 'stderr', … },
 ],
```
Plus two assertions in `prompt.test.ts` for the two bypasses that lint clean today.

---

### 4-6. HIGH — `team join`: wrong clone, stale roster, broken handle binding (`src/commands/team.ts:61-70, 86`)

```diff
-const existingTeam = … normalizeRemote(team.remote) === normalizeRemote(target.remote));
+const existingTeam = … sameRemote(team.remote, target.remote));
 const team = args.as ?? existingTeam?.[0] ?? remoteName(target.remote);
+const configured = configBefore.teams[team];
+if (configured && !sameRemote(configured.remote, target.remote)) throw new Error(`Team ${team} is already configured for … --as <local-name> …`);
-try { await readFile(pathJoin(clone,'team.json'),'utf8'); } catch { await cloneTeam(target.remote, clone, runner); }
+await openClone(target.remote, clone, runner);   // verifies origin, then fetch + reset --hard origin/main
-const identity = await resolveHandle(suggested, teamDocument, clone, io);
+const bound = configured?.handle;                 // spec §5.4: immutable once the people file exists
+const identity = boundLanded ? { ...suggested, handle: bound! } : await resolveHandle(…);
…
-config.default_handle = identity.handle;
+config.default_handle ??= identity.handle;        // a per-team replacement must not become the global default
```

---

### 7. HIGH — `login` clobbers the per-team handle and stored PAT (`src/commands/login.ts:17`)

```diff
-result.config.teams[args.team] = { remote: args.remote, handle: result.identity.handle, token: result.token };
+const bound = result.config.teams[args.team];
+if (bound?.handle && bound.handle !== result.identity.handle) io.print(`Keeping the existing handle ${bound.handle} …`);
+result.config.teams[args.team] = {
+  remote: args.remote,
+  handle: bound?.handle ?? result.identity.handle,
+  token: result.token ?? bound?.token ?? null,   // authenticateCreator returns null when gh is authed
+};
```

---

### 8. HIGH — creator PAT never reaches git (`src/commands/team.ts:45`, `src/lib/teamRepo.ts:104`)

New `gitCredentials(token)` in teamRepo.ts returns `-c credential.helper='!f() { … "$TERUM_SKILLS_TOKEN"; }; f'` plus the env var — secret stays out of argv and out of `.git/config`. Threaded through `bootstrap`'s `git()` helper, `git push -u origin main`, and `cloneTeam(remote, clone, runner, token)` (new optional 4th arg). Combined with the rollback above, a failed push no longer orphans a private repo.

---

### 9. HIGH — non-TTY gh gate is dead code (`src/lib/auth.ts:16`)

```diff
-if (!(dependencies.isTTY ?? (() => true))()) return false;
+if (!(dependencies.isTTY ?? stdinIsTTY)()) return false;
```
Single-point fix — no production caller injects `isTTY`, and `stdinIsTTY` lives in the one file exempt from the boundary rules.

---

### 10-11. HIGH — write guard: self-authorizing ownership + hand-rolled frontmatter (`src/lib/guard.ts:18-25`)

```diff
-const source = tree.after(skillFile) ?? tree.before(skillFile);
-const sourceAuthor = /^\s*author:\s*["']?([^"'\n]+)["']?\s*$/m.exec(source)?.[1];
-return sourceAuthor !== undefined && sourceAuthor.trim().toLowerCase() === author?.trim().toLowerCase();
+const source = before ?? after;              // an existing skill is judged by origin/main, not by the diff
+if (!skillAuthorIs(source, author)) return false;
+if (before !== undefined && after !== undefined && !skillAuthorIs(after, author)) return false;  // no hand-off
…
+function skillAuthorIs(source: string, author: string): boolean {
+  const parsed = parseSkillFrontmatter(source);   // the sanctioned parser: metadata.author, fenced frontmatter
+  return parsed.ok && parsed.data.metadata.author.trim().toLowerCase() === author.trim().toLowerCase();
+}
```
Requires updating `guard.test.ts` fixtures to real frontmatter (edit 20) — the current bare `author: …` strings would otherwise change which path throws first on line 19.

---

### 12. HIGH — `archivedRemovedOnly` re-admits/archives arbitrary handles (`src/lib/guard.ts:50`)

```diff
-return sameExcept(…) && before.archived.includes(handle) && after.archived.every(i => i !== handle) && after.archived.length === before.archived.length - 1;
+const expected = before.archived.filter((item) => item !== handle);
+return sameExcept(…) && before.archived.includes(handle)
+  && expected.length === before.archived.length - 1 && after.archived.length === expected.length
+  && expected.every((item, index) => item === after.archived[index]);   // pin the rest, like archivedAppendedOnly
```

---

### 13-14. HIGH — `safeWrite` stages unguarded files, and never checks the clone's origin (`src/lib/teamRepo.ts:14, 36`)

```diff
+  const origin = await runner.run('git', ['remote','get-url','origin'], { cwd: root });
+  if (!sameRemote(origin.stdout, remote)) throw new Error(`${root} is a clone of … refusing to write.`);
…
   await applyTree(root, files, tree);
-  await requireGit(runner, ['add', '--all'], root);
-  guard(tree, options);
-  if (tree.changedPaths.length === 0) return;
-  await requireGit(runner, ['commit', '-m', `terum-skills: ${options.action}`], root);
+  guard(tree, options);                       // authorize before anything reaches the index
+  if (tree.changedPaths.length === 0) return;
+  const paths = [...tree.changedPaths];
+  await requireGit(runner, ['add', '--', ...paths], root);
+  await requireGit(runner, ['commit', '-m', `terum-skills: ${options.action}`, '--', ...paths], root);
```
`git add <pathspec>` records removals, and every changedPath with `after === undefined` was tracked in `before`.

---

### 15. HIGH — `--remote <path>` throws after the push (`src/lib/teamRepo.ts:85`)

`normalizeRemote` gains a local-path branch (`/srv/git/team.git`, `./x`, `~/x`, `file://`, `C:\…` → `file/<resolved>`), plus an exported tolerant `sameRemote()` used by the three comparison sites above. `remoteName` and `isGitHubRemote` behave identically for every remote they handle today.

---

### 16-18. HIGH — remaining `src/commands/team.ts` / `src/lib/auth.ts` fixes

- **`parseJoinTarget` (team.ts:127)** — `/^[^/]+\/[^/]+$/` matches `git@gitlab.com:acme/skills` and `../team`. Replaced with a real GitHub owner/repo pattern (no `@`, no `:`, no leading dot), and full GitHub URLs now route through the invitation flow.
- **`collectGenericIdentity` (team.ts:97-105)** — deleted; `create()` calls `collectIdentity` instead, restoring the missing `github.toLowerCase()` default (`handleSchema`'s regex runs before its lowercasing transform, so `RyanLiu` currently throws a raw Zod error).
- **PAT probe (auth.ts:51)** — replaced with a `fetch`-based `probeGitHubToken()`; the branch is entered precisely when gh may be missing, and `systemRunner` rejects on ENOENT after the secret was already typed. New `ghInstalled()` also drives `createGitHubRepo`'s REST fallback.

---

### Test-support edits (required by the above, not optional)

| File | Why |
|---|---|
| `src/lib/__tests__/fixtures.ts:35` | `mappedRunner` must map the bare path back to the public URL in **output**, or the new origin assertion rejects `join.test.ts` / `create.test.ts`. |
| `src/lib/__tests__/guard.test.ts:4,11,19` | Fixtures need real YAML frontmatter for the sanctioned parser; adds regression cases for the author-flip and the body-`author:` bypasses. |

Suggested additional regression tests (not written above): an untracked `.DS_Store` in the clone must not appear in the pushed commit; `before ['me','alice','bob'] → after ['alice','carol']` under `join` must be refused; joining `orgB/skills` after `orgA/skills` must fail with the `--as` hint rather than pushing into orgA.
