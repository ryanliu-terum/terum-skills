
## Triage — 33 confirmed → 8 mechanical · 20 clear · 5 fork · 0 declined

Buckets are derived in code from each finding's ratings (single-fix gate: trivial + low risk + isolated + patch = mechanical). NOTHING here has been applied.

### Mechanical — one fix, trivial / low-risk / isolated, patch supplied; batch-apply on ONE confirmation (8)

- **high — resolveVersion accepts any commit-ish and returns the repo ROOT tree, so `install <skill>@<commit>` places the whole team repo as the skill** — `src/lib/version.ts:9`
  - Root cause: `resolveVersion` validates the *shape* of what git returned instead of validating *what the user pinned*. At src/lib/version.ts:9 a supplied version becomes the expression `${version}^{tree}`, and git's `^{tree}` peeler accepts any commit-ish: a branch, tag, or commit SHA peels to that commit's ROOT tree. The only guard, line 13 `if (!/^[0-9a-f]{40}$/i.test(tree))`, is satisfied by the root tree (verified in the worktree: `git rev-parse --verify 'HEAD^{tree}'` -> `3dd54475888ce809097eff985a14e15e226b9d51`, and `git cat-file -t main` -> `commit`). Because nothing downstream re-checks — install.ts:150-153 `parseRef` takes everything after the last `@` verbatim, install.ts:72 passes it straight in, install.ts:88 `materializeVersion` checks the tree out wholesale, and placer.ts:47 `place()` copies whatever directory it is handed with no SKILL.md check — `install <skill>@<commit>` materializes the entire team repo (team.json, people/, evals/, every other skill) and copies it into `~/.claude/skills/<skill>`. The spec is explicit that this must not happen: the doc comment on line 7 says "resolve exactly a skill tree, never a commit or a history walk", .planning/specs/2026-09-01-team-skill-sharing.md:101 defines `@<version>` as "the skill's content hash", and D13 calls the cache "pinned checkouts by full tree hash". The same unvalidated value also arrives from the team repo, not just the CLI: install.ts:40 replays `item.version` out of another teammate's `people/<handle>.json` (schema.ts:26 only constrains it to 40 characters) through the same call. (src/lib/version.ts:9)
  - Fix: In src/lib/version.ts, keep the `^{tree}` resolution but add a guard that runs only on the user-supplied branch (`if (version) await assertSkillTree(...)` after the 40-hex check on line 13). The helper makes two read-only git calls in the clone: `git cat-file -t <version>` must report `tree` (this rejects a commit SHA, a branch, or a tag outright, which is exactly what the line-7 doc comment promises), and `git ls-tree --name-only <tree> -- SKILL.md` must print `SKILL.md` (this rejects the repository root tree and any other non-skill tree, and covers a repo that oddly carries a top-level SKILL.md). Both invocations verified against git 2.50.1 in this worktree. The `version === undefined` path is untouched because `HEAD:skills/<name>` is already path-scoped, so the stubbed-runner unit test at src/lib/__tests__/version.test.ts:9-12 keeps passing. Add the adversarial cases (commit SHA, `main`, root tree hash, plus the still-valid abbreviated skill tree) to src/lib/__tests__/version.test.ts.

```diff
--- a/src/lib/version.ts
+++ b/src/lib/version.ts
@@ -11,7 +11,20 @@
   if (result.code !== 0) throw new Error(`Could not resolve version ${version ?? 'latest'} for ${name}: ${(result.stderr || result.stdout).trim()}`);
   const tree = result.stdout.trim();
   if (!/^[0-9a-f]{40}$/i.test(tree)) throw new Error(`Resolved version for ${name} is not a full tree hash.`);
+  if (version) await assertSkillTree(clone, name, version, tree, runner);
   return tree.toLowerCase();
 }
 
+/**
+ * A pin is a skill's own content hash (§7). `<rev>^{tree}` peels a commit, tag, or branch to the
+ * repository ROOT tree, itself a well-formed 40-hex hash, so the shape check above cannot tell the
+ * two apart and `install <skill>@<commit>` would place the whole team repo as the skill.
+ */
+async function assertSkillTree(clone: string, name: string, version: string, tree: string, runner: Runner): Promise<void> {
+  const type = await runner.run('git', ['cat-file', '-t', version], { cwd: clone });
+  if (type.code !== 0 || type.stdout.trim() !== 'tree') throw new Error(`Version ${version} for ${name} is not a skill content hash; pin a skill tree hash, not a commit, tag, or branch.`);
+  const listed = await runner.run('git', ['ls-tree', '--name-only', tree, '--', 'SKILL.md'], { cwd: clone });
+  if (listed.code !== 0 || listed.stdout.trim() !== 'SKILL.md') throw new Error(`Version ${version} for ${name} is not a skill tree: it has no SKILL.md.`);
+}
+
 /**
--- a/src/lib/__tests__/version.test.ts
+++ b/src/lib/__tests__/version.test.ts
@@ -11,4 +11,17 @@
     expect(tree).toBe('a'.repeat(40));
   });
 
+  it('refuses a commit-ish pin, which peels to the repository root tree', async () => {
+    const fixture = await bareTeam();
+    await pushFromSeed(fixture.seed, 'skills/sample/SKILL.md', '---\nname: sample\ndescription: sample\nlicense: UNLICENSED\nmetadata:\n  id: 11111111-1111-4111-8111-111111111111\n  author: Me <me@example.com>\n  terum-category: testing\n---\n');
+    const clone = await cloneWithIdentity(fixture.bare, join(fixture.root, 'clone'));
+    const commit = (await git(['rev-parse', 'HEAD'], clone)).trim();
+    const rootTree = (await git(['rev-parse', 'HEAD^{tree}'], clone)).trim();
+    const skillTree = (await git(['rev-parse', 'HEAD:skills/sample'], clone)).trim();
+    await expect(resolveVersion(clone, 'sample', commit)).rejects.toThrow(/not a skill content hash/);
+    await expect(resolveVersion(clone, 'sample', 'main')).rejects.toThrow(/not a skill content hash/);
+    await expect(resolveVersion(clone, 'sample', rootTree)).rejects.toThrow(/no SKILL\.md/);
+    expect(await resolveVersion(clone, 'sample', skillTree.slice(0, 8))).toBe(skillTree);
+  });
+
   it('materializes an immutable skill tree from a real bare repository', async () => {
```

- **high — Post-join endorsed install ignores `person.declined`/`installed`, silently resurrecting refused skills and erasing the decline record** — `src/commands/team.ts:153`
  - Root cause: The post-join endorsement offer builds its candidate list from `team.json.global` alone and never reads the joining member's own `people/<handle>.json` — the file that carries the `installed` and `declined` records which are what make endorsement opt-in (spec D14: "declines are recorded and never re-asked"; §6 uninstall: a declined id "suppresses every automatic placement of it (§5.2)"). `installOne` is written for an *explicit* re-install, so it deliberately strips the id from `declined` (install.ts:108) and pushes the people file, and `ensureConsent` returns early on the still-cached `config.approvals[skill.id]` (install.ts:123). Feeding an automatic path an unfiltered list therefore does more than re-offer: one `y` re-places every refused skill and permanently deletes the decline record in the shared repo, with no per-skill question. The sibling automatic path in `sync` (sync.ts:116) computes exactly the right candidate set; the join path is a second, independent copy of that rule that omits the filter. (src/commands/team.ts:153)
  - Fix: In `join()` in src/commands/team.ts, after the safeWrite loop and roster print, read the people file that joinMutation just pushed (`readPerson(clone, identity.handle)` from src/lib/skills.js — safeWrite's `finally` fetches and hard-resets to origin/main, so the file is on disk; the existing roster read at team.ts:215-216 already proves it) and apply sync's filter before both the count and the loop: `.filter((id) => !person.installed.some((entry) => entry.id === id) && !person.declined.includes(id))`. The existing `endorsed.length &&` guard then skips the prompt entirely when nothing is left. Add a collocated join.test.ts case for the archived-rejoin path with a pre-recorded `declined` id.

```diff
--- a/src/commands/team.ts
+++ b/src/commands/team.ts
@@ -8,6 +8,7 @@
 import { normalizeRemote, remoteName, remoteToGitUrl } from '../lib/remote.js';
 import { Result, failure, success } from '../lib/result.js';
 import { Runner, systemRunner } from '../lib/runner.js';
 import { Person, Team, parseJson, parseOrExplain, personSchema, teamNameSchema, teamSchema } from '../lib/schema.js';
+import { readPerson } from '../lib/skills.js';
 import { cloneOrigin, cloneTeam, MutableTree, openTeamRepo, treeText } from '../lib/teamRepo.js';
 import { installOne } from './install.js';
@@ -150,10 +151,12 @@
     // M2: the post-join endorsement offer deliberately reuses installOne, including its
     // individual allowed-tools consent and persisted approval record. The set prompt itself is
-    // one question, as §6 requires.
-    const endorsed = (await readTeam(clone)).global;
+    // one question, as §6 requires. §5.2/D14: an automatic path never re-offers what this member
+    // already installed or explicitly declined — installOne clears `declined`, so an unfiltered
+    // offer would erase the refusal — so it applies the same filter `sync` does.
+    const person = await readPerson(clone, identity.handle);
+    const endorsed = (await readTeam(clone)).global.filter((id) => !person.installed.some((entry) => entry.id === id) && !person.declined.includes(id));
     if (endorsed.length && await io.confirm(`Install ${endorsed.length} team-endorsed skill(s)?`)) {
       for (const id of endorsed) await installOne({ team, id, store, runner }, io);
     }
     return success({ team, handle: identity.handle, rejoined, roster });
   } catch (error) {
     return failure(error instanceof Error ? error.message : String(error));
--- a/src/commands/__tests__/join.test.ts
+++ b/src/commands/__tests__/join.test.ts
@@ -209,4 +209,16 @@
     expect((await store.read()).approvals[tool]).toBeDefined();
     expect(JSON.parse(await git(['show', 'main:people/me.json'], fixture.bare)).installed.map((entry: { id: string }) => entry.id)).toEqual(expect.arrayContaining([plain, tool]));
   });
+
+  it('a rejoin never re-offers a skill this member declined, and never erases the decline', async () => {
+    const plain = '77777777-7777-4777-8777-777777777777';
+    const { fixture, store, runner } = await setup({ people: { ghost: person('ghost', { declined: [plain] }) }, archived: ['ghost'] });
+    await pushFromSeed(fixture.seed, 'skills/plain/SKILL.md', `---\nname: plain\ndescription: plain\nlicense: UNLICENSED\nmetadata:\n  id: ${plain}\n  author: Seed <seed@example.com>\n  terum-category: testing\n---\n`);
+    await pushFromSeed(fixture.seed, 'team.json', `${JSON.stringify({ layout_version: 2, name: 'team', categories: [], global: [plain], projects: {}, archived: ['ghost'], policy: { publish: 'pr', skill_license: 'UNLICENSED' } })}\n`);
+    const io = new ScriptedPrompter(answers('ghost', 'Ghost Again', 'ghost@example.com'), [false]);
+    const result = await join({ target: REMOTE, config: store, runner }, io);
+    if (!result.ok) throw new Error(result.error);
+    expect(io.askedAbout('team-endorsed')).toBe(false);
+    expect(JSON.parse(await git(['show', 'main:people/ghost.json'], fixture.bare)).declined).toEqual([plain]);
+  });
 });
```

- **medium — share's name-collision check reads the un-pulled clone, so a stale clone clobbers or fails cryptically** — `src/commands/share.ts:48`
  - Root cause: share reads the team clone from disk with no fetch (src/commands/share.ts:46-48) to decide the repo-wide name-uniqueness invariant, but enforces it via a write that runs against a completely different view: repo.safeWrite (line 58) fetches and hard-resets to origin/main before running the mutation, and mirrorToTree (line 175) then deletes every tracked path under skills/<name>/ the local source lacks and overwrites the rest. Nothing between the read and the write revalidates. Two failures follow. (1) Silent clobber: if the same author already shared skills/<name> from another machine, guard.ownsSkill (src/lib/guard.ts:71-79) sees a matching pre-image author and permits the write, so the upstream folder is destructively mirrored over and re-identified with a newly minted UUID, orphaning the other machine's `shared` entry. (2) Cryptic late failure: a different author's folder is refused by the guard with "Write guard refused skills/<name>/SKILL.md for share by <handle>" — after the user's own SKILL.md has already been rewritten at line 55. The codebase's own precedent contradicts this placement: every other safeWrite mutation re-derives its write from tree.before() inside the mutation (install.ts:105, uninstall.ts:66-70, sync.ts:152-156), and team join checks handle uniqueness inside joinMutation (team.ts:172), throwing HandleCollisionError for the caller to catch — the comment at team.ts:170 records that safeWrite's finally leaves the clone clean when a mutation throws. share is the only site that decides an invariant outside the mutation. (src/commands/share.ts:47)
  - Fix: In src/commands/share.ts:58, replace the bare `mirrorToTree` arrow with a block that first checks the freshly reset tree — `if (tree.paths(`skills/${name}/`).length > 0) throw new Error(`Skill name ${name} already exists in team ${team}; choose a unique name.`)` — and only then mirrors. Keep the cheap pre-check at lines 47-48 as the fast, no-write abort for the common (fresh-clone) case. This mirrors team.ts:172 joinMutation, which already decides handle uniqueness inside the mutation and throws for the caller; safeWrite's finally block resets the clone and releases the lock on the throw, so nothing is left behind. Add the share.test.ts variant that clones first, then pushes a same-name, same-author skill from the seed, then shares.

```diff
--- a/src/commands/share.ts
+++ b/src/commands/share.ts
@@ -55,7 +55,12 @@
     await writeFile(join(source, 'SKILL.md'), updated, 'utf8');
     const files = await sourceFiles(source);
     const repo = openTeamRepo(clone, binding.remote, runner);
-    await repo.safeWrite((tree) => mirrorToTree(tree, `skills/${name}`, files), { action: 'share', handle: binding.handle, author, token: binding.token, message: `${binding.handle}: share ${name}` });
+    // The check above read a clone that may be days old; safeWrite hands the mutation the freshly
+    // reset tree — the only view guaranteed current — so the repo-wide uniqueness invariant is
+    // decided there too, exactly as `team join` decides handle uniqueness inside its mutation.
+    await repo.safeWrite((tree) => {
+      if (tree.paths(`skills/${name}/`).length > 0) throw new Error(`Skill name ${name} already exists in team ${team}; choose a unique name.`);
+      mirrorToTree(tree, `skills/${name}`, files);
+    }, { action: 'share', handle: binding.handle, author, token: binding.token, message: `${binding.handle}: share ${name}` });
     const baseline = await canonicalDigest(source);
     await store.update((fresh) => { fresh.shared[id] = { source, team, baseline }; });
     return success({ id, name, reconciled: description.length > 0 });
--- a/src/commands/__tests__/share.test.ts
+++ b/src/commands/__tests__/share.test.ts
@@ -291,6 +291,23 @@
     expect(await readFile(join(source, 'SKILL.md'), 'utf8')).toBe(original);
     expect((await second.read()).shared).toEqual({});
   });
+
+  it('refuses a name collision that reached origin after the local clone was taken', async () => {
+    const fixture = await bareTeam();
+    const store = createConfigStore(join(fixture.root, 'state'));
+    await cloneWithIdentity(fixture.bare, store.teamClone('team'));
+    await store.update((config) => { config.display_name = 'Me'; config.email = 'me@example.com'; config.teams.team = { remote: fixture.bare, token: null, handle: 'seed' }; });
+    // origin/main moves after the clone: the same name, the same author, from another machine.
+    await pushFromSeed(fixture.seed, 'skills/sample/SKILL.md', '---\nname: sample\ndescription: theirs\nlicense: UNLICENSED\nmetadata:\n  id: 11111111-1111-4111-8111-111111111111\n  author: Me <me@example.com>\n  terum-category: testing\n---\ntheirs\n');
+    const source = join(fixture.root, 'sample'); await mkdir(source);
+    await writeFile(join(source, 'SKILL.md'), '---\nname: sample\ndescription: mine\nmetadata:\n  terum-category: testing\n---\n');
+    const sha = await originSha(fixture.bare);
+    const result = await run({ path: source, team: 'team', config: store }, new ScriptedPrompter([], [true]));
+    expect(result).toMatchObject({ ok: false, error: expect.stringContaining('choose a unique name') });
+    expect(await originSha(fixture.bare)).toBe(sha);
+    expect(await git(['show', 'main:skills/sample/SKILL.md'], fixture.bare)).toContain('theirs');
+    expect((await store.read()).shared).toEqual({});
+  });
 
   it('mints one UUID before a retry and shares one committed skill identity', async () => {
     const fixture = await bareTeam();
```

- **medium — Declining consent deletes durable pending intent that a previous interrupted install left behind** — `src/commands/install.ts:79`
  - Root cause: `installOne` de-dups the pending push (install.ts:74) but does not de-dup its rollback (install.ts:79). `samePending` (install.ts:190) intentionally compares only `op`/`id`/`team`/`scope` and ignores `started`, so the decline path's `filter` removes whichever single matching entry happens to be in `config.pending` — not necessarily the one this call added. When a matching entry already exists, line 74 skips the push entirely, yet line 79 still deletes. That pre-existing entry is exactly the durable intent left by an earlier attempt that got past consent, placed the folder, wrote `placements`, and then died in the people-file `safeWrite` (install.ts:101-110) — the case the spec calls out ("If the safeWrite exhausts its deadline the skill is placed but untracked; ... leaves the entry for the next `sync` to finish", .planning/specs/2026-09-02-phase-1-build.md:310) and that install.test.ts:31 tests. Consent is re-prompted on such a retry whenever `grants.ok` is false (ensureConsent short-circuits on a stored approval only for well-formed grants, install.ts:116-123) or when the skill's grant hash changed, so a "no" at that second prompt silently erases the first attempt's recovery record. The comment's premise ("nothing observable moved") is true of this call but false of the entry it deletes: the folder is on disk and in the ledger, and sync.ts:47-56 replays `pending` to complete the people-file write. After the erasure the placement degrades into an orphan that sync.ts:138-149 can only offer for manual adoption interactively, and that `sync --hook` merely defers forever (sync.ts:148). (src/commands/install.ts:79)
  - Fix: In the `catch` at src/commands/install.ts:77-81, tighten the filter predicate to `!(samePending(entry, pending) && entry.started === pending.started)`. `started` is minted per call at line 73 and the line-74 de-dup guarantees at most one matching entry can ever exist, so this removes exactly the entry this call pushed and removes nothing when the push was skipped. Add the collocated regression test in src/commands/__tests__/install.test.ts: interrupt an install at the people-file safeWrite (rejecting push runner, as in the existing test at line 31), then re-run install and decline the malformed-grants prompt, and assert `pending` still has one entry.

```diff
--- a/src/commands/install.ts
+++ b/src/commands/install.ts
@@ -73,9 +73,11 @@
   const pending = { op: 'install' as const, id: skill.id, team: input.team, scope, version: latest, started: new Date().toISOString() };
   await input.store.update((fresh) => { if (!fresh.pending.some((entry) => samePending(entry, pending))) fresh.pending.push(pending); });
   try {
     await ensureConsent(input.store, skill, io);
   } catch (error) {
     // A declined pre-placement consent is not an interrupted install: nothing observable moved.
-    await input.store.update((fresh) => { fresh.pending = fresh.pending.filter((entry) => !samePending(entry, pending)); });
+    // Withdraw only the entry this call added: an identical entry left by an earlier attempt that
+    // already placed is the sole record that lets sync finish that attempt's people-file write.
+    await input.store.update((fresh) => { fresh.pending = fresh.pending.filter((entry) => !(samePending(entry, pending) && entry.started === pending.started)); });
     throw error;
   }
--- a/src/commands/__tests__/install.test.ts
+++ b/src/commands/__tests__/install.test.ts
@@ -76,6 +76,27 @@
     expect((await store.read()).approvals).toEqual({});
     expect((await store.read()).pending).toEqual([]);
     expect((await store.read()).placements).toEqual({});
   });
 
+  it('keeps an earlier interrupted install intent when a later consent prompt is declined', async () => {
+    const fixture = await bareTeam();
+    const id = '99999999-9999-4999-8999-999999999999';
+    await pushFromSeed(fixture.seed, 'skills/sample/SKILL.md', `---\nname: sample\ndescription: sample\nlicense: UNLICENSED\nallowed-tools:\n  Bash: \"*\"\nmetadata:\n  id: ${id}\n  author: Seed <seed@example.com>\n  terum-category: testing\n---\n`);
+    const home = join(fixture.root, 'home');
+    const store = createConfigStore(join(home, '.terum', 'skills'));
+    await cloneWithIdentity(fixture.bare, store.teamClone('team'));
+    await store.update((config) => { config.teams.team = { remote: fixture.bare, token: null, handle: 'seed' }; });
+    let clock = 0;
+    const rejecting = wrapRunner(systemRunner, async (command, args, _options, next) => command === 'git' && args[0] === 'push'
+      ? { code: 1, stdout: '', stderr: ' ! [rejected] HEAD -> main (non-fast-forward)' }
+      : next());
+    const interrupted = await run({ ref: 'sample', config: store, runner: rejecting, safeWrite: { deadlineMs: 1, now: () => clock, sleep: async () => { clock = 2; } } }, new ScriptedPrompter([], [true]));
+    expect(interrupted.ok).toBe(false);
+    expect((await store.read()).pending).toHaveLength(1);
+    expect((await store.read()).placements[join(home, '.claude', 'skills', 'sample')]).toMatchObject({ id });
+    const declined = await run({ ref: 'sample', config: store }, new ScriptedPrompter([], [false]));
+    expect(declined).toMatchObject({ ok: false, error: expect.stringContaining('malformed allowed-tools') });
+    expect((await store.read()).pending).toHaveLength(1);
+  });
+
   it('never overwrites a foreign target without force, and force moves it to quarantine before placing', async () => {
```

- **medium — place() rollback renames a staging dir that was never created, masking the real failure with a bogus ENOENT** — `src/lib/placer.ts:67`
  - Root cause: `place()` declares `displaced` unconditionally at src/lib/placer.ts:50 but only ever *creates* that directory at line 58, inside the `options.replace` branch. The outer failure handler at line 67 restores it based solely on "is the destination missing?", with no check that the displacement ever happened and no error suppression on the restore itself. So on the common path — destination absent (first install), `cp` at line 53 fails with ENOENT/EACCES/ENOSPC/EPERM — the handler runs `lstat(destination)`, gets ENOENT, and calls `rename(displaced, destination)` on a UUID path that was never created. That rename rejects with its own ENOENT, which escapes the catch block *before* `throw error` at line 68 is reached, so the original failure is destroyed and replaced by `ENOENT ... rename '<target>/.<name>.terum-<uuid>.old' -> '<target>/<name>'`. install.ts:57 (`failure(error.message)`) and sync.ts surface that message verbatim, so a disk-full or permission failure is reported to the user as a rename of a path that never existed. Both call sites are affected: src/commands/install.ts:95 and src/commands/sync.ts:102. The collocated test cannot catch it: src/lib/__tests__/placer.test.ts:49 asserts only `{ code: 'ENOENT' }`, and the masking error is also ENOENT, so it passes for the wrong reason. Sibling staging/rollback code (src/lib/config.ts:73-77, src/lib/version.ts:36-39, src/commands/team.ts:292-296, src/commands/share.ts:176) all compensate with `rm(..., { force: true })`, which cannot throw on a missing path — placer.ts:67 is the only compensating operation that can throw, so this is isolated. (src/lib/placer.ts:67 (unguarded restore; the path is declared at line 50 but only created at line 58))
  - Fix: In src/lib/placer.ts, change line 50 from `const displaced = join(...)` to `let displaced: string | undefined;` and compute/assign the `.old` path inside the replace branch (line 58) immediately after `rename(destination, staged)` succeeds. In the outer catch (lines 66-67), capture `const restore = displaced;`, run the existing `lstat(destination)` check only `if (restore)`, and change the restore to `await rename(restore, destination).catch(() => undefined)` so a failed rollback can never replace the original error. Tighten src/lib/__tests__/placer.test.ts:49 to `toMatchObject({ code: 'ENOENT', message: expect.stringContaining('missing-source') })` so the assertion names the real source error instead of passing on the masking one.

```diff
--- a/src/lib/placer.ts
+++ b/src/lib/placer.ts
@@ -47,22 +47,30 @@
 export async function place(source: string, targetRoot: string, name: string, options: { replace?: boolean; projectRoot?: string; runner?: Runner } = {}): Promise<{ path: string; snapshot: SkillSnapshot }> {
   const destination = join(targetRoot, name);
   const temporary = join(targetRoot, `.${name}.terum-${randomUUID()}.tmp`);
-  const displaced = join(targetRoot, `.${name}.terum-${randomUUID()}.old`);
+  // Assigned only once the existing destination has actually been moved aside, so the rollback
+  // below can never rename a staging path that was never created.
+  let displaced: string | undefined;
   await mkdir(targetRoot, { recursive: true });
   try {
     await cp(source, temporary, { recursive: true, errorOnExist: true, force: false });
     try {
       await rename(temporary, destination);
     } catch (error) {
       if (!options.replace || !isExistingDestination(error)) throw error;
-      await rename(destination, displaced);
+      const staged = join(targetRoot, `.${name}.terum-${randomUUID()}.old`);
+      await rename(destination, staged);
+      displaced = staged;
       await rename(temporary, destination);
-      await rm(displaced, { recursive: true, force: true });
+      await rm(staged, { recursive: true, force: true });
     }
     if (options.projectRoot) await appendExclude(options.projectRoot, `.claude/skills/${name}`, options.runner);
     return { path: destination, snapshot: await snapshotSkillDirectory(destination) };
   } catch (error) {
     await rm(temporary, { recursive: true, force: true });
-    // A failure after moving the existing target back into staging must not orphan it.
-    try { await lstat(destination); } catch (missing) { if (isMissing(missing)) await rename(displaced, destination); }
+    // A failure after moving the existing target back into staging must not orphan it, and a
+    // failed restore must never replace the error that actually caused the failure.
+    const restore = displaced;
+    if (restore) {
+      try { await lstat(destination); } catch (missing) { if (isMissing(missing)) await rename(restore, destination).catch(() => undefined); }
+    }
     throw error;
   } finally {
     await rm(temporary, { recursive: true, force: true });
--- a/src/lib/__tests__/placer.test.ts
+++ b/src/lib/__tests__/placer.test.ts
@@ -47,5 +47,5 @@
     expect(() => resolveTarget('unknown' as never, { kind: 'global' }, undefined, root)).toThrow('Unsupported agent');
     const target = join(root, 'target');
-    await expect(place(join(root, 'missing-source'), target, 'sample')).rejects.toMatchObject({ code: 'ENOENT' });
+    await expect(place(join(root, 'missing-source'), target, 'sample')).rejects.toMatchObject({ code: 'ENOENT', message: expect.stringContaining('missing-source') });
     expect(await readdir(target)).toEqual([]);
   });
```

- **medium — Hand-rolled basename with a hardcoded '/' breaks the target lock key on Windows** — `src/commands/uninstall.ts:59`
  - Root cause: `src/commands/uninstall.ts:59` derives the lock slug with `path.slice(path.lastIndexOf('/') + 1)` instead of `basename(path)`. The keys of `config.placements` are built by `place()` as `join(targetRoot, name)` (src/lib/placer.ts:67, stored at install.ts:97), so they carry platform-native separators — `\` on win32. There, `lastIndexOf('/')` returns -1, `slice(0)` yields the entire absolute path, and `lockTarget(root, <absolute path>)` reaches `skillTargetLockPath` (src/lib/placer/vendor/skillhub/skill-target-lock.ts:35-46), where `resolve(canonicalRoot, slug)` short-circuits on the absolute slug and hashes the *uncanonicalized* path. Install (`resolve(realpath(root), skill.name)`, install.ts:85) and sync (sync.ts:100) hash the canonicalized `root` + bare name, so the two produce different sha256 lock filenames — uninstall no longer serializes against a concurrent install/sync of the same skill, which is the sole purpose of the lock, and it fails silently (no error, the lock is simply taken on a different file). Windows is a supported platform in this repo (remote.ts:39 drive-letter handling, skill-fingerprint.ts:2 separator normalization, skill-target-lock.ts:78 win32 chmod skip). No PRODUCT-CONCERNS.md and no `.planning/debug/**/*.deferred.md` exist in this worktree, and there is no comment justifying the hand-rolled slice. (src/commands/uninstall.ts:59)
  - Fix: In src/commands/uninstall.ts, add `basename` to the existing `node:path` import on line 1 and replace `path.slice(path.lastIndexOf('/') + 1)` on line 59 with `basename(path)` — identical to the sibling call sites install.ts:93 and sync.ts:90/148.

```diff
--- a/src/commands/uninstall.ts
+++ b/src/commands/uninstall.ts
@@ -1,3 +1,3 @@
-import { dirname } from 'node:path';
+import { basename, dirname } from 'node:path';
 import { ConfigStore, createConfigStore } from '../lib/config.js';
 import { lockTarget, remove } from '../lib/placer.js';
@@ -57,5 +57,5 @@
   for (const [path] of matching) {
     const root = dirname(path);
-    const release = await lockTarget(root, path.slice(path.lastIndexOf('/') + 1));
+    const release = await lockTarget(root, basename(path));
     try {
       await remove(root, path, true);
```

- **medium — Placer.remove()'s path-safety guard cannot fire at its only production call site** — `src/lib/placer.ts:78`
  - Root cause: src/lib/placer.ts:78 builds its path-safety guard entirely out of values the caller derives from the path being deleted, so no clause can fire. `const root = resolve(targetRoot)` is compared against `dirname(resolve(path))`, but the only production caller (src/commands/uninstall.ts:58) computes `const root = dirname(path)` from that very path, making the comparison an identity; `isAbsolute(destination)` is unconditionally true after `resolve()`; and `ledgerOwnsPath` is the literal `true` at uninstall.ts:61. Nothing about the ledger key is validated before `rm(destination, { recursive: true, force: true })`, so any string that lands in `config.placements` — via a bug in place()/installOne or a hand-edited config — is recursively force-deleted, contrary to spec §6 (.planning/specs/2026-09-02-phase-1-build.md:311: refuses any path "not in the `placements` ledger or not under the resolved skills root"). Only src/lib/__tests__/placer.test.ts:70-71 ever passes arguments that make the guard throw. (src/lib/placer.ts:78)
  - Fix: In src/lib/placer.ts:74-82, replace the always-true `!isAbsolute(destination)` clause with a real root check — `AGENT_PATHS[agent]?.project(dirname(dirname(root))) !== root` — and add an `agent: SupportedAgent = 'claude-code'` parameter (AGENT_PATHS and SupportedAgent are already imported at line 5; both the global and project forms are `<base>/.claude/skills`, so one comparison covers both scopes). Rewrite the doc comment to state that the root is shape-validated rather than re-resolved, and why. Add an assertion at src/lib/__tests__/placer.test.ts:71 that `remove(target, join(target, 'sample'), true)` is refused when `target` is not a skills root — it silently succeeds today. Every legitimate ledger key satisfies the new check because keys are only ever written at install.ts:97 as `join(resolveTarget(...), skill.name)`, and the existing positive removal test (placer.test.ts:23) already uses a resolveTarget-derived root. Optional follow-on (not in the patch): have uninstall.ts:61 pass `Boolean(fresh.placements[path])` instead of the literal `true`, so the ownership parameter is load-bearing and re-checked under the target lock.

```diff
--- a/src/lib/placer.ts
+++ b/src/lib/placer.ts
@@ -69,14 +69,20 @@
   } finally {
     await rm(temporary, { recursive: true, force: true });
   }
 }
 
-/** A deletion is valid only for a ledger-owned path directly underneath this scope's skills root. */
-export async function remove(targetRoot: string, path: string, ledgerOwnsPath: boolean): Promise<void> {
+/**
+ * A deletion is valid only for a ledger-owned path directly underneath a real agent skills root.
+ * The root is validated by shape rather than re-resolved: a ledger path is provenance for one
+ * machine and one checkout (see sync.ts), so re-deriving it from this process's HOME would refuse
+ * a placement legitimately made from another checkout. What it must never be is an arbitrary
+ * string that a drifted `placements` key happens to hold.
+ */
+export async function remove(targetRoot: string, path: string, ledgerOwnsPath: boolean, agent: SupportedAgent = 'claude-code'): Promise<void> {
   const root = resolve(targetRoot);
   const destination = resolve(path);
-  if (!ledgerOwnsPath || dirname(destination) !== root || !isAbsolute(destination)) {
+  if (!ledgerOwnsPath || dirname(destination) !== root || AGENT_PATHS[agent]?.project(dirname(dirname(root))) !== root) {
     throw new Error(`Refusing to remove unowned placement ${path}`);
   }
   await rm(destination, { recursive: true, force: true });
 }
--- a/src/lib/__tests__/placer.test.ts
+++ b/src/lib/__tests__/placer.test.ts
@@ -65,8 +65,9 @@
-  it('fails fast for a second contender and refuses nested and absent-ledger removals', async () => {
+  it('fails fast for a second contender and refuses nested, absent-ledger, and off-root removals', async () => {
     const root = await temporaryDirectory(); const target = join(root, 'target');
     const release = await lockTarget(target, 'sample');
     await expect(lockTarget(target, 'sample')).rejects.toThrow('target is busy');
     await release();
     await expect(remove(target, join(target, 'sample', 'nested'), true)).rejects.toThrow('unowned');
     await expect(remove(target, join(target, 'sample'), false)).rejects.toThrow('unowned');
+    await expect(remove(target, join(target, 'sample'), true)).rejects.toThrow('unowned');
   });
```

- **medium — version.test.ts never exercises the non-full-hash rejection it is named for** — `src/lib/__tests__/version.test.ts:9`
  - Root cause: The test at src/lib/__tests__/version.test.ts:9-12 is named for a guard it never reaches. Its only stub run returns `stdout: 'a'.repeat(40) + '\n'`, which is a valid 40-char lowercase hex string, so `resolveVersion` takes the success path end to end: the non-zero-exit branch (`if (result.code !== 0) throw new Error(\`Could not resolve version ...\`)`, src/lib/version.ts:11), the shape guard (`if (!/^[0-9a-f]{40}$/i.test(tree)) throw new Error(\`Resolved version for ${name} is not a full tree hash.\`)`, src/lib/version.ts:13) and the normalization (`return tree.toLowerCase()`, src/lib/version.ts:14) are all dead to the suite. The only other `resolveVersion` callers are version.test.ts:24 (real git, always returns a full lowercase hash) and src/commands/install.ts:72 (real git, same), so no test in the repo can fail if the guard is weakened or deleted. Because `resolveVersion`'s return value becomes both a filesystem path segment (`join(store.root, 'cache', team, tree, name)`, src/lib/version.ts:23) and the ledger field `version: z.string().length(40)` (src/lib/schema.ts:27), losing the guard silently moves the failure from "refuse before any I/O" to "throw mid-install after placement". The runner is already injectable (`Runner` in src/lib/runner.ts), so covering the branches costs nothing but the assertions nobody wrote. (src/lib/__tests__/version.test.ts:10)
  - Fix: In src/lib/__tests__/version.test.ts, extend the existing `it('requires the full tree object returned by git')` (lines 9-12) with a small local stub factory and four extra assertions against the injectable Runner: uppercase 40-hex normalizes to lowercase (covers version.ts:14), `stdout: 'abbrev0\n'` rejects with /not a full tree hash/ (covers the guard at version.ts:13), empty stdout rejects the same way, and `{ code: 128, stderr: 'unknown revision' }` rejects with /Could not resolve version v1 for sample/ (covers version.ts:11). Test-only; no production file changes.

```diff
--- a/src/lib/__tests__/version.test.ts
+++ b/src/lib/__tests__/version.test.ts
@@ -6,10 +6,15 @@
 import { bareTeam, cloneWithIdentity, git, pushFromSeed } from './fixtures.js';
 
 describe('version cache (§7)', () => {
   it('requires the full tree object returned by git', async () => {
-    const tree = await resolveVersion('/clone', 'sample', undefined, { run: async () => ({ code: 0, stdout: 'a'.repeat(40) + '\n', stderr: '' }) });
+    const stub = (result: { code: number; stdout?: string; stderr?: string }) => ({ run: async () => ({ code: result.code, stdout: result.stdout ?? '', stderr: result.stderr ?? '' }) });
+    const tree = await resolveVersion('/clone', 'sample', undefined, stub({ code: 0, stdout: 'a'.repeat(40) + '\n' }));
     expect(tree).toBe('a'.repeat(40));
+    expect(await resolveVersion('/clone', 'sample', undefined, stub({ code: 0, stdout: 'A'.repeat(40) + '\n' }))).toBe('a'.repeat(40));
+    await expect(resolveVersion('/clone', 'sample', undefined, stub({ code: 0, stdout: 'abbrev0\n' }))).rejects.toThrow(/not a full tree hash/);
+    await expect(resolveVersion('/clone', 'sample', undefined, stub({ code: 0, stdout: '\n' }))).rejects.toThrow(/not a full tree hash/);
+    await expect(resolveVersion('/clone', 'sample', 'v1', stub({ code: 128, stderr: 'unknown revision' }))).rejects.toThrow(/Could not resolve version v1 for sample: unknown revision/);
   });
 
   it('materializes an immutable skill tree from a real bare repository', async () => {
```


### Clear — one fix clearly wins, but not mechanical; confirm each, or hand to /parallel-fix (20)

- **critical — Pinned install places a tree whose allowed-tools were never consented to** — `src/lib/version.ts:14`
  - Root cause: Consent is computed from a different tree than the one that is placed. `resolveVersion` (src/lib/version.ts:9-14) resolves `<version>^{tree}` and only validates that the result is 40 hex characters — nothing ties the tree to `skills/<name>` or to the skill's id. The consumers then compute the grant hash from the clone's HEAD copy and place the pinned bytes:

- src/commands/install.ts:66 `resolveSkill(clone, ...)` builds a `SkillRecord` by reading `<clone>/skills/<name>/SKILL.md` at HEAD (src/lib/skills.ts:16-30), so `skill.grants` is HEAD's `allowed-tools`.
- src/commands/install.ts:76 `ensureConsent(input.store, skill, io)` prompts/short-circuits on those HEAD grants (`skill.grants.normalized === 'none'` returns at :121 with no prompt).
- src/commands/install.ts:88 `materializeVersion(...latest...)` checks out the pinned tree, and :95 `place(source, ...)` writes those bytes into `~/.claude/skills/<name>`. Nothing between :76 and :95 re-parses the materialized `SKILL.md`.

So `install helper@<old-tree>` where HEAD narrowed `Bash(*)` → `none` places a live `Bash(*)` grant with no prompt at all. The same split runs in sync: src/commands/sync.ts:80 `const grants = skill.grants` (HEAD) gates :81 `approved(...)`, while :94 `materializeVersion(store, ..., entry.version, ...)` supplies the bytes actually placed at :102. The mirror image is locked in by src/__tests__/m2-walkthrough.test.ts:48, which expects a pinned `Bash(ls)` placement to be deferred forever over a HEAD widening it will never receive.

The missing-provenance half of the same cause is independently exploitable: because `^{tree}` accepts any tree-ish, `install helper@main` resolves the repository *root* tree and `place()` copies `team.json`, `people/`, and every skill into `~/.claude/skills/helper`; `install helper@<tree-of-other-skill>` places another skill's body (and its grants) under the approved skill's name and id. (src/commands/install.ts:76 (consent from HEAD) vs src/commands/install.ts:88 (places the pinned tree); enabled by src/lib/version.ts:9-14 and repeated at src/commands/sync.ts:80-94)
  - Rated: moderate / medium risk / pattern — siblings: Sweep worklist — every site that decides consent from one tree and places another, or accepts an unvalidated tree:
1. src/commands/install.ts:76 — `ensureConsent(input.store, skill, io)` uses HEAD's `skill.grants` while :88 `materializeVersion(..., latest, ...)` and :95 `place(source, ...)` use the pinned tree. Primary site.
2. src/commands/install.ts:97 and :107 — the `placements` ledger and the `people/<handle>.json` record store `version: latest` with an approval hash that was never derived from that version; the stored approval must be the placed tree's hash.
3. src/commands/sync.ts:80-86 — `const grants = skill.grants` (HEAD) drives `approved(...)`, the "allowed-tools changed" prompt, and the recorded `approvals[skill.id]`, while :94 `materializeVersion(store, entry.team, clone, skill.name, entry.version, runner)` supplies the bytes placed at :102. Same split, and it re-runs on every sync and every session-start hook.
4. src/commands/sync.ts:51-56 — pending-replay pre-check `approved(config, skill.id, skill.grants)` is HEAD-only, then `installOne({..., version, ...})` replays a pinned install. Must use the pinned grants too, otherwise hook mode (`interactive === false`) can fall through to `installOne` → `ensureConsent` → `io.confirm`, which does not exist on `NonInteractivePrompter` (src/lib/prompt.ts:22-26) — a crash inside the SessionStart hook.
5. src/lib/version.ts:9 — `${version}^{tree}` accepts any tree-ish with no tie to `skills/<name>`: `@main` resolves the repo root tree (placing `team.json`, `people/`, and every skill under `~/.claude/skills/<name>`), and another skill's tree resolves fine. The name/id assertion on the materialized SKILL.md belongs here or immediately after `materializeVersion`.
6. src/__tests__/m2-walkthrough.test.ts:48 — `deferred: ['guarded']` locks in the mirror-image behaviour and must flip to `[]` when the gate moves to the placed tree.
Not affected: src/commands/__tests__/sync.test.ts (every placement fixture uses `version: null`), src/commands/team.ts:155 and src/commands/sync.ts:123 (both call `installOne` with no `version`, so HEAD is the placed tree).
  - Why one fix: One option clearly wins. The rule the product already committed to is "you approve the tools that actually land on your machine" — so the check has to read the exact files being copied, not the newest files in the team repo. Option 1 does that in both places tools can land (a fresh pinned install, and the background sync that runs at every session start). Option 2 fixes only the first place and leaves the recurring one wrong. Option 3 makes the tool refuse any pinned install whose tools changed, which would break the pinned "paste this one line" install links the product advertises. The extra work in option 1 is a shared helper plus one test expectation that flips — the test currently asserts the wrong behaviour (it nags you to approve a wide grant for content that does not contain it), so changing it is part of the fix, not collateral.
  - **Option 1: Consent on the bytes that get placed (recommended)** — Depth 4/4 · Cost 2/4. Add a `readSkillAt(directory, team)` helper to src/lib/skills.ts that reads `<dir>/SKILL.md`, runs `parseSkillFrontmatter`, and returns a SkillRecord-shaped value (refactor `skillRecords` to use it so there is one parser). In src/commands/install.ts, hoist the `materializeVersion` call from :88 to just above the consent block at :75, derive `const placedSkill = latest ? await readSkillAt(source, input.team) : skill`, assert `placedSkill.name === skill.name && placedSkill.id === skill.id` (throw naming the pin otherwise — this also rejects `@main` root trees and other skills' trees), and pass `placedSkill` to `ensureConsent` at :76 so the recorded approval hash is the placed tree's. In src/commands/sync.ts, hoist `const source = entry.version ? await materializeVersion(...) : skill.directory` from :94 to above :80 and set `const grants = entry.version ? (await readSkillAt(source, entry.team)).grants : skill.grants` with the same id/name assertion, so :81-:86 gate and record on the placed tree. Do the same in the pending-replay pre-check at sync.ts:51 (use the pinned tree's grants when `pending.version` is set) so hook mode defers on the right grants and never reaches `ensureConsent` with a non-interactive `io`. Update src/__tests__/m2-walkthrough.test.ts:48 (`deferred: ['guarded']` → `[]`, since a pinned `Bash(ls)` placement is unaffected by a HEAD widening) and add a case that a pin to a wider historical grant prompts before placing. *Wins if:* Wins whenever pinning must stay usable (D31's pasted `install …@<version>` one-liner) and both the fresh-pin and the recurring-sync path must be safe — it is the only option that makes the consented bytes and the placed bytes the same bytes.
  - Option 2: Fix installOne only — Depth 2/4 · Cost 1/4. Apply just the src/commands/install.ts half: materialize before consent, re-parse the materialized SKILL.md, gate `ensureConsent` on those grants. Leave src/commands/sync.ts:80-94 gating on HEAD grants. *Wins if:* Wins only if you need the smallest safe change today and accept that sync — which re-evaluates every pinned ledger entry on every run and on every session-start hook — keeps checking HEAD's grants against pinned bytes, including the m2-walkthrough nag that trains users to approve `Bash(*)` for content pinned at `Bash(ls)`.
  - Option 3: Refuse pins whose grants differ from HEAD — Depth 3/4 · Cost 1/4. In installOne, after `resolveVersion`, read the pinned tree's `allowed-tools` and abort the install when its normalized hash differs from HEAD's, instead of re-plumbing consent. Same guard before sync's pinned re-place. *Wins if:* Wins only if the product would rather fail closed than ever ask a second consent question — but it breaks legitimate pinning the moment a skill's grants change at all, which is exactly when a pasted D31 one-liner is most likely to be old, and it still needs the same tree-parsing work, so it buys nothing over option 1.
- **high — One unreplayable pending intent makes every future sync (and the SessionStart hook) fail** — `src/commands/sync.ts:62`
  - Root cause: `sync.run()` treats a per-entry replay failure as a whole-run failure. In the pending-replay loop, the catch at src/commands/sync.ts:57-63 tolerates exactly one error string (`no matching project context`) and `throw error`s on everything else; that throw escapes the `for` loop and is caught only by the outer handler at sync.ts:132, which converts it to `failure()`. Everything after the replay loop — `reconcileShared` (67), the placements/update loop (70-106), the endorsement batch (110-125), `reconcileOrphans` (127) and `writeStamp` (130) — is skipped for every team.

The entry that poisons it is durable. `installOne` pushes the pending entry at install.ts:74 (before consent), and only the consent path unwinds it (install.ts:77-81). The foreign-collision abort at install.ts:92 (`Install target ... already contains another skill; retry with --force`) throws *after* the pending push and does not unwind it — `install.test.ts:94` asserts `pending` has length 1 at that point. Because the install never reached `place`, no `placements[destination]` entry is written, so on every later replay `owned` is false (install.ts:89) and `inspect` returns `foreign` again (placer.ts:29). The replay never passes `force`, so it throws the identical error forever: one `install` against a pre-existing same-named folder permanently breaks `sync` and the SessionStart hook on that machine until the user hand-edits config.json or deletes the folder.

The same abort (one-shot rather than permanent) occurs when a user declines the consent prompt during a replay — `ensureConsent` throws `Consent was declined for X.` (install.ts:125) — and when `lockTarget` reports the target busy. (src/commands/sync.ts:62)
  - Rated: moderate / medium risk / pattern — siblings: Same "one item throws, the whole batch dies" shape, all found in this diff:
- src/commands/sync.ts:50 — `uninstallOne` replay is not wrapped at all; a failed uninstall replay (safeWrite deadline exhausted, placer refusal) aborts the entire sync. Covered by recommended option 1.
- src/commands/sync.ts:123 — endorsement-batch `installOne` is not wrapped; the user answers yes to "Install N newly endorsed skill(s)" and then declines the per-skill tool prompt, `ensureConsent` throws (install.ts:125), and sync fails before `reconcileOrphans` (127) and `writeStamp` (130). Highly reachable; covered by recommended option 1.
- src/commands/sync.ts:67 → src/commands/share.ts:97 and share.ts:105 — `reconcileShared` continues past read errors but throws on a missing handle and does not guard the `safeWrite` at share.ts:98, so one shared skill's push failure aborts sync. Deliberately excluded from option 1: whether a failed push should be exit-0 is a product decision.
- src/commands/install.ts:39-42 (member bulk) and install.ts:51 (project bulk) — a throw on skill k aborts the loop after skills 1..k-1 were already placed and pushed, and the partial `results` array is discarded by the outer catch at install.ts:57. The spec (.planning/specs/2026-09-02-phase-1-build.md, `install` bullet) calls these "snapshot bulk forms with per-skill summaries", which the current all-or-nothing throw cannot produce.
- src/commands/uninstall.ts:24, :33, :44 — identical bulk loops with the same all-or-nothing behavior.
  - Why one fix: Option 1 dominates. Option 2 is a strict subset of it at essentially the same cost and leaves an identical bug one screen lower (decline the tool prompt for one newly endorsed skill and the whole sync still fails). Option 3 does not fix the reported problem at all — sync would still abort on a declined consent or a busy lock — and it additionally requires overturning an assertion a test deliberately makes. In plain terms: sync's job is to report what it could not do and keep going, and it already has the words for that ("deferred", "Blocked ..."); option 1 just makes it use them everywhere it replays an install or uninstall. Note that option 1 deliberately stops short of the shared-skill reconciler (sync.ts:67 / share.ts), because turning a genuine failed `git push` into an exit-0 notice is a product call, not a code call.
  - **Option 1: Per-item isolation for every install/uninstall replay in sync (recommended)** — Depth 3/4 · Cost 1/4. In src/commands/sync.ts, add one small helper used by all three per-item install/uninstall call-sites, so a failure records the item and continues instead of aborting run(): (a) replace the narrow catch at sync.ts:57-63 with a general one that keeps the specific project-context wording and otherwise pushes `deferred.push(skill.name)` plus `notice(...)` naming the skill and the error message, then `continue`; (b) wrap the uninstall replay at sync.ts:50 in the same handler; (c) wrap the endorsement-batch `installOne` at sync.ts:123 so a declined tool prompt defers that one skill (matching how sync.ts:85 already defers a declined update) instead of failing the whole run. Add sync.test.ts cases: a stuck pending entry from a foreign target (build it with the install.test.ts:81-102 setup, then run sync) and a declined consent during replay — both asserting `ok: true`, the deferred list names the skill, an unrelated placement still updates, and `run/<team>.stamp` is written. *Wins if:* Wins whenever sync is expected to be a reconciler that reports what it could not do (the vocabulary the file already has: `deferred`, `notice`, `Blocked ...`) rather than a transaction that aborts — it removes the failure for the whole replay loop and for the identical-shaped endorsement batch, at the same cost as the narrow fix.
  - Option 2: Widen only the existing catch at sync.ts:57-63 — Depth 2/4 · Cost 0/4. Leave sync.ts:50 and sync.ts:123 alone; change only the pending-replay catch so any error becomes a deferred entry + notice and `continue`. Add the one sync test for the stuck foreign-target entry. *Wins if:* Wins if you want the smallest, most reviewable diff on exactly the cited line and are content to fix the endorsement-batch abort (a declined tool prompt at sync.ts:123 still kills the whole run before reconcileOrphans and writeStamp) as a separate follow-up.
  - Option 3: Do not leave durable intent when the install aborted before anything moved — Depth 1/4 · Cost 1/4. In src/commands/install.ts, extend the unwind that today only covers `ensureConsent` (lines 77-81) to cover the pre-placement collision check, so the foreign-target abort at install.ts:92 clears the pending entry it pushed at install.ts:74 — nothing observable moved, exactly the rationale in the comment at install.ts:78. Requires changing the `expect((await store.read()).pending).toHaveLength(1)` assertion at src/commands/__tests__/install.test.ts:94. *Wins if:* Wins only if the product decides an install that collided should record no retry intent at all (today the entry is arguably correct: the user did ask to install, and a later sync should succeed once the foreign folder is gone). Even then it is a complement, not a substitute — sync still aborts on a declined-consent or busy-lock replay, so it does not fix the cited defect on its own.
- **high — One broken shared source aborts the entire sync run** — `src/commands/share.ts:81`
  - Root cause: `reconcileShared` (src/commands/share.ts:66-118) treats per-entry failure inconsistently. Two per-entry problems are guarded and `continue` (missing source at :70, unreadable repo records at :72-73), and the substantive "both sides diverged" case also prints-and-continues at :91-92 — establishing that a problem with one tracked skill is reported and skipped. But everything after :74 runs unguarded inside the same loop body: `readTeamPolicy` (:75), `readFile(sourceSkill)` (:81), `injectManagedFields` (:82, :86, :112 — which throws `'SKILL.md has no YAML frontmatter'`, `'Invalid SKILL.md frontmatter: …'`, or `'SKILL.md frontmatter must be a mapping'` per src/lib/skills.ts:61-65), `readFile(repoSkill)` (:85), `canonicalDigest` (:87-88), the `if (!binding?.handle) throw` at :97/:105, `safeWrite` (:98/:107), and `replaceDirectory` (:110). Any of those rejects the whole `reconcileShared` promise. Its single caller awaits it at sync.ts:67, *before* the placements loop, endorsement batch, orphan reconciliation, and `writeStamp`, and sync's outer `catch` (sync.ts:132) converts the throw into `failure(<raw message>)`. So one shared authoring folder in a transient bad state (mid-edit frontmatter, renamed SKILL.md, a team with no joined handle) aborts the entire sync run with a message that names neither the file nor the skill, and every unrelated step is skipped. The build spec's §5.3 "Other branches" list (.planning/specs/2026-09-02-phase-1-build.md:207-209) prescribes warn-and-keep-tracking for the branches it does name and never asks for a hard abort; the malformed-source branch simply is not enumerated, so the loop falls through to "throw". (src/commands/share.ts:81 (first unguarded throw; the unguarded region is the loop body from :75 to :116, inside the `for (const [id, tracked] of Object.entries(config.shared))` loop opened at :68))
  - Rated: moderate / low risk / pattern — siblings: Same shape — a sync loop that guards *some* per-entry failures with notice+continue but lets the rest escape and abort the whole run. Sweep worklist (not fixed by the recommended option, which is depth 2 on share.ts only):
1. src/commands/share.ts:68-117 — reconcileShared (the cited site; guarded at :70 and :72-73, unguarded :75-116).
2. src/commands/sync.ts:70-106 — the placements loop. Guards a missing repo skill (:79) and malformed allowed-tools (:82) with notice+continue, but `materializeVersion` (:94), `snapshotSkillDirectory(source)` (:95), `lockTarget` (:100), `place` (:102) and `moveToQuarantine` (:90) are unguarded, so one broken placement skips every later placement, the endorsement batch, orphan reconciliation, and writeStamp.
3. src/commands/sync.ts:47-65 — the pending-replay loop. Guards a vanished skill (:49) and one specific installOne message (:58-61), but `findSkill` (:48, throws via skillRecords when any repo SKILL.md is malformed) and every other installOne/uninstallOne failure abort the run for all teams.
4. src/commands/sync.ts:110-125 — the endorsement batch. `readPerson`, `skillRecords`, `readTeam` and `installOne` are unguarded, so one team with a malformed people file kills the batch for every other team.
5. src/commands/sync.ts:138-175 — reconcileOrphans. `actorPerson` swallows read errors (:177) but each `repo.safeWrite` (:152, :164) is unguarded, so one failed adopt/decline push abandons the remaining orphans and skips writeStamp.
Note the differing severity: in share.ts the failing input is a user-owned authoring folder that is expected to be transiently malformed, which is why that site is the one to fix first.
  - Why one fix: One option clearly wins. In plain terms: this reconciler already has a rule — if one shared skill is in a bad state, say so and move on to the next one. That rule is just applied to only some of the ways a skill can be in a bad state, so a half-saved SKILL.md stops the entire sync. Option 1 finishes the rule the code already started, and adds the skill's name to the message so the user knows which folder to fix. Option 2 does exactly the same thing with a bigger reorganisation and no extra benefit. Option 3 is deliberately narrower: it would keep "the push to the team repo failed" as a hard sync failure. That is the only genuine question here, and the file already answers it — when two copies of a skill genuinely conflict (line 91) the code prints a message and lets sync report success, so a failed push being reported the same way is consistent, and nothing is lost because the baseline is not advanced, so the next sync retries.
  - **Option 1: Guard the whole per-entry body (reviewer's suggestion) (recommended)** — Depth 2/4 · Cost 1/4. In src/commands/share.ts, open a `try {` after the existing `if (!record) … continue;` guard at :74 and close it after the final `}` of the if/else at :116 with `} catch (error) { io.print(`Could not reconcile shared ${record.name} (${id.slice(0, 8)}): ${error instanceof Error ? error.message : String(error)}`); }`. Starting the try at :75 keeps `record` in scope and already narrowed, so the message can name the skill (the finding's complaint that the current message names neither file nor skill). This covers every throw site in the body: readTeamPolicy :75, readFile :81/:85/:111, injectManagedFields :82/:86/:112, canonicalDigest :87-88, the missing-handle throws :97/:105, safeWrite :98/:107, replaceDirectory :110, and store.update :108/:115. Add a share/sync regression test: a tracked shared source whose SKILL.md frontmatter is invalid YAML, asserting `sync` still returns ok, prints the per-skill message, and still updates an unrelated placement and writes run/<team>.stamp. *Wins if:* Wins whenever reconcileShared's own established convention (report the entry, keep going — :70, :72-73, :91) is the intended contract; it makes that convention total instead of partial, in ~42 re-indented lines with one caller and no API change.
  - Option 2: Extract `reconcileOne` and guard the call — Depth 2/4 · Cost 2/4. Move the loop body of `reconcileShared` (share.ts:69-116) into a private `async function reconcileOne(store, runner, io, id, tracked): Promise<void>` that keeps its existing early-return guards as returns, then reduce the loop to `for (…) { try { await reconcileOne(…); } catch (error) { io.print(`Could not reconcile shared ${basename(tracked.source)} (${id.slice(0, 8)}): …`); } }`. Same test as option 1, plus the option to unit-test one entry directly. *Wins if:* Wins if the team wants the guarded unit to be nameable and directly testable, or expects more branches in this reconciler later — but it is a larger diff in a file that deliberately favors dense inline bodies, and it buys no extra coverage over option 1.
  - Option 3: Guard only the read/parse/digest steps — Depth 1/4 · Cost 1/4. Wrap just the content-reading region (share.ts:75-88, plus the re-read at :111-113) in try/catch + print + continue, leaving the missing-handle throws (:97/:105) and the `safeWrite` push failures (:98/:107) to propagate and fail the sync run as they do today. *Wins if:* Wins if a failed push or an unjoined team must remain a hard, non-zero sync failure rather than one printed line among many — but it leaves a whole class of per-entry aborts in place, and it contradicts the divergence branch at :91, which already downgrades a substantive refusal to a printed line while sync returns ok.
- **high — materializeVersion's concurrent-cache-write guard catches EEXIST, but rename onto an existing cache entry raises ENOTEMPTY** — `src/lib/version.ts:35`
  - Root cause: src/lib/version.ts:35 guards the cache-publish rename with an EEXIST-only predicate: `try { await rename(staging, destination); } catch (error) { if (!(error instanceof Error && 'code' in error && error.code === 'EEXIST')) throw error; }`. The branch is meant to absorb "another process already materialized this tree", but a cache entry always contains at least SKILL.md, so the destination is a non-empty directory, and POSIX rename(2) onto a non-empty directory reports ENOTEMPTY on darwin and Linux (EEXIST is permitted but not what these platforms return). The guard therefore never fires on any targeted platform and the ENOTEMPTY escapes materializeVersion into src/commands/install.ts:88 and src/commands/sync.ts:94 — in sync it aborts the entire placement loop, so one concurrent hook-triggered sync fails the whole command. Second defect in the same statement: on the platform where EEXIST is raised, the branch returns `destination` without deleting `staging` (the `rm(staging)` at line 38 only runs on the rethrow path), permanently leaking a `<name>.tmp-<uuid>` directory into the cache. The immediate cause is a re-derived, weaker copy of a predicate the repo already owns: src/lib/placer.ts:110-112 `isExistingDestination` tests `EEXIST || ENOTEMPTY` for exactly this rename race, but it is module-private so the new file open-coded a narrower check. Sweep of every errno guard in src/: only version.ts:35 is wrong — placer.ts:111 is correct, and skill-target-lock.ts:72 is correct because there EEXIST really is the only code `mkdir` of a lock directory can return. (src/lib/version.ts:35)
  - Rated: moderate / low risk / isolated
  - Why one fix: No real fork here. All three options fix the crash and the leftover temp directory identically; they differ only in where the shared "someone else got there first" check lives. The recommended one reuses the check the codebase already has instead of leaving a second, weaker copy next to it — which matters because that second copy is precisely what caused this bug — and costs a single extra word (`export`) on an existing function. The minimal option is strictly less: same fix, duplicate check left in place. The shared-helper option is tidier housekeeping but does not make the bug any more fixed, touches a third file, and can be done any time later. Nothing here hinges on a product decision.
  - **Option 1: Reuse the canonical predicate and always clean staging (recommended)** — Depth 3/4 · Cost 1/4. src/lib/placer.ts:110 — add `export` to `isExistingDestination`. src/lib/version.ts — import it (`import { isExistingDestination } from './placer.js';`) and replace the line-35 catch body with `if (!isExistingDestination(error)) throw error;`. Move `await rm(staging, { recursive: true, force: true })` from the catch at line 38 into the `finally` at lines 40-42 (after a successful rename `staging` no longer exists, so the rm is a no-op there), which makes the outer catch a bare rethrow and lets it be deleted, leaving `try { ... } finally { await rm(staging, ...); await rm(index, { force: true }); }`. Add a concurrency regression test to src/lib/__tests__/version.test.ts: two `materializeVersion` calls for the same tree via `Promise.all`, asserting both resolve to the same path, the content is intact, and no `.tmp-` entry survives in `join(store.root, 'cache', 'team', tree)`. *Wins if:* Wins whenever this errno race should be handled identically everywhere in the codebase: one exported predicate means the next writer cannot re-derive a weaker copy, which is exactly how this bug was introduced, and the staging cleanup lands on every exit path for one line.
  - Option 2: Minimal in-file fix — Depth 2/4 · Cost 0/4. src/lib/version.ts:35 only — widen the inline condition to `(error.code === 'EEXIST' || error.code === 'ENOTEMPTY')` and add `await rm(staging, { recursive: true, force: true });` inside that branch before `return destination;`. No new import, no change to placer.ts, catch/finally structure untouched. *Wins if:* Wins if you do not want the cache layer (version.ts) to import from the placement layer (placer.ts) at all, or you want the smallest possible diff to land now and accept two copies of the same predicate.
  - Option 3: Lift the errno predicates into a shared fs helper — Depth 4/4 · Cost 2/4. New src/lib/fs-errors.ts exporting `isMissing` and `isExistingDestination`; src/lib/placer.ts drops its private copies at lines 109-112 and imports them; src/lib/version.ts imports both, using `isMissing` for the ENOENT probe it currently open-codes at line 24 and `isExistingDestination` at line 35, plus the same staging-cleanup move as option 1. Behavioral fix is identical to option 1. *Wins if:* Wins if more modules will need these predicates — there are already three hand-rolled errno checks (version.ts:24, placer.ts:109-112, skill-target-lock.ts:72) — and you would rather own one helper module than an export chain that points version.ts at placer.ts.
- **high — share --relocate accepts any path, which a later reconcile rm -rf's** — `src/commands/share.ts:150`
  - Root cause: `relocate()` writes an unvalidated, arbitrary filesystem path into `config.shared[id].source` — the single field the §5.3 reconciler treats as a Terum-owned skill folder. Every other writer of that field validates: `share <path>` (share.ts:41-43) requires `SKILL.md`, a legal skill name, and no privileged content, then asks a y/N (share.ts:54). `relocate` checks only `exists(path)` (share.ts:149) — no SKILL.md, no `metadata.id` match against the id being re-pointed, no confirmation, and it is not even handed the Prompter (`run` calls it as `relocate(store, args.relocate)` at share.ts:32, unlike `forget` which gets `io` at :31). Once that pointer is wrong, three separate reconciler paths act on it as if it were the user's skill: `reconcileShared` writes managed frontmatter into `<path>/SKILL.md` (share.ts:83); the source-wins row uploads the whole tree via `sourceFiles(tracked.source)` + `mirrorToTree` and pushes it to the shared team repo (share.ts:106-107 — the guard only constrains destination paths under `skills/<name>/`, not what is read from disk, guard.ts:42-43); and the repo-wins row calls `replaceDirectory(record.directory, tracked.source)` (share.ts:110, and unconditionally from `--keep-repo` at share.ts:135). A second, independent defect compounds it: `replaceDirectory` (share.ts:176) is copy → `rm(to, {recursive, force})` → rename, i.e. it deletes the destination *before* the replacement is visible, and it deletes outside the quarantine root. That inverts the safe order the codebase already uses everywhere else — `place()` in placer.ts:52-61 renames the existing tree aside to `.old`, renames the new copy in, and only then `rm`s the displaced copy — and it contradicts the stated invariant in AGENTS.md:42 ("never delete outside `~/.terum/skills/quarantine/`") and the build spec's invariants 27 and 34 (`.planning/specs/2026-09-02-phase-1-build.md:482,492`: move-asides land in quarantine, "only `sync prune` deletes, only from there, only after a y/N listing every path"). So a mistyped `--relocate` is silently promoted to a recursive delete or a bulk push of an unrelated directory, and even a correct relocate loses the tree if the process dies between the `rm` and the `rename`. (src/commands/share.ts:149)
  - Rated: moderate / medium risk / isolated
  - Why one fix: Option 1 is simply options 2 and 3 done together, and neither piece is expensive — both live in one file, and the only knock-on is one existing test needing a scripted "yes". Nothing else beats it on any axis that matters here. In plain terms: option 3 stops the user from ever pointing the tool at the wrong folder, option 2 makes the folder recoverable if it somehow still happens, and there is no reason to buy only one of those. The one judgement call inside option 1 is a detail, not a fork: when the relocated folder has no Terum id in its SKILL.md (say the user restored it from a backup taken before sharing), should the command refuse outright or warn and let them confirm? Refusing is safer and the user still has the `share --forget <id>` then `share <path>` escape hatch, so refusing is the suggested default.
  - **Option 1: Validate the relocation target and make replaceDirectory move-aside-then-delete (recommended)** — Depth 4/4 · Cost 1/4. Two coordinated edits in src/commands/share.ts. (1) `relocate` (share.ts:147-152) takes `io: Prompter` (plumbed from the `run` dispatch at share.ts:32, matching how `forget` already receives it at :31): after the `exists` check, read `<path>/SKILL.md`, run it through `parseSkillFrontmatter` (src/lib/schema.ts:110), and refuse unless it parses and `metadata.id === parsed.id`; then `io.confirm("Re-point shared <name> to <resolved path>?")` printing the resolved absolute path, so a bare `.` or a parent folder is visible before it is accepted. (2) `replaceDirectory` (share.ts:176) mirrors `place()` in src/lib/placer.ts:52-61 instead of inverting it: `cp` into the staging sibling, `rename(to, `${to}.terum-<uuid>.old`)` (or `moveToQuarantine(to, join(store.root,'quarantine'), basename(to))` from src/lib/placer.ts:84 to match the spec's quarantine invariant), `rename(temporary, to)`, and only then `rm` the displaced tree — with the staging copy cleaned up in a `finally`. Update src/commands/__tests__/share.test.ts:217 to pass `new ScriptedPrompter([], [true])` (a bare `ScriptedPrompter` throws PromptClosedError once a confirm exists, fixtures.ts:27-28), and add two cases: relocate refused when the target has no SKILL.md / a foreign `metadata.id`, and `--keep-repo` leaving the old source tree recoverable rather than deleted. *Wins if:* Wins whenever you want the wrong pointer rejected at the one place it can be created *and* the destructive primitive made recoverable — it is the union of options 2 and 3 and closes the push-to-repo row that option 2 alone leaves open.
  - Option 2: Validate relocate only — Depth 3/4 · Cost 1/4. Only edit (1) above: `relocate` takes the Prompter, requires a parseable `SKILL.md` whose `metadata.id` equals the tracked id, and confirms with the resolved absolute path. `replaceDirectory` is left as-is. Same test update at src/commands/__tests__/share.test.ts:217 plus a refusal case. *Wins if:* Wins if you want the smallest change that closes the security hole (all three misuse rows stem from the bad pointer) and are willing to accept the delete-before-rename window on legitimate paths as a pre-existing crash-only risk.
  - Option 3: Harden replaceDirectory only — Depth 2/4 · Cost 0/4. Only edit (2) above: rewrite `replaceDirectory` (share.ts:176) as copy → rename-old-aside → rename-new-in → delete-aside, or route the old tree through `moveToQuarantine` (src/lib/placer.ts:84). `relocate` keeps accepting any existing path. No signature or Prompter plumbing; no existing test changes. *Wins if:* Wins if the priority is a zero-risk change that cannot refuse any relocate that works today — it makes the deletion recoverable at both call sites (share.ts:110 and :135) but still lets a mistyped path be written into and bulk-pushed to the team repo via share.ts:83 and :106-107.
- **high — place() copies symlinks from the team clone straight into ~/.claude/skills, and the digest cannot see them** — `src/lib/placer.ts:53`
  - Root cause: `place()` hands the entire untrusted source tree to `fs.cp` and never inspects dirent types: `await cp(source, temporary, { recursive: true, errorOnExist: true, force: false })` (src/lib/placer.ts:53). Node's defaults for `cp` are `dereference: false` and `verbatimSymlinks: false`, which means a symlink in the source is *recreated as a symlink* whose target is `path.resolve(dirname(src), <committed target>)` — i.e. the relative target is resolved against the local clone path and written back as an absolute link. The source is untrusted team-repo content in both entry paths: unpinned installs pass `skill.directory`, the clone path `~/.terum/skills/teams/<team>/skills/<name>/` (install.ts:88), and pinned installs pass the `git read-tree` + `git checkout-index -a` materialization (version.ts:30-32), which also materializes mode-120000 entries. So a symlink committed under `skills/<name>/` lands inside `~/.claude/skills/<name>/`, the directory the agent auto-loads, pointing at an arbitrary absolute path on the victim's machine (e.g. `../../../../../.ssh/id_rsa` → `~/.ssh/id_rsa`).

Nothing downstream compensates, which is why it stays silent: both directory walkers that produce the integrity values count only `entry.isFile()` and recurse only into `entry.isDirectory()` — `walk()` in src/lib/skills.ts:97-101 (feeds `canonicalDigest`) and `listSkillFiles` in the vendored src/lib/placer/vendor/skillhub/skill-fingerprint.ts:49-53 (feeds `snapshotSkillDirectory`, the placement fingerprint and sync's tamper check). A symlink is neither, so it is invisible to the digest, to the ledger fingerprint, and to `diffSkillFiles`. The only consent gate on the install path is the `allowed-tools` prompt (install.ts:115-127), which says nothing about file content. The write side of this same codebase does guard this explicitly — `assertInsideClone` refuses "Refusing to write through the symlink" and checks `realpath` containment (src/lib/teamRepo.ts:221-235) — and the spec's own placement decision is "plain copies, no symlinks" (.planning/specs/2026-09-02-phase-1-build.md:107, D15), so the read/placement side is the outlier, not an intentional design. (src/lib/placer.ts:53)
  - Rated: moderate / medium risk / pattern — siblings: Same mistake / same blind spot, in the order they should be swept:
1. src/lib/placer.ts:53 — `cp(source, temporary, { recursive: true, ... })` copies untrusted clone/cache content into `~/.claude/skills/<name>` with symlinks preserved. THE defect. Reached from src/commands/install.ts:95 and src/commands/sync.ts:102.
2. src/commands/share.ts:176 — `replaceDirectory` does `cp(from, temporary, { recursive: true })` with the same defaults, copying repo skill content into the user's local authoring folder (called from reconcileShared share.ts:110 and resolveDivergence share.ts:135). Same class, lower blast radius (not agent-auto-loaded); covered free by the shared helper.
3. src/lib/skills.ts:97-101 — `walk()` recurses only on `isDirectory()` and records only `isFile()`, so `canonicalDigest` (skills.ts:47) cannot see a symlink added to or removed from a skill tree.
4. src/lib/placer/vendor/skillhub/skill-fingerprint.ts:49-53 — `listSkillFiles` has the identical file/dir-only filter, so `snapshotSkillDirectory` (the placement fingerprint at placer.ts:63, sync's tamper check at sync.ts:88-96, and `diffSkillFiles`) is blind to links. Vendored: changing it needs the header + NOTICE update (AGENTS.md:47-54).
5. src/commands/share.ts:174 — `sourceFiles()` silently drops symlinks instead of rejecting them, so `share` publishes a skill whose content is quietly incomplete rather than telling the author.
6. src/commands/share.ts:161-173 — `hasPrivilegedContent` uses the same file/dir-only walk, so a symlink named `hooks` or `.claude-plugin` is not flagged by the privileged-content gate (mitigated today only because item 5 drops it before publish).
Not affected (checked): `remove`/`rm` (placer.ts:81) and `moveToQuarantine`/`rename` (placer.ts:88) do not follow links; teamRepo.ts:221-235 already guards the repo-write side correctly and is the model to copy.
  - Why one fix: Option 1 is the one that both stops the leak and tells you it happened, and neither alternative beats it on an axis that matters here. Option 3 (silently skip the link) makes the placed folder safe but hides the fact that a team repo tried to plant a link into the directory the agent auto-loads — for a security fix, silence is the wrong default, and it is a strict subset of option 1 otherwise. Option 2 is option 1 plus a later sweep: once both copy sites refuse links, the digest/fingerprint blindness has no remaining path through Terum (a link that never gets copied never needs to be hashed), so the extra depth only buys detection of links planted by something that already has write access to the placed folder — worth doing eventually, not worth paying the vendored-file provenance and digest-semantics cost to ship this fix. The one judgement call left inside option 1 is small and local: whether a rejected skill should fail the whole `sync` run or be reported and skipped like the other per-skill blocks already are.
  - **Option 1: Refuse non-plain entries at copy time (place + share's replaceDirectory) (recommended)** — Depth 3/4 · Cost 1/4. Add one helper in src/lib/placer.ts — an async `fs.cp` `filter` (or an equivalent pre-walk) that `lstat`s each source path and throws unless it `isFile()` or `isDirectory()`, with a message in the house style (`Refusing to place <name>: <path> is not a regular file or directory`, mirroring teamRepo.ts:231). Wire it into the `cp` options at placer.ts:53, which covers both entry paths (install.ts:95 and sync.ts:102) and both source kinds (the live clone and the `checkout-index` cache). Export it and use it at the sibling copy in share.ts:176 `replaceDirectory`, which copies repo content back into the user's authoring folder with the same defaults. The existing catch/finally at placer.ts:64-71 already removes the staging folder, so a rejection leaves nothing behind. Collocated test in src/lib/__tests__/placer.test.ts: `symlink()` an escaping target into a source folder (the fixture pattern already exists at teamRepo.test.ts:121), assert `place()` rejects and that the target root has no `.tmp` leftovers. One nuance to decide while writing it: a throw from place() at sync.ts:102 is not caught locally, so it fails the whole sync run — if that is not wanted, catch it there and `notice(...)`+`continue` like the other per-skill blocks (sync.ts:79-82). *Wins if:* Wins if the goal is that untrusted repo content can never become a link on the victim's disk, and the loud failure is acceptable — nothing Terum itself publishes can contain a symlink (share.ts:174 `sourceFiles` drops them), so the new error can only fire on content hand-committed with raw git.
  - Option 2: Also make the walkers see non-plain entries — Depth 4/4 · Cost 2/4. Everything in option 1, plus close the blindness that let it stay silent: make `walk()` in src/lib/skills.ts:97-101 and `listSkillFiles` in src/lib/placer/vendor/skillhub/skill-fingerprint.ts:49-53 either record symlinks (hash the link target as content, so adding/removing one changes `canonicalDigest` and the placement fingerprint) or throw. Touching the vendored file requires updating its provenance header and the NOTICE entry per AGENTS.md:47-54, and it changes digest semantics for any tree that contains a link. *Wins if:* Wins if tamper detection itself must cover links — i.e. you want `sync`'s fingerprint check to notice a symlink appearing inside an already-placed folder or a shared source folder, not merely to stop new ones being copied in, and you accept editing the vendored file plus its NOTICE.
  - Option 3: Filter symlinks out of the copy instead of failing — Depth 2/4 · Cost 0/4. Same `filter` hook at placer.ts:53, but return `false` for non-plain entries so they are skipped, matching what share.ts:174 `sourceFiles` already does on the publish side. No error, no new failure path, no change at any call site. *Wins if:* Wins if install/sync availability is worth more than the signal — one odd or hostile skill folder should degrade to a partial copy rather than fail its install and (at sync.ts:102, where nothing catches it) the entire sync run.
- **high — sync silently re-places orphaned placements before offering adoption** — `src/commands/sync.ts:70`
  - Root cause: The placements update loop in `sync.ts` re-places before orphan status is ever consulted. The loop at src/commands/sync.ts:70-106 guards only four cases — no matching project root (75), `declined` (77), skill gone from the repo (79), and unapproved grants (81) — and then falls through to `place(source, root, skill.name, …)` at line 102. The orphan predicate ("no matching `pending` entry and no matching `installed` record in `people/<handle>.json`") is not part of that guard list; it exists only inside `reconcileOrphans`, split across src/commands/sync.ts:141 (pending check) and src/commands/sync.ts:147 (installed check), and `reconcileOrphans` is not called until src/commands/sync.ts:127 — three phases after the loop has already overwritten the folder. Two independent facts let the defect through the remaining guards: (1) `uninstallOne` only records a `declined` entry when the skill is team-endorsed (`const auto = teamJson?.includes(input.id) ?? false` at src/commands/uninstall.ts:73-74), so a personal skill uninstalled on machine A writes nothing to `declined` and the skip at line 77 never fires on machine B; (2) `placements` is machine-local, so machine B's ledger entry survives machine A's uninstall untouched. Once the repo copy advances, `repoSnapshot.fingerprint !== entry.fingerprint` defeats the up-to-date short-circuit at line 96 and line 102 re-places. Under `sync --hook` this happens with no prompt at all, and `reconcileOrphans` merely pushes the path onto `deferred` afterwards — the skill the user uninstalled elsewhere is silently refreshed at session start, violating build spec §6 ("Untracked placements in the ledger with no `pending` entry and no `installed` record are orphans …, offered for adoption, never silently re-placed"). The collocated tests do not catch this because `orphanedPlacement()` (src/commands/__tests__/sync.test.ts:282) leaves both the on-disk copy and the repo copy at the ledger fingerprint, so line 96 short-circuits before the re-place is reached. (src/commands/sync.ts:77)
  - Rated: moderate / low risk / isolated
  - Why one fix: Option 1 is the safe, complete answer and option 2 is option 1 plus an extra step that can be added later without any rework. Both stop the bad behaviour — a skill you uninstalled on another machine will no longer be quietly put back. The only thing option 1 leaves behind is cosmetic and self-healing: if you say "yes, adopt" to an orphan, the folder is not refreshed to the newest version until the next sync, which happens automatically at the start of your next session. Option 2 removes that one-cycle lag, but to do it you have to move the step that talks to the team's git server ahead of the step that updates files on disk — so if that network write fails, none of your skills get updated at all that run, and the order of the questions you are asked changes. Trading a self-correcting cosmetic lag for a new way the whole sync can fail is a bad deal, so option 1 wins outright.
  - **Option 1: Hoist the orphan predicate and guard the placements loop (recommended)** — Depth 3/4 · Cost 1/4. In src/commands/sync.ts add a single-source predicate `function isOrphan(config, person, placement): boolean` that returns true when no `config.pending` entry matches `{id, team, scope}` AND no `person.installed` entry matches `{id, scope}` (i.e. exactly the two conditions currently inlined at sync.ts:141 and sync.ts:147, with `declined` deliberately left to the callers). In the placements loop, bind the person object that line 77 already fetches and throws away — `const person = binding?.handle ? await actorPerson(store, entry.team, binding.handle) : undefined;` — keep the existing `person?.declined.includes(entry.id)` skip, then add `if (person && isOrphan(await store.read(), person, entry)) continue;` immediately after it and before `findSkill` at line 78, with a comment citing §6. Refactor `reconcileOrphans` (sync.ts:141,147) to call the same helper so the two consumers cannot drift again. Orphans then fall through untouched to `reconcileOrphans` at line 127, which already prompts for adoption interactively and pushes to `deferred` in hook mode — so no new output paths are introduced and `deferred` gains no duplicate entries. Add a sync.test.ts case that extends `orphanedPlacement()` with a `pushFromSeed(..., skill('new'))` before running sync, asserting the placed SKILL.md still reads `description: old` and `placed: 0` — the gap the current orphan tests miss because they leave both fingerprints equal to the ledger's. *Wins if:* You want the §6 invariant honoured with the smallest behavioural delta — no reordering of side-effecting phases, no new failure mode — and you accept that an orphan the user adopts is brought up to date on the next sync rather than the current one.
  - Option 2: Also run reconcileOrphans before the placements loop — Depth 4/4 · Cost 2/4. Everything in option 1 (the guard is still required, because in hook mode `reconcileOrphans` only defers and resolves nothing), plus move the `await reconcileOrphans(...)` call from sync.ts:127 up to just after `reconcileShared` at sync.ts:67. Adopted orphans then have an `installed` record in the clone before the loop runs — `safeWrite` leaves the clone reset to origin/main with the pushed change, which the existing test at sync.test.ts:86 already relies on — so they are updated in the same sync; declined ones are caught by the pre-existing `declined` skip at line 77. *Wins if:* You care that an adopted orphan is brought fully up to date within the same `terum sync` run, and you accept that adoption's network write (`repo.safeWrite` push) now runs ahead of all local placement, so a push failure aborts the sync before anything is placed, and the adopt/decline prompts move ahead of the allowed-tools approval and newly-endorsed-batch questions.
- **medium — A failed appendExclude discards an already-completed placement and wedges later syncs** — `src/lib/placer.ts:62`
  - Root cause: `place()` treats the git-exclude bookkeeping as part of the placement transaction, even though the placement is already durable and visible by then. At src/lib/placer.ts:62 the `appendExclude` call sits inside the same `try` as the staging copy and the `rename`, i.e. *after* the new skill directory has already been renamed onto `destination`. The rollback `catch` (placer.ts:64-68) only removes the staging dir and restores `displaced` **when `destination` is missing** — after a successful rename `lstat(destination)` succeeds, so nothing is undone; the error just propagates. So any failure from `appendExclude` (git `rev-parse --git-path` non-zero, EACCES/EROFS/ENOSPC writing `.git/info/exclude`, or the `mkdir(dirname(exclude))` at placer.ts:100) throws out of `place()` with the skill folder already on disk. install.ts:95 therefore never reaches the `store.update` at install.ts:96-98, so no `placements[destination]` ledger entry is written. On the next attempt install.ts:89 computes `owned = false`, `inspect` (placer.ts:26-31) returns `foreign`, and install.ts:92 throws `Install target ... already contains another skill; retry with --force`. sync.ts:57-63 only swallows the `'no matching project context'` message, so that throw aborts the whole `sync` run — including the SessionStart hook path — on every subsequent invocation until the user deletes the directory by hand. The same call at sync.ts:102 is milder (the ledger entry already exists so `owned` stays true) but still aborts the entire sync loop for a cosmetic ignore-file line. Nothing in the spec makes the exclude line load-bearing for placement correctness: .planning/specs/2026-09-02-phase-1-build.md:116 describes it as a per-clone convenience ("safe whether the org commits or ignores `.claude/`, and immune to `git add -A`"), not an integrity requirement. (src/lib/placer.ts:62)
  - Rated: moderate / medium risk / pattern — siblings: Same shape — fallible work executed after the placement is already visible at `destination`, inside a `try` whose rollback cannot undo it: (1) src/lib/placer.ts:62 `appendExclude` (the reported site); (2) src/lib/placer.ts:63 `await snapshotSkillDirectory(destination)` in the same return expression — if the placed tree cannot be read back (e.g. an unreadable subdirectory whose mode `cp` preserved), `place` throws with the folder on disk and install.ts:96 again never records the ledger entry, producing the identical `foreign`/wedge outcome; option 1 does NOT cover this line (option 3 does). Inherited call sites that get the fix for free once placer.ts:62 is non-fatal: src/commands/install.ts:95 (the wedge path) and src/commands/sync.ts:102 (ledger entry survives, but the throw still aborts the whole sync loop). Also worth a look while sweeping: sync.ts:57-63 catches only the `'no matching project context'` message, so any other installOne error kills the entire sync run including the SessionStart hook — a broader error-tolerance gap this finding merely exposes.
  - Why one fix: Option 1 is the only one that fixes both halves of the problem at once: the skill stays installed and recorded, and future syncs keep working, while the user is still told the ignore line could not be written. Option 2 gets to roughly the same place but costs a test rewrite for no user-visible gain. Option 3 is stricter about leaving the disk clean, but it keeps a broken permissions bit on one file able to fail every install and every sync — the loud-failure loop the finding is about. Option 3 is still worth doing later as extra hardening (it also covers the rarer case where reading back the freshly placed folder fails); it is not a competing choice.
  - **Option 1: Non-fatal exclude append, surfaced as a notice (recommended)** — Depth 3/4 · Cost 1/4. In src/lib/placer.ts:62, wrap the `appendExclude` call in its own try/catch and, on failure, push a human-readable line ("Placed <destination> but could not add .claude/skills/<name> to .git/info/exclude: <reason>") onto a `notices: string[]` added to `place()`'s return value. Callers surface it: src/commands/install.ts:95-98 prints each notice via `io.print` after the ledger `store.update`, and src/commands/sync.ts:102-103 feeds them into the existing `notice()` helper so they land in `SyncResult.notices`. Add a placer test that makes the exclude write fail (stub `Runner` returning a path under a read-only dir, or a non-zero `rev-parse`) and asserts `place` still resolves with the directory in place plus a notice; optionally an install test asserting the ledger entry exists and a re-run does not report `already contains another skill`. *Wins if:* Wins whenever a placed skill must stay recorded and re-syncable even if the repo's .git/info/exclude is unwritable, while still telling the user the ignore line is missing — lowest cost, leaves the existing exclude test (src/lib/__tests__/placer.test.ts:27-41) green.
  - Option 2: Move the exclude append out of place() into the callers, after the ledger write — Depth 3/4 · Cost 2/4. Drop the `projectRoot`/`runner` options from `place()` (src/lib/placer.ts:47,62) so `place` is purely filesystem work. Call the already-exported `appendExclude` from src/commands/install.ts right after the `store.update` that records `fresh.placements[placed.path]` (install.ts:96-98) and from src/commands/sync.ts right after the fingerprint update (sync.ts:103), wrapping each in try/catch that prints/notices the failure. Update src/lib/__tests__/placer.test.ts:27-41 to exercise `appendExclude` directly instead of through `place`. *Wins if:* Wins if the team wants `place()` to own only the copy/rename atomicity and to keep all git-side effects in the command layer, and is willing to rewrite the placer exclude test to get that separation.
  - Option 3: Make place() transactional: undo the rename on any post-rename failure — Depth 4/4 · Cost 2/4. Restructure src/lib/placer.ts:56-68 to track that the rename succeeded, and in the rollback `catch` remove the newly visible `destination` and restore `displaced` before rethrowing, so `place()` is strictly all-or-nothing for every step after the copy (covers the `appendExclude` call at line 62 and the `snapshotSkillDirectory(destination)` at line 63). Keep exclude failures fatal. *Wins if:* Wins if the invariant that matters most is "a throwing place() never leaves anything on disk" (also covering a failing snapshot), and it is acceptable that `install` and every subsequent `sync` still fail loudly and repeatedly while .git/info/exclude stays unwritable.
- **medium — uninstall re-implements ref/team parsing and rejects the qualified refs its own error advertises** — `src/commands/uninstall.ts:37`
  - Root cause: `uninstall.run()` resolves the team *before* it ever looks at the ref, then hands the ref to a resolver that cannot parse it. Line 19 picks the team from `--team` or "there is exactly one configured team", and line 20 throws `Select a configured team with --team or a qualified ref.` when neither holds. Line 37 then calls `findSkill(clone, team, args.ref.split('@')[0]!)`, and `findSkill` (src/lib/skills.ts:33-41) matches only an exact `skills/<name>` folder name or a UUID prefix. `split('@')[0]` strips a version suffix and nothing else, so the `<team>/<name>` and `<org>/<repo>/<skill>` forms that §6 of the spec defines for every ref (.planning/specs/2026-09-02-phase-1-build.md:249-253, and §6 `uninstall <ref>` at line 311) are never decomposed. Net effect: with two teams configured, `terum-skills uninstall alpha/sample` fails at line 20 with an error that tells the user to supply a qualified ref — the one thing the code below cannot parse; with one team configured it reaches line 37 and fails with `No skill alpha/sample in team alpha.`. The reason the grammar is missing rather than wrong is that it exists exactly once, as module-private helpers `parseRef` (install.ts:150) and `teamForReference` (install.ts:160) inside install.ts, so a second verb taking a `<ref>` has nothing to import and re-implements a strictly weaker version. `parseRef` also gets the version split right (`lastIndexOf('@')` guarded by `at > 0`) where uninstall's `split('@')[0]` returns `''` for a leading-`@` input. (src/commands/uninstall.ts:19 (team resolved before the ref is parsed; payload at src/commands/uninstall.ts:37), because the canonical ref grammar is module-private at src/commands/install.ts:150-171)
  - Rated: moderate / medium risk / pattern — siblings: Same "each command re-implements ref/team resolution and the shared bodies get copied" mistake, all locations found:
- src/commands/uninstall.ts:19-20 and :37 — the finding site: inline single-team-or---team selection plus `split('@')[0]`, no `<team>/<name>` or `<org>/<repo>/<skill>` handling.
- src/commands/share.ts:154 — a third, independent `selectTeam` with its own divergent message (`Select a team with --team.`) and no ambiguity hint; would fold into the shared `teamForReference`.
- src/commands/install.ts:150-171 — the canonical `parseRef`/`teamForReference`, module-private, which is why the duplication happens at all.
- src/commands/uninstall.ts:22 and :28 vs src/commands/install.ts:136-148 — member/project argument normalization duplicated; install's `parseOperation` rejects `@` pins on those forms (install.ts:139, :144), uninstall silently accepts them (a member literally named `seed@1` would be looked up verbatim).
- src/commands/install.ts:190 and src/commands/uninstall.ts:81 — byte-identical `samePending` helpers.
- src/commands/install.ts:101-110 and src/commands/uninstall.ts:66-76 — near-identical `people/<handle>.json` safeWrite mutate bodies (read, parse, filter `installed` by id+scope, adjust `declined`, `tree.set`).
  - Why one fix: Both options fix the user-visible bug in exactly the same way and by exactly the same amount — after either, `uninstall alpha/sample` and `uninstall org/repo/sample` work the way `install` already does, and both verbs give the same ambiguity message. The only difference is where the shared code lives. Option 0 puts it in the library module that already holds the other skill-lookup helpers, which costs one extra file move and, in exchange, gives `share`'s third hand-rolled copy of team selection somewhere to go later; option 1 leaves it inside the install command and saves that move. Nothing about the product hangs on the choice, so the slightly tidier home is worth the extra few minutes. One genuine question does sit inside the fix either way, and an implementer should answer it rather than inherit today's silent behavior: what should `uninstall sample@1.2.3` do? Right now the version is quietly thrown away and the skill is removed entirely, which is defensible (uninstall removes by skill, not by version) but is currently an accident rather than a decision.
  - **Option 1: Lift parseRef/teamForReference into src/lib/skills.ts and reuse in both verbs (recommended)** — Depth 4/4 · Cost 2/4. Move `parseRef` (install.ts:150-159) and `teamForReference` (install.ts:160-171) into src/lib/skills.ts, which already owns findSkill/readTeam/readPerson and can import `Config` from ./schema.js and `normalizeRemote` from ./remote.js (no cycle: remote.ts and schema.ts are leaves). install.ts imports both and keeps its one-line `selectTeam` wrapper (install.ts:172) unchanged. In uninstall.ts, parse the ref in the skill branch and derive the team from it: replace lines 19-20 with `const team = await teamForReference(config, args.team)` for the member/project branches, and in the skill branch (after the line 36 guard) do `const reference = parseRef(args.ref)` then `const team = await teamForReference(config, reference.team ?? args.team, reference.remote, reference.name)`, passing `reference.name` — not `args.ref.split('@')[0]` — to findSkill at line 37. Decide explicitly what a version pin means for uninstall (today it is silently dropped): either ignore `reference.version` deliberately with a comment, or reject it the way install.ts:139/144 rejects pins it cannot honor. Update the collocated test at src/commands/__tests__/uninstall.test.ts:12-15, whose expectation `'Select a configured team'` becomes the shared `'ambiguous'` message (matching install.test.ts:13), and add cases for `team/skill` and for a version-pinned ref. *Wins if:* You want one ref grammar living where every verb can reach it — share.ts:154's third, divergent copy of team selection (`Select a team with --team.`) can then adopt it too, and the next verb that takes a <ref> inherits the right behavior for free.
  - Option 2: Export the two helpers from install.ts and import them in uninstall.ts — Depth 4/4 · Cost 1/4. Add `export` to `parseRef` (install.ts:150) and `teamForReference` (install.ts:160), leaving them in place, and import them into uninstall.ts. Identical call-site rewrite and identical test updates as option 0; no file move. Import graph stays acyclic — install.ts does not import uninstall.ts, and sync.ts already imports from both. *Wins if:* You want the smallest reviewable diff before a release cut and are content leaving a shared grammar inside a command module, where share.ts:154 is unlikely to ever reach for it.
- **medium — Scope equality by JSON.stringify breaks against the schema's own passthrough fields** — `src/commands/uninstall.ts:56`
  - Root cause: `uninstallOne` decides which local placements to delete and which people-file `installed` entry to drop by full structural serialization of the scope object: `JSON.stringify(entry.scope) === JSON.stringify(input.scope)` (src/commands/uninstall.ts:56 and :71). But the two sides come from different origins. `input.scope` for `uninstall member <handle>` (uninstall.ts:24) and for `uninstall <ref>` (uninstall.ts:44) is read out of the team repo's people file through `personSchema` → `installedSchema.scope` → `scopeSchema`, which is `z.object({...}).passthrough()` (src/lib/schema.ts:19-22) and therefore keeps any key a newer or hand-edited client wrote. The other side, `entry.scope` from the local `placements` ledger, was written by `installOne` from `selectScope()` (src/commands/install.ts:97, :185-189) and is always the bare `{kind}`/`{kind, project}` pair. I confirmed against the pinned zod 4.5.4 that `.passthrough()` normalizes key ORDER (`{project:'p',kind:'project'}` parses to `{"kind":"project","project":"p"}`, so ordering is not the hazard) but preserves unknown keys verbatim (`{"kind":"project","project":"p","agent":"claude-code"}` survives intact). So a scope carrying any extra key can never string-equal the ledger's canonical form: the filter at :56 matches nothing, no directory is removed, the filter at :71 drops nothing, and the command still returns `success` with `removed: 0` — a silent no-op reported as a completed uninstall. The underlying cause is that "scope identity" has no canonical definition anywhere in the codebase; six call-sites each re-implement it inline as byte equality over a schema that is deliberately non-canonical. (src/commands/uninstall.ts:56)
  - Rated: moderate / medium risk / pattern — siblings: The same "scope equality by JSON.stringify" mistake appears at eight locations across three commands; all were read and confirmed to compare a scope that can originate from the passthrough-parsed people file or config file against a locally constructed canonical one. Sweep worklist:
- src/commands/uninstall.ts:56 — selects which placements to delete from disk (the cited site).
- src/commands/uninstall.ts:71 — selects which `installed` entry to drop from the people file.
- src/commands/uninstall.ts:81 — `samePending` compares `scope: unknown` legs; governs de-duplication of pending intent.
- src/commands/install.ts:106 — filters the existing `installed` entry before re-pushing it; a mismatch leaves a duplicate entry for the same skill+scope in the shared people file.
- src/commands/install.ts:190 — `samePending`, a verbatim duplicate of uninstall.ts:81.
- src/commands/sync.ts:141 — skips placements that have pending intent; a mismatch makes reconcileOrphans treat an in-flight placement as an orphan.
- src/commands/sync.ts:147 — decides a placement is orphaned because no `installed` entry matches; a mismatch prompts "Adopt orphaned placement at ..." for a skill the person already has installed.
- src/commands/sync.ts:157 — the guard inside the adopt safeWrite; a mismatch appends a second `installed` entry for the same skill+scope.
Note that uninstall.ts:81 and install.ts:190 are byte-identical duplicated helpers, so the sweep is also the natural moment to hoist `samePending` into one shared definition.
  - Why one fix: One option clearly wins; there is no product decision hiding here. The question "what makes two scopes the same?" already has a single answer written into the code: the scope union is exactly `{kind}` or `{kind, project}` (src/lib/placer.ts:10), so comparing those two fields is the complete and correct definition today. Option 1 does the same thing but only in the one command we caught, leaving the identical trap armed in sync and install. Option 2 looks cheapest — delete one word from the schema — but it fixes the comparison by throwing away data: this tool's whole design is that an older copy of the CLI can safely rewrite a shared team file written by a newer copy, and stripping unknown fields breaks exactly that promise for every entry it rewrites, not just the one being changed. So Option 0 is the only one that both fixes the bug and keeps the forward-compatibility the schema was written for. One maintenance note, not a fork: if `scope` ever gains a field that genuinely distinguishes two placements (an `agent` key is the obvious candidate, since the placement path table is already keyed by agent), `sameScope` must be updated at the same time — which is the point of having one function instead of six copies.
  - **Option 1: sameScope helper, swept across every scope comparison (recommended)** — Depth 4/4 · Cost 1/4. Add `export function sameScope(a: PlacementScope, b: PlacementScope): boolean` beside the `PlacementScope` type in src/lib/placer.ts:10 (src/lib/schema.ts beside `scopeSchema` is an equally defensible home) comparing `a.kind === b.kind` plus `a.project === b.project` when kind is 'project'. Replace the six inline serializations with it: src/commands/uninstall.ts:56, uninstall.ts:71, src/commands/install.ts:106, src/commands/sync.ts:141, sync.ts:147, sync.ts:157 — all three files already import from placer.js, so no new import edges. Then narrow the two verbatim-duplicated `samePending` helpers (uninstall.ts:81, install.ts:190) from `scope: unknown` to `PlacementScope` and route their scope leg through `sameScope` too, so an interrupted op whose stored pending scope carries an extra key is not re-pushed as a duplicate. Add a case to src/commands/__tests__/uninstall.test.ts modelled on the existing 'uninstalls member skills sequentially' test but seeding `people/seed.json` with `scope: { kind: 'global', agent: 'claude-code' }`, asserting `removed: 1`, an emptied `placements`, and an emptied `installed`. *Wins if:* Wins whenever scope keeps its forward-compatible passthrough — i.e. unknown keys written by a newer client must survive an older client rewriting the shared people file. That is the standing posture: every object in schema.ts is `.passthrough()`.
  - Option 2: Canonicalize scope at uninstall's two entry points only — Depth 2/4 · Cost 0/4. In src/commands/uninstall.ts add a small local `canonical(scope)` returning `{ kind: 'project', project: scope.project }` or `{ kind: 'global' }`, and wrap the remote-sourced scopes before they reach `uninstallOne`: `scope: item.scope` at line 24 (the `member` loop) and `scope: entry.scope` at line 44 (the single-`ref` loop). uninstallOne then only ever compares two locally-produced canonical objects, so lines 56 and 71 can stay as they are. install.ts and sync.ts are left untouched. *Wins if:* Wins if you want the smallest possible diff on the one command that is demonstrably broken and are content to leave sync's orphan adoption (sync.ts:141/147/157) and install's replace-existing-entry filter (install.ts:106) exposed to the identical mismatch.
  - Option 3: Strip unknown keys: drop .passthrough() from scopeSchema — Depth 3/4 · Cost 1/4. In src/lib/schema.ts:19-22, remove `.passthrough()` from both members of `scopeSchema` so zod's default strip mode discards unknown keys at parse time. Every downstream comparison then sees a canonical two-field object and all six call-sites become correct without being edited. *Wins if:* Wins only if the team rules that scope is a closed shape with no forward-compatible extension. Otherwise it trades this bug for a worse one: the people file is rewritten wholesale at install.ts:107 and uninstall.ts:75, so an older client would silently erase a newer client's scope fields from the shared git repo for every entry it touches, not just the one being changed.
- **medium — One unreadable team aborts the whole search after other teams' hits were printed** — `src/commands/search.ts:19`
  - Root cause: `src/commands/search.ts` performs every per-team clone read inside the function's single outer `try` (opened at :13, caught at :44), so the loop has no per-iteration isolation. `readTeam(clone)` (:21), `skillRecords(clone, team)` (:22) and `readdir(join(clone, 'people'))` (:23) all throw for an ordinarily reachable state — a `teams.<name>` entry that has no clone on disk. That state is documented, not corruption: `src/lib/login.ts` only calls `bindTeam(fresh, team, { remote, token: ... })` (login.ts:41) and never clones, and `src/lib/schema.ts:55-57` says handle "is null until `team create`/`team join` has proven it against the roster (a `login` that runs first stores only the remote and the token)". Because hits for earlier teams are already printed at :38 before the throw, the user gets real results on stdout *plus* a raw `ENOENT ... scandir '<root>/teams/b/team.json'` and a non-zero exit, so a script keying on the exit code discards correct results. The same all-or-nothing abort fires when a single skill in any one team has malformed frontmatter, since `skillRecords` throws for the whole clone (`src/lib/skills.ts:26-27`). Verified: no deferral exists (no PRODUCT-CONCERNS.md, no `.deferred.md` entries), and the build spec's `search` contract (.planning/specs/2026-09-02-phase-1-build.md:305) says only that it reads "each configured team's clone" — it never asks for whole-command failure on one bad clone. (src/commands/search.ts:19 (loop body reads at :21-23; single outer catch at :44))
  - Rated: trivial / low risk / pattern — siblings: Same all-or-nothing per-team loop elsewhere (sweep worklist):
- src/commands/sync.ts:41-66 — `for (const [team, binding] of Object.entries(config.teams))` runs `runner.run('git', ['pull', '--ff-only'], { cwd: clone })` at :43 and throws `Could not fast-forward ${team}` at :44. For the same bound-but-not-cloned team, spawn rejects outright (src/lib/runner.ts `child.on('error', reject)` with a nonexistent `cwd`), so the user cannot even self-heal by running the command the notice would recommend; either way every other team's pending install/uninstall replay is abandoned.
- src/commands/sync.ts:110-125 — the newly-endorsed-globals loop calls `readPerson` / `skillRecords` / `readTeam` per team with no isolation; one unreadable team aborts the whole sync after earlier teams were already placed on disk.
- src/commands/sync.ts:130 — `for (const team of Object.keys(...)) await writeStamp(store, team)` has the same shape (lower impact; mkdir/writeFile under the private root rarely fail).
- Counter-example to copy, not fix: src/commands/share.ts:66-117 `reconcileShared` already does per-entry `catch (error) { io.print(...); continue; }` at :72-73 and `continue` at :70/:74.
  - Why one fix: Option 1 dominates at this site. Option 2 costs the same but only handles the "team not cloned yet" case — a single badly formatted skill file in one team would still wipe out the whole search after results were printed. Option 3 fixes more, but only by also changing how `sync` behaves when one team fails to pull, and `sync` writes to the team repo, so whether it should push on past a broken team is a separate call that shouldn't ride along with a fix to a read-only command. Plain English: make `search` report what it could read and print a one-line note about the team it couldn't, then decide about `sync` separately.
  - **Option 1: Per-team try/catch with a skip notice (recommended)** — Depth 2/4 · Cost 0/4. In src/commands/search.ts, wrap the per-team body (:20-39) in its own try/catch inside the `for` at :19. On error, `io.print` one line naming the team and the message (e.g. "<team> could not be read (<message>); run `terum-skills sync`.") and continue to the next team; leave `await staleNotice(...)` (:40) outside the inner try so the stale line still prints for a skipped team, and keep the outer try/catch at :13/:44 for store/config failures. The command keeps returning `success(hits)` with the teams it could read. Add a collocated case to src/commands/__tests__/search.test.ts: two configured teams where the second has no clone directory (the post-`login`, pre-`team join` state) -> `ok: true`, the first team's hit still in `value`, and the notice printed. This mirrors reconcileShared in src/commands/share.ts:72-73, which already does catch-print-continue per entry. *Wins if:* Wins whenever `search` should stay a read-only, best-effort report: it removes both failure classes at this site (missing clone AND one malformed SKILL.md) for the same few lines as any narrower fix.
  - Option 2: Pre-filter teams whose clone is missing — Depth 1/4 · Cost 0/4. Before the reads at src/commands/search.ts:21, skip teams whose clone directory is absent using the existing `existsDirectory` helper (src/lib/skills.ts:110) — print "<team> is not cloned yet; run `terum-skills sync`." and continue — leaving the outer try/catch to handle everything else unchanged. *Wins if:* Wins if the team wants a genuinely corrupt clone (malformed frontmatter, unparseable team.json) to keep failing the whole command loudly with a non-zero exit, and only the documented not-yet-cloned state to degrade to a notice.
  - Option 3: Shared per-team isolation helper for every multi-team loop — Depth 4/4 · Cost 2/4. Add one helper (lib/config.ts or a new lib/teams.ts) that iterates `config.teams`, isolates each iteration, and returns per-team notices; adopt it at src/commands/search.ts:19 and at the sync loops (src/commands/sync.ts:41 and :110), so one unreachable or broken clone never abandons the other teams' work in either verb. *Wins if:* Wins if the sync sweep is being done in the same change and the team wants a single isolation rule for all multi-team loops — but it changes sync's write-path semantics (a failed `git pull` for one team would stop aborting the rest), which needs its own decision and tests.
- **medium — search tests never assert the install count or the endorsement classification the command exists to report** — `src/commands/__tests__/search.test.ts:41`
  - Root cause: The `searchFixture` helper never produces the data that would make the two derived columns non-trivial, and no assertion in the suite ever names them. `searchFixture` (src/commands/__tests__/search.test.ts:83) always starts from the default `bareTeam()` roster, whose only member is `person('seed')` with `installed: []` (src/lib/__tests__/fixtures.ts:52), and its optional third parameter writes only `teamJson.projects` — it has no way to set `team.json.global` and no way to seed a second person. So in all eight tests `installs` is necessarily 0 and `endorsed` is 'personal' (or 'project:product' in the one project test, which asserts only `expect.objectContaining({ name: 'target' })` at :41). Consequently `search.ts:34-36` — the endorsement ternary and the `people.reduce` install tally — plus the whole `readdir(join(clone,'people'))` / `readPerson` pipeline at search.ts:23 are unconstrained: replacing them with the literals `installs: 0` and `endorsed: 'personal'` keeps the suite green. The build spec (.planning/specs/2026-09-02-phase-1-build.md:305) requires both columns in search's output line, so the untested code is a spec deliverable, not incidental. (src/commands/__tests__/search.test.ts:83 (fixture cannot express installs or global endorsement); assertion gap manifests at src/commands/__tests__/search.test.ts:41)
  - Rated: moderate / low risk / isolated
  - Why one fix: No real trade-off. Both options are test-only and both are safe to undo; the recommended one costs maybe twenty extra lines in a helper that already exists in the same file, and in exchange it is the only one that actually catches the bug class in question — a wrong install tally or a wrong endorsement label. The cheap option leaves the install count pinned at zero everywhere, so someone could delete the entire roster-reading code from the search command and the tests would still pass. There is no product or design question here: the spec already says the search line must show an install count and an endorsement badge, so the only question is whether the tests check it, and the fuller option is barely more work.
  - **Option 1: Seed a roster + global endorsement in searchFixture and assert both columns across all three branches (recommended)** — Depth 4/4 · Cost 1/4. In src/commands/__tests__/search.test.ts, widen `searchFixture` (line 83) with a fifth optional argument `extra?: { global?: string[]; people?: Record<string, string[]> }`: (a) run the existing team.json rewrite block when `projects || extra?.global` and set `teamJson.global = extra.global ?? []` alongside `teamJson.projects`; (b) before the `cloneWithIdentity` call, for each `[handle, ids]` in `extra?.people ?? {}` do `await pushFromSeed(fixture.seed, 'people/' + handle + '.json', JSON.stringify(person(handle, { installed: ids.map((id) => ({ id, version: null, scope: { kind: 'global' }, since: '2026-09-04' })) }), null, 2) + '\n')` — the exact `installed` shape already used at src/commands/__tests__/sync.test.ts:289 and install.test.ts:230, and valid per `installedSchema` (src/lib/schema.ts:24). Then add one test with three skills: A only in `global`, B only in `projects.product`, C in neither; fixture call `searchFixture('team', skills, { product: [B] }, undefined, { global: [A], people: { bob: [A] } })`; run with `term: ''` and assert the returned `value` array (ordered by skill folder name, since skillRecords sorts) contains `objectContaining({ installs: 1, endorsed: 'global' })` for A, `{ installs: 0, endorsed: 'project:product' }` for B, `{ installs: 0, endorsed: 'personal' }` for C, plus `expect(io.lines.join('\n')).toContain('installs:1  global')` so the printed column is pinned too. Test-only; do not touch search.ts. *Wins if:* You want the suite to actually constrain search.ts's two spec-required derived columns — this is the only option under which hard-coding `installs: 0` / `endorsed: 'personal'` turns the suite red, and it is still test-only and revert-safe.
  - Option 2: Tighten the assertion in the existing project-filter test only — Depth 2/4 · Cost 0/4. In src/commands/__tests__/search.test.ts:41, replace `expect.objectContaining({ name: 'target' })` with a full object assertion including `endorsed: 'project:product'` and `installs: 0`, and add an `io.lines` check for the printed `installs:0` column in the first test. No fixture change. *Wins if:* You want a one-line change with zero fixture churn and accept that `installs` stays pinned at 0 in every test (so a hard-coded `installs: 0` still passes) and that the `global` branch of the endorsement ternary stays completely unexecuted.
- **medium — `previousAuthor` replaces rather than widens the accepted pre-image author, and only one of two `sync` skill-writes supplies it** — `src/lib/guard.ts:71`
  - Root cause: `ownsSkill` treats `previousAuthor` as a REPLACEMENT for the actor's own author line in the pre-image check — `if (before !== undefined && authorOf(before) !== (previousAuthor ?? me)) return false;` (src/lib/guard.ts:71) — instead of widening the accepted set to {me, previousAuthor}. That makes the field mandatory-once-relevant: any `sync` skill-folder write whose repo pre-image still carries an older author line is refused unless the caller explicitly asserts that older line.

Of the three `action: 'sync'` skill-folder writers in src/commands/share.ts, only two supply it. `reconcileShared` passes `previousAuthor: record.frontmatter.metadata.author` at share.ts:98 and share.ts:107 — the pre-image's OWN author, so the row-a check ("the committed SKILL.md's `metadata.author` decides who may touch an existing folder", guard.ts:54-58) degenerates to a tautology on that path. `resolveDivergence` (share.ts:131) performs the same §5.3 mirror for `share --keep-source <id>` and passes no `previousAuthor` at all, and unlike the reconciler it never runs `injectManagedFields` over the source before mirroring.

Concrete failure (verified by reading the code path end to end): a user shares `foo` (committed author `Ann <ann@x.test>`), later changes `display_name`/`email`, then hits the divergence branch at share.ts:90-93. That branch repairs the SOURCE's managed fields (share.ts:82-83) but `continue`s before `refreshRepo`, so the repo copy keeps the old author. The printed remedy is `share --keep-source <id>`; running it reaches share.ts:131 with `before` author `ann <ann@x.test>`, `me` = the new line, `previousAuthor` undefined → `ownsSkill` false → `GuardError: Write guard refused skills/foo/SKILL.md for sync by <handle>`, while the automatic reconciler succeeds on byte-identical content. A second variant exists even with `previousAuthor` supplied: if `--keep-source` is run without a preceding `sync`, the un-refreshed source still names the old author, so the post-image check `authorOf(after) !== me` refuses too.

Coverage gap: src/lib/__tests__/guard.test.ts:114-123 exercises `guard()` only with a hand-built context, and the `--keep-source` integration test (src/commands/__tests__/share.test.ts:163) never changes identity, so neither call-site shape is covered. (src/lib/guard.ts:71 (substitution instead of widening); missing supplier + missing managed-field refresh at src/commands/share.ts:131)
  - Rated: moderate / medium risk / pattern — siblings: Same-pattern locations found (sweep worklist; all skill-folder writers were enumerated via `grep -rn "safeWrite(" src` — the only others are people/team writes at src/commands/team.ts:127, src/commands/sync.ts:152, src/commands/sync.ts:164, src/commands/install.ts:101, src/commands/uninstall.ts:66, which never touch `skills/**` and are unaffected):
- src/commands/share.ts:131 — `resolveDivergence` / `share --keep-source`: `action:'sync'` skill mirror with NO `previousAuthor` and no `injectManagedFields` refresh of the source. This is the failing site.
- src/commands/share.ts:98 and src/commands/share.ts:107 — `reconcileShared`: pass `previousAuthor: record.frontmatter.metadata.author`, i.e. the pre-image's own author, so the row-a pre-image check is satisfied tautologically on the whole sync path; the only surviving check there is that the post-image names the actor. If the widening in option 1 lands, consider whether these should keep asserting the pre-image author at all or assert the actor's prior config identity.
- src/lib/guard.ts:66 and src/lib/guard.ts:71 — the `previousAuthor ?? me` substitution and its comment at guard.ts:63-65; this is what turns a missing `previousAuthor` into a refusal instead of a fallback.
- src/commands/share.ts:58 — the only other skill-folder writer (`action:'share'`, new folder, `before === undefined`): correct as-is, no change needed.
Test gaps to close in the same sweep: src/lib/__tests__/guard.test.ts:114-123 (guard-only, hand-built context; no case where the pre-image already names the actor while `previousAuthor` names an older line) and src/commands/__tests__/share.test.ts:163 (the `--keep-source` integration test never changes `display_name`/`email`, so it cannot see this refusal).
  - Why one fix: Option 1 is the one to take. Option 2 is the same idea done halfway: it unsticks the exact path in the bug report but still refuses if the user runs `share --keep-source` before a `sync` has repaired their skill file, so the same "the tool told me to run this and it refused" complaint comes back in a slightly different order. Option 3 does everything option 1 does plus merges the two near-identical copy-to-repo routines into one; that is tidier for the future but is a restructure of a brand-new file that is still being reviewed, and it buys no behaviour option 1 does not already deliver — it can be done later on its own merits. No product decision is in play: the spec already says the author line is refreshed from the user's config, so making both paths agree is a correctness fix, not a choice.
  - **Option 1: Widen the guard and make `--keep-source` a full §5.3 mirror (recommended)** — Depth 3/4 · Cost 1/4. (1) src/lib/guard.ts:71 — change the substitution to a widening: `if (before !== undefined && authorOf(before) !== me && authorOf(before) !== previousAuthor) return false;` (with `previousAuthor` left as the sync-only, normalized value from line 66), so the actor's own author line is always accepted for the pre-image and `previousAuthor` only ADDS the one line being replaced; update the comment at guard.ts:63-65 to say 'may additionally present' rather than 'may present the committed author it is replacing'. (2) src/commands/share.ts:120-135 (`resolveDivergence`, keepSource branch) — before `sourceFiles(tracked.source)`, mirror what the reconciler does at share.ts:75-83: `const team = await readTeamPolicy(clone);` read `join(tracked.source,'SKILL.md')`, `injectManagedFields(contents,{license: team.license, id, author})`, write it back if changed; then pass `previousAuthor: record.frontmatter.metadata.author` in the safeWrite options at share.ts:131. (3) Tests: add a guard.test.ts case asserting sync is allowed when the pre-image already carries the actor's CURRENT author while `previousAuthor` names an older one; extend the `--keep-source` test in src/commands/__tests__/share.test.ts:163 with an email change before the divergence so it exercises the refused shape. *Wins if:* You want `share --keep-source` to work after an identity change in every order the user can reach it (with or without a prior `sync`), without restructuring the three sync writers.
  - Option 2: Minimal: supply `previousAuthor` at share.ts:131 only — Depth 2/4 · Cost 0/4. Add `previousAuthor: record.frontmatter.metadata.author` to the safeWrite options at src/commands/share.ts:131, leaving guard.ts:71 and the missing `injectManagedFields` refresh alone. *Wins if:* You want the smallest possible change to unblock the documented prompt→command path and are willing to leave the run-`--keep-source`-without-a-prior-`sync` variant refused (source still names the old author, so the post-image check fails) and the stale-clone case (repo already refreshed by another machine, so `before` names `me` but `previousAuthor` names the old line) refused.
  - Option 3: Extract one shared mirror helper for both sync writers — Depth 4/4 · Cost 2/4. In src/commands/share.ts, factor share.ts:107 and share.ts:131 into a single `mirrorSourceToRepo(clone, binding, record, source, {license, id, author})` that refreshes the source's managed fields, builds `sourceFiles`, and calls safeWrite with `action:'sync'` + `previousAuthor: record.frontmatter.metadata.author`; call it from both `reconcileShared` and `resolveDivergence`, and apply the same guard.ts:71 widening as option 1. *Wins if:* You expect more `sync` skill-folder writers (or more managed fields) to be added and want the call-site split to be impossible by construction rather than fixed twice — worth it only if you accept a structural refactor of a file that is itself still under review.
- **medium — Five newly registered CLI verbs have no wiring test; `install member` with no value degrades to a raw ENOENT** — `src/cli.ts:45`
  - Root cause: `src/cli.ts:45` registers `install <ref> [value]` with the selector operand OPTIONAL, and its ternary maps `ref === 'member'` to `{ kind: 'member', member: value }` while dropping `ref` entirely. `terum-skills install member` therefore hands the command layer `{ kind: 'member', member: undefined }` with no recoverable operand. `src/commands/install.ts:138` completes the failure by treating "nothing was supplied" as a valid handle — `const member = args.member ?? args.ref ?? ''` coerces to the empty string rather than throwing the usage error that already exists two lines below (install.ts:147), so `readPerson(clone, '')` (src/lib/skills.ts:44) opens `<clone>/people/.json` and the user sees `ENOENT ... open '.../people/.json'`. `src/commands/uninstall.ts:22` repeats the same coercion. The reason none of this was caught is the collocated wiring test: the `harness()` at `src/__tests__/cli.test.ts:11` still builds `buildProgram(execute, { login, team })`, and because the five new `CliVerbs` fields were declared optional (`install?`, `share?`, ...), every un-stubbed verb falls through `(verbs.install ?? install)` to the REAL command instead of failing loudly — so the only non-trivial logic added to cli.ts (the double ternary, five verb registrations) has zero coverage and the hole was invisible. (src/cli.ts:45 (optional `[value]`, `ref` not forwarded); coercion at src/commands/install.ts:138 and src/commands/uninstall.ts:22; untested harness at src/__tests__/cli.test.ts:11)
  - Rated: moderate / low risk / pattern — siblings: Same "missing selector coerced to empty string" mistake, four sites (sweep worklist):
- src/commands/install.ts:138 — `const member = args.member ?? args.ref ?? '';` → `readPerson(clone, '')` → ENOENT on `people/.json`.
- src/commands/install.ts:143 — `const project = args.project ?? args.ref ?? '';` → `teamJson.projects['']` → misleading `Unknown project .` message.
- src/commands/uninstall.ts:22 — `readPerson(store.teamClone(team), args.member ?? args.ref ?? '')` → identical ENOENT.
- src/commands/uninstall.ts:28 — `const project = args.project ?? args.ref ?? '';` → `Unknown project .`.

Same "new verb registered with no wiring coverage" gap, five sites, none reached by the `harness()` at src/__tests__/cli.test.ts:11 (the optional `CliVerbs` fields make `(verbs.X ?? X)` fall through to the real command instead of failing the test):
- src/cli.ts:44 `share [path]` (--team/--allow-privileged/--keep-source/--keep-repo/--relocate/--forget)
- src/cli.ts:45 `install <ref> [value]` (the double ternary — the defect site)
- src/cli.ts:46 `uninstall <ref> [value]` (same ternary)
- src/cli.ts:47 `sync` (--hook/--prune, incl. the `options.hook ? {hook:true,...} : {...}` branch)
- src/cli.ts:48 `search <term>` (--category/--author/--project)

Not part of the sweep: `share [path]` being optional at src/cli.ts:44 is deliberate — the `--forget <id>` / `--relocate <id:path>` forms take no path (src/commands/share.ts:14-24 declares `path?`).

Internal callers are unaffected: src/commands/team.ts:155 and src/commands/sync.ts:50,56,123 use `installOne`/`uninstallOne`, never `run()`, so the new guards only ever see CLI (and future UI) input.
  - Why one fix: Option 1 clearly wins. Both options stop `terum-skills install member` from crashing with a confusing file-not-found error, but option 1 also protects the other way into the same code — the in-app UI planned for the next phase calls these exact functions directly — while option 2 only fixes the typed-command path and leaves the crash reachable from the app. Option 2's one genuine advantage is prettier built-in help text, which option 1 can match by putting the correct usage string inside the error message. Option 1 is also less code and less risk, since it adds a check rather than restructuring how commands are registered.
  - **Option 1: Reject an empty member/project selector in the command layer, and cover all five verbs in cli.test.ts (recommended)** — Depth 3/4 · Cost 1/4. (1) src/commands/install.ts `parseOperation` (lines 136-149): replace `const member = args.member ?? args.ref ?? ''` and `const project = args.project ?? args.ref ?? ''` with a resolved value that throws before the `@` check when it is empty — e.g. `Provide a handle: \`install member <handle>\`.` and `Provide a name: \`install project <name>\`.`, matching the wording already used at install.ts:147. (2) Mirror both guards in src/commands/uninstall.ts — line 22 (`readPerson(store.teamClone(team), args.member ?? args.ref ?? '')`) and line 28 (`const project = args.project ?? args.ref ?? ''`) — so `uninstall member` fails the same way. (3) src/__tests__/cli.test.ts: extend `harness()` (line 11) to stub `share`, `install`, `uninstall`, `sync`, `search` alongside `login`/`team`, each pushing `{ verb, ...args }` onto `calls`; add a wiring assertion for `install member u`, `install project p`, `install team/skill@ref`, `uninstall member u`, `sync --hook --prune`, `share --relocate id:/p`, `search t --category c`; and add one case that drives the REAL install verb for `install member` (no value) and asserts the failing Result carries the usage message, not an ENOENT. *Wins if:* Wins whenever the usage error must protect more than the terminal: the spec's library-first rule (§3/D23) has the phase-2 UI call the same `run(args, io)` functions, so a guard in the command layer covers both entry points, and the fix lands the regression net in the file that exists for exactly this purpose.
  - Option 2: Make member/project real commander subcommands so commander rejects the missing operand — Depth 2/4 · Cost 2/4. In src/cli.ts:45-46 replace each single `install <ref> [value]` / `uninstall <ref> [value]` registration with a parent command carrying two subcommands — `member <handle>` and `project <name>`, each re-declaring `--team` (and `--force` for install) — plus a default action handler on `<ref>` for the plain skill form. Commander then emits `error: missing required argument 'handle'` for `install member`, and `install --help` documents the three spec forms. Still requires the same cli.test.ts harness extension to cover all five verbs. *Wins if:* Wins if the team wants commander itself to own every usage error and `--help` to spell out the three grammar forms from spec §306, and accepts that a direct `run({ kind: 'member' })` call from the phase-2 UI still coerces to `''` and hits the same ENOENT.
- **medium — isHookSync duck-types SyncResult with no compile-time link and no test can reach it** — `src/index.ts:24`
  - Root cause: The hook's stderr formatting was put in the one module nothing can import or type-check against. `src/index.ts:24` re-declares `SyncResult`'s shape inline (`value is { hook: true; deferred: string[]; notices: string[] }`) instead of importing the interface exported at `src/commands/sync.ts:20`, so nothing ties the predicate to the type it is checking: rename or drop `notices`/`deferred` on `SyncResult` and `tsc` stays green while the predicate returns false at runtime and the spec-mandated line `N skills need review — run \`terum-skills sync\`` (spec .planning/specs/2026-09-02-phase-1-build.md:324, :374) silently stops being printed. The second half of the cause is placement: `execute` and `isHookSync` are unexported in a module whose body ends in a top-level `await buildProgram(execute).parseAsync()` (src/index.ts:29), so importing it from a test runs the whole CLI — no test can reach the announcement. `src/cli.ts` exists precisely because a prior review flagged the entrypoint as untestable (.planning/reviews/hybrid-working-2026-09-04-pass1.review.md:56) and injects `execute`/`verbs` so mapping is testable without a terminal (src/__tests__/cli.test.ts:10) — this new formatting logic was placed on the wrong side of that seam. The collocated sync tests assert only the returned arrays (src/commands/__tests__/sync.test.ts:65, :109, :119, :176, :240) and never the rendered stderr text. Note the split itself is forced and correct: eslint.config.js:16 exempts only `src/lib/prompt.ts` and `src/index.ts` from the `process.stderr` ban, so `sync.ts` must return strings and `index.ts` must write them — the fix is to move the strings, not the write. (src/index.ts:24)
  - Rated: moderate / low risk / isolated
  - Why one fix: Option 1 is a strict subset of option 0 — same idea, stops halfway — and option 2 does the same work but parks a formatting helper in the file whose own header says it does commander wiring and nothing else, while sync.ts is already where these stderr strings are written. Option 0 costs one extra small function and one extra test case and is the only one that leaves the spec's announcement wording actually checked by something. No product or design question is open here.
  - **Option 1: Own the announcement in sync.ts, write it in index.ts, assert the exact string (recommended)** — Depth 4/4 · Cost 1/4. In src/commands/sync.ts, next to `SyncResult` (line 20), export a pure `hookStderr(value: unknown): string[]`: it keeps the current runtime checks but narrows to `SyncResult & { hook: true }` (compile-anchored to the interface one line above it), returns `[]` for anything else, and otherwise returns `[...value.notices, ...(value.deferred.length ? [`${value.deferred.length} skills need review — run \`terum-skills sync\``] : [])]`. src/index.ts:13-16 collapses to `else for (const line of hookStderr(outcome.value)) process.stderr.write(`${line}\n`);` and the local predicate at src/index.ts:24-26 is deleted (index.ts keeps the process.stderr write, which eslint.config.js:16 only allows there). Add one case to src/commands/__tests__/sync.test.ts — reuse the existing deferral fixture at line 240 and assert `hookStderr(result.value)` equals the notice lines plus the literal spec sentence, and that a non-hook result yields `[]`. *Wins if:* Wins whenever the exact stderr wording is part of the contract (the spec quotes it verbatim at :324) — this is the only option under which any test can execute the announcement at all, and it puts the string next to the sibling `notices` strings sync.ts already authors.
  - Option 2: Type-anchor the predicate in place, change nothing else — Depth 2/4 · Cost 0/4. In src/index.ts add `import type { SyncResult } from './commands/sync.js';` and change line 24 to `function isHookSync(value: unknown): value is SyncResult & { hook: true }`, leaving the runtime checks and the formatting where they are. A rename or removal of `notices`/`deferred` on SyncResult then fails to compile at src/index.ts:14-15, where the narrowed value is read. *Wins if:* Wins if this branch has to land with the smallest possible diff and the team is content that the spec's stderr sentence stays permanently unverified — it closes the silent-drift half of the defect and none of the untestability half.
  - Option 3: Extract to cli.ts, test in cli.test.ts (the reviewer's suggestion) — Depth 4/4 · Cost 1/4. Export `hookLines(result: SyncResult): string[]` from src/cli.ts next to `buildProgram`; src/index.ts imports it and loops over it; assert the exact spec string in src/__tests__/cli.test.ts alongside the existing flag-mapping tests. *Wins if:* Wins if you want every user-facing string owned by the CLI layer rather than by commands/ — but src/cli.ts:12-15 documents itself as '§3: commander wiring only. Every verb is run(args, io) in src/commands; this file maps flags to it', and sync.ts already builds the sibling `notices` lines (sync.ts:79, :82, :91), so this option adds a second home for sync's output text.
- **medium — Newly-shipped vendored lock-dir assertions have no test now that the exclusion is dropped** — `tsconfig.build.json:6`
  - Root cause: The defect is a test-coverage gap, not a logic bug. This change deletes `"src/lib/placer/vendor/skillhub/skill-target-lock.ts"` from BOTH excludes (tsconfig.json and tsconfig.build.json:6), which is correct and necessary — `src/lib/placer.ts:6` now imports `acquireSkillTargetLock`, so the file must typecheck and must be emitted to `dist/` or the shipped `bin` would fail at runtime (re-adding the exclusion is not an available option). The consequence is that a module that was previously never compiled is now shipped product code. Two of its exports encode the module's only security invariant and no test reaches them: `assertPrivateLockDir` (skill-target-lock.ts:55-66) rejects a non-directory, a symlink, and a lock dir whose `uid` differs from the caller's; `ensurePrivateLockDir` (skill-target-lock.ts:68-81) repairs `details.mode & 0o077` back to 0o700. They matter because the lock dir is created in the shared, world-writable OS temp root — `join(tmpdir(), \`terum-skills-target-locks-${uid}\`)` (skill-target-lock.ts:43) — so these checks are what stop another local user from pre-creating, symlinking, or group/other-writing that directory and thereby redirecting or squatting every skill-target lock. The reason nothing covers them is `src/lib/__tests__/placer.test.ts:5`: it is the module's only test-side importer and it imports `skillTargetLockPath` alone. Its five cases (placer.test.ts:10, 27, 43, 65, 74) exercise acquisition, contention, and stale reclaim, all of which traverse `ensurePrivateLockDir`'s happy path only — the dir is freshly created at 0o700 by `mkdir`, `isDirectory()` is true, `isSymbolicLink()` is false, and the uid always matches. Every rejection branch and the chmod-repair branch are therefore dead to the suite: inverting `details.uid !== currentUid`, dropping `|| details.isSymbolicLink()`, or deleting the `chmod` call would leave `npm test` fully green. Sibling ownership/symlink guards in this repo are covered (teamRepo.ts:231's symlink refusal is asserted at teamRepo.test.ts:115-126), so this module is the outlier rather than an instance of a repo-wide habit. (src/lib/__tests__/placer.test.ts:5 (only importer of skill-target-lock; pulls in `skillTargetLockPath` alone, leaving skill-target-lock.ts:55-81 untested) — triggered by tsconfig.build.json:6 promoting that module to shipped dist/ code)
  - Rated: moderate / low risk / isolated
  - Why one fix: Both options add exactly the same tests and cost the same; the only difference is which file they live in, so there is no real trade-off to decide. A separate `skill-target-lock.test.ts` wins on a tie-break: the repo already keeps a dedicated suite for the other vendored file (`skill-fingerprint.test.ts`), these two functions are pure lock-directory-hygiene checks with nothing to do with the Placer's §7 behaviour that placer.test.ts documents, and §12 says its suite list is a minimum ("Named suites at minimum"), so an extra file breaks no rule. If a reviewer objects to a new file, moving the same two cases into placer.test.ts is a copy-paste with no loss of coverage — the decision is reversible and does not need a product call.
  - **Option 1: Dedicated vendored-module suite (recommended)** — Depth 3/4 · Cost 0/4. Add `src/lib/__tests__/skill-target-lock.test.ts`, mirroring the existing vendored-module suite `src/lib/__tests__/skill-fingerprint.test.ts`. Case 1 (pure, no fs): call `assertPrivateLockDir('/tmp/x', details, 501)` with four hand-built `LockDirectoryDetails` literals — `{isDirectory:()=>true,isSymbolicLink:()=>false,uid:501,mode:0o700}` must not throw; `isDirectory:()=>false` and `isSymbolicLink:()=>true` must each throw /unsafe/; `uid:502` must throw /owned by another user/. The interface is structural and unexported, so the literals need no production change. Case 2 (fs, `it.skipIf(process.platform === 'win32')`): `mkdir` a dir under `temporaryDirectory()` then `chmod(dir, 0o755)` (explicit chmod, because umask can already strip the bits mkdir was asked for), call `ensurePrivateLockDir(dir)`, and assert `(await lstat(dir)).mode & 0o077` is 0. Optionally a third case asserting `ensurePrivateLockDir` is idempotent on an already-0o700 dir. *Wins if:* Wins whenever the goal is simply that these three rejection branches and the chmod repair can no longer be silently deleted — it is test-only, costs minutes, is revert-safe, touches no production code, and follows the precedent already set for the other vendored file.
  - Option 2: Append the cases to placer.test.ts — Depth 3/4 · Cost 0/4. Same four assertPrivateLockDir shapes and the same ensurePrivateLockDir chmod-repair case, but added as two new `it` blocks inside the existing `describe('native Placer (§7)')` in `src/lib/__tests__/placer.test.ts`, extending the existing import on line 5 to `{ assertPrivateLockDir, ensurePrivateLockDir, skillTargetLockPath }`. This is the reviewer's suggestion. *Wins if:* Wins if the team wants the suite list in build spec §12 line 425 (`lib/__tests__/{guard,schema,version,readme,teamRepo,auth,placer,hook}.test.ts`) to stay the literal set of lib suites, or simply prefers no new test files — placer.test.ts already imports from both vendored modules, so the lock tests are not out of place there.
- **medium — An unguarded reconcileShared failure aborts the whole sync run** — `src/commands/sync.ts:67`
  - Root cause: `reconcileShared`'s per-skill loop (src/commands/share.ts:68-117) handles every *read-side* problem by printing one line and continuing (missing source :70, unreadable skill records :72-73, missing repo copy :74, two-sided divergence :91) but leaves its *write-side* failures unguarded: `throw new Error('Team … has no joined handle.')` at :97 and :105 (reachable — `handle` is nullable in `teamConfigSchema`, src/lib/schema.ts:58, so a team that was set up but never joined and still has a tracked `shared` entry hits it), plus everything `safeWrite` raises at :98/:107 — `PushRefused` (src/lib/teamRepo.ts:117), `SafeWriteExhausted` after the 30 s deadline (:122), plain `Error` from `requireGit` for any failed fetch/reset/add/commit (:75), and `lockfile.lock`'s ELOCKED when a second process holds the clone (:90). Because sync calls it bare on its main path (src/commands/sync.ts:67) inside the outer try that converts any throw into `failure(...)` (:132), a single transient share push aborts the whole run: the placements loop (:70-106, which is what quarantines hand-edited copies and applies updates), the newly-endorsed batch (:110-125), `reconcileOrphans` (:127) and `writeStamp` (:130) are all skipped, and the user sees only the share error. (src/commands/share.ts:68 (unguarded write paths in reconcileShared's per-skill loop, :97-:107), surfacing at src/commands/sync.ts:67)
  - Rated: moderate / low risk / pattern — siblings: Same "an unguarded per-item throw aborts the whole sync run" mistake, all in src/commands/sync.ts (sweep worklist):\n- src/commands/sync.ts:50 — `uninstallOne(...)` in the pending replay is unguarded; its safeWrite can throw PushRefused/SafeWriteExhausted and kills the rest of the run.\n- src/commands/sync.ts:55-63 — `installOne(...)` is wrapped, but the catch only recognizes the `no matching project context` message and rethrows everything else (including safeWrite failures), so it aborts the remaining pending entries and every later stage.\n- src/commands/sync.ts:123 — `installOne(...)` inside the newly-endorsed batch loop is unguarded; one failure abandons the rest of the batch, reconcileOrphans (:127) and writeStamp (:130).\n- src/commands/sync.ts:127 → reconcileOrphans' safeWrite calls at src/commands/sync.ts:152 and :164 are unguarded; a refused adopt/decline push skips the remaining orphans and writeStamp.\nDeliberately excluded: src/commands/sync.ts:44 (ff-only pull failure) is intentionally fatal and pinned by src/commands/__tests__/sync.test.ts:73-80 ("leaves an old stamp after a failed pull").
  - Why one fix: Option 1 fixes the problem where it starts and costs almost nothing extra. Option 2 is a one-liner but only half a fix — sync would survive, yet the first shared skill that fails to publish would still block every other shared skill in the same pass. Option 3 is the same idea applied to four more places in sync; that is a worthwhile follow-up sweep but it changes what "sync succeeded" means for the whole command, which is a bigger call than this bug needs. There is no genuine product fork here: the tool already behaves this way when a shared skill cannot be published — the two-sided-divergence case prints a line, leaves the edit unpublished, and sync still reports success (asserted in src/commands/__tests__/share.test.ts:118) — so reporting a failed push the same way is consistent with the existing contract rather than a new decision.
  - **Option 1: Report-and-continue per shared skill (inside reconcileShared) (recommended)** — Depth 3/4 · Cost 1/4. In src/commands/share.ts:68-117, wrap the loop body (from the `const team = await readTeamPolicy(clone);` line at :75 through the end of the iteration) in try/catch — or extract it into a `reconcileOne(...)` helper, turning its four `continue`s into `return`s — and in the catch do `io.print(`Could not reconcile shared ${record?.name ?? id.slice(0, 8)}: ${error instanceof Error ? error.message : String(error)}`)` and fall through to the next entry. Catch-all, not a typed filter: `requireGit`'s plain `Error` and lockfile's ELOCKED are the most likely transient failures and a typed filter on SafeWriteExhausted/PushRefused would miss them. sync.ts:67 then needs no change, because reconcileShared stops throwing for these conditions. Add one test in src/commands/__tests__/sync.test.ts using `wrapRunner` (fixtures already support it, see sync.test.ts:48-58) to fail `git push` for the share write and assert sync still returns ok, still quarantines a hand-edited placement, and prints the reconcile failure line. *Wins if:* Wins when one contended or refused share push should cost only that skill's reconcile — the rest of sync AND the remaining shared skills still run — which is exactly the failure described here.
  - Option 2: Guard the call site only — Depth 2/4 · Cost 0/4. At src/commands/sync.ts:67, wrap the call: `if (interactive) { try { await reconcileShared(store, runner, io as Prompter); } catch (error) { notice(`Shared reconciliation stopped: ${error instanceof Error ? error.message : String(error)}`); } }`. One line of behavior change, and the message lands in SyncResult.notices as well as stdout. *Wins if:* Wins if the change must be minimal and plainly revertable, and it is acceptable that the first failing shared skill still skips every later shared skill in the same pass (fine for a user with exactly one shared skill).
  - Option 3: Uniform per-item fault tolerance across sync's main path — Depth 4/4 · Cost 2/4. Introduce one small guard helper in src/commands/sync.ts and apply it to every per-item step that can throw: the pending replay (`uninstallOne` at :50, `installOne` at :56 where only the project-context message is currently handled), `reconcileShared` at :67, the endorsement-batch `installOne` at :123, and `reconcileOrphans` at :127 (whose safeWrite calls at :152/:164 can throw before writeStamp at :130). Each failure becomes a notice and the run continues; requires deciding the result contract (stay `success` with notices, or add a `failed: string[]` to SyncResult and a non-zero exit) plus tests per site. Keep the ff-only pull at :44 fatal — that is deliberate and covered by sync.test.ts:73-80. *Wins if:* Wins if the team wants sync to be uniformly best-effort and to report a partial run, closing the identical pending-replay and orphan-adoption aborts in the same change rather than in a follow-up sweep.
- **medium — skillRecords hard-throws on one malformed folder, breaking search/install/uninstall/sync for the whole team** — `src/lib/skills.ts:24`
  - Root cause: `skillRecords` treats the team clone's `skills/` tree as trusted, well-formed input: its per-folder body (readFile of SKILL.md, then two `throw`s on parse/name mismatch) runs unguarded inside the enumeration loop, so any single bad folder propagates out of the whole function instead of being confined to that folder. The repo is a plain git repo any member can push to outside the CLI (GitHub web UI, another client), and `guard.ts` is only a client-side pre-commit check inside `safeWrite` ("the guard prevents accidents, not abuse", guard.ts:64-65) — it cannot stop a folder without SKILL.md or with a mismatched frontmatter name from landing on main. The spec itself puts this invariant at write time ("unique repo-wide, enforced at `share`", spec §4.1), so re-asserting it as a fatal error at read time is what turns one member's bad push into a team-wide outage: every consumer goes through `skillRecords`/`findSkill`, so read-only `search` (search.ts:22), the SessionStart `sync` hook (sync.ts:48/78/113), `install` (install.ts:130), `uninstall` (uninstall.ts:37) and `share` (share.ts:47) all fail wholesale. The codebase already knows repo data can be bad and handles it tolerantly at two sibling sites (share.ts:72-73 catches around `skillRecords`; sync.ts:176-178 `actorPerson` catches around `readPerson`), which is exactly the containment `skillRecords` is missing. (src/lib/skills.ts:24-27 (unguarded per-folder body of the `skillRecords` loop, src/lib/skills.ts:16-30))
  - Rated: moderate / medium risk / pattern — siblings: Same mistake — a bulk read over externally-writable repo data where one bad entry aborts the whole batch — appears at these locations (sweep worklist):
1. src/lib/skills.ts:24-27 — the finding itself; the primitive all eight call sites inherit (skills.ts:34, install.ts:130, uninstall.ts:37, share.ts:47/72/125, sync.ts:48/78/113, search.ts:22).
2. src/commands/search.ts:23 — `Promise.all((await readdir(join(clone,'people'))).filter(...).map((file) => readPerson(clone, file.slice(0,-5))))`. `readPerson` (skills.ts:44) goes through `parseJson`, which throws on bad JSON or a schema miss (schema.ts:126-132). One corrupted or hand-edited `people/*.json` pushed outside the CLI makes read-only `search` fail for every member, and the install-count column it feeds is purely informational — exactly the case that should degrade, not abort. Fix identically: skip and report the unreadable person file.
Two tolerant precedents already in the tree to copy rather than invent: src/commands/share.ts:72-73 (try/catch around `skillRecords`, prints and continues) and src/commands/sync.ts:176-178 (`actorPerson` swallows a bad `readPerson`). Deliberately NOT in scope: `readTeam` (skills.ts:43) and `readPerson` for the actor's own handle (sync.ts:112, uninstall.ts:41) — a single required file whose absence or corruption legitimately fails the command.
  - Why one fix: Not a real fork. Both options stop one bad folder from breaking everyone; the only thing the cheap option buys is a few hours saved, and it pays for that by making the broken folder invisible and by opening a new data-loss path (sharing a skill whose name matches a SKILL.md-less repo folder would wipe that folder's contents instead of being rejected as a name collision). Since the whole point of this finding is 'a bad push must not destroy other people's work', trading the report and the collision check away to save an afternoon is the wrong trade. Recommend the tolerant-with-report version.
  - **Option 1: Tolerant enumeration with reported problems (recommended)** — Depth 4/4 · Cost 2/4. In src/lib/skills.ts, wrap the per-folder body (lines 23-28) in try/catch and collect `{ name, reason }` into a `problems` array instead of throwing; expose both halves (e.g. `enumerateSkills(clone, team): Promise<{ records: SkillRecord[]; problems: SkillProblem[] }>`, with `skillRecords` kept as a thin wrapper returning `records` so existing callers compile). Then: (a) `findSkill` (skills.ts:33) rethrows the collected reason only when `reference` equals a problem folder name, so `install <name>`/`uninstall <name>` of the actually-broken skill keep today's exact message; (b) search.ts:22 and sync.ts:48/78/113 emit one line per problem via `io.print`/`notice(...)` — the same shape as the existing sync.ts:79 'its skill is no longer in the repository' notice — and carry on with the good records; (c) share.ts:48 compares the new name against records ∪ problems so the repo-wide uniqueness check still rejects a name whose folder exists but is unparseable. Add an enumeration test (there is no collocated skills.test.ts today; the nearest fixture harness is src/commands/__tests__/search.test.ts) covering: folder with no SKILL.md, unparseable frontmatter, folder/frontmatter name mismatch — each skipped with a notice while sibling skills still resolve, plus `install <broken-name>` still reporting the reason. *Wins if:* You want one bad folder to cost exactly that folder — the other skills keep working, the user is told which folder is broken and why, and share's name-uniqueness check stays honest.
  - Option 2: Silent skip inside skillRecords only — Depth 2/4 · Cost 0/4. Touch src/lib/skills.ts alone: try/catch the per-folder body at lines 23-28 and `continue` on any failure. No signature change, no caller edits, no notices. *Wins if:* You need the unblock in this milestone and accept two regressions it creates: nothing ever names the broken folder (a member whose `install foo` fails gets 'No skill foo in team X' with no reason), and share.ts:48 no longer sees the folder — so `share ./foo` against a repo folder `skills/foo/` that has no SKILL.md passes the collision check and mirrorToTree (share.ts:175) deletes that folder's existing files before writing yours, converting a blocked name collision into silent data loss.
- **medium — SKILL.md is decoded and re-encoded on the write path, so it is the one file in a skill folder that does not round-trip byte-for-byte** — `src/lib/teamRepo.ts:193`
  - Root cause: `textContent()` (src/lib/teamRepo.ts:208-210) classifies `skills/<name>/SKILL.md` as "text", so `makeTree`'s `before` (:187) and `after` (:193) call `content.toString('utf8')` on it. `applyTree` writes exactly what `after()` returns (`await writeFile(destination, next)`, :245-247), and every path it writes is an overlay entry — so a Buffer handed in by `mirrorToTree` (src/commands/share.ts:175, fed by `sourceFiles` at :174 which `readFile`s raw Buffers) is decoded to a string and re-encoded as UTF-8 on the way to disk. Node maps every invalid UTF-8 byte to U+FFFD (EF BF BD), so SKILL.md is the one file in a skill folder that does not round-trip byte-for-byte, while `references/pixel.png` and every other asset does.

Two consequences make this worse than a cosmetic mangling, and both were confirmed by reading the surrounding flow:
1. Silent local data loss on the next sync. After the repo copy is mangled, `canonicalDigest` (src/lib/skills.ts:47-56) gives `repoDigest !== baseline` while `sourceDigest === baseline`. In `reconcileShared` (share.ts:90) the divergence branch needs BOTH to differ, so it is skipped; :100 fails; :104 `sourceDigest !== baseline` is false; control falls to the else at :109, which runs `replaceDirectory(record.directory, tracked.source)` — copying the corrupted repository copy over the user's own source folder and then recording that as the new baseline (:115).
2. The decode is not needed by any consumer. guard.ts has its own `asText` (:100, used at :77 and :96) and every command call-site already wraps the read in `treeText`: install.ts:105, uninstall.ts:70, sync.ts:156 and :168, team.ts:177 and :180, and the collocated test at teamRepo.test.ts:144. The whitelist is dead weight that also has to be hand-extended for each new text path (README.md, evals/*).

No recorded deferral or load-bearing comment defends it: there is no PRODUCT-CONCERNS.md and no `.planning/debug/**/*.deferred.md` in the worktree, `.planning/specs/2026-09-02-phase-1-build.md` says nothing about utf8/binary/mirroring encodings, and the only comment near the code (":218 Decode a tree value only at a text consumer; binary paths stay byte-for-byte in the tree") argues FOR removing the decode from the tree, not for keeping it. (src/lib/teamRepo.ts:208-210 (`textContent()` whitelist), applied at :187 (`before`) and :193 (`after`), written out at :245-247 (`applyTree`))
  - Rated: moderate / medium risk / pattern — siblings: Same mistake — round-tripping SKILL.md through a UTF-8 decode on a path that then writes bytes back — appears at these sites (sweep worklist):
- src/lib/teamRepo.ts:187 — `before` decodes text-classified paths (`content.toString('utf8')`).
- src/lib/teamRepo.ts:193 — `after` decodes; this is the value `applyTree` writes.
- src/lib/teamRepo.ts:208-210 — `textContent()`, the whitelist itself; must be hand-extended for every future text path (README.md, evals/*).
- src/commands/uninstall.ts:73 — `teamJson?.includes(input.id)` calls a string method directly on a tree value; the only call-site not already going through `treeText`.
- src/commands/share.ts:44 + :55 — `readFile(SKILL.md, 'utf8')` then unconditional `writeFile(..., updated, 'utf8')` re-encodes the USER'S OWN source file at share time, before any mirroring. Same defect class, different file (the user's folder rather than the repo).
- src/commands/share.ts:81-83 — `reconcileShared` repairs the source SKILL.md through the same utf8 read/write pair.
- src/commands/share.ts:111-113 — post-`replaceDirectory` refresh, same utf8 read/write pair.
- src/lib/skills.ts:52 — `canonicalDigest` hashes `canonicalSkillMd(content.toString('utf8'))`, so the digest that drives divergence detection is computed over a lossy decode; combined with share.ts:90/109 this is what turns a mangled repo copy into `replaceDirectory` overwriting the user's source.
- src/lib/skills.ts:24 — `skillRecords` reads SKILL.md as utf8 (read-only, no write-back; lowest priority, listed for completeness).
  - Why one fix: One option clearly wins. The stated point of this change was that a shared skill folder arrives in the team repo exactly as it left the user's machine; option 1 is the only one that actually delivers that for every file, and it is the smallest change that does. Option 2 is strictly worse — it stops the corruption but leaves the pre-image still being decoded, which turns the bug into a hard "Staged diff does not match the mutation" error on every later sync of the affected skill, so it trades a quiet problem for a noisy one without finishing the job. Option 3 (refuse odd files up front) is a reasonable product policy and could be added later, but it needs a human to decide "must a SKILL.md be plain UTF-8 text?", it touches three more places, and it does not remove the underlying whitelist that someone has to remember to update every time a new text file is added to the repo layout. Option 1 is right no matter how that product question is eventually answered.
  - **Option 1: Drop the text/binary whitelist — the tree stores bytes, consumers decode (recommended)** — Depth 4/4 · Cost 1/4. In src/lib/teamRepo.ts delete `textContent()` (:208-210); make `before` (:183-188) return the cached Buffer unchanged (`return content;`); reduce `after` (:191-194) to `overlay.has(path) ? overlay.get(path) : before(path)`. `sameContent` (:211-215) can then collapse to a Buffer/string-tolerant equals or stay as-is. One call-site must move with it: src/commands/uninstall.ts:73 `teamJson?.includes(input.id)` becomes `treeText(teamJson).includes(...)` — `Buffer.includes` is overloaded in @types/node (buffer.d.ts:1878-1879), so calling `.includes` on a `string | Buffer` union will not typecheck. Also re-check the `if (!raw)` guards (install.ts:104, uninstall.ts:69, sync.ts:155/167): an empty Buffer is truthy where `''` was falsy, so a zero-byte people file now falls through to `parseJson` instead of the "Missing <path>" error — both throw, but the message changes. Extend the collocated test at src/commands/__tests__/share.test.ts:27 (it already proves binary assets survive) to put a non-UTF-8 byte in SKILL.md and assert `git show main:skills/sample/SKILL.md` is byte-identical after a sync mirror. *Wins if:* Wins whenever the tree is meant to be a faithful mirror of a skill folder: it removes the re-encode, the phantom-change failure mode, and the per-path whitelist that would otherwise need hand-extending for README.md and evals/* in M3 — and it is required regardless of what the product later decides about non-UTF-8 SKILL.md.
  - Option 2: Stop decoding on the write path only — Depth 2/4 · Cost 0/4. Leave `before`'s decode alone and change only `after` (src/lib/teamRepo.ts:191-194) back to the plain passthrough `overlay.has(path) ? overlay.get(path) : before(path)`. Because `changedPaths` only ever yields overlay keys, this alone stops applyTree from writing a re-encoded SKILL.md. One line, no call-site changes, plain revert. *Wins if:* Wins only if you need the smallest possible revert-safe change right now and accept trading silent corruption for a loud one: with `before` still decoding, `sameContent` (:196, :211-215) compares the raw overlay bytes against the U+FFFD-decoded pre-image, so a non-UTF-8 SKILL.md is reported changed forever — `git add` then stages nothing and the staged-diff equality check at :110-113 throws GuardError on every subsequent share/sync of that skill.
  - Option 3: Declare SKILL.md a UTF-8 text format and reject non-UTF-8 at the entry points — Depth 3/4 · Cost 2/4. Leave the tree layer as-is and validate at the three places a source folder enters it: share.ts before :55, and in `reconcileShared` (share.ts:106) and `resolveDivergence` (share.ts:130), assert that SKILL.md's bytes survive a decode/encode round-trip, failing with a clear message instead of mirroring. Note this also has to cover share.ts:44+55, which today reads the user's source SKILL.md as utf8 and unconditionally writes it back as utf8 — the initial `share` already launders non-UTF-8 bytes in the user's own file. *Wins if:* Wins if the product decides SKILL.md must be declared UTF-8 and a loud refusal at share time is preferable to storing arbitrary bytes — but it needs a product ruling the code cannot settle, touches three entry points, and leaves the tree-layer whitelist (and its M3 maintenance burden) in place.
- **medium — Hook-mode notices are only written on the ok branch, so the quarantine relocation message is silently dropped when sync later fails** — `src/index.ts:13`
  - Root cause: In hook mode `sync` deliberately suppresses immediate printing (src/commands/sync.ts:31 — `notices.push(line); if (!args.hook) io.print(line)`), so the returned `SyncResult.notices` array is the only channel those lines ever reach a human on. But the flush added in this diff hangs off the success branch (src/index.ts:13 `else if (isHookSync(outcome.value))`), and the only other exit from `sync` is `failure(message)` (src/commands/sync.ts:132), whose Result variant is `{ ok: false; error: string }` (src/lib/result.ts:3) and has nowhere to carry the accumulated array. So every notice buffered before a throw is discarded. The load-bearing one is `Local changes at ${path} moved to ${quarantined}.` (sync.ts:91), emitted immediately after the irreversible `rename` in `moveToQuarantine` (src/lib/placer.ts:84-90), and — unlike the `Blocked ...` notices at sync.ts:79/82 and the deferral list, which are recomputed and re-announced on the next hook run — it is one-shot: once the directory is moved, the next sync sees no local edit and never mentions it again. Everything from sync.ts:92 to sync.ts:130 can throw after that rename: `materializeVersion`/`snapshotSkillDirectory` (:94-95), `lockTarget` → `acquireSkillTargetLock` with `retries: 0` throwing `TargetBusyError` on any concurrent holder (src/lib/placer/vendor/skillhub/skill-target-lock.ts:21-31), `place`/`store.update` (:102-103), the next loop iteration's `git pull`, `readPerson`/`skillRecords`/`readTeam` (:112-115), and `writeStamp` (:130). The user's SessionStart hook then prints only e.g. `install target is busy: ...` and never learns their hand edits were moved to ~/.terum/skills/quarantine/&lt;ISO8601&gt;/&lt;name&gt;. This also contradicts the spec's stated rule (.planning/specs/2026-09-02-phase-1-build.md:330: sync "overwrites, moves the edited copy into ~/.terum/skills/quarantine/&lt;ISO8601&gt;/&lt;name&gt;/, and prints one line naming it — never a silent overwrite"). (src/index.ts:13 (notice flush is on the `else if` ok-branch only), together with src/commands/sync.ts:132 (catch returns a notice-less `failure`) and src/lib/result.ts:3 (failure variant carries only a string))
  - Rated: moderate / medium risk / isolated
  - Why one fix: Option 1 wins without a real trade-off. Option 2 produces exactly the same text on screen for one file less of churn, but it smuggles a user-facing sentence about a moved folder inside an error string, so nothing can test or reuse it cleanly — a poor bargain for the one line of code it saves. Option 3 is not really a competing answer: it is option 1 plus a bigger behavioural change (one broken skill no longer aborts the whole background sync), which is worth considering on its own later but does not change what has to happen here. Worth noting a fourth idea that is ruled out: letting sync write to stderr directly as the move happens would be the most robust of all, but the spec pins the hook's channel to a prompter with "print and nothing else" (build spec §3, line 51), so it would take a spec change, not a bug fix.
  - **Option 1: Carry notices on the failure Result and flush them in index.ts (recommended)** — Depth 3/4 · Cost 1/4. src/lib/result.ts:2-6 — widen the failure variant to `{ ok: false; error: string; notices?: string[] }` and give `failure(error, notices?)` an optional second argument (all 8 existing one-arg call sites in src/commands/*.ts are unaffected). src/commands/sync.ts — hoist `const notices: string[] = []` (line 30) above the `try`, and make the catch at line 132 `return failure(message, args.hook ? notices : [])` (non-hook mode already printed them at line 31, so passing them there would double-print). src/index.ts:12 — on the failure branch write each `outcome.notices ?? []` line to stderr before `${outcome.error}`. Test: extend src/commands/__tests__/sync.test.ts:114 ('returns local-change notices in hook mode') with a run that throws after the quarantine move — hold the target lock via acquireSkillTargetLock, or inject a failing runner — and assert `{ ok: false, notices: [expect.stringContaining('moved to')] }`. *Wins if:* You want the quarantine line to reach stderr on every failure path with the notices still a structured, assertable field, without changing what sync does or reopening the spec.
  - Option 2: Prefix the buffered notices onto the failure message inside sync's catch — Depth 2/4 · Cost 0/4. src/commands/sync.ts only: hoist `const notices: string[] = []` above the `try`, and change line 132 to `return failure(args.hook && notices.length ? `${notices.join('\n')}\n${message}` : message)`. index.ts already writes `outcome.error` to stderr, so the bytes a hook user sees are identical to option 1. No change to src/lib/result.ts or src/index.ts. *Wins if:* You want the smallest possible diff in one file and accept that human-facing notices ride inside the error string, so tests must assert on `result.error` substrings and any future command needing this re-implements the concatenation.
  - Option 3: Contain per-placement failures so one bad target never discards the run, plus option 1 — Depth 4/4 · Cost 3/4. Wrap the per-placement body in the ledger loop (src/commands/sync.ts:71-105) in its own try/catch that turns a throw — TargetBusyError, a failed `place`, a failed `materializeVersion` — into a notice plus a `deferred` entry and continues to the next placement, so accumulated notices are never thrown away by an unrelated skill's failure; keep the stamp unwritten (sync.ts:130) whenever anything failed, per the spec's 'stamp only after a sync that fully succeeded' rule. Still needs option 1's change as well, because failures outside that loop (`git pull`, `readPerson`/`readTeam` at :112-115, `writeStamp` at :130) remain top-level throws. *Wins if:* You also want a busy or broken single target to stop aborting the whole hook sync — but it changes sync's success/failure semantics and its interaction with the stamp rule, so it needs a spec decision rather than just a bug fix.

### Forks — no single fix clearly wins; run `/decision-walk <this report>` (5)

- **high — Skill uninstall drives local removal from the shared people file, so a second machine silently removes nothing** — `src/commands/uninstall.ts:42`
  - Root cause: `src/commands/uninstall.ts:42` makes the shared, remote `people/<handle>.json` `installed` array the ONLY source of the scopes to uninstall, while the record of what THIS machine actually placed is the per-machine `config.placements` ledger (`src/lib/schema.ts:69`, written by `install.ts:96-98`).

The two documents cannot be substituted for each other: `installedSchema` (`src/lib/schema.ts:24-29`) has no machine field, and `installOne` dedupes by `(id, scope)` before pushing (`install.ts:106-107`), so two machines sharing one handle share exactly ONE `installed` entry. Once machine A's uninstall clears it (`uninstall.ts:71`), machine B's `person.installed.filter((entry) => entry.id === record.id)` yields `[]`, the `for` loop at `:44` never executes, and `uninstallOne` — the only code that removes placements and deletes ledger entries (`:56-64`) — is never called. `run` then returns `success([])`, and `execute` in `src/index.ts:11-17` prints nothing for a non-hook success, so the user gets exit 0, no output, and the skill directory still on disk and still loaded by Claude Code.

The `declined` write at `:73-74` makes it unrecoverable for auto-endorsed skills: `sync.ts:77` skips any placement whose id is in `declined`, and `sync.ts:146` makes `reconcileOrphans` skip it too, so no later `sync` ever offers to adopt or remove the stranded placement.

Test gap: every case in `src/commands/__tests__/uninstall.test.ts` seeds the people entry and the placement together (lines 22-33, 48, 135-136); none exercises a ledger placement with no matching `installed` entry. (src/commands/uninstall.ts:42)
  - Why it is a fork: A real fork, and it is a scope call rather than a design call — the edit itself is the same either way.

Everyone agrees on the rule: the shared team file says what you WANT installed, the local ledger says what is actually on this computer's disk, and uninstall must consult the ledger before deciding it has nothing to do. The question is how much of the command to fix now. Option 1 fixes only `uninstall <skill>`, which is the case that was tested and confirmed — smallest change, easiest to review. Option 2 also fixes `uninstall member <your-handle>`, which has the exact same second-machine bug one line up, and `uninstall project <name>`, which has a milder version of it; that means three commands change behavior at once and need three new tests.

Pick option 1 if you want the confirmed bug gone with minimum blast radius and are willing to track the other two as a sweep. Pick option 2 if you would rather not knowingly leave the identical bug sitting in the sibling command. Option 3 (changing the shared team file format so each computer gets its own entry) is only right if you separately decide the team repo should display which machine has what — it is a data migration across every team's repo to solve a problem the local ledger already solves.
  - **Option 1: Union the local ledger into the single-skill path (recommended)** — Depth 2/4 · Cost 1/4. In `run()` at src/commands/uninstall.ts:41-45, build the scope list from BOTH sources instead of the people file alone: `person.installed.filter(e => e.id === record.id).map(e => e.scope)` unioned with `Object.values(config.placements).filter(p => p.id === record.id && p.team === team).map(p => p.scope)`, deduped by `JSON.stringify(scope)` (the same equality `uninstallOne` already uses at :56 and :71) so a scope present in both does not run twice. When the union is empty, `throw new Error(`No installation of ${args.ref} found on this machine.`)` instead of returning `success([])`, so index.ts prints it on stderr with exit 1. Add a collocated test to uninstall.test.ts: seed a placement in `config.placements` with the people file already cleared, assert the directory is gone, `placements` is `{}`, and `pending` is `[]`. Calling `uninstallOne` for a ledger-only scope is safe — `safeWrite` compares content (`teamRepo.ts:195`, `sameContent` at :210) and returns `{changed:false}` at :104-105 without an empty commit. *Wins if:* You want the verified defect closed with the smallest reviewable change, and are content to sweep the member/project paths as separate follow-ups rather than changing three commands' semantics in one patch.
  - Option 2: Ledger-driven scopes for all three uninstall paths via one helper — Depth 4/4 · Cost 2/4. Add a helper in src/commands/uninstall.ts, e.g. `ledgerScopes(config, team, id)` returning `Object.values(config.placements).filter(p => p.id === id && p.team === team).map(p => p.scope)`, plus a `JSON.stringify`-based dedupe, and apply it at all three enumeration sites: the skill path (:41-45) as in option 1; the member path (:22-24) unioning each `member.installed` entry's scope with the ledger scopes for that id, gated on `args.member === config.teams[team].handle` so uninstalling ANOTHER member's set keeps its current remote-driven meaning; and the project path (:29-33) unioning `{kind:'project',project}` with ledger scopes whose `scope.project === project`, which also catches a skill de-listed from `team.json projects[].skills` while still placed. Each path fails with a concrete message when its union is empty. Needs three new tests in uninstall.test.ts, one per path. *Wins if:* `uninstall member <your-own-handle>` is a workflow you care about — it carries the identical second-machine no-op at :24 — and you would rather fix all three enumerations in one reviewed pass than ship the same bug twice.
  - Option 3: Make people-file installed entries per-machine — Depth 4/4 · Cost 4/4. Add a machine identifier to `installedSchema` (src/lib/schema.ts:24) and key `installed` entries by `(id, scope, machine)`, so `install.ts:106-107` no longer collapses two machines into one entry and `uninstall.ts:42` filtering by handle+id genuinely means 'what this machine has'. Requires a `layout_version` bump (currently `z.literal(2)`, schema.ts:32), a stable machine id in config, migration of existing `people/*.json` in every team repo, and matching updates in join/sync/reconcileOrphans. *Wins if:* The product actually wants the team repo to show WHERE each person has a skill installed (per machine) so `ls`/`search` can report it — i.e. the local ledger is deemed the wrong home for that truth. Otherwise this is a schema migration to fix something the existing per-machine ledger already records correctly.
- **high — uninstall rm -rf's every recorded placement path with no fingerprint or project re-validation** — `src/commands/uninstall.ts:61`
  - Root cause: `uninstallOne` treats a ledger *key* as proof of content ownership. At src/commands/uninstall.ts:57 it selects placements by `{id, team, scope}` alone, then at :58 derives the "skills root" from the path itself (`const root = dirname(path)`) and at :61 calls `remove(root, path, true)`. Because `root` is `dirname(path)`, the path-safety guard inside `remove()` (src/lib/placer.ts:77: `dirname(destination) !== root`) can never fire — it compares a value against itself — and the `ledgerOwnsPath` argument is hardcoded `true`. So the only thing standing between an `rm(destination, { recursive: true, force: true })` and any directory on the machine is whether that exact string is still a key in `config.placements`. Nothing re-reads the directory. The ledger entry carries a `fingerprint` written at placement time (install.ts:97) and `uninstallOne` never compares it, so a placed copy the user hand-edited, or a recorded path that now holds something else entirely (checkout deleted and re-created, folder replaced by the user's own skill of the same name), is destroyed with no quarantine and no recovery. Both sibling paths verify before they act: sync.ts:75 refuses to even inspect a project placement whose checkout does not resolve, and sync.ts:88-92 quarantines instead of overwriting when `current.fingerprint !== entry.fingerprint`. The spec requires the same — line 311 says Placer remove "refuses any path that is not in the `placements` ledger **or not under the resolved skills root**", and `dirname(path)` is not a resolved root; line 330/492 say a local-changed copy is moved aside, never destroyed. Both entry points inherit the gap, since sync.ts:50 replays pending uninstalls through this same function non-interactively from the SessionStart hook. (src/commands/uninstall.ts:58-61)
  - Why it is a fork: Two separate questions are tangled in this finding, and only one of them is settled. Settled: uninstall should never delete a folder whose contents are not exactly what Terum put there. Every other code path already checks this (sync moves an edited copy into quarantine rather than clobbering it), the spec says so twice, and option 0 makes uninstall behave the same way. That part should just be fixed. Unsettled, and a product call rather than an engineering one: when the same project skill is installed in two checkouts of the same repo on one laptop, should `uninstall` run from one checkout also remove the copy sitting in the other? The spec answers this both ways — one section says the two copies share a single "installed" record and therefore rise and fall together, another says every project decision is answered by "where the command is standing." Option 0 keeps today's answer (both copies go, but nothing unrecognised is destroyed). Option 1 switches to per-checkout, which means a second decision has to be made too: how the copy left behind in the other checkout ever gets cleaned up, because as the code stands today nothing would ever touch it again. Recommend applying option 0 now regardless, and putting the per-checkout question to Ryan separately.
  - **Option 1: Fingerprint-gate the delete; quarantine on drift (recommended)** — Depth 3/4 · Cost 1/4. In the loop at src/commands/uninstall.ts:58-63, before calling remove(): snapshot the placed directory with snapshotSkillDirectory(path) (catch -> undefined). If the path still exists and its fingerprint differs from the ledger entry's `entry.fingerprint` — or the snapshot fails while the path exists at all (a file, or an unreadable tree) — call moveToQuarantine(path, join(input.store.root, 'quarantine'), basename(path)) instead of remove(), and io.print one line naming the destination (drop the `void io` on uninstall.ts:52). Drop the placements entry either way, so `removed` and the people-file write are unchanged. This is a copy of sync.ts:88-92, same helper, same quarantine root, so there is one relocation path, not two. Add a collocated uninstall.test.ts case mirroring sync.test.ts:180 ('quarantines ... without losing the edit') for both a hand-edited global placement and a repurposed cross-checkout path. *Wins if:* Uninstall is a per-machine operation over one `installed` record — which is what spec §5.4 states ('two checkouts of the same project on one laptop are two `placements` entries against one `installed` record'). Then deleting every placement is correct, and the only real bug is destroying bytes that are not what we placed; this fixes exactly that, at both entry points, without reopening the record model.
  - Option 2: Also re-validate the project checkout (full sync parity) — Depth 4/4 · Cost 3/4. On top of option 0, for `entry.scope.kind === 'project'` resolve the current worktree with the same matchingProjectRoot() predicate sync.ts:72 uses (sync.ts:195-202) and skip any placement whose path is not under that root, so uninstall run from checkoutA never touches checkoutB. This is not just a guard: it needs a new rule for the placements it now leaves behind. uninstall writes the skill ID into `declined` for anything listed in team.json (uninstall.ts:76-78), and both sync.ts:78 and reconcileOrphans (sync.ts:152) skip declined IDs — so checkoutB's copy and its ledger entry would be stranded on disk permanently, cleaned by nothing. Shipping this means also designing how a pending uninstall survives until every checkout has been visited, and rewriting uninstall.test.ts:123-143, whose contract is the opposite. *Wins if:* The product call is that uninstall is per-checkout, which spec §5.2 line 162 supports ('Project resolution is by worktree, and it is the same procedure everywhere (`install`, `uninstall`, both sync modes) ... the answer always comes from where the command is standing'). Wins if a user must be able to keep a project skill in one worktree while removing it from another — but only with the follow-on cleanup rule designed, or it trades data loss for permanent litter.
  - Option 3: Push the guarantee into the placer primitive — Depth 4/4 · Cost 2/4. Change src/lib/placer.ts:75 `remove(targetRoot, path, ledgerOwnsPath: boolean)` to take the ledger's expected fingerprint instead of a caller-asserted boolean, and have remove() itself snapshot the destination and throw 'Refusing to remove modified placement' on mismatch. Update the four assertions in src/lib/__tests__/placer.test.ts:22,23,70,71 and the single production call site. No future caller can then rm -rf an unverified path by passing `true`. *Wins if:* You want the invariant enforced by the primitive rather than by each caller remembering it. But as written it makes a hand-edited copy *block* uninstall with an error instead of moving it aside, which is worse for the user than option 0 and contradicts the 'move aside, never refuse' shape of spec lines 330/492; it is best as a follow-up hardening once option 0 defines the behavior.
- **medium — Sync re-places by repo folder name but keys the ledger by the original path** — `src/commands/sync.ts:99`
  - Root cause: In sync's re-place branch the destination folder and the ledger key come from two different sources. The parent directory comes from the ledger (`root = dirname(path)`), but the leaf comes from the repository record (`skill.name`, resolved by ID at sync.ts:78), while the ledger row that gets stamped is still keyed by the original `path`. As long as the repo folder is never renamed the two agree, so the divergence is invisible; the moment a teammate renames `skills/<old>` to `skills/<new>` (a supported edit — identity is the UUID, and skillRecords at src/lib/skills.ts:27 forces folder name == frontmatter `name`, so the rename also changes SKILL.md and therefore the repo fingerprint, guaranteeing the `continue` at sync.ts:96 cannot fire) sync places a SECOND folder at `dirname(path)/<new>` and stamps the OLD ledger row with the NEW folder's fingerprint. From then on the ledger describes a folder that does not exist: run 2 snapshots the stale `<old>` folder, sees a fingerprint mismatch and quarantines a live, unedited placement (sync.ts:88-93) with a false "Local changes" notice; run 3 gets `current === undefined`, so `replace: Boolean(current)` is false and `place` renames onto the now-existing `<new>` directory, ENOTEMPTY is rethrown (src/lib/placer.ts:57), the whole `run` catch converts it to `failure(...)`, and every subsequent `sync` — including the SessionStart hook — fails. Nothing in the code re-keys `placements` when the skill's name changes, and no test renames a shared skill. (src/commands/sync.ts:99-103 (leaf name taken from `skill.name` while `store.update` stamps `fresh.placements[path]`))
  - Why it is a fork: This is a genuine product fork, not just an engineering one. All three options stop the crash — the question is what should happen on your laptop when a teammate renames a shared skill in the team repo. Option 1 says the rename follows you: your folder is renamed to match, the old copy is deleted, and you start invoking the skill by its new name (this is what the spec implies, since a skill's identity is its UUID and renames are supposed to carry across). Option 2 says your folder keeps the name it had; only its contents refresh — nothing on your disk is renamed or deleted, but the folder name and the skill's own declared name then disagree, and the rename never reaches you. Option 3 says sync refuses to guess: it prints a line telling you the skill was renamed and to reinstall it, and touches nothing. Option 1 is the only one that ends with your machine matching the team, but it is also the only one that renames and deletes a folder on the user's disk without asking, including in the silent session-start hook. Somebody has to make that call; the code cannot settle it.
  - **Option 1: Follow the rename: place at the new name, re-key the ledger, drop the old copy (recommended)** — Depth 3/4 · Cost 1/4. In src/commands/sync.ts, inside the placements loop, compute `const leaf = basename(path)` alongside `const root = dirname(path)` (line 99). Keep placing at `skill.name` (lock and `place` unchanged), then make the ledger match what was actually written: in the same `store.update` at line 103, when `leaf !== skill.name`, `delete fresh.placements[path]` and write `fresh.placements[result.path] = { ...placement, fingerprint: result.snapshot.fingerprint }`; after the update (still inside the lock) call `remove(root, path, true)` from src/lib/placer.ts:75 — which already refuses any path not directly under `root` and whose `rm(force: true)` tolerates the already-quarantined case — to drop the stale generated copy, and emit one `notice()` naming both folder names, matching the existing one-line-when-anything-moved convention. Add a sync test that pushes a `git mv skills/sample skills/sample-two` (plus the matching frontmatter `name:`), then asserts: exactly one folder on disk at the new name, exactly one ledger key at the new path with the repo fingerprint, no quarantine entry, and that the second and third sync are clean no-ops. *Wins if:* Wins if a rename made in the team repo is supposed to reach every machine automatically — the spec's model is that a skill's identity is its UUID and 'references survive renames' (.planning/specs/2026-09-01-team-skill-sharing.md:69), and the placed folder is generated output that should track the repo, so sync is the thing that applies the rename. Residual gap it does not close: `install` run between the rename and the next sync still writes a second ledger entry (src/commands/install.ts:84-98).
  - Option 2: Pin the placed leaf to the ledger's own basename — Depth 2/4 · Cost 0/4. In src/commands/sync.ts lines 99-102, derive the leaf from the ledger instead of the repo: `const leaf = basename(path); const release = await lockTarget(root, leaf); const result = await place(source, root, leaf, { replace: Boolean(current), projectRoot, runner });`. `join(root, leaf) === path` by construction, so the placed folder and the stamped ledger key can never diverge — the contents refresh in place, the crash loop and the false quarantine disappear, and nothing on the user's disk is renamed or deleted. *Wins if:* Wins if the placed folder's name belongs to the user's machine rather than the repo: it is a two-token change with no new delete path and no behavior change for any non-renamed skill. Accept only if it is tolerable that after an upstream rename the placed copy's directory name disagrees with its own SKILL.md `name:` (Claude Code dispatches by directory name, so the user keeps invoking the old name forever).
  - Option 3: Classify a repo-side rename as blocked and report it — Depth 2/4 · Cost 0/4. In src/commands/sync.ts, before line 99: `if (basename(path) !== skill.name) { notice(`Blocked ${path}: renamed to ${skill.name} upstream; uninstall and reinstall it.`); continue; }`. Nothing is placed, quarantined, or re-keyed, so the ledger and the disk stay consistent and the notice repeats once per sync until the user acts. Slots into the existing §6 'blocked' vocabulary alongside the deleted-skill case at line 79. *Wins if:* Wins if the rule is that sync never resolves a ledger/clone disagreement on its own (the spec's stated 'blocked' stance: report and touch nothing) and leaving the skill stale with a repeating notice until the user reinstalls is an acceptable end state.
- **medium — Auto-endorsement is decided by a raw substring scan of team.json instead of reading global/projects** — `src/commands/uninstall.ts:73`
  - Root cause: `uninstallOne`'s safeWrite mutation decides whether the uninstall must be recorded as a decline by scanning team.json's raw text for the skill's UUID: `const teamJson = tree.before('team.json'); const auto = teamJson?.includes(input.id) ?? false;` (src/commands/uninstall.ts:72-73). `tree.before` returns team.json decoded as a string (src/lib/teamRepo.ts:184-186 + `textContent`, teamRepo.ts:209 lists `team.json` as text), so `.includes` is a document-wide substring search, not a read of the two fields the spec names. The spec predicate is structural — build spec §6 line 311: "Uninstalling **any** skill that an automatic path would otherwise place — team-endorsed global *or* project-list — records its ID in `declined`" — i.e. `team.global` and `team.projects[*].skills`. Because every schema in src/lib/schema.ts is `.passthrough()` (teamSchema at schema.ts:31-38 keeps unknown top-level keys, and `categories`/`archived` are plain string arrays), the UUID can legitimately appear in team.json outside those two lists — a category string, an `archived`-adjacent value, or any field written by a newer client — and every such occurrence flips `auto` to true, writing the ID into `person.declined`. A `declined` ID is then permanently excluded from auto-install (src/commands/sync.ts:116) and from re-placement of an existing ledger entry (src/commands/sync.ts:77), so a text coincidence in a shared file silently and durably opts the user out of a skill. The collocated tests cannot tell the two predicates apart: uninstall.test.ts:69 puts the id in `global`, uninstall.test.ts:91 puts it in `projects.product.skills`, and uninstall.test.ts:113 uses an id that appears nowhere in team.json — substring and structural agree on all three. (src/commands/uninstall.ts:73)
  - Why it is a fork: Both options replace the text search with a real check of the two lists, and both are one-expression changes — that part is not in doubt. What is left is a product question the code cannot answer: when you remove a skill from your own machine, should the fact that some team project you have never worked in recommends that skill count as "the system would put this back", and therefore permanently opt you out of it? The written spec says yes (option 0, the safer direction: over-opting-out is undone by simply installing the skill again, whereas under-opting-out means the skill silently reappears after you removed it). The finding's own example says that feels wrong to a user (option 1). Option 0 is the right immediate fix because it is what the spec currently states and it is free to revert; option 1 is the same fix plus a deliberate rule change, so it should only be taken with the spec line updated at the same time.
  - **Option 1: Parse team.json and test the spec's endorsement lists (recommended)** — Depth 3/4 · Cost 0/4. In the `repo.safeWrite` mutation in `uninstallOne` (src/commands/uninstall.ts:72-73), parse the tree copy instead of scanning it: `const endorsed = teamJson === undefined ? undefined : parseJson(teamSchema, treeText(teamJson), 'team.json');` and set `auto = endorsed ? endorsed.global.includes(input.id) || Object.values(endorsed.projects).some((project) => project.skills.includes(input.id)) : false;`. Add `teamSchema` to the existing `../lib/schema.js` import (line 7) — `parseJson` and `treeText` are already imported. Keep the missing-file tolerance (`undefined` → `auto = false`) that `?? false` provides today. Add a test in src/commands/__tests__/uninstall.test.ts where the UUID appears in team.json only outside `global`/`projects[].skills` (e.g. in `categories`) and assert `declined` does not contain it. *Wins if:* Wins as long as the decline trigger should stay exactly what build spec §6 line 311 says — endorsed globally or on any project list — which is also what §5.2 line 164 implies by ruling out a per-project decline; it is the smallest change, is revert-safe, and matches how every other team.json reader in the repo already works.
  - Option 2: Scope-aware endorsement check — Depth 3/4 · Cost 1/4. Same structural parse, but narrow the project half of the predicate to the scope actually being uninstalled: `auto = endorsed.global.includes(input.id) || (input.scope.kind === 'project' && (endorsed.projects[input.scope.project]?.skills.includes(input.id) ?? false))`. This additionally stops a project-list endorsement for a project the user has never checked out from turning a purely personal global uninstall into a permanent decline. It contradicts the literal wording of build spec §6 line 311 ("team-endorsed global *or* project-list") and sits close to §5.2 line 164's "there is no separate per-project decline", so it needs the spec line amended and a decision recorded alongside the code change. *Wins if:* Wins if the product answer is that a project you do not work in should never be able to opt you out of a skill you installed personally — i.e. the team accepts narrowing the spec's decline trigger, at the cost that a skill on a project list can silently come back the first time you open that project's checkout.
- **medium — New join endorsement test only exercises the all-accept path** — `src/commands/__tests__/join.test.ts:204`
  - Root cause: The new test at src/commands/__tests__/join.test.ts:204 constructs `new ScriptedPrompter(answers(), [true, true])` — the one script in which `installOne` never throws — so the endorsement loop added at src/commands/team.ts:151-156 is only ever driven through its all-accept branch. Every other branch is unexercised, and each is where team.ts diverges from its sibling loop in src/commands/sync.ts:116-125. sync builds `candidates` by filtering `teamJson.global` down to ids that are not already in `person.installed`, not in `person.declined`, and that actually resolve to a `skillRecords` entry; team.ts iterates `(await readTeam(clone)).global` raw and calls `installOne` on every id inside the outer try, so: (a) an endorsed id with no `skills/<name>/` folder makes `resolveSkill` throw `No skill <id> in team <team>.`, which the catch at team.ts:157 turns into `ok:false` for the whole join even though the people-file write already pushed; (b) a `false` on `Approve these tools for <name>?` throws out of the loop the same way, so declining one grant fails the join; (c) on a rejoin, ids already in `person.declined` are re-offered and, if accepted, installed and stripped from `declined`, contradicting spec line 164 ("an ID in `declined` is never placed by any automatic path — not the join offer…") and D14 ("declines are recorded and never re-asked"). The assertion `io.countAsked('Install 2 team-endorsed')` is a substring probe against a message that embeds the count, so it passes whether or not the loop filters anything. Only the decline-the-set case (`confirms: [false]` → `ok:true`, nothing installed) can be written as a passing test against the code as it stands; the other three assert behavior the code does not yet have. (src/commands/__tests__/join.test.ts:204)
  - Why it is a fork: The choice depends on a decision this file cannot settle: whether the companion fix to the join loop (the team.ts:155 finding) ships in the same batch. Three of the four missing test cases describe outcomes the code does not currently produce — declining one tool grant, or an endorsed skill whose folder was deleted, currently makes the whole `team join` report failure even though the member was already added to the team. So you cannot simply "add the missing tests": you either fix the loop and pin the corrected behavior (option 1), or you write down the current wrong behavior as the expectation and revisit it (option 2). Option 3 goes further and merges the join and sync versions of the same offer into one piece of code, which is the durable answer if a third caller is coming, but it edits shared code that already works and is a bigger change than this finding alone justifies. There is also a smaller open product question inside option 1: when a user answers "no" to the whole endorsed set, today nothing is recorded and they are asked again next time — join and sync agree on that, but the spec's D14 wording ("declines are recorded and never re-asked") could be read as requiring the set decline to be remembered. Pick the reading before writing that assertion.
  - **Option 1: Land the failure-path cases together with the join endorsement-loop fix (recommended)** — Depth 4/4 · Cost 1/4. In src/commands/team.ts:151-156, build the endorsed candidate list the way sync.ts:116 does — resolve `(await readTeam(clone)).global` through `skillRecords(clone, team)`, dropping ids with no folder, ids already in `person.installed`, and ids in `person.declined` — prompt with that filtered count, and wrap each `installOne` so a declined grant or a single failed install prints a line and continues instead of unwinding the whole join. Then add four cases to src/commands/__tests__/join.test.ts alongside the existing one at :197: (1) `[false]` on the set → `ok:true`, `Approve these tools` never asked, `people/me.json` `installed` is `[]`; (2) `[true, false]` → `ok:true`, `installed` ids exactly `[plain]`; (3) a third id in `team.json.global` with no `skills/` folder → `ok:true`, `installed` exactly `[plain, tool]`; (4) a second join after case 2 with `declined` seeded → the set prompt is not asked for the declined id (assert the exact question text, e.g. `io.asked` contains `Install 1 team-endorsed skill(s)?`) and `declined` still contains it. Replace `countAsked('Install 2 team-endorsed')` with an equality check on the full question string so the count can no longer drift silently. *Wins if:* The sibling team.ts:155 finding is being fixed in this same batch — then one commit both corrects the loop and pins all four branches, and join stops behaving differently from sync for the same data.
  - Option 2: Test-only: add the decline-the-set case and characterize the rest — Depth 1/4 · Cost 0/4. Touch only src/commands/__tests__/join.test.ts. Add the decline-the-set case (`new ScriptedPrompter(answers(), [false])` → `ok:true`, no `Approve these tools` question, `installed` `[]`), which passes against the code as written, and add two cases that assert today's actual behavior — `[true, false]` → `toMatchObject({ ok: false, error: 'Consent was declined for tool.' })` and an unresolvable endorsed id → `ok:false, error: expect.stringContaining('No skill')` — each with a comment naming the team.ts:155 finding so the expectation is visibly provisional. Also tighten the substring assertion to exact question text. *Wins if:* The team.ts loop fix is deferred out of this batch: this is the only option that lands green today, and it makes the two wrong outcomes visible in the suite instead of invisible.
  - Option 3: Extract one shared endorsement-offer helper and unit-test the filter — Depth 4/4 · Cost 2/4. Move the candidate-selection logic into a single exported helper (e.g. `endorsedCandidates(clone, team, handle)` in src/lib/skills.ts, plus an `offerEndorsed(...)` wrapper) and call it from both src/commands/team.ts:151-156 and src/commands/sync.ts:110-125. Unit-test the pure filter directly (missing folder, already installed, declined, empty result) and keep one integration case per call site — join.test.ts for the join offer, and a new interactive case in sync.test.ts, which today only covers the hook-mode defer at sync.test.ts:60. *Wins if:* You want join and sync provably identical rather than two parallel implementations that already drifted once — worth it if a third caller (setup, M3) is about to make the same offer.

### Declined by triage — overrule if you disagree (the standalone test applies) (0)

_none_

### Untriaged — the triage agent returned nothing usable; investigate by hand or resume the run (0)

_none_
