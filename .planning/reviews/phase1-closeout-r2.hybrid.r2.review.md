# ultrareview: phase1-closeout

**Verdict.** The fix diff (one commit, `2eb01ed`, base `3a9088a`, 19 files / +270 −47) does what round 1 asked in outline, but three of the eleven fixes stop one step short of the property they claim, and one is untested by construction. `place()`'s new recovery path composes the right "your previous copy is at `<quarantine>`" message and then lets an unguarded `rm` in the trailing `finally` replace it (placer.ts:86) — the exact failure mode the line-77 `.catch()` was added for, with the comment reasoning backwards about which copy is dangerous; the same restructure also throws on a placement that *fully landed* when only the displaced-copy cleanup failed, bypassing the `result.notices` channel eleven lines above it. `sync`'s pre-lock fast path moved two fingerprint reads outside `lockTarget` **and** ahead of `inspect()`, so a concurrent `place()` or a non-directory at the ledger path now produces the false `Blocked` + `deferred` entry the fast path exists to prevent. `search`'s slices-of-eight rewrite is never driven past one chunk by any test in the suite (largest fixture is 3 skills), so a wrong stride or slice bound ships green, and the per-row `allSettled` degrade was applied at 1 of 3 `latestTree` fan-out sites while turning a systemic git failure into exit 0. No critical or high findings survived verification, so the harden loop converges here; every item below is medium or low and is a candidate for the close-out ledger rather than a merge blocker.

| Severity | Confirmed |
| --- | --- |
| Critical | 0 |
| High | 0 |
| Medium | 7 |
| Low | 4 |
| **Total** | **11** |

---

## Correctness & tests

- [ ] **[medium] `place()`'s unguarded `finally` rm replaces the composed recovery error, losing the quarantine location on the exact path the new `.catch()` was added for** — `src/lib/placer.ts:86` *(merges 12 reviewer reports across Correctness & tests / Security & data-loss / Terum invariants / Reuse, simplify, perf — the most independently rediscovered finding in the pass)*
  **Evidence.** Line 77 guards the catch-block cleanup with the rationale `// the finally repeats it; it must not abort the recovery below`, but line 86 still runs `await rm(temporary, { recursive: true, force: true });` bare. Per JS semantics a throw from `finally` *replaces* the throw from `catch`, `{ cause }` included. Both calls target the same path with the same options, so the only way the first rejects is a persistent non-ENOENT failure (`force` suppresses ENOENT only): EACCES on the skills root, EPERM/EBUSY from an AV or open handle, EIO on a network mount — in which case the second fails identically. Via `sync.ts:156`, the swap lands, `fsForTests.rm(displaced)` fails, `restoreDisplaced` moves the previous copy to `<store.root>/quarantine/<ts>/<name>`, the catch composes `"… — the previous sample could not be restored to …; it is at /…/quarantine/…"`, and then line 86 throws `EACCES: permission denied, rmdir '/….terum-<uuid>.tmp'`. The user is told about a staging directory and never learns where their previous copy went — the whole point of invariant 34. Secondary: on the **success** path line 86 runs too, converting a placement that landed on disk into a throw, so `sync`/`install` skip `store.update` and the next sync sees the correct copy as drift and quarantines it. The new test at `placer.test.ts:164` cannot reach this — its stub only fails paths ending `.old`, so both `rm(temporary)` calls succeed.
  **Fix.** `finally { await rm(temporary, { recursive: true, force: true }).catch(() => undefined); }` — unconditionally safe, since on both success paths `temporary` was already renamed away and the rm is a no-op. Correct the line-77 comment (the `finally` is the copy that can abort the recovery, not the one that makes it safe), or drop the catch-block copy and keep one guarded cleanup. Add a placer test that fails the `.tmp` path as well as `.old` and asserts the message still contains `could not be restored` plus the quarantine path. **Triage note:** `.planning/debug/harden/phase1-closeout-r2.deferred.md` declines a *different* finding about this pair on the ground that "a staging-cleanup failure cannot abort the recovery" — this is a rebuttal of that ground, not a re-raise of the duplication point. Sibling sweep: `src/commands/share.ts:268-271`, `src/lib/placer.ts:170` (`moveDirectory`'s post-`cp` rm), `src/lib/placer.ts:126` (`remove()`), `src/commands/leave.ts:46-52`.

- [ ] **[medium] `sync`'s pre-lock fast path snapshots the ledger path before `inspect()`, so a non-directory or symlink at a placement is no longer reported as a foreign collision** — `src/commands/sync.ts:136`
  **Evidence.** Lines 135-137 read the destination before the lock *and* before the ownership check; in the non-rename case `destination === path`, the exact path `inspect()` used to vet first (`sync.ts:148-149`). `snapshotIfPresent` (`placer.ts:145`) does `lstat` then `snapshotSkillDirectory` → `readdir`; it neither requires a plain directory nor refuses a link, while `inspect()` classifies anything whose `lstat().isDirectory()` is false as `foreign`. (1) A regular file at the placement path makes line 136 throw `ENOTDIR: not a directory, scandir …`, which escapes to the loop catch at 171-175 — printing `Blocked <path>: ENOTDIR …` *and* doing `deferred.push(...)`, where before it printed the actionable `… is not a placement this tool owns; leaving both untouched.` and left `deferred` alone. The `deferred` array is rendered by the hook as "N item(s) need review", so the returned value drifts too. (2) A symlink at the placement path: `lstat` succeeds, `readdir` follows the link, and a fingerprint match `continue`s silently — following a symlink where this repo's stated rule, quoted verbatim at `src/lib/fs.ts:13`, is "refuse, never follow". The new test at `sync.test.ts:124` pins only the healthy case.
  **Fix.** Gate the fast path on `(await inspect(path, true)).kind === 'ours'` before snapshotting, or make a non-ENOENT failure at line 136 fall through to the locked path. Add sync tests for a regular file and for a symlink-to-identical-tree at the ledger path, asserting the `Blocked … is not a placement this tool owns` line and `deferred: []`.

- [ ] **[medium] The "slices of eight" rewrite is never exercised past one chunk — a wrong stride or slice bound passes the whole suite** — `src/commands/__tests__/search.test.ts:97` *(merges 5 reports across Correctness & tests / Security & data-loss / Terum invariants / Reuse, simplify, perf)*
  **Evidence.** The rewrite at `search.ts:44-49` has two risky properties: the fan-out is really capped at 8, and `latest[index]` stays aligned with `filtered[index]` **across a chunk boundary**. Neither is covered. Every `searchFixture(...)` in the file seeds at most 3 skills (the new ghost test at :97 seeds 2), so `for (... index += 8)` runs exactly **one** iteration in every test and `filtered.slice(0, 8)` always returns the whole list. Escaping mutations: `index += 4` (overlapping chunks → duplicated rows, every row past the first 8 reading another skill's hash); `slice(index, 8)` (a plausible typo → every later chunk empty, `latest[index]!` yields a TypeError on `settled.status`); `latest.unshift(...)` for `push`. All three are green today. No search test passes `args.runner`, so the `Runner` seam that would make the concurrency bound observable is unused.
  **Fix.** One test passing a fake `runner` (`SearchArgs` already accepts `runner?`) over >8 matching skills — no git, no extra pushes: the fake records in-flight count and resolves `HEAD:skills/<name>` to a per-name sentinel. Assert (1) max concurrent calls === 8, (2) every hit's `latest` is `shortHash` of its **own** name's sentinel (pins cross-chunk alignment), with the unresolvable folder placed 9th so the `—` row and the reported line land on that exact skill.

- [ ] **[low] `search` now returns an untracked, unshared folder as a real hit, unlike the malformed-folder path four lines below which excludes it** — `src/commands/search.ts:52`
  **Evidence.** A rejected `latestTree` is degraded into a row rather than dropped (`hits.push(hit)` at :52 runs unconditionally). `latestTree` only rejects when `git rev-parse HEAD:skills/<name>` fails — i.e. the folder is not in HEAD, untracked garbage, exactly the state the new test stages. That folder is in no teammate's repo yet is returned in the `Result` value and printed as an installable row, while the sibling case is treated the opposite way: `skillRecords` reports a malformed folder via `onProblem` and **excludes** it, which `search.test.ts:110` pins as `value: [healthy]`. A programmatic consumer (the SPA) gets one kind of junk filtered and the other surfaced. Actionable, not cosmetic: `installOne` resolves through `findSkill(clone, team, ref)` off the clone's disk, and with no explicit version `const latest = input.version ? … : null` never touches git — so `install team/ghost` succeeds and records a placement with team provenance for content that exists only on that one machine.
  **Fix.** Either exclude a rejected hit from `hits` (report-and-skip, matching `skillRecords`' `onProblem` contract) and adjust `search.test.ts:102` to expect `[healthy]`, or keep the row with an explicit `unresolved: true` on `SearchHit` so the SPA need not string-match `'—'`. If surfacing is the intended product call, note the mismatch with `findSkill`/`installOne` (`install.ts:62-123`) in the close-out.

- [ ] **[low] New search test's assertions do not pin its own claim — no count on rows or reported lines, and the healthy `latest` matcher passes vacuously** — `src/commands/__tests__/search.test.ts:102` *(merges 2 reports across Correctness & tests / Reuse, simplify, perf)*
  **Evidence.** The test is titled "costs **one** row and **one** reported line", but neither count is asserted. `expect.arrayContaining` admits extra and duplicate entries, so a regression pushing the ghost hit twice or emitting a third phantom row is green. `toContain` on a joined string admits N copies of the message, so moving the `io.print` at `search.ts:50` outside the `rejected` guard (printing per skill) still passes. The ghost's rendered `ls` row is never asserted at all, so dropping `io.print(formatSkill(...))` for degraded hits would not fail. And `expect.not.stringMatching(/^—$/)` is vacuously true for any non-string — vitest's `StringMatching.asymmetricMatch` computes `isA('String', other) && sample.test(other)` and negates it — so `latest: undefined` (field dropped or renamed) passes. Exact output is fully determined here: `many` is false, `skillRecords` sorts `ghost` before `healthy`, and the only extra line is the stale notice, so `io.lines` is exactly 4 known lines.
  **Fix.** Use the sibling's altitude at `search.test.ts:82`: compute `const tree = (await git(['rev-parse','HEAD:skills/healthy'], clone)).trim().slice(0,8)` and assert `value` equals the exact 2-element order-pinned array; plus `expect(io.lines.filter((l) => l.startsWith('team/ghost:'))).toHaveLength(1)` and `…filter((l) => l.startsWith('  ghost —'))).toHaveLength(1)` — the phrasing the sync test added in this same diff already uses.

- [ ] **[low] The new "nothing was displaced" placer test passes against the pre-fix code, so it does not pin `displacedExists`** — `src/lib/__tests__/placer.test.ts:184`
  **Evidence.** With a non-existent source, `assertNoSymlinks` throws ENOENT before anything is staged, so `destination` and `displaced` are both absent. Base `restoreDisplaced` began `if (!(await isAbsent(destination)) || await isAbsent(displaced)) return undefined;` — `isAbsent(displaced)` is true, it returned `undefined`, `stranded` was `undefined`, and base rethrew the raw ENOENT identically. The test green-lights the pre-fix code and pins nothing about the flag added at placer.ts:56/66/69/82. The flag's only real effect over `if (await isAbsent(displaced)) return undefined` is when `lstat(displaced)` fails with a **non**-ENOENT error, which nothing exercises.
  **Fix.** Make it adversarial: stub `fsForTests` so the displaced-path probe raises EACCES on a failure path where nothing was moved aside, and assert the thrown error is still the raw placement error naming no `.old` path.

---

## Security & data-loss

- [ ] **[medium] `sync`'s pre-lock fast path reads the placed copy outside the target lock, so a concurrent `place()` produces the spurious "Blocked" the fix claims to prevent** — `src/commands/sync.ts:136` *(merges 3 reports within Security & data-loss)*
  **Evidence.** The comment at :132-134 promises an up-to-date placement "never contends for a target another run holds, and never reports that run as a block", then reads `snapshotSkillDirectory(source)` and `snapshotIfPresent(path)` before `lockTarget` at :141. `snapshotIfPresent` → `snapshotSkillDirectory` is a two-phase read (`listSkillFiles` walks, then `readFile` per entry — `src/lib/placer/vendor/skillhub/skill-fingerprint.ts:20-25`) and is deliberately **not** ENOENT-tolerant mid-scan; its own doc at `placer.ts:144` says "one lstat decides that, so an ENOENT raised mid-scan is an error, not 'gone'" — a rule that is only sound under the target lock, which is exactly what this fix removed. Before the change that read happened inside `lockTarget` via `quarantineDrift`. With two sessions syncing at once (a session-start hook on two Claude Code windows), run A holds the lock inside `place()` and renames the staged copy over `~/.claude/skills/sample`; run B's pre-lock read lists the old tree then `readFile`s a path the rename just removed → ENOENT escapes to the per-placement catch at :171-175, emitting `Blocked <path>: ENOENT …` and pushing onto `deferred`. That is precisely the false block the fast path was written to remove. The new test (`an up-to-date placement never contends`) holds the lock with no concurrent writer.
  **Fix.** Make the pre-lock probe advisory: `const placedNow = await snapshotIfPresent(path).catch(() => undefined);` (and the same for `snapshotSkillDirectory(source)`) so any throw falls through to `lockTarget` and the authoritative under-lock decision at :152-155, which re-takes it via `quarantineDrift` where the strict-ENOENT rule is still correct. Worth a test that renames the placed directory away mid-run while the lock is held. `src/commands/uninstall.ts:127` takes the same unconditional per-path target lock and deserves the same look.

---

## Terum invariants

- [ ] **[low] `search`'s `allSettled` turns a systemic git failure into a full page of '—' rows with exit 0** — `src/commands/search.ts:45` *(merges 2 reports within Terum invariants)*
  **Evidence.** Every `latestTree` rejection is absorbed per row (`latest: settled.status === 'fulfilled' ? shortHash(...) : '—'`) and never reaches the per-team `catch` at :56 that does `failures.push(...)`, so the exit-code gate at :63 (`if (failures.length === Object.keys(config.teams).length && failures.length) return failure(...)`) is unreachable for any git-side failure. The comment scopes the degradation to "one unresolvable folder — present on disk but not in HEAD", but `latestTree` throws for *every* git failure mode: `git` not on PATH (spawn failure), an unborn HEAD in a freshly-initialized clone, a corrupt object store. With one configured team and an unborn HEAD, `Promise.all` previously rejected → `team could not be searched: …` → `failure(...)` → non-zero exit; now it returns `success(hits)` with every row `—` and exits 0, so a script gating on the exit code treats a completely unresolvable search as clean.
  **Fix.** Keep the per-hit degrade, but count rejections per team and treat "every hit in a team rejected and `filtered.length > 0`" as the team-level failure it used to be (push to `failures` so :63 can still return `failure`). Same shape applies to `src/commands/ls.ts:41-45` if the parked `latestTrees` helper lands.

---

## Reuse, simplify, perf

- [ ] **[medium] `place()` reports a placement that fully succeeded as a failure when only the displaced-copy cleanup fails, instead of using its own notices channel** — `src/lib/placer.ts:82` *(merges 4 reports across Reuse, simplify, perf / Security & data-loss / Terum invariants)*
  **Evidence.** `restoreDisplaced`'s new doc names the branch explicitly ("when it is not (the new copy landed and only the cleanup failed) … move the copy to quarantine"), but the caller still throws at :82-83. `place()` already owns the right mechanism eleven lines up — `result.notices`, used for exactly this "placed, but a secondary step failed" shape when `appendExclude` fails (:72-74). The message is wrong on its face: the previous copy was never meant to be restored, and the new copy *is* correctly at `destination` — the new test asserts both the throw and `readFile(join(target,'sample','SKILL.md')) === 'v2'` (`placer.test.ts:176-177`), locking in the contradiction. Failure: an open handle inside the old skill folder (an editor, an agent) on Windows, or an immutable/mode-0500 subdir on POSIX, makes `rm(displaced, {recursive:true})` fail with EBUSY/EPERM/EACCES while lines 66-68 have already fully landed the swap. Then — via `install.ts:105`, `place` throws before `input.store.update` at 106-108, so the new copy sits at `~/.claude/skills/<name>` with **no** ledger entry; the retry hits `inspect()` → `collision.kind === 'foreign'` and refuses with "already contains another skill; retry with --force", so the user's own just-installed skill blocks its own reinstall. Via `sync.ts:156`, the outer catch prints `Blocked` and defers, so `placements[path].fingerprint` keeps the OLD value while disk holds the new content; the next sync's `quarantineDrift` sees a mismatch and prints "Local changes at … moved to …" for changes the tool itself made, dumping a redundant quarantine copy.
  **Fix.** Distinguish "the swap landed" from "the placement failed": once `rename(temporary, destination)` at :67 has succeeded, a failed `rm(displaced)` should quarantine the displaced copy and **return** `{ path, snapshot, notices: [`Placed ${destination} but the previous ${name} could not be removed; it is at ${stranded}`] }` — the shape :72-74 already uses and both callers already print (`install.ts:109`, `sync.ts:168`) — so the ledger records the fingerprint actually on disk. Keep the throw only for the genuine failure case (destination empty, restore failed); have `restoreDisplaced` return a tagged result rather than a bare path so the caller can tell them apart. Update `placer.test.ts:164` to assert a successful result carrying the notice plus the quarantined v1. Sibling: `src/commands/share.ts:268-271`.

- [ ] **[medium] `latestTree`-per-skill resilience fixed at 1 of 3 call sites; `ls` still dies whole on the same ghost folder** — `src/commands/search.ts:45` *(merges 2 reports across Reuse, simplify, perf / Terum invariants)*
  **Evidence.** One concern, three divergent implementations: `search.ts:44-45` batched 8-wide with `allSettled` (one bad folder costs one row); `ls.ts:41-45` `return Promise.all(names.map(async (name) => … shortHash(await latestTree(runner, clone, name)) …))` — unbounded fan-out where a single rejection propagates out of `listSkills` to `run`'s catch, so the whole `ls` returns `failure(...)` and prints no roster and no skills; `readme.ts:129-137` (`readReadmeData`, used by `readme.ts:18`) a fully serial `for (const name of names) { … await latestTree(…) }` that also throws on the first bad folder. The diff deleted the comment recording the coupling (`// One git child per hit, all at once (ls does the same);`) and replaced it without touching `ls`. The new test's own comment names the trigger as a real production state ("A safeWrite that lost its clone lock skips its cleanup, and `reset --hard` never removes an untracked folder") — on exactly that clone, `search` degrades gracefully while `ls` and `readme` are bricked. All three read folder names off disk via `readdir`, so all three see the ghost.
  **Fix.** Lift the fan-out + settle policy into one helper beside `latestTree` in `src/lib/readme.ts` — e.g. `latestTrees(runner, clone, names): Promise<Map<string, string | undefined>>` doing the 8-wide `allSettled` — and consume it from `search.ts:44-45`, `ls.ts:41-45`, and `readme.ts:129-137`, which also removes the third serial variant.

- [ ] **[medium] `setup`'s hoisted clone guard reimplements a weaker copy of `ensureClone`'s completeness predicate** — `src/commands/setup.ts:134` *(merges 4 reports across Reuse, simplify, perf / Terum invariants)*
  **Evidence.** The guard decides "is this a complete clone?" with two `exists` probes (`!(await exists(clone))` at :133, `!(await exists(join(clone,'team.json')))` at :134). The repo already owns that predicate — `ensureClone` in `src/commands/team.ts:384-390`, the very verb the new message tells the user to run, and the one whose wording it near-copies ("exists but is not a complete clone of … move it aside and retry"). The copy drops both git-level checks: `cloneOrigin(clone) === null` and `origin !== normalized`. Failure: `team leave`'s `rm(clone, {recursive:true, force:true})` (`leave.ts:46`) is interrupted after `.git` is unlinked but before `team.json`, or the folder is restored from an archive that skipped dotfiles — both `exists` probes pass, so setup declares the machine set up, prompts for a skill share, an invite and the hook, prints `Members:` from team.json and exits ok, while `sync`, `publish` and `install` all fail on a directory with no git repo. That is exactly the state the guard was added to catch, and `team join` would refuse it. Second gap: a clone whose origin points at a different remote than `config.teams[team].remote` passes setup and then fetches/pushes the wrong repository.
  **Fix.** `cloneOrigin` is already exported from `src/lib/teamRepo.ts:331` and `runner` is in scope at `setup.ts:74`. Best: export the read-only half of `ensureClone` as e.g. `describeClone(clone, normalized, runner) -> 'absent' | 'incomplete' | { origin }` and have both `team.ts:384-389` and `setup.ts:133-134` consume it, so the two sites cannot drift and the message text lives in one place. At minimum, add `await cloneOrigin(clone, runner) === null` to the incomplete branch and compare the returned origin against `normalizeRemote(remote)`.

## Triage — 11 confirmed → 5 mechanical · 5 clear · 1 fork · 0 declined

Buckets are derived in code from each finding's ratings (single-fix gate: trivial + low risk + isolated + patch = mechanical). NOTHING here has been applied.

### Mechanical — one fix, trivial / low-risk / isolated, patch supplied; batch-apply on ONE confirmation (5)

- **medium — sync's pre-lock fast path reads the placed copy outside the target lock, so a concurrent place() produces the spurious "Blocked" the fix claims to prevent** — `src/commands/sync.ts:136`
  - Root cause: `snapshotIfPresent` is a deliberately strict, two-phase probe: `lstat` decides "gone", and any ENOENT raised after that (during `listSkillFiles`'s readdir walk or the per-file `readFile` at src/lib/placer/vendor/skillhub/skill-fingerprint.ts:16-25) is re-thrown as a real error. Its own doc comment states the rule — "the path itself is gone — one lstat decides that, so an ENOENT raised mid-scan is an error, not 'gone'" (src/lib/placer.ts:143-148) — and that rule is only sound while the target lock is held, because only the lock guarantees no other process is renaming the tree underneath the scan. The round-1 fix moved this probe OUT of the lock: sync.ts:136 now calls it before `lockTarget(root, skill.name)` at sync.ts:141. A concurrent run inside `place()` does `rename(destination, displaced)` then `rename(temporary, destination)` (src/lib/placer.ts:65-67), so the placed tree legitimately disappears between this run's walk and its reads. The resulting ENOENT is not caught anywhere on the fast path, so it escapes to the per-placement catch at sync.ts:171-175, which emits `Blocked <path>: ENOENT …` and pushes the skill onto `deferred` (rendered by execute.ts as "N skills need review") — exactly the spurious block the hoist was meant to eliminate. In short: a read whose error contract depends on holding a lock was moved outside that lock without relaxing the contract at the new call site. (src/commands/sync.ts:136 (unguarded call; contract at src/lib/placer.ts:143-148, racing writer at src/lib/placer.ts:65-67, escape at src/commands/sync.ts:171-175))
  - Fix: In src/commands/sync.ts:136, change `const placedNow = await snapshotIfPresent(path);` to `const placedNow = await snapshotIfPresent(path).catch(() => undefined);` with a comment saying the unlocked probe is advisory: a failure means "undecided", not "error". Nothing else moves — `snapshotIfPresent`'s strict rule stays intact for the locked caller (`quarantineDrift`, src/lib/placer.ts:138), and the authoritative decision is still re-taken under the lock at sync.ts:152-155, so a genuine failure (EACCES, a truly corrupt tree) is raised there and still reports Blocked. Optional collocated test in a new file (the vi.mock idiom already used by src/lib/__tests__/placer-remove.test.ts, which mocks node:fs/promises module-wide in its own file): make `readFile` under `.claude/skills/sample` throw ENOENT exactly once, then assert sync returns `{ placed: 0, deferred: [] }` with no `Blocked` line.

```diff
--- a/src/commands/sync.ts
+++ b/src/commands/sync.ts
@@ -132,7 +132,10 @@
         // The read-only "nothing to do" decision comes first, outside the lock: an up-to-date placement —
         // the steady state of every session-start sync — never contends for a target another run holds,
         // and never reports that run as a block. The full decision is re-taken under the lock below.
         const repoSnapshot = await snapshotSkillDirectory(source);
-        const placedNow = await snapshotIfPresent(path);
+        // Advisory only. Unlocked, this probe races a concurrent place()'s rename pair, so
+        // snapshotIfPresent's "an ENOENT mid-scan is an error, not gone" rule does not hold here: any
+        // read failure means "undecided", and falls through to the lock, where quarantineDrift re-takes
+        // the decision under that rule — a genuine failure still surfaces there as Blocked.
+        const placedNow = await snapshotIfPresent(path).catch(() => undefined);
         if (placedNow?.fingerprint === entry.fingerprint && repoSnapshot.fingerprint === entry.fingerprint) continue;
         // The target lock is taken before anything about the destination is decided and held across
```

- **medium — sync's pre-lock fast path snapshots the ledger path before inspect(), so a non-directory or symlink at a placement is no longer reported as a foreign collision** — `src/commands/sync.ts:136`
  - Root cause: The pre-lock fast path added at src/commands/sync.ts:135-137 decides "nothing to do" from `snapshotIfPresent(path)` (src/lib/placer.ts:145), a probe that only distinguishes ENOENT-at-lstat from present — it never requires a plain directory and never refuses a link. It now runs AHEAD of the decision that did vet shape: `inspect(destination, …)` at src/commands/sync.ts:148, which classifies anything whose `lstat().isDirectory()` is false as `foreign` (src/lib/placer.ts:28-31). In the non-rename case `destination === join(dirname(path), skill.name) === path`, so the fast path reads the very inode inspect() used to vet first. Two states diverge: (1) a regular file at the ledger path — `snapshotSkillDirectory` → `listSkillFiles` → `readdir` throws ENOTDIR (src/lib/placer/vendor/skillhub/skill-fingerprint.ts:41-56), which escapes to the loop's outer catch at src/commands/sync.ts:167-169 and prints `Blocked <path>: ENOTDIR …` plus `deferred.push(basename(path))`, where the old code printed `… is not a placement this tool owns` and left `deferred` alone (execute.ts renders deferred as "N item(s) need review"); (2) a symlink at the ledger path — `lstat` succeeds, `readdir` follows the link, and a fingerprint-matching target makes the run `continue` silently, following a symlink the repo's own stated rule refuses (src/lib/fs.ts:9-13 "the repo's rule for symlinks everywhere: refuse, never follow"). The round-1 prescription (.planning/reviews/phase1-closeout-r2.hybrid.r1.review.md:460) asked only for a read-only probe and did not carry the shape rule across with it. (src/commands/sync.ts:136)
  - Fix: In src/commands/sync.ts:136, snapshot the ledger path only when it is a plain directory: `const placedNow = (await inspect(path, true)).kind === 'ours' ? await snapshotIfPresent(path) : undefined;` (inspect is already imported at sync.ts:4; `true` asks about shape alone — a non-directory or a symlink returns 'foreign' regardless of the flag, and ownership is still decided under the lock at :148). A file or a symlink at the path therefore skips the fast path, takes the lock, and reaches the existing `Blocked … is not a placement this tool owns` notice with `deferred` untouched. Add two sync.test.ts cases next to the up-to-date-placement test at :138 — a plain file at the ledger path and a symlink pointing at a content-identical copy — each asserting `{ placed: 0, deferred: [] }` plus the Blocked line.

```diff
--- a/src/commands/sync.ts
+++ b/src/commands/sync.ts
@@ -132,7 +132,11 @@
         // The read-only "nothing to do" decision comes first, outside the lock: an up-to-date placement —
         // the steady state of every session-start sync — never contends for a target another run holds,
         // and never reports that run as a block. The full decision is re-taken under the lock below.
+        // The placed copy is read only when the ledger path is a plain directory, judged by the same
+        // classifier the locked pass uses: a file or a symlink there stays the foreign collision reported
+        // below (the repo's rule for symlinks everywhere: refuse, never follow), never a "nothing to do".
+        // `true` asks about shape alone — ownership is still read, and acted on, under the lock.
         const repoSnapshot = await snapshotSkillDirectory(source);
-        const placedNow = await snapshotIfPresent(path);
+        const placedNow = (await inspect(path, true)).kind === 'ours' ? await snapshotIfPresent(path) : undefined;
         if (placedNow?.fingerprint === entry.fingerprint && repoSnapshot.fingerprint === entry.fingerprint) continue;
         // The target lock is taken before anything about the destination is decided and held across
         // the collision check, the quarantine move and the placement — install's shape — so the
--- a/src/commands/__tests__/sync.test.ts
+++ b/src/commands/__tests__/sync.test.ts
@@ -144,6 +144,28 @@
       expect(await run({ config: store }, io)).toMatchObject({ ok: true, value: { placed: 0, deferred: [] } });
       expect(io.lines.filter((line) => line.startsWith('Blocked'))).toEqual([]);
     } finally { await release(); }
   });
 
+  it('a plain file at the ledger path is still the foreign collision the lock reports, not a read that fails', async () => {
+    const { fixture, store } = await configuredSkill(); const home = join(fixture.root, 'home');
+    expect((await install({ ref: 'sample', config: store, home }, new ScriptedPrompter())).ok).toBe(true);
+    const path = join(home, '.claude', 'skills', 'sample');
+    await rm(path, { recursive: true, force: true }); await writeFile(path, 'user-owned, not a placement');
+    const io = new ScriptedPrompter();
+    expect(await run({ config: store }, io)).toMatchObject({ ok: true, value: { placed: 0, deferred: [] } });
+    expect(io.lines.filter((line) => line === `Blocked ${path}: ${path} already exists and is not a placement this tool owns; leaving both untouched.`)).toHaveLength(1);
+    expect(await readFile(path, 'utf8')).toBe('user-owned, not a placement');
+  });
+
+  it('a symlink at the ledger path is refused, never followed, even when it points at an identical tree', async () => {
+    const { fixture, store } = await configuredSkill(); const home = join(fixture.root, 'home');
+    expect((await install({ ref: 'sample', config: store, home }, new ScriptedPrompter())).ok).toBe(true);
+    const path = join(home, '.claude', 'skills', 'sample'); const mirror = join(home, 'mirror');
+    await cp(path, mirror, { recursive: true }); await rm(path, { recursive: true, force: true }); await symlink(mirror, path);
+    const io = new ScriptedPrompter();
+    expect(await run({ config: store }, io)).toMatchObject({ ok: true, value: { placed: 0, deferred: [] } });
+    expect(io.lines.filter((line) => line === `Blocked ${path}: ${path} already exists and is not a placement this tool owns; leaving both untouched.`)).toHaveLength(1);
+    expect(await readFile(join(mirror, 'SKILL.md'), 'utf8')).toContain('description: old');
+  });
+
   it('reports a placement whose target lock another process holds as blocked and leaves it untouched', async () => {
     const { fixture, store } = await configuredSkill(); const home = join(fixture.root, 'home');
     expect((await install({ ref: 'sample', config: store, home }, new ScriptedPrompter())).ok).toBe(true);
```

- **low — search's allSettled turns a systemic git failure into a full page of '—' rows with exit 0** — `src/commands/search.ts:45`
  - Root cause: `src/commands/search.ts:45` switched the per-team fan-out from `Promise.all` to `Promise.allSettled`, and line 51 turns every rejected settle into `latest: '—'`. Nothing between line 45 and the end of the `try` block rolls those rejections up into anything, so a `latestTree` rejection can no longer reach the per-team `catch` at line 56 that does `failures.push(...)`. The exit-code gate at line 63 (`if (failures.length === Object.keys(config.teams).length && failures.length) return failure(...)`) only ever counts failures pushed by that catch, so it is now unreachable for ANY git-side failure. The comment at lines 40-43 scopes the degradation to "one unresolvable folder — present on disk but not in HEAD", but `latestTree` (src/lib/readme.ts:117-121) rejects for every git failure mode: `systemRunner.run` rejects on the child's `'error'` event (git not on PATH, src/lib/runner.ts:25), and a non-zero exit throws for an unborn HEAD or a corrupt object store just as it does for a path missing from HEAD. With one configured team and every hit unresolvable, `run` returns `success(hits)` with every row `'—'`; `createExecute` then leaves `process.exitCode` at 0 (src/index.ts:23-27), so a script gating on the exit code reads a completely unresolvable search as clean. Before the branch, `Promise.all` rejected, the catch printed `team could not be searched: …` and the gate returned `failure(...)`. The missing piece is a team-level rollup: "every hit in this team failed to resolve" is exactly the team failure the catch used to record. (src/commands/search.ts:45 (rollup missing before the catch at :56; consumed by the exit gate at :63))
  - Fix: In src/commands/search.ts, after the per-hit print loop (immediately after `await staleNotice(...)` at line 55, still inside the `try`), add one guarded check: `if (filtered.length && latest.every((settled) => settled.status === 'rejected')) failures.push(`${team}: no skill version could be read; see the per-skill reasons above.`);` — plus a short comment saying per-row degradation is for ONE unresolvable folder while a wholly unresolvable team is still a team failure. The `filtered.length &&` guard matters because `[].every(...)` is true, and a zero-hit team must keep exiting 0 (spec §11: 'zero hits exits 0 with one line'). No other line changes: the '—' rows and the per-skill reason lines stay exactly as round 1 shipped them, and the existing gate at line 63 does the rest. Add one collocated test in src/commands/__tests__/search.test.ts next to the ghost test (line 97): a fixture whose only committed skill does NOT match the term plus an untracked `skills/ghost` that does, so the single team's whole match list is unresolvable and `run` returns `{ ok: false }`.

```diff
--- a/src/commands/search.ts
+++ b/src/commands/search.ts
@@ -52,5 +52,10 @@
         hits.push(hit);
         io.print(formatSkill({ id: hit.id, name: hit.name, author: hit.author, category: hit.category, installs: hit.installs, latest: hit.latest, endorsement: hit.endorsed }));
         }
         await staleNotice(store, team, io, args.now ?? Date.now);
+        // Per-row degradation covers ONE unresolvable folder. When every hit in the team failed to
+        // resolve, the git side itself is unusable — git not on PATH, an unborn HEAD, a corrupt
+        // object store — and the team is as unsearched as the catch below would have called it, so
+        // the exit gate must still see it rather than exiting 0 on a page of '—' rows.
+        if (filtered.length && latest.every((settled) => settled.status === 'rejected')) failures.push(`${team}: no skill version could be read; see the per-skill reasons above.`);
       } catch (error) {
--- a/src/commands/__tests__/search.test.ts
+++ b/src/commands/__tests__/search.test.ts
@@ -103,4 +103,11 @@
     expect(io.lines.join('\n')).toContain('team/ghost: Could not resolve the latest version of ghost');
   });
 
+  it('fails instead of returning a page of dashes when no team could resolve any hit', async () => {
+    const { store, clone } = await searchFixture('team', [{ name: 'healthy', description: 'plain description', category: 'testing', author: 'Seed <seed@example.com>', id: '11111111-1111-4111-8111-111111111111' }]);
+    await mkdir(join(clone, 'skills', 'ghost')); await writeFile(join(clone, 'skills', 'ghost', 'SKILL.md'), skillFile({ name: 'ghost', description: 'needle', category: 'testing', author: 'Seed <seed@example.com>', id: '33333333-3333-4333-8333-333333333333' }));
+    // The only match is the folder git cannot resolve, so the team resolved nothing at all: a
+    // script gating on the exit code must not read that as a clean search.
+    expect(await run({ term: 'needle', config: store }, new ScriptedPrompter())).toMatchObject({ ok: false });
+  });
+
   it('skips and reports a malformed skill folder while returning healthy matches', async () => {
```

- **low — New test's assertions do not pin its own claim — no count on rows or reported lines, and the healthy `latest` matcher passes vacuously** — `src/commands/__tests__/search.test.ts:102`
  - Root cause: The new test's assertions are membership/substring matchers over an output that is fully determined, so neither half of its own title ("costs one row and one reported line") is pinned. `expect.arrayContaining` at search.test.ts:102 admits extra and duplicated `value` entries; `expect(io.lines.join('\n')).toContain(...)` at :103 admits the report line being printed N times (a regression that moved the `io.print` at src/commands/search.ts:50 outside the `settled.status === 'rejected'` guard would still pass); the ghost's rendered `ls` row from src/commands/search.ts:53 is never asserted at all, so dropping `formatSkill` for degraded hits stays green; and `expect.not.stringMatching(/^—$/)` is vacuously true for any non-string (vitest's StringMatching computes `isA('String', other) && sample.test(other)` then negates), so `latest: undefined` passes. The output is fully determined: `many` is false (one team), `skillRecords` sorts folder names (src/lib/skills.ts:23) so `ghost` precedes `healthy`, `installs` is 0 (bareTeam seeds `person('seed')` with no installs, fixtures.ts:72) and `endorsed` is '—' (TEAM_JSON has empty `global`), leaving `io.lines` as exactly three known lines once the stale notice is suppressed with the file's own `freshStamp` helper. The sibling test at search.test.ts:70-83 already asserts exactly that shape (`expect(io.lines).toEqual([...])` plus a `tree` computed with `git rev-parse HEAD:skills/<name>`), so the loose matchers sit below this file's established altitude rather than being a deliberate choice. (/Users/ryanliu/Documents/Terum/terum-codex/phase1-closeout/src/commands/__tests__/search.test.ts:102)
  - Fix: In src/commands/__tests__/search.test.ts:97-104: add `await freshStamp(store, 'team')` after the ghost folder is written (suppresses the stale notice, exactly as the sibling tests at :62-68 and :70-83 do), compute `const tree = (await git(['rev-parse', 'HEAD:skills/healthy'], clone)).trim().slice(0, 8)`, replace `expect.arrayContaining` with a literal two-element array inside `toMatchObject` (vitest enforces array length for `toMatchObject`, pinning both the count and the filter order: ghost then healthy) carrying `latest: '—'` and `latest: tree`, and replace the joined-string `toContain` with `expect(io.lines).toEqual([...])` over three elements: `expect.stringContaining('team/ghost: Could not resolve the latest version of ghost')` (the git stderr tail is version-dependent), the ghost `ls` row, and the healthy `ls` row.

```diff
--- a/src/commands/__tests__/search.test.ts
+++ b/src/commands/__tests__/search.test.ts
@@ -98,9 +98,13 @@
     const { store, clone } = await searchFixture('team', [{ name: 'healthy', description: 'needle', category: 'testing', author: 'Seed <seed@example.com>', id: '11111111-1111-4111-8111-111111111111' }]);
     // A safeWrite that lost its clone lock skips its cleanup, and `reset --hard` never removes an untracked folder.
     await mkdir(join(clone, 'skills', 'ghost')); await writeFile(join(clone, 'skills', 'ghost', 'SKILL.md'), skillFile({ name: 'ghost', description: 'needle too', category: 'testing', author: 'Seed <seed@example.com>', id: '33333333-3333-4333-8333-333333333333' }));
+    await freshStamp(store, 'team');
     const io = new ScriptedPrompter();
-    expect(await run({ term: 'needle', config: store }, io)).toMatchObject({ ok: true, value: expect.arrayContaining([expect.objectContaining({ name: 'healthy', latest: expect.not.stringMatching(/^—$/) }), expect.objectContaining({ name: 'ghost', latest: '—' })]) });
-    expect(io.lines.join('\n')).toContain('team/ghost: Could not resolve the latest version of ghost');
+    const tree = (await git(['rev-parse', 'HEAD:skills/healthy'], clone)).trim().slice(0, 8);
+    // Both halves of the title are counts, so both are pinned exactly: two rows in filter order
+    // (`ghost` sorts first) and one report line. An extra or duplicated row, a report printed per
+    // skill, or a dropped `formatSkill` row for the degraded hit has to fail here.
+    expect(await run({ term: 'needle', config: store }, io)).toMatchObject({ ok: true, value: [expect.objectContaining({ name: 'ghost', latest: '—' }), expect.objectContaining({ name: 'healthy', latest: tree })] });
+    expect(io.lines).toEqual([expect.stringContaining('team/ghost: Could not resolve the latest version of ghost'), '  ghost — Seed <seed@example.com>; testing; 0 installs; —; —', `  healthy — Seed <seed@example.com>; testing; 0 installs; ${tree}; —`]);
   });
```

- **low — The new "nothing was displaced" placer test passes against the pre-fix code, so it does not pin displacedExists** — `src/lib/__tests__/placer.test.ts:184`
  - Root cause: The new test drives place() down a path where the `.old` path genuinely does not exist (a missing source makes assertNoSymlinks throw ENOENT before anything is staged), and on that path the pre-fix probe and the post-fix flag agree: base restoreDisplaced's `await isAbsent(displaced)` returned true → undefined → raw rethrow, exactly what the fixed code does via `displacedExists === false`. So the test is green against 3a9088a and pins nothing. The flag's only distinct behavior over the probe is when the probe itself fails with a non-ENOENT error (EACCES/EIO on the skills root): base's `.catch(() => displaced)` then reported a stranded `.old` copy the run never created, while the flag skips restoreDisplaced entirely. Nothing can reach that branch today because isAbsent calls `lstat` directly (placer.ts:106) and the fsForTests seam only exposes `rename` and `rm` (placer.ts:11) — so the case the flag exists for has no test hook at all. The test's two assertions are also already covered by placer.test.ts:49 and :214. (src/lib/__tests__/placer.test.ts:184 (untestable branch it should pin: src/lib/placer.ts:82 via the direct lstat at src/lib/placer.ts:106))
  - Fix: src/lib/placer.ts:11 — add a third member to the existing test seam, `lstat: (path: string) => lstat(path)` (wrapped, not the raw binding, because node's lstat overloads are awkward to restub), and update the seam docstring; src/lib/placer.ts:106 — `await fsForTests.lstat(path)` instead of `await lstat(path)` (isAbsent has exactly two callers, both inside restoreDisplaced). src/lib/__tests__/placer.test.ts:184 — restub fsForTests.lstat to throw EACCES for any path ending `.old`, delegating otherwise, then assert the missing-source placement still rejects with `{ code: 'ENOENT' }` and never says 'could not be restored'. Remove the flag and the test fails on both assertions.

```diff
--- a/src/lib/placer.ts
+++ b/src/lib/placer.ts
@@ -8,6 +8,7 @@
 import { Runner, systemRunner } from './runner.js';
 
-/** The one seam placer.test.ts needs to make a rename fail between the two moves of a replace, cross a volume (mirrors hook.ts), or fail the removal of a displaced copy after the swap. */
-export const fsForTests = { rename, rm };
+/** The one seam placer.test.ts needs to make a rename fail between the two moves of a replace, cross a volume (mirrors hook.ts), fail the removal of a displaced copy after the swap, or fail the probe for a displaced copy with something other than ENOENT. */
+// `lstat` is wrapped rather than passed through raw: its overloads make the bare binding awkward to restub from a test.
+export const fsForTests = { rename, rm, lstat: (path: string) => lstat(path) };
 
 export type PlacementScope = { kind: 'global' } | { kind: 'project'; project: string };
@@ -103,5 +104,5 @@
 }
 
 async function isAbsent(path: string): Promise<boolean> {
-  try { await lstat(path); return false; } catch (error) { if (isMissing(error)) return true; throw error; }
+  try { await fsForTests.lstat(path); return false; } catch (error) { if (isMissing(error)) return true; throw error; }
 }
--- a/src/lib/__tests__/placer.test.ts
+++ b/src/lib/__tests__/placer.test.ts
@@ -183,7 +183,16 @@
 
-  it('a failure before anything was displaced rethrows the placement error itself, naming no copy that was never made', async () => {
-    const root = await temporaryDirectory(); const target = join(root, '.claude', 'skills');
-    await expect(place(join(root, 'no-such-source'), target, 'sample', { quarantineRoot: join(root, 'quarantine') })).rejects.toThrow(/ENOENT/);
-    await expect(place(join(root, 'no-such-source'), target, 'sample', { quarantineRoot: join(root, 'quarantine') })).rejects.not.toThrow(/could not be restored/);
-  });
+  it('a failure before anything was displaced rethrows the placement error itself, even when the probe for the displaced copy fails', async () => {
+    const root = await temporaryDirectory(); const target = join(root, '.claude', 'skills');
+    // The probe for the `.old` path fails with something other than ENOENT (a locked-down skills root):
+    // a run that displaced nothing must still rethrow its own error and name no copy it never made.
+    const realLstat = fsForTests.lstat;
+    fsForTests.lstat = async (path) => {
+      if (path.endsWith('.old')) throw Object.assign(new Error('EACCES: simulated probe failure'), { code: 'EACCES' });
+      return realLstat(path);
+    };
+    try {
+      await expect(place(join(root, 'no-such-source'), target, 'sample', { quarantineRoot: join(root, 'quarantine') })).rejects.toMatchObject({ code: 'ENOENT' });
+      await expect(place(join(root, 'no-such-source'), target, 'sample', { quarantineRoot: join(root, 'quarantine') })).rejects.not.toThrow(/could not be restored/);
+    } finally { fsForTests.lstat = realLstat; }
+  });
```


### Clear — one fix clearly wins, but not mechanical; confirm each, or hand to /parallel-fix (5)

- **medium — place()'s unguarded `finally` rm replaces the composed recovery error, so the quarantine location is lost on the exact path the new `.catch()` was added for** — `/Users/ryanliu/Documents/Terum/terum-codex/phase1-closeout/src/lib/placer.ts:86`
  - Root cause: The `finally` at src/lib/placer.ts:86 runs `await rm(temporary, { recursive: true, force: true });` with no `.catch`. A `finally` block runs after the `catch` has thrown, and per JS semantics an exception raised inside `finally` REPLACES the pending completion — both the `catch`'s composed error (message + `{ cause }`) and a `return` from the `try`. The two `rm` calls at lines 77 and 86 target the same path with the same options, so the only way line 77's guarded call rejects (`force: true` already suppresses ENOENT) is a persistent, non-ENOENT failure — EACCES on the skills root, EPERM/EBUSY from an AV or open handle, EIO on a network mount — and in exactly that case line 86 fails identically and throws. So the `.catch(() => undefined)` added at line 77 this round buys nothing: the comment "the finally repeats it; it must not abort the recovery below" has it backwards — the `finally` copy is the one that can abort the recovery, and it is the unguarded one. Consequences, both confirmed against the code: (1) on the failure path, the invariant-34 recovery message `"… — the previous <name> could not be restored to <dest>; it is at <quarantine path>"` built at line 83 is discarded and the user instead sees `EACCES … rmdir '/…/.sample.terum-<uuid>.tmp'`, losing the only report of where their previous copy went, plus the `{ cause }` chain; (2) on the success path, `return result` at line 75 is discarded and a placement that actually landed on disk throws — sync.ts:156 then skips the `store.update` at 158-163 that records the new fingerprint (it goes to the outer catch at sync.ts:170 and only prints `Blocked …`), so the next sync sees the correctly-placed copy as drift and quarantines it. The new test at src/lib/__tests__/placer.test.ts:163 cannot catch this: its stub throws only for paths ending `.old`, and the `finally`/catch cleanups use the direct `rm` import rather than the `fsForTests.rm` seam, so no test can currently make the staging cleanup fail. (/Users/ryanliu/Documents/Terum/terum-codex/phase1-closeout/src/lib/placer.ts:86)
  - Rated: trivial / low risk / pattern — siblings: Sweep worklist — best-effort cleanup in a `finally` (or in a `catch` after a decision) that can throw and replace a more informative error:
1. /Users/ryanliu/Documents/Terum/terum-codex/phase1-closeout/src/lib/placer.ts:86 — the site under review; `finally { await rm(temporary, { recursive: true, force: true }); }` replaces the catch's invariant-34 recovery message and discards the success path's `return result`.
2. /Users/ryanliu/Documents/Terum/terum-codex/phase1-closeout/src/commands/share.ts:272 — `replaceDirectory`'s `finally { await rm(temporary, { recursive: true, force: true }); }` has the identical shape and is NOT touched by this diff (git diff 3a9088a...HEAD shows no src/commands/share.ts hunk). Same failure: after the catch at 268-271 restores the author's folder with `moveDirectory(displaced, to)` and rethrows, an EACCES/EPERM on the `${to}.terum-<uuid>` staging directory replaces that error. The same catch also calls `await moveDirectory(displaced, to)` (line 270) unguarded, so a failed restore likewise replaces the original error instead of reporting where the author's tree went — the sibling defect worth fixing in the same sweep.
Checked and deliberately NOT part of the pattern: placer.ts:127 (`remove`'s `rm(destination, …)`) and placer.ts:170 (`moveDirectory`'s post-`cp` `rm(from, …)`) are the operation itself, not best-effort cleanup — their failure must propagate, and neither sits in a `finally`; src/commands/leave.ts:46-52's `Promise.all` of `rm`s is likewise the work being asked for, inside `withCloneLock`, not a cleanup; src/lib/teamRepo.ts:384 already uses the correct form (`finally { await release().catch(() => undefined); }`), which is the model the placer `finally` should follow.
  - Why one fix: No real fork. Both options fix the bug the same way and to the same degree — stop a failed cleanup of a throwaway staging folder from becoming the error the user sees. The recommended one is just the smaller edit: add 'ignore errors' to one line. The alternative additionally deletes a now-redundant duplicate cleanup line, which is tidier but reopens a note the team already closed as 'harmless', for no behavioural gain. Nothing product-level is at stake, and neither choice changes what happens on a normal, successful install.
  - **Option 1: Guard the finally (and correct the line-77 comment) (recommended)** — Depth 2/4 · Cost 1/4. In /Users/ryanliu/Documents/Terum/terum-codex/phase1-closeout/src/lib/placer.ts, change line 86 from `await rm(temporary, { recursive: true, force: true });` to `await fsForTests.rm(temporary, { recursive: true, force: true }).catch(() => undefined);` — routing it through the seam already exported at line 11 (`export const fsForTests = { rename, rm };`) so the failure is reachable from a test. Rewrite the line-77 trailing comment to state the truth: both staging cleanups are best-effort, and the finally is the copy that would otherwise replace the recovery error. Add a case to src/lib/__tests__/placer.test.ts alongside the one at line 163 that stubs `fsForTests.rm` to throw for the `.tmp` staging path as well as the `.old` path, and asserts the rejection message still contains 'simulated cleanup failure', 'could not be restored', and that the copy is under `<root>/quarantine`. Behaviour is unconditionally safe: on both success paths `temporary` was already renamed onto `destination`, so the finally rm is a no-op there; on failure paths line 77 already attempted the identical removal. The only thing given up is a thrown error about a leftover hidden `.<name>.terum-<uuid>.tmp` directory, which is strictly cheaper than losing the stranded-copy report. *Wins if:* You want the smallest revert-safe change that makes the recovery message and the successful-placement return survive a persistent staging-cleanup failure — it wins unless you also want the now-pointless duplicate cleanup deleted in the same commit.
  - Option 2: One cleanup only, in a guarded finally — Depth 2/4 · Cost 1/4. Delete the `rm(temporary, …)` at src/lib/placer.ts:77 entirely and keep a single guarded cleanup in the `finally` at line 86 (`await fsForTests.rm(temporary, { recursive: true, force: true }).catch(() => undefined);`). Safe because the recovery never reads `temporary`: `restoreDisplaced` (placer.ts:95-103) only probes `displaced` and `destination`, and `moveToQuarantine` only moves `displaced`, so running cleanup after the recovery instead of before it changes nothing. Same test addition as option 1. Note this overrules the r2 ledger entry `[declined] Duplicate rm(temporary) — the catch-block copy is dead now that a finally does the same` in .planning/debug/harden/phase1-closeout-r2.deferred.md, whose stated ground ('the finally repeats it; the duplicate is harmless') is precisely what this finding rebuts. *Wins if:* You want the dead duplicate gone as well as the defect fixed, and are willing to reopen the r2 ledger's 'harmless duplicate' decline in the same change.
- **medium — place() reports a placement that fully succeeded as a failure when only the displaced-copy cleanup fails, instead of using its own notices channel** — `src/lib/placer.ts:82`
  - Root cause: place() conflates "the swap failed" with "the swap landed and only our own cleanup failed". At src/lib/placer.ts:67-69 the replace path renames the staged copy onto the destination (the placement is complete at that instant), then removes the moved-aside copy with `await fsForTests.rm(displaced, …)` and only afterwards clears `displacedExists = false`. A throw from that rm (EBUSY/EPERM from an open handle on Windows, EACCES on a mode-0500 subdir that `fs.cp` reproduced, an immutable flag on POSIX) therefore escapes into the outer catch with `displacedExists` still true, and the catch at :82-83 reads that flag as "the placement failed": it quarantines the previous copy via restoreDisplaced (whose new occupied-destination branch at :97-102 exists precisely for this case) and rethrows `"<error> — the previous <name> could not be restored to <destination>; it is at <stranded>"`. The message is false on its face — the previous copy was never meant to be restored, and the new copy is correctly at `destination` — and the rejection propagates as a placement failure even though place() owns the right channel for "placed, but a secondary step failed" eleven lines above (`result.notices` at :71-74, used for a failed appendExclude). Because `replace: true` is only ever passed when a ledger entry already exists (`collision.kind === 'ours'` in install.ts:104 and sync.ts:155), the throw skips `store.update` in both callers (install.ts:106-108, sync.ts:158-164), so the ledger keeps the OLD fingerprint while disk holds the new content: install exits non-zero on a successful install, and sync prints `Blocked <path>: …` and defers, after which the next sync's quarantineDrift sees the mismatch and prints "Local changes at <path> moved to <quarantine>" for content the tool itself wrote, dumping a redundant copy into quarantine. The collocated test pins the contradiction (placer.test.ts:175-177 asserts the rejection AND that the destination holds 'v2'). (/Users/ryanliu/Documents/Terum/terum-codex/phase1-closeout/src/lib/placer.ts:68 (cleanup rm escaping post-swap) and :82-83 (catch treating displacedExists as a failed placement))
  - Rated: moderate / medium risk / isolated
  - Why one fix: No real trade-off. The first option is the only one that makes the tool tell the truth: the skill really was installed, so it should say so, record it, and add one warning line about the old copy that it could not delete (which it has already moved to the quarantine folder). The other two leave the tool saying "install failed" about an install that succeeded, which then makes the next sync treat the tool's own freshly written files as if the user had edited them and move them aside a second time. Option 1 uses the warning channel that already exists in the same function for the same kind of "it worked, but one tidy-up step didn't" case, and both commands already print those warnings, so nothing new has to be wired up. The only cost is that one recently added test has to be flipped from expecting an error to expecting a success plus a warning — and that test currently asserts both the error and the fact that the new files are correctly in place, which is the contradiction being fixed.
  - **Option 1: Resolve the post-swap cleanup failure inside place() and report it through notices (recommended)** — Depth 3/4 · Cost 1/4. In src/lib/placer.ts, handle the cleanup where it happens instead of letting it fall into the failure catch. At :67-69 set `displacedExists = false` immediately after `await fsForTests.rename(temporary, destination)` succeeds (the swap has landed; nothing is left to restore), then call a new non-throwing helper in place of the bare rm — e.g. `const stranded = await discardDisplaced(displaced, name, options.quarantineRoot);` where `discardDisplaced` does `try { await fsForTests.rm(displaced, { recursive: true, force: true }); return undefined; } catch { return quarantineRoot === undefined ? displaced : await moveToQuarantine(displaced, quarantineRoot, name).catch(() => displaced); }`. After the result object is built at :71, push the notice in the existing shape: `if (stranded) result.notices.push(\`Placed ${destination} but the previous ${name} could not be removed; it is at ${stranded}\`)`. Leave restoreDisplaced and the :82-83 throw for the genuine failure (the second rename threw, the destination is empty, the restore is real). Update the collocated test src/lib/__tests__/placer.test.ts:164-182 to await a resolved placement and assert the notice text plus the same on-disk facts it already checks (destination 'v2', no `.terum-` leftovers, quarantined 'v1') instead of asserting a rejection at :172-176. *Wins if:* Wins whenever the ledger must record what is actually on disk: the fingerprint is stamped, the exit code is honest, the user still learns where the old copy went, and both callers already drain `notices` (install.ts:109, sync.ts:168, hook mode via execute.ts:39) so no new plumbing is needed.
  - Option 2: Keep the throw; re-stamp the ledger from the callers after a failed place() — Depth 1/4 · Cost 2/4. Leave placer.ts as it is and compensate in both commands: in install.ts around 105-110 and sync.ts around 155-170, catch the place() rejection, call `snapshotIfPresent(destination)` (already exported at placer.ts:145) and, when a copy is present whose fingerprint differs from the ledger entry, write the new fingerprint before rethrowing / reporting `Blocked`. *Wins if:* Wins only if place()'s current contract — every non-success is a rejection — must be preserved for an out-of-repo consumer; it stops the phantom-drift quarantine on the next run but still reports a successful install as a failure, keeps the false 'could not be restored' wording, and duplicates the recovery reasoning in two commands.
  - Option 3: Message-only: keep the throw, correct the wording — Depth 0/4 · Cost 0/4. In placer.ts:82-83, branch the message on whether `destination` is occupied and say `"<error> — <name> was placed at <destination>, but the previous copy could not be removed; it is at <stranded>"` for the post-swap case, leaving the control flow and the test's rejection assertion untouched. *Wins if:* Wins only under a hard freeze where the misleading sentence is the sole thing worth changing; it leaves the stale ledger, the non-zero exit on a successful install, and the next-sync quarantine of the tool's own copy exactly as they are.
- **medium — The "slices of eight" rewrite is never exercised past one chunk — a wrong stride or slice bound passes the whole suite** — `src/commands/__tests__/search.test.ts:97`
  - Root cause: The round-1 fix replaced `await Promise.all(filtered.map(...))` with a manual chunking loop — `for (let index = 0; index < filtered.length; index += 8) latest.push(...await Promise.allSettled(filtered.slice(index, index + 8).map((skill) => latestTree(runner, clone, skill.name))))` (src/commands/search.ts:45) — whose two risky properties are (a) the fan-out really being capped at 8 in flight and (b) `latest[index]` staying aligned with `filtered[index]` ACROSS a chunk boundary (consumed at src/commands/search.ts:49 as `const settled = latest[index]!`). Neither property is exercised by any test, because every `searchFixture(...)` call in src/commands/__tests__/search.test.ts seeds at most 3 skills (the 3-skill list at :33-37; the new ghost test at :97-104 yields exactly 2 matches — one seeded skill plus the untracked `ghost` folder). With `filtered.length <= 3` the loop body runs exactly once in every test and `filtered.slice(0, 8)` is always the whole list, so the push/slice/stride arithmetic is never evaluated a second time. Verified escaping mutations: `index += 4` (overlapping chunks; every row past the 8th reads another skill's hash) stays green; `slice(index, 8)` (plausible typo → every later chunk empty → `latest[index]` undefined → TypeError on `settled.status`) stays green; `latest.unshift(...)` stays green. Compounding it, no search test ever passes `args.runner` (SearchArgs already accepts `runner?`, src/commands/search.ts:11), so the one seam that makes the concurrency bound observable — `latestTree` calling `runner.run('git', ['rev-parse', 'HEAD:skills/<name>'])` (src/lib/readme.ts:117-121) — is unused by the whole file. The new ghost test does pin the allSettled per-row degradation (the other half of the fix), just not the batching half. (src/commands/__tests__/search.test.ts:97 (largest fixture in the file is 3 skills; this test yields 2) — code under test: src/commands/search.ts:45)
  - Rated: moderate / low risk / isolated
  - Why one fix: Option 1 gives strictly more coverage than option 2 at the same effort and runs faster (no git pushes at all), and option 3 changes shipping code to close a testing gap while stepping on a decision that is explicitly parked for Ryan. The one caveat, not a fork: if the parked decision later replaces the eight-at-a-time loop with a single `git ls-tree` per clone, the "max 8 in flight" line in the new test has to be updated (the alignment and per-row assertions survive either implementation) — that is a one-line edit the day that lands, and a test that speaks up when the concurrency behaviour changes is the point.
  - **Option 1: Cross-boundary test through the Runner seam (fake runner, 10 untracked skill folders) (recommended)** — Depth 3/4 · Cost 1/4. In src/commands/__tests__/search.test.ts, beside the ghost test (after line 104), add one test that: (1) builds an empty fixture — `const { store, clone } = await searchFixture('team', [])` — so no git pushes are needed; (2) writes ten untracked skill folders `skill-01`…`skill-10` directly into `join(clone, 'skills')` with the existing `skillFile({ name, description: 'needle', category: 'testing', author: 'Seed <seed@example.com>', id: <distinct uuid> })` helper (skillRecords reads the folder from disk and sorts by name — src/lib/skills.ts:23 — so the slice boundary falls deterministically between skill-08 and skill-09); (3) passes a local `Runner` fake as `run({ term: 'needle', config: store, runner: fake }, io)` that answers `['rev-parse', 'HEAD:skills/<name>']` with `{ code: 0, stdout: `${name}${'0'.repeat(32)}\n` }` (names are exactly 8 chars, so `shortHash` yields the skill's OWN name), answers `skill-09` with `{ code: 1, stderr: 'not in HEAD' }`, and increments/decrements an in-flight counter around one `setImmediate` yield while recording the max. Assert: every hit's `latest === hit.name` except skill-09's, which is '—'; the printed line is exactly `team/skill-09: Could not resolve the latest version of skill-09: not in HEAD`; and the recorded max in flight is 8. That third assertion is the only one that fails under an unbounded `Promise.all` (max 10); the first fails under `index += 4` / `unshift`; `slice(index, 8)` throws. *Wins if:* Wins whenever you want both risky properties of the loop pinned in one cheap, git-free test — it is the only option that makes the 8-cap observable at all, and it costs no extra git subprocesses.
  - Option 2: Alignment-only test on the real git fixture (nine-plus pushed skills) — Depth 2/4 · Cost 1/4. Extend src/commands/__tests__/search.test.ts with a test that calls `searchFixture('team', skills)` with nine or more real skills (searchFixture pushes each one from the seed clone, so the hashes are genuine) plus one untracked ghost folder that sorts into the second slice, then asserts each returned hit's `latest` equals `(await git(['rev-parse', `HEAD:skills/${name}`], clone)).trim().slice(0, 8)` for its OWN name, that only the ghost row is '—', and that the reported line names the ghost. No fake runner, no new test seam. *Wins if:* Wins if the project wants search tests to keep exercising real `git rev-parse` end to end (every existing test in this file does) and is willing to trade the fan-out-cap assertion plus ~9 sequential pushes (~45 git subprocesses) for that fidelity.
  - Option 3: Extract a tested `mapBounded(items, limit, fn)` helper and use it in search — Depth 4/4 · Cost 2/4. Move the chunking out of src/commands/search.ts:44-45 into a small exported helper in src/lib (e.g. `mapBounded(items, 8, fn)` returning `PromiseSettledResult[]` in input order), unit-test the helper directly over the boundary (order preservation at n=17, cap observed, one rejection isolated), and have search call it. This is the ledger's fork option 2 ('cap the per-hit fan-out with a small bounded map at search and ls') in helper form, so ls.ts:41 could adopt it later. *Wins if:* Wins only if the pending /decision-walk fork (.planning/debug/harden/phase1-closeout-r2.deferred.md:8-9) has already been settled on the bounded-map option — otherwise it pre-empts a decision Ryan reserved, and it touches production code to fix a test-coverage gap.
- **medium — latestTree-per-skill resilience fixed at 1 of 3 call sites; ls still dies whole on the same ghost folder** — `src/commands/search.ts:45`
  - Root cause: listSkills builds its rows with `return Promise.all(names.map(...))`, where `names` is every directory under `skills/` read straight off disk (src/commands/ls.ts:39), and each row awaits `latestTree(runner, clone, name)`. latestTree (src/lib/readme.ts:117-121) runs `git rev-parse HEAD:skills/<name>` and throws whenever the folder is not in HEAD. Because that await sits inside Promise.all, the first rejection rejects the whole aggregate, which propagates out of listSkills into run's catch (src/commands/ls.ts:35) and turns the entire command into failure(...) — no roster line and no skill line, for a clone whose other N-1 skills are healthy. The trigger is a real on-disk state the branch's own new test names: "A safeWrite that lost its clone lock skips its cleanup, and `reset --hard` never removes an untracked folder" (src/commands/__tests__/search.test.ts ghost-folder case). The fix diff hardened exactly this concern at src/commands/search.ts:44-45 (slices of eight + Promise.allSettled; a rejected row prints one line and shows latest '—') and deleted the comment that recorded the coupling — "One git child per hit, all at once (ls does the same)" — without touching the sibling. A third variant, readReadmeData's serial for-loop (src/lib/readme.ts:129-136), also throws on the first unresolvable folder. (src/commands/ls.ts:45 (rejection escapes through the Promise.all at src/commands/ls.ts:41 into run's catch at src/commands/ls.ts:35))
  - Rated: trivial / low risk / pattern — siblings: Same mistake — "resolve the latest tree for every folder read off disk, and let one rejection kill the command":
1. src/commands/ls.ts:41-46 (listSkills) — the fix site; unbounded Promise.all, rejection reaches run's catch at line 35 and returns failure(...) for the whole command, including the member/project forms at lines 28-29.
2. src/lib/readme.ts:129-136 (readReadmeData) — serial for-loop, `const latest = await latestTree(...)` at line 134 throws on the first unresolvable folder. Its only caller is src/commands/readme.ts:18, the hidden host-side entry point for the scaffolded Action, which already fails loudly on other malformed-repo conditions (readme.ts:132-133 reject an invalid or mismatched SKILL.md). Whether a CI gate SHOULD degrade instead of failing red is a judgement call, not an automatic sweep item — flag it rather than patching it blind.
3. src/commands/search.ts:44-45 — already fixed on this branch; the reference implementation for the '—' + one-reported-line treatment.
Adjacent, related but not the same defect (do not batch it in): src/commands/ls.ts:23 uses readPeople (src/lib/readme.ts:140-143), another Promise.all that rejects the whole command on a single unparseable people/*.json, where search.ts:30 deliberately reports and skips the bad member file. Same "ls is the strict one, search is the tolerant one" split, different call.
Also note: the per-hit git fan-out shared by sites 1-3 is parked, not open — [fork] entry at src/commands/search.ts:41 in .planning/debug/harden/phase1-closeout-r2.deferred.md ("ls still forks one git per skill; the shared latestTrees helper is the call"). That entry parks the SHAPE of the fan-out fix, not the crash-whole failure mode, and neither of its two options makes ls survive a ghost folder on its own — which is why this stays a fix rather than a decline.
  - Why one fix: In plain terms: today, one leftover folder in the team's local copy makes `ls` print absolutely nothing — no member list, no skills — while `search` handles the same folder by showing one dash and one explanatory line. The recommended fix makes `ls` behave like `search`: skip the version for that one folder, say why, keep every other row. It is about three lines, reversible, and touches nothing shared. The bigger cleanup (one git call per team instead of one per skill, shared by all three places) is genuinely better engineering, but it is already written down as a decision waiting for Ryan (the [fork] entry in .planning/debug/harden/phase1-closeout-r2.deferred.md), it touches code three commands depend on, and it changes how the GitHub Action reacts to a broken repo. The small fix does not get in that decision's way and would simply be deleted if the big one lands, so there is no real trade-off to resolve first.
  - **Option 1: Per-row catch in ls.listSkills (fan-out-neutral) (recommended)** — Depth 2/4 · Cost 1/4. In src/commands/ls.ts, thread the existing io Prompter into listSkills (call site line 25, signature line 38) and give the one await at line 45 a .catch that prints `${name}: ${message}` and returns '—'. shortHash already passes '—' through unchanged (src/lib/readme.ts:180), and format() already renders it, so the row prints with latest '—' exactly like search's degraded row. ~3 production lines, no change to the Promise.all fan-out. Add an ls.test.ts case mirroring the new search.test.ts ghost-folder test: an untracked skills/ghost folder still yields the roster plus the healthy rows, one reported line, and latest '—'. *Wins if:* You want `ls` to stop dying on a ghost folder now, without prejudging the fan-out scope call that is already parked for a human — this fix stays correct and deletable under either outcome of that call.
  - Option 2: Mirror search exactly in ls (slices of eight + allSettled) — Depth 2/4 · Cost 1/4. Restructure listSkills the way search.ts:44-45 was restructured: read the frontmatter for all names, then resolve latestTree in slices of eight with Promise.allSettled, printing the reason for each rejected entry and using '—' for its latest. Leaves readReadmeData alone. *Wins if:* Reading ls and search side by side and seeing literally the same batching + settle policy is worth more than staying neutral on the parked fan-out decision — this implements that ledger fork's option 2 for ls before Ryan picks.
  - Option 3: Shared latestTrees helper next to latestTree, adopted at all three sites — Depth 4/4 · Cost 2/4. Add `latestTrees(runner, clone, names): Promise<Map<string, string>>` in src/lib/readme.ts backed by one `git ls-tree HEAD:skills` (teamRepo.ts:263 skillTrees is the existing parser to lift), with a guard for an empty/missing skills tree, and consume it from src/commands/search.ts:44-45, src/commands/ls.ts:41-46 and src/lib/readme.ts:129-136. A folder absent from the map is simply '—' everywhere, so the divergence, the N-child fan-out and the serial third variant all disappear. *Wins if:* Ryan is ready to settle the parked scope call now: this is option 1 of the [fork] entry in .planning/debug/harden/phase1-closeout-r2.deferred.md, and it also closes the related M4 treeBetween fan-out fork — but it touches shared code used by three commands and changes what the Action's README path does with a malformed checkout.
- **medium — setup's hoisted clone guard reimplements a weaker copy of ensureClone's completeness predicate** — `src/commands/setup.ts:134`
  - Root cause: setup's hoisted guard answers "is this a complete clone?" with two filesystem probes only — `exists(clone)` and `exists(join(clone,'team.json'))` — and never asks git, so it accepts two states that the repair it names (`team join`, whose `ensureClone` at team.ts:384-391 owns the same decision) refuses: (1) a directory holding team.json with no git repository, which is exactly what an interrupted `team leave` rm or a dotfile-skipping restore leaves behind — `ensureClone` catches it with `cloneOrigin(clone, runner) === null`; and (2) a clone whose origin points at a different remote than `config.teams[team].remote` — `ensureClone` catches it with `origin !== normalized`. In both states setup declares the machine set up, goes on to prompt for a skill share, invite teammates, write the SessionStart hook and print the roster, and exits ok, while `sync` (sync.ts:61 refreshClone), `publish` (publish.ts:38) and `install` all fail on a directory that is not a git repo or is the wrong repo. The copy is also textually derivative of ensureClone's message ("exists ... is not a complete clone of ... move it aside"), so the two definitions of "complete" are now free to drift. (src/commands/setup.ts:134 (guard block 132-136), duplicating and weakening src/commands/team.ts:384-391 (`ensureClone`))
  - Rated: moderate / medium risk / isolated
  - Why one fix: One option clearly wins. The cheap alternative (option 2) does exactly the same user-visible work as the recommended one — it makes setup refuse a folder that is not really a git clone, and one that points at the wrong repository — but it does so by writing the rule out a second time by hand, next to the copy in `team join` that it was already drifting from. That is the actual complaint. The recommended option puts the rule in one place that both the wizard and the `team join` repair read from, so the next person who tightens it only has to tighten it once. The extra work is one small shared function plus rewiring a seven-line private helper that the existing join tests already cover, and it is useful no matter how the still-open product question ("should setup just re-clone the missing folder itself?") is answered, because all three answers to that question still need someone to decide whether the folder on disk is a real clone.
  - **Option 1: Shared describeClone predicate in teamRepo.ts, consumed by both ensureClone and setup (recommended)** — Depth 4/4 · Cost 1/4. Add `export async function describeClone(clone: string, normalized: string, runner: Runner)` to src/lib/teamRepo.ts immediately beside the `cloneOrigin` it already owns (line 331), returning a discriminated state: `{state:'absent'}` when `!(await exists(clone))`; `{state:'incomplete'}` when `await cloneOrigin(clone, runner) === null || !(await exists(join(clone,'team.json')))`; `{state:'foreign', origin}` when `origin !== normalized`; `{state:'ok', origin}` otherwise. Rewrite src/commands/team.ts:384-391 `ensureClone` to switch on it, keeping its existing actions and message text at the call site (absent -> `cloneTeam`; incomplete -> "exists but is not a complete clone of ..."; foreign -> "is a clone of X, not Y; pass --as"). Replace the two `exists` probes at src/commands/setup.ts:133-134 with the same call, passing `normalizeRemote(remote)` (config holds the raw remote on the creator path, team.ts:243, and the normalized one on the join path, team.ts:304), mapping absent -> the existing "clone at X is missing; run <repair>" message and incomplete/foreign -> the existing "is not a complete clone ...; move it aside, run <repair>" message (add the origin to the foreign wording). Add two setup tests beside the new one at setup.test.ts:60: a clone whose `.git` was removed but `team.json` survives (today it passes the guard) and a clone of a second bare fixture while config names the first. *Wins if:* Wins whenever the two sites must never again disagree about what 'a complete clone' means: one exported predicate is the only definition, it closes both git-level holes at once, and it stays correct under every branch of the pending 'should setup re-clone itself' product fork, since all three branches still need this decision made the same way.
  - Option 2: Inline the two missing git checks in setup's guard only — Depth 2/4 · Cost 0/4. In src/commands/setup.ts add `cloneOrigin` to the imports from '../lib/teamRepo.js' and `normalizeRemote` to the existing '../lib/remote.js' import, then in the guard at 132-136 resolve `const origin = await cloneOrigin(clone, runner)` once after the absent check, fold `origin === null ||` into the incomplete branch at :134, and add a third branch for `origin !== normalizeRemote(remote)` reusing team.ts:390's 'is a clone of X, not Y' wording. src/commands/team.ts:384 is left untouched, so the predicate still lives in two hand-written copies. *Wins if:* Wins if the team wants the smallest possible diff before the deferred re-clone product call lands (.planning/debug/harden/phase1-closeout.deferred.md:10) and is willing to accept that setup and `team join` keep two independent copies of the completeness rule that can drift again.

### Forks — no single fix clearly wins; run `/decision-walk <this report>` (1)

- **low — search now returns an untracked, unshared folder as a real hit, unlike the malformed-folder path four lines below which excludes it** — `src/commands/search.ts:52`
  - Root cause: The round-1 fix turned a rejected `latestTree` into a *returned* row instead of routing it through the report-and-skip contract the same loop already uses. At src/commands/search.ts:49-52, `settled.status === 'rejected'` only changes the rendered `latest` to the `'—'` sentinel and prints a reason line; the hit is still `hits.push(hit)`-ed into the success value and printed as a normal `ls`-format row. `latestTree` (src/lib/readme.ts:117-121) rejects exactly when `git rev-parse HEAD:skills/<name>` exits non-zero — i.e. the folder is enumerated from the clone's WORKING TREE by `skillRecords` (src/lib/skills.ts:20-39, `readdir` of `skills/`) but is absent from HEAD (the untracked-leftover state the new test at src/commands/__tests__/search.test.ts:97-104 stages). Four lines above, the sibling junk-folder path does the opposite: `skillRecords`' `onProblem` (search.ts:28) reports a malformed folder and EXCLUDES it from `records`, pinned by search.test.ts:106-112 as `value: [only healthy]`. So `run()`'s structured value now filters one kind of unusable folder and surfaces the other, and the only machine-readable difference on the surfaced one is the `latest === '—'` string — `SearchHit` (search.ts:12) carries no flag. It is actionable rather than cosmetic because `installOne` → `findSkill` (src/commands/install.ts:63-66, src/lib/skills.ts:41-50) resolves from the clone's disk and, with no explicit version, `const latest = input.version ? await resolveVersion(...) : null` (install.ts:73) never consults git, so the phantom installs and records team provenance. Note the rejection is NOT exclusively "absent from HEAD": `systemRunner` wires `child.on('error', reject)` (src/lib/runner.ts:26), so a spawn EAGAIN/ENOENT (git not on PATH) rejects the same way — which is why blanket exclusion is not automatically the right answer. (/Users/ryanliu/Documents/Terum/terum-codex/phase1-closeout/src/commands/search.ts:49-52 (type that carries no marker: search.ts:12; rejecting callee: src/lib/readme.ts:117-121; the sibling that excludes: src/lib/skills.ts:20-39 via search.ts:28; the site that makes it actionable: src/commands/install.ts:63-73))
  - Why it is a fork: A real product call is left, and the code cannot settle it: should a skill folder that sits in the team's folder on this machine but was never actually committed to the team repo show up in `search` at all? Option 1 says yes, show it but stamp it 'version unknown' in a way a program can read, so the future web UI can grey out its Install button — cheap, additive, and it keeps the promise the last review round already made in a comment. Option 2 says no, hide it exactly like the other broken folder is hidden — tidier and more consistent, but it also hides a perfectly real, shared skill on the day `git` itself will not start (the code cannot tell those two failures apart today), and in that case the user is told 'No skills found', which is worse than a row with a dash. Option 3 removes the guesswork by teaching the underlying helper to distinguish 'not committed' from 'git broke', which is the genuinely correct answer but touches code that three commands share right at close-out — worth doing only if it rides along with the batching rework already parked for a decision. I recommend option 1 for this pass and, if option 2's stricter rule is what the product wants, do it via option 3 rather than option 2 so the git-is-broken case does not become a silent empty result.
  - **Option 1: Mark the unresolved row in the type; keep returning it (recommended)** — Depth 2/4 · Cost 0/4. src/commands/search.ts:12 — add `unresolved: boolean` to `SearchHit` with a one-line doc comment naming what it means (`HEAD:skills/<name>` lookup failed: on disk but not in HEAD, or git would not run). src/commands/search.ts:51 — set `unresolved: settled.status === 'rejected'` in the hit literal (the `latest: '—'` rendering and the existing report line at :50 stay). src/commands/__tests__/search.test.ts:102 — assert `unresolved: true` on the ghost row and `unresolved: false` on the healthy one, so the marker is pinned rather than inferred from the `'—'` string. `SearchHit` is referenced nowhere outside search.ts (grep: search.ts:12/15/21 only) and cli.ts:90 passes the Result straight to `execute`, so the added field reaches no other caller. *Wins if:* Wins if search's corpus stays what spec §6 says it is — 'every `skills/*/SKILL.md` of each configured team's clone' (.planning/specs/2026-09-02-phase-1-build.md:307), i.e. the working tree — so a failed git lookup must never silently delete a row from a SEARCH result, while the phase-2 SPA still gets a typed way to refuse to install it; it is also the only option that does not reverse the r1 triage decision the comment at search.ts:40-43 documents.
  - Option 2: Report-and-skip: exclude the rejected hit, matching skillRecords' onProblem contract — Depth 3/4 · Cost 1/4. src/commands/search.ts:50 — after the report line, `continue` (skip both `hits.push` and the `formatSkill` print) so a folder whose `HEAD:skills/<name>` cannot be resolved leaves output the same shape a malformed folder does: one reported line, no row. Update the comment at search.ts:40-43 (it currently promises 'costs one row and one reported line') and rewrite search.test.ts:102 to `value: [expect.objectContaining({ name: 'healthy' })]` plus the existing printed-reason assertion. Optionally drop the `'—'` branch from the `latest` expression, which then becomes unreachable. *Wins if:* Wins if the product rule is 'search returns only skills that exist in the team's HEAD', and a false negative is preferred to a phantom — accepting that a rejection is not proof of untrackedness (src/lib/runner.ts:26 rejects on spawn error), so a machine with no `git` on PATH would print N reason lines and then 'No skills found.' while returning `ok: true, value: []`.
  - Option 3: Distinguish the two rejection causes at the helper, then skip only the genuinely-absent folder — Depth 4/4 · Cost 2/4. src/lib/readme.ts:117-121 — split `latestTree` into a throwing form (kept for the README/publish paths that must fail closed) and a resolving form that returns `undefined` when `git rev-parse` merely exits non-zero (folder absent from HEAD) while still rejecting on a spawn/IO error. src/commands/search.ts:45-52 then skips-and-reports the `undefined` case (uniform with skillRecords) and degrades to `'—'` only for a real git failure, so neither failure mode is wrong. Decide the same question at the other per-skill callers — src/commands/ls.ts:45 (today one unresolvable folder fails all of `ls`), src/lib/readme.ts:134 and :162 (deliberately fail-closed, per the r1 sweep note) — and add collocated tests for both branches. *Wins if:* Wins if it lands together with the already-parked `latestTrees` fork (.planning/debug/harden/phase1-closeout-r2.deferred.md, [fork] on src/commands/search.ts:41 — 'the shared latestTrees helper is the call'), since that rework touches the same helper and the same three call sites; doing both in one edit is far cheaper than doing them a week apart.

### Declined by triage — overrule if you disagree (the standalone test applies) (0)

_none_

### Untriaged — the triage agent returned nothing usable; investigate by hand or resume the run (0)

_none_
