# Rev-2 verification: the 53 medium/low findings that were never adjudicated

Source: the 6-lens adversarial verification pass over `.planning/specs/2026-09-11-library-marketplace-refactor.md` **rev 2**, 2026-09-11.
102 findings raised; the 49 blocker/high were adversarially refuted (27 survived and are applied in rev 2/rev 3).
**These 53 were never argued against and are not applied.** They are raised against rev 2 — check each against rev 3 before acting, since rev 3 reversed three locked sections (D9, D12, D13).


## M0 [medium] §10.1 :: `approved()` is cited at `:620-622`, which is sync's error handler, not `approved()`
DETAIL: The disposition table's last row reads "`approved()` (`:620-622`) | **Move** to `install.ts` — it is only about tool-grant consent." `approved()` is not at those lines at either revision. At faa402c, :620-622 is the tail of `run()`'s catch block (`const message = error instanceof Error …; return failure(silent ? message : …)`); at d09d278, :620-622 is the `unreachable.length` failure return. The real function is a two-line export at :626-628 (faa402c) / :631-633 (d09d278). An implementer taking the range literally moves sync's failure reporting into install.ts and leaves `approved()` behind in a file §10 deletes in full — and `approved()` has two live call sites (:281, :425) that would break.
EVIDENCE: src/commands/sync.ts:626 — `export function approved(config: Awaited<ReturnType<ConfigStore['read']>>, id: string, grants: ReturnType<typeof allowedTools>): boolean {` (:627 is the body, :628 the brace). src/commands/sync.ts:620-622 is `const message = error instanceof Error ? error.message : String(error); return failure(silent ? message : \`Sync failed: ${message}\`, …); }`
PROPOSED FIX: Change the row to "`approved()` (`:626-628` at faa402c / `:631-633` at d09d278, plus its two call sites at `:281` and `:425`) | **Move** to `install.ts` — it is only about tool-grant consent."

## M1 [medium] §11.2 :: `SettingsDialogs.tsx:104` does not exist — the file is 94 lines; the Local-state paragraph is in SettingsContent.tsx
DETAIL: §11.2 ends: "Update `SettingsDialogs.tsx:27` and the Local-state paragraph at `:104` to drop \"connected skill records\" and \"baseline\"." The first half is exact — SettingsDialogs.tsx:27 is the `'Its connected skill records and any pending operations on this machine'` bullet. The second half is not: SettingsDialogs.tsx has only 94 lines, contains no line 104, and the string "baseline" appears nowhere in it. The Local-state paragraph that mentions baseline is at line 104 of a different file — `SettingsContent.tsx`, the file §11.2's own first sentence names. The bare `:104` reads as continuing SettingsDialogs.tsx, so an implementer opens a 94-line file and either gives up on the edit or hunts blind.
EVIDENCE: wc -l desktop/src/screens/settings/SettingsDialogs.tsx → 94. desktop/src/screens/settings/SettingsContent.tsx:104 — `<Group label="Local state"><Card><Row title="Local state" desc="~/.terum/skills · … every connected skill's baseline is lost, and your handle, email and display name are re-asked.">`
PROPOSED FIX: Write it as two explicit citations: "Update `SettingsDialogs.tsx:27` (drop \"connected skill records\") and the Local-state paragraph at `SettingsContent.tsx:104` (drop \"every connected skill's baseline is lost\")."

## M2 [medium] §13.1(a) :: The committed Action is described as running `npx …@latest` "four times with `contents: write`" — only one of the four jobs has write
DETAIL: §13.1(a) opens: "Every team repo runs `.github/workflows/terum-skills.yml`, which pins `npx -y terum-skills@latest` four times with `contents: write`." The count of four is right; the permission is not. The seeded workflow has four jobs, and only `readme` carries `contents: write`. `hygiene` and `receipt-check` run `contents: read`; `publish-comment` runs `contents: read` plus `pull-requests: write`. As written the spec claims four un-updatable write-capable entry points into every team repo, quadrupling the apparent blast radius of the migration-debt hazard. The underlying hazard is still real — the `readme` job does have `contents: write` and does commit and push — so the paragraph's conclusion survives, but its scoping sentence would mislead anyone sizing the risk or auditing what the Action can touch.
EVIDENCE: src/commands/team.ts, WORKFLOW literal: :565-566 `permissions:` / `contents: read` (hygiene, npx at :580); :584-585 `contents: read` (receipt-check, npx at :591); :594-595 `contents: write` (readme, npx at :599); :610-612 `contents: read` + `pull-requests: write` (publish-comment, npx at :620)
PROPOSED FIX: Replace with: "…which pins `npx -y terum-skills@latest` four times across four jobs; only the `readme` job runs with `contents: write` (`src/commands/team.ts:594-595`) and it is the one that commits and pushes, so it is the only one that can damage the repo — the other three run `contents: read`."

## M3 [medium] §1.1 :: The quote "a version is the 40-char lowercase tree hash" is attributed to version.ts:12; it lives in receipt.ts:54
DETAIL: §1.1's first and most load-bearing bullet reads: "`src/lib/version.ts:12` — *\"a version is the 40-char lowercase tree hash\"*." That string is not in version.ts anywhere. version.ts:12 is `if (/^[0-9a-f]{39}$/i.test(version)) throw new Error(\`Version ${version} for ${name} must be a full skill tree hash…\`)` — a comment-and-guard about 39-character truncations. The quoted sentence is the zod error message on the receipt schema's `version` field, which §1.1's own fourth bullet correctly cites two lines later. So the section's headline evidence for "versioning already exists, it is just invisible" double-counts one source and points the reader at a file that never says it. The rest of the bullet (`resolveVersion()` resolves `HEAD:skills/<name>` and refuses a commit, a tag or a blob) is accurate — version.ts:14 refuses non-tree, :19 requires SKILL.md at the tree root, :22 is the `HEAD:skills/${name}` expression.
EVIDENCE: src/lib/evals/receipt.ts:54 — `version: z.string().regex(VERSION_PATTERN, 'a version is the 40-char lowercase tree hash'),`  vs  src/lib/version.ts:12 — `if (/^[0-9a-f]{39}$/i.test(version)) throw new Error(...)`; `grep -rn "40-char" src/lib src/commands` returns only receipt.ts:54 and guard.ts:39
PROPOSED FIX: Rewrite the bullet as: "`src/lib/version.ts:9-27` — `resolveVersion()` resolves `HEAD:skills/<name>` to a tree and refuses a commit, a tag or a blob. The vocabulary is explicit in the receipt schema's own error text: *\"a version is the 40-char lowercase tree hash\"* (`src/lib/evals/receipt.ts:54`)."

## M4 [low] §6.4 :: `root` is described as changing to a value it already has
DETAIL: §6.4 instructs: "`[modify] src/commands/evalReport.ts` — the history directory filter at `:55` changes from `/^[0-9a-f]{40}$/i` to `parseVersionFolder(d) !== null`; **`root` becomes `join(clone,'evals',record.id)`**; sort by `(version DESC, run_id DESC)`." The `:55` half is exact. The `root` half describes a no-op: `root` is already character-for-character `join(clone, 'evals', record.id)` at evalReport.ts:34. An implementer reads this as "root is currently something else" and burns time looking for the difference, or worse, assumes the clause implies a semantic change (e.g. re-keying receipts by name) that §6.4's next paragraph explicitly rules out.
EVIDENCE: src/commands/evalReport.ts:34 — `const root = join(clone, 'evals', record.id);`
PROPOSED FIX: Delete the `root` clause, or restate it as a no-change note: "`root` already is `join(clone,'evals',record.id)` (`:34`) and stays — only `currentDirectory` (`:35`) moves from the tree hash to the version folder."

## M5 [low] §8.4 :: "both fan-outs still go through `mapWithConcurrency(…, 8, …)`" — the catalog fan-out uses 4, deliberately
DETAIL: §8.4's read-cost paragraph asserts of existing code that "both fan-outs still go through `mapWithConcurrency(…, 8, …)`". No call site in either tree passes 8. The desktop catalog's member fan-out — the one §8.4 is about — uses `CATALOG_CONCURRENCY = 4`, and the constant carries a comment explaining the 4 is a deliberate ceiling under the bridge's cap of eight, not an arbitrary number. The CLI's own calls use `LOCAL_SCAN_CONCURRENCY`, `FINGERPRINT_CONCURRENCY`, and a literal 4. Stating 8 as the existing shape invites an implementer to "restore" 8 in §4.1's new `skillVersions` fan-out and in the catalog, pushing the desktop read path to the bridge cap.
EVIDENCE: desktop/src/backend/tauri/index.ts:355-356 — `/** Four member reads leave headroom under the bridge cap of eight. */` / `const CATALOG_CONCURRENCY = 4;`, used at :782; `grep -rn "mapWithConcurrency(" src desktop/src` shows no non-test call passing 8
PROPOSED FIX: Replace with: "both fan-outs still go through `mapWithConcurrency`; the catalog keeps `CATALOG_CONCURRENCY = 4` (`desktop/src/backend/tauri/index.ts:356`), which is bounded by the bridge cap of eight and must not be raised." And in §4.1, justify the new `skillVersions` limit rather than presenting 8 as the house value.

## M6 [low] §11.4 :: "offers **Run eval** with no gate" — it is gated on the `runEvalInApp` feature flag today
DETAIL: §11.4's opening sentence is "`cardActions` (`desktop/src/components/domain/skill-card-actions.ts:20-25`) unconditionally pushes `moveAction`, `placeAction`, `publishAction`, and offers **Run eval** with no gate." The cited range is exact and the `moveAction`/`placeAction`/`publishAction` half is exact (`:24`). But Run eval is not ungated: `:23` is `if(runEvalInApp)actions.push({key:'run-eval',…})`. The spec's actual point — that there is no *`skill.path`* gate, so the row is offered for a skill that is not on this machine — is correct and its fix stands; the word "unconditionally" just overstates it and could lead a reader to look for a push that isn't there.
EVIDENCE: desktop/src/components/domain/skill-card-actions.ts:23 — `if(runEvalInApp)actions.push({key:'run-eval',label:'Run eval',to:at('tab=evals','dialog=run-eval'),reason:null});`
PROPOSED FIX: "…and offers **Run eval** behind the `runEvalInApp` feature flag only — with no check that the skill is on this machine."

## M7 [low] §7.5 (and §11.3's `:134-140`) :: `placer.remove()` is credited with a ledger check it cannot perform
DETAIL: §7.5's last bullet says "A stale ledger key makes the placement undeletable by the tool (`placer.remove()` refuses any path without a matching entry)", and §11.3 leans on the same mechanism ("never a path it did not place — which `placer.remove()` already enforces at `:134-140`"). `placer.remove()` takes `(targetRoot, path, expectedFingerprint, quarantineRoot)` and has no access to `config.placements`; it cannot see entries. What `:134-140` enforces is a shape check — the root must satisfy `isSkillsRoot()` and the target must sit directly under it — and what stops an unplaced folder being deleted is the fingerprint comparison inside `quarantineDrift` at `:144-147`. The "only paths in the ledger" guarantee lives one level up, in `removePlacements`, whose own doc comment states it. The conclusions in both sections hold; the attribution does not, and §11.3's locked replacement rule is safer to state against the function that actually enforces it.
EVIDENCE: src/lib/placer.ts:134 — `export async function remove(targetRoot: string, path: string, expectedFingerprint: string, quarantineRoot: string)`; :137-139 the isSkillsRoot/direct-child refusals; :144-147 the quarantineDrift fingerprint gate. src/commands/uninstall.ts:226-229 — `/** The placement ledger is the sole authority for paths that may be removed locally. … */ export async function removePlacements(store, matching, io)`
PROPOSED FIX: §11.3: "…deletes only paths present in `config.placements` — which `removePlacements` (`src/commands/uninstall.ts:226-229`) is documented as the sole authority for — and never a path whose bytes have drifted, which `placer.remove()` enforces through `quarantineDrift` at `src/lib/placer.ts:144-147`." §7.5: "A stale ledger key makes the placement undeletable by the tool: nothing in `config.placements` names it, so `removePlacements` never targets it."

## M8 [low] §11.3 :: The `teardownTeam` protection is described as one-directional; the code also protects a placement that CONTAINS an authoring source
DETAIL: §11.3 says `teardownTeam` "skips deleting any placement whose path is, or is inside, a connected authoring source". The predicate is three-way: equal, placement inside authoring source, OR authoring source inside placement. The third case matters for the locked replacement rule that follows — under today's code a placement directory that happens to contain an authoring folder is also spared, and the new rule (delete every `config.placements` path) does not spare it. That is arguably intended given §3.6 deletes `config.shared`, but the spec should say so rather than leave the reader believing the third case never existed.
EVIDENCE: src/commands/leave.ts:77-80 — `const shared = sources.find((source) => { const authoring = resolve(source); return placement === authoring || placement.startsWith(authoring + sep) || authoring.startsWith(placement + sep); });`
PROPOSED FIX: "…skips deleting any placement whose path is, contains, or is inside a connected authoring source (`src/commands/leave.ts:77-80`). The replacement rule drops the containing case too: under a Finder-mirror Library a placement that wraps an authoring folder is not a case we still recognise."

## M9 [low] §12 (and §10) :: connect.ts is 597 lines, not 598
DETAIL: §12 opens `connect` (the whole verb and `src/commands/connect.ts`, 598 lines …)`. The file is 597 lines at both faa402c and d09d278. Trivially wrong on its own, but it is the second line-count in the spec that does not match the declared base — §10's "all 802 lines" of sync.ts is correct at faa402c and 807 at d09d278 — and together they are the cheapest corroboration that the survey ran on the older tree (see the base-drift finding).
EVIDENCE: wc -l src/commands/connect.ts → 597; `git show origin/main:src/commands/connect.ts | wc -l` → 597; `git show origin/main:src/commands/sync.ts | wc -l` → 807 (spec says 802)
PROPOSED FIX: "`src/commands/connect.ts`, 597 lines" and "`[delete] src/commands/sync.ts` — all 807 lines at `d09d278` (802 at `faa402c`)".

## M10 [medium] §4.2 :: §4.2 deletes the `'connect'` GuardAction while `guardRawPush` still constructs one, and leaves `ownsSkill` without a contract under layout 3
DETAIL: §4.2's first bullet deletes `'connect'`, `'eval'`, `'eval-assets'` and `'decline'` from `GuardAction`, and its second deletes `SKILL_ACTIONS` and the `skills/` regex (row a). `guardRawPush` — which the same bullet list edits, but only for path shapes and one error message — synthesises a `GuardContext` with `action: 'connect'` to reuse `ownsSkill`. That is a compile error, and beneath it a design hole: with row a gone, `ownsSkill` has no caller inside `guard()`, and nothing in the spec says what it reads now that `SKILL.md` lives at `skills/<name>/v<N>/SKILL.md` rather than `skills/<name>/SKILL.md`. An implementer will invent an answer for the one guard the spec calls "the authorization model".
EVIDENCE: src/lib/guard.ts:117 `if (skill && ownsSkill(tree, skill[1]!, { action: 'connect', handle, author: identity.author })) continue;`; src/lib/guard.ts:52-53 `const skill = /^skills\/([^/]+)\/.+$/.exec(path);` / `if (skill && SKILL_ACTIONS.includes(context.action) && ownsSkill(…)) continue; // row a`; spec §4.2:239-240,247
PROPOSED FIX: Add a bullet to §4.2: "`ownsSkill` loses its `GuardAction` parameter (it never used one but the raw-push path passed `'connect'`) and reads the latest version's committed `SKILL.md` — `skills/<name>/v<max>/SKILL.md` from the **pre-image**, `listVersions` over `tree.paths('skills/<name>/')`. `guardRawPush` calls it as `ownsSkill(tree, name, { handle, author })`. A path under a `v<N>` folder that does not yet exist in the pre-image is a new skill and takes the new-folder branch."

## M11 [medium] §4.2 / §5.1 :: Row a″ inherits a precondition that layout 3 can never satisfy, and admits a narrower set than §5.1 step 9 writes
DETAIL: §4.2 says row a″ "Replaces today's row h" and restates only the regex and "add-or-replace, under `'publish'` only". Today's row h body also requires the skill to already exist at `skills/<name>/SKILL.md` in the pre-image — a path that never exists in layout 3 (SKILL.md is at `skills/<name>/v<N>/SKILL.md`), and which a first publish has no pre-image for at all. An implementer who keeps the body gets `Write guard refused skills/x/evals/triggers.yaml for publish` on every publish that ships eval assets. Separately, §5.1 step 9 writes `skills/<name>/evals/**` while row a″ admits only `triggers.yaml` and `cases/*.ya?ml`, so any other file a user happens to keep under `evals/` aborts the whole publish inside `safeWrite` rather than being reported and skipped.
EVIDENCE: src/lib/guard.ts:95 `return tree.before(`skills/${match[1]!}/SKILL.md`) !== undefined;` inside `permitsEvalAsset`; src/lib/guard.ts:79 `const EVAL_ASSET_PATH = /^skills\/([^/]+)\/evals\/(?:cases\/[^/]+\.ya?ml|triggers\.yaml)$/;`; spec §4.2:242, §5.1:280
PROPOSED FIX: Row a″ becomes: "…add-or-replace, under `'publish'` only. **The row-h precondition `tree.before('skills/<name>/SKILL.md') !== undefined` is dropped** — that path does not exist in layout 3 and a first publish has no pre-image; the publish action is the authorization. Only these two shapes are admitted: publish reports and skips anything else it finds under the local `evals/` rather than writing it."

## M12 [medium] §5.2 / §5.3 / §3.4 :: An identical republish plus a yes to the profile prompt writes `version: null` into a schema that forbids null
DETAIL: §5.3 types `PublishResult.version` as `string | null` — "'v4', or null when identical" — and §5.2 places the profile prompt unconditionally "after the write", which on the identical path is an `ok` result with `created: false`. §9.3's `offerProfileEntry(io, {id,name,version,via})` is then called with `version === null`, and §3.4 types `profileEntrySchema.version` as `z.string().regex(VERSION_FOLDER)` — "never null". A user republishing an unchanged skill (the §5.2 happy path: attach an eval, don't mint) and answering yes gets a zod throw inside the people-file write. §5.1 step 8 also assigns `identicalTo = K`, an integer, where §5.3 types it `string | null // 'v2'`.
EVIDENCE: spec §5.3:293-298, §5.2:285-287, §3.4:177, §5.1:279, §9.3:500; the write lands in `personSchema` (src/lib/schema.ts:64-87), whose `parseJson` throws on a failed parse rather than returning a Result
PROPOSED FIX: §5.2: "Profile prompt, after the write, carrying the version the skill now sits at — `version` when minted, `identicalTo` when identical. It is never null: an identical publish still points the profile entry at the existing version." §5.1 step 8: "`identicalTo = versionFolderName(K)` (the string `'v2'`, matching §5.3)."

## M13 [medium] §6.4 / §6.2 :: §6.4's evalReport change list is a no-op on the line that matters and never re-points the local-run reader, so new local evals disappear from `eval-report`
DETAIL: §6.4 says "`root` becomes `join(clone,'evals',record.id)`" — `root` is already exactly that. The lines that actually break are unmentioned: `teamCurrent = await resolveVersion(...)` (the function §4.1 guts), `currentDirectory`, the `newest.receipt.version !== teamCurrent` cross-check, and — most consequentially — `localRoot = resolve(store.root,'evals',teamName,record.id)`, the reader for this machine's own runs. §6.2 re-keys that store to `~/.terum/skills/evals/local/<digest>/<runId>/` and promises the old trees "are read for display and never written again," which only holds if `eval-report` reads both. As written it reads only the old path, so every run made after M3 is invisible in the only verb that shows local runs, and §7.3's Library card is the sole surviving reader of the new store.
EVIDENCE: src/commands/evalReport.ts:33 `const teamCurrent = await resolveVersion(clone, record.name, undefined, args.runner ?? systemRunner);`; :34 `const root = join(clone, 'evals', record.id);`; :55 `if (!/^[0-9a-f]{40}$/i.test(directory)) continue;`; :63 `const localRoot = resolve(store.root, 'evals', teamName, record.id);`; spec §6.4:349, §6.2:333
PROPOSED FIX: Replace §6.4's evalReport bullet with: "`teamCurrent` (`:33`) becomes `listVersions(clone, record.name)[0]` and `currentDirectory` its folder name; the receipt cross-check at `:42` compares against that folder; the history filter at `:55` becomes `parseVersionFolder(d) !== null`; sort by (version DESC, run_id DESC); **`localRuns` reads both stores** — the content-keyed `evals/local/<digest>/` for every digest this skill's folder has had, plus the legacy `evals/<team>/<id>/` (§6.2, read-only) — and adds `fallbackFrom: string | null`. `root` (`:34`) is already correct and does not change."

## M14 [medium] §3.2 / §3.4 / §8.2 :: "One module; nothing else parses or formats a version segment" is unreachable — the desktop imports nothing from `src/`
DETAIL: §3.2 declares `src/lib/versions.ts` the single owner of the version vocabulary ("Nothing else parses or formats a version segment") and calls `versionLabel` "the ONLY UI form". But the desktop is a separate bundle that imports nothing from `src/` — it re-declares every CLI shape as its own zod schema and shells out. §3.4's fifth row (`cliLocalRow.placement.version`) and §8.2/§8.3's user-facing strings therefore all live on the far side of a boundary `versions.ts` cannot cross, so `VERSION_FOLDER` and `Version N` will necessarily exist twice. The spec should say which copy is canonical and how they are kept honest, or an implementer will either duplicate silently or try to import `node:fs/promises` (which `listVersions` uses) into the browser bundle — the exact hazard §1.5 flags for `serve-verbs.ts`.
EVIDENCE: a repo-wide grep for desktop imports of `src/lib` returns nothing; desktop/src/backend/tauri/index.ts:68 `const cliLocalRow = z.object({ …, placement: z.strictObject({ id: z.string(), team: z.string(), version: z.string().length(40).nullable() }).nullable(), … }).strict();`; src/lib/serve-verbs.ts is described in spec §1.5:78 as a leaf that "must import nothing"
PROPOSED FIX: Add to §3.2: "`versions.ts` splits in two: `src/lib/versions.ts` (pure — `VERSION_FOLDER`, `parseVersionFolder`, `versionFolderName`, `versionLabel`; imports nothing, safe for any bundle) and the fs-using `listVersions`, which lives in `src/lib/teamRepo.ts`. The desktop imports nothing from `src/`, so `desktop/src/backend/version.ts` holds a deliberate second copy of the pure four, pinned to the CLI's by a shared fixture test (`['v2','v10','v9'] → [10,9,2]`, `v03` rejected, `versionLabel(3) === 'Version 3'`)."

## M15 [low] §7.4 / §11.4 :: The Library card's field list drops `teamed`, which the card's own router needs, and implies a `SkillCard` split the spec never declares
DETAIL: §7.4 gives an exhaustive-looking list — "Library `SkillCard` fields: name, path, project (root label), description, category, frontmatter, placed, onDiskOnly, localEval, localEvalStale" — and §11.4 keeps `cardActions` as the shared menu builder. `detailPath()` reads `skill.teamed` to decide whether a card opens `/skill/local?path=…` or `/skill/<name>`; with `teamed` gone, every Library card (including a folder that belongs to no team) routes to the team detail page by name. `SkillCard` is also one interface serving both `Library.skills` and `Catalog.skills`, so an exhaustive Library list plus a marketplace-only `installedVersion` (§8.3) implies two types where there is one. The list also renames `desc` to `description` without saying so.
EVIDENCE: desktop/src/components/domain/skill-card-actions.ts:10 `return !skill.teamed&&skill.path?'/skill/local?path='+encodeURIComponent(skill.path):'/skill/'+encodeURIComponent(skill.name);`; desktop/src/backend/types.ts:33 `export interface SkillCard {teamed:boolean;path:string|null;…;installs:string;…;placed:boolean;onDiskOnly:boolean;teamState:TeamState;…}`; types.ts:62 `Catalog {…skills:SkillCard[]…}` and :79 `Library {…skills:SkillCard[]…}`
PROPOSED FIX: §7.4: "`SkillCard` splits into `SkillCard` (the shared core the ⋯ menu reads: `teamed`, `path`, `name`, `desc`, `category`, `frontmatter`, `placed`, `onDiskOnly`, `project`, `flags`, `indicators`) plus `LibraryCard = SkillCard & { localEval; localEvalStale }` and `MarketCard = SkillCard & { installs; teamState; latestVersion; evalVersion; installedVersion }`. The Library card carries no team-derived field; `teamed` stays because `detailPath` (`skill-card-actions.ts:10`) routes on it."

## M16 [low] §2 D4 / §5.2 / §3.2 :: Two locked user-facing strings violate D1's own rendering rule
DETAIL: D1 locks "Rendered as **'Version 3'** in every user-facing string" and §3.2 calls `versionLabel` "the ONLY UI form". D4, four decisions later, writes its card example as `v4 · you have v2 → Reinstall` (§8.3 renders the same card correctly as `Version 4 · you have Version 2`). §5.2's identical-publish message — locked verbatim and gated by a §14.1 test — reads "identical to version X", lowercase and with X's rendering unspecified, so an implementer can legitimately produce "identical to version v2", "version 2", or "version Version 2" and still claim conformance. Implementers copy example strings.
EVIDENCE: spec §2:84 (D1), §2:90 (D4 `v4 · you have v2`), §3.2:143 `versionLabel(n: number): string;  // \`Version ${n}\`  <- the ONLY UI form`, §5.2:285, §14.1:629
PROPOSED FIX: Rewrite D4's example as `Version 4 · you have Version 2 → Reinstall`, and add to §5.2: "X is rendered by `versionLabel`, so the shipped string is `…is identical to Version 2 of the skill already in the team repository.` — the §14.1 verbatim test pins that exact form."

## M17 [low] §9.1 :: Deleting the empty-roots early return also breaks non-interactive installs, which §9.1's rationale does not cover
DETAIL: §9.1 says "**Always ask where.** … Delete `if (!roots.length) return { kind: 'global' }` — an interactive install always asks." The rationale is scoped to interactive, but the line it deletes is the non-interactive path's default: immediately below it, a non-interactive caller with no destination throws. After the deletion, a headless `terum-skills install <skill>` on a machine with no registered projects — the common case right after `setup`, and the shape a CI or hook invocation takes — stops defaulting to `~/.claude/skills` and fails with "Pass --into global or --into <checkout root>".
EVIDENCE: src/commands/install.ts:253-255 `const roots = [...new Set(await Promise.all(((await store.read()).checkouts ?? []).map(checkoutPath)))];` / `if (!roots.length) return { kind: 'global' };` / `if (!interactive) throw new Error('Pass --into global or --into <checkout root>');`
PROPOSED FIX: §9.1: "Delete the `if (!roots.length) return { kind: 'global' }` early return **for the interactive path only** — an interactive install always asks, even with no projects registered (the picker then offers Global alone). A non-interactive install with no `--into` keeps today's behaviour and resolves to Global; it must not start failing on a fresh machine."

## M18 [medium] §6.3, §12 :: RunEvalDialog frames every eval as running against the team repo's version — the exact model §6.3 inverts — and is never named
DETAIL: §12 deletes "the `evalCommitChoice` capability and the 'Commit the receipt to the team' checkbox", which is one line of this file. The rest of it contradicts §6.3 outright: its body sentence tells the user the run "Evaluates the team's current version <8 hex> from the team repo… not your installed <8 hex>", and its no-capability branch says "The receipt is committed to the team repo when it completes." After §6.3 an eval runs against a LOCAL folder and commits nothing. `evalRun.start` also still passes `team` and `commit`. The prompt's "All evals have to be run locally" and "If you want to eval a marketplace skill, install it, have it show up in your library, then eval it locally" are the sentences this dialog's copy denies.
EVIDENCE: desktop/src/screens/skill/RunEvalDialog.tsx:29 `const version=s.versions?.teamCurrent;` · :30 `const versionLine=`Evaluates the team's current version${version?' '+version.slice(0,8):''} from the team repo${version&&s.versions?.placed&&s.versions.placed!==version?', not your installed '+s.versions.placed.slice(0,8):''}.`;` · :27 `...s.evalEstimateText+' The receipt is committed to the team repo when it completes.'...` · :24 `evalRun.start({ref:s.name,name:s.name,...(s.team?{team:s.team}:{}),commit:active?.commit??commit});`
PROPOSED FIX: Add to §6.3: "`[modify] desktop/src/screens/skill/RunEvalDialog.tsx` — `versionLine` becomes *Evaluates the copy on this machine at `<pathLabel>`.*; delete the `teamCurrent`/`placed` comparison (`:29-30`), the 'committed to the team repo' sentence (`:27`), the commit checkbox and the `commit`/`team` arguments to `evalRun.start` (`:24`). Same for `desktop/src/app/eval-run-context`'s `commit` field and `run-eval.test.tsx`."

## M19 [medium] §7.4 :: LibraryScreen.tsx itself is never named, and it still renders a team-derived banner the spec says the Library must not carry
DETAIL: §7.4 changes `inventoryCard` and the `SkillCard` field list and gives the empty-state sentence, but names no screen file. `LibraryScreen.tsx` gates its Add-project affordance on `features.checkouts` and calls `backend.checkouts.add` — both renamed by §7.1 — and it renders `Team unreadable: … · cards show local state only` from `data.team`, plus a `provenance` string, on a screen §7.4 declares carries "no team-derived field at all". The same `backend.checkouts.add` + `features.checkouts` pair is the actual Add-project button in the sidebar, which §7.1 cites as the justification for the rename but never lists as a file to change.
EVIDENCE: desktop/src/screens/library/LibraryScreen.tsx:34 `const canAddProject=features?.checkouts===true;` · :41 `await action.run(()=>backend.checkouts.add(path));` · :29 `...data.team.kind==='unreadable'?<ErrorLine>Team unreadable: {data.team.message} · cards show local state only</ErrorLine>:null...` · desktop/src/backend/types.ts:79 `export interface Library {scanned:string[]|null;skills:SkillCard[];overview:...;title:string;provenance?:string|null;root:Root;team:LibraryTeam;problems?:...}` · desktop/src/components/domain/Sidebar.tsx:20 `function addCheckout(id:string){setErrorAt(id);void action.run(()=>backend.checkouts.add(id));}` · Sidebar.tsx:48 `if(!features?.checkouts)return null;`
PROPOSED FIX: Add to §7.4: "`[modify] desktop/src/screens/library/LibraryScreen.tsx` and `desktop/src/components/domain/Sidebar.tsx` — `features.checkouts` → `features.projects`, `backend.checkouts.add` → `backend.projects.add`; delete the `Team unreadable` banner (`LibraryScreen.tsx:29`) and drop `team: LibraryTeam` and `provenance` from the `Library` DTO (`desktop/src/backend/types.ts:79`) — under §7.4 the Library carries no team-derived field. The sidebar's `Projects` nav row currently links to `#/marketplace/projects` (`Sidebar.tsx:31`) while its children are local roots; re-point it at the Library."

## M20 [medium] §5.3 :: The desktop's PublishResult.version currently holds a PR URL, so §5.3's field deletions silently repurpose it
DETAIL: §5.3 says `prUrl`, `compareUrl`, `branch` and `policy` are "deleted from the DTO and from every reader" — but the desktop DTO's `version` field is not an independent field: the adapter populates it from `value.prUrl ?? value.branch ?? null`. Delete the two sources and `version` becomes permanently `null` unless the mapping is rewritten, which §5.3 does not say. Because the new CLI DTO also has a `version` key, a careless mapping compiles and typechecks while meaning the wrong thing. The `readers` phrasing also does not name where they are.
EVIDENCE: desktop/src/backend/types.ts:113 `export interface PublishResult {name:string;version:string|null;changed:boolean;prUrl:string|null}` (with the comment at :112 `/** \`prUrl\` is set only under \`policy.publish: 'pr'\` … */`) · desktop/src/backend/tauri/index.ts:822 `publish: (args: PublishArgs) => run(['publish', ...], cliPublish, (value): PublishResult => ({ name: value.name, version: value.prUrl ?? value.branch ?? null, changed: value.changed ?? true, prUrl: value.prUrl ?? null }), ['clone']),` · desktop/src/backend/tauri/index.ts:50 `const cliPublish = z.object({ name: z.string(), branch: z.string().nullable(), prUrl: z.string().nullable(), changed: z.boolean().optional() }).passthrough();`
PROPOSED FIX: Name the readers in §5.3: "`cliPublish` (`desktop/src/backend/tauri/index.ts:50`) becomes `{ name, version: z.string().nullable(), created: z.boolean(), identicalTo: z.string().nullable(), attachedEvals: z.number(), profileAdded: z.boolean(), project: z.string() }`; the mapping at `:822` maps `version` straight through — note it today reads `value.prUrl ?? value.branch`, so a straight rename is NOT a no-op. `PublishResult` (`desktop/src/backend/types.ts:113`) drops `prUrl` and gains `created`/`identicalTo`; the mock at `mock/index.ts:193` follows."

## M21 [medium] §7, §7.3 :: "1:1 copy of your local files" is never resolved against folders the scanner rejects
DETAIL: Three prompt sentences insist on a total mirror: "The library section should entirely be a reflection of your own local skills", "This library section should be a 1:1 copy of your local files", and "From that point on, it will track all the skills within that folder." The scanner classifies every folder as `candidate` / `rejected` / `failed`, and the count predicate admits a `rejected` entry only when its reason is in `FRONTMATTER_PROBLEMS` — so a folder with, say, an unreadable SKILL.md or a symlink is excluded from the Library's own count today. §7 never rules on whether a Finder-style mirror shows those folders (greyed, with the reason) or continues to hide them. This is the difference between "a mirror" and "a filtered view", and it is the single most literal sentence in the prompt.
EVIDENCE: src/lib/local-skills.ts:15-17 `export type Inspection = | { kind: 'candidate'; ... } | { kind: 'rejected'; reason: SourceProblem; detail: string; ... } | { kind: 'failed'; reason: string };` · src/lib/local-skills.ts:194 `const skillFolders = inventory.entries.filter(entry => entry.shared.length > 0 || entry.placement !== undefined || entry.inspection.kind === 'candidate' || (entry.inspection.kind === 'rejected' && FRONTMATTER_PROBLEMS.has(entry.inspection.reason))).length;`
PROPOSED FIX: Add to §7.3: "**Every direct child folder of a Library root is a card**, whatever its inspection state — a 1:1 mirror hides nothing. `candidate` renders normally; `rejected`/`failed` render with the name, the path, no description and the inspection reason as the card's body, and are excluded only from eval and publish actions. `localSkillCounts`'s predicate (`src/lib/local-skills.ts:194`) becomes a plain count of direct child directories."

## M22 [medium] §4.1, §8.4 :: `src/commands/ls.ts`'s skill row still carries a tree hash, an endorsement and an `unresolved` flag after §12 deletes their sources
DETAIL: §8.4 makes `ls --team` the marketplace's only skill read, but no section gives `ls.ts` a modify instruction beyond §7.2's `extraRoots` deletion. `LsSkill.latest` is a shortened tree hash, `endorsement` comes from `skillEndorsement` (deleted in §12), and `unresolved` means the `HEAD:skills/<name>` lookup failed — a concept that no longer exists. §11.5 gives exactly this treatment to `cliSearch`/`SearchHit` (the surface with NO production caller) and gives none to `cliLsSkill`/`LsSkill` (the surface the entire marketplace depends on).
EVIDENCE: src/commands/ls.ts:44 `export interface LsSkill { id: string; ...; latest: string; endorsement: string; ...; unresolved: boolean; receipt: LsReceipt | null; }` · src/commands/ls.ts:115 `skills.push({ ..., latest: shortHash(latest ?? '—'), endorsement: skillEndorsement(team, id), unresolved: latest === undefined, ... });`
PROPOSED FIX: Add to §8: "`[modify] src/commands/ls.ts` — `LsSkill.latest` becomes the `v<N>` folder of the highest version (never `shortHash`), `LsSkill` gains `versionCount: number` and the §8.1 card eval, and `endorsement` and `unresolved` are deleted from the row, from `formatSkill`'s printed output and from `cliLsSkill` (`desktop/src/backend/tauri/index.ts`). `LsResult` gains the `people[]` limb §8.4 assumes."

## M23 [low] §3.2, §5.1 :: "Next = highest existing + 1" has no stated value for the first publish
DETAIL: The prompt is explicit: "If the skill does not share a name with any other skill currently in the repository, it simply gets added as Version 1." §3.2 and §5.1 step 9 both say only "highest + 1, never count + 1", with no empty-array case; `listVersions` returns `[]` for a missing directory, so a literal reading gives `undefined + 1`. The intent is recoverable from two incidental phrases elsewhere (§5.1 step 5 "minted at v1", §6.4 "publish mints one at v1"), but it is nowhere in the rule itself.
EVIDENCE: Spec §3.2: "Ordinals may have gaps (a version folder deleted by hand). **Next = highest existing + 1, never count + 1.**" and "`listVersions` … ENOENT -> []" · Spec §5.1 step 9: "**Otherwise:** `N = highest + 1` (never `count + 1`)."
PROPOSED FIX: Amend both to: "Next = (highest existing ?? 0) + 1 — so a name with no version folder mints `v1`, per the prompt's 'it simply gets added as Version 1'." Add it to §14.1's publish test list as a named case.

## M24 [low] §11.2 :: The Settings ▸ Sharing nav row survives the deletion of its content pane
DETAIL: §11.2 deletes the `case 'sharing'` block in `SettingsContent.tsx` and says "Replace with a Publishing group", and names `SettingsDialogs.tsx:27` and `:104`. The section list that puts "Sharing" in the settings sidebar lives in a third file that is not named, so an implementer who follows §11.2 literally leaves a nav row labelled Sharing pointing at a pane now called Publishing — or, if they delete the case without the row, at nothing.
EVIDENCE: desktop/src/screens/settings/settings-data.ts:5 `['account','Account','user'],['teams','Team','users'],['machine','This machine','laptop'],['sync','Sync','refresh'],['updates','Updates','arrow-up-circle'],['inbox','Inbox','inbox'],['evals','Evals','flask'],['sharing','Sharing','upload'],['appearance','Appearance','sun'],...`
PROPOSED FIX: Add to §11.2: "`[modify] desktop/src/screens/settings/settings-data.ts:5` — the `['sharing','Sharing','upload']` row becomes `['publishing','Publishing','upload']`, and the `'sync'` row's pane is re-described as fetch-only per §10. `settings.test.tsx`'s section-list assertions move in the same commit."

## M25 [low] §11, §14 :: Four screen directories are named nowhere in the spec; two of them change
DETAIL: Enumerating `desktop/src/screens`: **frame/** (FrameScreen.tsx) — never mentioned; it is a 3-line shell and genuinely unaffected, fine to omit. **share/** (ShareScreen.tsx, ShareInvite.tsx) — never mentioned; it drives `useSyncAction` and offers "Sync now" as the recovery action for a roster read failure, so §10's re-definition of sync (fetch-only, places nothing) changes what that button does and what its copy should say. **marketplace/** — the algorithms are specified in §8 but not one of its six files is named (see the AddSkillsDialog finding). **onboarding/** — only `SetupBoot.tsx` is named in §9.2; `OnboardingScreen.tsx`, `OnboardingParts.tsx` and `onboarding-store.ts` render and store the step list §9.2 reorders. Named and covered: library (§7.4, partially), settings (§11.2), skill (§11.4), search (§11.5), inbox (§11.5, as an open ruling).
EVIDENCE: desktop/src/screens/share/ShareScreen.tsx:21 `const syncAction=useSyncAction();` · :45 `...primary="Sync now" secondary="Open settings" onPrimary={syncAction.open}...` · desktop/src/screens/frame/FrameScreen.tsx:3 `export function FrameScreen(){return <Shell selected="Global"><ScreenFrame/></Shell>;}`
PROPOSED FIX: Add a one-line disposition table to §11 covering all nine screen directories, with an explicit "unaffected" for `frame/` and `search/` and a named change for `share/` ("the Sync-now recovery action keeps working but its hover copy becomes *Fetch the team repo* — it no longer places anything"), `marketplace/` and `onboarding/`. A reviewer should be able to read one table and know every screen was considered.

## M26 [low] §9.1, §3.5 :: Uninstall's relationship to installed[] and profile[] is never stated
DETAIL: `uninstall` is listed in §4.2's new `PEOPLE_ACTIONS` and cited in §3.5 for its per-scope bookkeeping, but no section says what it does to the two lists after the refactor. The prompt's "A user's profile can only contain skills from the marketplace they have explicitly added" leaves it open whether uninstalling a profiled skill removes the profile entry; §3.5's "Removing from a profile never touches disk" answers the converse direction only. Ambiguity here produces two plausible implementations and a people file that disagrees with the machine.
EVIDENCE: Spec §4.2: "`PEOPLE_ACTIONS` becomes `['join','install','uninstall','profile','publish']`" · Spec §3.5: "`installed[]` — automatic, written by `install`, one entry per id. **`scope` is kept.** It is already there, `uninstall`'s per-scope bookkeeping reads it (`src/commands/uninstall.ts:197-210`)" · src/lib/guard.ts:36 `const PEOPLE_ACTIONS: readonly GuardAction[] = ['join', 'install', 'uninstall', 'sync', 'profile', 'decline'];`
PROPOSED FIX: Add to §9.1: "`uninstall` removes the `installed[]` entry for the last scope removed, exactly as today, and **leaves `profile[]` untouched** — a profile is a curated statement about a skill, not a claim that it is on this machine; only `profile --remove` empties it. State this in the uninstall notice so the user is not surprised to still see it on their profile."

## M27 [medium] §13.1b :: The un-upgraded-teammate hazard is already closed by `z.literal(2)`, and the mitigation §13.1b prescribes cannot fire on a migrated repo
DETAIL: I traced the chain the spec asserts. Two of its four links do not hold. (1) "`skillRecords` treats a missing root as an empty team (`src/lib/skills.ts:28-29`)" — on a migrated repo `skills/` still EXISTS; the empty result comes from the per-name `readFile(skills/<name>/SKILL.md)` throwing ENOENT into the per-skill catch at :39-41. So the prescribed fix — "make `skillRecords` distinguish 'the skills root is absent' from 'the team has no skills', and have every identity-bearing caller refuse on the former" — never triggers on the case it was written for. (2) "`connectOne` mints a fresh UUID and overwrites the user's own `SKILL.md`" — it cannot. `connectOne` parses the clone's `team.json` with `teamSchema` at :223, one line after minting the id and twenty lines BEFORE the local write at :243, and `layout_version: z.literal(2)` rejects a layout-3 document. The candidate is skipped with a zod error; nothing local is touched and nothing is pushed. I checked the neighbouring paths for the same window and they also fail closed: `reconcileShared` reports "Repository copy … is missing" and defers, and sync's placement loop hits `if (!skill) { notice('Blocked …: its skill is no longer in the repository.'); continue; }` — no quarantine, no delete. The §16.1 "hard ordering constraint between releases" rests on a hazard that does not exist; if it is kept (and it should be, on general grounds) the spec should say why, because an implementer told to build the `skillRecords` root-absent distinction will build a guard that protects nothing.
EVIDENCE: src/lib/skills.ts:29 `catch (error) { if (isMissing(error)) return []; throw error; }` (root-absent path — not the one that fires); src/lib/skills.ts:40 `options.onProblem?.({ name, message: … });` (the path that actually fires, per-skill); src/commands/connect.ts:222-223 `id = randomUUID(); // minted before safeWrite…` / `const teamDoc = parseJson(teamSchema, await readFile(join(clone, 'team.json'), 'utf8'), 'team.json');`; src/commands/connect.ts:242-243 `phase = 'source-mutated';` / `await writeFile(join(source, 'SKILL.md'), updated, 'utf8');`; src/lib/schema.ts:54 `layout_version: z.literal(2),`; src/commands/sync.ts:415 `if (!skill) { notice(\`Blocked ${path}: its skill is no longer in the repository.\`); continue; }`
PROPOSED FIX: Rewrite §13.1b as: "**(b) An un-upgraded teammate fails closed today — keep it that way.** On a migrated repo an old CLI's auto-share reaches `connect.ts:223`, where `teamSchema`'s `layout_version: z.literal(2)` rejects a layout-3 `team.json` before the local `SKILL.md` write at `:243`; the candidate is skipped and nothing is uploaded or rewritten. The ordering constraint below is therefore prudence, not a fix for an open hole. **The invariant to protect is the literal:** `layout_version` must remain a value an older CLI cannot parse, so a layout-3 `team.json` can never be read as a layout-2 one. Do NOT build the 'skills root absent vs. empty team' distinction as the mitigation — on a migrated repo the root is present and that branch never runs; the empty result comes from the per-skill catch at `skills.ts:40`. If `skillRecords` is changed at all, the useful change is to make a skill whose folder holds no `v<N>` an `onProblem` the caller can count, so a layout-3 CLI on a layout-2 repo reports 'every skill unreadable' instead of 'no skills'."

## M28 [medium] §10 / §10.1 :: Deleting sync.ts strands `config.pending` — nothing else replays or clears it
DETAIL: §10.1 is an exhaustive disposition table for "everything sync does today that goes", and the pending replay is not in it. `install` and `uninstall` each push a pending entry before they touch disk and clear their own on success; the ONLY code that replays or clears an entry left behind by an interrupted run lives in sync.ts. After §10 deletes those 802 lines, a user who Ctrl-Cs an install, or whose machine sleeps mid-place, carries that entry forever: `status` keeps reporting it, `uninstall --machine` refuses to remove `config.json` while `c.pending.length !== 0`, and the half-placed folder is never completed or rolled back. The spec is aware pending exists — §3.4's preprocess explicitly maps versions "in `pending`" — which makes the omission from §10.1 look like an oversight rather than a decision.
EVIDENCE: src/commands/sync.ts:252 `for (const pending of current.pending.filter((entry) => entry.team === team)) {` (the sole replayer); src/commands/install.ts:108 `await input.store.update((fresh) => { const existing = fresh.pending.find(…); if (existing) existing.version = latest; else fresh.pending.push(pending); });`; src/commands/install.ts:162 `fresh.pending = fresh.pending.filter((entry) => !samePending(entry, pending));` (success path only); src/commands/uninstallMachine.ts:135 `const removal = await store.remove((c) => … && c.pending.length === 0 && …);`
PROPOSED FIX: Add a row to §10.1's table: "| Pending replay (`:245-300`) | **Move** to `install.ts`/`uninstall.ts`: each verb replays its own team's stale entries at start (an entry older than the current run is finished or dropped), and `prune` gains a `--pending` mode that clears entries whose destination no longer exists. Deleting sync without rehoming this strands every interrupted install on the machine forever and wedges `uninstall --machine` at `uninstallMachine.ts:135`. |"

## M29 [medium] §9.2 / §11.1 :: §9.2's new step order silently deletes setup's `hook` and `wrapper` steps, and no upgrade path refreshes the rewritten bundled skill
DETAIL: §9.2 says "replace the `discover` step with `projects` in the same ordinal slot" and then gives the order as `welcome, app, role, github, team, actions, invite, projects, evals, community, done` — eleven steps. Today's `Step` union has thirteen, and the two the list drops are `hook` (offers the session hook) and `wrapper` (offers and refreshes the bundled `/terum-skills` Claude Code skill). An implementer who takes the enumerated order as the spec — which is how §9.2 reads, since it hands the same list to four fixtures that must "move in the same commit" — ships a setup that no longer offers the session hook and no longer places the operator manual, neither of which is in §12's deletion list. This compounds §11.1: `wrapper` is the ONLY code path that refreshes an already-placed `~/.claude/skills/terum-skills/SKILL.md`. §11.1 rewrites that file end to end but never says how the rewrite reaches a machine that already has the old one, so an existing user's Claude Code session keeps being told to run `connect`, `checkout add|remove|list` and `sync --prune` — all deleted verbs — until they happen to re-run setup.
EVIDENCE: src/commands/setup.ts:76 `type Step = 'welcome' | 'app' | 'role' | 'github' | 'team' | 'actions' | 'invite' | 'discover' | 'evals' | 'community' | 'hook' | 'wrapper' | 'done';`; src/lib/wrapper.ts:12-20 `…is placed under the user's global Claude Code skills root by `setup`… one offer with its own y/N, an idempotency key…, an in-place refresh of its own copy, removal on machine uninstall`; src/lib/wrapper.ts:24 `export const BUNDLED_WRAPPER = join(packageRoot() ?? …, 'dist', 'claude', 'skills', 'terum-skills', 'SKILL.md');`
PROPOSED FIX: Correct the order in §9.2 to keep every existing step: "Order becomes `welcome, app, role, github, team, actions, invite, projects, evals, community, hook, wrapper, done` — `projects` replaces `discover` in place; `hook` and `wrapper` are unchanged and are NOT deleted." And add to §11.1: "The rewritten SKILL.md reaches an existing machine only through setup's `wrapper` step, which refreshes its own managed copy in place (`wrapper.ts:12-20`). Add a one-time refresh on the first run of the new CLI: when `inspectWrapper` reports `managed` and the placed copy's body differs from `BUNDLED_WRAPPER`, rewrite it. Without this, every existing user's Claude Code session keeps driving `connect`, `checkout add` and `sync --prune`."

## M30 [medium] §3.4 / §13 :: Nulling 40-hex versions on read makes the next people-file write commit `null` for every pre-upgrade install, and §13 never re-keys `people/*.json`
DETAIL: §3.4's preprocess is presented as one object-level `z.preprocess` under a heading entirely about `configSchema` and `ConfigStore.read()`, but the fields it must cover span two schemas: `placements`/`pending` are in `configSchema` (local, private) while `installedSchema` is inside `personSchema`, which parses committed team-repo files. Two consequences the spec does not state. (1) If the implementer preprocesses only `configSchema`, every committed `people/<handle>.json` with 40-hex `installed[].version` becomes unparseable, and `regenerateReadmeInTree` re-parses every people file inside `safeWrite` and throws — so every write verb on a generic-git remote fails until each member's file is hand-edited. (2) If the preprocess IS applied to `personSchema`, every pre-upgrade install's recorded version is mapped to `null` on read, and the next verb that rewrites the file serializes the whole person object — committing `null` over provenance the repo still holds. §13 migrates `team.json` and the receipts but never touches `people/*.json`, even though the migration is the one moment that knows the tree-hash→`v1` mapping and could re-key them correctly.
EVIDENCE: src/lib/schema.ts:47-52 `export const installedSchema = z.object({ id: skillIdSchema, version: z.string().length(40).nullable(), … })` — referenced from `personSchema`, not `configSchema`; src/lib/readme.ts:173-177 `const people = tree.paths().filter(…).map((path) => { … return parseJson(personSchema, asText(value), path); });` (throws inside safeWrite); src/commands/uninstall.ts:210 `tree.set(path, \`${JSON.stringify({ ...person, installed, declined }, null, 2)}\n\`);` (writes back the whole preprocessed object)
PROPOSED FIX: Split §3.4's preprocess paragraph in two and add a migration step: "**Two preprocesses, not one.** `configSchema` gets one (for `placements` and `pending`); `personSchema` gets its own (for `installed[]`). They cannot share a `z.preprocess` — `installedSchema` is reached through `personSchema`, which parses committed team-repo files inside `safeWrite` (`readme.ts:173-177`), and a people file that fails to parse there fails every write verb." And add §13 step 3b: "Re-key `people/<handle>.json`: every `installed[].version` equal to the tree hash `HEAD:skills/<name>` resolved to at migration time becomes `'v1'`; every other 40-hex value becomes `null`. Doing it here is the only chance — after migration nothing can recover which version a 40-hex value named, and the first `uninstall` or `install` would commit `null` over it (`uninstall.ts:210`)."

## M31 [low] §12 / §7.4 :: `config.shared` has two readers outside the deletion list: `canonicalLedger` and `ls --local`'s state column
DETAIL: §12 deletes `config.shared`, and §11.3 names `leave.ts` and `uninstallMachine.ts` as the readers to fix. Two more exist and are named nowhere. `canonicalLedger(config)` takes `Pick<Config, 'shared' | 'placements'>` and builds `sharedPaths` from it; it is called by `ls --local`, by `localSkills` (which attaches a `shared` ref list to every `LocalEntry`), and by `autoShareRoots`. `ls --local`'s per-row state column is computed from `entry.shared`. §7.4 only covers the desktop's `inventoryCard`; the CLI's own `ls --local` output shape is not in any change list, and `ls` is a `SERVE_READ_VERBS` member, so a type error here also breaks the desktop's read path.
EVIDENCE: src/lib/local-skills.ts (origin/main) `export async function canonicalLedger(config: Pick<Config, 'shared' | 'placements'>) { const [sharedPaths, placementPaths] = await Promise.all([ Promise.all(Object.entries(config.shared).map(…)), … ]); }`; src/commands/ls.ts:186-189 (origin/main) `const extraRoots = (await Promise.all([ ...Object.values(config.shared).map(ref => nearestRepoRoot(dirname(ref.source))), … ]))`; src/lib/serve-verbs.ts `export const SERVE_READ_VERBS: readonly string[] = Object.freeze(['status', 'ls', 'eval-report', 'search', 'validate', 'update']);`
PROPOSED FIX: Add to §12's deletion list, inline with `config.shared`: "— and its remaining readers: `canonicalLedger`'s `sharedPaths` limb and `Pick<Config,'shared'|'placements'>` signature (`src/lib/local-skills.ts`), `localSkills`'s `LocalEntry.shared` field, and `ls --local`'s per-row state column derived from it (`src/commands/ls.ts:186-189`). `ls` is in `SERVE_READ_VERBS`, so leaving these behind breaks the desktop read path as well as the CLI."

## M32 [medium] §1.5 / §7.1 :: SERVE_READ_VERBS needs no additions — but the spec must say so and must rule `project` out, because serve gates on argv[0] only
DETAIL: You asked whether SERVE_READ_VERBS needs edits the spec does not name. Answer: no additions are required (§7.5 already rules the three D6 modes out, and `ls`/`status`/`eval-report` stay in the list). But §1.5's rule — "Any new read verb must be added there; no write verb may ever be" — cannot be honoured for L-PROJ, and the spec does not notice. `serve` matches on the first argv element alone, so admitting the read `project list` would admit `project add` and `project remove` — writes sharing the clone writer lock, the shutdown hooks and a process-global cwd — into the long-lived read session. The same trap applies to `checkout discover` today (§7.1 makes `discover` a pure read and a natural candidate for the fast path). Left unstated, an implementer chasing the sub-second read budget adds `'project'` and silently opens the write path.
EVIDENCE: /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor (origin/main) src/lib/serve-verbs.ts:15 `export const SERVE_READ_VERBS: readonly string[] = Object.freeze(['status', 'ls', 'eval-report', 'search', 'validate', 'update']);` · src/commands/serve.ts:66 `if (!SERVE_READ_VERBS.includes(request.argv[0] ?? '')) { report({ verb, ok: false, error: \`serve does not run ${verb}; spawn it as its own process\`, exitCode: 1 }); }`
PROPOSED FIX: Add to §7.1: "**`SERVE_READ_VERBS` is not edited by this refactor.** `serve` gates on `argv[0]` alone (src/commands/serve.ts:66), so adding `'project'` would admit `project add` and `project remove` — writes — onto the shared read session. `project list` and `project discover` therefore spawn their own process like every other write-adjacent verb. If a fast local-project read is ever wanted, it must be a distinct top-level verb name, not a mode of `project`." State the same for `checkout discover` → `project discover`.

## M33 [medium] §8.1 / §8.2 :: `selectCardEval` and `evalVersionLabel` are specified as algorithms with no home file, and the test §14.1 names belongs to a file no milestone modifies
DETAIL: §8.1 gives an exact algorithm and §8.2 an exact label contract, but neither says where the code lives. `selectCardEval` takes a `clone` and does readdirs, so it is CLI-side, and §14.1's test is `src/commands/__tests__/ls-receipt.test.ts` — implying `src/commands/ls.ts` — yet §8 lists no `[modify] src/commands/ls.ts` and M5's file list is empty except for an assertion about `catalog()`. `evalVersionLabel(card)` is a desktop render concern and `desktop/src/components/domain/SkillCard.tsx` is never named. §8.2's geometry claim is also optimistic: the bottom-left group is `min-width:0;overflow:hidden` inside a fixed `height:20px` row whose chips are `flex-shrink:0`, so a new text node competes for width with the existing chips rather than being free, and §14 separately requires any new board in `desktop/FIDELITY.md` and the 91-row assertion — but §8.2 names no board.
EVIDENCE: /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/src/commands/ls.ts:115 (`listSkills` builds the card's `receipt` limb today) · /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/desktop/src/components/domain/SkillCard.css:1 `.skill-card{…height:148px;…}.skill-card-bottom{display:flex;align-items:center;justify-content:space-between;gap:6px;height:20px}.skill-card-bottom>div{display:flex;align-items:center;gap:6px;min-width:0;overflow:hidden}…>div>.chip{flex-shrink:0}` · /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/desktop/e2e/fidelity/__tests__/boards.test.ts:9 `expect(BOARDS).toHaveLength(91);`
PROPOSED FIX: Add to §8: "`[new] src/lib/evals/receipt-store.ts` holds `selectCardEval` (alongside the `receiptFiles`/`newestReceiptAt` §12 moves there). `[modify] src/commands/ls.ts` — `listSkills` (:81-115) calls it and emits `receipt`, `evalVersion: number|null`, `latestVersion: number`, `latestEvalState` on each `LsSkill`. `[modify] desktop/src/backend/tauri/index.ts` — `cliLsSkill` mirrors the three new fields; `inventoryCard` maps them onto the card. `[new] evalVersionLabel` in `desktop/src/components/domain/presentation.ts`, rendered by `SkillCard.tsx` in the `.skill-card-bottom` left group." And add to §14: one new fidelity board, 'Marketplace · stale eval chip', landing in `desktop/FIDELITY.md` and bumping `boards.test.ts:9` from 91 to 92 in the same commit, marked in-progress until re-rendered on Ryan's Mac.

## M34 [medium] §7.1 / §7.2 :: The L-PROJ rename collides with three declarations the table does not list: `Surfaces.checkouts`, `FRAME_FEATURES`, and the existing `backend.projects`
DETAIL: §7.1 correctly flags that the two feature keys must not collide, but the rename touches more declarations than the table names. `FEATURE_KEYS` already contains BOTH `checkouts` and `projects` (so `checkouts`→`projects` requires `projects`→`teamProjects` first, in the same edit); `Surfaces` carries a separate `checkouts:boolean` set in two adapters and read in two components; `FRAME_FEATURES` declares both on the CLI side; and `Backend.projects` already exists meaning team projects. Also missed: the mock backend's `checkouts` service, and §7.2's `localSkillRoots(home, projects)` drops `cwd` but leaves the 4th parameter `extraRoots` unaccounted for even though its only producer (ls.ts:187-190) is deleted.
EVIDENCE: /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/desktop/src/backend/types.ts:12 `export const FEATURE_KEYS = [… 'checkouts','projects','refresh','discover','appUpdate','autoSync'] as const;` · :15 `export interface Surfaces {checkouts:boolean;divergence:boolean;…}` · /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/src/lib/frames.ts:46 `checkouts: true, projects: true,` · /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/desktop/src/backend/Backend.ts:22 `checkouts: { add(path:string):Run<CheckoutAdded>; remove(path:string):Run<CheckoutRemoved>; discover(args:DiscoverArgs):Run<DiscoverResult> };` and :24 `projects: { create(args:{name:string;remote?:string}):Run<ProjectCreated> };` · /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/desktop/src/backend/mock/index.ts:121 `checkouts:{add:…,remove:…,discover:…}`
PROPOSED FIX: Extend §7.1's table with a 'Declarations renamed in one commit' row listing all of them: `FEATURE_KEYS` (types.ts:12) — rename `projects`→`teamProjects` FIRST, then `checkouts`→`projects`; `Surfaces.checkouts`→`Surfaces.projects` (types.ts:15) and its two producers (tauri/index.ts:628, mock/index.ts:115) and two consumers (Sidebar.tsx:48, SettingsContent.tsx:83); `FRAME_FEATURES` (src/lib/frames.ts:46) the same pair; `FRAME_VERBS` (frames.ts:32) `checkout add|remove|list|discover` → `project add|remove|list|discover` and `project create` → `team project create`; `Backend.checkouts`→`Backend.projects`, `Backend.projects`→`Backend.teamProjects` (Backend.ts:22,:24) plus both adapters (tauri/index.ts:701, mock/index.ts:121); `CheckoutAdded`/`CheckoutRemoved` → `ProjectAdded`/`ProjectRemoved` (types.ts:75,:78) and their zod mirrors. Also state that `localSkillRoots` drops both `cwd` and `extraRoots`.

## M35 [medium] §9.1 :: Deleting install.ts:255 also changes non-interactive install, which the spec does not intend
DETAIL: §9.1 says "Delete `if (!roots.length) return { kind: 'global' }` (`:255`) — an interactive install always asks." The line sits ABOVE the non-interactive guard, so deleting it changes headless behaviour too: an install on a machine with no registered projects currently falls through to Global and afterwards throws 'Pass --into global or --into <checkout root>'. That breaks scripted installs and the `install` path the desktop drives when no project has been added — a regression the spec's own sentence does not ask for.
EVIDENCE: /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/src/commands/install.ts:254-256 `const roots = […]; if (!roots.length) return { kind: 'global' }; if (!interactive) throw new Error('Pass --into global or --into <checkout root>');`
PROPOSED FIX: Change §9.1 to: "Move the zero-roots shortcut below the interactivity guard — `if (!interactive) { if (!roots.length) return { kind: 'global' }; throw new Error('Pass --into global or --into <project root>'); }` — so a non-interactive install with no registered projects still installs globally while an interactive one always asks."

## M36 [medium] §4.2 (row a″) :: Row a″ silently drops row h's 'the skill exists' precondition and names no layout-3 replacement
DETAIL: Today's row h requires both append-only semantics AND that the skill already exists (`tree.before('skills/<name>/SKILL.md') !== undefined`). §4.2's row a″ specifies only the path regex, `add-or-replace`, and `'publish'` only. Two readings: keep the existence check with its layout-2 path (in which case every publish's eval-asset write is refused, because `skills/<name>/SKILL.md` never exists under layout 3), or drop it (in which case a publish can create `skills/<anything>/evals/cases/x.yaml` for a skill that does not exist). Neither is stated.
EVIDENCE: /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/src/lib/guard.ts:93-98 `function permitsEvalAsset(tree: GuardTree, path: string): boolean { const match = EVAL_ASSET_PATH.exec(path); if (!match) return false; if (tree.before(path) !== undefined || tree.after(path) === undefined) return false; return tree.before(\`skills/${match[1]!}/SKILL.md\`) !== undefined; }`
PROPOSED FIX: State row a″ in full: "`/^skills\/([^/]+)\/evals\/(?:cases\/[^/]+\.ya?ml|triggers\.yaml)$/` under `'publish'` only; `tree.after(path) !== undefined` (add or replace, never remove); and the skill must exist in the POST-image — `tree.paths(\`skills/${name}/\`).some(p => /^skills\/[^/]+\/v[1-9][0-9]*\/SKILL\.md$/.test(p))` — which admits the first publish of a brand-new skill (whose v1 folder is in the same mutation) and refuses eval assets for a name with no version folder at all." Add both cases to §14.1's guard test list.

## M37 [medium] §6.5 :: The eval-queue change names neither `queueKey` nor a migration, and `readEvalQueue` rethrows on a stale file
DETAIL: §6.5 changes `EvalQueueItem` on three declarations and says "`src/lib/evals/queue.ts`'s `itemSchema` changes to match". Two things it does not name. First, `queueKey` — the dedupe identity used by `enqueueEvals`/`dequeueEvals` — is `JSON.stringify([team, skill, version])`; with `team` optional and `version` gone it must be re-derived (presumably `[skill, contentHash]`), and leaving it produces `undefined` keys that collapse every queued item into one. Second, `readEvalQueue` treats any non-ENOENT failure as fatal and rethrows, so an existing `run/eval-queue.json` holding 40-hex items throws on the first read after upgrade and the drainer dies — the same 'brick on read' class §3.4 is careful about for `config.json`.
EVIDENCE: /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/src/lib/evals/queue.ts:18 `export const queueKey = (item: EvalQueueItem): string => JSON.stringify([item.team, item.skill, item.version]);` · :21-26 `export async function readEvalQueue(root: string): Promise<EvalQueue> { try { return queueSchema.parse(JSON.parse(await readFile(queuePath(root), 'utf8'))); } catch (error) { if ((error as NodeJS.ErrnoException).code === 'ENOENT') return { schema: 1, items: [] }; throw error; } }`
PROPOSED FIX: Add to §6.5: "`queueKey` becomes `JSON.stringify([item.skill, item.contentHash])`. `queueSchema.schema` becomes `z.union([z.literal(1), z.literal(2)])` and `readEvalQueue` drops any item that does not parse under the v2 item schema rather than throwing — a stale queue is discardable state, not data loss, and a hard throw takes the whole drainer down. Write `schema: 2` on the next `updateEvalQueue`." Add a test: a `run/eval-queue.json` full of 40-hex items reads back as an empty queue and the next enqueue writes schema 2.

## M38 [medium] §12 :: `decline` is deleted on the CLI side only; six desktop declarations survive, and the `CliVerbs.decline` the spec points at does not exist in this tree
DETAIL: §12 deletes "`decline` (verb + `src/commands/decline.ts` + `CliVerbs.decline`)". `CliVerbs` appears nowhere in `src/` or `desktop/` at faa402c — only in `.planning/` documents — so that half of the instruction cannot be executed as written, while the six real declarations are unnamed: `Backend.decline`, the mock, the adapter and its zod schema, `FRAME_VERBS`, and a live caller in the Inbox. §11.5 does propose deleting the Inbox's `update`/`review` item kinds but does not connect that to the `decline` deletion, and §11.5 explicitly leaves the Inbox ruling open.
EVIDENCE: /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/desktop/src/backend/Backend.ts:36 `decline(args: { ref: string }): Run<{ id: string }>;` · /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/desktop/src/backend/tauri/index.ts:42 `const cliDecline = z.object({ handle: z.string(), id: z.string(), declined: z.literal(true) });` · :821 `decline: args => run(['decline', '--', args.ref], cliDecline, value => ({ id: value.id }), ['clone']),` · /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/desktop/src/backend/mock/index.ts:192 `decline:args=>long('share',async()=>{…})` · /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/src/lib/frames.ts:32 `… 'profile', 'decline', 'refresh', …` · /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/desktop/src/screens/inbox/InboxScreen.tsx:66 `else if (label === 'Decline') { const result = await driveRun(backend.decline({ ref: item.skillRef }), …) }`
PROPOSED FIX: Replace the §12 clause with: "`decline` — the verb, `src/commands/decline.ts`, its registration in `src/cli.ts`, its entry in `FRAME_VERBS` (src/lib/frames.ts:32), `Backend.decline` (Backend.ts:36), the mock (mock/index.ts:192), the adapter and `cliDecline` (tauri/index.ts:42, :821), and the Inbox's Decline branch (InboxScreen.tsx:66) — all in one change. `personSchema.declined` is retained per §3.5." Drop the `CliVerbs` reference (no such symbol exists at this base) or say which file it means. Roll §11.5's Inbox ruling into the same milestone so the Decline branch has a decided home.

## M39 [medium] §12 / §3.6 :: `config.shared` has three readers beyond leave.ts and uninstallMachine.ts, and `emptyConfig` declares it
DETAIL: §11.3 carefully rules on `leave.ts` and `uninstallMachine.ts`, but three more places read the field and are not named. `librarySize` takes `Pick<Config,'shared'|'placements'|'checkouts'>` — §12 deletes only its "sync call site", so the function and its signature survive and stop compiling; §3.5 in fact keeps `local_skills` alive and moves the write to publish/install/profile, so `librarySize` needs a new shape, not deletion. `canonicalLedger` takes `Pick<Config,'shared'|'placements'>` and is consumed by connect's auto-share (going) but its `sharedPaths` limb has other readers. And `emptyConfig()` constructs `shared: {}`, so the required key must come out of the schema and the constructor together.
EVIDENCE: /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/src/lib/local-skills.ts:205 `export async function librarySize(home: string, config: Pick<Config, 'shared' | 'placements' | 'checkouts'>, stateRoot: string): Promise<number | null> {` · :196 `export async function canonicalLedger(config: Pick<Config, 'shared' | 'placements'>) { const [sharedPaths, placementPaths] = await Promise.all([Promise.all(Object.entries(config.shared)…` · /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/src/lib/schema.ts:132 `shared: z.record(z.string(), z.object({ source: z.string(), team: z.string(), baseline: z.string().optional() }).passthrough()),` · :139 `export const emptyConfig = (): Config => ({ teams: {}, shared: {}, approvals: {}, pending: [], placements: {} });`
PROPOSED FIX: Extend §12's `config.shared` entry to: "…plus `configSchema.shared` (schema.ts:132) and `emptyConfig` (:139); `librarySize` (local-skills.ts:205) narrows to `Pick<Config,'placements'|'projects'>` and keeps its callers per §3.5; `canonicalLedger` (:196) loses its `sharedPaths` limb and its `shared` parameter, or is deleted with its last caller if connect was the only one." Note that existing `config.json` files keep the stale `shared` key inertly under `.passthrough()`, exactly as §3.6 says for `checkouts`.

## M40 [medium] §10.2 :: `ChangeSource` is declared in types.ts, not invalidation.ts, and no emit site is re-pointed to `'marketplace'`
DETAIL: §10.2 says "`[rewrite] desktop/src/app/invalidation.ts` — add a `'marketplace'` `ChangeSource`." The union lives in `desktop/src/backend/types.ts:140`; invalidation.ts only holds the `Record<ChangeSource, readonly string[]>` map, which is exhaustive and so forces the map entry once the union changes. More importantly, sources are EMITTED per verb as the fourth argument to the adapter's `run(...)` helper, and §10.2 names no emit site — so adding the union member changes nothing observable until `sync`'s `['clone']` becomes `['marketplace']`. The mock backend's `listeners` calls are a second emit surface.
EVIDENCE: /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/desktop/src/backend/types.ts:140 `export type ChangeSource='config'|'clone'|'placed'|'stamp';` · /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/desktop/src/app/invalidation.ts:4-9 `const prefixes: Record<ChangeSource, readonly string[]> = { config: […], clone: ['library','skill','catalog','roster','inbox','receipts','status'], placed: […], stamp: […] };` · /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/desktop/src/backend/tauri/index.ts:821 `decline: args => run(['decline','--',args.ref], cliDecline, value => ({id:value.id}), ['clone']),` (the `['clone']` fourth argument is the emit)
PROPOSED FIX: Rewrite §10.2's second bullet as: "`[modify] desktop/src/backend/types.ts:140` — `ChangeSource` gains `'marketplace'`. `[modify] desktop/src/app/invalidation.ts:4` — add `marketplace: ['catalog','skill','roster','status']` and remove `'library'` from `clone`. `[modify] desktop/src/backend/tauri/index.ts` — the `sync`/`refresh` adapter entry emits `['marketplace']` instead of `['clone']`; mirror it in `desktop/src/backend/mock/index.ts`."

## M41 [medium] §3.4 / §9.1 :: Version strings reach two GitHub-facing strings that break when `v4` replaces a tree hash
DETAIL: `version_full` feeds a raw GitHub tree URL and a copyable install command, both built from what used to be a 40-hex tree. With `v4` the tree URL becomes `<repo>/tree/v4/skills/<name>/` — `v4` is not a git ref, so the link 404s — and the share command becomes `install <repo>/<name>@v4`, which §9.1 explicitly makes the CLI refuse. §3.4 changes the persisted type in five declarations and §2's D1 mentions this URL only to justify lowercase; no milestone instructs either string to change.
EVIDENCE: /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/desktop/src/backend/tauri/index.ts:213 `return { version: version?.slice(0, 12) ?? '—', version_full: version, shareCommand: repo ? \`npx -y terum-skills@latest install ${repo}/${name}${version ? '@' + version.slice(0, 12) : ''}\` : '—' };` · /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/desktop/src/screens/skill/SkillScreen.tsx:28 `…url=repoUrl?\`${repoUrl}/tree/${s.version_full??'HEAD'}/skills/${s.name}/\`:null…`
PROPOSED FIX: Add a bullet to §8 or §9.1: "`detailVersionFields` (tauri/index.ts:212) drops the `@<version>` suffix from `shareCommand` (install now always takes the latest) and `version` stops being sliced — `versionLabel(n)` is the only UI form. `SkillScreen.tsx:28`'s repo link becomes `${repoUrl}/tree/HEAD/skills/${encodeURIComponent(s.name)}/${s.version_full ?? ''}` so it points at the immutable version folder on the default branch, with `encodeURIComponent` as D1 notes."

## M42 [low] §11.4 :: Run eval is already gated on a feature flag, so "with no gate" mis-describes the starting point
DETAIL: §11.4 says `cardActions` "offers **Run eval** with no gate". It is gated on `runEvalInApp`; what it lacks is the `skill.path` gate §11.4 wants. Minor, but an implementer reading the sentence may remove the existing flag check and re-enable the row in shells whose CLI cannot run evals in-app.
EVIDENCE: /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/desktop/src/components/domain/skill-card-actions.ts:23 `if(runEvalInApp)actions.push({key:'run-eval',label:'Run eval',to:at('tab=evals','dialog=run-eval'),reason:null});`
PROPOSED FIX: Reword to: "**Run eval** is gated on the `runEvalInApp` feature flag but not on whether the skill is on this machine. Keep the flag gate and add: `reason = skill.path ? null : 'Install it first — evals run against the copy on your machine.'`, rendered as a disabled row rather than a hidden one, so the menu keeps its fixed shape (the file's own convention)."

## M43 [low] §5.1 step 7 :: The identical-version scan re-reads every byte of every prior version synchronously, once per safeWrite attempt, and the spec bounds nothing
DETAIL: The mutation handed to `safeWrite` is re-run from scratch on every retry attempt, and `makeTree`'s `before()` is `readFileSync` with a cache that is rebuilt per attempt. §5.1 step 7 asks it to digest EVERY existing version of the skill. A skill on its twentieth publish with fifty files means ~1,000 synchronous reads per attempt, on the event loop, inside the clone writer lock. §3.1 forbids a per-version manifest (correctly — it would break byte identity), so there is no cheap shortcut, but the spec should at least name the shape and the bound.
EVIDENCE: /Users/ryanliu/Documents/Terum/skill-management-software-wt-refactor/src/lib/teamRepo.ts:307-311 `const before = (path: string): string | Buffer | undefined => { if (!tracked.has(path)) return undefined; let content = cache.get(path); if (content === undefined) { content = readFileSync(join(root, path)); cache.set(path, content); } return content; };` · :193-195 `const tree = makeTree(root, tracked, executable); const returned = mutate(tree);` (inside the retry `while` loop at :121)
PROPOSED FIX: Add a sentence to §5.1 step 7: "Walk versions in DESCENDING order and stop at the first digest equal to `candidate`; a skill's own history is the only thing read, so the cost is O(bytes in skills/<name>/) per attempt. If that ever becomes a problem the fix is a digest index outside the version folders, never a manifest inside one (§3.1)." Optionally note that `safeWrite` re-runs the mutation per retry, so the scan repeats.

## M44 [medium] §7.5 / §11.4 :: `skill move` is a second, incompatible "Move" — the existing one is install-then-uninstall under a named Ryan ruling, and the spec leaves both live
DETAIL: The card menu and the detail page already have a Move. It is deliberately NOT a filesystem move: `moveTo()` runs `backend.install` into the destination and then `backend.uninstallSkill` from the old scope, in that order, with the comment '…so a failure leaves the skill placed twice rather than nowhere (Ryan, 2026-09-09)'. §7.5's `skill move <path> --to <global|project-root>` is a `moveDirectory()` rename plus a ledger update. §11.4 reassigns only `placeAction` between the two card surfaces and says nothing about `moveAction`, which `cardActions` pushes unconditionally — so after the refactor a marketplace card's 'Move to…' re-places via two CLI runs while a Library card's 'Move' renames a folder, both labelled Move, with different failure modes and different safety rails.
EVIDENCE: desktop/src/screens/skill/SkillScreen.tsx:85-87 — "/** Move is install-into-the-destination then uninstall-from-the-old-scope: two CLI runs, in that order, so a failure leaves the skill placed twice rather than nowhere (Ryan, 2026-09-09). */ async function moveTo(){"; desktop/src/components/domain/skill-card-actions.ts:24 `actions.push(moveAction(skill,at),placeAction(skill,at),publishAction(skill,at));`; src/lib/placer.ts:184 `export async function moveDirectory(from, to)`.
PROPOSED FIX: Add to §7.5: "`skill move` REPLACES the install-then-uninstall re-place. `moveTo()` (`SkillScreen.tsx:87`) and the `dialog=move` two-run sequence are deleted; `moveAction` routes to `backend.skill.move` on both surfaces. This is strictly better than the ruling it supersedes: one filesystem move plus one ledger update cannot leave the skill placed twice or placed nowhere. Note the supersession so the 2026-09-09 ruling is not read as still binding."

## M45 [medium] §3.2 / §5.1 :: `listVersions` is readdir-only, but publish needs the version list inside safeWrite's pure mutation — forcing a second, untested enumerator the spec forbids
DETAIL: §3.2 locks `listVersions(clone, skillName)` on `readdir` and says 'Ordering is always numeric on `n`. … `listVersions` is the only sorter and its test must include a two-digit case.' §5.1 step 7 then calls for `listVersions` INSIDE the safeWrite mutation, which is a pure function over `MutableTree` — it must see the tree's overlay, not the disk, and safeWrite retries the whole mutation after a fresh fetch+reset. An implementer following both sentences literally writes an ad-hoc `tree.paths('skills/<name>/')` + regex + sort inline, which is the second sorter §3.2 exists to prevent and the one the two-digit test will not cover — so `v10` sorts before `v9` in the one place that decides the next version number.
EVIDENCE: src/lib/teamRepo.ts:181-183 — `const tree = makeTree(root, tracked, executable); const returned = mutate(tree);` inside the `while (now() <= deadline)` loop that begins `await requireGit(['fetch','origin']); await requireGit(['reset','--hard','origin/main']);`. src/lib/teamRepo.ts:322-329 `paths(prefix = '')`. Compare src/commands/project.ts:56-58, which re-reads `tree.before('team.json')` inside the loop with an explicit comment about exactly this hazard.
PROPOSED FIX: Add to §3.2: "`export function versionsInTree(tree: Pick<GuardTree,'paths'>, skillName: string): SkillVersion[]` — the same parse and the same numeric descending sort, over `tree.paths('skills/<name>/')`. `listVersions` and `versionsInTree` share one sorter and one parser; the §14.1 two-digit test covers both." Change §5.1 step 7 to name `versionsInTree`.

## M46 [medium] §4.1 :: `src/lib/version.ts` is left holding one function next to a new `src/lib/versions.ts` — two near-identically-named modules in the same directory
DETAIL: §4.1 says '[rewrite] src/lib/version.ts — delete `resolveVersion`'s tree-hash contract. Keep `materializeVersion` unchanged.' The file is 60 lines of which `resolveVersion` is 20; after the rewrite it is a single 26-line function whose one remaining consumer is the eval's incumbent arm, sitting beside a brand-new `src/lib/versions.ts` that owns the version vocabulary. Two files one character apart, one of which nothing about versions belongs to any more — every future import of 'the versions module' is a coin flip, and §9.1's '`materializeVersion` stays for the eval's incumbent arm and is not reachable from a user ref' is exactly the kind of constraint a misnamed module erodes.
EVIDENCE: src/lib/version.ts:9 `export async function resolveVersion(...)` and src/lib/version.ts:35 `export async function materializeVersion(store, team, clone, name, tree, runner)` — the whole file. Spec §3.2 creates `src/lib/versions.ts`.
PROPOSED FIX: Change §4.1's version.ts line to: "`[delete] src/lib/version.ts` — `resolveVersion` goes with the tree-hash contract; `materializeVersion` moves verbatim into `src/lib/evals/` (it is now used only by the eval's incumbent arm and is deliberately unreachable from a user ref, §9.1). Nothing named `version.ts` survives beside `versions.ts`."

## M47 [medium] §4.2 :: The guard's author-ownership machinery is orphaned but not deleted, and `guardRawPush` still passes a `GuardAction` the spec removes
DETAIL: §4.2 deletes `'connect'` from `GuardAction` and replaces row a with the path-shaped, add-only row a′ under `'publish'`. But `ownsSkill` — row a's implementation — is still called by `guardRawPush` with the literal `{ action: 'connect', … }`, which becomes a type error. `ownsSkill` also carries the `previousAuthor` managed-author-refresh branch gated on `context.action !== 'sync'`, and §4.2 keeps `'sync'` in `GuardAction` even though `sync.ts` is deleted in full and `'sync'` is removed from `PEOPLE_ACTIONS` — so `'sync'` survives as an action no row admits and no verb passes. `canonicalSkillDigest` exists solely to serve that branch (one call site), so it is orphaned too. The spec's own rule 2 ('this refactor deletes more than it adds') is not applied here.
EVIDENCE: src/lib/guard.ts:117 `if (skill && ownsSkill(tree, skill[1]!, { action: 'connect', handle, author: identity.author })) continue;`; src/lib/guard.ts:142-160 `ownsSkill`, with :148 `context.previousAuthor === undefined || context.action !== 'sync' ? undefined : …` and :156 `if (canonicalSkillDigest(before) !== canonicalSkillDigest(after)) return false;`; src/lib/guard.ts:10 `GuardAction` still listing `'sync'`; src/lib/skills.ts:152 `canonicalSkillDigest` (only caller is guard.ts:156).
PROPOSED FIX: Extend §4.2's first bullet to: "`GuardAction`: delete `'connect'`, `'sync'`, `'eval'`, `'eval-assets'`, `'decline'`. Add `'migrate'`. With row a gone, delete `ownsSkill`, `authorOf`, `GuardContext.author`/`previousAuthor`, and `canonicalSkillDigest` (`src/lib/skills.ts:152`, whose only caller is `ownsSkill`). `guardRawPush` admits a skill path by the same add-only `v<N>` shape as row a′, not by author comparison — and therefore no longer needs the 'this machine has no name and email' branch."

## M48 [medium] §11.3 :: The `config.shared` reader list is short by five modules, including the `status` DTO the desktop renders
DETAIL: §11.3 says 'Both read the field §10.1 deletes' and names only `leave.ts` and `uninstallMachine.ts`. `config.shared` is a REQUIRED key of `configSchema` and is read by five more modules that the spec never lists: `status.ts` publishes it as `ledger.shared` in the DTO Settings renders; `local-skills.ts` takes it in four exported signatures (`canonicalLedger`, `localSkills`, `librarySize`) and surfaces it as `LocalEntry.shared`, which drives `candidatesOf` and `localSkillCounts`; `discover.ts` takes it; and `ls.ts` reads it in six places — the `extraRoots` computation §7.2 deletes, plus `stateOf`, `healthOf`, and the `LocalSection.rows[].shared`/`connected` limbs the desktop's `inventoryCard` consumes as `connectedSources`. Half-deleting a required schema field across seven modules is precisely the 'two active paths' state rule 2 forbids.
EVIDENCE: src/commands/status.ts:35 `shared: { id: string; source: string; team: string }[];` and :54 `ledger.shared = Object.entries(config.shared).map(…)`; src/lib/local-skills.ts:90 `canonicalLedger(config: Pick<Config,'shared'|'placements'>)`, :105 `localSkills(root, config: Pick<Config,'shared'|'placements'>, …)`, :205 `librarySize(home, config: Pick<Config,'shared'|'placements'|'checkouts'>, …)`, :18 `LocalEntry … shared: SharedRef[]`, :182, :189; src/lib/discover.ts:42 `config: Pick<Config,'shared'|'placements'>;`; src/commands/ls.ts:188, :196, :214, :236, :256, :261, :265; src/lib/schema.ts:132 `shared: z.record(…)` (required).
PROPOSED FIX: Rewrite §11.3's opening to list every reader and its disposition: "`config.shared` is read by `leave.ts` (protection list — replaced by the placements-only rule below), `uninstallMachine.ts:29,31,79,135`, `status.ts:35,45,54` (drop the `ledger.shared` limb and the desktop `SHARED` rows that read it), `local-skills.ts:18,90,105,130,182,189,205` (drop the parameter from all four signatures and `LocalEntry.shared` entirely), `discover.ts:42`, and `ls.ts:188,196,214,236,256,261,265` (`extraRoots`, `stateOf`, `healthOf`, and the `shared`/`connected` row limbs — with them goes `inventoryCard`'s `connectedSources`). All of these change in the same commit as the schema key."

## M49 [medium] §7.1 (L-PROJ) :: `project` is already taken three times — the command module, the feature key, and the backend seam — and §7.1 names only the lib-module rename
DETAIL: §7.1's table locks the lib rename (`src/lib/checkouts.ts` → `src/lib/projects.ts`) and the feature keys, but every other `project` name it assigns is already occupied by the TEAM-project feature: `src/commands/project.ts` is the team `project create` verb, so renaming `src/commands/checkout.ts` to the locked local `project add|remove|list` verb collides on the filename and on `CliVerbs.project`; `backend.projects.create` is already the team seam the spec re-labels `backend.teamProjects.create`; and `features.projects` today means team projects and has live consumers in `MarketplaceScreen`. §7.1's only nod to this is 'the two keys must not collide', which reads as a caution when it is actually a rename of existing consumers — and a feature key whose MEANING silently flips across CLI versions (an older CLI advertising `projects: true` for team projects would be read by the new app as 'this CLI has the local project registry').
EVIDENCE: src/commands/project.ts:26 "`project create` — the act that names a team project. Guard row (i)"; src/cli.ts:87 `const project = program.command('project').description('Create the team projects that place skills inside a repository checkout');`; src/cli.ts:41 `CliVerbs { checkout?: …; project?: typeof runProject; … }`; desktop/src/backend/Backend.ts:24 `projects: { create(args:{name:string;remote?:string}):Run<ProjectCreated> };`; desktop/src/screens/marketplace/MarketplaceScreen.tsx:44 `if (!features?.projects) return null;` and :95 `q&&features?.projects?<Button icon="plus" …>Add skills</Button>`; src/lib/frames.ts:46 `checkouts: true, projects: true,`.
PROPOSED FIX: Add a row to §7.1's table and a sentence after it: "Command module — `src/commands/project.ts` becomes the LOCAL registry verb (renamed from `checkout.ts`); today's team-project verb moves to `src/commands/teamProject.ts` and `CliVerbs.teamProject`, registered under `team project create`. The feature key `projects` changes meaning from team to local: every current consumer (`MarketplaceScreen.tsx:44`, `:95`, `Backend.ts:24`) moves to `teamProjects` in the same commit, and `FRAME_FEATURES` (`src/lib/frames.ts:46`) declares `projects`, `teamProjects` and drops `checkouts`."

## M50 [medium] §10.1 / §11 :: Moving `prune` out of sync breaks the desktop's Prune button and the hint beside it — neither is in §11's missed-surface list
DETAIL: §10.1 moves `prune`/`underQuarantine` to a new `src/commands/prune.ts` verb, and §11.1 correctly catches the bundled Claude Code skill's `sync --prune` reference. But the desktop drives Prune through `backend.sync({prune:true})`, which builds the argv `['sync', …, '--prune', …]`, and the Settings Quarantine group renders the literal command string as a TerminalHint. After `src/commands/sync.ts` is deleted and `--prune` is dropped from the `sync` registration, the button fails with a commander unknown-option error and the hint teaches a command that no longer exists. `desktop/src/backend/Backend.ts`'s `sync` args type and the `SyncArgs.prune` field go with it.
EVIDENCE: desktop/src/backend/tauri/index.ts:824 `run(['sync', …, ...(args.prune ? ['--prune'] : []), …], cliSync, …)`; desktop/src/screens/settings/SettingsContent.tsx:83 `<Group label="Quarantine" trailing={… onClick={prune} …} note={<TerminalHint command="npx -y terum-skills@latest sync --prune" prefix="Prune stands for"/>}>`; src/cli.ts:168 the `sync` registration carrying `.option('--prune', …)`.
PROPOSED FIX: Add to §11 (as §11.6): "**The desktop Prune button.** `backend.sync({prune})` (`desktop/src/backend/tauri/index.ts:824`) and `SyncArgs.prune` become `backend.prune()` running `['prune']`; the Quarantine group's TerminalHint (`SettingsContent.tsx:83`) changes to `npx -y terum-skills@latest prune`. Register `prune` in `invocation-catalog.ts` so the tripwire covers the new literal."

## M51 [medium] §5.1 steps 3-6 :: Hygiene runs before the managed fields are injected, so every never-published folder fails HYG1 at step 3
DETAIL: §5.1 orders: 1 resolve local folder, 2 assert, 3 'Hygiene checks as today', 4 read files into a map, 5 inject managed fields, 6 digest. Today's publish resolves the skill from the CLONE, where the managed fields are already present, so 'as today' hides the problem. The new publish resolves a folder on the user's machine, and `skillFrontmatterSchema` requires `license`, `metadata.id`, `metadata.author` AND `metadata['terum-category']` — HYG1 validates against exactly that schema and HYG5 compares the declared `license` to team policy. The product's own help text states the consequence. So a first publish of a freshly authored skill — the single most important new flow in this spec — refuses at step 3 with a frontmatter error about fields publish is about to write two steps later. A happy accident makes the fix free: `assessHygiene` already takes `{ files: Map<string,Buffer>; executable }`, the exact shape step 4 builds.
EVIDENCE: src/lib/schema.ts:142-146 `skillFrontmatterSchema = z.object({ name, description, license, … metadata: z.object({ id: skillIdSchema, author: z.string(), 'terum-category': z.string() }).passthrough() })`; src/lib/evals/hygiene.ts:46 the HYG1 strict-schema error and :84-90 HYG5's license comparison; src/lib/evals/hygiene.ts:120 `export function assessHygiene(name, input: { files: Map<string,Buffer>; executable }, license, allowExecutable = false)`; src/cli.ts:148 help text: "A folder that has never been connected fails HYG1 on the managed fields connect adds (license, metadata.id, metadata.author, metadata.terum-category); connect it first."
PROPOSED FIX: Reorder §5.1 to 1, 2, then: "3. Read the folder into `{ files, executable }` applying the D2 ignore list. 4. `injectManagedFields` into the in-memory `SKILL.md` … 5. Hygiene checks on the INJECTED map — `assessHygiene(name, {files, executable}, policy.skill_license)` already takes exactly this shape — including the privileged-grants confirmation. A folder that has never been published has no managed fields until step 4, so hygiene must never run before it. 6. `candidate = skillContentDigest(files)`." Add to §14.1: a publish test on a folder whose SKILL.md has only `name` and `description`.

## M52 [low] §5.1 step 8 / §3.5 / §9.3 :: Two smaller loose ends around the new people-file and eval-asset writes
DETAIL: (a) D2 puts `evals/**` outside the digest, so editing an eval case leaves the digest unchanged and §5.1 step 8 takes the 'identical' branch — which attaches receipts but never writes `skills/<name>/evals/**` (only step 9 does). A user who edits a test case and republishes is told 'identical to version 2' and their case is silently never uploaded. (b) §5.1 step 10 says 'Only the profile prompt writes the people file here', but §3.5 says `local_skills` is 'written opportunistically on any people-file write (publish, install, profile)' and §4.2 adds `'publish'` to `PEOPLE_ACTIONS` — three statements, two of which require publish to write the people file unconditionally. (c) `src/lib/profile-entry.ts` is justified as a shared module, but the people-file safeWrite it needs is already written in `profile.ts` and should be the thing it factors out rather than a fourth hand-rolled copy (publish, install, uninstall and profile each hand-roll the same read-parse-set-write today).
EVIDENCE: Spec §5.1 step 8 ("the mutation is empty and `safeWrite` returns `changed: false`") vs step 9 ("Write `skills/<name>/evals/**` (unversioned, add-or-replace)"); spec §5.1 step 10 vs §3.5's `local_skills` bullet vs §4.2's `PEOPLE_ACTIONS`. src/commands/profile.ts:18-36 — the read-parse-mutate-`tree.set` people-file safeWrite; the same shape at src/commands/uninstall.ts:214-227.
PROPOSED FIX: (a) Amend step 8: "…do not mint. `identicalTo = K`. Still write `skills/<name>/evals/**` (add-or-replace) and attach any local receipts whose `content_digest` equals `candidate`…" and add a §14.1 case: an eval-case-only edit republishes as `created:false` yet lands the case. (b) Change step 10 to "Publishing is not installing: `installed[]` is untouched. The people file is written once, after the version write, carrying the profile entry when the user says yes and `local_skills` from the Library scan step 1 already performed." (c) In §9.3 add: "`profile-entry.ts` also exports `writePersonFile(tree, handle, mutate)` — the one read-parse-validate-`tree.set` helper `publish`, `install`, `uninstall` and `profile` all use; the four hand-rolled copies go."
