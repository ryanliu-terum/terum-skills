# ultrareview: m1-wave

**Verdict.** Reviewed `git -C /Users/ryanliu/Documents/Terum/terum-codex/m1-wave diff origin/main...HEAD` (44 files, +972/−1059) across four dimensions with 3-vote adversarial verification: 128 raw findings → 58 distinct after pre-verification merge → **20 confirmed**, 10 contested, 18 unverified. Nothing is critical, but the three high findings are all silent-data-loss shaped and all sit in code this wave introduced: the non-nullable `teams.<team>.handle` schema turns a config the *previous build* wrote into a hard parse failure for **every** verb — including `sync --hook` on session start and the `team join` that would repair it — with no migration and no repair path in-tool; safeWrite's new compromised-lock abort throws but leaves the unconditional `finally` to `git reset --hard origin/main` inside a clone another writer now owns, which can rewind that writer's commit and turn its push into a no-op "success"; and the new scp credential guard misses the `user:pa/ss@host/org/repo.git` spelling, so a password reaches `config.json`, `.git/config`, and error messages while `hasEmbeddedCredentials` reports false. The medium band is mostly the new control flow being untested (both `onCompromised` branches, `create`'s under-lock `assertBindable`, the `hook === true` discriminator — each deletable with the suite still green) and fail-open/lossy roster handling (`catch { continue; }` in `assertLoginUnclaimed` can revoke an active teammate's access; reclaim-by-email silently blanks a member's GitHub login, which then blocks `team remove`). One hermeticity regression: `src/__tests__/bin.test.ts` compiles the project into the developer's own `dist/`.

| Severity | Confirmed |
| --- | --- |
| Critical | 0 |
| High | 3 |
| Medium | 13 |
| Low | 4 |
| **Total** | **20** |

---

## Security & data-loss (5)

- [ ] **[HIGH] Schema tightening of `teams.<team>.handle` bricks every verb for a config the previous build wrote** — `/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/lib/schema.ts:67` (test pin at `src/lib/__tests__/config.test.ts:36`) · *merges 8 reports across Security & data-loss / Terum invariants / Correctness & tests*
  `handle: handleSchema.nullable()` → `handle: handleSchema`, and the round-trip case `'a team entry may be bound to a remote before it has a handle (login before join)'` is replaced by a hard-reject case. But `origin/main`'s `login --team --remote` produced exactly that shape: `bindTeam(fresh, team, { remote, token })` with old `bindTeam` filling `handle: current?.handle ?? null`. Since `read()` is `parseJson(configSchema, …)` and `update()` reads under the lock (`src/lib/config.ts:31,60`), one `"handle": null` entry fails *every* verb with `Invalid ~/.terum/skills/config.json: teams.t.handle: …expected string, received null` — including `sync --hook` (`src/commands/sync.ts:39`) on every session start and `team join`, the only verb that would rewrite it. No migration (`grep -rn 'migrat' src` is empty), and the message names no fix. Note the same diff *did* preserve read compatibility for the other retired key (`token`, via `.passthrough()`).
  **Fix:** accept and repair on read — `handle: handleSchema.nullable().optional()` plus a normalize step that drops/marks-unbound entries with no proven handle (call sites already null-check: `sync.ts:85,132`, `uninstall.ts:44,56`, `team.ts:54`, `share.ts:38`) — or catch this ZodIssue in `createConfigStore.read()` and rethrow with the repair instruction.

- [ ] **[HIGH] Compromised safeWrite lock aborts the attempt but still hard-resets the clone another writer now owns** — `/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/lib/teamRepo.ts:146` · *merges 5 reports across Security & data-loss / Correctness & tests / Terum invariants / Reuse*
  `onCompromised: () => { compromised = true; }` (`:96`) + `if (compromised) throw new Error(lostLock(root))` (`:100`, `:134`) only stop *this* process from pushing; the `finally` still runs `git fetch origin` + `git reset --hard origin/main` (`:147`) and `rm`s created paths. `compromised` means process B stole the stale lock and is running safeWrite in the **same clone**. If B is between `commit -q` (`:133`) and `push` (`:135`), A's cleanup rewinds B's HEAD to `origin/main`; B's `git push -q origin HEAD:refs/heads/main` exits 0 ("Everything up-to-date") and B returns `{ changed: true, pushedTo: 'main' }` for a mutation that was never written. The sibling at `src/lib/config.ts:62` gets this right by skipping the destructive step.
  **Fix:** gate the cleanup — `if (!compromised) { fetch; reset --hard; removeCreated… }`, keeping only `await release().catch(() => undefined)`; the next safeWrite fetches and hard-resets anyway (the comment at `:144` already says so). Add a test forcing `onCompromised` that asserts no `reset --hard` after the throw.

- [ ] **[MED] `team join` pushes the roster entry, then fails on the post-lock rebind check** — `/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/commands/team.ts:276` · *merges 2 reports across Security & data-loss / Correctness & tests*
  `join` does its durable push first (`:267 repo.safeWrite(… action:'join' …)`, never reverted — safeWrite's `finally` only resets the local clone) and *then* takes the config lock and calls `assertBindable(fresh, team, normalized)` (`:278`), which throws. The new test `src/commands/__tests__/join.test.ts:192-201` enshrines the outcome: push lands, command returns `{ ok: false, error: '… already configured as team other' }`, `Object.keys(teams) === ['other']` — and never checks origin, so it does not notice `people/me.json` is now an active member. Re-running `team join <remote>` resolves to the existing entry with `fixedHandle`, writing `people/someone.json` and leaving the orphaned `people/me.json` ACTIVE forever. Contradicts the contract two lines down (`:283`) and diverges from `create`, which explains the same post-write failure (`:216-221`).
  **Fix:** make the post-lock failure explanatory (name the durable roster write and the recovery command) or bind under the name `assertBindable` found, since the remote is provably the same repository; align `create`'s `:220-225` path.

- [ ] **[MED] Shared-GitHub-login revocation guard fails open on any unparseable `people/*.json`** — `/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/commands/team.ts:146` · *merges 6 reports across Security & data-loss / Correctness & tests / Terum invariants / Reuse*
  `try { person = parseJson(personSchema, treeText(tree.before(path)!), path); } catch { continue; }` skips any sibling roster file that misses a `personSchema` field, so its `github` is never compared. The caller five lines up justifies re-validating raw JSON precisely because "a legacy repository can carry a value written before the schema existed" (`:66-70`). Failure: `people/alice.json` is legacy (missing `bio`) and declares login `alice`; `team remove alice-work` parses it, `continue`s, the guard passes, and `gh api -X DELETE …/collaborators/alice` (`:97-99`) revokes an active member's access while reporting success. The same `catch` also swallows a TypeError from `tree.before(path)!` being `undefined` for overlay-created paths (`teamRepo.ts:222-226`).
  **Fix:** fail closed — on parse failure read the raw `github` (the same lenient strategy `remove()` uses for the target at `:70`) and refuse on a collision, or refuse outright naming the unreadable path. Add a `remove.test.ts` case with a schema-invalid twin declaring `member-gh` asserting refusal and no DELETE.

- [ ] **[MED] `remoteName()` yields an empty team name for the `.git`-directory local remote this batch newly declares supported** — `/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/lib/remote.ts:207` (test at `src/lib/__tests__/remote.test.ts:149`) · *merges 5 reports across Security & data-loss / Correctness & tests / Terum invariants / Reuse*
  `remote.test.ts:27,31` newly assert `/tmp/x/team/.git` normalizes to `file:/tmp/x/team/.git` and round-trips, but the new `remoteName` case list covers only `/tmp/x/team.git`, `file:/tmp/x/team.git`, `/tmp/x/team`; the idempotence loop (`:57`) only asserts `not.toThrow`, never the value. `return path.slice(path.lastIndexOf('/') + 1).replace(/\.git$/i, '')` turns basename `.git` into `''`, and the sole consumer `src/commands/team.ts:245` feeds it to `teamNameSchema` (`/^[A-Za-z0-9]…/`), which rejects `''`.
  **Fix:** strip a trailing `/.git` before taking the basename so the parent segment names the team (what `git clone` does); add `expect(remoteName('/tmp/x/team/.git')).toBe('team')`.

## Terum invariants (6)

- [ ] **[HIGH] New scp credential guard still lets a password containing `/` through** — `/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/lib/remote.ts:114` · *merges 3 reports across Terum invariants / Correctness & tests*
  `if (/^[^/]*@/.test(scp[3]!) || /@[^/:]*:/.test(scp[3]!)) throw unsupported(…)` — the second branch only fires when a `:` follows the `@`. For `user:AbC/dEf@github.com/org/repo.git`, SCP_FORM yields host=`user`, path=`AbC/dEf@github.com/org/repo.git`: neither regex matches. Result: `normalizeRemote` → `user:AbC/dEf@github.com/org/repo` written to `~/.terum/skills/config.json` by `bindTeam`; `remoteToGitUrl` feeds the secret to `git clone` / `git remote add origin --` (`teamRepo.ts:305`, `team.ts:429`) so it lands in `.git/config`; and `stripRemoteCredentials` returns the input unchanged for kind `scp`, so `hasEmbeddedCredentials` is **false** and `team.ts:183/239` never prints `credentialNotice`. Breaks the module's own header invariant (`remote.ts:11`). The suite gets within one character: `remote.test.ts:118` covers the colon form `me:aWxs/K3Q@git.example:team.git` only.
  **Fix:** reject any `@` in an scp path — `if (scp[3]!.includes('@')) throw unsupported(input, 'credentials in an scp-style remote are not supported')`; a legitimate `[user@]host:path` never carries a second `@`. Add `me:aWxs/K3Q@git.example/team.git` and `user:AbC/dEf@github.com/org/repo.git` to the `rejected` list at `remote.test.ts:113-119`.

- [ ] **[MED] New bin test compiles into the repo's own `dist/`, breaking suite hermeticity and racing any concurrent build** — `/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/__tests__/bin.test.ts:15` · *merges 4 reports across Terum invariants / Reuse / Correctness & tests / Security & data-loss*
  `run(process.execPath, [resolve(root, 'node_modules', 'typescript', 'bin', 'tsc'), '-p', 'tsconfig.build.json'], { cwd: root })` with `outDir: "dist"` makes `vitest run` create/overwrite `<repo>/dist/**` — the directory `npm run build`/`prepack` emit into and `package.json` `bin`/`files` ship. Every other part of the suite is deliberately hermetic (`setup.ts:12-16` mints a throwaway HOME; `fixtures.ts:38-42` routes scratch through `TEMP_DIRS`; `vitest.config.ts` states the suite "must not depend on, or touch, the developer's ambient state"). Concurrent `npm run build` + `npm test` gives two `tsc` processes writing `dist/index.js`, and the shebang assertion fails nondeterministically. Also duplicates the `build` script, hardcodes `node_modules/typescript/bin/tsc` (breaks under pnpm/hoisted layouts), and re-states `testTimeout: 120_000`.
  **Fix:** `tsc -p tsconfig.build.json --outDir <temporaryDirectory()>` and resolve `bin` from there; resolve the compiler via `createRequire(import.meta.url).resolve('typescript/bin/tsc')`.

- [ ] **[MED] `collectIdentity`'s documented "leave it empty" GitHub login is unreachable once a login is suggested** — `/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/lib/auth.ts:65` · *merges 2 reports across Terum invariants / Correctness & tests*
  The `if (!trimmed) return { ok: true, value: '' }` branch cannot be reached when `suggested` is non-empty, because `terminalPrompter.text` returns `answer.trim() || defaultValue || ''` (`src/lib/prompt.ts:79`); `suggested` is non-empty whenever `existing.github` is set or `gh api user -q .login` succeeds (`auth.ts:62`). (1) A user with no GitHub account on a gh-logged-in machine can never clear the suggestion — it is written to `people/<handle>.json` and `config.github`. (2) `configSchema.github` is only `z.string().optional()` (`schema.ts:73`) and old `collectIdentity` never validated it, so a legacy invalid value becomes the default and three Enters yield `Invalid github login after 3 attempts` — `login` cannot complete without hand-editing config.json. The added test (`auth.test.ts:39-49`) only covers the empty case with *no* default.
  **Fix:** ask without a `defaultValue` (seed the suggestion as prompt text) or accept a `-`/`none` sentinel to clear; tighten `configSchema.github` to `z.union([z.literal(''), githubLoginSchema]).optional()`. Add auth tests for `{ …emptyConfig(), github: 'octocat' }` answering `''`, and for a persisted invalid value.

- [ ] **[MED] Decision-1 reclaim blanks the member's GitHub login, which then blocks `team remove`** — `/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/commands/team.ts:338` (test at `src/commands/__tests__/join.test.ts:149`) · *merges 2 reports across Terum invariants / Reuse*
  The new reclaim-by-email case joins with an empty GitHub-login answer and asserts only `display_name`. `joinMutation` overwrites `github: identity.github` unconditionally (`:333-343`) while `bio`/`installed`/`declined` are preserved with `existing?.x ?? …`, so `people/ajay.json` silently goes from `github: 'Real-Ajay'` to `''`. That blank then blocks the primary removal path: `team.ts:72` `parseOrExplain(githubLoginSchema, targetRaw.github, …)`, pinned by `remove.test.ts:87-101`. Decision 1 exists for the second laptop — where there is no config and typically no gh, so pressing Enter is the natural answer.
  **Fix:** `github: identity.github || existing?.github || ''` on reclaim/rejoin (or refuse an empty answer when the email match hit a file carrying a login), and assert `github` stays `'Real-Ajay'` in the join test.

- [ ] **[MED] Derived-branch push treats a lease/CAS violation as retryable, so the retry force-overwrites the other writer's branch** — `/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/lib/teamRepo.ts:188` · *merges 2 reports across Terum invariants / Correctness & tests*
  `if (!STALE_LEASE.test(lastError)) return { ok: false, retryable: RETRYABLE.test(lastError), error: lastError };` — `RETRYABLE` (`:59`) contains `cannot lock ref`, `incorrect old value`, `remote ref updated since checkout`, i.e. the CAS vocabulary emitted when the lease's old-oid genuinely does not match. `retryable: true` returns to the loop (`:135-140`), which re-runs `fetch origin` + `reset --hard` (`:101-102`), renewing the lease, then re-pushes `--force-with-lease` to the same `branch` (`:184`) — overwriting the other member's commit and skipping the `<branch>-2` fallback that exists for exactly this (`:171-174`). The new test asserts `result.pushedTo === 'publish/x'` after `cannot lock ref … is at abc but expected def`, locking the overwrite in. Latent: no production caller passes `options.branch` yet.
  **Fix:** keep CAS-shaped rejections out of the retryable set for non-`main` branches, or fall through to `<branch>-2`; alternatively pin the lease to the sha read at entry (`--force-with-lease=refs/heads/<t>:<sha>`) and fix the same hazard at `:190`. Extend the test to push a competing commit between attempts and assert it survives.

- [ ] **[MED] After dropping the per-team token, `invite` and `team remove` never check gh auth** — `/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/commands/invite.ts:27` · *merges 2 reports (Terum invariants)*
  Both verbs dropped `{ env: gitAuthEnv(binding.token) }` and now depend on ambient `gh auth`, but neither gained a `ghState`/`detectOrOfferGh` precheck like `team create`'s `authenticateCreator` (`auth.ts:112-117`). Logged out, `invite`'s `gh api -X PUT` returns non-zero with no `HTTP nnn`, so `httpStatus` is null and line 34 tells the user about the 50-invitations-per-day cap. `team.ts:77-78` cannot distinguish logged-out from not-an-admin and prints "Team removal requires GitHub repository admin permission." With gh absent entirely, `systemRunner` rejects (`runner.ts:25`) and the verb fails with raw `spawn gh ENOENT` — newly reachable for anyone who previously had a PAT configured.
  **Fix:** call `ghState(runner)` at the top of `invite.run` and of the `ownerRepo !== null` branch in `team.remove`, failing with `authenticateCreator`'s wording. Sweep `invite.ts:27`, `team.ts:77,80,84,87,99,104`. Add tests on `fakeGh('me', {}, false)` and `noGhRunner`.

## Correctness & tests (8)

- [ ] **[MED] New empty-login test only covers the generic-remote half of `revoking`; the GitHub archive-only path is untested** — `/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/commands/__tests__/remove.test.ts:153` · *merges 2 reports across Correctness & tests / Terum invariants*
  The pinned behaviour is `const revoking = ownerRepo !== null && !args.archiveOnly` (`team.ts:65`). The new test sets `config.teams.team = { remote: fixture.bare }` so `ownerRepo === null` — only the left conjunct. Mutating the source to `const revoking = ownerRepo !== null;` survives the whole suite (the only GitHub archive-only test, line 104, uses `member` with a valid login and fails earlier on the admin probe). The uncovered combination is real: a GitHub-hosted member with `github: ''` (now legal per `schema.ts:55`) archived with `--archive-only`. The comment at line 182 claims coverage no assertion provides.
  **Fix:** add a `prepared()` case (GitHub remote + `mappedRunner`) with `github: ''`, `{ kind: 'remove', handle: 'nogh', archiveOnly: true }`, admin probe true; assert `ok: true`, archive landed, no collaborators/invitations call.

- [ ] **[MED] `create`'s new under-lock `assertBindable` re-check has no test, unlike `join`'s** — `/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/commands/team.ts:222`
  `join`'s twin (`:278`) is covered by `join.test.ts:192-201` via `wrapRunner`; `create.test.ts` has no equivalent. Every duplicate-binding test there (`:67-77`, `:120-133`) exercises only the pre-prompt checks at `team.ts:169-170/181-182` and would still pass if line 222 were deleted. `assertBindable`'s own doc (`auth.ts:159-161`) says the point is the interleaving case — and for `create` that path (repository scaffolded, then bind refused) leaves the most confusing state.
  **Fix:** mirror `join.test.ts:192-201` — wrap the runner so the scaffold `git push` binds `remote` to another team name, then assert the failure, the config keeping only the other entry, and the message pointing at `team join`.

- [ ] **[MED] No test anywhere covers the new compromised-lock abort in `config.update`** — `/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/lib/config.ts:57` (test file `src/lib/__tests__/config.test.ts:48`) · *merges 2 reports across Correctness & tests / Terum invariants*
  `grep -rn "compromis|Lost the lock" src/` matches only `config.ts` and `teamRepo.ts` — zero test files. The two lock-adjacent cases added in this diff (throwing-mutate at `:48`, corrupt-config at `:60`) never drive `onCompromised`. Delete `if (compromised) throw …` and the suite stays green, restoring the stale-snapshot-renamed-over-another-writer bug the comment at `config.ts:48-50` exists to prevent. The `release().catch(() => undefined)` swallow is likewise unpinned.
  **Fix:** force the compromise (steal/delete `config.json.lock` during an async `mutate`, or inject `onCompromised`) and assert `update()` rejects with `/Lost the lock/` **and** config.json is byte-identical. Same helper covers `teamRepo.ts:90,100,134`.

- [ ] **[MED] No test for the new safeWrite lock-compromised abort** — `/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/lib/__tests__/teamRepo.test.ts:304` · *merges 2 reports (Correctness & tests)*
  The three cases added here (`:288`, `:304`, `:322`) cover credential redaction, ref-lock retry and deletion restore; none exercises `compromised`, so `lostLock` (`teamRepo.ts:61`), both guards (`:100`, `:134`) and the `release().catch()` (`:156`) are unexecuted. Paired with finding #2 above, the untested `finally` is also actively destructive.
  **Fix:** acquire the lock then delete/`utimes` `join(dirname(root), '.'+basename(root)+'.safewrite.lock')` to fire `onCompromised`; assert safeWrite rejects with the `lostLock` message, nothing was pushed to the bare remote, and the clone was not reset.

- [ ] **[LOW] `bin.test.ts` asserts exact child stderr while inheriting the runner's full env** — `/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/__tests__/bin.test.ts:25` · *merges 2 reports across Correctness & tests / Security & data-loss*
  `const env = { ...process.env, HOME: home, USERPROFILE: home }` (`:19`) then `expect(failed.stderr.trim()).toBe('Unsupported remote: not a remote')`. An inherited `NODE_OPTIONS` (`--experimental-*`, `--import`, `--loader`) or any deprecation warning writes to the child's stderr and fails the strict `toBe` for reasons unrelated to the CLI. The env override is also half-applied: `GH_CONFIG_DIR` (set by `setup.ts` under the *worker's* home) still points outside the new HOME.
  **Fix:** build the env explicitly (or `NODE_OPTIONS: ''`, `NODE_NO_WARNINGS: '1'`, re-point `GH_CONFIG_DIR`) and/or assert the last line: `expect(failed.stderr.trim().split('\n').at(-1)).toBe(…)`.

- [ ] **[LOW] `--repo` (and `--org`) are silently ignored when `--remote` is passed** — `/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/cli.ts:37` · *merges 4 reports across Correctness & tests / Terum invariants / Reuse*
  `create` branches on `args.remote` first (`team.ts:174`) and reads `args.repo`/`args.org` only in the `else` (`:189-199`), so `team create alpha --remote https://git.example/x.git --repo skills-repo` drops `--repo` without a word and scaffolds into `x.git`. Commander applies no mutual exclusion, and every new `--repo` case (`create.test.ts:179,195`) exercises the GitHub path only.
  **Fix:** reject the combination in `create` (`'--repo and --org apply only when creating the repository on GitHub; drop them when using --remote.'`) or wire `.conflicts(['remote'])`, and add a cli/create case.

- [ ] **[LOW] A failed post-push re-probe makes `create` give recovery advice guaranteed to fail** — `/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/commands/team.ts:214`
  The realistic trigger for the post-push branch is a network fault (the new test's own `stderr: 'hung up unexpectedly'`), which usually also fails the following `git ls-remote --heads`. `heads.code !== 0` collapses to `scaffolded === false`, so the user is told the remote "holds no scaffold" and sent to `team create <name> --remote <url>` — which the `--remote` preflight (`:186-187`) refuses with "already has branches". `create.test.ts:241` only covers a succeeding re-probe.
  **Fix:** make a failed re-probe a third outcome ("could not determine whether the scaffold reached <remote>; check the repository, then run `team join <remote>` if it has a main branch"), and add a case where `wrapRunner` fails both `push` and `ls-remote`.

- [ ] **[LOW] Twin-login refusal is never tested on the re-applied tree** — `/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/commands/__tests__/remove.test.ts:165`
  `assertLoginUnclaimed` is deliberately inside the pure safeWrite mutation so it re-runs after every fetch/reset (`team.ts:127-129`), but the test pushes `people/twin.json` *before* `run(...)`, proving only that a refusal happens somewhere. Moving the check into a preflight would keep both assertions green while re-opening the race.
  **Fix:** use the mid-safeWrite race pattern (`wrapRunner` intercepting the first `git push` to `pushFromSeed(fixture.seed, 'people/twin.json', …)`, as in `teamRepo.test.ts:228` / `share.test.ts:373`) and assert the retry refuses and pushes nothing.

## Reuse, simplify, perf (1)

- [ ] **[MED] `execute.test.ts`'s non-hook case cannot fail — the `hook === true` discriminator is untested** — `/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/lib/__tests__/execute.test.ts:40`
  The negative case passes `notices: []` and `deferred: []`, so `writeHookNotices()` (`execute.ts:38-41`) writes nothing regardless of whether the value was recognized as a hook sync. Delete `(value as Partial<SyncResult>).hook === true` from `isHookSync` (`execute.ts:35`) and the suite stays green (`createExecute` is used only by `src/index.ts` and this file). The shipped regression: a plain `sync` already prints notices to stdout (`sync.ts:33`), so they would be emitted twice plus a spurious "N skills need review — run `terum-skills sync`" — exactly the two-channel contract (§8) this file defends.
  **Fix:** give the `quiet` case a non-empty payload — `success({ placed: 1, deferred: ['a'], notices: ['note'], changed: true, hook: false })` with `expect(quiet.lines).toEqual([])`.

---

## Contested (split adversarial verdict — needs human adjudication)

Not folded into counts or top findings. Each is one vote from flipping.

- **(high / Terum invariants) `team remove` on a GitHub team cannot remove a member who has no GitHub login** — `src/commands/team.ts:72` — vote 1-2
  Evidence: login validation is conditional on `revoking`, but `revoking` is true for EVERY non-`--archive-only` removal on a GitHub remote: `const revoking = ownerRepo !== null && !args.archiveOnly; const login = revoking ? parseOrExplain(githubLoginSchema, targetRaw.github, \`GitHub login for ${targetHandle}\`) : '';`. An empty `github` is now a *supported* roster state (`schema.ts:53-55` widened to `z.union([z.literal(''), githubLoginSchema])`, `auth.ts:67` accepts blank, `team.ts:338` writes it verbatim). Admin runs `terum-skills team remove nogh` on `github.com/acme/team` where `people/nogh.json` has `"github": ""` → `parseOrExplain` throws before any gh call with "Invalid GitHub login for nogh: a GitHub login is 1-39 letters, digits, or single internal hyphens", a message that says the roster is malformed when there is simply no GitHub access to revoke. Only escape is `--archive-only`, which the error does not mention; the GitHub combination is untested (`remove.test.ts:153` uses a generic remote).

- **(medium / Correctness & tests) `team create` asks for the GitHub repository name before it checks that gh exists** — `src/commands/team.ts:189` — vote 2-1
  Evidence: the repo-name question is now the FIRST thing the GitHub path does; `authenticateCreator` (which owns the gh-installed/authenticated stop) runs after it. On a gh-less machine `terum-skills team create alpha` prompts `GitHub repository name [alpha]:` and only then prints "Creating a GitHub team needs the GitHub CLI (gh)…". Non-interactively `terminalPrompter.ask` throws `PromptClosedError('not-interactive')` first, so the user gets `Cannot ask "GitHub repository name": this command needs an interactive terminal (stdin is not a TTY).` instead of the actionable gh message. `create.test.ts:114` masks it: its first `'me'` answer is silently eaten by the new repo question, so the test proves nothing about ordering.

- **(medium / Reuse, simplify, perf) A pre-rev-9 `token` in config.json is kept and rewritten forever, not dropped** — `src/lib/schema.ts:65` — vote 1-2
  Evidence: the new doc says "an old `token` key in an existing config.json is read and ignored", but `teamConfigSchema` keeps `.passthrough()` (`schema.ts:67`), so zod preserves the key, and `update()` re-serializes the whole config (`config.ts:63`), rewriting the stale PAT on every install/share/sync. The only remover is `delete current.token` in `bindTeam` (`auth.ts:146`), which now runs only from `team create`/`team join` — commands an already-joined user never re-runs. `config.test.ts:33-34` only asserts the key is tolerated on read; nothing asserts it is dropped.

- **(medium / Reuse, simplify, perf) New `assertBindable()` leaves three hand-rolled copies of itself in team.ts, and half of it is unreachable at the `create()` call site** — `src/lib/auth.ts:163` — vote 1-2
  Evidence: its doc says "`create` and `join` check this before they prompt AND again under the config lock", but neither verb calls it before prompting. `team.ts:181-182` duplicates the first half verbatim including the message string; `team.ts:246-248` duplicates the second half with different wording; `team.ts:169` is a third variant. Under the lock in `create`, `if (fresh.teams[name]) throw …` (`:221`) throws for any existing entry, so `assertBindable`'s `const existing = config.teams[team]; if (existing && existing.remote !== normalized)` (`auth.ts:167-168`) is dead at that call site. Violates AGENTS.md "One active path per behavior."

- **(medium / Reuse, simplify, perf) `uninstall member` / `uninstall project` does one full fetch+commit+push per skill against the same people file** — `src/commands/uninstall.ts:26` — vote 2-1
  Evidence: `run()` fans out one `uninstallOne` per (skill, scope), each ending in its own `repo.safeWrite(...)` — a complete `fetch` + `reset --hard` + `ls-files` + `add` + `write-tree`/`diff --cached` + `commit` + `push` cycle (`teamRepo.ts:99-141`) per skill, all rewriting the *same* `people/<handle>.json`. Each iteration also re-reads the config three times (lines 53, 58, 88) and takes the config lock three more (57, 66, 82), serially. 20 installed skills → 20 sequential pushes and commits, with a partially updated roster if the 11th push is refused.

- **(medium / Reuse, simplify, perf) New duplicate-login guard is tested (and implemented) after the confirm and 4 gh round-trips, unlike every sibling refusal** — `src/commands/__tests__/remove.test.ts:179` — vote 2-1
  Evidence: the test is named "…before archiving or touching the host", but its `host` map answers `repos/acme/team -q .permissions.admin`, `collaborators?permission=admin`, `collaborators`, and `invitations`, and the prompter is `new ScriptedPrompter([], [true])` — all four gh reads AND the "Revoke GitHub access for @member-gh and archive member? (y/N)" confirm complete before the refusal fires; the only host assertion is the weak `runner.calls.some(call => call.args.includes('DELETE'))).toBe(false)`. Cause: the guard lives inside `archiveMutation` (`team.ts:132`), inside safeWrite's mutate callback, whereas every sibling refusal is a preflight (`'You cannot remove yourself'` `:56`, `'last remaining admin'` `:82`). The check also re-runs on every push retry.

- **(low / Correctness & tests) cli.test.ts swapped a behavioural rejection assertion for an internal-state check on login** — `src/__tests__/cli.test.ts:40` — vote 2-1
  Evidence: `await expect(program.parseAsync(['login','--team','alpha'], { from: 'user' })).rejects.toMatchObject({ code: 'commander.missingMandatoryOptionValue' })` was replaced by `expect(program.commands.find(c => c.name() === 'login')?.options).toEqual([])`. The replacement inspects commander's internal `options` array (correctly excluding the lazy `_helpOption`) but no longer exercises the rev-9 Decision 4 contract — that `terum-skills login --team alpha` is *rejected at parse time*. No test in the repo now parses `login` with the removed flags, or with a stray positional.

- **(low / Security & data-loss) A pre-Decision-2 PAT is left at rest in config.json forever; only create/join purge it** — `src/lib/auth.ts:146` — vote 1-2
  Evidence: `delete current.token` in `bindTeam` is the only removal, and `bindTeam` now runs only from `team create` (`team.ts:224`) and `team join` (`team.ts:280`) — the rewritten `login` writes identity only (`login.ts:23`). `teamConfigSchema` is `.passthrough()`, so `read()` keeps the unknown `token` and `update()` writes it back on every unrelated mutation. A user who ran the previous release's `login --team --remote` and pasted a fine-grained PAT keeps that live credential in `~/.terum/skills/config.json` indefinitely. The same commit weakens the file's warning (`config.ts:9`: "it holds tokens" → "it names your teams, identity, and every placed path") and `schema.ts:65` claims the old key "is read and ignored" — it is read, ignored, and re-written.

- **(low / Correctness & tests) "never asked for a token" is asserted with a /PAT|token/i keyword scan that passes for every prompt the code can produce** — `src/lib/__tests__/auth.test.ts:77` — vote 1-2
  Evidence: `expect(io.asked.some(q => /PAT|token/i.test(q))).toBe(false)` at `:77`, `:84`, `:96`. The only reachable questions are `'GitHub login'`, `'Team handle'`, `'Your name'`, `'Your email'` (`auth.ts:65,79,72,73`) — none contains "PAT" or "token", so the predicate is false for the current code and for any future credential prompt worded differently ("Paste your fine-grained GitHub credential", "App password"). The genuine guarantee is structural: `Prompter` has no `secret` method (`prompt.ts:14-20`) and this diff drops it from `ScriptedPrompter` (`fixtures.ts:31`).

- **(low / Security & data-loss) GIT_TERMINAL_PROMPT restore writes the literal string "undefined" when the var was unset** — `src/lib/__tests__/runner.test.ts:22` — vote 1-2
  Evidence: `const ambient = process.env.GIT_TERMINAL_PROMPT; … finally { process.env.GIT_TERMINAL_PROMPT = ambient; }`. Node coerces `process.env` assignments with `String(value)`, so an `undefined` ambient sets the six-character string `"undefined"` rather than deleting the key; git parses the var as a boolean and dies with `bad boolean environment value 'undefined'`, breaking any later `stdio: 'inherit'` run in the worker. Latent today only because `vitest.config.ts:18` sets `GIT_TERMINAL_PROMPT: '0'` — the same config line the new comment at `runner.test.ts:15` depends on.

---

## Unverified (not adversarially checked — efficient mode)

Listed for completeness; excluded from counts and top findings.

- (low / Correctness & tests) New `isMember` cases test an export with no production caller — `src/lib/__tests__/guard.test.ts:112`
- (low / Security & data-loss) `--org` is concatenated into the `gh repo create` positional unvalidated, while the sibling `--repo` added in the same change is validated — `src/cli.ts:36`
- (low / Security & data-loss) Guard's archived check trusts the people file's self-declared `handle` instead of its filename — `src/commands/team.ts:147`
- (low / Terum invariants) New `process.exit` lint rule does not cover `src/lib/prompt.ts`, contradicting its own message — `eslint.config.js:24`
- (low / Reuse, simplify, perf) `validateName`'s `what` prefix duplicates and contradicts `askUntilValid`'s own "Invalid <question>:" prefix — `src/commands/team.ts:152`
- (low / Reuse, simplify, perf) `CreatorAuthOptions` / `options.fixedHandle` on `authenticateCreator` is dead after login.ts stopped calling it — `src/lib/auth.ts:100`
- (low / Reuse, simplify, perf) `identityForJoiner` is a one-use wrapper with a now-dead gh fallback, while login.ts inlines the same three lines — `src/lib/auth.ts:123`
- (low / Reuse, simplify, perf) Protected-branch refusal is now asserted twice with near-identical setup — `src/lib/__tests__/teamRepo.test.ts:316`
- (low / Terum invariants) Failed-clone assertion is tautological: the fake stderr is built from the already-scrubbed URL — `src/lib/__tests__/teamRepo.test.ts:300`
- (low / Reuse, simplify, perf) Credential assertion `not.toContain('@')` is applied to a message that embeds the ambient temp path — `src/lib/__tests__/teamRepo.test.ts:297`
- (low / Reuse, simplify, perf) Compromised-lock handling hand-duplicated between config.ts and teamRepo.ts, and the copies diverged — `src/lib/teamRepo.ts:61`
- (low / Reuse, simplify, perf) safeWrite cleanup spawns one `git ls-files` per created path — `src/lib/teamRepo.ts:148`
- (low / Reuse, simplify, perf) `team remove` issues three independent paginated gh api calls serially — `src/commands/team.ts:80`
- (low / Reuse, simplify, perf) Non-nullable `handle` leaves 10 unreachable "no joined handle" guards behind — `src/lib/schema.ts:67`
- (low / Reuse, simplify, perf) bin test's child env sets a second HOME but leaves `GH_CONFIG_DIR` pointing at the worker HOME — `src/__tests__/bin.test.ts:19`
- (low / Reuse, simplify, perf) Fourth copy of the same gh-API handler map added to remove.test.ts — `src/commands/__tests__/remove.test.ts:168`
- (low / Reuse, simplify, perf) Comment asserts archive-only behavior that the following line does not exercise — `src/commands/__tests__/remove.test.ts:182`
- (low / Reuse, simplify, perf) `GITHUB_LOGIN_RULE` hand-copies the rule text that already lives on `githubLoginSchema`, breaking the HANDLE_RULE/TEAM_NAME_RULE convention — `src/lib/auth.ts:18`

## Triage — 20 confirmed → 7 mechanical · 10 clear · 3 fork · 0 declined

Buckets are derived in code from each finding's ratings (single-fix gate: trivial + low risk + isolated + patch = mechanical). NOTHING here has been applied.

### Mechanical — one fix, trivial / low-risk / isolated, patch supplied; batch-apply on ONE confirmation (7)

- **high — New scp credential guard still lets a password containing `/` through into config, .git/config, and messages** — `src/lib/remote.ts:114`
  - Root cause: The scp-branch credential guard enumerates credential *shapes* instead of refusing the character that can only appear in one. `SCP_FORM` (src/lib/remote.ts:28) splits `user:secret@host/path` into host=`user`, path=`secret@host/path`, so any embedded credential shows up as an extra `@` inside the scp path. The guard at src/lib/remote.ts:114 tests only two spellings of that: `/^[^/]*@/` (the `@` is in the first path segment) and `/@[^/:]*:/` (a `:` host/path separator follows the `@`). A password containing `/` combined with a slash-separated repo path matches neither, e.g. `user:AbC/dEf@github.com/org/repo.git` -> host=`user`, path=`AbC/dEf@github.com/org/repo.git` (verified by executing both regexes). The input is then accepted as a valid scp remote, so `normalizeRemote` writes `user:AbC/dEf@github.com/org/repo` into `~/.terum/skills/config.json` (bindTeam, src/lib/auth.ts:147), `remoteToGitUrl` hands the whole secret to `git clone` / `git remote add origin --` where it lands in `.git/config` (src/lib/teamRepo.ts:304, src/commands/team.ts:429), and because `stripRemoteCredentials` returns scp inputs unchanged (src/lib/remote.ts:176) `hasEmbeddedCredentials` is false, so the credential notice at src/commands/team.ts:183 and :239 never prints. That breaks the module's own header invariant (src/lib/remote.ts:10-13) and the spec's §5.1 rule that "a credential embedded in the URL is dropped before the remote reaches git, `.git/config`, or any message" (.planning/specs/2026-09-02-phase-1-build.md:138). I additionally confirmed a spelling the reviewer's proposed narrower rule still misses: `user:pa/ss@github.com` has no `/` or `:` after the `@` at all, so any shape-matching variant leaks it too. (src/lib/remote.ts:114)
  - Fix: In parseRemote's scp branch, replace the two shape regexes at src/lib/remote.ts:114 with `if (scp[3]!.includes('@')) throw unsupported(input, 'credentials in an scp-style remote are not supported');` and rewrite the comment above it to say why (a real `[user@]host:path` never carries a second `@`; the login `@` is consumed by SCP_FORM group 1, so any `@` left in the path is a credential). Add `'me:aWxs/K3Q@git.example/team.git'` and `'user:aWxs/K3Q@github.com/org/repo.git'` to the `rejected` list at src/lib/__tests__/remote.test.ts:113-118 (the loop already asserts no SECRET appears in the throw message or in stripRemoteCredentials), plus one `expect(hasEmbeddedCredentials('me:aWxs/K3Q@git.example/team.git')).toBe(true)` so the credential-notice path is covered.

```diff
--- a/src/lib/remote.ts
+++ b/src/lib/remote.ts
@@ -108,10 +108,12 @@
   const scp = SCP_FORM.exec(trimmed);
   // A one-character "host" is a Windows drive letter, never an SSH host.
   if (scp && scp[2]!.length > 1) {
-    // scp-style remotes carry no password, so `user:tok@host:path` parses as host `user` with the
-    // token inside the path. An `@` in the first path segment is that shape; refuse it.
-    // A password containing `/` moves that `@` past the first segment; the real host:path separator then follows it.
-    if (/^[^/]*@/.test(scp[3]!) || /@[^/:]*:/.test(scp[3]!)) throw unsupported(input, 'credentials in an scp-style remote are not supported');
+    // scp-style remotes carry no password, so `user:tok@host:path` parses as host `user` with the
+    // token inside the path — and a password holding `/` or `:` can move that `@` anywhere in it.
+    // The login `@` is already consumed by the pattern's first group, so any `@` LEFT in the path
+    // is a credential: refuse the character outright rather than matching credential shapes. One
+    // spelling missed here writes the secret into `config.json` and `.git/config` with no notice.
+    if (scp[3]!.includes('@')) throw unsupported(input, 'credentials in an scp-style remote are not supported');
     assertRemoteParts(input, { host: scp[2]!, path: scp[3]!, user: scp[1] ?? '', optionShapedPath: true });
     return { kind: 'scp', user: scp[1] ?? '', host: scp[2]!, path: scp[3]! };
   }
--- a/src/lib/__tests__/remote.test.ts
+++ b/src/lib/__tests__/remote.test.ts
@@ -109,6 +109,7 @@
     expect(stripRemoteCredentials('not a remote')).toBe('not a remote');
     expect(hasEmbeddedCredentials('https://u:tok@github.com/o/r.git')).toBe(true);
     expect(hasEmbeddedCredentials(' https://tok@github.com/o/r.git')).toBe(true);
+    expect(hasEmbeddedCredentials('me:aWxs/K3Q@git.example/team.git')).toBe(true);
     for (const clean of ['git@github.com:o/r.git', 'ssh://git@github.com/o/r.git', 'github.com/o/r', '/tmp/x/team.git']) expect(hasEmbeddedCredentials(clean), clean).toBe(false);
     const rejected = [
       'https://user:tok@github.com', 'https://user:tok@github.com/', '-https://user:tok@github.com/x', 'user:tok@host:path/', 'user:t@k@host:path/',
       'https://me:gh@p_leak@git.example/.git', ' https://me:p@ss@-evil.example/x',
       // a password with `/` (or `/` and `:` and `@`) fails the structured parse; the message fallback must still not echo it
       'https://me:aWxs/K3Q@git.example/team.git', 'https://me:a/b:c@d@git.example/team.git', 'me:aWxs/K3Q@git.example:team.git',
+      // the same scp-style credential spelled with a `/` host separator: no second `@` reaches an scp path
+      'me:aWxs/K3Q@git.example/team.git', 'user:aWxs/K3Q@github.com/org/repo.git',
     ];
```

- **medium — New empty-login test only covers the generic-remote half of `revoking`; the GitHub archive-only path is untested** — `/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/commands/__tests__/remove.test.ts:153`
  - Root cause: The branch narrowed login validation in `team remove` from unconditional to conditional: `const revoking = ownerRepo !== null && !args.archiveOnly;` (src/commands/team.ts:67) now gates both `parseOrExplain(githubLoginSchema, targetRaw.github, ...)` (team.ts:72) and the `login` argument handed to `archiveMutation` (team.ts:95). The new test added with that change (src/commands/__tests__/remove.test.ts:153) pins only the LEFT conjunct: it binds `config.teams.team = { remote: fixture.bare, ... }`, so `githubOwnerRepo(binding.remote)` returns null and `revoking` is false regardless of `archiveOnly`. The existing tests cover `ownerRepo !== null && !archiveOnly` (remove.test.ts:87, the malformed-login loop, which includes `github: ''`) and `ownerRepo === null && !archiveOnly` (remove.test.ts:62, refused earlier by `hostOperationAllowed`). The one GitHub archive-only test (remove.test.ts:104) removes `member`, whose github is the valid `member-gh`, and fails at the admin probe before the distinction can matter. So the combination the change exists to support — a GitHub-hosted team archiving a member whose `github` is `''` (legal per src/lib/schema.ts:55, `z.union([z.literal(''), githubLoginSchema])`) with `--archive-only` — has no test. Dropping `&& !args.archiveOnly` from team.ts:67 regresses that real user path to `Error: GitHub login for <handle> ...` and the whole suite still passes. The trailing comment at remove.test.ts:182 ("an archive-only removal never consults the login at all") asserts coverage that block does not provide: every `run(...)` in it omits `archiveOnly`. (/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/commands/__tests__/remove.test.ts:153 (coverage gap; the expression it is meant to pin is /Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/commands/team.ts:67))
  - Fix: In /Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/commands/__tests__/remove.test.ts, insert a new `it(...)` immediately after the generic-remote case (after line 163) that uses `prepared()` (GitHub REMOTE + `mappedRunner`), pushes `people/nogh.json` with `person('nogh', { github: '' })`, answers only the admin probe with `true` and rejects every other gh call, runs `{ kind: 'remove', handle: 'nogh', archiveOnly: true, config: store, runner }`, and asserts `ok: true`, `archived === ['nogh']`, and that the admin probe is the ONLY gh call (no collaborators/invitations listing, no `-X DELETE`). Also trim the stale clause from the comment at line 182 so it no longer claims coverage that block does not provide.

```diff
--- a/src/commands/__tests__/remove.test.ts
+++ b/src/commands/__tests__/remove.test.ts
@@ -161,5 +161,17 @@
     await expect(run({ kind: 'remove', handle: 'nogh', archiveOnly: true, config: store }, new ScriptedPrompter([], [true]))).resolves.toMatchObject({ ok: true });
     expect(JSON.parse(await git(['show', 'main:team.json'], fixture.bare)).archived).toEqual(['nogh']);
   });
 
+  it('archives a member who has no GitHub login on a GITHUB remote too: --archive-only never validates the login or reaches the access APIs', async () => {
+    const { fixture, store } = await prepared();
+    await pushFromSeed(fixture.seed, 'people/nogh.json', `${JSON.stringify(person('nogh', { github: '' }), null, 2)}\n`);
+    const runner = mappedRunner(REMOTE, fixture.bare, (args) => args.join(' ') === 'api repos/acme/team -q .permissions.admin'
+      ? { code: 0, stdout: 'true\n', stderr: '' }
+      : { code: 1, stdout: '', stderr: 'unexpected gh' });
+    await expect(run({ kind: 'remove', handle: 'nogh', archiveOnly: true, config: store, runner }, new ScriptedPrompter([], [true]))).resolves.toMatchObject({ ok: true });
+    expect(JSON.parse(await git(['show', 'main:team.json'], fixture.bare)).archived).toEqual(['nogh']);
+    // The admin probe is the ONLY gh call: no collaborator or invitation listing, no DELETE.
+    expect(runner.calls.filter((call) => call.command === 'gh').map((call) => call.args.join(' '))).toEqual(['api repos/acme/team -q .permissions.admin']);
+  });
+
   it('refuses to revoke a GitHub login that another ACTIVE member also declares, before archiving or touching the host', async () => {
@@ -180,7 +192,7 @@
     expect((await git(['rev-parse', 'main'], fixture.bare)).trim()).toBe(before);
     expect(runner.calls.some((call) => call.args.includes('DELETE'))).toBe(false);
-    // An ARCHIVED twin does not block, and an archive-only removal never consults the login at all.
+    // An ARCHIVED twin does not block; that an archive-only removal never consults the login is pinned above.
     await pushFromSeed(fixture.seed, 'team.json', `${JSON.stringify({ layout_version: 2, name: 'team', categories: [], global: [], projects: {}, archived: ['twin'], policy: { publish: 'pr', skill_license: 'UNLICENSED' } }, null, 2)}\n`);
     const again = mappedRunner(REMOTE, fixture.bare, host);
```

- **medium — create's new under-lock assertBindable re-check has no test, unlike join's** — `src/commands/team.ts:222`
  - Root cause: The lock-time guard added at src/commands/team.ts:222 (`assertBindable(fresh, name, remote)` inside `store.update`) is unreachable from every existing `create` test, so it is unpinned. `create` reads its snapshot at team.ts:168 and does its pre-prompt checks at team.ts:169-171 against that snapshot; the guard at 222 only fires when the config changed BETWEEN that read and the `store.update` at line 220 (config.ts:46-62 re-reads under the lockfile, so `fresh` really is a second read). No test in src/commands/__tests__/create.test.ts mutates the store during that window: the two duplicate-binding tests (create.test.ts:67-77 and :120-133) only exercise the pre-prompt checks, and the only test that wraps the runner around the scaffold push (create.test.ts:241-257) injects a push failure rather than a concurrent config write. Deleting line 222 would leave the whole suite green. The twin at team.ts:278 IS pinned, by join.test.ts:192-201, which does exactly the missing thing — a `wrapRunner` hook that calls `store.update` when it sees `git push`. The gap matters most for `create` specifically, because this is the one path where the repository is already scaffolded and pushed when the binding is refused. (src/commands/__tests__/create.test.ts:257 (no test covers the guard at src/commands/team.ts:222))
  - Fix: Append one `it` to the `team create (§6)` describe in src/commands/__tests__/create.test.ts, built exactly like join.test.ts:192-201: `wrapRunner(mappedRunner(...))` whose hook calls `store.update` to bind `git.example/new-team` to a team named `other` when it sees `git` + `push`, then run `create({ name: 'new-team', remote: publicRemote, ... })` and assert (a) it fails with 'already configured as team other', (b) `Object.keys(config.teams)` is `['other']` — the other process's entry survives and no second entry is written, (c) `git ls-remote --heads <bare>` still contains `refs/heads/main`, documenting that the scaffold landed and only the binding was refused. Test-only; no production file changes.

```diff
--- a/src/commands/__tests__/create.test.ts
+++ b/src/commands/__tests__/create.test.ts
@@ -255,5 +255,20 @@
     expect(await readdir(pathJoin(store.root, 'teams'))).toEqual([]);
     expect((await git(['ls-remote', '--heads', bare])).trim()).toContain('refs/heads/main');
   });
 
+  it('re-checks remote uniqueness under the config lock, so a remote bound by another process while this create ran is not bound twice', async () => {
+    const { root, bare } = await emptyBare();
+    const publicRemote = 'https://git.example/new-team.git';
+    const store = createConfigStore(pathJoin(root, 'local'));
+    // Another process binds this remote under a different name while the scaffold is being pushed.
+    const runner = wrapRunner(mappedRunner(publicRemote, bare), async (command, args, _options, next) => {
+      if (command === 'git' && args[0] === 'push') await store.update((config) => { config.teams.other = { remote: 'git.example/new-team', handle: 'someone' }; });
+      return next();
+    });
+    const result = await create({ name: 'new-team', remote: publicRemote, config: store, runner }, new ScriptedPrompter(['me', 'me', 'Me', 'me@example.com']));
+    expect(result).toMatchObject({ ok: false, error: expect.stringContaining('already configured as team other') });
+    expect(Object.keys((await store.read()).teams)).toEqual(['other']);
+    // The create got as far as the scaffold: the repository exists, only the second binding was refused.
+    expect((await git(['ls-remote', '--heads', bare])).trim()).toContain('refs/heads/main');
+  });
+
 });
```

- **medium — Decision-1 reclaim blanks the member's GitHub login, which then blocks `team remove` for them** — `src/commands/__tests__/join.test.ts:149`
  - Root cause: `joinMutation` rebuilds the people file with `github: identity.github` unconditionally (src/commands/team.ts:338), while every other field it reclaims is either impossible to blank (display_name/email are validated non-empty in collectIdentity, src/lib/auth.ts:72-73) or explicitly preserved (`bio`/`installed`/`declined` via `existing?.x ?? …`, team.ts:339-341). An empty GitHub login is a VALID answer — collectIdentity returns `{ ok: true, value: '' }` for a blank (src/lib/auth.ts:66-67) and `terminalPrompter.text` yields `''` when both the typed answer and the suggestion are empty (src/lib/prompt.ts:79, `answer.trim() || defaultValue || ''`). On the exact case rev-9 Decision 1 was added for — the second laptop, where there is no `config.github` and typically no gh to derive a suggestion — pressing Enter therefore rewrites the reclaimed `people/<handle>.json` from `github: 'real-ajay'` to `github: ''`. That file is the only record of the member's login, and `team remove` reads it back through `parseOrExplain(githubLoginSchema, targetRaw.github, …)` whenever it is about to revoke (src/commands/team.ts:72), so the admin can no longer revoke that member's repository access (remove.test.ts:96-100 pins the blank-login refusal). The new join test only asserts `display_name` (join.test.ts:153), so the loss is invisible. (/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/commands/team.ts:338)
  - Fix: In `joinMutation` (src/commands/team.ts:338) write `github: identity.github || existing?.github || ''` with a one-line comment saying why (a blank answer on the second laptop must not erase the login `team remove` needs), and strengthen the byEmail sub-case in src/commands/__tests__/join.test.ts:153 to assert the file keeps `github: 'Real-Ajay'` alongside `display_name: 'Ajay Three'`. A different NON-EMPTY answer still replaces the login, so a real update is unaffected; only 'answered nothing' stops being destructive. Covers reclaim, archived rejoin (join.test.ts:47) and repeat-join (join.test.ts:56) in one expression.

```diff
--- a/src/commands/team.ts
+++ b/src/commands/team.ts
@@ -330,12 +330,15 @@
     if (!archived && !mayReclaim) throw new HandleCollisionError(handle);
     if (archived && !samePerson) throw new HandleCollisionError(handle);
   }
+  // A blank answer at 'GitHub login' (the second laptop: nothing in config to suggest, and often no
+  // gh to derive one) must never ERASE the login the roster already carries — `team remove` reads it
+  // back from this file to revoke access (§6). A different, non-empty answer still replaces it.
   const person: Person = {
     ...(existing ?? {}),
     handle,
     display_name: identity.displayName,
     email: identity.email,
-    github: identity.github,
+    github: identity.github || existing?.github || '',
     bio: existing?.bio ?? '',
     installed: existing?.installed ?? [],
     declined: existing?.declined ?? [],
--- a/src/commands/__tests__/join.test.ts
+++ b/src/commands/__tests__/join.test.ts
@@ -148,7 +148,8 @@
     const byEmail = createConfigStore(pathJoin(fixture.root, 'local-email'));
     const emailIo = new ScriptedPrompter(['', 'ajay', 'Ajay Three', 'other@example.com']);
     const third = await join({ target: REMOTE, config: byEmail, runner }, emailIo);
     if (!third.ok) throw new Error(third.error);
     expect(third.value.handle).toBe('ajay');
-    expect(JSON.parse(await git(['show', 'main:people/ajay.json'], fixture.bare)).display_name).toBe('Ajay Three');
+    // The blank login answer must not erase the login already on the roster: `team remove` needs it to revoke access.
+    expect(JSON.parse(await git(['show', 'main:people/ajay.json'], fixture.bare))).toMatchObject({ display_name: 'Ajay Three', github: 'Real-Ajay' });
     // An unbound machine whose identity does not match gets the collision prompt and never touches the file.
```

- **medium — execute.test.ts's non-hook case cannot fail — the `hook === true` discriminator is untested** — `src/lib/__tests__/execute.test.ts:40`
  - Root cause: The negative case in the hook-sync test uses an empty payload (`deferred: []`, `notices: []`), so `writeHookNotices()` would emit nothing whether or not the value was classified as a hook sync. The assertion `expect(quiet.lines).toEqual([])` is therefore vacuous, and the `hook === true` clause of `isHookSync` (src/lib/execute.ts:35) — the discriminator that keeps a plain `terum-skills sync` from double-printing its notices (stdout via sync.ts:33 plus stderr via execute.ts:39) and appending a spurious "N skills need review — run `terum-skills sync`" line — has no test that can fail. `createExecute` is consumed only by src/index.ts and this test file (verified by grep), so no other test covers it; by contrast the `Array.isArray` clauses ARE guarded, since removing them makes `writeHookNotices` throw on the `success({ team: 't' })` case in test 1 and turn `ok.lines`/`ok.codes` non-empty. (/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/lib/__tests__/execute.test.ts:40 (guarding src/lib/execute.ts:35))
  - Fix: In src/lib/__tests__/execute.test.ts:40, change the `quiet` payload from `deferred: [], notices: []` to non-empty values — `success({ placed: 1, deferred: ['a'], notices: ['note'], changed: true, hook: false })` — keeping `expect(quiet.lines).toEqual([])`, and add `expect(quiet.codes).toEqual([])` so the success path's exit-code contract is asserted on this case too. With that payload, deleting `(value as Partial<SyncResult>).hook === true` from isHookSync (src/lib/execute.ts:35) makes the suite red with two extra stderr lines, which is exactly the shipped regression (notices duplicated on stdout+stderr plus the bogus re-run hint).

```diff
--- a/src/lib/__tests__/execute.test.ts
+++ b/src/lib/__tests__/execute.test.ts
@@ -36,8 +36,9 @@
     await failed.execute(async () => failure('Could not fast-forward team: offline', { placed: 0, deferred: [], notices: ['note'], changed: false, hook: true }));
     expect(failed.lines).toEqual(['note', 'Could not fast-forward team: offline']);
     expect(failed.codes).toEqual([1]);
     const quiet = sink();
-    await quiet.execute(async () => success({ placed: 1, deferred: [], notices: [], changed: true, hook: false }));
+    await quiet.execute(async () => success({ placed: 1, deferred: ['a'], notices: ['note'], changed: true, hook: false }));
     expect(quiet.lines).toEqual([]);
+    expect(quiet.codes).toEqual([]);
   });
 });
```

- **low — bin.test.ts asserts exact child stderr while inheriting the runner's full env, so any Node warning breaks it** — `src/__tests__/bin.test.ts:25`
  - Root cause: src/__tests__/bin.test.ts:19 builds the spawned bin's environment as a full spread of the vitest worker's environment (`{ ...process.env, HOME: home, USERPROFILE: home }`), so every ambient variable the developer or CI happens to have set reaches the child. Line 25 then asserts the child's *entire* trimmed stderr equals one exact string (`expect(failed.stderr.trim()).toBe('Unsupported remote: not a remote')`). Those two facts are incompatible: anything that makes Node itself write to stderr at startup counts as CLI output. I reproduced it on the repo's own Node (v25.8.2): `NODE_OPTIONS='--experimental-loader=data:text/javascript,' node -e "console.error('ok')"` prints a 3-line ExperimentalWarning before the program's own output, and `NODE_OPTIONS='--experimental-permission' node -e ...` aborts with `node: --experimental-permission is not allowed in NODE_OPTIONS`, which would also break the `--help` assertion at line 21. A `--import`-based instrumentation wrapper (tsx, OpenTelemetry) is the common real-world form. The test therefore measures the developer's shell as much as the bin's contract. Secondarily, the env override is half-applied: src/lib/__tests__/setup.ts:16 sets `GH_CONFIG_DIR` to a path under the *worker's* throwaway home, and line 19 replaces HOME/USERPROFILE with a fresh temp dir while leaving GH_CONFIG_DIR pointing outside it — inert on this path (src/lib/remote.ts:56 throws before gh is reached) but wrong the moment the test covers a gh-using verb. (/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/__tests__/bin.test.ts:19 (assertion that breaks: :25; ambient GH_CONFIG_DIR source: /Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/lib/__tests__/setup.ts:16))
  - Fix: In src/__tests__/bin.test.ts, change line 19 to `const env = { ...process.env, HOME: home, USERPROFILE: home, GH_CONFIG_DIR: join(home, '.config', 'gh'), NODE_OPTIONS: '' };`, add `join` to the existing `node:path` import on line 3, and leave the exact-stderr assertions at :25/:26 untouched. Keeps the deliberately inherited hermetic vars from vitest.config.ts (GIT_CONFIG_GLOBAL=/dev/null, GIT_CONFIG_NOSYSTEM, GIT_TERMINAL_PROMPT) that the child's own git subprocesses rely on, while removing the one ambient variable that can make Node print before the CLI does.

```diff
--- a/src/__tests__/bin.test.ts
+++ b/src/__tests__/bin.test.ts
@@ -1,5 +1,5 @@
 import { execFile } from 'node:child_process';
 import { readFile } from 'node:fs/promises';
-import { resolve } from 'node:path';
+import { join, resolve } from 'node:path';
 import { fileURLToPath } from 'node:url';
 import { promisify } from 'node:util';
@@ -17,4 +17,7 @@
     expect((await readFile(bin, 'utf8')).split('\n')[0]).toBe('#!/usr/bin/env node');
     const home = await temporaryDirectory();
-    const env = { ...process.env, HOME: home, USERPROFILE: home };
+    // The assertions below pin the child's entire stdout/stderr, so the spawn must not inherit an
+    // ambient NODE_OPTIONS (an `--import`/`--experimental-loader` wrapper prints its own startup
+    // warning), and gh's config dir has to follow this HOME, not the vitest worker's (setup.ts).
+    const env = { ...process.env, HOME: home, USERPROFILE: home, GH_CONFIG_DIR: join(home, '.config', 'gh'), NODE_OPTIONS: '' };
     const help = await run(process.execPath, [bin, '--help'], { cwd: root, env });
```

- **low — A failed post-push re-probe makes `create` give recovery advice that is guaranteed to fail** — `src/commands/team.ts:214`
  - Root cause: `src/commands/team.ts:215` reduces a three-way probe result to a two-way boolean: `const scaffolded = heads.code === 0 && heads.stdout.trim() !== '';`. The `ls-remote` re-probe has three possible outcomes — "remote has branches", "remote is empty", and "could not ask" — but the boolean folds "could not ask" into the same branch as "remote is empty". The realistic trigger for the post-`bootstrap` catch is a transport fault (the branch's own new test uses `hung up unexpectedly`), and that same fault normally makes the immediately following `git ls-remote` fail too, so the *unknown* case is the likely one, not a corner. The resulting message tells the user "The repository <remote> exists but holds no scaffold" and directs them to `team create <name> --remote <remote>` — advice that the `--remote` emptiness preflight at `src/commands/team.ts:186-187` then refuses with "already has branches", because the push did land. Note the preflight at line 185-186 gets this right (`if (heads.code !== 0) throw new Error('Cannot reach ...')`), so the recovery path is the only place that treats a failed probe as a fact. Nothing in the spec (`.planning/specs/2026-09-02-phase-1-build.md` §6, §6.0) prescribes the two-message shape, and the comment above the line only explains why the re-probe exists, not why a failed probe should count as "empty". (src/commands/team.ts:215)
  - Fix: In the post-bootstrap catch in `create` (src/commands/team.ts:210-219), replace the `scaffolded` boolean with a tri-state derived from the probe — `unknown` when `heads.code !== 0`, `pushed` when stdout is non-empty, `absent` otherwise — and add a third message for `unknown` that names the probe error and gives both routes conditionally (`team join <remote>` if the repo has a main branch, `team create <name> --remote <remote>` if it is still empty). Extend the collocated test `src/commands/__tests__/create.test.ts:241` with a case whose `wrapRunner` hook lets the push land, reports it failed, and then fails the re-probe, asserting the message does not claim 'holds no scaffold'.

```diff
--- a/src/commands/team.ts
+++ b/src/commands/team.ts
@@ -207,13 +207,17 @@
     await store.ensureRoot();
     try {
       await bootstrap(remote, clone, name, identity, runner);
     } catch (error) {
       const reason = error instanceof Error ? error.message : String(error);
       // Re-probe before claiming anything: a failure after the push leaves a scaffolded remote that
       // `team join` can finish from, and a retry with `--remote` would only be refused as non-empty.
+      // The probe usually fails for the same reason the push did, so an unreachable remote is a
+      // third outcome, not evidence of emptiness: claiming "no scaffold" there would send the user
+      // to a `team create --remote` retry that the emptiness preflight above refuses.
       const heads = await runner.run('git', ['ls-remote', '--heads', '--', remoteToGitUrl(remote)]);
-      const scaffolded = heads.code === 0 && heads.stdout.trim() !== '';
-      throw new Error(scaffolded
-        ? `${reason}\nThe scaffold was pushed to ${remote} but the local clone could not be completed; run \`team join ${remote}\` to finish, or delete the repository and retry.`
-        : `${reason}\nThe repository ${remote} exists but holds no scaffold. Fix the cause and retry with \`team create ${name} --remote ${remote}\`, or delete the repository.`);
+      const scaffold = heads.code !== 0 ? 'unknown' : heads.stdout.trim() !== '' ? 'pushed' : 'absent';
+      throw new Error(scaffold === 'unknown'
+        ? `${reason}\nWhether the scaffold reached ${remote} could not be checked: ${(heads.stderr || heads.stdout).trim()}. Look at the repository: if it has a main branch run \`team join ${remote}\`; if it is still empty retry with \`team create ${name} --remote ${remote}\`.`
+        : scaffold === 'pushed'
+          ? `${reason}\nThe scaffold was pushed to ${remote} but the local clone could not be completed; run \`team join ${remote}\` to finish, or delete the repository and retry.`
+          : `${reason}\nThe repository ${remote} exists but holds no scaffold. Fix the cause and retry with \`team create ${name} --remote ${remote}\`, or delete the repository.`);
     }
--- a/src/commands/__tests__/create.test.ts
+++ b/src/commands/__tests__/create.test.ts
@@ -239,4 +239,4 @@
   });
 
-  it('a failed scaffold push leaves no staging directory and says the remote holds no scaffold; a failure after the push says to team join instead', async () => {
+  it('a failed scaffold push leaves no staging directory and says the remote holds no scaffold; a failure after the push says to team join instead, and an unreachable re-probe says the outcome is unknown rather than claiming the remote is empty', async () => {
     const { root, bare } = await emptyBare();
@@ -254,4 +254,19 @@
     expect(later).toMatchObject({ ok: false, error: expect.stringMatching(/hung up unexpectedly[\s\S]*run `team join https:\/\/git\.example\/boot\.git`/) });
     expect(await readdir(pathJoin(store.root, 'teams'))).toEqual([]);
     expect((await git(['ls-remote', '--heads', bare])).trim()).toContain('refs/heads/main');
+    // The re-probe fails too — the fault that broke the push usually breaks it as well. The scaffold
+    // IS on the remote, so the message must not claim it is empty and send the user back to create.
+    const unreachable = await emptyBare();
+    let pushed = false;
+    const offlineAfterPush = wrapRunner(mappedRunner(publicRemote, unreachable.bare), async (command, args, _options, next) => {
+      if (command === 'git' && args[0] === 'ls-remote' && pushed) return { code: 1, stdout: '', stderr: 'Could not resolve host: git.example' };
+      if (command === 'git' && args[0] === 'push') { await next(); pushed = true; return { code: 1, stdout: '', stderr: 'hung up unexpectedly' }; }
+      return next();
+    });
+    const unchecked = await create({ name: 'boot', remote: publicRemote, config: store, runner: offlineAfterPush }, new ScriptedPrompter(['me', 'me', 'Me', 'me@example.com']));
+    if (unchecked.ok) throw new Error('expected the create to fail');
+    expect(unchecked.error).toMatch(/hung up unexpectedly[\s\S]*could not be checked[\s\S]*team join https:\/\/git\.example\/boot\.git/);
+    expect(unchecked.error).not.toContain('holds no scaffold');
+    expect((await git(['ls-remote', '--heads', unreachable.bare])).trim()).toContain('refs/heads/main');
+    expect(await readdir(pathJoin(store.root, 'teams'))).toEqual([]);
+    expect((await store.read()).teams).toEqual({});
   });
```


### Clear — one fix clearly wins, but not mechanical; confirm each, or hand to /parallel-fix (10)

- **high — Schema tightening of teams.<team>.handle bricks every verb for a config the previous build wrote (no migration, no in-tool repair)** — `src/lib/__tests__/config.test.ts:36`
  - Root cause: `teamConfigSchema` in src/lib/schema.ts:67 was tightened from `handle: handleSchema.nullable()` to `handle: handleSchema`, but the read path was not given any tolerance for the one on-disk shape the *previous* build wrote. On origin/main, `login --team X --remote Y` called `bindTeam(fresh, team, { remote, token })` with no handle, and old `bindTeam` resolved that to `handle: entry.handle === undefined ? (current?.handle ?? null) : entry.handle` → `"handle": null` in ~/.terum/skills/config.json. The branch deletes that verb (src/commands/login.ts is now bare) and asserts the invariant in the schema comment ("so it is never null"), yet nothing rewrites or ignores the rows the retired verb already left behind. Because `createConfigStore().read()` is a single whole-file `parseJson(configSchema, ...)` (src/lib/config.ts:32) and `update()` re-reads through the same function under the lock (src/lib/config.ts:60), one legacy row poisons the entire config: every verb dies with `Invalid ~/.terum/skills/config.json: teams.t.handle: Invalid input: expected string, received null` — including `sync --hook` on every session start and including `team join`, the only verb that could rewrite the row to a valid handle (it calls `store.read()` at src/commands/team.ts:239 before anything else). The error text names the field but no repair, so the only exit is hand-editing or deleting config.json. The same diff deliberately kept read compatibility for the sibling retired key (`token`, tolerated via `.passthrough()` + `delete current.token` at src/lib/auth.ts:146, and promised in spec §5.4 rev 9: "an old `config.json` that still carries the key is read and the key ignored"), which shows the read path is the intended repair point and that this row shape was simply missed. The new test at src/lib/__tests__/config.test.ts:35-36 pins the brick as intended behaviour. (/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/lib/schema.ts:67 (blast radius via src/lib/config.ts:32 and :60; pinned by src/lib/__tests__/config.test.ts:35-36))
  - Rated: moderate / medium risk / pattern — siblings: Same mistake class — a value the previous build wrote to ~/.terum/skills/config.json is re-interpreted by new code with no read-time migration — appears twice in this diff. Sweep worklist:
1. /Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/lib/schema.ts:67 — `teams.<team>.handle` tightened to non-null; the finding site (fix per the recommended option). Blast radius: /Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/lib/config.ts:32 (`read`) and :60 (`update` re-reads under the lock), so every verb fails, including the repair verb `team join` (/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/commands/team.ts:239) and `sync --hook` (/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/commands/sync.ts:39).
2. /Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/lib/remote.ts:150 — `normalizeRemote` for local paths changed from `file:${stripGitSuffix(remote.path)}` (origin/main) to `file:${remote.canonical}`, which now KEEPS a `.git` suffix. A `teams.<team>.remote` string persisted by the previous build for a local remote (`file:/srv/team` for `/srv/team.git`) no longer equals what the new normalizer produces for the same input, so `teamByRemote` (/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/lib/auth.ts:153) misses the row and `assertBindable` (same file, :163) then rejects or double-binds: `team join /srv/team.git` on an already-configured team hits `Team <name> is configured for file:/srv/team, not file:/srv/team.git`. Not fatal to every verb like #1, but it is the same missing normalize-on-read step and the same file heavily exercised by the `file:` remotes in the test suite.
3. Precedent to mirror, not a bug: /Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/lib/auth.ts:145-147 (`delete current.token`) plus `.passthrough()` on `teamConfigSchema` — the already-agreed shape for tolerating a retired key. Whatever is done for #1 and #2 should look like this and be documented in the same schema comment block (src/lib/schema.ts:62-66), which currently claims old-key tolerance while #1 contradicts it.
Checked and clean: /Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/lib/schema.ts:53-55 (`personSchema.github`) was widened to `z.union([z.literal(''), githubLoginSchema])`, so old roster files keep parsing; the remaining `configSchema` fields (schema.ts:70-79) are unchanged by this branch.
  - Why one fix: In plain English: the tool already promises that a config file written by the old build still opens (the retired `token` key is deliberately read and ignored — spec §5.4 rev 9 and the comment on src/lib/schema.ts:63-66 say so). Given that promise, silently bricking on the *other* leftover from the same retirement is an inconsistency, not a policy — so the fix that keeps the promise AND keeps the new 'a team entry always has a proven handle' rule is the one to take. The alternatives are each strictly worse on the axis that matters: option 2 buys a few lines of savings by giving up the invariant the whole rev-9 change was for (and by leaving 'handle can be null' in the type forever), and option 3 still leaves a user hand-editing a file the tool is supposed to own — it only makes the dead end polite. The one thing a human could overrule: if the team decides that, at version 0.0.0 and unpublished, no old config files exist worth protecting, option 3 becomes acceptable — but then the schema comment should stop implying old keys are handled.
  - **Option 1: Drop legacy handle-less rows at read (keep the non-null invariant) (recommended)** — Depth 4/4 · Cost 2/4. In src/lib/schema.ts, keep `teamConfigSchema` as `{ remote, handle: handleSchema }` (line 67) but wrap the `teams` field at line 74 in a preprocess that removes entries whose `handle` is exactly `null` — the one shape the retired `login --team --remote` wrote — before the record is parsed: `teams: z.preprocess((value) => (value && typeof value === 'object' && !Array.isArray(value) ? Object.fromEntries(Object.entries(value as Record<string, unknown>).filter(([, entry]) => !(entry && typeof entry === 'object' && (entry as { handle?: unknown }).handle === null))) : value), z.record(z.string(), teamConfigSchema))`, with a comment naming it as the rev-9 counterpart of the `token` drop in src/lib/auth.ts:146. The next `store.update()` persists the repaired shape (read → mutate → atomic write), and `team join <remote> --as <name>` re-creates the entry with a proven handle. Replace the pinned reject in src/lib/__tests__/config.test.ts:35-36 with a case asserting the legacy row is dropped and the rest of the file still parses, and keep the existing stale-`token` case above it. Anything other than `null` (a missing key, a malformed string) still fails loudly, so real corruption is not swallowed. *Wins if:* The project keeps the promise it already makes in spec §5.4 rev 9 — an old config.json still opens — while new code keeps relying on `TeamConfig.handle: string`; this is the only option that satisfies both.
  - Option 2: Parse-tolerant, use-strict (revert the tightening) — Depth 3/4 · Cost 1/4. Revert src/lib/schema.ts:67 to `handle: handleSchema.nullable()`, restore the `?? undefined` that origin/main had at the `boundHandle` read (src/commands/team.ts:250 — main's line 204 was `existing?.[1].handle ?? undefined`, needed because `joinMutation(tree, identity, boundHandle: string | undefined)` at team.ts:317 takes `undefined`, and `undefined` is exactly the "machine unbound for this team" case the reclaim rule wants), and restore the deleted round-trip test in src/lib/__tests__/config.test.ts. Every consumer already null-guards and keeps working unchanged: src/commands/install.ts:65, src/commands/uninstall.ts:42 and :55, src/commands/share.ts:38, :107, :115, :145, src/commands/sync.ts:85, :132, :160, src/commands/team.ts:54. *Wins if:* You want the smallest, most revert-safe change and are content that `handle` stays nullable in the type forever — the guards stay live and 'never null' stays a writer-only convention rather than a parsed invariant.
  - Option 3: Actionable dead-end (message only) — Depth 1/4 · Cost 0/4. Leave the schema tight and catch the failure in `createConfigStore().read()` (src/lib/config.ts:31-36): when the ZodError's only issues are `teams.<name>.handle` receiving `null`, rethrow with the repair spelled out — e.g. `~/.terum/skills/config.json holds a team entry written by a build before rev 9 (teams.<name> has no proven handle). Remove that entry (or delete config.json) and run \`team join <remote> --as <name>\`.` Keep the test at config.test.ts:36 but assert the new guidance text as well. *Wins if:* The maintainers decide an unpublished 0.0.0 tool owes no on-disk migration at all, and the only real obligation is that the failure not dead-end the user — then the schema comment should also say pre-rev-9 team rows are unsupported rather than implying old keys are handled.
- **high — Compromised safeWrite lock aborts the attempt but still hard-resets the clone another writer now owns (silent drop)** — `src/lib/teamRepo.ts:146`
  - Root cause: The `finally` cleanup in `safeWrite` is unconditional, so the abort path the branch just introduced ends up performing the exact destructive act the abort exists to prevent. `let compromised = false` / `onCompromised: () => { compromised = true; }` (src/lib/teamRepo.ts:90-96) plus `if (compromised) throw new Error(lostLock(root));` at :100 and :134 only stop THIS process from pushing; the throw exits the `try` and lands in the cleanup at :143-151, which unconditionally runs `git fetch origin` + `git reset --hard origin/main` (:146-147) and `removeCreated` for every path in `created` (:148-151) with `cwd: root` — the same shared clone that, by the definition of `compromised`, another process now holds the lock on and is actively writing in. So process A's abort resets process B's HEAD/worktree mid-operation. If B is between its commit (:133) and its push (:135), B then pushes a HEAD that equals origin/main, git exits 0 ("Everything up-to-date"), and B returns `{ changed: true, pushedTo: 'main' }` for a join/install/share that was silently discarded. The sibling call site with the identical new pattern gets this right: src/lib/config.ts:62 checks `compromised` and skips the destructive `writeAtomically` entirely, leaving its `finally` to do nothing but `release()`. teamRepo's comment at :144 ("Cleanup can never change the outcome: the next safeWrite fetches and hard-resets anyway") is the argument for why the cleanup is skippable — it just was never wired to the new flag. No test exercises the compromised path in either file. (src/lib/teamRepo.ts:143-151 (unconditional cleanup; the destructive line is src/lib/teamRepo.ts:147))
  - Rated: moderate / low risk / isolated
  - Why one fix: One option clearly wins. The bug is that a process which has lost its turn still wipes the shared folder another process is writing in, and the fix is to tell it "you lost your turn, so don't touch anything" — which is exactly what the sibling config file already does. It cannot change what happens on a normal run, because the flag it checks is false unless the lock was actually stolen. The runner-up (option 2) only buys tidier leftovers: after option 1, an aborted attempt can leave a few stray files in the local team folder that nothing reads and nothing publishes, and it pays for that with a second lock grab on an error path. The third option is a real redesign of how writes work and is not warranted by this finding.
  - **Option 1: Gate the finally cleanup on !compromised (mirror config.ts) (recommended)** — Depth 2/4 · Cost 1/4. In src/lib/teamRepo.ts, wrap the three cleanup steps inside the outer `finally`'s `try` (lines 146-151: `await git(['fetch','origin'])`, `await git(['reset','--hard','origin/main'])`, and the `for (const path of created) { ... removeCreated(...) }` loop) in `if (!compromised) { ... }`. Leave the `catch {}` and the inner `finally { await release().catch(() => undefined); }` (:154-157) exactly as they are, so the lock is always released and the real error still propagates. Extend the comment at :144 to state the reason: when the lock was lost the clone belongs to another process, and that process's own `fetch` + `reset --hard origin/main` at the top of its loop (:101-102) normalizes the tree, so touching it here can only destroy its work. Add the missing coverage in a NEW test file (e.g. src/lib/__tests__/teamRepo.compromised.test.ts) that does `vi.mock('proper-lockfile')` with a `lock()` that calls the caller's `onCompromised()` before resolving its release, then runs `safeWrite` against a real fixture clone through a spying Runner and asserts (a) it rejects with the `lostLock` message, (b) no `reset --hard` is issued after the throw, and (c) the clone's HEAD and `git status --porcelain` are unchanged. Keep the mock in its own file so the real-lock 8-way concurrency test in src/lib/__tests__/teamRepo.test.ts:265+ keeps using the genuine lock. *Wins if:* Wins whenever the goal is to close the silent-drop hole with the least new machinery in a concurrency-critical path: it is the same guard the already-correct sibling (src/lib/config.ts:51-67) uses, it cannot change behaviour on any run where the lock is never lost (so §12's clean-clone assertions and spec invariant 24 still hold on the exhaustion path they were written for), and it is a plain revert if it ever misbehaves.
  - Option 2: Re-acquire the lock in the finally, clean only if we get it — Depth 3/4 · Cost 2/4. Same gate as option 1, but instead of skipping cleanup outright when `compromised`, try to take the lock again inside the `finally` — `await lockfile.lock(root, { lockfilePath: ..., realpath: false, stale: 60_000, retries: 0 })` — and run the fetch/reset/removeCreated block only if that acquire succeeds, releasing it afterwards; on ELOCKED (another process really is in the clone) skip everything. This keeps the disk-hygiene half of the cleanup that option 1 gives up: `git reset --hard` in the next writer does NOT delete untracked files, so the paths this aborted attempt created stay in the clone until something else removes them. *Wins if:* Wins if leftover untracked files in ~/.terum/skills/teams/<team> are considered unacceptable rather than merely untidy — e.g. if a later reviewer wants the §12 'clean status' property to hold even on the lost-lock path. Costs a second lock acquisition on an error path with its own new failure modes (acquire can throw for reasons other than ELOCKED, and can itself be compromised), and needs tests for both branches.
  - Option 3: Stop sharing one working tree per clone — Depth 4/4 · Cost 3/4. Remove the hazard rather than the symptom: give each safeWrite a private working tree — `git worktree add` a temp dir off the team clone (or a per-process clone) — so mutate → apply → add → commit → push never run in a directory another process can reset, and the per-clone lock guards only the shared object store. The `finally` then cleans only this process's private tree, and there is nothing for a lost lock to corrupt. Touches openTeamRepo/safeWrite/cloneTeam in src/lib/teamRepo.ts, the §5.4 on-disk layout, and every safeWrite test. *Wins if:* Wins only if lost-lock races are actually observed in the field, because it is the sole option that also closes the remaining window between the `compromised` check at :134 and the push at :135 (and every other unchecked instant). It rewrites the write path the spec pins at §6.0/§5.4 and invalidates the existing concurrency tests, so it is not justified by this finding alone.
- **medium — New bin test compiles into the repo's own dist/, breaking the suite's hermeticity and racing any concurrent build** — `/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/__tests__/bin.test.ts:15`
  - Root cause: src/__tests__/bin.test.ts:15 shells out to `tsc -p tsconfig.build.json` with `cwd: root` and no `--outDir` override, and tsconfig.build.json:4 sets `"outDir": "dist"` relative to the repo root. So the act of running `vitest run` compiles the whole project into the developer's own `<repo>/dist/` — the exact directory `npm run build`/`prepack` emit into and that package.json `bin`/`files` ship (package.json:11,14,19-20). Line 16 then reads the artifact back out of that shared location. Two consequences follow mechanically: (a) a concurrent `npm run build` (or a second vitest worker, or a CI job that builds and tests in parallel) has a second `tsc` writing the same `dist/index.js`, so the `expect((await readFile(bin,'utf8')).split('\n')[0]).toBe('#!/usr/bin/env node')` read can land mid-write; (b) the run leaves a full build behind, unlike every other fixture in the suite, which routes scratch state through `temporaryDirectory()` into `os.tmpdir()` (src/lib/__tests__/fixtures.ts:38-42) and is rm'd in the shared `afterEach` (src/lib/__tests__/setup.ts:22-26). vitest.config.ts:3-6 states the invariant this violates in so many words: "The suite must not depend on, or touch, the developer's ambient state". Secondary defects at the same line: the compiler is reached by a hardcoded `node_modules/typescript/bin/tsc` path rather than package resolution, and the explicit `120_000` timeout on line 27 restates `testTimeout: 120_000` from vitest.config.ts:13. (/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/__tests__/bin.test.ts:15-16 (with /Users/ryanliu/Documents/Terum/terum-codex/m1-wave/tsconfig.build.json:4 supplying `"outDir": "dist"`))
  - Rated: moderate / low risk / isolated
  - Why one fix: Option 1 is the only one that actually delivers what the test suite promises about itself, and it costs about four lines in a single test file. The project already writes the rule down in vitest.config.ts ("The suite must not depend on, or touch, the developer's ambient state") and already ships the machinery to obey it (every other fixture builds its scratch state in the system temp folder and deletes it afterwards); option 1 just makes this one test follow the same house rule. The alternatives are cheaper but each leaves something behind: option 2 still writes inside the developer's checkout, option 3 stops plain `npm test` from covering the shipped command at all. Nothing about the choice depends on a product or design judgement, so there is no fork to walk. The one caveat worth flagging to whoever applies it: the obvious one-line version of this fix (just point the compiler at a temp folder) does not work — the compiled program still needs to find its dependencies and be recognised as a modern-JavaScript module, which is why option 1 adds two small setup lines rather than one.
  - **Option 1: Hermetic temp emit with a minimal runnable package layout (recommended)** — Depth 3/4 · Cost 1/4. In /Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/__tests__/bin.test.ts, replace the in-repo build with a scratch build: `const out = await temporaryDirectory();` (already auto-cleaned via TEMP_DIRS/setup.ts), then make `out` runnable before compiling — `await writeFile(join(out,'package.json'), '{"type":"module"}\n')` and `await symlink(resolve(root,'node_modules'), join(out,'node_modules'), 'junction')` — then run `tsc -p tsconfig.build.json --outDir <out>` (CLI options override the project's `outDir`; `rootDir: src` keeps the entry at `<out>/index.js`) and set `const bin = join(out,'index.js')`. Resolve the compiler with `createRequire(import.meta.url).resolve('typescript/bin/tsc')` instead of the hardcoded path (verified safe: node_modules/typescript@6.0.3 has no `exports` field, and `bin.tsc` is `./bin/tsc`). Optionally drop the redundant `120_000` on line 27. NOTE: the two scaffolding lines are not optional polish — the emitted tree imports bare specifiers (`dist/index.js:2` `from 'commander'`, plus yaml/proper-lockfile/zod), so a bare `--outDir <tmpdir>` dies with ERR_MODULE_NOT_FOUND; and with no package.json in scope, `.js` has no `"type": "module"` and only survives via Node's module-syntax detection, which can print a reparse warning on stderr and break the exact `expect(failed.stderr.trim()).toBe('Unsupported remote: not a remote')` assertion. *Wins if:* Wins whenever the suite's stated invariant (vitest.config.ts:3-6, 'must not depend on, or touch, the developer's ambient state') is meant literally: it is the only option after which `vitest run` writes nothing inside the checkout, and the ~4 extra lines live in one test file with zero production impact and fail loudly, not silently, if wrong.
  - Option 2: Unique scratch subdir inside the already-gitignored dist/ — Depth 2/4 · Cost 0/4. Keep the build in the checkout but stop it colliding with the shipped path: in src/__tests__/bin.test.ts, `await mkdir(join(root,'dist'),{recursive:true})`, `const out = await mkdtemp(join(root,'dist','bin-test-'))`, push `out` onto the exported `TEMP_DIRS` (src/lib/__tests__/fixtures.ts:7) so the existing afterEach removes it, run `tsc -p tsconfig.build.json --outDir <out>`, and read `join(out,'index.js')`. No symlink and no synthetic package.json are needed: from `<root>/dist/bin-test-x/index.js` Node's package scope lookup still reaches `<root>/package.json` (`"type":"module"`) and bare specifiers still resolve up to `<root>/node_modules`. *Wins if:* Wins if the team reads the invariant as 'do not clobber or race the developer's build output' rather than 'do not write in the checkout at all' — it removes the overwrite, the race and the stale artifact in ~3 mechanical lines with none of option 1's resolution scaffolding, at the price of still creating dist/ and leaving a subdir behind if a crash skips cleanup.
  - Option 3: Move the compile out of the test into an explicit build-then-test script — Depth 2/4 · Cost 1/4. Delete the `tsc` invocation from src/__tests__/bin.test.ts so the test only reads `dist/index.js`; exclude `src/__tests__/bin.test.ts` from vitest.config.ts's `include`; add `"test:bin": "npm run build && vitest run src/__tests__/bin.test.ts"` to package.json scripts and wire it into CI after `npm run build`. *Wins if:* Wins if the ~whole-project tsc compile on every `npm test` and every watch-mode rerun is the dominant pain (it also stops re-implementing package.json's `"build"` in a second place), and the team accepts that plain `npm test` no longer covers the shipped bin and that the artifact test still depends on ambient dist/.
- **medium — Shared-GitHub-login revocation guard fails open on any unparseable people/*.json, silently revoking an active teammate's access** — `src/commands/team.ts:146`
  - Root cause: The guard conditions its authority on data quality it explicitly cannot assume. `assertLoginUnclaimed` (src/commands/team.ts:141-150) is the last thing standing between `team remove <handle>` and `gh api -X DELETE repos/<owner>/<repo>/collaborators/<login>` (team.ts:99). To decide whether another ACTIVE member also claims that login, it insists the entire sibling document validate against `personSchema` first — `try { person = parseJson(personSchema, treeText(tree.before(path)!), path); } catch { continue; }` (team.ts:146) — and on any failure it `continue`s, which is indistinguishable from "this file declares a different login". `personSchema` (src/lib/schema.ts:50-59) requires handle, display_name, a valid email, github, bio, installed, declined, so one legacy or hand-edited `people/*.json` missing `bio` (or carrying a non-RFC email) silently removes that member from the exclusivity check. The contradiction is five lines up in the same diff: team.ts:68-72 deliberately reads the TARGET's login from the raw JSON because "a legacy repository can carry a value written before the schema existed" — the guard then assumes no such file exists for everyone else. Nothing downstream compensates: `guard()` (src/lib/guard.ts:38-48) only inspects changed paths for `team-remove`, and `regenerateReadmeInTree`, which does parse every person strictly, runs only for non-GitHub remotes (src/lib/teamRepo.ts:113), i.e. exactly the path where `revoking` is false. The same `catch` also swallows a TypeError from the `tree.before(path)!` assertion, so a future mutation that sets a people path before calling `archiveMutation` would disable the guard silently rather than crash. Secondary inconsistency in the same loop: line 144 keys the "skip the target" test off the FILENAME while line 147 keys the archived lookup off the parsed `person.handle`, so a file whose in-body handle disagrees with its name is judged against the wrong archived entry. (src/commands/team.ts:146)
  - Rated: moderate / low risk / isolated
  - Why one fix: Option 1 covers everything option 2 covers and costs less operationally. Any file option 2 would block on, option 1 still inspects — it reads the one field that decides the question (`github`) straight out of the raw JSON, so a twin declaring the same login is caught whether or not the rest of the record is valid; the only case option 1 still refuses outright is a file whose bytes are not JSON at all, where there genuinely is nothing to compare. Option 2's extra strictness buys no additional protection for the teammate at risk and adds a new way for a healthy removal to be blocked by an unrelated stale record. Option 3 is the same fix plus a refactor to keep the two readers in sync forever; worth doing only if the person document's shape is expected to keep moving. No product decision is pending here — the intent is already written down in the guard's own docblock (team.ts:125-129) and in the legacy-data comment at team.ts:68-70.
  - **Option 1: Compare the raw github field; refuse only when the bytes are unreadable (recommended)** — Depth 3/4 · Cost 1/4. Rewrite the loop body of assertLoginUnclaimed (src/commands/team.ts:141-150) so the sibling's login is read the same lenient way remove() already reads the target's (team.ts:71): take `const source = tree.before(path)`, throw a named error if it is undefined (replacing the `!` assertion), `JSON.parse(treeText(source))` inside a try that THROWS a message naming the unreadable path instead of `continue`, then compare `String((raw as {github?: unknown} | null)?.github ?? '').trim().toLowerCase()` against `login.toLowerCase()`. Key the archived lookup off the filename capture `match[1]` (already lowercase, the roster's canonical key) rather than a parsed `person.handle`, which removes the line 144 / line 147 split. Drop the now-unused strict parse. Add a case to src/commands/__tests__/remove.test.ts beside the twin test at line 165 that pushes a `people/twin.json` built from `person('twin', { github: 'Member-GH' })` with `bio` deleted (fails personSchema, still valid JSON) and asserts the removal is refused, main is unmoved, and no DELETE reaches the runner; optionally a second case pushing non-JSON bytes asserting the refusal names the path. *Wins if:* Wins whenever a real roster can contain a legacy or hand-edited people file: it protects the co-claimant using the only evidence that matters (the github field), keeps every unrelated schema defect from blocking unrelated removals, and reuses the raw-read strategy the diff already chose for the target.
  - Option 2: Fail closed on any personSchema failure — Depth 3/4 · Cost 1/4. Keep the strict `parseJson(personSchema, ...)` at src/commands/team.ts:146 but replace `catch { continue; }` with a throw: `Refusing to remove ${targetHandle}: ${path} does not parse as a member record, so it cannot be proved to declare a different GitHub login; fix the roster before revoking access.` Same new test case in remove.test.ts. *Wins if:* Wins if the team wants a broken roster to be an operator-visible stop sign rather than something the tool reasons around — i.e. any invalid people/*.json should block every access-revoking removal until a human repairs it, accepting that an unrelated bad file blocks unrelated removals.
  - Option 3: One shared raw-login reader for target and siblings — Depth 4/4 · Cost 2/4. Extract a helper (e.g. `declaredLogin(json: unknown): string` in src/commands/team.ts or src/lib/schema.ts) that pulls `github` out of a raw parsed document and normalises it, then use it BOTH at team.ts:71-72 for the target (still piping the result through githubLoginSchema, since that value becomes a REST path segment) and inside assertLoginUnclaimed for every sibling; refuse when a sibling's bytes are not JSON. Same test additions as option 1, plus a unit test for the helper. *Wins if:* Wins if you expect the person document's shape to keep changing (github moving, becoming a list of logins) and want one reader that both the revoke-target path and the exclusivity guard must go through, so the two can never drift apart again.
- **medium — remoteName() yields an empty team name for the `.git`-directory local remote this batch newly declares supported** — `src/lib/__tests__/remote.test.ts:149`
  - Root cause: `remoteName` strips a `.git` suffix from the basename without checking that anything is left. src/lib/remote.ts:207 is `return path.slice(path.lastIndexOf('/') + 1).replace(/\.git$/i, '');` — when the last path segment IS `.git` (the gitdir of a non-bare repo, e.g. `/tmp/x/team/.git`), the basename is `.git` and the strip leaves `''`. This batch made that input first-class: `filePath()` (remote.ts:81-85) now keeps `.git` as part of a local remote's identity, and remote.test.ts:27/31 newly assert `/tmp/x/team/.git` normalizes to `file:/tmp/x/team/.git` and round-trips through `remoteToGitUrl`. Verified by running the built module (`node -e` against dist/lib/remote.js): `/tmp/x/team/.git -> normalize "file:/tmp/x/team/.git", remoteName ""`; same empty result for `/tmp/x/team/.git/`, `file:///srv/a/.git`, and — via the host path, through `cleanPath` — `https://gitlab.com/org/.git`, `git@host.example:org/.git`, `myhost:org/.git`. The only consumer is src/commands/team.ts:245 `parseOrExplain(teamNameSchema, existing?.[0] ?? args.as ?? remoteName(target.remote), 'team name')`; `teamNameSchema` (schema.ts:14, `/^[A-Za-z0-9][A-Za-z0-9._-]{0,99}$/`) rejects `''`, so `team join /srv/x/team/.git` fails with "Invalid team name: a team name is 1-100 characters…" instead of defaulting to `team`, contradicting the spec line "The team name is the repo basename unless `--as <name>` overrides" (.planning/specs/2026-09-02-phase-1-build.md:299) and git's own `clone` directory naming (which strips a trailing `/.git`). The test block that would have caught it (remote.test.ts:149-153) omits this spelling, and the idempotence loop only asserts `not.toThrow` (remote.test.ts:57). (src/lib/remote.ts:207)
  - Rated: trivial / low risk / pattern — siblings: Same mistake — stripping `.git` without checking what remains — at one other location, plus the test-shape gap that hid it:
1. src/lib/remote.ts:59-61 `stripGitSuffix` (via `cleanPath`, remote.ts:63-65): strips the `.git` characters and leaves the separator, so `cleanPath('org/.git') === 'org/'` (verified by execution). Consequences: `normalizeRemote('https://gitlab.com/org/.git') === 'gitlab.com/org/'`, which re-normalizes to `'gitlab.com/org'` — a violation of the idempotence invariant asserted at src/lib/__tests__/remote.test.ts:51-59 — and `sameRemote('https://gitlab.com/org/.git', 'https://gitlab.com/org')` is false even though both name one repository. Same for `git@host.example:org/.git` and `myhost:org/.git`. Pre-existing (unchanged in this diff) but wrong on its own merits.
2. src/lib/__tests__/remote.test.ts:57 — `expect(() => remoteName(once), input).not.toThrow()` inside the idempotence loop is the only coverage the `/tmp/x/team/.git` entry (added at line 52) gets from `remoteName`; a value-blind assertion cannot catch an empty result. Same shape at lines 55-56 for `remoteToGitUrl` and `hostOperationAllowed`.
No other consumer of `remoteName` exists (only src/commands/team.ts:245), so no further call-site sweep is needed.
  - Why one fix: Option 0 clearly wins. It is a one-expression change that makes `team join /srv/x/team/.git` name the team `team` — exactly what git itself does and what the spec promises — and I confirmed by running the real module that it changes no other remote's name. The extra work in option 1 fixes a different, related bug (two server remotes that name the same repo compare as different), which is already on the sweep list and can be decided on its own. Option 2 is not a bug fix at all: it would change the product's rule that a folder path is a team's identity, which this batch and the spec deliberately locked in.
  - **Option 1: Parent segment when the basename is `.git` (drop a trailing `/.git` before taking the basename) (recommended)** — Depth 2/4 · Cost 0/4. In src/lib/remote.ts:202-208, between computing `path` and returning, drop trailing slashes and a trailing `/.git` component: `const repoPath = path.replace(/\/+$/, '').replace(/\/\.git$/i, ''); return repoPath.slice(repoPath.lastIndexOf('/') + 1).replace(/\.git$/i, '');`. This matches what `git clone /srv/x/team/.git` names the directory (`team`). Collocated test: add `expect(remoteName('/tmp/x/team/.git')).toBe('team');` to the case list at src/lib/__tests__/remote.test.ts:149-153, and replace the value-blind `expect(() => remoteName(once), input).not.toThrow();` at remote.test.ts:57 with an assertion that the name is non-empty (e.g. `expect(remoteName(once), input).not.toBe('')`) so the loop can catch this class again. Verified against the built module: every existing expectation is unchanged (`team-skills`, `Team-Skills`, `team`, `repo`, `re:po`, `myhost:team.git`→`team`) and the broken spellings become `/tmp/x/team/.git`→`team`, `file:///srv/a/.git`→`a`, `https://gitlab.com/org/.git`→`org`, `git@host.example:org/.git`→`org`. The degenerate `/.git` (a repo at the filesystem root) still yields `''` and therefore still the existing "Invalid team name" error telling the user to pass `--as`. *Wins if:* Wins whenever the goal is to make the newly-supported `/path/.git` spelling produce the name git itself would, without touching remote identity/comparison semantics; it fixes both the local and the host spelling of the empty name in one expression and cannot change any other remote's name.
  - Option 2: Also fix `stripGitSuffix` so a `/.git` path component is dropped in host paths — Depth 3/4 · Cost 1/4. Option 0 plus src/lib/remote.ts:59-61: make `stripGitSuffix` remove a whole trailing `/.git` component, e.g. `const trimmed = path.replace(/\/+$/, ''); return trimmed.replace(/\/\.git$/i, '').replace(/\.git$/i, '');`. Today `cleanPath('org/.git')` returns `'org/'` (verified), so `normalizeRemote('https://gitlab.com/org/.git')` is `'gitlab.com/org/'`, which is NOT idempotent (re-normalizing gives `'gitlab.com/org'`, breaking the invariant remote.test.ts:51-59 asserts) and makes `sameRemote('https://gitlab.com/org/.git','https://gitlab.com/org')` false. Needs new cases in the idempotence list and the normalize test. *Wins if:* Wins if you want the whole `.git`-as-a-path-component class closed in one commit — including the normalize-idempotence and sameRemote break for host paths ending in `/.git` — rather than leaving it as a separate sweep item; costs a change to normalized output for those (currently already-broken) remotes.
  - Option 3: Canonicalize `/path/.git` to `/path` in `normalizeRemote` (one identity for a repo and its gitdir) — Depth 4/4 · Cost 2/4. In `filePath` (src/lib/remote.ts:81-85), strip a trailing `/.git` from `canonical` so `/tmp/x/team/.git` and `/tmp/x/team` normalize to the same `file:/tmp/x/team`; `remoteName` then needs no change. Requires rewriting the round-trip assertion added this batch (remote.test.ts:31, which demands `remoteToGitUrl(normalizeRemote('/tmp/x/team/.git')) === '/tmp/x/team/.git'`), the doc comment at remote.ts:76-80 ("`.git` is KEPT… the path is the identity"), and the spec sentence at .planning/specs/2026-09-02-phase-1-build.md:138. *Wins if:* Wins only if the product decides a non-bare repo and its gitdir must be ONE team identity (joining `/srv/x/team/.git` after `/srv/x/team` should update the existing team, not create a second one) — a spec change, not a bug fix.
- **medium — collectIdentity's documented "leave it empty" GitHub login is unreachable once a login is suggested** — `src/lib/auth.ts:65`
  - Root cause: `collectIdentity` hands the suggested login to `askUntilValid` as `io.text`'s *defaultValue* (src/lib/auth.ts:65), and the channel's contract is that a blank answer means "take the default": `terminalPrompter.text` returns `answer.trim() || defaultValue || ''` (src/lib/prompt.ts:79), a substitution the test double mirrors faithfully (src/lib/__tests__/fixtures.ts:31). So the validator's empty branch at src/lib/auth.ts:67 (`if (!trimmed) return { ok: true, value: '' }`) is dead code whenever `suggested` is non-empty, i.e. whenever `existing.github` is set or gh is logged in (src/lib/auth.ts:60-64). The prompt therefore cannot produce the empty login that the rest of this same branch treats as a first-class state — `personSchema.github` was widened to `z.union([z.literal(''), githubLoginSchema])` (src/lib/schema.ts:55) and `team remove` is tested against a member with `github: ''` (src/commands/__tests__/remove.test.ts:153) — while the rule string printed to the user still promises "(leave it empty if you have none)" (src/lib/auth.ts:18). Two failures follow. (1) Nobody can clear or blank a suggested login: on a machine where gh is logged in as someone else, or where a stale/wrong login is persisted, the suggestion is written to `people/<handle>.json` and `config.github` regardless. (2) `configSchema.github` is still unvalidated `z.string().optional()` (src/lib/schema.ts:73) and origin/main's `collectIdentity` stored whatever was typed (`github = (await io.text('GitHub login', github)).trim()`), so a legacy config can hold a non-login (an email, a display name). On this build that value becomes the default, three Enters re-submit it, and `askUntilValid` throws `Invalid github login after 3 attempts` — a user with no GitHub account cannot finish `login` without hand-editing config.json. A secondary contributor is that the hint never surfaces: because a blank answer is *valid* (it silently becomes the default), the rule line that mentions the empty option is never printed. (src/lib/auth.ts:65)
  - Rated: moderate / medium risk / isolated
  - Why one fix: Option 1 wins on every axis that matters today. The spec describes this prompt as one editable question with the guess pre-filled, and every existing flow and test is built that way — option 1 keeps that shape and just adds a way to say \"I have none\", while option 2 charges everyone an extra yes/no question during first-run setup to buy only slightly better discoverability, and option 3 rewrites how every question in the tool handles the Enter key to fix one question. Option 3 only becomes the right call once the setup wizard (which has its own \"press Enter to skip\" step) is being built; nothing on this branch needs it. The one judgement left inside option 1 is cosmetic rather than a real fork: which character clears the field (`-` is proposed because it can never be a real GitHub username) and the exact wording of the one-line hint.
  - **Option 1: Clear sentinel + sanitized suggestion (all inside collectIdentity) (recommended)** — Depth 3/4 · Cost 1/4. In `collectIdentity` (src/lib/auth.ts:59-70): (1) adopt `existing.github` as `suggested` only when it parses — `githubLoginSchema.safeParse(existing.github).success` — so a legacy unvalidated config value can never become an unclearable default (and the `gh api user` lookup still runs behind it, matching the existing '' case at auth.test.ts:47-50); (2) add an explicit clear path to the validator: treat a bare `-` as "I have none" and return `{ ok: true, value: '' }`. `-` is unambiguous because `githubLoginSchema` requires a leading alphanumeric, so it can never be a real login (unlike `none`, which is a legal login); (3) make it discoverable — rewrite `GITHUB_LOGIN_RULE` (src/lib/auth.ts:18) to say `-` rather than "leave it empty", and, when `suggested` is non-empty, print one hint line before the prompt ("press Enter to keep <suggested>, or answer - if you have none"), because a blank answer is accepted silently and the rule line is otherwise never shown. Tests in src/lib/__tests__/auth.test.ts: a case with `{ ...emptyConfig(), github: 'octocat' }` answering `-` and expecting `github === ''`, and a case with an invalid persisted `github` (e.g. `'Ryan Liu'`) asserting it is not offered as the default and `login` still completes. *Wins if:* You want to keep the single editable prompt the spec describes (§5.4: default "shown for confirmation, and editable at the prompt") and keep the fix confined to auth.ts — cheapest change that closes both failures.
  - Option 2: Confirm the suggestion, then ask with no default — Depth 3/4 · Cost 2/4. In `collectIdentity`, when `suggested` is non-empty first ask `io.confirm(`Use GitHub login @${suggested}?`)`; on yes take it as-is, on no fall through to `askUntilValid(io, 'GitHub login', undefined, …)` where a blank answer genuinely is empty. No sentinel character for the user to learn, and it also defuses the legacy-invalid-config case (the bad value is only ever offered, never forced). *Wins if:* You judge a sentinel character too obscure for first-run onboarding and are willing to pay an extra y/N question on every `login`/`team create`/`team join`, plus a `confirms` entry in roughly fourteen scripted prompts across src/lib/__tests__/auth.test.ts and src/commands/__tests__/{login,join,create}.test.ts.
  - Option 3: Fix the Prompter contract so blank always means blank — Depth 4/4 · Cost 3/4. Change `Prompter.text` so a blank answer is returned as `''` and default substitution becomes the caller's job: drop the `|| defaultValue` fallback in `terminalPrompter.text` (src/lib/prompt.ts:79) and its mirror in `ScriptedPrompter.text` (src/lib/__tests__/fixtures.ts:31), have `askUntilValid` apply the default only when the validator opts in, and audit every call site (src/lib/auth.ts:71-73, src/commands/team.ts:164, 189, 198) plus the ~94 scripted prompt arrays that currently rely on `''` meaning "press Enter". *Wins if:* More optional prompts are imminent — the §6.1 `setup` wizard's invite step is specced as "space- or comma-separated; empty skips" (spec line 344) — and you want "blank means blank" true channel-wide before they land rather than one sentinel per optional question.
- **medium — Derived-branch push treats a lease/CAS violation as retryable, so the retry force-overwrites the other writer's branch** — `src/lib/teamRepo.ts:188`
  - Root cause: src/lib/teamRepo.ts:188 classifies a derived-branch push refusal with the same shared RETRYABLE set that the plain `main` push uses (:181), but the two pushes have opposite safety properties. The `main` push is non-forcing, so handing a refusal back to the retry loop is safe: the loop's `fetch origin` + `reset --hard origin/main` (:101-102) can only replay the mutation onto the newer tip. The derived-branch push at :185 is `--force-with-lease` with NO pinned expect value, so its lease is derived from `refs/remotes/origin/<target>` — the exact ref that same `fetch origin` force-updates at the top of the next attempt. RETRYABLE (:59) contains the ref-transaction CAS vocabulary (`cannot lock ref`, `incorrect old value`, `remote ref updated since checkout`), which is precisely how a server reports "someone else moved publish/<name>". So the one condition the `<branch>-2` fallback exists for (doc comment :170-174) is routed into the retry loop instead, the refetch re-arms the lease against the other writer's commit, and the second `--force-with-lease` push to the SAME branch succeeds and destroys it. This contradicts spec §6.0 step 4 (.planning/specs/2026-09-02-phase-1-build.md:269: "a lease failure falls back to `publish/<name>-2` rather than clobbering"), and no decision in .planning/decisions/2026-09-04-m1-hardening-decision-walk.md authorizes the change. The identical hazard exists on the pre-existing both-targets-stale return at :190 (`retryable: true` → same refetch → renewed lease → overwrite of `branch`), and the branch's new test (src/lib/__tests__/teamRepo.test.ts:305-314) pins the wrong behavior by injecting the CAS wording `cannot lock ref 'refs/heads/publish/x': is at abc but expected def` and asserting `pushedTo === 'publish/x'` after two pushes. (/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/lib/teamRepo.ts:188 (sibling: same file :190; test that locks it in: src/lib/__tests__/teamRepo.test.ts:305-314))
  - Rated: moderate / low risk / pattern — siblings: Same mistake — a `--force-with-lease` refusal handed back to a loop whose first act (`fetch origin`, src/lib/teamRepo.ts:101) re-arms the very lease that refused — appears at:
1. /Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/lib/teamRepo.ts:188 — new on this branch: `retryable: RETRYABLE.test(lastError)` makes CAS-shaped refusals (`cannot lock ref`, `incorrect old value`, `remote ref updated since checkout`) retryable, skipping the `<branch>-2` fallback.
2. /Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/lib/teamRepo.ts:190 — pre-existing: after BOTH `branch` and `<branch>-2` fail the lease, `return { ok: false, retryable: true, ... }` sends control back through the same refetch, and the next attempt's push to `branch` succeeds with a renewed lease. Fixing only :188 leaves this path live, so it must be fixed in the same change.
3. /Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/lib/teamRepo.ts:170-174 — the doc comment asserts the wrong invariant ("ref-lock contention retries exactly as it does for `main` (the shared RETRYABLE set)"); it must be corrected or it will re-authorize the bug.
4. /Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/lib/__tests__/teamRepo.test.ts:305-314 — the new test injects the CAS wording and asserts `result.pushedTo === 'publish/x'` with `pushes === 2`, locking the overwrite in; it must be retargeted to the transient-lock wording and joined by a competing-commit-survives case.
NOT affected, checked: teamRepo.ts:181 (the `main` push shares RETRYABLE but is non-forcing, so a retry can only replay onto the new tip) and src/commands/team.ts:446 (`git push -u origin main` into a freshly scaffolded empty remote — no lease, no retry loop). `grep -rn "'push'|RETRYABLE|STALE_LEASE|force-with-lease" src` finds no other push classifier in the repo.
  - Why one fix: No product decision is at stake — the spec already says a lease failure must fall back to `publish/<name>-2` rather than clobber a teammate's branch, so "don't overwrite it" is settled. Option 0 is the smallest change that actually delivers that: it keeps the retry the branch author wanted (a real, transient server lock) while sending "someone else moved this branch" back to the `-2` fallback, and it closes the identical older hole three lines below. Option 1 is a stronger version of the same answer, not a different one — it makes git itself enforce the rule instead of relying on reading git's error text — and it can be done later as hardening without any user-visible difference. Option 2 is strictly less: it undoes the new bug but leaves the older one and throws away the useful part of the change.
  - **Option 1: Classify a lost lease by its full CAS wording, not by "stale info" alone (recommended)** — Depth 3/4 · Cost 1/4. In src/lib/teamRepo.ts add a `LEASE_LOST` regex beside RETRYABLE/STALE_LEASE (:59-60) covering every wording a lease violation is reported with — `stale info`, `is at <sha> but expected <sha>`, `incorrect old value`, `remote ref updated since checkout` — and use it in place of STALE_LEASE at :188, so those refusals fall through to `<branch>-2` instead of being returned as retryable. What is still matched by RETRYABLE but not by LEASE_LOST (a genuine transient server lock: `cannot lock ref 'refs/heads/x': Unable to create '...x.lock': File exists`) keeps retrying, which preserves the branch's stated intent. Also make the both-targets-lost return at :190 terminal (`retryable: false`) so a double lease loss surfaces as PushRefused rather than looping back into a renewed-lease overwrite. Update the doc comment :170-174 (it currently claims contention "retries exactly as it does for main"). Rework the new test at src/lib/__tests__/teamRepo.test.ts:305-314 to inject the transient lock wording for the retry assertion, and add a case that injects the CAS wording, pushes a competing commit to `publish/x` between the two attempts, and asserts the competing commit survives and the write lands on `publish/x-2`. *Wins if:* You are content for the no-clobber property to rest on git's message wording — which is already how RETRYABLE and STALE_LEASE govern every other push decision in this file — and want the spec's `<branch>-2` fallback restored in about an hour without new plumbing.
  - Option 2: Pin the lease to the sha observed at the first attempt — Depth 4/4 · Cost 2/4. Capture `git rev-parse --verify --quiet refs/remotes/origin/<target>` for both `branch` and `<branch>-2` on the FIRST loop iteration of safeWrite (after the first `fetch origin`, teamRepo.ts:101), hold those shas across retries, and thread them into push() so :185 becomes `--force-with-lease=refs/heads/<target>:<sha>` (empty expect value when the ref does not exist yet). A retry after a refetch then cannot silently adopt a newer tip: the second push fails the pinned lease with `stale info` and takes the `-2` fallback. Fixes :188 and :190 at once and immunizes any future retry path. Test as in option 0. *Wins if:* You do not want a data-loss guarantee to depend on matching git's English error strings at all, and accept threading a pinned sha through safeWrite -> push plus handling the not-yet-existing-ref (`ref:` with empty expect) spelling.
  - Option 3: Revert line 188 to terminal — Depth 2/4 · Cost 0/4. Restore `return { ok: false, retryable: false, error: lastError };` at teamRepo.ts:188, revert the doc-comment sentence at :172-174, and delete or retarget the new "retries ref-lock contention on a derived branch" assertion in src/lib/__tests__/teamRepo.test.ts. Derived-branch behavior returns to origin/main's: any non-stale-lease refusal is a PushRefused carrying git's message. Leaves the pre-existing :190 double-stale-lease loop untouched. *Wins if:* You want this branch to land now with zero new judgment about git's vocabulary, and are willing to file the transient-lock retry and the :190 loop as separate follow-ups.
- **medium — After dropping the per-team token, invite and team remove never check gh auth, so a logged-out gh is reported as a rate-limit or permissions failure** — `src/commands/invite.ts:27`
  - Root cause: Decision 2 stripped the injected `{ env: gitAuthEnv(binding.token) }` from every gh call, so `invite` and the GitHub half of `team remove` now depend entirely on an ambient `gh auth` session — but neither verb gained the gh-readiness precheck that `team create` inherits from `authenticateCreator` (src/lib/auth.ts:107-117, which names gh, https://cli.github.com, `gh auth login`, and the --remote alternative). Their first `gh api` call therefore runs unconditionally, and its failure is interpreted by branches that assume gh actually reached GitHub: invite.ts:34 blames 'GitHub status unknown' plus the 50-invitations-per-repo-per-day cap, and team.ts:78 blames 'Team removal requires GitHub repository admin permission' — when the real cause is a logged-out gh. When gh is not installed at all, systemRunner rejects instead of resolving (`child.on('error', reject)`, src/lib/runner.ts:25) and the verb surfaces the raw `spawn gh ENOENT` string. `ghState` already swallows both cases correctly (auth.ts:20-32); it is simply never called from these two verbs. (src/commands/invite.ts:27 (first unguarded `gh api`), with the identical omission at src/commands/team.ts:76-77)
  - Rated: moderate / medium risk / pattern — siblings: Unguarded gh call sites this fix must cover: src/commands/invite.ts:27; src/commands/team.ts:77, :80, :84, :87, :99, :104 (all inside `remove`'s `ownerRepo !== null` branch — one guard at team.ts:76 covers all six). Already-guarded siblings, kept as the reference wording/shape: src/commands/team.ts:193 and :202 (`team create`, guarded via `authenticateCreator`, src/lib/auth.ts:107-117), src/commands/team.ts:405 and :410 (`acceptOrDirect`, gated by `gh.authenticated`), src/lib/auth.ts:62 (`gh api user`, gated by `options.gh?.authenticated`), src/commands/login.ts:21 (`detectOrOfferGh`). Test files to sweep for gh handlers that do not answer `--version` / `auth status`: src/commands/__tests__/invite.test.ts (3 `ghOnlyRunner` handlers at lines 10, 26, 42) and src/commands/__tests__/remove.test.ts (11 `mappedRunner` gh handlers).
  - Why one fix: All three give the user the right message instead of a bogus "you hit the daily invite cap" or "you are not an admin". The first puts the check in one shared place, so the next GitHub command anyone writes gets it for free and there is only one sentence to keep correct — for the same roughly-an-hour of work as the narrower options. The one judgement call it contains (should `invite`/`team remove` offer to run `gh auth login` for you, or just tell you to?) is already settled by copying what `team create` does today, so no real trade-off is left open.
  - **Option 1: Shared requireGh guard in auth.ts, used by both host verbs (recommended)** — Depth 4/4 · Cost 1/4. Add `export async function requireGh(io, runner, action): Promise<GhState>` to src/lib/auth.ts beside `authenticateCreator` (auth.ts:107-120): it runs `detectOrOfferGh` (so an interactive user gets the same y/N gh-login offer `team create` gives, and it is a no-op on non-interactive channels) and throws the two existing messages — 'needs the GitHub CLI (gh) … install it from https://cli.github.com and run `gh auth login`' / 'GitHub authentication is required … run `gh auth login` and retry' — parameterised by `action`. Refactor `authenticateCreator` to call it so the wording exists once. Call it in src/commands/invite.ts after the `hostOperationAllowed` check (lines 18-19, so the generic-remote refusal still precedes any gh call) and in src/commands/team.ts inside the `if (ownerRepo !== null)` branch (line 76) before the admin probe at line 77 — one guard covers team.ts:77/80/84/87/99/104. Tests: teach the gh handlers in src/commands/__tests__/invite.test.ts (3 `ghOnlyRunner` handlers) and src/commands/__tests__/remove.test.ts (11 `mappedRunner` handlers) to answer `--version` and `auth status`, and add an invite case on `fakeGh('me', {}, false)` and one on `noGhRunner` — plus the same pair for `team remove` — asserting the message names gh / `gh auth login`, not the invitation cap or admin permission, and that no `gh api` call was made. *Wins if:* You want one sentence about a missing or logged-out gh that every current and future GitHub verb inherits — it wins outright once more host verbs land (M3 `setup`, `team leave`).
  - Option 2: Inline ghState precheck at each verb (reviewer's literal suggestion) — Depth 3/4 · Cost 1/4. Leave src/lib/auth.ts alone. Call `ghState(runner)` at the top of `invite.run` (after the `hostOperationAllowed` check, src/commands/invite.ts:19) and inside `team.remove`'s `ownerRepo !== null` branch (src/commands/team.ts:76), each throwing its own locally written install / `gh auth login` message. Same test-handler updates and same new logged-out / no-gh cases as option 1. *Wins if:* You want the shared auth.ts file untouched in this wave and accept two hand-copied gh messages that can drift apart from `authenticateCreator`'s.
  - Option 3: Diagnose only after a gh call has already failed — Depth 2/4 · Cost 1/4. Keep the call order as-is. Where a gh failure is ambiguous — invite.ts:34's `status === null` branch and team.ts:78's admin branch — consult `ghState(runner)` and, when gh is missing or logged out, replace the invitation-cap / admin-permission text with the gh message. Additionally wrap the `runner.run('gh', …)` calls in both verbs so a spawn ENOENT rejection (src/lib/runner.ts:25) becomes that same message rather than `spawn gh ENOENT`. *Wins if:* `invite` and `team remove` must not gain two extra gh subprocess calls on every successful run, and you want the 14 existing gh test handlers left exactly as they are.
- **low — `--repo` (and `--org`) are silently ignored when `--remote` is passed** — `src/cli.ts:37`
  - Root cause: `create()` in src/commands/team.ts is a two-branch dispatcher — `if (args.remote) { …generic-git… } else { …gh repo create… }` (team.ts:174 vs 189) — but it never validates that the caller's arguments belong to only one branch. `args.repo` is read solely at team.ts:189 and `args.org` solely at team.ts:191/195, both inside the `else`, so when `--remote` is present those two fields are dropped with no error, no notice, and no trace in the result. src/cli.ts:36-39 makes the combination reachable: it registers `--org`, `--repo` and `--remote` on the same `team create` subcommand and forwards all of them verbatim (`{ kind: 'create', name, ...options }`) with no commander exclusivity. Net effect: `terum-skills team create acme --remote https://git.example/x.git --repo skills-repo` scaffolds into `x.git`, prints `Created team acme at https://git.example/x.git`, and never mentions that `--repo` did nothing. Nothing licenses the silence — the codebase's own precedent for an inapplicable flag is `join`, which prints `…ignoring --as <name>` at team.ts:244 and has a test for it (join.test.ts:64). Spec rev 9 (.planning/specs/2026-09-02-phase-1-build.md:296) documents `team create [<name>] [--org <org>] [--remote <url>]` and says a `--repo` flag "was rejected as the wrong shape for a question-first CLI", so the flag's presence is itself unmentioned drift; but the `--org` half of the silent drop exists regardless of how that is resolved. (src/commands/team.ts:174 (branch that consumes `--remote` and never reads `args.repo`/`args.org`); flag surface at src/cli.ts:36-39)
  - Rated: trivial / low risk / pattern — siblings: Same first-branch-wins shape, where an earlier `if` consumes one option and later ones are dropped without a word:
- src/commands/team.ts:174 — the finding itself (`if (args.remote)` swallows `--repo`/`--org`).
- src/commands/share.ts:31-34 — `if (args.forget) … if (args.relocate) … if (args.keepSource || args.keepRepo) … if (!args.path) throw` with no precedence comment and no exclusivity in the cli.ts:69 registration, which registers `--keep-source`, `--keep-repo`, `--relocate`, `--forget` and a positional `[path]` together. `share ./my-skill --forget <id>` silently ignores the path; `share --forget a --relocate b:c` silently ignores `--relocate`; `--keep-source x --keep-repo y` silently ignores `--keep-repo` via `args.keepSource ?? args.keepRepo!`. No test covers any of those combinations.
Checked and clean (explicit handling, not silent drops): src/cli.ts:71-72 install/uninstall `member`/`project` routing (options are spread into every branch), src/cli.ts:73 `sync --hook --prune` (`prune` is carried into both shapes), src/cli.ts:59-60 `ls` subcommand `--team` fallback (`options.team ?? ls.opts().team`), and `team remove --archive-only`, which is validated by `hostOperationAllowed(binding.remote, …)` in team.ts rather than ignored.
  - Why one fix: Option 0 wins on every axis that matters. It is about two lines, it catches both `--repo` and `--org` in one check, and because it sits in the command function rather than the flag parser it also protects the future setup wizard that the spec says will pass those same values as plain fields. Option 1 (a commander flag rule) is strictly less coverage for the same effort — it only guards typing at the terminal. Option 2 is a genuinely separate question (should the `--repo` flag exist at all, since the written spec says it was rejected but the decision log's file notes say to add it?), and it is worth raising with whoever owns the spec, but it does not change the answer here: even if `--repo` were removed tomorrow, `--org` combined with `--remote` would still need the refusal that Option 0 adds.
  - **Option 1: Refuse the combination in create() (library boundary) (recommended)** — Depth 3/4 · Cost 0/4. In src/commands/team.ts, immediately after `try {` at line 163 (before the team-name prompt on line 164, so nothing is asked or touched first), add: `if (args.remote && (args.repo !== undefined || args.org !== undefined)) throw new Error('--repo and --org apply only when creating the repository on GitHub; drop them, or drop --remote.');` with a one-line comment saying why (`--remote` targets a repository that already exists). Add a create.test.ts case next to the existing `--repo` cases (create.test.ts:179/195) asserting `create({ name: 'acme', remote: publicRemote, repo: 'skills-repo', … })` returns `{ ok: false }` with that message and that `runner.calls` is empty, plus the same for `org`. *Wins if:* Wins whenever the guard should hold for every caller, not just argv: src/cli.ts:15-18 declares the file 'commander wiring only. Every verb is run(args, io) in src/commands', and spec line 296 says `repo` and `org` are plain library fields the M3 wizard will pass — so a check that lives in team.ts protects the wizard, `setup`, and tests too, and covers `--org` and `--repo` in one expression.
  - Option 2: commander `.conflicts(['remote'])` in cli.ts — Depth 2/4 · Cost 0/4. In src/cli.ts, chain `.conflicts(['remote'])` onto the `--org` (line 36) and `--repo` (line 37) options so commander 15 rejects the combination during parse, and add a cli.test.ts case asserting `['team','create','alpha','--remote','https://x/y.git','--repo','other']` never reaches the stub verb. *Wins if:* Wins only if the CLI is accepted as the sole caller that can ever pass both and you prefer commander's own usage/error text over a Result failure — it leaves a direct `create({ remote, repo })` library call (the M3 wizard, `setup`, any test) still silently dropping the field.
  - Option 3: Drop `--repo` from the CLI and notice `--org`, per spec rev 9 — Depth 4/4 · Cost 1/4. Remove `.option('--repo <repo>', …)` from src/cli.ts:37 and its type in the action signature (keeping `repo` on CreateArgs as the library-only field spec line 296 prescribes: "a `--repo` flag as the UX was rejected as the wrong shape for a question-first CLI"), and in the `args.remote` branch print an ignore notice for `--org` mirroring join's `io.print('…ignoring --as …')` at team.ts:244. Update cli.test.ts:27 (which currently asserts `--repo` wiring) and the create.test.ts `--repo` cases to call the library directly. *Wins if:* Wins if the team decides the shipped CLI must match spec rev 9's explicit rejection of a `--repo` flag rather than the decision walk's implementation note (.planning/decisions/2026-09-04-m1-hardening-decision-walk.md:128: 'src/cli.ts: `<name>` optional, `--repo` and `--org` as overrides') — a product call the code cannot settle, and one that still needs option 0's handling for `--org` + `--remote`.
- **low — Twin-login refusal is never tested on the re-applied tree, so a regression to a preflight check would pass** — `/Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/commands/__tests__/remove.test.ts:165`
  - Root cause: The new twin-login test seeds the colliding roster onto origin/main BEFORE the command runs, so it only proves "the refusal happens somewhere" — it never exercises the one state the check exists for. `assertLoginUnclaimed` (src/commands/team.ts:141) is deliberately called from inside the pure safeWrite mutation `archiveMutation` (team.ts:135), whose doc comment states the invariant must hold "on the fresh origin/main roster" (team.ts:126-130). safeWrite re-runs that mutation after every fetch/hard-reset on a lost race (src/lib/teamRepo.ts:101-105), which is what closes the window between remove()'s preflight `git show origin/main:people/<h>.json` (team.ts:64-66) and the push. Because src/commands/__tests__/remove.test.ts:167 pushes people/twin.json from the seed before `run(...)` is called, the very first mutation pass already sees the twin — so moving the check into a preflight before `repo.safeWrite(...)` at team.ts:95 would leave both the refusal assertion and the "nothing pushed / no gh DELETE" assertions green while re-opening the race (a twin added by another machine between the preflight and the push would have their access silently revoked). The invariant's LOCATION, not just its existence, is the thing under test, and nothing pins it. Two sibling call-sites already do this correctly — src/commands/__tests__/join.test.ts:70 races joinMutation's HandleCollisionError and src/lib/__tests__/teamRepo.test.ts:228 races the tree re-read — so the pattern and fixtures for the missing test already exist in this repo. (src/commands/__tests__/remove.test.ts:167 (seeding happens before `run(...)`, so the check at src/commands/team.ts:135/141 is never exercised on the re-applied tree))
  - Rated: moderate / low risk / pattern — siblings: Same mistake (an invariant deliberately placed INSIDE a re-applied safeWrite mutation, but only ever tested from a state that a preflight would also catch): (1) src/commands/__tests__/remove.test.ts:165-186 — the finding; the check lives at src/commands/team.ts:135 and :141. (2) src/commands/share.ts:65 — the repo-wide skill-name check inside the safeWrite mutation, with an explicit comment that only the freshly reset tree is authoritative; its only two tests, src/commands/__tests__/share.test.ts:333 and src/commands/__tests__/share.test.ts:350, both seed the collision before `run(...)`, where the preflight at src/commands/share.ts:54 already catches it — deleting the in-mutation check would keep both green. Correct references to copy (already race-tested, no action needed): src/commands/__tests__/join.test.ts:70 (joinMutation's HandleCollisionError raced mid-push) and src/lib/__tests__/teamRepo.test.ts:228 ('a mutation that sees the fresh tree can react to a concurrent change'). Not affected: the install/uninstall/sync mutations (src/commands/install.ts:105, src/commands/uninstall.ts:70, src/commands/sync.ts:169/181) are idempotent re-writes with no refusal invariant, so they do not belong on this worklist.
  - Why one fix: Option 1 is the obvious pick and there is no real trade-off to settle. It is test-only, about fifteen lines, and it copies a pattern this codebase already uses twice for exactly this situation, so it adds no new machinery and cannot break anything for users. Option 3 is cheaper but does not actually test the thing that would regress. Option 2 is not a competing design — it is literally option 1 plus one extra test for a similar weak spot in a different command that this branch did not touch; you can do that extra test now or as a follow-up without changing or redoing anything from option 1.
  - **Option 1: Race the twin in mid-safeWrite (mirror the join.test.ts pattern) (recommended)** — Depth 3/4 · Cost 1/4. Add one `it(...)` to src/commands/__tests__/remove.test.ts, after the existing twin test (~line 187). Keep `prepared()` clean (no twin on origin/main at start), build `const base = mappedRunner(REMOTE, fixture.bare, host)` reusing the existing `host` gh handler, then `const runner = wrapRunner(base, async (command, args, _options, next) => { if (command === 'git' && args[0] === 'push' && pushes++ === 0) { await pushFromSeed(fixture.seed, 'people/twin.json', `${JSON.stringify(person('twin', { github: 'Member-GH' }), null, 2)}\n`); raced = (await git(['rev-parse', 'main'], fixture.bare)).trim(); } return next(); })`. Assert: the run resolves `{ ok: false, error: expect.stringContaining('active member twin also declares GitHub login @member-gh') }`; `git rev-parse main` on the bare equals `raced` (i.e. only the racer's commit landed, our archive did not); `JSON.parse(await git(['show','main:team.json'], fixture.bare)).archived` is `[]`; `base.calls.some((call) => call.command === 'gh' && call.args.includes('DELETE'))` is false; and `git status --porcelain` in `store.teamClone('team')` is empty (safeWrite's finally cleaned up). The first push is rejected non-fast-forward ('fetch first' matches teamRepo.ts:59 RETRYABLE), so safeWrite re-fetches, re-resets and re-runs archiveMutation — which now sees twin.json and throws. A preflight-only implementation would push the archive and fail this test. *Wins if:* Wins whenever the goal is to pin THIS diff's invariant at the place it lives, with a ~15-line test that copies a pattern already proven in this repo (join.test.ts:70) and touches no production code.
  - Option 2: Same race test, plus sweep the identical gap in share — Depth 4/4 · Cost 2/4. Option 1, plus an equivalent mid-push race test for the in-mutation repo-wide skill-name check at src/commands/share.ts:65 (whose comment says 'only the freshly reset tree handed to safeWrite is authoritative'). Its two existing tests, src/commands/__tests__/share.test.ts:333 and :350, both seed the collision before `run(...)`, where the preflight check at src/commands/share.ts:54 already catches it — so deleting the in-mutation check would leave them green too. New test: intercept the first `git push` to `pushFromSeed(fixture.seed, 'skills/sample/SKILL.md', ...)` and assert the retry refuses with 'choose a unique name', nothing lands from our clone, and the local source SKILL.md is unchanged. *Wins if:* Wins if you would rather close the whole class now than file a follow-up — the share gap is real, pre-existing, and outside this diff, so it is otherwise easy to forget.
  - Option 3: Unit-test archiveMutation directly against a fake tree — Depth 1/4 · Cost 0/4. `archiveMutation` is already exported (src/commands/team.ts:131). Add a test in a new/existing team-level test that builds a minimal `MutableTree` stub (before/after/paths/set/remove/changedPaths) containing team.json plus people/member.json and people/twin.json, and asserts `archiveMutation(tree, 'member', 'member-gh')` throws the twin error and leaves `tree.changedPaths` empty. *Wins if:* Wins only if the priority is the cheapest possible pin: it catches deletion of the check from the mutation, but not the regression actually described — remove() passing `undefined` for `login`, or the check being hoisted into a preflight — and it never exercises the fetch/reset retry that makes the check load-bearing.

### Forks — no single fix clearly wins; run `/decision-walk <this report>` (3)

- **medium — `team join` pushes the roster entry, then fails on the post-lock rebind check — published membership reported as a failure, with no rollback or notice** — `src/commands/team.ts:276`
  - Root cause: `join` performs its only unrevertable side effect — the `safeWrite` fetch/reset/commit/push of `people/<handle>.json` to origin/main (src/commands/team.ts:267) — and only afterwards takes the config lock and re-validates bindability (team.ts:276-281). `assertBindable` (src/lib/auth.ts:163-169) throws a bare, config-only message ("<remote> is already configured as team <other>." / "Team <team> is configured for <x>, not <y>; pass --as"), that error falls through to join's outer catch (team.ts:305-307) and is returned verbatim as `{ ok: false, error }`. There is no post-durable-write error path at all: nothing in that message says the push already landed, so the CLI reports a plain failure for an operation whose remote half succeeded and cannot be undone locally (safeWrite's `finally` only resets the local clone; teamRepo.ts:16-17). The result is a live `people/<handle>.json` on the team repo that this machine has no config record of; a re-run resolves to the pre-existing entry (`existing`/`boundHandle`, team.ts:245-259) and forces the OTHER handle, so the published entry stays ACTIVE by the §4.1 membership predicate forever, and the clone dir created at `store.teamClone(team)` is stranded too. The same shape exists in `create` (team.ts:220-225): its hand-written message at line 221 covers only the `fresh.teams[name]` branch, while the `assertBindable` added at line 222 throws the bare message even though the repo was already created, scaffolded and pushed. Note the re-check itself is correct and was added on purpose (requested by .planning/reviews/hybrid-working-2026-09-04-pass2.review.md:228); what is missing is the honest reporting of the durable state when it fires. (src/commands/team.ts:276-281 (post-push `store.update` in `join`; absolute: /Users/ryanliu/Documents/Terum/terum-codex/m1-wave/src/commands/team.ts) — sibling at team.ts:220-225 in `create`; thrower at src/lib/auth.ts:163-169)
  - Why it is a fork: Fork, and it is a product call, not a code call. Everyone agrees the current behaviour is wrong: the user's membership really was published to the team repo, and the CLI says only "that remote is already configured as team other" — so the user thinks nothing happened while a roster entry of theirs is live and permanent. Option 1 (recommended) tells the truth and points at a real recovery; it is also needed under every alternative, because the second reason this check can fire (your team name is taken by a different remote) can never be auto-resolved. The genuine question is whether, when the remote is provably the same repository, the join should instead just SUCCEED under the name the machine already has for it (option 2) — the spec's own line "a second join of an already-configured remote updates that entry rather than creating a duplicate team" points that way, but doing it silently overwrites the other run's recorded handle, which the spec elsewhere calls immutable, and strands that person's roster entry instead. So: honest failure the user can act on, versus a quiet success that moves the mess somewhere else. Option 3 (reserve the binding before pushing) genuinely prevents the bad state, but it lets a configured team exist with no published membership — a state five other commands currently assume is impossible — which is a lot of new surface for a race that needs two joins of one remote under two different names at once.
  - **Option 1: Name the durable write in the post-lock failure (join + create) (recommended)** — Depth 2/4 · Cost 1/4. In `join`, wrap the `store.update` at src/commands/team.ts:276-281 in try/catch and rethrow an enriched Error: keep the original `assertBindable` reason as the first line, then state the durable half — "Your membership is already published to <normalized> as people/<identity.handle>.json, and the clone is at <store.teamClone(team)>; nothing was recorded on this machine. Rename or drop the conflicting <other> entry in ~/.terum/skills/config.json and re-run `team join <normalized>`, or ask a team admin to run `team remove <handle>` to retire the duplicate roster entry." (M1 has no `team leave`, so do not suggest one — src/commands/ has no leave.ts; and do NOT tell the user a bare re-run finishes the job, because `existing`/`boundHandle` at team.ts:245-259 would force the other entry's handle). Apply the same wrap to `create` at team.ts:220-225 so the `assertBindable` branch at line 222 also names the created+scaffolded repo, reusing the wording style already at line 217. Update src/commands/__tests__/join.test.ts:186-201 to also assert the message names the published handle/roster path (and add the matching assertion to the create suite), keeping the existing `already configured as team other` substring so the invariant under test is unchanged. *Wins if:* Wins if the rule is 'a command that has already made an unrevertable remote write must say so', while the config's one-remote-one-name invariant is kept exactly as the re-check enforces it today — the accurate, actionable message is then the entire fix, and it is required for the second `assertBindable` branch (name taken by a different remote) under every alternative anyway.
  - Option 2: Adopt the existing entry instead of failing when the remote provably matches — Depth 3/4 · Cost 2/4. In the post-lock update in `join`, before asserting, call `teamByRemote(fresh, normalized)`; if it returns an entry under a different name, bind the just-published handle into THAT name (`bindTeam(fresh, byRemote[0], { remote: normalized, handle: identity.handle })`), print "This remote is configured as team <other>; recorded your membership there", and return `success({ team: byRemote[0], ... })`. Keep the throw (with option 1's enriched message) only for the name-collision branch. Requires also reconciling the clone: config would point at teamClone(byRemote[0]) while this run cloned into teamClone(team), so either rename the clone or re-clone on next use. Test at join.test.ts:186-201 flips from `ok:false` to `ok:true` with `team: 'other'`. *Wins if:* Wins if spec §6 (2026-09-02-phase-1-build.md line 297: 'a second join of an already-configured remote updates that entry rather than creating a duplicate team') is meant to govern the post-lock race too — i.e. the product would rather the join succeed under whatever name the remote already has than report a failure; the price is silently overwriting the other run's `teams.<other>.handle`, which §5.4 calls immutable once its people file exists, orphaning THAT roster entry instead.
  - Option 3: Reserve the binding before the push, roll back on failure — Depth 4/4 · Cost 3/4. Move the bindability check + a provisional `bindTeam` into a `store.update` taken after `ensureClone`/identity collection but BEFORE `repo.safeWrite` (team.ts:262-267), then update the entry's handle in a second `store.update` after the write returns; on any failure between the two, remove the entry we created (only when we created it — not on the rejoin path where `existing` was already bound). The loser of a concurrent join then fails before publishing anything, so no orphan roster entry is ever created. Keep option 1's message for the residual window (a config file edited by a non-`join` writer). *Wins if:* Wins if publishing a permanent roster entry the machine cannot record is treated as real data loss rather than a reporting bug — but it introduces a new state (a configured team with no published membership) that `ls`, `share`, `install`, `sync` and `remove` all currently assume cannot exist, plus rollback paths and interaction with the handle-collision retry loop (team.ts:265-274), so it needs its own test sweep.
- **medium — No test anywhere covers the new compromised-lock abort branch added to config.update** — `src/lib/__tests__/config.test.ts:48`
  - Root cause: The lost-update guard is only reachable through proper-lockfile's internal refresh timer, and nothing in the code lets a test reach it. src/lib/config.ts:52-58 builds the lock options inline — no injection point, and no explicit `update` — so the library clamps the refresh to `stale/2` (node_modules/proper-lockfile/lib/lockfile.js:220-221: `options.update = Math.max(Math.min(options.update, options.stale / 2), 1000)`), i.e. 15s for config.json and 30s for safeWrite. `compromised` can therefore only flip 15s+ after a lock is stolen, which no test can wait for and no seam can fake. The result: `if (compromised) throw` at config.ts:62 and teamRepo.ts:100/:134, plus both `await release().catch(() => undefined)` swallows (config.ts:67, teamRepo.ts:156), are dead code from the suite's point of view — deleting any of them leaves the whole suite green and silently restores the stale-snapshot-renamed-over-another-writer bug the comment at config.ts:48-50 says the change exists to prevent. Secondary consequence of the same root cause: with a 15s refresh and config mutates that are all synchronous and finish in milliseconds (install.ts:76/:99/:115, share.ts:69, team.ts:220, login.ts:23, sync.ts:97), the guard is nearly unreachable in production too. (src/lib/config.ts:52-67 (inline lock options with no `update` and no seam; untested guard at :62 and swallow at :67), mirrored at src/lib/teamRepo.ts:91-100, :134, :156; missing coverage at src/lib/__tests__/config.test.ts:48-58)
  - Why it is a fork: This is a genuine fork, and it is a small product call rather than a technical unknown. Everyone agrees the safety check that says "another program grabbed the lock, so do not save" has never once been exercised by a test — delete it today and every test still passes. The question is how to test it. Option 1 makes the app check on its lock once a second instead of once every 15-30 seconds; that is a real (tiny) change to how the shipped tool behaves, and it also means the safety check can actually fire in real use, where today it almost never would. The price is two tests that genuinely wait about two seconds each, which is the classic source of occasional flaky failures. Options 2 and 3 leave the shipped behavior exactly as it is and instead give the tests a back door to pretend the lock was stolen: instant, never flaky, but they only prove our own code reacts correctly, and the protection stays nearly dormant in production. If nobody wants to make the call, take Option 1: it is the only one that also fixes the "the check would rarely fire for real" half of the problem, and the vendored lock helper in this same repo already pins an explicit refresh interval, so the change is in keeping with existing practice.
  - **Option 1: Real stolen-lock tests, with a 1s lock refresh to make the steal observable (recommended)** — Depth 4/4 · Cost 1/4. Production: add `update: 1_000` to the lock options at src/lib/config.ts:52-58 and src/lib/teamRepo.ts:91-97, matching the in-repo precedent at src/lib/placer/vendor/skillhub/skill-target-lock.ts:21-26 (which already pins `update: 3_000`); proper-lockfile allows a minimum of 1000ms and the refresh timer is unref'd (lockfile.js:176-182), so nothing keeps the CLI alive. Tests: (a) src/lib/__tests__/config.test.ts — an async mutate that `rm`s `<root>/config.json.lock` and awaits ~2s, asserting `store.update(...)` rejects /Lost the lock/, that config.json is byte-identical to before, and that a following `store.update` still succeeds (this last assertion is what pins the `release().catch(() => undefined)` swallow, since release rejects with 'Lock is already released' after a compromise — lockfile.js:183-200 deletes the registry entry); (b) src/lib/__tests__/teamRepo.test.ts — a `wrapRunner` hook (the seam already used at teamRepo.test.ts:304-310) that on the first `git push` deletes `<dirname>/.<basename>.safewrite.lock` and waits ~2s, asserting safeWrite rejects /Lost the safeWrite lock/, that origin/main is unmoved, and that the clone is clean; a second case that steals it before the first fetch covers the loop-top guard at teamRepo.ts:100. *Wins if:* You want the guard proven against the real proper-lockfile timer — that the option is still named `onCompromised`, that the ENOENT/ECOMPROMISED path actually fires it, and that release rejects afterwards — and you want the branch to be reachable in production (1s instead of 15s/30s), not only in a stub.
  - Option 2: Inject the lock function as a test seam and call onCompromised directly — Depth 3/4 · Cost 1/4. Add an optional lock seam to both sites and leave runtime timing untouched: `createConfigStore(root, lock: typeof lockfile.lock = lockfile.lock)` in src/lib/config.ts:29, and a `lock?` field on SafeWriteOptions (src/lib/teamRepo.ts:27-36, which already carries exactly this kind of test-only seam: `deadlineMs`, `backoff`, `now`, `sleep`). Tests pass a wrapper that delegates to the real `lockfile.lock` but captures `options.onCompromised`, then invokes it from inside the awaited mutate (config) or from a `wrapRunner` push hook (teamRepo), asserting the same rejections, byte-identical config.json, unmoved origin/main, and that the next update/safeWrite still works. No sleeps, fully deterministic, no production behavior change. *Wins if:* You would rather not touch runtime lock behavior at all and refuse timing-dependent tests, accepting that these tests prove only our own branch and would never catch proper-lockfile changing or renaming the callback that drives it.
  - Option 3: Mock proper-lockfile in the two test files only — Depth 3/4 · Cost 1/4. Zero production change: `vi.mock('proper-lockfile')` in src/lib/__tests__/config.test.ts and teamRepo.test.ts, delegating to `importActual` while exposing the captured `onCompromised` to the test, which fires it mid-mutate / mid-push and asserts the same rejections and byte-identical / unpushed state. Note this would be the suite's first module mock — grep shows zero `vi.mock`/`vi.spyOn`/fake timers anywhere under src/ — and it needs care with vitest hoisting plus the CJS default export. *Wins if:* You refuse both a production timing change and a test-only parameter on the public `createConfigStore` / `SafeWriteOptions` surfaces, and are willing to introduce module mocking as a new convention in an otherwise mock-free suite.
- **medium — No test for the new safeWrite lock-compromised abort, and the finally still hard-resets the shared clone** — `src/lib/__tests__/teamRepo.test.ts:304`
  - Root cause: The wave added a `compromised` flag to safeWrite (src/lib/teamRepo.ts:90/96) and two aborts (:100, :134) whose stated purpose is "aborts the attempt before anything is pushed, instead of two writers reset-and-committing over one working tree" — but the abort path still runs the destructive cleanup. The `finally` at src/lib/teamRepo.ts:143-151 unconditionally executes `git fetch origin`, `git reset --hard origin/main`, and the `created`-path removal loop in the SHARED per-team clone, with no reference to `compromised`. A compromised lock means, by proper-lockfile's own semantics (setLockAsCompromised fires only when the lockfile is gone or its mtime is no longer ours, i.e. another process took it), that a second process is now operating in that same clone. So the very throw that is supposed to keep us from touching the other writer's tree is immediately followed by the two commands that reset it. The blast radius: process A aborts at :100/:134, its `finally` reset lands between process B's `commit` (:133) and B's `push` (:135); B's HEAD is rewound to origin/main, so `git push -q origin HEAD:refs/heads/main` prints "Everything up-to-date" and exits 0, `outcome.ok` is true, and B returns `{ changed: true, pushedTo: 'main' }` for a mutation that never reached the remote — a silent false success, plus A's `removeCreated` loop can delete files B just wrote. None of this is caught by the suite: `compromised`, `lostLock` (:61), both guards and the `release().catch()` (:156) have zero test coverage (grep for `compromis` matches only src/lib/teamRepo.ts and src/lib/config.ts; the three tests this wave added to src/lib/__tests__/teamRepo.test.ts cover credential redaction, ref-lock retry and deletion restore), so the branch is both wrong and unexercised. Writing that test needs a seam the code does not currently expose: with the hardcoded `stale: 60_000` and no `update` option, proper-lockfile's updater only notices the theft after ~30s, so a deterministic test needs either `vi.mock('proper-lockfile')` (a pattern the repo does not yet use anywhere) or a test-only stale/update knob on SafeWriteOptions alongside the existing `now`/`sleep`/`backoff` knobs. (src/lib/teamRepo.ts:143 (the unconditional fetch/reset/remove cleanup in the `finally`; flag set at :90/:96, aborts at :100 and :134); missing coverage at src/lib/__tests__/teamRepo.test.ts:304)
  - Why it is a fork: A genuine fork, and it is a judgment call about how much of the problem to buy now. Option 1 is the exact repair of what this wave broke: the code decided to abandon the write because another program grabbed the shared folder, then went ahead and wiped that folder anyway. Stopping the wipe is three lines and cannot affect any normal run. But it only helps from the moment the lock library notices the theft — and that notice can arrive up to thirty seconds late. During that blind window the other program's own routine reset can still rewind our work, and our command will report "pushed to main" having pushed nothing. Option 2 adds one extra step (push the exact commit we just made, by name, instead of "whatever the folder currently points at"), which makes that false success impossible no matter who moved the folder. The cost of option 2 is that it touches the single push path every command in the product uses, so it needs its own verification, whereas option 1 touches only a path that runs when things have already gone wrong. Pick option 1 to close the finding cleanly in this hardening wave; pick option 2 if a command that says it saved your work when it did not is unacceptable even in a rare window.
  - **Option 1: Guard the cleanup on `compromised`, and cover the branch (recommended)** — Depth 3/4 · Cost 1/4. src/lib/teamRepo.ts:143-151 — wrap the cleanup body (`git fetch origin`, `git reset --hard origin/main`, and the `for (const path of created)` removal loop) in `if (!compromised) { ... }`, leaving only `await release().catch(() => undefined)` on the lost-lock path. Leaving the clone dirty is already sanctioned by the comment on :144 ("Cleanup can never change the outcome: the next safeWrite fetches and hard-resets anyway") and by the process that now owns the lock, which resets at :102 on its own next iteration. Then add the missing coverage: a case in src/lib/__tests__/teamRepo.test.ts that fires `onCompromised` mid-safeWrite — either `vi.mock('proper-lockfile')` passing through to the real module while capturing `options.onCompromised`, or a test-only stale/update knob on SafeWriteOptions so removing the `.<clone>.safewrite.lock` dir compromises within ~1s — asserting (a) rejection with /Lost the safeWrite lock/, (b) the bare remote's main sha is unchanged, (c) the aborting process ran no `reset --hard` after the throw (observable via wrapRunner); plus the mirror case in src/lib/__tests__/config.test.ts for the identical untested abort at src/lib/config.ts:62. *Wins if:* You want exactly the finding closed, with the source change confined to a branch that only executes when the lock is already lost, and no change to the push path every verb depends on.
  - Option 2: Also push the committed sha instead of `HEAD` — Depth 4/4 · Cost 2/4. Everything in option 1, plus: after the commit at src/lib/teamRepo.ts:133 capture `const head = (await requireGit(['rev-parse', 'HEAD'])).stdout.trim()` and pass it into `push(git, branch, head)` (src/lib/teamRepo.ts:176-191) so both refspecs become `${head}:refs/heads/<target>` instead of `HEAD:refs/heads/<target>`. This closes the residual hole option 1 cannot: proper-lockfile only reports the theft on its next update tick (up to `stale/2` = 30s later), so for that whole window another writer's own `reset --hard origin/main` at :102 can rewind our HEAD and silently turn our push into an exit-0 no-op even with the flag guarded. Pushing an explicit sha makes the outcome honest — the commit lands, or the push is rejected non-fast-forward and retried. Add a test that resets the clone between `commit` and `push` via wrapRunner and asserts the mutation actually reached origin. *Wins if:* You want the 'reported success, pushed nothing' class gone regardless of who rewinds the clone's HEAD — including the up-to-30-second window where the lock is already stolen but `onCompromised` has not fired yet, which no flag can cover.

### Declined by triage — overrule if you disagree (the standalone test applies) (0)

_none_

### Untriaged — the triage agent returned nothing usable; investigate by hand or resume the run (0)

_none_
