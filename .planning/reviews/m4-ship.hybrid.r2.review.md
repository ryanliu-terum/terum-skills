# ultrareview: m4-ship

Fix-scoped confirmation pass (round 2) over `b2ec572`, base `8cd84b3`. Verified against the worktree at `/Users/ryanliu/Documents/Terum/terum-codex/m4-ship`. **The four highs from the M4 full pass are structurally addressed** — the hook launcher fails open, deletions are refused, the `-z --no-renames` diff is rename-aware, stamping is per-team — but three of the four fixes carry a new defect of their own, and one high-severity leak was *introduced* by the push-guard fix. No critical findings. The dominant theme in the lows is that the fix diff's most load-bearing new behaviors (per-team stamp isolation, `localPushGuardLauncher`, `shellQuote`, `STAMP_SKEW_MS`) have no test that would fail if they were reverted — the fixes are correct but unpinned.

| Severity | Count |
|---|---|
| Critical | 0 |
| High | 1 |
| Medium | 9 |
| Low | 9 |
| **Total confirmed** | **19** |

Contested: 1 (listed separately, not counted). Unverified: 0.

---

## Terum invariants

- [ ] **HIGH — New push-guard refusals echo the raw remote; a push-by-URL credential lands on stderr and in git argv** — `src/commands/guardPush.ts:52` *(merges 4 reports across Terum invariants / Security & data-loss)*
  Lines 52 and 56 interpolate `args.remote` verbatim: `` `…${args.remote}/main could not be resolved … Run \`git fetch ${args.remote}\`` `` and `` `…Run \`git fetch ${args.remote}\` and retry` ``. `args.remote` is git's `$1` to the pre-push hook; per githooks(5) it *is* the credentialed URL when pushing by URL — the diff's own `mainOf` doc comment (line 71) says so. This violates the documented invariant at `src/lib/remote.ts:11-13` ("Embedded credentials … are removed before a remote reaches git argv, `.git/config`, or a message"), asserted by `src/lib/__tests__/remote.test.ts:127`. The same function already strips the *other* copy — lines 34 and 61 use `stripRemoteCredentials(args.url)` — so only the new lines leak. Line 73 additionally puts the token into a `git rev-parse` argv, visible to `ps`. Not reachable before this fix: `git show 8cd84b3:src/commands/guardPush.ts` never interpolates `args.remote`.
  **Fix:** compute `const remoteLabel = stripRemoteCredentials(args.remote)` once at the top of `run` and use it at :52 (both interpolations) and :56. At line 73, skip the `refs/remotes/<remote>/main` lookup when the parameter parses as a URL (it can never resolve anyway) so no credential reaches git argv. Correct sibling call-sites for the sweep: guardPush.ts:34/61, teamRepo.ts:181/331, publish.ts:112/172, remote.ts:221, setup.ts:161/178/195, team.ts:195, leave.ts:33.

- [ ] **MEDIUM — `reclaimStaleLock`'s restore rename overwrites a lock acquired during the move-aside window, so two holders can run at once** — `src/lib/hook.ts:215` *(merges 6 reports across Terum invariants / Security & data-loss / Correctness & tests / Reuse)*
  The move-aside at :212 is unconditional, so between :212 and :215 the lock path is *free* and a concurrent `acquireTeamLock` wins `open(path,'wx')`. On the mismatch branch, `await rename(aside, path)` (:215) then clobbers that fresh holder's record and hands the file back to the live holder — two concurrent `sync --hook` runs on one team, the exact failure §8's mutex exists to prevent. The docstring's "act only on the exact record they … judged" holds for the removal but not the move-aside. Reachable in production: §8 treats any lock older than `LOCK_STALE_MS` as stale "whoever holds it" (hook.ts:156), so C can hold B-era judgement bytes while B is live; C moves B's record aside, D acquires in the window, C restores B over D. Line 215 can also leave `<team>.lock.stale-<uuid>` behind, which nothing sweeps.
  **Fix:** make the restore fail-if-present — `link(aside, path)` then `unlink(aside)`, treating EEXIST as "a new holder legitimately owns it, drop the aside copy" — or re-create with `open(path,'wx')` instead of `rename`. Add a `run/*.lock.stale-*` sweep on acquire or in leave.ts:48. (Alternative worth deciding once: drop the hand-rolled mutex for `proper-lockfile`, already a direct dependency at config.ts:57, teamRepo.ts:100/403, skill-target-lock.ts:22 — r1's recommendation, still open.)

- [ ] **MEDIUM — The stamp loop re-reads teams after all work, so work that appeared mid-run is stamped as done** — `src/commands/sync.ts:220`
  `for (const team of Object.keys((await store.read()).teams))` takes a *fresh* read, while every per-team pass ran against the snapshot from sync.ts:62 and `config.pending` filtered at :88. `ConfigStore.read()` re-reads with no cache (config.ts:37-42), so state another process wrote *during* the run is visible at :220 but was never processed. Failure: the hook finishes team T's pending loop at 10:00:00; `install --project` records a pending entry for T at 10:00:05; the run stamps T at 10:00:20. The session opened inside the matching checkout at 10:05 — the run that deferral is waiting for — is a silent no-op until 11:00. That is exactly what the new "leaves a project pending install deferred outside its worktree — without stamping" test was added to prevent. A team joined concurrently between :62 and :220 is stamped without ever being refreshed.
  **Fix:** iterate the `config.teams` snapshot from :62 rather than a fresh read, and skip any team the final read still shows pending entries for (`final.pending.some((entry) => entry.team === team)`) — the same completeness predicate `incomplete` expresses.

- [ ] **MEDIUM — Push-guard fail-open is only tested one file-existence deep; any CLI-internal failure blocks every push from the clone** — `src/lib/__tests__/teamRepo.test.ts:421`
  The new test proves fail-open for exactly one cause (a missing launcher file), and the hook's guard is only `[ -x <node> ] && [ -f <entry> ]` (teamRepo.ts:367). But the doc comment at teamRepo.ts:361 asserts "A non-zero exit is therefore always the guard's own refusal", which is false: `guardPush.run` funnels every internal error into `failure(...)` (guardPush.ts:63) and `createExecute` maps any failing Result to exit 1 (execute.ts:22-26). A corrupt `~/.terum/skills/config.json`, a schema-parse error, or any unexpected throw makes the launcher present-but-failing and the user cannot `git push` from the team clone at all — with a message that never says the push was unchecked or that `--no-verify` exists.
  **Fix:** either give guardPush a distinct exit code for "could not evaluate for an infrastructure reason" that the hook maps to the same fail-open warning, or drop the "always the guard's own refusal" claim at teamRepo.ts:361. Add a case where the launcher exists but exits non-zero for a non-guard reason.

- [ ] **LOW — `team leave` cleans two `run/` artifacts but not the `.lock.stale-<uuid>` aside a crashed reclaim leaves** — `src/commands/leave.ts:50`
  The comment claims completeness ("Both §8 run artifacts"), but `reclaimStaleLock` (hook.ts:210-217) creates a third: `${path}.stale-${randomUUID()}`. A process killed between the rename and the rm/rename-back leaves it forever — nothing in the codebase removes it, it survives `team leave` and a rejoin under the same name, and accumulates in a directory otherwise kept to two files per team. The new mutex test only asserts no `.stale-` litter on the success path.
  **Fix:** derive the artifact set from the directory — a `runArtifacts(storeRoot, team)` helper in lib/hook.ts (which already owns `stampPath`/`lockPath`) that readdirs `run/` and removes `${name}.stamp`, `${name}.lock`, `${name}.lock.stale-*`. Extend the leave test's ENOENT assertions with a seeded stale aside.

## Security & data-loss

- [ ] **MEDIUM — Per-team stamp fix misses the four "Blocked …" branches: a partial sync is still stamped "fully synced"** — `src/commands/sync.ts:220` *(merges 7 reports across Security & data-loss / Terum invariants / Correctness & tests)*
  The fix's own comment claims "a team this run … left work undone in (a deferral, a blocked placement), keeps its old stamp", but `incomplete` is populated only by `defer()`. Four placement outcomes `notice()` and `continue` without it: sync.ts:130 (skill no longer in repo), :134 (pinned version not in clone), :140 (allowed-tools malformed), :160 (foreign collision). Spec §8: "**The stamp is written only after a sync that fully succeeded.**" Failure: a force-pushed history sends a pinned placement down the :134 branch; the run prints one line to stderr and stamps `run/team.stamp`; every `sync --hook` for the next hour takes the :72 `stampIsFresh` early-continue — no fetch, no pending replay, no orphan pass, no repeat of the notice. The contrast is inside this same diff: the CloneBusy path (:83) deliberately uses `skipped.add(team)`. The new test at sync.test.ts:198 exercises only the *thrown-error* blocked path (which does defer via the :186 catch); the four early-continue branches have no test.
  **Fix:** pick one rule and apply it at all five sites. The coherent rule: mark incomplete where a retry can plausibly succeed (:134 and :130, transient clone-behind state), stamp where a human is needed (:140 malformed frontmatter, :160 foreign collision) — then narrow the :216-219 comment to match. A `blocked(team, line)` wrapper that both notices and marks incomplete stops the two loops drifting again. Same class of untracked-but-unfinished work exists in `reconcileShared` (share.ts:81/84/85/104/105/111), which is not even handed `defer` — worth a sweep decision at the same time.

- [ ] **MEDIUM — `team leave` deletes a live §8 team mutex it never holds, so an in-flight hook sync is not serialized against** — `src/commands/leave.ts:50`
  leave.ts:44-52 removes `lockPath(store.root, name)` unconditionally inside `withCloneLock`, but `leave` never *acquires* that lock. The new test cements deleting a live one: leave.test.ts:26-27 seeds `{ pid: process.pid, host: 'this-host', started: new Date().toISOString() }` — a record `judgeLock` classifies as `{kind:'live'}` — and :31 asserts it is gone. The comment says "a hook killed mid-sync", but the record written is a *running* holder, so the test cannot distinguish a stale-only reclaim from unconditional destruction. Failure: a `sync --hook` for T is inside the placement loop (holds the team lock, not the clone lock, so `withCloneLock` does not refuse); `team leave T` deletes the live lock; a third hook then finds `run/T.lock` free and runs concurrently. Worse, the still-running sync can `place()` into `~/.claude/skills/<name>` after leave removed it, and its `store.update` bails on `if (!placement) return` (sync.ts:170-175) — the resurrected folder lands with no ledger entry, so the next `team join` sees `collision.kind === 'foreign'` (:160) and blocks until the user clears it by hand.
  **Fix:** take the mutex instead of destroying it — `acquireTeamLock(store.root, name)` before the destructive phase, refusing on `null` with the shape leave already uses for a busy clone (leave.test.ts:125-137), releasing in a `finally` (the release is token-guarded, so the lock file goes with it). If unconditional removal is deliberate, make the test seed a *dead* pid / >10-minute-old `started` and add a second case pinning the live-lock behavior explicitly.

- [ ] **LOW — M2 walkthrough hook assertions cannot distinguish "nothing to do" from "every placement blocked"** — `src/__tests__/m2-walkthrough.test.ts:50`
  The clock advance is the right fix (the run really executes now), but the assertion set is blind to the failure mode the newly-live run can have. In hook mode `notice()` (sync.ts:46) never prints — lines ride `SyncResult.notices` — so `hook.lines` is empty for any hook run that places nothing, and the four blocked outcomes (:130/:134/:140/:160) add to `notices` without touching `deferred`. A regression blocking both of B's placements would still produce `placed: 0, deferred: [], hook.lines: []` with both copies byte-identical. The sibling added in this same diff already asserts the stronger shape (sync.test.ts:643).
  **Fix:** add `notices: []` to the `toMatchObject` at m2-walkthrough.test.ts:50.

## Correctness & tests

- [ ] **MEDIUM — New throws in the hook's per-team lock setup abort the whole sync run instead of costing one team** — `src/lib/hook.ts:193` *(merges 2 reports across Correctness & tests / Security & data-loss)*
  `judgeLock` now rethrows anything that is not ENOENT (`catch (error) { if (…code === 'ENOENT') return { kind: 'vanished' }; throw error; }`), where the old `lockIsStale` returned `false` for every error; and `mkdirPrivate(dirname(path))` (:168, replacing a plain `mkdir`) throws on a `run/` that is a symlink or foreign-owned (fs.ts:24-25). Both escape `acquireTeamLock`, whose call site at sync.ts:73 sits *outside* the per-team try/catch (which starts at :77). The exception unwinds the whole loop into the outer catch at sync.ts:222 and returns `failure(...)` — every remaining team is dropped and the hook exits 1, contradicting the contract three lines above the loop (sync.ts:63-67). Reachable with a `run/<team>.lock` left by a `sudo`-run CLI (EACCES) or a restored `run/` with foreign ownership.
  **Fix:** wrap the hook-mode gate (sync.ts:71-76) in its own try/catch doing `notice(...); skipped.add(team); continue;` — the shape the `CloneBusy` handler already uses at :79-83 — or have `acquireTeamLock` return null for unexpected filesystem errors after surfacing them. Add a mutex test making `judgeLock`'s read fail non-ENOENT and asserting the other team still syncs.

- [ ] **LOW — M2 §12 walkthrough asserts none of the two behaviors its name claims (unpinned tracking, widened-grant deferral)** — `src/__tests__/m2-walkthrough.test.ts:50` *(merges 2 reports across Correctness & tests / Terum invariants)*
  The test is named "keeps B pinned, tracks B unpinned, and defers A's widened tool grant in hook sync", but with the hook now really running it asserts `deferred: []`. (1) B's only copy of `guarded` was installed pinned (line 30), so the loop resolves from `materializeVersion(entry.version)` — the old `Bash(ls)` tree — and `approved()` passes; the widened grant is never evaluated for B. (2) B's unpinned skill is `plain`, and nothing changes `plain` upstream after line 41, so line 55 asserts it did *not* change. Not a product bug — hook-mode grant deferral is genuinely covered at sync.test.ts:484/:503 — but the milestone acceptance walkthrough advertises coverage it lacks, so the M2 exit criterion is misread as verified.
  **Fix:** give B a second, *unpinned* placement of `guarded` (or push a benign `plain` edit) before line 50, then assert `{ placed: 1, deferred: ['guarded'] }` with `hook.lines` still `[]` — that covers both branches in one run. If the walkthrough should stay minimal, rename it to what it verifies ("keeps B's pinned copy untouched when A widens the grant") so the missing branch is not double-counted.

- [ ] **LOW — `shellQuote`'s whole reason for existing — paths with spaces or quotes — is never exercised** — `src/lib/teamRepo.ts:350`
  `shellQuote` is documented as "a HOME with a space or a quote is still one word" and is the only thing standing between a launcher path and word-splitting in the generated hook (teamRepo.ts:367-374). Every test that *runs* the generated script uses space-free paths under `temporaryDirectory()`/`mkdtemp(tmpdir())` (teamRepo.test.ts:415-419, bin.test.ts:70). Removing the quoting entirely — the exact regression it prevents, and the one that bites the `/Users/My Name/` install — leaves the suite green. The npx fallback body is likewise only string-matched (teamRepo.test.ts:425-427), never executed, so its fail-open branch is unverified.
  **Fix:** add one case writing the stub launcher into a directory whose name contains a space and a single quote (e.g. `join(fixture.root, "a dir's name")`), generating the hook with that entry and asserting the same `code: 3` / flat-argument result — plus one running `pushGuardHook(null)` with `npx` stripped from PATH, asserting exit 0 and the "NOT checked" warning.

- [ ] **LOW — `stampIsFresh`'s future-skew tolerance is tested only at 24h, so dropping `STAMP_SKEW_MS` entirely would pass** — `src/lib/__tests__/hook-mutex.test.ts:122`
  hook.ts:147 reads `age > -STAMP_SKEW_MS && age < STAMP_FRESH_MS`, with `STAMP_SKEW_MS = 60_000` documented as existing for "filesystem timestamp granularity and rounding, not a clock step". The added assertion uses a 24-hour step, which `age >= 0 && age < STAMP_FRESH_MS` or `Math.abs(age) < STAMP_FRESH_MS` both satisfy — while breaking the case the constant was added for: a stamp a few hundred ms ahead of `Date.now()` reads as not-fresh and every hook run re-syncs.
  **Fix:** add the boundary the constant defines — `stampIsFresh(root, 'team', () => Date.now() - 30_000)` → `true`, alongside the 24h case that is `false`.

## Reuse, simplify, perf

- [ ] **MEDIUM — The new deletion refusal breaks publish's own printed remediation `git push origin --delete <branch>`** — `src/commands/guardPush.ts:46` *(merges 5 reports across Reuse / Correctness & tests / Security & data-loss)*
  The fix turns every deletion into a hard refusal (guardPush.ts:46), pinned by guard-push.test.ts:72. But publish.ts:173 still prints `` delete the branch with `git push origin --delete ${branch}` ``, and the only repo where `origin` is the team remote is the team clone — the one repo `installPushGuard` arms, now on every join as well as every clone (team.ts:392, teamRepo.ts:389). git does run pre-push for a delete-only push. So a user who hits the `assertBranchesReusable` wall, follows the printed command from `~/.terum/skills/teams/<team>`, gets a second refusal instead of the unblock. Every internal push (teamRepo.ts:199/210, team.ts:494) already passes `--no-verify`, so this printed command is the one push path in the product the fix regressed.
  **Fix:** print `git push --no-verify origin --delete ${branch}` at publish.ts:173 (a branch deletion is an explicit, attributed act, and it is the shape the guard's own message names) or point at the host UI; update the two regex assertions at publish.test.ts:148 and :192 that match `--delete publish/sample`. Sweep: `grep -rn 'git push' src --exclude-dir=__tests__ | grep -v no-verify` currently returns only this site plus team.ts:530's generated GitHub Action, which runs outside the armed clone.

- [ ] **MEDIUM — Per-team stamp isolation — the exact behavior the `incomplete` Set fix added — has no test** — `src/commands/__tests__/sync.test.ts:119` *(merges 3 reports across Reuse / Correctness & tests)*
  The fix replaced a run-wide notion with a per-team set (sync.ts:37-38) gated at :220, with the comment "one team's deferral never withholds another's stamp". No test exercises that claim. The changed test is the only multi-team test reaching a deferral and cannot distinguish: `other.stamp` is asserted to exist at :112 *inside* the held-lock block, and the final run at :117 re-checks only that `team.stamp` is absent (:119) — `other.stamp` already exists from the earlier run, so a stale file is indistinguishable from a freshly written one. The only other two-team test (:597, "two teams do not serialize") exercises the `skipped` path; nothing defers in it. Replacing `const incomplete = new Set<string>()` with `let incomplete = false` would make every team lose its stamp whenever any one deferred, and the whole suite would still pass.
  **Fix:** in the changed test, `rm` `stampPath(store.root, 'other')` (or capture its mtime) after the `finally { await release(); }` and before the :117 run, then assert it is written again while `team.stamp` stays ENOENT — `other` is complete in that run, so the assertion is deterministic. Or add a deferral case to the two-team test at :597.

- [ ] **MEDIUM — `localPushGuardLauncher()` is never executed by any test** — `src/lib/__tests__/teamRepo.test.ts:411`
  `localPushGuardLauncher()` (teamRepo.ts:344) decides, on a real install, whether the pre-push hook launches an absolute `node dist/index.js` or degrades to `npx` on git's blocking path. Under vitest `import.meta.url` is `.../src/lib/teamRepo.ts`, so `../index.js` resolves to `src/index.js`, which does not exist — every unit-test arming silently takes the `null`/npx branch. The arming test asserts only `toContain('guard-push')` (satisfied by both forms), and the `@latest` assertions at :428-429 call `pushGuardHook(null)` directly, bypassing the resolver. `src/__tests__/bin.test.ts` is the one place a real built tree exists (`out/index.js` beside `out/lib/`) and it opts out at :71 by passing an explicit launcher. Changing the resolver's relative path or its `existsSync` check would ship a hook that runs `npx -y terum-skills@<version>` on every push — a network fetch on git's blocking path, running published rules rather than installed ones — with zero failing tests.
  **Fix:** in bin.test.ts, drive one arming through the built CLI's own default (call `installPushGuard(clone, systemRunner)` from the built `out/lib/teamRepo.js` via a small `node -e` in `out/`, or assert the hook an end-to-end `team join` wrote) and assert the body contains the absolute `resolve(out, 'index.js')` and not `npx`.

- [ ] **LOW — win32 guard on the hook-mode assertion is dead: the same test unconditionally spawns `sh`** — `src/lib/__tests__/teamRepo.test.ts:410` *(merges 3 reports across Reuse / Correctness & tests / Security & data-loss)*
  Line 410 guards one assertion (`if (process.platform !== 'win32') expect((await stat(hook)).mode & 0o777).toBe(0o700);`), but the `sh` helper eight lines above (`spawn('sh', [script, ...args], …)`, :397) is invoked unconditionally at :418 and :422. On Windows the spawn emits ENOENT and closes with `code: null`, so `expect(ran).toEqual({ code: 3, … })` fails regardless — the platform check buys nothing. Every sibling that cannot run on Windows uses the skip form (sync.test.ts:186, hook-mutex.test.ts:105).
  **Fix:** use `it.skipIf(process.platform === 'win32')` for the whole case and drop the inline `if` at :410; the `pushGuardHook(null)` / `@latest` string checks can move to a platform-independent case.

- [ ] **LOW — `treeBetween` serializes two git spawns per wanted path on git's blocking pre-push path** — `src/commands/guardPush.ts:93`
  The `Promise.all` at :94 parallelizes only the two ends of one path; the `for` loop awaits each path in turn, so a push touching K skill folders costs 2K strictly-serialized `git show` processes while `git push` blocks. The narrowing this fix added (team.json + one SKILL.md per folder) removed the worst of it, but the remaining serialization is free to drop — the file's own sibling `skillTrees` (teamRepo.ts:267) shows the house preference for resolving many blobs in one pass. The loop re-runs per ref group, so a multi-ref push multiplies it.
  **Fix:** `await Promise.all([...wanted].map(async (path) => { … }))`, populating the two maps inside. (`git cat-file --batch` would be the single-call version, but the `Runner` interface has no stdin.)

- [ ] **LOW — `search.ts` is the last hand-rolled `run/<team>.stamp` + `3_600_000` copy after this diff consolidated the other sites** — `src/commands/sync.ts:274`
  Two spellings of the same knowledge are left behind. (1) The changed line hand-joins the directory it just imported a helper for: `mkdirPrivate(join(store.root, 'run'))` beside `stampPath(store.root, team)`. (2) `search.ts:66` owns a private copy of both the path and the freshness rule — `now() - (await stat(join(store.root, 'run', \`${team}.stamp\`))).mtimeMs > 3_600_000` — which is `stampIsFresh` inverted, minus the `STAMP_SKEW_MS` guard this fix added a test for. `now() - mtimeMs` is negative for a future-dated stamp and therefore never `> 3_600_000`, so after a backwards clock step or a `run/` copied from another machine, `search` silently suppresses its "may be stale" hint in exactly the scenario hook.ts now documents as untrustworthy.
  **Fix:** replace search.ts:65-68's body with `if (!(await stampIsFresh(store.root, team, now))) io.print(...)` (it already returns false on ENOENT, matching the current catch), and swap sync.ts:274's `join(store.root, 'run')` for `dirname(stampPath(store.root, team))` — or export a `runDir(storeRoot)` from hook.ts beside `stampPath`/`lockPath` and use it in all three places.

- [ ] **LOW — New `sh` helper inherits the ambient env, then asserts `stderr: ''` exactly — the flake bin.test.ts already defends against** — `src/lib/__tests__/teamRepo.test.ts:419`
  The helper at :397 spawns with no `env`, so the child inherits `process.env` including `NODE_OPTIONS`, and :419 asserts the launched node process wrote nothing at all to stderr. The sibling suite in this same batch treats that as a known hazard and pins it with a comment saying so — bin.test.ts:36-37: "An explicit child env: no inherited NODE_OPTIONS or warnings on stderr", building `{ PATH, HOME, USERPROFILE, GH_CONFIG_DIR, NODE_NO_WARNINGS: '1' }`. Any developer or CI runner with `NODE_OPTIONS` set, or a node emitting an ExperimentalWarning, fails this on an unrelated cause.
  **Fix:** pass an explicit env to `sh` (`env: { PATH: process.env.PATH ?? '', NODE_NO_WARNINGS: '1' }`) the way bin.test.ts:37 does, or relax :419 to assert `stderr` does not contain the guard's own warning rather than exact emptiness.

---

## Contested (split adversarial verdict — needs human adjudication)

- **(low / Reuse, simplify, perf) cli.test.ts asserts the guard-push verb exists but never that it is hidden, despite the test name and an in-file pattern for it** — `src/__tests__/cli.test.ts:136` *(vote 2-1)*

  The new describe is titled "wires the hidden `guard-push` verb" and `src/cli.ts:80` registers it as `.command('guard-push <remote> <url> [refs...]', { hidden: true })`, but hiddenness is never asserted. The only structural assertion is a tautology:

  ```ts
  expect(program.commands.find((command) => command.name() === 'guard-push')).toBeDefined();
  ```

  which cannot fail — the `parseAsync` on line 134 already dispatched to the `guardPush` stub and line 135 asserted the arguments, so the command provably exists. The file already has the assertion that would carry weight, at line 92 for the other hidden verb: `expect(program.helpInformation()).not.toContain('readme');`. Dropping `{ hidden: true }` from cli.ts:80 would surface an internal hook verb in `terum-skills --help` with no failing test. (bin.test.ts:43-45 only asserts help *contains* 'terum-skills'.)

## Unverified (not adversarially checked — efficient mode)

(none)

## Triage — 19 confirmed → 9 mechanical · 10 clear · 0 fork · 0 declined

Buckets are derived in code from each finding's ratings (single-fix gate: trivial + low risk + isolated + patch = mechanical). NOTHING here has been applied.

### Mechanical — one fix, trivial / low-risk / isolated, patch supplied; batch-apply on ONE confirmation (9)

- **high — New push-guard refusals echo the raw remote — a push-by-URL credential lands on stderr (and in git argv)** — `src/commands/guardPush.ts:52`
  - Root cause: `args.remote` is git's pre-push `$1` — the push target as typed, which per githooks(5) is the credentialed URL itself when someone pushes by URL ("if a named remote is not being used both values will be the same"). The fix diff added three new uses of it that skip the house scrubber: two stderr messages (guardPush.ts:52 twice, :56 once) and, via `forkPoint`/`mainOf`, a `git rev-parse --verify --quiet refs/remotes/<remote>/main` argument (:51 → :73). Every sibling use of the same push target in this very file goes through `stripRemoteCredentials` (:34, :61), and the helper is already imported at :5, so the new lines are the only ones that break the invariant documented at src/lib/remote.ts:11-13 ("Embedded credentials … are removed before a remote reaches git argv, `.git/config`, or a message") and asserted in src/lib/__tests__/remote.test.ts:127. `mainOf`'s own doc comment at :71 states the URL case explicitly, so the code knows `$1` can be a URL and still interpolates it raw. Result: `git push https://user:$TOKEN@host/org/repo.git publish/mine` from an armed clone with `origin/main` unfetched writes the token to stderr (the CI build log) and puts it in a `git rev-parse` argv where `ps` can read it. (/Users/ryanliu/Documents/Terum/terum-codex/m4-ship/src/commands/guardPush.ts:52 (same defect at :56 and, through forkPoint at :51, at mainOf :73))
  - Fix: In `run` (guardPush.ts, after line 36) add `const remoteLabel = stripRemoteCredentials(args.remote);` with a one-line comment saying why (git's `$1` is the credentialed URL when pushing by URL). Use `remoteLabel` in the two interpolations on line 52 and the one on line 56, and pass it to `forkPoint` on line 51 so `mainOf`'s `refs/remotes/<remote>/main` argument is credential-free too. Add one assertion to the existing fork-point test in src/commands/__tests__/guard-push.test.ts (after the `origin/main` deletion at line 94) that a run with `remote: 'https://user:ghp_secret_token@github.com/org/team.git'` refuses with an error that does not contain the token.

```diff
--- a/src/commands/guardPush.ts
+++ b/src/commands/guardPush.ts
@@ -35,3 +35,6 @@
     const [team, binding] = configured;
     const author = config.display_name && config.email ? `${config.display_name} <${config.email}>` : undefined;
+    // git's `$1` is the push target as typed — the credentialed URL itself, not a name, when someone
+    // pushes by URL (githooks(5)) — so it is scrubbed once here: no message and no git argument below echoes a token.
+    const remoteLabel = stripRemoteCredentials(args.remote);
     const git: Git = (parts) => runner.run('git', parts, { cwd });
@@ -51,6 +54,6 @@
-      const base = ZERO_SHA.test(remoteSha) ? await forkPoint(git, args.remote, localSha) : remoteSha;
-      if (base === null) throw new Error(`Push guard: ${remoteRef} is new and ${args.remote}/main could not be resolved to check it against, so ownership cannot be checked. Run \`git fetch ${args.remote}\`, or bypass with \`git push --no-verify\` (attributed to you).`);
+      const base = ZERO_SHA.test(remoteSha) ? await forkPoint(git, remoteLabel, localSha) : remoteSha;
+      if (base === null) throw new Error(`Push guard: ${remoteRef} is new and ${remoteLabel}/main could not be resolved to check it against, so ownership cannot be checked. Run \`git fetch ${remoteLabel}\`, or bypass with \`git push --no-verify\` (attributed to you).`);
       // `--no-renames`: a rename is a delete plus an add, so the path being taken away is judged too — the
       // spelling safeWrite uses. `-z`: paths arrive verbatim, never C-quoted.
       const listed = await git(['diff', '--name-only', '--no-renames', '-z', base, localSha]);
-      if (listed.code !== 0) throw new Error(`Push guard could not diff ${base.slice(0, 8)}..${localSha.slice(0, 8)}: ${(listed.stderr || listed.stdout).trim()}. Run \`git fetch ${args.remote}\` and retry, or bypass with \`git push --no-verify\` (attributed to you).`);
+      if (listed.code !== 0) throw new Error(`Push guard could not diff ${base.slice(0, 8)}..${localSha.slice(0, 8)}: ${(listed.stderr || listed.stdout).trim()}. Run \`git fetch ${remoteLabel}\` and retry, or bypass with \`git push --no-verify\` (attributed to you).`);
--- a/src/commands/__tests__/guard-push.test.ts
+++ b/src/commands/__tests__/guard-push.test.ts
@@ -94,3 +94,5 @@
     await git(['update-ref', '-d', 'refs/remotes/origin/main'], clone);
     expect(await run({ remote: 'origin', url: fixture.bare, refs: ['refs/heads/publish/mine', own, 'refs/heads/publish/mine', ZERO], cwd: clone, config: store }, io)).toMatchObject({ ok: false, error: expect.stringMatching(/origin\/main could not be resolved[\s\S]*git push --no-verify/) });
+    // git hands the hook the push target as `$1` — the credentialed URL itself when someone pushes by URL — and a refusal must never echo it.
+    expect(await run({ remote: 'https://user:ghp_secret_token@github.com/org/team.git', url: fixture.bare, refs: ['refs/heads/publish/mine', own, 'refs/heads/publish/mine', ZERO], cwd: clone, config: store }, io)).toMatchObject({ ok: false, error: expect.not.stringContaining('ghp_secret_token') });
   });
```

- **medium — The new deletion refusal breaks publish's own printed remediation `git push origin --delete <branch>`** — `src/commands/guardPush.ts:46`
  - Root cause: The fix diff hardened the pre-push guard so that ANY deletion line is an unconditional refusal (src/commands/guardPush.ts:46 — `if (ZERO_SHA.test(localSha)) throw new Error(\`Push guard refused deleting ${remoteRef}: a deletion is pure loss and cannot be shown to be yours (D12). Delete it on the host, or bypass with \`git push --no-verify\` ...\`)`), and it also made that guard armed on every clone AND every join (src/lib/teamRepo.ts:389 installPushGuard, called from src/commands/team.ts:392). What the fix did NOT do is sweep the one user-facing remediation string that instructs a plain deletion: src/commands/publish.ts:173 still prints ``delete the branch with `git push origin --delete ${branch}` `` — a command that only means anything inside the team clone (`store.teamClone(team)`, publish.ts:37), which is precisely the repo the same diff arms. So the product's own printed unblock for the `assertBranchesReusable` wall is now refused by the product's own hook: git runs pre-push for a delete-only push and hands it `(delete) 000… refs/heads/publish/<name> <sha>`, the exact shape src/commands/__tests__/guard-push.test.ts:72 asserts is refused. Every internal push already passes `--no-verify` (teamRepo.ts:199,210; team.ts:494), and team.ts:530's `git push` lives in the generated GitHub Action which runs in CI with no hook, so publish.ts:173 is the single regressed path. The M3 spec pinned that wording (.planning/specs/2026-09-05-m3-publish-leave.md:75), but it predates the M4 deletion refusal, so it is stale text rather than a load-bearing reason. (src/commands/publish.ts:173 (the stale advice), contradicted by src/commands/guardPush.ts:46 (the new refusal))
  - Fix: src/commands/publish.ts:173 — change the printed remedy from `git push origin --delete ${branch}` to `git push --no-verify origin --delete ${branch}` (and mention deleting on the host), so the advice names the same escape hatch the guard's own refusal names. Tighten the two regex assertions at src/commands/__tests__/publish.test.ts:148 and :192 to pin `--no-verify origin --delete publish/sample…` (they currently only match `--delete publish/sample`, so they pass either way — pinning stops the advice drifting back).

```diff
--- a/src/commands/publish.ts
+++ b/src/commands/publish.ts
@@ -170,6 +170,6 @@
     if (!sha) continue;
     if (await isExactlyThisEndorsement(runner, clone, sha, id, scope, remote)) continue;
     const where = compare(remote, branch) ?? `${stripRemoteCredentials(remote)} — branch ${branch}`;
-    throw new Error(`${branch} already exists on the remote with a different endorsement (${where}). Merge or close its pull request, or delete the branch with \`git push origin --delete ${branch}\`, then retry.`);
+    throw new Error(`${branch} already exists on the remote with a different endorsement (${where}). Merge or close its pull request, or delete the branch on the host — or with \`git push --no-verify origin --delete ${branch}\`, since the push guard refuses an unattributed deletion (D12) — then retry.`);
   }
 }
--- a/src/commands/__tests__/publish.test.ts
+++ b/src/commands/__tests__/publish.test.ts
@@ -146,4 +146,4 @@
       const theirRunner = mappedRunner(REMOTE, other.fixture.bare);
       const refused = await run({ ref: 'sample', config: other.store, runner: theirRunner }, new ScriptedPrompter());
-      expect(refused, label).toMatchObject({ ok: false, error: expect.stringMatching(/publish\/sample already exists on the remote with a different endorsement[\s\S]*compare\/main\.\.\.publish\/sample[\s\S]*--delete publish\/sample/) });
+      expect(refused, label).toMatchObject({ ok: false, error: expect.stringMatching(/publish\/sample already exists on the remote with a different endorsement[\s\S]*compare\/main\.\.\.publish\/sample[\s\S]*--no-verify origin --delete publish\/sample/) });
       expect(await originSha(other.fixture.bare, 'publish/sample')).toBe(theirs);
       expect(await originSha(other.fixture.bare)).toBe(otherMain);
@@ -190,4 +190,4 @@
     const runner = mappedRunner(REMOTE, fixture.bare);
     const refused = await run({ ref: 'sample', config: store, runner }, new ScriptedPrompter());
-    expect(refused).toMatchObject({ ok: false, error: expect.stringMatching(/publish\/sample-2 already exists on the remote with a different endorsement[\s\S]*--delete publish\/sample-2/) });
+    expect(refused).toMatchObject({ ok: false, error: expect.stringMatching(/publish\/sample-2 already exists on the remote with a different endorsement[\s\S]*--no-verify origin --delete publish\/sample-2/) });
     expect(await originSha(fixture.bare, 'publish/sample-2')).toBe(theirs);
     expect(await originSha(fixture.bare)).toBe(main);
```

- **medium — Per-team stamp isolation — the exact behavior the `incomplete` Set fix added — has no test; a regression to a single boolean would pass the whole suite** — `src/commands/__tests__/sync.test.ts:119`
  - Root cause: The fix's central contract — sync.ts:36-37 "Tracked per team: one team's deferral never withholds another's stamp", enforced by the per-team gate `if (!skipped.has(team) && !incomplete.has(team))` at src/commands/sync.ts:220 — has no test that can distinguish it from a run-wide boolean. Every test that reaches a deferral is single-team (sync.test.ts:161, 186, 377, 638), and every multi-team test either defers nothing (sync.test.ts:126 `deferred: []`; sync.test.ts:597 mutex-skipped, `notices: []`) or, in the one case that does defer (sync.test.ts:117), asserts `other.stamp` exists only at line 112 *before* the deferring run, inside the held-lock block, and never re-checks it afterwards — so a stale `other.stamp` left over from the earlier run is indistinguishable from a freshly written one. Concretely: replacing `const incomplete = new Set<string>()` (sync.ts:37) with `let incomplete = false` would stop `other` from being stamped by the run at sync.test.ts:117, yet the file already exists on disk from the run inside the try block, so `access` still resolves and no assertion in the suite fails. I verified the fixture facts that make this decidable: `TEAM_JSON` in src/lib/__tests__/fixtures.ts:52 has `global: []`, so a second `bareTeam()` team has no endorsed candidates and nothing to defer, and `ScriptedPrompter`'s `interactive` defaults to `false` (fixtures.ts:20), so the line-117 run really takes the non-interactive defer branches. (src/commands/__tests__/sync.test.ts:117-119 (missing coverage for the gate at src/commands/sync.ts:220))
  - Fix: Add one test in src/commands/__tests__/sync.test.ts (immediately after the existing 'defers newly endorsed global skills in hook mode' test at line 54-63, which already proves the deferral setup works): `configuredSkill()` for team `team`, push a team.json with `global: [ID]` so `team` has an endorsed candidate a hook run cannot ask about; add a second team `other` via `bareTeam()` + `cloneWithIdentity(other.bare, store.teamClone('other'))` + `store.update` binding (default TEAM_JSON has `global: []`, so `other` is clean); run once in hook mode with no stamps on disk and assert `deferred: ['sample']`, `access(stampPath(store.root,'team'))` rejects ENOENT, and `access(stampPath(store.root,'other'))` resolves. Neither stamp exists beforehand, so the `other` assertion can only pass if THIS run wrote it.

```diff
--- a/src/commands/__tests__/sync.test.ts
+++ b/src/commands/__tests__/sync.test.ts
@@ -60,6 +60,19 @@
     expect(io.lines).toEqual([]);
     expect((await store.read()).placements).toEqual({});
     void fixture;
   });
 
+  it('stamps every team the run did complete: one team\'s deferral never withholds another\'s stamp (§8)', async () => {
+    const { fixture, store } = await configuredSkill();
+    // `team` has an endorsed candidate this hook run cannot ask about, so it defers; `other` has nothing to do.
+    await pushFromSeed(fixture.seed, 'team.json', `${JSON.stringify({ layout_version: 2, name: 'team', categories: [], global: [ID], projects: {}, archived: [], policy: { publish: 'pr', skill_license: 'UNLICENSED' } }, null, 2)}\n`);
+    const other = await bareTeam();
+    await cloneWithIdentity(other.bare, store.teamClone('other'));
+    await store.update((config) => { config.teams.other = { remote: other.bare, handle: 'seed' }; });
+    const io: NonInteractivePrompter & { lines: string[] } = { interactive: false, lines: [], print(line) { this.lines.push(line); } };
+    expect(await run({ hook: true, config: store }, io)).toMatchObject({ ok: true, value: { deferred: ['sample'] } });
+    // Neither stamp existed before this run, so `other`'s can only have been written by it.
+    await expect(access(stampPath(store.root, 'team'))).rejects.toMatchObject({ code: 'ENOENT' });
+    await expect(access(stampPath(store.root, 'other'))).resolves.toBeUndefined();
+  });
+
   it('writes a stamp after interactive success but leaves an old stamp after a failed pull', async () => {
```

- **medium — Push-guard fail-open is only tested one file-existence deep; any CLI-internal failure blocks every push from the clone** — `src/lib/__tests__/teamRepo.test.ts:421`
  - Root cause: `guardPush.run`'s catch-all at src/commands/guardPush.ts:63 returns `failure(error instanceof Error ? error.message : String(error))` for every throw alike, and createExecute (src/lib/execute.ts:23-26) maps any failing Result to one stderr line and exit 1. The verb's six deliberate refusals all self-identify ("Push guard refused …", "Push guard: …") and all name the attributed `git push --no-verify` bypass, but anything the verb hits before it can judge — `store.read()` on a corrupt or schema-invalid `~/.terum/skills/config.json` (src/lib/config.ts:37-42 rethrows, src/lib/schema.ts:166-171 throws `Invalid <path>: …`), an unreadable clone, `normalizeHandle`'s `Write guard refused an invalid handle …` (src/lib/guard.ts:83), any unexpected throw — exits 1 through the same path with a raw message that never names the guard, never says the push went unchecked, and never mentions the bypass. Since the fix's hook only guards `[ -x <node> ] && [ -f <entry> ]` (src/lib/teamRepo.ts:367), a present-but-failing launcher propagates that exit 1 and git aborts the push, so the invariant the fix's own doc comment asserts — "A non-zero exit is therefore always the guard's own refusal" (src/lib/teamRepo.ts:361-362) — is false, and the user is locked out of `git push` from the team clone with no stated way out. Note the CLI-side rule is deliberately fail-closed once it runs ("a guard that cannot evaluate must not permit", src/commands/guardPush.ts:74-77), so the defect is the message and the false invariant, not the blocking itself. The new test (src/lib/__tests__/teamRepo.test.ts:421-425) covers only the missing-launcher cause; the launcher-exits-3 case at :415-418 asserts the status propagates, and nothing asserts what a non-verdict failure says. (src/commands/guardPush.ts:63)
  - Fix: In src/commands/guardPush.ts replace the catch-all body at :63 with `return failure(pushGuardMessage(error));` and add a small `pushGuardMessage(error)` helper below `run`: a message already starting with `Push guard` (every deliberate refusal here and every GuardError from `guardRawPush`) is passed through unchanged; anything else is wrapped as ``Push guard could not run: <message>. Nothing was checked, so the push is refused; fix that, or bypass with `git push --no-verify` (attributed to you).`` In src/lib/teamRepo.ts:361-362 replace "A non-zero exit is therefore always the guard's own refusal" with the true statement — once the launcher runs, a non-zero exit is always the guard speaking (a refusal, or its declining to permit what it could not evaluate), never a bare internal error. Add one case to src/commands/__tests__/guard-push.test.ts that overwrites `<store root>/config.json` with non-JSON and asserts the refusal matches /^Push guard could not run: Invalid[\s\S]*git push --no-verify/.

```diff
--- a/src/commands/guardPush.ts
+++ b/src/commands/guardPush.ts
@@ -61,6 +61,19 @@
     if (checked) io.print(`terum-skills push guard: ${checked} path(s) to ${stripRemoteCredentials(args.url)} are yours.`);
     return success({ team, checked });
-  } catch (error) { return failure(error instanceof Error ? error.message : String(error)); }
+  } catch (error) { return failure(pushGuardMessage(error)); }
 }
 
+/**
+ * Every non-zero exit from this verb aborts the push, so every message it exits with has to be the
+ * guard's own. A refusal already names who refused and how to bypass; anything else — a corrupt
+ * `~/.terum/skills/config.json`, an unreadable clone, an unexpected throw — is the guard declining
+ * to permit what it could not evaluate (`forkPoint`'s rule), and is re-voiced so the pusher learns
+ * which tool blocked the push and that the attributed bypass is theirs to take.
+ */
+function pushGuardMessage(error: unknown): string {
+  const message = error instanceof Error ? error.message : String(error);
+  if (message.startsWith('Push guard')) return message;
+  return `Push guard could not run: ${message}${message.endsWith('.') ? '' : '.'} Nothing was checked, so the push is refused; fix that, or bypass with \`git push --no-verify\` (attributed to you).`;
+}
+
 async function revParse(git: Git, ref: string): Promise<string | null> {
--- a/src/lib/teamRepo.ts
+++ b/src/lib/teamRepo.ts
@@ -359,6 +359,8 @@
  * with one warning line when it does not (an npx cache pruned, a node upgraded away): the guard
  * prevents accidents, not abuse, and `--no-verify` bypasses it anyway, so a push blocked by
- * infrastructure would buy no safety — but a push that was NOT checked must say so. A non-zero exit
- * is therefore always the guard's own refusal. git hands the pushed refs to the hook on stdin, and
+ * infrastructure would buy no safety — but a push that was NOT checked must say so. Once the
+ * launcher does run, a non-zero exit is always the guard speaking — a refusal, or its declining to
+ * permit what it could not evaluate — never a bare internal error: guardPush.run re-voices anything
+ * else and names the same attributed bypass. git hands the pushed refs to the hook on stdin, and
  * the CLI reads stdin nowhere outside the Prompter (§3), so the hook turns them into arguments.
  * Without a built entry the fallback is `npx` pinned to this package's version — never `@latest`.
--- a/src/commands/__tests__/guard-push.test.ts
+++ b/src/commands/__tests__/guard-push.test.ts
@@ -124,3 +124,10 @@
     expect(await run({ remote: 'origin', url: fixture.bare, refs: ['refs/heads/main', own, 'refs/heads/main', main], cwd: clone, config: store }, io)).toMatchObject({ ok: false, error: expect.stringMatching(/no name and email[\s\S]*terum-skills login/) });
   });
+
+  it('re-voices a failure that is not a verdict — a corrupt config.json — so the blocked push still names the guard and the attributed bypass', async () => {
+    const { fixture, store, clone, main } = await prepared();
+    const own = await commitOnMain(clone, 'skills/mine/SKILL.md', skill('mine', MINE, 'Seed <seed@example.com>', 'edited'));
+    await writeFile(join(fixture.root, 'state', 'config.json'), '{ this is not json');
+    expect(await run({ remote: 'origin', url: fixture.bare, refs: ['refs/heads/main', own, 'refs/heads/main', main], cwd: clone, config: store }, new ScriptedPrompter())).toMatchObject({ ok: false, error: expect.stringMatching(/^Push guard could not run: Invalid[\s\S]*git push --no-verify/) });
+  });
 });
```

- **low — win32 guard on the hook-mode assertion is dead: the same test unconditionally spawns `sh`** — `src/lib/__tests__/teamRepo.test.ts:410`
  - Root cause: The `it(...)` at src/lib/__tests__/teamRepo.test.ts:405 is registered unconditionally, but its body shells out through the `sh` helper defined at :396-403 (`spawn('sh', [script, ...args], ...)` at :397) at lines 418 and 422. On Windows `sh` is not on PATH, so the child emits ENOENT and closes with `code: null`, and `expect(ran).toEqual({ code: 3, ... })` at :419 fails — the test cannot pass on win32 no matter what. The inline platform guard the fix added at :410 (`if (process.platform !== 'win32') expect((await stat(hook)).mode & 0o777).toBe(0o700);`) therefore protects a single assertion inside a test that is already doomed nine lines later: it advertises Windows-awareness the test does not have, and buys nothing. The repo's actual convention for a test that cannot run on win32 is a whole-test skip (`it.skipIf(process.platform === 'win32')` — config.test.ts:29,39; skill-target-lock.test.ts:28; hook-mutex.test.ts:105; sync.test.ts:186), while the inline `if` form is used only in tests whose remaining body is genuinely platform-neutral (hook.test.ts:17, hook.adversarial.test.ts:54, hook-mutex.test.ts:25, placer.test.ts:98). Note this whole describe block (`the clone-local push guard arming (D12)`) is new in the fix diff b2ec572, so the mismatch was introduced here, not inherited. (src/lib/__tests__/teamRepo.test.ts:405 (unskipped `it`; dead guard at :410; unconditional `spawn('sh', ...)` helper at :397 called at :418 and :422))
  - Fix: In src/lib/__tests__/teamRepo.test.ts, change line 405 from `it('cloneTeam arms the guard: ...'` to `it.skipIf(process.platform === 'win32')('cloneTeam arms the guard: ...'`, and simplify line 410 to the bare `expect((await stat(hook)).mode & 0o777).toBe(0o700);` since the whole case is now POSIX-only. Two lines, test-only, matching the five sibling skipIf call-sites.

```diff
--- a/src/lib/__tests__/teamRepo.test.ts
+++ b/src/lib/__tests__/teamRepo.test.ts
@@ -403,10 +403,10 @@
   });
 
-  it('cloneTeam arms the guard: the hook at 0700 with core.hooksPath pinned to the clone; the hook turns git\'s stdin lines into arguments, fails open when its launcher is gone, and never falls back to `@latest`', async () => {
+  // POSIX-only: the case drives the generated `#!/bin/sh` hook through `sh`, which Windows has no portable path to.
+  it.skipIf(process.platform === 'win32')('cloneTeam arms the guard: the hook at 0700 with core.hooksPath pinned to the clone; the hook turns git\'s stdin lines into arguments, fails open when its launcher is gone, and never falls back to `@latest`', async () => {
     const fixture = await bareTeam();
     const clone = join(fixture.root, 'armed');
     await cloneTeam(fixture.bare, clone);
     const hook = join(clone, '.git', 'hooks', 'pre-push');
-    if (process.platform !== 'win32') expect((await stat(hook)).mode & 0o777).toBe(0o700);
+    expect((await stat(hook)).mode & 0o777).toBe(0o700);
     expect(await readFile(hook, 'utf8')).toContain('guard-push');
     expect((await git(['config', '--local', 'core.hooksPath'], clone)).trim()).toBe('.git/hooks');
```

- **low — shellQuote's whole reason for existing — paths with spaces or quotes — is never exercised by a test** — `src/lib/teamRepo.ts:350`
  - Root cause: The push-guard tests added in b2ec572 only ever generate a hook from paths that need no quoting, so `shellQuote` and the npx fail-open branch are dead weight as far as the suite is concerned. `bareTeam()` roots come from `temporaryDirectory()` = `mkdtemp(join(tmpdir(), 'terum-test-'))` (src/lib/__tests__/fixtures.ts:40) and `bin.test.ts` uses `mkdtemp(tmpdir())` — never a space or a quote. The one executed hook (teamRepo.test.ts:414-419) is built from `join(fixture.root, 'stub.js')`, so the `[ -x <node> ] && [ -f <entry> ]` check and the `exec <node> <entry> guard-push "$@"` line at src/lib/teamRepo.ts:367-368 parse identically whether or not `shellQuote` (src/lib/teamRepo.ts:354) is applied: deleting the function body down to plain interpolation leaves the suite green, and the real-world regression it exists to stop (`/Users/My Name/`, an npx cache under a quoted directory) would land silently. Separately, the `launcher === null` branch (`command -v npx >/dev/null 2>&1`) is only string-matched at teamRepo.test.ts:428-429 — the fail-open path that decides whether a push is blocked or waved through with a warning is never executed. I confirmed by hand that both proposed cases pass against the current code (a hook generated with entry `/Applications/Google Chrome.app/Contents/Info.plist` kept the path as one word; the null-launcher body under a stripped PATH printed "NOT checked" and exited 0), so this is a missing regression guard, not a latent quoting bug. (src/lib/__tests__/teamRepo.test.ts:414 (space-free `stub` path; asserted at :419) — guarding src/lib/teamRepo.ts:354 (`shellQuote`) and the npx branch at src/lib/teamRepo.ts:367)
  - Fix: In the `the clone-local push guard arming (D12)` block of src/lib/__tests__/teamRepo.test.ts: (1) give the local `sh` helper (line 396) an optional `env` parameter defaulting to `process.env` and spawn `/bin/sh` absolutely instead of the PATH-resolved `'sh'`; (2) after the existing fail-open assertions (line 425) add a case that mkdirs `join(fixture.root, "a dir's name")`, writes the same echo-and-exit-3 stub into it, generates the hook with that entry, and asserts the identical `{ code: 3, stdout: 'guard-push origin https://x/y.git refs/heads/main a1 refs/heads/main b2\n', stderr: '' }` — dropping shellQuote turns that into `[ -f /tmp/.../a` plus word-split argv and the assertion fails; (3) add a case that writes `pushGuardHook(null)` to a script and runs it with `PATH` pointed at a nonexistent directory, asserting exit 0 and stderr containing 'NOT checked'. No production code changes.

```diff
--- a/src/lib/__tests__/teamRepo.test.ts
+++ b/src/lib/__tests__/teamRepo.test.ts
@@ -394,7 +394,7 @@
 describe('the clone-local push guard arming (D12)', () => {
-  /** Drive a hook the way git does: arguments plus the ref lines on stdin. */
-  const sh = (script: string, args: string[], stdin: string) => new Promise<{ code: number | null; stdout: string; stderr: string }>((done) => {
-    const child = spawn('sh', [script, ...args], { stdio: ['pipe', 'pipe', 'pipe'] });
+  /** Drive a hook the way git does: arguments plus the ref lines on stdin. An absolute `/bin/sh`, because the npx-fallback case below strips PATH and a PATH-resolved shell would then fail to spawn at all. */
+  const sh = (script: string, args: string[], stdin: string, env: NodeJS.ProcessEnv = process.env) => new Promise<{ code: number | null; stdout: string; stderr: string }>((done) => {
+    const child = spawn('/bin/sh', [script, ...args], { stdio: ['pipe', 'pipe', 'pipe'], env });
     const out: Buffer[] = []; const err: Buffer[] = [];
     child.stdout.on('data', (chunk: Buffer) => out.push(chunk));
     child.stderr.on('data', (chunk: Buffer) => err.push(chunk));
@@ -423,5 +423,19 @@
     expect(skipped.code).toBe(0);
     expect(skipped.stdout).toBe('');
     expect(skipped.stderr).toContain('NOT checked');
+    // The launcher is quoted, not merely interpolated: a directory with a space and a quote in it is still one word to the shell.
+    const awkward = join(fixture.root, "a dir's name");
+    await mkdir(awkward, { recursive: true });
+    const awkwardStub = join(awkward, 'stub.js');
+    await writeFile(awkwardStub, 'console.log(process.argv.slice(2).join(" ")); process.exit(3);\n');
+    const quoted = join(fixture.root, 'quoted.sh');
+    await writeFile(quoted, pushGuardHook({ node: process.execPath, entry: awkwardStub }));
+    expect(await sh(quoted, ['origin', 'https://x/y.git'], 'refs/heads/main a1 refs/heads/main b2\n')).toEqual({ code: 3, stdout: 'guard-push origin https://x/y.git refs/heads/main a1 refs/heads/main b2\n', stderr: '' });
+    // The npx fallback's fail-open, run rather than read: with no npx on PATH the hook warns and exits 0 instead of blocking the push.
+    const fallback = join(fixture.root, 'fallback.sh');
+    await writeFile(fallback, pushGuardHook(null));
+    const withoutNpx = await sh(fallback, ['origin', 'https://x/y.git'], 'refs/heads/main a1 refs/heads/main b2\n', { ...process.env, PATH: join(fixture.root, 'no-npx') });
+    expect(withoutNpx.code).toBe(0);
+    expect(withoutNpx.stderr).toContain('NOT checked');
     // Under the TypeScript sources there is no built entry: the fallback is npx pinned to this package's version.
     const { version } = createRequire(import.meta.url)('../../../package.json') as { version: string };
```

- **low — stampIsFresh's future-skew tolerance is tested only at 24h, so dropping STAMP_SKEW_MS entirely would pass** — `src/lib/__tests__/hook-mutex.test.ts:122`
  - Root cause: The only future-dated stamp case in the test steps the clock back a full day (`() => Date.now() - 24 * 3_600_000`, an age of -24h), which is 1440x outside the 60s tolerance the code defines, so nothing in the suite pins the band STAMP_SKEW_MS exists for. `stampIsFresh` (src/lib/hook.ts:147) is `age > -STAMP_SKEW_MS && age < STAMP_FRESH_MS` with STAMP_SKEW_MS = 60_000 (src/lib/hook.ts:132) documented as covering "filesystem timestamp granularity and rounding, not a clock step". Both regressions that would break that contract satisfy every existing assertion: dropping the tolerance (`age >= 0 && age < STAMP_FRESH_MS`) still returns false at -24h and true for a just-written stamp (whose mtime is normally <= Date.now()), and widening it (`Math.abs(age) < STAMP_FRESH_MS`) also returns false at -24h because 24h > 1h. The test's four assertions therefore constrain only the two far ends (just-written = fresh, +2h and -24h = not fresh) and leave the near-future boundary — the only reason the constant exists — untested in both directions. (src/lib/__tests__/hook-mutex.test.ts:122)
  - Fix: In src/lib/__tests__/hook-mutex.test.ts, inside the `stampIsFresh` test (lines 114-123), add two assertions before the existing -24h line: `expect(await stampIsFresh(root, 'team', () => Date.now() - 30_000)).toBe(true)` (inside the 60s tolerance -- a just-written stamp dated a little ahead of the clock is still fresh) and `expect(await stampIsFresh(root, 'team', () => Date.now() - 120_000)).toBe(false)` (outside it -- the tolerance is a minute, not an hour). Keep the -24h assertion and its comment, and widen the `it(...)` title to name the new near-future cases. Both new assertions carry >=30s of slack against a slow runner, so neither is timing-flaky.

```diff
--- a/src/lib/__tests__/hook-mutex.test.ts
+++ b/src/lib/__tests__/hook-mutex.test.ts
@@ -114,10 +114,14 @@
-  it('stampIsFresh: absent is not fresh, just written is, older than an hour is not, and a stamp dated in the future is not either', async () => {
+  it('stampIsFresh: absent is not fresh, just written is, older than an hour is not, a stamp a little ahead of the clock still is, and one dated further ahead is not', async () => {
     const root = await temporaryDirectory();
     expect(await stampIsFresh(root, 'team')).toBe(false);
     await mkdir(join(root, 'run'), { recursive: true });
     await writeFile(stampPath(root, 'team'), new Date().toISOString());
     expect(await stampIsFresh(root, 'team')).toBe(true);
     expect(await stampIsFresh(root, 'team', () => Date.now() + 2 * 3_600_000)).toBe(false);
+    // Filesystem timestamp granularity and rounding can date a just-written stamp a little ahead of the clock: still just written.
+    expect(await stampIsFresh(root, 'team', () => Date.now() - 30_000)).toBe(true);
+    // Further ahead than rounding explains is not: the tolerance is a minute, not an hour.
+    expect(await stampIsFresh(root, 'team', () => Date.now() - 120_000)).toBe(false);
     // A clock stepped back a day, or a run/ copied from another machine: not evidence of a recent sync.
     expect(await stampIsFresh(root, 'team', () => Date.now() - 24 * 3_600_000)).toBe(false);
   });
```

- **low — treeBetween serializes two git spawns per wanted path on git's blocking pre-push path** — `src/commands/guardPush.ts:93`
  - Root cause: `treeBetween` awaits inside its iteration over `wanted` (src/commands/guardPush.ts:93-97): each path's pair of `git show` spawns must fully resolve before the next path's pair starts. The two reads per path are independent of every other path's reads — the loop body only does `before.set(path, ...)` / `after.set(path, ...)` into two Maps keyed by path, with no cross-path ordering, no short-circuit, and no shared accumulator — so the serialization buys nothing. The inner `Promise.all` parallelizes only the base/head ends of a single path, leaving 2K strictly serialized child processes for K wanted paths, on the pre-push hook path where `git push` is blocked waiting on the hook to exit. `run()` calls `treeBetween` once per 4-element ref group (line 58), so a multi-ref push re-pays it per group. The fix under review narrowed `wanted` from "every changed path" to "team.json plus one SKILL.md per touched skill folder", which shrank K but left the serialization untouched. (src/commands/guardPush.ts:93)
  - Fix: In src/commands/guardPush.ts:93-97, replace `for (const path of wanted) { ... }` with `await Promise.all([...wanted].map(async (path) => { ... }))`, keeping the existing body verbatim (the inner base/head `Promise.all`, and the two `before.set` / `after.set` guarded on `code === 0`). Map writes from concurrent callbacks are safe — single-threaded event loop, one distinct key per callback — and `wanted` is a Set, so no key collides. No signature, caller, or return-shape change: `treeBetween` is called from exactly one place (line 58) and the returned `before`/`after`/`changedPaths` are identical. No test change: src/commands/__tests__/guard-push.test.ts drives the real `systemRunner` against bare-repo fixtures and asserts on outcomes, not on git call order.

```diff
--- a/src/commands/guardPush.ts
+++ b/src/commands/guardPush.ts
@@ -90,10 +90,11 @@
   const wanted = new Set<string>(changedPaths.filter((path) => path === 'team.json'));
   for (const path of changedPaths) { const folder = /^skills\/([^/]+)\//.exec(path); if (folder) wanted.add(`skills/${folder[1]}/SKILL.md`); }
   const before = new Map<string, string>(); const after = new Map<string, string>();
-  for (const path of wanted) {
-    const [was, is] = await Promise.all([git(['show', `${base}:${path}`]), git(['show', `${head}:${path}`])]);
-    if (was.code === 0) before.set(path, was.stdout);
-    if (is.code === 0) after.set(path, is.stdout);
-  }
+  // Every wanted blob is independent of every other, so the whole set is read in one pass: this runs inside `git push`, which blocks until the hook exits.
+  await Promise.all([...wanted].map(async (path) => {
+    const [was, is] = await Promise.all([git(['show', `${base}:${path}`]), git(['show', `${head}:${path}`])]);
+    if (was.code === 0) before.set(path, was.stdout);
+    if (is.code === 0) after.set(path, is.stdout);
+  }));
   return { before: (path) => before.get(path), after: (path) => after.get(path), changedPaths };
 }
```

- **low — New `sh` helper inherits the ambient env, then asserts `stderr: ''` exactly — the flake bin.test.ts already defends against** — `src/lib/__tests__/teamRepo.test.ts:419`
  - Root cause: The `sh` test helper spawns the hook script without an explicit child env, so the `node` process the hook execs inherits the developer's/CI's `process.env` — including `NODE_OPTIONS` — and can also emit its own process warnings; the collocated assertion then demands byte-exact `stderr: ''`, so any ambient `NODE_OPTIONS` (or a node build that prints an ExperimentalWarning/deprecation) fails the test for a reason unrelated to the push guard. Neither `vitest.config.ts` (which sets only GIT_* vars) nor `src/lib/__tests__/setup.ts` (which deletes GIT_*/GH_* vars) neutralizes `NODE_OPTIONS` or `NODE_NO_WARNINGS` for child processes, and the sibling suite in this same batch pins exactly those two hazards by hand (src/__tests__/bin.test.ts:36-37: "An explicit child env: no inherited NODE_OPTIONS or warnings on stderr"). (/Users/ryanliu/Documents/Terum/terum-codex/m4-ship/src/lib/__tests__/teamRepo.test.ts:397 (assertion it breaks: same file line 419))
  - Fix: src/lib/__tests__/teamRepo.test.ts:397 — pass `env: { PATH: process.env.PATH ?? '', NODE_NO_WARNINGS: '1' }` to the `spawn('sh', ...)` call in the `sh` helper (both call-sites in the test go through it), with a one-line comment matching bin.test.ts:36. PATH must stay because the hook body itself runs `cat` via `$(cat)`; dropping NODE_OPTIONS removes the injected-flag hazard and NODE_NO_WARNINGS removes the node-warning hazard, which are the two ways stderr becomes non-empty on a passing guard.

```diff
--- a/src/lib/__tests__/teamRepo.test.ts
+++ b/src/lib/__tests__/teamRepo.test.ts
@@ -394,7 +394,8 @@
 describe('the clone-local push guard arming (D12)', () => {
   /** Drive a hook the way git does: arguments plus the ref lines on stdin. */
   const sh = (script: string, args: string[], stdin: string) => new Promise<{ code: number | null; stdout: string; stderr: string }>((done) => {
-    const child = spawn('sh', [script, ...args], { stdio: ['pipe', 'pipe', 'pipe'] });
+    // An explicit child env, as in bin.test.ts: no inherited NODE_OPTIONS and no node warnings, both of which would break the exact `stderr: ''` below for reasons unrelated to the guard. PATH stays — the hook body runs `cat`.
+    const child = spawn('sh', [script, ...args], { stdio: ['pipe', 'pipe', 'pipe'], env: { PATH: process.env.PATH ?? '', NODE_NO_WARNINGS: '1' } });
     const out: Buffer[] = []; const err: Buffer[] = [];
     child.stdout.on('data', (chunk: Buffer) => out.push(chunk));
     child.stderr.on('data', (chunk: Buffer) => err.push(chunk));
```


### Clear — one fix clearly wins, but not mechanical; confirm each, or hand to /parallel-fix (10)

- **medium — reclaimStaleLock's restore rename overwrites a lock acquired during the move-aside window, so two holders can run at once** — `src/lib/hook.ts:215`
  - Root cause: `reclaimStaleLock` vacates the lock path before it verifies whose record it holds, and then restores by overwrite. `await rename(path, aside)` (src/lib/hook.ts:212) moves aside whatever is at the path — not the bytes `judgeLock` handed over — so from that instant until line 215 the mutex file does not exist and any concurrent `acquireTeamLock` wins `open(path, 'wx', 0o600)` (hook.ts:173) and legitimately believes it holds the team lock. The identity comparison at line 214 happens only after the vacate, and on mismatch line 215's `await rename(aside, path)` is an unconditional overwrite: it hands the displaced (live) record back on top of the record the new holder just created. Both processes now hold the §8 mutex and run `sync --hook` on one team concurrently — the exact failure the mutex exists to prevent — and the displaced new holder's own release is a no-op, because the content-checked release closure at hook.ts:176 reads back bytes that are no longer its own. The mismatch branch is reachable in production, not only in the hand-written test: §8 declares any lock older than LOCK_STALE_MS stale whoever holds it (hook.ts:199), so a long sync's lock is legitimately reclaimed and re-created between one reclaimer's `judgeLock` read and its rename. Secondary defect from the same line: `rename(aside, path)` is also the only thing that removes the aside copy on the mismatch path, so a crash (or a throw) between the two renames strands a `run/<team>.lock.stale-<uuid>` forever — `src/commands/leave.ts:47-50` removes only `<team>.stamp` and `<team>.lock`. The same move-aside/restore shape elsewhere in the tree does guard its restore: `restoreDisplaced` checks `isAbsent(destination)` before renaming back (src/lib/placer.ts:87) and runs under `acquireSkillTargetLock`; the mutex reclaim has neither guard. (src/lib/hook.ts:215 (restore); window opens at src/lib/hook.ts:212)
  - Rated: moderate / low risk / isolated
  - Why one fix: One option clearly wins. The bug is that when the reclaim decides to put a record back, it slams it down on top of whatever is there; the fix is to put it back only if the spot is still empty, and to throw the copy away if someone else has legitimately claimed it in the meantime. Option 1 is exactly that, in one function, with no new dependency. Option 2 does the same thing with a hard link, which buys one narrow nicety (the restored file keeps its original timestamp, which only matters for a lock file that was corrupt to begin with) in exchange for a filesystem feature that is not available everywhere — and its unavailable-fallback is option 1 anyway. Option 3 is genuinely more thorough, but it is not a bug fix: it swaps out the whole locking mechanism, deletes the pid/host record the written spec requires, and forces a rewrite of the mutex test file. That question was already put on the table in round 1 and passed over in favor of the current design; re-deciding it belongs in its own conversation, not in the patch that closes this hole.
  - **Option 1: Non-clobbering restore: re-create the displaced record with an exclusive create (recommended)** — Depth 3/4 · Cost 1/4. src/lib/hook.ts:210-217 — keep the atomic move-aside (it is what makes one reclaimer win among many), read the moved bytes once into a local, and replace the mismatch restore `await rename(aside, path)` at line 215 with an exclusive create: `open(path, 'wx', 0o600)` + write those bytes back, treating EEXIST as "a holder took the free path while it was aside, it owns the mutex now" and simply dropping the aside copy. Finish every mismatch exit with `await rm(aside, { force: true })` so the restore failing never strands the copy, and drop the aside (return false) when it cannot be read back at all rather than throwing out of `acquireTeamLock`. Correct the docstring at hook.ts:204-208, whose "put back untouched" claim is what the code fails to honor. For the pinning test, route the move-aside at line 212 through the existing `fsForTests` seam (hook.ts:9, already used at hook.ts:76) and add a case to src/lib/__tests__/hook-mutex.test.ts beside the existing reclaim test at line 89 that stubs `fsForTests.rename` to perform the rename and then create a competing lock in the window (the idiom at src/lib/__tests__/placer.test.ts:129-136), asserting the competing record survives byte-for-byte, the reclaim returns false, and no `.stale-` file remains. Optionally sweep `<team>.lock.stale-*` in src/commands/leave.ts:47-50 via a readdir filter, alongside the existing stamp/lock removals. *Wins if:* Wins whenever you want the double-holder window closed inside this commit's own scope while keeping the O_CREAT|O_EXCL + {pid, host, token, started} protocol §8 pins: it is the smallest change that makes the restore lose to a live holder instead of overwriting it, adds no new filesystem primitive, and leaves the other two lock schemes in the tree untouched.
  - Option 2: Atomic restore via hard link — Depth 3/4 · Cost 1/4. Same shape as option 1, but restore with `link(aside, path)` (add `link` to the node:fs/promises import at src/lib/hook.ts:2) followed by `rm(aside, { force: true })`, treating EEXIST as "a new holder owns it, drop the copy". The displaced record keeps its original inode and mtime, and there is no window in which `path` exists holding a half-written record. Needs one extra branch for filesystems without hard links (Windows on non-NTFS, some network shares return EPERM/ENOTSUP), which must fall back to something — and the only non-clobbering fallback is option 1's exclusive create, so this ends up being option 1 plus a link fast path. *Wins if:* Wins if the reset mtime in option 1 matters: `judgeLock` falls back to the file's mtime for a record it cannot parse (src/lib/hook.ts:199), so re-creating the file gives an unreadable record a fresh ten-minute lease, whereas a hard link preserves the original timestamp — and only if the store is guaranteed to live on a hard-link-capable filesystem.
  - Option 3: Drop the hand-rolled mutex; take the team lock with proper-lockfile — Depth 4/4 · Cost 2/4. Replace the body of `acquireTeamLock` (src/lib/hook.ts:163-185) with `lockfile.lock(lockPath(...), { lockfilePath, realpath: false, stale: LOCK_STALE_MS, retries: 0, onCompromised })` mapping ELOCKED to null — the shape already used at src/lib/config.ts:58, src/lib/teamRepo.ts:102, src/lib/teamRepo.ts:450 and src/lib/placer/vendor/skillhub/skill-target-lock.ts:22 — and delete `judgeLock`, `reclaimStaleLock` and `isPidAlive` outright. No code ever vacates the lock path, so the window cannot exist, and the library refreshes the lock while held so a long sync is never judged stale in the first place. Requires rewriting src/lib/__tests__/hook-mutex.test.ts (every case asserts on the {pid, host, started} JSON) and amending §8, which pins that record and the dead-pid rule. *Wins if:* Wins if you would rather have one locking mechanism in the codebase than three and are willing to amend the §8 spec text — this is the only option where the reclaim window, the stale-file litter and the "long sync gets stolen from" trigger all disappear rather than being narrowed.
- **medium — Per-team stamp fix misses the four "Blocked …" branches: a partial sync is still stamped "fully synced"** — `src/commands/sync.ts:220`
  - Root cause: The §8 completion gate is driven by one set, `incomplete`, and that set has exactly one writer: the `defer()` helper at src/commands/sync.ts:38 (`const defer = (team, ...labels) => { deferred.push(...labels); incomplete.add(team); }`). Inside the placement-reconciliation loop, four outcomes report a placement the run did NOT handle through `notice(...)` followed by a bare `continue`, never touching `defer()`/`incomplete`: sync.ts:130 (the skill is no longer in the repository), sync.ts:134 (the ledger pins a tree this clone does not have), sync.ts:140 (allowed-tools is malformed), sync.ts:160 (a foreign folder sits at the rename destination). Only the catch-all at sync.ts:185-186 calls `defer(entry.team, basename(path))`. So the sweep at sync.ts:220 (`if (!skipped.has(team) && !incomplete.has(team)) await writeStamp(store, team)`) sees a clean team and stamps `run/<team>.stamp` "fully synced" after a run that left a placement unhandled. That contradicts the comment the same fix wrote three lines above (sync.ts:216-219: "a team this run ... left work undone in (a deferral, a blocked placement), keeps its old stamp") and spec §8 verbatim (.planning/specs/2026-09-02-phase-1-build.md:385: "**The stamp is written only after a sync that fully succeeded.** A failed or partial run leaves the old stamp so the next session retries"). Downstream, src/lib/hook.ts `stampIsFresh` makes sync.ts:72 early-`continue` for the next hour: no fetch, no pending replay, no shared reconcile, no orphan pass, and no repeat of the Blocked notice. The blocked notice is emitted once to stderr and then silenced for an hour — exactly the failure mode §8 exists to prevent. Test-side confirmation: the fix diff's new stamp assertions (sync.test.ts:118/173/201/387) all exercise deferral paths or the *throwing* blocked path (which goes through the catch at 186); the four non-throwing blocked branches have existing tests (sync.test.ts:211 pinned version, :288/:305 foreign rename, :424 deleted skill) and not one of them asserts anything about the stamp. (src/commands/sync.ts:130 (also :134, :140, :160) — blocked branches `notice(...)` + `continue` without `defer()`; consumed by the stamp gate at src/commands/sync.ts:220 and the comment at src/commands/sync.ts:216-219)
  - Rated: moderate / medium risk / pattern — siblings: Same mistake — the run reports work it did not do, but nothing records the team as incomplete, so sync.ts:220 stamps it "fully synced":
1. src/commands/sync.ts:130 — `if (!skill) { notice(\`Blocked ${path}: its skill is no longer in the repository.\`); continue; }` (THE finding; fix here).
2. src/commands/sync.ts:134 — `notice(\`Blocked ${path}: pinned version ... is not in the team repository ...\`); continue;` — the most clearly transient of the four (a push that landed after our fetch); fix here.
3. src/commands/sync.ts:140 — `if (!grants.ok) { notice(\`Blocked ${skill.name}: allowed-tools is malformed.\`); continue; }` — note the sibling line 141 immediately below it DOES call `defer(entry.team, skill.name)` for the un-approved case, so the two adjacent branches already disagree; fix here.
4. src/commands/sync.ts:160 — `if (collision.kind === 'foreign') { notice(\`Blocked ${path}: ${destination} already exists ...\`); continue; }` — fix here.
5. src/commands/sync.ts:124 — `if (entry.scope.kind === 'project' && !projectRoot) continue;` — silent skip, deliberately excluded from the gate by the comment at sync.ts:218-219 ("A project placement outside its checkout is not undone work: that placement belongs to another session"). Cited here only so the sweep does not re-open it; it has a stated reason and is NOT part of this fix.
6. src/commands/share.ts:81, :84, :85, :104, :105, :111 — `reconcileShared`'s six `io.print(...)`+`continue` paths (missing shared source, unreadable record, missing repo copy, invalid rename target, name collision, digest divergence needing `share --keep-source/--keep-repo`). It is called at sync.ts:111 and is not even handed the `defer` callback (its signature takes `skipTeams` only, share.ts:75), so unfinished shared-source reconciliation always stamps. Same class, one function away; pre-existing (not in this fix diff) — sweep candidate, and the one that decides whether the `blocked()` helper should be threaded out of `run()`.
7. src/commands/sync.ts:85 and src/commands/sync.ts:194 — the `onProblem` callbacks (`notice(\`Skipping ${team}/${problem.name}: ${problem.message}\`)`) on `skillRecords` / `endorsedCandidates`: a skill in the clone that could not be read is skipped and reported, and the team is still stamped fully synced. Lowest-severity member of the family; decide it with the same rule.
  - Why one fix: Plain English: the little timestamp file means "this team is fully up to date, don't bother checking again for an hour". The spec says to only write it when the run actually finished everything. The fix that just landed does that for most cases, but four cases where the tool prints "Blocked ..." and moves on still write the file — so the warning is shown once and then the tool goes quiet for an hour, in the exact situation it should keep retrying. Option 1 simply routes those four through the same bookkeeping as everything else, which is what the code's own comment already claims and what the spec says in one sentence. Option 2 (rewrite the comment instead) is cheaper but leaves a known deviation from the spec on the path that matters. Option 3 (treat some blocks as retryable and some as permanent) is defensible, but it invents a rule nobody wrote down, and the cost it avoids — a team that nags at every session start until a human fixes something — is a cost this same fix already accepted for other unfinished work (an un-adopted orphan, an un-approved endorsed skill). So Option 1 wins; the only thing a human might overrule is that nagging, and that call is already parked as the stamp-semantics fork in .planning/debug/harden/m4-ship.deferred.md — resolving it either way still needs the per-team "was this team finished?" bookkeeping Option 1 completes.
  - **Option 1: Route every blocked placement through one notice+defer helper (recommended)** — Depth 3/4 · Cost 1/4. In src/commands/sync.ts, next to the existing `notice` (line 46) and `defer` (line 38), add a single wrapper — `const blocked = (team: string, label: string, line: string) => { notice(line); defer(team, label); };` — and use it at all four early-continue sites so the two loops cannot drift again: :130 `blocked(entry.team, basename(path), \`Blocked ${path}: its skill is no longer in the repository.\`)`, :134 (same shape, `basename(path)`), :140 `blocked(entry.team, skill.name, \`Blocked ${skill.name}: allowed-tools is malformed.\`)`, :160 `blocked(entry.team, basename(path), ...)`. The catch-all at :185-186 becomes the same call. The comment at :216-219 then already describes the code and needs no change. Tests (src/commands/__tests__/sync.test.ts): add one case to the stamp-completion coverage for a NON-throwing block (a malformed allowed-tools skill) asserting `run/team.stamp` is ENOENT and `deferred` names the skill, and add the same stamp-absent assertion to the three existing blocked tests at :211 (pinned version), :288 (foreign rename target) and :424 (deleted repository skill). *Wins if:* Wins if the stamp is to mean what spec §8:385 already says — a partial run leaves the old stamp — and you accept the price the applied interim already accepts for deferrals: a team holding a block only a human can clear stops being rate-limited, fetching and re-printing its Blocked line at every session start until the block is resolved.
  - Option 2: Narrow the comment: only deferrals withhold the stamp — Depth 1/4 · Cost 0/4. Leave the four branches as they are and rewrite src/commands/sync.ts:216-219 to state the rule the code actually implements: a team this run skipped, or that deferred work, keeps its old stamp; a reported *blocked* placement does not withhold it, because it needs a human and would otherwise disable the hourly gate forever. Add one test asserting a blocked (non-throwing) placement DOES stamp, so the chosen rule is pinned rather than accidental. *Wins if:* Wins if the product answer is that a permanently blocked placement must not permanently disable the hourly rate limit (and must not make `search` print "may be stale" forever), and you are willing to carry a documented deviation from §8:385 until the parked stamp-semantics fork in .planning/debug/harden/m4-ship.deferred.md is walked.
  - Option 3: Split the rule: transient blocks withhold, human-needed blocks stamp — Depth 2/4 · Cost 1/4. Mark incomplete only where a later fetch can plausibly clear the block — sync.ts:134 (a pinned tree this clone does not have yet: a race with a push, or a rewritten history) and sync.ts:130 (the skill may be re-added upstream) — leaving sync.ts:140 (malformed allowed-tools) and sync.ts:160 (a stranger's folder at the destination) stamped because only the user can clear them; then rewrite the comment at :216-219 to name that distinction explicitly, and add a test per side. *Wins if:* Wins if the hourly quiet matters more than uniformity and someone is willing to own a second rule: it keeps the retry for the states a fetch fixes while not nagging forever on the two states that need a human — at the cost of a distinction the spec does not make and that the next editor must re-derive.
- **medium — New throws in the hook's per-team lock setup abort the whole sync run instead of costing one team** — `src/lib/hook.ts:193`
  - Root cause: The hook-mode gate in sync.ts runs OUTSIDE the per-team containment boundary. In the team loop, `if (args.hook) { stampIsFresh(...); acquireTeamLock(...) }` occupies sync.ts:71-76, while the per-team `try { refreshClone(...) } catch (CloneBusy) { notice; skipped.add(team); continue }` only starts at sync.ts:77. So any exception thrown during lock setup escapes the loop entirely and lands in the whole-run catch at sync.ts:222, which returns `failure(...)`: every remaining team is dropped, NO team gets stamped at sync.ts:220 (so nothing is recorded as synced even for teams that already finished), and createExecute (src/lib/execute.ts:23-26) sets exit code 1 for the SessionStart hook. That directly contradicts the contract written three lines above the loop (sync.ts:63-67: "A team this run cannot work on costs exactly that team ... every other team still syncs, so the SessionStart hook still exits 0"). The fix diff widened the set of escaping throws: `judgeLock` now rethrows every non-ENOENT error (src/lib/hook.ts:193) where the replaced `lockIsStale` returned `false` for any read/stat error (→ `acquireTeamLock` returned null → that team was simply skipped); `reclaimStaleLock` rethrows non-ENOENT rename errors (src/lib/hook.ts:213); and `mkdirPrivate` (src/lib/hook.ts:168, replacing a plain `mkdir`) now throws outright when `run/` is a symlink, not a directory, or owned by another uid (src/lib/fs.ts:19-20). Concretely reachable: a `run/<team>.lock` left behind by a sudo-run CLI (root-owned 0600) makes `readFile` fail EACCES inside judgeLock, and every session start thereafter fails the sync for ALL teams instead of skipping that one. No test covers a non-ENOENT filesystem error on the lock path (src/lib/__tests__/hook-mutex.test.ts only exercises ENOENT/EEXIST/stale/live paths). (src/commands/sync.ts:73 (gate at 71-76, outside the per-team try/catch that begins at 77; escapes to the run-level catch at 222))
  - Rated: moderate / low risk / pattern — siblings: Same mistake — a call that can throw sitting inside the per-team loop but outside the per-team containment — and the throw sites the fix must cover:
- src/commands/sync.ts:71-76 — the hook-mode gate (`stampIsFresh` at 72, `acquireTeamLock` at 73) outside the try that starts at 77. THIS finding; fixed by the recommended option.
- src/commands/sync.ts:85 — `await skillRecords(clone, team, ...)` is likewise inside the team loop and outside any try; its readdir rethrows every non-missing error (src/lib/skills.ts:24), so an EACCES on `<clone>/skills` also aborts the whole run and drops the remaining teams. Pre-existing (not in this fix diff), same class, same one-line remedy (bring it inside the same per-team catch).
- Throw sites the gate's catch must absorb, for the record: src/lib/hook.ts:168 (`mkdirPrivate` → src/lib/fs.ts:19-20 refusals), 178 (non-EEXIST `open`, pre-existing), 193 (`judgeLock` non-ENOENT rethrow, new in this diff), 213 (`reclaimStaleLock` non-ENOENT rename rethrow, new in this diff), and src/commands/sync.ts:72 (`stampIsFresh` non-ENOENT rethrow, pre-existing).
- Test gap to close in the same sweep: neither src/lib/__tests__/hook-mutex.test.ts nor src/commands/__tests__/sync.test.ts exercises a non-ENOENT filesystem error anywhere on the `run/` path, which is why the regression landed silently.
  - Why one fix: Option 1 covers strictly more failure modes than the other two (it catches anything the lock setup can throw, not just the one call that regressed), costs the same hour, and is the only one that both contains the failure and prints the reason. The apparent trade-off — that a symlinked or foreign-owned `run/` directory stops being a hard error — is not a real loss: nothing is written to the suspicious directory either way, the refusal message still goes to stderr once per team, and an interactive `terum-skills sync` still fails loudly because writeStamp (sync.ts:274) calls the same guard inside the run-level try. In plain terms: one broken lock file should cost one team, not the whole session's sync, and the user should still be told what broke.
  - **Option 1: Contain the hook-mode gate per team (call-site try/catch) (recommended)** — Depth 3/4 · Cost 1/4. In src/commands/sync.ts, wrap the whole `if (args.hook)` gate body (lines 71-76: `stampIsFresh`, `acquireTeamLock`, `releases.push`) in its own try/catch whose catch mirrors the CloneBusy handler at 79-84: `notice(`Skipping ${team}: ${error instanceof Error ? error.message : String(error)}`); skipped.add(team); continue;` (rethrow PromptClosedError is unnecessary here — the gate never prompts). `skipped.add` is what already suppresses the stamp (line 220), the orphan pass (line 235) and the endorsed pass (line 193), so the failing team is left completely alone and every other team still syncs and stamps. Add a test in src/commands/__tests__/sync.test.ts (skipIf win32 || getuid()===0) that writes `run/team.lock` with live-holder JSON, chmod 0o000 so judgeLock's readFile fails EACCES, and asserts `run(...)` is ok:true, the OTHER team's clone advanced and got its stamp, `run/team.stamp` is still absent, and notices contains the skip line. *Wins if:* Wins whenever you want every lock-setup failure — an unreadable/foreign-owned lock file, a refused `run/` directory, an EACCES on the exclusive create — to cost exactly one team and still say why on stderr, which is the contract already written at sync.ts:63-67 and the shape the CloneBusy handler four lines below already uses.
  - Option 2: Make the lock helper fail closed and report through the caller — Depth 2/4 · Cost 1/4. In src/lib/hook.ts, stop letting per-lock-file errors escape: have `judgeLock` return `{ kind: 'live' }` for non-ENOENT read/stat errors (hook.ts:193) and `reclaimStaleLock` return false for non-ENOENT rename errors (hook.ts:213), so `acquireTeamLock` returns null and sync.ts:74 skips that team — the pre-fix behaviour — and add an optional `onProblem?: (line: string) => void` to `TeamLockOptions` so the reason still reaches `notice` instead of vanishing. Leave `mkdirPrivate(dirname(path))` (hook.ts:168) throwing, on the argument that a symlinked or foreign-owned `run/` is a store-wide problem rather than one team's. Update src/lib/__tests__/hook-mutex.test.ts with an unreadable-lock case asserting null + one onProblem line. *Wins if:* Wins if you deliberately want the store-wide `run/` guards (symlink, wrong owner) to stay a loud run-aborting failure and only the per-team lock FILE to degrade into a skip — i.e. if the mkdirPrivate refusal is meant to stop the hook dead rather than be reported per team.
  - Option 3: Minimal regression revert in judgeLock — Depth 1/4 · Cost 0/4. Single-line change at src/lib/hook.ts:193: `catch (error) { if ((error as NodeJS.ErrnoException).code === 'ENOENT') return { kind: 'vanished' }; return { kind: 'live' }; }` — restoring the pre-fix `lockIsStale` semantics (any read/stat failure means 'do not touch it', acquire returns null, that team is skipped silently). Nothing else changes. *Wins if:* Wins only if the goal before ship is to undo exactly the behaviour this fix diff changed with the smallest possible edit, accepting that mkdirPrivate's refusal, the non-EEXIST `open` error (hook.ts:178) and the reclaim rename error still abort the entire run, and that the skip stays completely silent.
- **medium — `team leave` deletes a live §8 team mutex it never holds, so an in-flight hook sync is not serialized against** — `src/commands/leave.ts:50`
  - Root cause: `team leave` participates in the §8 per-team mutex only as a destroyer: src/commands/leave.ts:50 unconditionally `rm`s `run/<name>.lock` inside the clone-lock block, while nothing anywhere in leave.ts ever calls `acquireTeamLock`. The mutex's whole contract (src/lib/hook.ts:163-185, spec §8 "Three windows opening at once must not run three pulls into one clone") is that the file's existence *is* the claim and only its holder — proven by the token in the record — removes it; the token-guarded release closure at hook.ts:176 and the judged reclaim at hook.ts:210 exist precisely so no one else can free a record they do not own. Leave bypasses both: it deletes whatever record is there, live or stale. So while a `sync --hook` is inside the placement loop (sync.ts:115-188, holding the team lock but not the clone lock, so `withCloneLock` at leave.ts:44 does not refuse), leave frees the team's mutex and a third hook can take it and run a second pull into the clone leave is dismantling; and the still-running sync can `place()` a folder back after leave removed it, whose `store.update` at sync.ts:170-175 bails on `if (!placement) return`, leaving a ledger-less folder that the next `team join` reports as `foreign` (sync.ts:160) and refuses to place over. The collocated test cements it rather than catching it: leave.test.ts:26-27 seeds `{pid: process.pid, host: 'this-host', started: <now>}`, which `judgeLock` classifies `{kind:'live'}` (fresh `started`, host !== this machine's `hostname()`), and line 31 asserts it is deleted — so the comment's "a hook killed mid-sync" case is never actually exercised. The fix diff followed the r1 review's own prescription verbatim (.planning/reviews/m4-ship.hybrid.r1.review.md:479 "seed a `run/team.lock` holding this process's live pid and assert it is ENOENT after leave"), which is why the regression arrived with a green test. (src/commands/leave.ts:50)
  - Rated: moderate / medium risk / isolated
  - Why one fix: No real fork — the house rule is already written down elsewhere in this same command. `team leave` already waits for, then politely refuses, a team folder another operation is writing to; option 1 applies that identical rule to the second lock the tool uses (the one that stops two windows syncing the same team at once) instead of bulldozing it. It costs a few lines, it still cleans up the abandoned lock a crashed sync leaves behind — which is the whole reason the line under review was added — and it also removes a way a background sync can quietly resurrect a folder that leave just deleted. The only thing it gives up is that when a sync genuinely is running, leave answers "retry in a moment" instead of ploughing through; that wait is self-limiting, because the lock is treated as abandoned after ten minutes no matter who wrote it, and the refusal message names the file so a stuck user can clear it by hand. Option 2 is strictly less: it stops the destruction but still lets the running sync leave a stray skill folder behind that blocks the next join. Option 3 changes nothing and only makes the test honest about a behaviour we do not actually want.
  - **Option 1: Take the §8 mutex in leave instead of destroying it (recommended)** — Depth 3/4 · Cost 1/4. src/commands/leave.ts: import `acquireTeamLock` alongside the existing `lockPath`/`stampPath` from '../lib/hook.js'. After the confirm at leave.ts:34-36 and before the fresh placement read at leave.ts:40, add `const releaseTeam = await acquireTeamLock(store.root, name);` and, on `null`, throw the same shape leave already uses for a busy clone (teamRepo.ts:433): `Another terum-skills sync holds the session lock on ${name} (${lockPath(store.root, name)}); retry when it finishes, or remove that file if no session is syncing.` Wrap the destructive phase (leave.ts:40-60: fresh read → removePlacements → the withCloneLock block → store.update) in `try { … } finally { await releaseTeam(); }`, hoisting `removed`/`removedPaths` so lines 67-69 still see the count. Delete `rm(lockPath(store.root, name), { force: true })` from the Promise.all at leave.ts:50 — the token-guarded release (hook.ts:176) removes exactly the record leave wrote, and the acquire's own reclaim path (hook.ts:181/210) already clears a stale lock (dead pid on this host, or `started` >10 min), so the r1 case this line was added for — a killed hook's lock outliving leave and silently skipping the next join's hook syncs — still closes. Refusing before any removal (unlike the clone-lock refusal, which fires after placements are gone) also means a contended leave changes nothing at all. Tests in src/commands/__tests__/leave.test.ts: change the seed at :27 to a genuinely stale record (`started: new Date(Date.now() - 20 * 60_000).toISOString()`, same host/pid) and keep the ENOENT assertion at :31 — it now proves the reclaim, not the destruction; add one case seeding a live record (fresh `started`, `host: 'this-host'`) asserting `{ ok: false, error: /lock/i }`, that `placed.path`, the clone and `teams.team` all survive, and that the lock file is byte-identical afterwards. *Wins if:* Wins whenever teardown should serialize against a running hook sync exactly the way it already serializes against a safeWrite (leave.test.ts:125-137 already refuses a busy clone), and a retry window that can never exceed the 10-minute LOCK_STALE_MS is an acceptable price for never freeing a mutex someone else holds.
  - Option 2: Clear the lock only when it judges stale — Depth 2/4 · Cost 1/4. src/lib/hook.ts: export a small `clearStaleTeamLock(storeRoot, team, options?: TeamLockOptions): Promise<boolean>` that runs the existing `judgeLock` and, only on `{kind:'stale'}`, `reclaimStaleLock(path, judged.raw)`; leave a `live` record untouched. src/commands/leave.ts: replace `rm(lockPath(store.root, name), { force: true })` at :50 with a call to it (outside the Promise.all, since it is now a judged read-then-act). Leave never blocks and never deletes a live record; the in-flight sync keeps its mutex, so the third-hook double-run window closes. Tests: stale seed → lock gone and leave succeeds; live seed → leave succeeds and the lock file survives byte-identical. *Wins if:* Wins if the product rule is that a teardown the user explicitly confirmed must always complete — never be refused because of hook state — and you accept that the still-running sync can re-place a folder leave just removed, leaving a ledger-less directory that blocks the next join of that name (sync.ts:160).
  - Option 3: Keep the unconditional removal; make the test assert what it claims — Depth 0/4 · Cost 0/4. No production change. In src/commands/__tests__/leave.test.ts:26-27, reseed the record as the case the comment describes — a dead pid or `started` more than LOCK_STALE_MS old — so the existing ENOENT assertion at :31 pins the stale-reclaim story, and add a second explicit case seeding a live record (fresh `started`, foreign host) asserting leave deletes it too, with a comment stating that a teardown of this team deliberately voids any sync still running against it. *Wins if:* Wins only if the team decides that destroying a live §8 mutex during teardown is intended behaviour (the departing team's sync is doomed anyway), so the only real defect is that the test's seed does not match the case it names.
- **medium — The stamp loop re-reads teams after all work, so work that appeared mid-run is stamped as done** — `/Users/ryanliu/Documents/Terum/terum-codex/m4-ship/src/commands/sync.ts:220`
  - Root cause: The stamp loop decides "this team is fully synced" from state the run never processed. Every per-team pass works from the snapshot taken at src/commands/sync.ts:62 (`const config = await store.read()`, iterated at :69) and from that team's pending list read at :88, but the stamp loop at :220 takes a *fresh* `await store.read()` for its team set — and `ConfigStore.read()` re-reads config.json from disk with no cache (src/lib/config.ts:37-42), so anything another process wrote to config.json between :62 and :220 is visible to the stamp loop yet was never synced by this run. Two concrete holes: (1) a team bound by a concurrent `team join` (src/commands/team.ts:298-305 `bindTeam` under `store.update`, which happens BEFORE the endorsed-install offer) appears in the :220 read, is neither `skipped` nor `incomplete` (this run never touched it — never refreshed its clone, never took its §8 mutex, never replayed its pending), and gets a fresh stamp, so `stampIsFresh` (src/lib/hook.ts:146) makes the next hook session a silent no-op for that team for up to an hour; (2) a pending entry recorded for an already-processed team after its :88 loop ran (install.ts:90 / uninstall.ts:79 push pending under the config lock, and nothing serializes user verbs against a hook run — `acquireTeamLock` is taken only by sync --hook, src/commands/sync.ts:73) survives an interrupted verb and is stamped over as done. The new `incomplete` set is a correct predicate for work THIS run saw fail; it is silent about work that appeared after the run looked, and :220 pairs it with a team set that is deliberately fresher than the run. (src/commands/sync.ts:220)
  - Rated: moderate / low risk / isolated
  - Why one fix: Option 1 dominates. It does everything option 2 does plus closes the second hole, for essentially the same cost and the same one-file blast radius — option 2 is strictly a subset. Option 3 is the only one that removes the race outright, but it buys that by making ordinary commands (install, uninstall, join) wait on or refuse under a background sync, which is a visible behavior change and a much bigger job than a medium-severity timing gap justifies. In plain terms: the cheap fix is "only mark a team done if it is one of the teams we actually worked on, and re-check right before marking that no new work has been queued for it"; the expensive alternative is "make everything else wait while the background sync runs", which nobody wants for this.
  - **Option 1: Stamp only what this run saw, re-verified against the final read (recommended)** — Depth 3/4 · Cost 1/4. Replace the single line at src/commands/sync.ts:220 with a loop over the run's own snapshot, gated on the final on-disk state: `const final = await store.read(); for (const team of Object.keys(config.teams)) { if (skipped.has(team) || incomplete.has(team) || !Object.hasOwn(final.teams, team)) continue; if (final.pending.some((entry) => entry.team === team)) continue; await writeStamp(store, team); }`. `config` is the :62 snapshot and is in scope at :220. The snapshot iteration means a team joined mid-run is never stamped (and a team removed mid-run by `leave`, which deletes its stamp at leave.ts:49, does not get an orphan stamp written back); the `final.pending` guard is the durable form of the same completeness claim `incomplete` makes, and it catches work recorded after this team's :88 pass. Safe against permanent stamp suppression: both replay primitives clear the entry they replayed by `samePending` regardless of `started` (install.ts:136, uninstall.ts:106), so after a genuinely complete run no pending remains for a processed team. Update the §8 comment at :216-219 to state the new rule, and add a collocated test in src/commands/__tests__/sync.test.ts using the existing `wrapRunner` seam (src/lib/__tests__/fixtures.ts:135) to bind a second team — or push a pending entry — during the fetch, asserting that team is not stamped. *Wins if:* Wins whenever you want the stamp's claim to hold for the durable work record (teams + pending) without changing what any user-facing verb does; it closes both halves of the race for the cost of five lines in one file.
  - Option 2: Snapshot-only: iterate config.teams instead of re-reading — Depth 1/4 · Cost 0/4. At src/commands/sync.ts:220 change `Object.keys((await store.read()).teams)` to `Object.keys(config.teams)` and leave the skipped/incomplete predicate as it is. *Wins if:* Wins if you judge only the `team join` half real — i.e. you accept that a mid-run pending entry is always cleared by the verb that wrote it — and want the smallest possible, obviously-reviewable change with no new predicate touching the §8 rate limit.
  - Option 3: Serialize the mutating verbs against the hook run — Depth 4/4 · Cost 3/4. Extend the §8 per-team mutex (src/lib/hook.ts:163 `acquireTeamLock`, today taken only by sync --hook at sync.ts:73) to install/uninstall/team join, so no process can record pending or bind a team while a hook sync holds that team, making the :220 read provably equal to the :62 snapshot. *Wins if:* Wins only if the stamp must be a hard guarantee rather than a best-effort rate limit — and you accept that interactive verbs would then block on, or refuse under, a background hook's lock, which is a user-visible behavior change and a much larger blast radius than this finding warrants.
- **medium — `localPushGuardLauncher()` is never executed by any test — every arming site either passes an explicit launcher or gets `null`** — `src/lib/__tests__/teamRepo.test.ts:411`
  - Root cause: `localPushGuardLauncher()` (src/lib/teamRepo.ts:344-347) decides what the shipped pre-push hook launches from a fact that is only true in the BUILT tree — `new URL('../index.js', import.meta.url)` resolves `dist/lib/teamRepo.js` → `dist/index.js`, guarded by `existsSync`. Every test runs the TypeScript sources, where that URL resolves to `src/index.js` (the file is `src/index.ts`), so `existsSync` is always false and the function's true branch — the branch that ships to every real install — never executes. The one suite that builds a real `out/index.js` beside `out/lib/` (src/__tests__/bin.test.ts) opts out of the default at line 71 by passing `{ node: process.execPath, entry: bin }`, and the new arming test's only body assertion (src/lib/__tests__/teamRepo.test.ts:411, `toContain('guard-push')`) is satisfied identically by the absolute-path form and the `npx` form, while its `@latest` assertions call `pushGuardHook(null)` directly. Net: no test constrains the resolver against the layout the build actually produces, so changing `'../index.js'` or the `existsSync` check silently downgrades every armed clone to `npx -y terum-skills@<version> guard-push` — a registry fetch on git's blocking path running published rules instead of the installed ones — with a green suite. (/Users/ryanliu/Documents/Terum/terum-codex/m4-ship/src/lib/teamRepo.ts:344 (untested true branch; test gap at /Users/ryanliu/Documents/Terum/terum-codex/m4-ship/src/__tests__/bin.test.ts:71 and /Users/ryanliu/Documents/Terum/terum-codex/m4-ship/src/lib/__tests__/teamRepo.test.ts:411))
  - Rated: moderate / low risk / pattern — siblings: Sweep worklist — shipped-layout / shipped-string defaults whose real behavior no test executes:
1. /Users/ryanliu/Documents/Terum/terum-codex/m4-ship/src/lib/teamRepo.ts:344 `localPushGuardLauncher()` — this finding; the `existsSync`-true branch never runs under vitest, and the one built tree (src/__tests__/bin.test.ts:71) passes an explicit launcher.
2. /Users/ryanliu/Documents/Terum/terum-codex/m4-ship/src/lib/teamRepo.ts:354 `packageVersion()` — the sibling build-layout-relative resolve added by the same commit (`createRequire(import.meta.url)('../../package.json')`, `dist/lib/` → `<pkg>/package.json`). Exercised only from the source layout at src/lib/__tests__/teamRepo.test.ts:427-429; its behavior in the shipped layout is asserted nowhere. The recommended built-tree test can cover it in the same pass (e.g. also assert the built module's `pushGuardHook(null)` carries this package's version).
3. /Users/ryanliu/Documents/Terum/terum-codex/m4-ship/src/lib/__tests__/teamRepo.test.ts:411 — the sibling assertion to keep in sync: `toContain('guard-push')` cannot distinguish the absolute-path hook from the npx hook; it should say what it means for the source layout (`toContain('npx')` / not an absolute entry).
4. /Users/ryanliu/Documents/Terum/terum-codex/m4-ship/src/lib/hook.ts:11 `HOOK_COMMAND` — asserted only by `expect(HOOK_COMMAND).toContain('terum-skills')` (src/lib/__tests__/hook.test.ts:57), which survives any verb or flag typo. Same class, non-blocking path, already noted in the r1 review.
5. /Users/ryanliu/Documents/Terum/terum-codex/m4-ship/src/commands/team.ts (WORKFLOW npx steps) — asserted only by `expect(WORKFLOW).toContain('npx -y terum-skills@latest')` (src/commands/__tests__/create.test.ts:30), so the verb names in the shipped Action are unverified.
6. /Users/ryanliu/Documents/Terum/terum-codex/m4-ship/src/commands/team.ts:395 and :498 — the two other `installPushGuard` call sites (join re-arm, create bootstrap). They run with the default launcher in create.test.ts/join.test.ts but no test reads the resulting hook body; covered transitively once the resolver itself is pinned, worth one assertion each if you want those paths pinned independently.
Not a defect and out of scope for the sweep: the printed-command `npx -y terum-skills@latest` spellings in src/lib/readme.ts, src/commands/invite.ts, src/commands/setup.ts (mandated by .planning/specs/2026-09-02-phase-1-build.md:24).
  - Why one fix: Option 1 removes the actual gap — the only place in the suite where a real built install exists is also the only place that currently refuses to use the real default, so making that one test use the default costs nothing and makes the whole existing push scenario (refusal, allow, multi-ref) run against the hook a real user gets. Option 2 does the same check but only as an assertion on the side, so it is a strictly weaker version of the same work at the same cost; option 3 is weaker still and pays for it by adding a test-only argument to shipped code. The one thing worth knowing before choosing option 1: it edits a test that passes today, so if arming through the built module turns out to be awkward in that environment, option 2 is the drop-in fallback with no change to the existing test.
  - **Option 1: Arm the end-to-end guard test through the built package's own default (recommended)** — Depth 3/4 · Cost 1/4. In /Users/ryanliu/Documents/Terum/terum-codex/m4-ship/src/__tests__/bin.test.ts, stop opting out at line 71: arm the clone by calling the BUILT module's `installPushGuard(clone)` with no launcher argument, so `localPushGuardLauncher()` runs in the layout it targets (`out/index.js` beside `out/lib/`). Two equivalent mechanics: (a) spawn `run(process.execPath, ['--input-type=module', '-e', `import { installPushGuard } from ${JSON.stringify(pathToFileURL(resolve(out,'lib','teamRepo.js')).href)}; await installPushGuard(${JSON.stringify(clone)});`], { env: { ...process.env, HOME: home } })` (verified: node v25.8.2 accepts `--input-type=module -e` with top-level await); or (b) `const built = await import(pathToFileURL(resolve(out,'lib','teamRepo.js')).href)` in-process and call `built.installPushGuard(clone, systemRunner)`. Then assert the written hook: `expect(hook).toContain(resolve(out, 'index.js'))` and `expect(hook).not.toContain('npx')`. Keep the explicit-launcher third argument at line 103 — the pruned/fail-open case needs a deliberately missing entry — and leave teamRepo.test.ts:411 as the source-tree/null-fallback half. *Wins if:* Wins whenever you want the one real `git push` through a real hook to be pushed through the hook the SHIPPED resolver produced: after it, a wrong relative path, a broken existsSync, or a build layout that moves index.js fails the suite at the assertion and again at the push.
  - Option 2: Add a separate built-tree arming test, leave the existing push test untouched — Depth 2/4 · Cost 1/4. Add a new `it(...)` inside the same `describe('the built bin (dist/index.js)')` in src/__tests__/bin.test.ts (it shares the `out` build from beforeAll): clone a `bareTeam()` bare through the built module's own `cloneTeam` (child `node --input-type=module -e ...`, or a dynamic import of `out/lib/teamRepo.js`), then read `.git/hooks/pre-push` and assert it contains `resolve(out, 'index.js')` and not `npx`. Line 71 keeps its explicit launcher. *Wins if:* Wins if you would rather not touch a currently passing end-to-end push test: it pins the resolver against the real build layout just as well, but the actual push in the existing test still runs a hand-armed hook, so a resolver regression is caught by one assertion instead of by the whole D12 scenario.
  - Option 3: Parameterize the resolver and unit-test both branches — Depth 2/4 · Cost 1/4. Change src/lib/teamRepo.ts:344 to `export function localPushGuardLauncher(moduleUrl: string = import.meta.url): PushGuardLauncher | null` and resolve `new URL('../index.js', moduleUrl)`. In src/lib/__tests__/teamRepo.test.ts, build a fixture layout (`<dir>/index.js` plus `<dir>/lib/teamRepo.js`) and assert the function returns `{ node: process.execPath, entry: <dir>/index.js }` for that module URL and `null` when `<dir>/index.js` is absent. *Wins if:* Wins only if the built-tree suite (bin.test.ts, which runs tsc in beforeAll) is too slow or too flaky in your CI to carry this coverage: it pins the `'../index.js'` relative hop and the existsSync branch cheaply, but it asserts against a synthetic layout, so a change to what the build actually emits still ships green — and it adds a test-only parameter to a production function.
- **low — M2 §12 walkthrough still asserts none of the two behaviours its name claims (unpinned tracking, widened-grant deferral)** — `src/__tests__/m2-walkthrough.test.ts:50`
  - Root cause: The walkthrough's fixture cannot reach the two branches its name advertises, so the (now really-running) hook sync at src/__tests__/m2-walkthrough.test.ts:50 correctly returns `{ placed: 0, deferred: [] }` — the literal negation of "tracks B unpinned" and "defers A's widened tool grant".

(1) Deferral unreachable: B's only copy of `guarded` is installed PINNED at line 30 (`install({ ref: \`guarded@${tree}\` })`), and the install's ScriptedPrompter([], [true]) recorded an approval for the *old* `Bash(ls)` grant hash. In the placement loop, sync.ts:137 resolves a pinned entry from `materializeVersion(store, team, clone, name, entry.version)` — the old tree — so the grants hashed at sync.ts:138 are still `Bash(ls)` and `approved()` (sync.ts:226) returns true. A's widened `Bash(*)` is never evaluated for B, so the §8 "never places a new or widened grant" branch (sync.ts:141 `defer(entry.team, skill.name)`) is not reached at all. That is correct product behavior — pinning is exactly what should insulate B — but it means the clause "defers A's widened tool grant" describes a branch the test cannot execute.

(2) Tracking unreachable: B's only UNPINNED placement is `plain` (installed line 41, `version: null`), and nothing changes `plain` upstream after that — line 45 rewrites the `guarded` source only. So for `plain` the loop reaches sync.ts:167 (`current.fingerprint === entry.fingerprint && repoSnapshot.fingerprint === entry.fingerprint`) and `continue`s. Line 55's `expect(await readFile(plainPath,'utf8')).toBe(plainBytes)` therefore asserts the *no-op* path, not the re-place path; the unpinned tracking branch (sync.ts:168 `place(...)`) never runs anywhere in this walkthrough (the earlier sync at line 40 runs before `plain` is even installed).

The b2ec572 fix (adding `now: () => Date.now() + 2*3_600_000` plus the `bClone` HEAD assertion) is what exposed this: it turned a vacuous run into a real one, and the real one demonstrably proves only "keeps B pinned" — one of the three claims in the title. Net effect is a coverage-advertising defect: a reader of `src/__tests__/`, and the M2 exit criterion this file is the acceptance artifact for, is told the walkthrough verifies unpinned tracking and hook-mode grant deferral when it verifies neither.

Two corrections to the reviewer's hypothesis, both verified: (a) "give B a second, *unpinned* placement of `guarded`" is not cheap — B's global placement path is `~/.claude/skills/guarded`, so a second unpinned install collides with the pinned one at the same ledger key (`inspect` → `ours` → re-place) and would destroy the "keeps B pinned" assertion at line 54/58; a genuine second copy needs project scope (a product repo fixture + `team.json projects`). (b) The suggested assertion "`hook.lines` still `[]`" is wrong for any option that places something: sync.ts:215 prints the reload directive whenever `args.hook && placed`, so once the run places anything, `hook.lines` becomes `['{"hookSpecificOutput":{"hookEventName":"SessionStart","reloadSkills":true}}']` (the shape asserted at sync.test.ts:345 and :373).

Not a settled deferral: `.planning/debug/harden/m4-ship.deferred.md` holds only the stamp-semantics fork and the installPushGuard decline — nothing about this walkthrough. (src/__tests__/m2-walkthrough.test.ts:15 (the test name), against the fixture gaps at src/__tests__/m2-walkthrough.test.ts:30 (B's only `guarded` copy is pinned) and :41 (B's only unpinned copy, `plain`, never changes upstream); the assertion that exposes it is src/__tests__/m2-walkthrough.test.ts:50. Product-side branches that stay unreached: src/commands/sync.ts:137 (pinned source materialized from the old tree) and src/commands/sync.ts:167 (up-to-date short-circuit).)
  - Rated: moderate / low risk / isolated
  - Why one fix: Everyone agrees on the problem: the test's name promises three things and the body proves one. Option 0 is the best trade because it makes the cheap missing claim true (B's unpinned skill actually gets re-placed when A edits it, which nothing in either walkthrough currently shows) and renames the expensive one honestly, all inside one test file, in minutes. Option 1 is the same effort but leaves the walkthrough proving strictly less. Option 2 is the only one that beats Option 0 on coverage, and what it buys is a second copy of a test that already exists and passes (src/commands/__tests__/sync.test.ts:484 asserts exactly `deferred: ['sample']` for a widened grant in hook mode) — paid for with a new fixture skill and an extra approval prompt in the milestone's acceptance test. Duplicating a passing test is not worth the extra fixture, so Option 0 wins outright. The one thing a human could reasonably overrule: if "the M2 walkthrough must show every consent rule end-to-end for two people" is a hard acceptance requirement rather than a nice-to-have, pick Option 2 — nothing in the code settles that, but the spec's own §12 list assigns the hook-deferral case to the consent/sync suite, which is where it already lives.
  - **Option 1: Earn the tracking claim, rename the deferral claim (recommended)** — Depth 3/4 · Cost 0/4. Test-only edit in src/__tests__/m2-walkthrough.test.ts (~8 lines). (1) Turn `secondSkill` (line 12) into a function of description, mirroring `firstSkill`, and update its call site at line 36. (2) Before the existing A-side sync at line 46, also rewrite the `plain` source — `await writeFile(join(secondSource, 'SKILL.md'), secondSkill('plain edited'))` — so the one `sync({ config: aStore })` at line 46 pushes both A's widened `guarded` and the benign `plain` edit (the reconcileShared source-changed row, share.ts:128, is the same path line 34 already exercises for `guarded`). (3) Line 50 becomes `{ placed: 1, deferred: [] }`. (4) Line 51 becomes `expect(hook.lines).toEqual(['{"hookSpecificOutput":{"hookEventName":"SessionStart","reloadSkills":true}}'])` — sync.ts:215 prints the directive once `placed` is non-zero; keeping `toEqual([])` would fail. (5) Line 55 asserts `plain`'s placed copy now contains the new description instead of `toBe(plainBytes)`; lines 53-54 and 57-61 are unchanged, so the pinned copy, the ledger versions and bob.json are still guarded. (6) Rename line 15 to what the body then proves, e.g. 'keeps B’s pinned copy untouched under A’s widened grant, tracks B’s unpinned copy, and never places the widened grant', reserving the word 'defers' for sync.test.ts:484. Run `npx vitest run src/__tests__/m2-walkthrough.test.ts` — `placed: 1` and the stdout directive are derived from sync.ts, not observed. *Wins if:* Wins whenever the walkthrough should stop advertising coverage it lacks AND cheaply gain the one branch it can actually reach (unpinned re-place + the §8 stdout directive), with hook-mode grant deferral accepted as covered by src/commands/__tests__/sync.test.ts:484 and :503.
  - Option 2: Rename only — Depth 2/4 · Cost 0/4. Change the title at src/__tests__/m2-walkthrough.test.ts:15 to what the body proves and nothing else, e.g. 'keeps B’s pinned copy and B’s unpinned copy untouched when A widens a grant in hook sync'. Note that the reviewer's proposed rename keeps 'tracks B unpinned', which is still overstated per root-cause (2), so the tracking clause must be dropped too. Optionally add a one-line comment above line 50 recording WHY `deferred` is empty (B's only `guarded` copy is pinned, so sync.ts:137 hashes the old tree's grants) and pointing at src/commands/__tests__/sync.test.ts:484 for the deferral branch, so the next reader does not re-file this. *Wins if:* Wins if the walkthrough is deliberately a frozen 'nothing changes' snapshot of B's machine and the only thing worth removing is the false advertisement — no new fixture motion, no re-derived assertions, nothing to run beyond the existing suite.
  - Option 3: Full earn — add a third, unpinned, grant-carrying skill — Depth 4/4 · Cost 1/4. Option 0 plus a genuine deferral in the same hook run: A shares a third skill carrying `allowed-tools` (reuse `firstSkill`'s shape under a new name), B installs it UNPINNED with `new ScriptedPrompter([], [true])` so the initial grant is approved, then A widens that skill's grant alongside `guarded` at line 45. The single hook run at line 50 then yields `{ placed: 1, deferred: ['<third>'] }` with the reload directive on stdout, and every clause of the current title is proven end-to-end in the two-person fixture. A second unpinned copy of `guarded` is NOT the way to do this — it shares B's global placement path with the pinned copy. Also assert the §8 consequence the deferral now has: `await expect(access(stampPath(bStore.root, 'team'))).rejects.toMatchObject({ code: 'ENOENT' })`, the shape used at src/commands/__tests__/sync.test.ts:386. ~15 new lines. *Wins if:* Wins if M2's acceptance walkthrough must itself demonstrate the hook refusing a second user's widened grant — i.e. if the single-store unit test at sync.test.ts:484 is not accepted as evidence for the two-person §12 criterion.
- **low — M2 walkthrough hook assertions cannot distinguish "nothing to do" from "every placement blocked"** — `src/__tests__/m2-walkthrough.test.ts:50`
  - Root cause: The hook-mode assertion at src/__tests__/m2-walkthrough.test.ts:50 omits `notices` from its `toMatchObject`, and `notices` is the ONLY channel that the four notice-only failure paths in sync use. In hook mode `notice()` (src/commands/sync.ts:46) is `notices.push(line); if (!args.hook) io.print(line)` — it never prints — so `hook.lines` is empty for any hook run regardless of outcome. The four blocked outcomes in the placement loop (sync.ts:130 "its skill is no longer in the repository", :134 "pinned version ... is not in the team repository", :140 "allowed-tools is malformed", :160 "already exists and is not a placement this tool owns") each push a notice and `continue` without incrementing `placed` and without calling `defer()`. Result: a regression that blocks BOTH of B's placements produces exactly `{ placed: 0, deferred: [] }` with `hook.lines: []`, and because a blocked placement is by definition left untouched, the follow-on byte assertions (lines 54-55, pinnedBytes/plainBytes unchanged) also stay green. The new `git show HEAD:skills/guarded/SKILL.md` check on line 53 only proves the fetch reached B's clone, not that either placement was evaluated. The clock advance added in this fix diff is what makes the run live and therefore makes this gap load-bearing: the assertions now describe a run that really executes the placement loop, but they cannot see it fail. (src/__tests__/m2-walkthrough.test.ts:50)
  - Rated: trivial / low risk / pattern — siblings: Sweep worklist — hook-mode assertions that claim "the run correctly did nothing" but omit `notices`, the only channel the notice-only blocked paths (sync.ts:130/134/140/160) write to in hook mode:
1. src/__tests__/m2-walkthrough.test.ts:50 — the cited site, inside this fix diff. `{ placed: 0, deferred: [] }` + `hook.lines: []`.
2. src/commands/__tests__/sync.test.ts:243 — declined-orphan hook run, `{ placed: 0, deferred: [] }` + `declinedHook.lines: []` (lines 245-246 assert untouched bytes and ledger, which a blocked placement also satisfies). Pre-existing, outside the fix diff.
Related but weaker: src/commands/__tests__/sync.test.ts:240 — the interactive counterpart of #2; interactive runs DO print notices into the ScriptedPrompter, but the test asserts only `interactive.asked` and never inspects `interactive.lines`, so a "Blocked ..." line there is likewise invisible.
Already correct (the shape to copy): src/commands/__tests__/sync.test.ts:581, :608, :626, :644, :647 all pin `notices: []`.
  - Why one fix: Adding `notices: []` is a one-line change to a test that costs nothing, cannot break anything else, and exactly closes the hole that was verified: every way this run can silently block a placement writes a line to `notices`, so the test would now go red. It is also the same assertion the authors already wrote two files over in this very commit, so it makes the test suite more consistent rather than less. Option 1 is not a different fix, just the same one-liner applied to a second, older test as well — a scheduling call, already captured as the sweep worklist. Option 2 checks more (it would also catch a placement being skipped completely silently), but it buys that by making the walkthrough test depend on which internal git command sync happens to run, which means routine refactors would break the test for no real reason. For a low-severity test-coverage gap that trade is not worth it.
  - **Option 1: Assert the empty notice channel (recommended)** — Depth 2/4 · Cost 0/4. In src/__tests__/m2-walkthrough.test.ts:50, add `notices: []` to the `toMatchObject` value so it reads `{ ok: true, value: { placed: 0, deferred: [], notices: [] } }`. That is the only channel the four notice-only blocked outcomes (sync.ts:130/134/140/160) write to in hook mode, so any of them now fails the test. Verified this passes for this fixture: B's config.shared is empty (A shared, not B), both skills parse cleanly so skillRecords emits no onProblem line, B has no pending entries, `deferred: []` already proves endorsedCandidates returned nothing, and `placed: 0` proves place() never ran so there are no result.notices. The sibling assertions added in this same commit (sync.test.ts:644 and :647) already use this exact shape. *Wins if:* You want the verified gap closed by a one-line, zero-risk change that mirrors an assertion this same commit already established — the default.
  - Option 2: Sweep both hook-mode "nothing happened" assertions — Depth 3/4 · Cost 1/4. Option 0 plus the same `notices: []` addition at src/commands/__tests__/sync.test.ts:243 (the declined-orphan hook run, `run({ hook: true, config: decline.store, now: later }, declinedHook)` asserting `{ placed: 0, deferred: [] }` with `declinedHook.lines: []`), which has the identical blind spot: a blocked placement there is also indistinguishable from the intended declined-and-skipped outcome, including the untouched-bytes and untouched-ledger follow-ups on lines 245-246. *Wins if:* You would rather close the pre-existing sibling in the same commit than carry it as a separate sweep item — costs a re-run of the sync suite to confirm the declined-orphan fixture really emits no notices, and touches a file region outside the fix diff.
  - Option 3: Positive probe that both placements were evaluated — Depth 4/4 · Cost 2/4. Additionally wrap the runner with `wrapRunner` (src/lib/__tests__/fixtures.ts:135) in the walkthrough and count `git cat-file -e <pinned tree>^{tree}` invocations, asserting the pinned entry demonstrably reached sync.ts:133 rather than being skipped earlier. This also covers the silent-continue class that notices cannot see — sync.ts:119 (team skipped), :124 (project scope without a matching root), :127 (person roster mismatch), :128 (declined) all `continue` with no notice and no deferral. *Wins if:* You also want the silent-continue paths covered (a person-roster or scope check misfiring skips both placements with no notice at all) and you accept that the walkthrough test then hard-codes which git command sync uses to validate a pin, so an internal change to that check breaks the test.
- **low — `team leave` cleans two run/ artifacts but not the `.lock.stale-<uuid>` aside a crashed reclaim leaves** — `/Users/ryanliu/Documents/Terum/terum-codex/m4-ship/src/commands/leave.ts:50`
  - Root cause: `reclaimStaleLock` mints a per-process, random aside name — `src/lib/hook.ts:211` `const aside = \`${path}.stale-${randomUUID()}\`` — and that name exists only in the live reclaimer's local variable: it is created by `rename(path, aside)` (hook.ts:212) and removed either by `rm(aside)` (hook.ts:214) or `rename(aside, path)` (hook.ts:215). A process killed inside that window leaves `run/<team>.lock.stale-<uuid>` with no owner, and nothing in the codebase ever enumerates `run/` — `acquireTeamLock` only ever opens the exact `lockPath` (hook.ts:167), `stampIsFresh`/`writeStamp`/`search.ts` only stat/write the exact `stampPath`. The teardown that is supposed to leave a clean slate, `src/commands/leave.ts:48-50`, enumerates the run/ artifacts as a literal two-name list (`rm(stampPath(...))`, `rm(lockPath(...))`) under a comment asserting completeness ("Both §8 run artifacts"), so it structurally cannot see a prefix-named leftover. Result: the orphan survives `team leave`, survives a rejoin under the same name, and accumulates forever in a directory the tool otherwise keeps to two files per team. It is litter only — `acquireTeamLock` never consults the aside, so the mutex still works — which is why the severity is low. (/Users/ryanliu/Documents/Terum/terum-codex/m4-ship/src/commands/leave.ts:49-50 (literal two-name teardown); orphan minted at /Users/ryanliu/Documents/Terum/terum-codex/m4-ship/src/lib/hook.ts:211)
  - Rated: moderate / low risk / pattern — siblings: The general shape — a uuid-named intermediate that only the live process knows how to remove, paired with a teardown that enumerates literal names — recurs. Sweep worklist (paths absolute in /Users/ryanliu/Documents/Terum/terum-codex/m4-ship):

1. src/commands/team.ts:468 `const staging = \`${clone}.bootstrap-${randomUUID()}\`` vs src/commands/leave.ts:46 `rm(clone, {recursive:true, force:true})` — same defect in the SAME function under review: a kill during `team create` bootstrap leaves a whole repo directory `teams/<team>.bootstrap-<uuid>` next to the clone; leave removes only the exact `clone` path, and `ensureClone` (team.ts:388) only inspects the exact clone path. Highest-value sibling, since the orphan is a full git clone, not a 100-byte file.
2. src/lib/placer.ts:52-53 `.${name}.terum-<uuid>.tmp` and `.${name}.terum-<uuid>.old` in the user's skills target root — a kill mid-place orphans them; `removePlacements`/uninstall only remove exact ledger paths, so nothing ever sweeps them out of ~/.claude/skills.
3. src/lib/hook.ts:70 `.settings.json.<uuid>.tmp` next to ~/.claude/settings.json — removed only in the catch at hook.ts:77; a kill between `open` and `rename` orphans it in a directory this tool does not own (weakest candidate — arguably out of scope to sweep).
4. src/lib/config.ts:83 `${path}.${process.pid}.<uuid>.tmp` next to config.json — same window, nothing sweeps.
5. src/commands/share.ts:261 `${to}.terum-<uuid>` — same window.
6. src/lib/version.ts:38-39 `${destination}.tmp-<uuid>` / `.index` — the one case already covered incidentally, because it lands inside `cache/<team>` and leave.ts:47 removes that directory wholesale.
  - Why one fix: Option 1 is strictly better here. The leftover file is harmless clutter — the lock itself still works, because nothing ever reads the `.stale-` copy — so the only thing worth buying is that `team leave` genuinely leaves nothing behind, which is exactly what option 1 does, in one small helper, with zero effect on any other command. Option 2 tries to also tidy up during ordinary session-start syncs, but to do that safely it has to tell "abandoned leftover" apart from "a copy another process is holding for a fraction of a second" — and getting that wrong deletes a lock another window is actively using. Paying a real risk of breaking the mutex to tidy up litter is a bad trade.
  - **Option 1: Prefix-aware run/ teardown behind a runArtifacts helper in lib/hook.ts (recommended)** — Depth 3/4 · Cost 1/4. Add `removeRunArtifacts(storeRoot, team)` to src/lib/hook.ts alongside stampPath/lockPath (hook.ts:138-139): `readdir(join(storeRoot,'run'))` with ENOENT -> [], then `rm(..., {force:true})` every entry equal to `${team}.stamp`, equal to `${team}.lock`, or starting with `${team}.lock.stale-`. In src/commands/leave.ts:48-50 replace the two literal `rm` calls inside the `Promise.all` with `removeRunArtifacts(store.root, name)` and drop the now-unused `lockPath`/`stampPath` imports (leave.ts:4). Extend src/commands/__tests__/leave.test.ts:25-28 to also seed `run/team.lock.stale-<uuid>` and assert after leave that `readdir(join(store.root,'run'))` contains no entry starting with `team.`. hook.ts already imports readdir/rm/join, so no new imports there. *Wins if:* Wins whenever the guarantee that matters is the one the code comments claim — `team leave` hands the next join of this name a clean run/ directory — and you do not want new delete logic on the session-hook hot path.
  - Option 2: Also self-heal: sweep orphaned asides on every acquireTeamLock — Depth 4/4 · Cost 2/4. Option 1 plus, in acquireTeamLock after `mkdirPrivate(dirname(path))` (hook.ts:168), readdir run/ and delete `${team}.lock.stale-*` entries judged old enough to be orphans, and make the rename-back at hook.ts:215 tolerate ENOENT. Needs a discriminator a live reclaimer's aside cannot fail: mtime is inherited through `rename` from a lock that was by definition already older than LOCK_STALE_MS, so an mtime gate would delete in-flight asides; it has to be `ctimeMs` (updated by rename on POSIX, not on Windows) or a marker written into the aside, i.e. a change to the reclaim protocol itself. *Wins if:* Wins only if run/ must self-heal on machines that never run `team leave` — and you accept a delete on the mutex path that can race a live reclaim whose aside may hold a *fresh* holder's record about to be renamed back (deleting it destroys a live lock record, a strictly worse failure than the litter being cleaned up).
- **low — search.ts is the last hand-rolled run/<team>.stamp + 3_600_000 copy after this diff consolidated the other sites** — `src/commands/sync.ts:274`
  - Root cause: `staleNotice` in search.ts owns a private, unclamped copy of the stamp freshness rule instead of calling the helper that now encodes it. src/commands/search.ts:66 reads `if (now() - (await stat(join(store.root, 'run', `${team}.stamp`))).mtimeMs > 3_600_000) io.print(`${team} may be stale; ...`)` — it rebuilds the path that `stampPath(storeRoot, team)` (src/lib/hook.ts:138) already returns, re-spells `STAMP_FRESH_MS` as the literal `3_600_000` (hook.ts:130), and compares a *signed* age with `>`. For a stamp dated in the future (clock stepped back, a `run/` directory copied from another machine) the age is negative, so it is never `> 3_600_000` and the notice is silently suppressed — precisely the case this diff just declared untrustworthy in `stampIsFresh` (hook.ts:146-147: `age > -STAMP_SKEW_MS && age < STAMP_FRESH_MS`, with a new test at src/lib/__tests__/hook-mutex.test.ts:114-124 asserting `() => Date.now() - 24 * 3_600_000` is not fresh). The user then gets neither the hook sync nor the warning that it did not happen. The same diff also left the second spelling in place one line away from the helper it imported: src/commands/sync.ts:274 `writeStamp` calls `mkdirPrivate(join(store.root, 'run'))` for the parent and `stampPath(store.root, team)` for the file. This diff moved leave.ts (src/commands/leave.ts:49-50) and sync.ts's file path onto the helpers but stopped before the last two hand-rolled spellings. (src/commands/search.ts:66)
  - Rated: trivial / low risk / pattern — siblings: Sweep worklist — private spellings of the `run/<team>.stamp` path or its freshness rule, after this diff consolidated leave.ts (src/commands/leave.ts:49-50) and sync.ts's file path:
1. src/commands/search.ts:66 — THE finding: hand-built path + literal `3_600_000` + unclamped signed comparison; the only site with a behavioural defect (a future-dated stamp suppresses the "may be stale" notice). Fix here.
2. src/commands/sync.ts:274 (`writeStamp`) — hand-joins `join(store.root, 'run')` for the parent directory in the same expression that calls `stampPath(store.root, team)` for the file. Cosmetic only (same string), but it is the last literal `'run'` in production code outside hook.ts.
3. src/lib/hook.ts:138-139 (`stampPath`/`lockPath`) — the intended owner of the path; nothing to change unless option 3 is chosen.
4. Explicit NON-site, do not sweep: src/lib/hook.ts:158-160 (`judgeLock`) — `now() - started > LOCK_STALE_MS` is the same unclamped shape but fails conservatively (a future `started` reads as "not stale", so a live lock is respected). Clamping it would let a second hook run concurrently; the r1 review lists it "for awareness, not for the sweep".
5. Tests still hand-build the path (src/commands/__tests__/search.test.ts:157-158, leave.test.ts:23-24, hook-mutex.test.ts:40,63,92,107,117); sync.test.ts:387,584,620 already uses `stampPath`. Fixture-level duplication, harmless — no action required.
  - Why one fix: One option clearly wins, and the choice needs no product decision. Everyone agrees on the repair — search's "this team may be stale" hint must not go quiet just because the stamp file claims a time in the future. Option 1 does it by having search ask the one function that already knows the rule, which also deletes the stray copy of the one-hour number and the last hand-typed folder name; option 2 patches the arithmetic and leaves both copies to drift apart again; option 3 is option 1 with an extra one-line helper whose only benefit arrives if a still-undecided design change adds more files to that folder — and it can be added then in a minute. Note the earlier round's stated reason for skipping this ("that file is being reviewed on another branch right now") has expired: the phase1-closeout branch is already merged into this branch's base.
  - **Option 1: Consume stampIsFresh in search, derive run/ from stampPath in sync (recommended)** — Depth 4/4 · Cost 1/4. (1) src/commands/search.ts:65-68 — replace the hand-rolled comparison with the shared predicate, preserving today's fail-loud-on-unreadable behaviour: `async function staleNotice(store, team, io, now) { let fresh = false; try { fresh = await stampIsFresh(store.root, team, now); } catch { /* an unreadable stamp is not evidence of a recent sync */ } if (!fresh) io.print(`${team} may be stale; run \`terum-skills sync\`.`); }`. `stampIsFresh` already returns false on ENOENT, matching the current catch; the try/catch keeps the non-ENOENT stat error (EACCES) printing the notice instead of throwing out of the per-team try at search.ts:46 and turning a cosmetic hint into `${team} could not be searched`. Add `import { stampIsFresh } from '../lib/hook.js';`, drop the now-unused `stat` from the `node:fs/promises` import at search.ts:1 (`join` is still used at line 30). (2) src/commands/sync.ts:274 — `await mkdirPrivate(dirname(stampPath(store.root, team)))`; `dirname` is already imported (sync.ts:2, used at line 148), so no import churn and the literal `'run'` leaves production code entirely, living only in hook.ts:138-139. (3) src/commands/__tests__/search.test.ts — add the mirror of the hook-mutex case next to 'omits the stale notice when its stamp is younger than one hour' (line 62): `await freshStamp(store, 'team'); expect((await run({ term: 'needle', config: store, now: () => Date.now() - 24 * 3_600_000 }, io)).ok).toBe(true); expect(io.lines.join('\n')).toContain('may be stale');` — red today, green after. *Wins if:* Recommended: it deletes the divergent freshness rule rather than patching it, so `run/<team>.stamp` has exactly one definition of 'fresh' and one path constructor, and it costs one import swap plus one test in a file that already has the fixture (`freshStamp`) and the `now` knob.
  - Option 2: Clamp search.ts's comparison in place — Depth 2/4 · Cost 0/4. Leave the hand-rolled path and constant at src/commands/search.ts:66 and only fix the sign: `const age = now() - (await stat(join(store.root, 'run', `${team}.stamp`))).mtimeMs; if (age < 0 || age > 3_600_000) io.print(...)`. Same test addition as option 1. *Wins if:* You want the smallest possible edit to a file this diff never touched — no new import, no coupling of search.ts to hook.ts — and accept that `3_600_000`, the `run/` path and the freshness rule stay duplicated (and that the next change to STAMP_SKEW_MS/STAMP_FRESH_MS silently skips this copy again).
  - Option 3: Export runDir(storeRoot) from hook.ts and route all three sites through it — Depth 4/4 · Cost 1/4. Option 1's search.ts change, plus add `export function runDir(storeRoot: string): string { return join(storeRoot, 'run'); }` beside src/lib/hook.ts:138-139, define `stampPath`/`lockPath` in terms of it, and use `mkdirPrivate(runDir(store.root))` at src/commands/sync.ts:274 (and `mkdirPrivate(runDir(storeRoot))` in place of `mkdirPrivate(dirname(path))` at hook.ts:167). *Wins if:* The parked stamp-meaning fork (.planning/debug/harden/m4-ship.deferred.md, option 2: a second `run/<team>.synced` marker) is likely to land, so `run/` gains more artifacts and a named directory helper earns its export — otherwise it is one more export to spell the same thing `dirname(stampPath(...))` already says.

### Forks — no single fix clearly wins; run `/decision-walk <this report>` (0)

_none_

### Declined by triage — overrule if you disagree (the standalone test applies) (0)

_none_

### Untriaged — the triage agent returned nothing usable; investigate by hand or resume the run (0)

_none_
