# m7-S7e notes

- Rows RM-08, MC-12, RM-29, CP-40, CP-37, CP-24, CP-39 (+ AC-17, CP-O3 riders). CLI + app. Batch 9 of tranche 1, built off main 2d6f247, then merged main (S7f, S7q) with eight conflicts folded; re-merged after S7d.
- Codex (gpt-6-astra, high) reported `complete`. Three open questions ruled: RM-08's description reuses the printed release-status lines and `latest` is `describeUpdate`'s highest valid observation; the advice renders in the existing dialog wrapper at `?dialog=update` with whitespace preserved (no new board); only `surfaces.update` flips here. Codex's `implementation-notes.md` (its PR-body draft) is kept beside this file.
- **RM-08:** `update` returns an `UpdateReport` (`running`, `latest`, `observation`, `launch`, `description`, `advice`, `lines`) beside its unchanged printed lines; the adapter validates it with a strict object (an unexpected or missing key fails the read rather than passing through) and serves `update()`; Settings ▸ Updates shows the DTO's description and advice, never a fixture string, and waits for the read before declaring the board ready.
- **MC-12:** machine uninstall discloses retained eval runs (`evals/` kept and named) and removes the downloaded app bundle (`app/`, all versions, recursively) after the same consent. **RM-29:** the duplicate `COMMUNITY_URL` in `constants.ts` is gone; **CP-O3:** the community link is the Discord invite. **CP-40 / CP-37:** the zero-team hint is the shared setup text (`selectTeam` throws `getStartedLines`), and the tests that pinned the old wording moved with it. **AC-17:** `capabilities.appVersion` from the build (`vite`/`vitest` define). **CP-24:** README documents `ls member`/`ls project`, `team workflow-update`, `app`, the teardown and the shipped shell. **CP-39:** the four stale eval bans leave the phase-1 spec (§§2, 6.1, 12, 13 default 41); Ryan is named.
- Real-data proof: `update-replay.test.ts` replays `update` and `status` frames recorded from the rebuilt CLI under the fixture team (offline; a local bare remote, so `update` cannot probe the network): the report's four advice lines are non-empty, its seven printed lines equal `value.lines`, `latest`/`observation` are null/unknown.
- What lights: Settings ▸ Updates (the CLI's release description and installation-specific advice) from real data. `surfaces.update` is true on the real adapter.
