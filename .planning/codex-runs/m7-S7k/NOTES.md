# m7-S7k notes

- Rows RM-41, RM-48, RM-S1, TJ-12, BM-02, BM-03, BM-09, BM-10, RM-22, RM-07, AD-20, AD-24. CLI + app. Batch 6 of tranche 1, built off main 2d6f247, then merged main (S7f, S7q) with nine conflicts folded by hand.
- Codex (gpt-6-astra, high) reported `partial` with one fork: the Account row's presence-only copy moves pixels on the Settings boards. Five open questions, three deviations (a temp cargo target in the sandbox; the root default-concurrency run tripped the unchanged eight-writer safeWrite test under load and passed at `--maxWorkers=4`; the Terum MCP was not available to it). Its `implementation-notes.md` is kept beside this file.
- **Orchestrator reverted one Codex edit:** it had appended a line to `.planning/decisions/2026-09-08-m7-takeover-decision-walk.md` (Ryan's binding ledger, never an implementer's file). The same sentence lives in the phase-1 spec's §5.1 TJ-12 paragraph, which is where the spec asked for it.
- **Rulings on the open questions.** Account row: presence-only (RM-45) stands, and pixels moved through the canvas: patch `.patches/patch_am.py` rewrites the row's "ok" state to `git present · gh present` with a description that says authentication is not checked, `render-mac.mjs --all` re-rendered the oracle, the fixture was regenerated. The app renders one status value (both present) or one quiet value naming the absent tool; the button stays "Open gh" and opens S7q's terminal hand-off popup (copies `gh auth login`). `ghState` gains `presenceOnly` for status; other callers keep their auth probe. `machine.name`/`hostname` are both carried and empty on the real adapter until S7r. `TEAM_POLICY.projects` is null on the real adapter until a project read model exists; `SHARED_SPECIMEN` is null (a board specimen, not data). APPROVALS join against `ls --local`'s skills, which carry no grants hash today, so the real list is empty until a team-scoped `ls` is read there (follow-up).
- **Merge with S7f + S7q.** `types.ts`: S7k's explicit `TeamStatus` (nullable fields, policy object, pending, join block) gains S7q's optional `cloneState`/`readable`; `Machine`, `Identity`, `StatusResult.tools`. `Footer`: S7k's identity (`me.initials`, `me.footerLabel`; the TZ literal is gone from the component) + S7q's clone-state title and `machineRegistry` gating of the machine name. `Shell`/`Sidebar`: both prop sets (`me`, `team`, `projects`, features readiness, the inline status error). Adapter: S7k's full `cliStatus` serves `status()`/`settings()`; S7f's team bootstrap keeps its own subset schema renamed `cliStatusTeams` (a plain `z.object` strips unknown keys; nothing passes through); one `cliScope`; `surfaces` = status, settings, library, skill. `SettingsContent`: S7k's nullable Teams rows + S7q's `cloneStateCopy` on the Clone row and the sign-in popup; the catalog dependency is gone. Tests: S7k's status replay beside S7f's inventory tests; two tests moved to the new Account truth (`git present · gh present`, the `Open gh` button).
- Real-data proof: `index.test.ts` replays `status` and `ls --local` recorded from the rebuilt CLI under the fixture team (`frames/`), asserting the served handle, repository, member count, clone path, policy `pr` → "Pull request", categories `[ops, engineering, debugging]`, the deploy-check placement, identity `seed`, tools, `counts` = `{Global}` only, and a failing status frame with a value still serving its teams; the approval join is tested for a stale, current and missing hash.
- What lights: the status footer (identity from `identity`/the team handle; machine name still `—` until S7r) and Settings ▸ Teams / Sync / This machine / Account / Updates(version) from real data. `surfaces.status` and `surfaces.settings` are true on the real adapter.
