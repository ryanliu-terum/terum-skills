# desktop-scroll-panes: notes

- What: the Library grid and the Skill detail column scroll (spec `.planning/specs/desktop-scroll-panes.md`). Every other pane already scrolled with the repo's hidden-scrollbar pattern; these two clipped rows 4-5 of the Global grid and everything below the fold on the skill tabs.
- Codex (gpt-6-astra): run 1 (high) wrote the two CSS changes and `e2e/routes/scroll.spec.ts` (7 cases: real wheel input, pinned header/rail, console errors asserted empty); 2 of 7 failed because the nested tab panes still clipped. Run 2 (medium) released the clip on the five panes with `overflow:visible;min-height:auto`; that moved three boards (SkillDetailNotInstalled, SkillDetailInstall, SkillDetailInstallLight) by exactly 1 px, traced by the orchestrator to `min-height:auto`. Run 3 (low) dropped it: visible overflow still feeds the scroll container, so no pane changes size.
- Orchestrator gates on the final tree (outside the sandbox, TERUM_DESIGN_DIR set): typecheck 0, lint 0, vitest 67 files / 1014 tests, routes 108/108, fidelity 88/88 (Main and Light at 0 px).
- Oracle provenance: the orchestrator's canvas had to be brought up to the repo's fixture first. `patch_ak.py` (from `design-strings/`) applied cleanly; `patch_al.py` and `patch_am.py` (S7q's Connect relabel, S7k's presence-only GitHub row) were never committed, so they are RECONSTRUCTED here from the run notes and the app strings (copies beside this file). With them the baseline was 88/88 before this change; before them it was 57/88. A fresh `tools/export-design.py` from that canvas is byte-identical in content to the committed fixture; only the build.py md5 stamp differs, so `npm run export:check` reports STALE across machines. Please commit every canvas patch beside its run record.
- Out of scope, recorded: the sample-data counts (Global 30 vs 15 cards, project counts, Team installs 148 vs 106) and per-scope overview stats; the sidebar collapse controls (no handler; D9 boards not drawn).
