# m7-S7d notes

- Rows RM-09, CP-35, BM-06, BM-07, AD-17 (ledger D13: this batch edits `frames.ts` first; S7b, S7ae and S7l stack on it). CLI + app. Batch 8 of tranche 1, built off main 2d6f247, then merged main (S7f, S7q) with five conflicts folded; re-merged after S7k and S7ad.
- Codex (gpt-6-astra, high) reported `partial` for one reason: the CLI's neutral cancellation line is retained in `useWorkflow.notice` but no board draws a place for it, so nothing renders it (the cancelled dialog closes quietly and paints no error). Orchestrator ruling: accepted as the conservative reading; the display location is a design call for Teddy and is recorded for the PR body. Two other open questions ruled: BM-06's "exclude commander.help" applies to exit 0 only (a missing argument is `commander.help` at exit 1 and still gets one failed result frame naming the attempted verb); GAPS.md's typed-decline clause is closed by this batch (maintainer edit here). Codex's `implementation-notes.md` is kept beside this file.
- **Typed declines (RM-09):** `Result` gains `cancelled: true`, with `cancelled()`, `CancelledError` and `fromError()` in `result.ts`; every y/N decline in connect, install (both consent declines), publish, leave, team remove/join-invitation and uninstall throws or returns it; `execute.ts` carries it to the result frame as `declined: true`, and `frames.ts` no longer greps the error text. The adapter maps `declined` onto `Result.cancelled`; the mock's declines are typed the same way; a declined `sync --prune` is a success with the CLI's own print line (`Prune cancelled; nothing deleted.`).
- **Usage errors (BM-06) and the separator (BM-07):** a commander usage error emits exactly one failed result frame (`report()` is once-only) naming `attemptedVerb()`; help and version at exit 0 stay exempt. The `--frames` scan and the `--hook` refusal stop at the first `--`.
- **AD-17 argv order:** every adapter argv is verb, flags, `--`, positionals, so a leading-dash operand cannot be read as a flag. `install <ref> [value]` parses `install -- member mira` as two positionals (commander), so the member/project forms are unaffected. `search` keeps its query after `--`.
- **CP-35:** `ledgerScopes` is exported unchanged for the consequence copy; the Remove dialog already says `Remove <name>?` and names no scope until S7g/S7k data arrives.
- Frames: the data-verb recordings Codex re-recorded here predate S7f's payload, so `replay.test.ts` reads S7f's frames for `status`/`ls`/`search` and this batch's own `usage-error.jsonl` (`install -x`) and `decline.jsonl` (an explicit-path connect answered no) for the two new cases; the stale copies were removed from this directory.
- What lights: nothing on screen. What changes for a real teammate: saying no in any dialog closes it quietly instead of painting a red error; a usage error from the app's own argv is one honest failure frame instead of a hung run; a skill named with a leading dash cannot be mistaken for a flag.
