# m7-S7b notes

- Rows PF-03, PF-04, PF-07, PF-09, AD-25, AD-26, CP-19; ledger D1 (`roles` stays false, `memberRole: true` added), D2 (no favorites/follow), D13 (this batch changes one `FRAME_FEATURES` line after S7d). CLI + app. Batch 10 of tranche 1, built off main 14ccc45 (S7d) while S7c was unmerged, then merged main through S7e (3e7dc44; three conflicts folded) and S7c (75bfe69; two conflicts folded).
- Codex (gpt-6-astra, high) reported `complete` with four open questions, ruled here: (1) an empty `--project` list cannot be expressed (omitted flags leave membership unchanged; no clearing flag invented) — accepted, recorded as a follow-up; (2) `roster()`/`catalog()` refuse when more than one team is configured (the seam has no team selector) — accepted, the honest reading; (3) the project install dialog omits the grant preview when `bulkInstall` is empty — accepted; (4) `changed` carries committed field names (`display_name`) — accepted. One deviation accepted: the CP-19 inventory gate exposed that the public `app` verb was missing from `FRAME_VERBS`; it is added (additive, protocol 1).
- **CLI half:** `personSchema` gains optional `role` (≤32) and `projects` (no defaults, absence preserved through a round trip); `GuardAction` gains `profile` and `decline`, row (b) admits them on the caller's own path; `profile [--name] [--bio] [--role] [--project]…` writes only self-describing fields through `safeWrite`, validates `--project` against `team.json` read from the pre-image inside the mutation, refuses `email`/`github`/`handle`, and `--name` also writes `config.display_name`; `decline <ref>` appends an id to `declined` idempotently and refuses an installed id; `team join`'s reclaim preserves `role` and `projects`; `ls` roster and `ls member`, `status` members carry `role: string | null` and `projects: string[]`; `frames.test.ts` gains the CP-19 gate (every public commander path ↔ `FRAME_VERBS`; every `FRAME_FEATURES` key documented in `docs/frame-protocol.md`).
- **App half:** `Backend.profile` and `Backend.decline` (`touches: ['clone']`); `roster()` from `status` members + `ls` activity; `catalog()` from `ls`, `ls member <handle>` per person, `ls --local` and `status`; `surfaces.roster` and `surfaces.catalog` flip to true; the Share job label and Teams column show only when `features.memberRole` is true and never read the `role:<handle>` pref (TJ-02's permission chip keeps it; S7h replaces it with host truth); Marketplace people cards read the real role/projects; the Inbox "Decline" runs the verb instead of writing a pref; `followers`, `evaluated`, `favorites` are `null` (D2). The `app` verb is in the hello inventory.
- **Fold onto main:** S7g's `placement()` joins by ledger id, so S7b's catalog joins were switched from `row.name` to `row.id` / `skill.id` (Codex wrote them against the pre-S7g name join); S7g's typed `LocalSection` beside S7b's roster fields; S7c's `setIdentity` beside `profile`/`decline` on the seam; one `surfaces()` line serving status, settings, library, skill, update, roster and catalog. **Maintainer edit:** the root README documents `profile` and `decline` (S7e's CP-24 gate requires every public command path in a README code span).
- Frames: re-recorded from the merged CLI against the fixture (fixture.sh beside this file; mira has `role: Platform`, `projects: [terum]`; the `profile --role Platform` and `decline diagnose` writes were run for real against the fixture's bare remote, then `ls member seed` recorded the persisted decline).
- What lights: Share (the roster with the real job label and Teams column) and Marketplace (people, projects, categories, top rated by installs) from real data; a teammate's own role/projects are written by the CLI. Follow-ups: no way to clear a project list from the app; `joined`/`lastSeen`/`followers` stay `—`/null until their read models exist (PF-05/PF-06, D2).
