# m7-S7ag notes

- Rows AD-11, AD-12, AD-13 (Rust child lifecycle). No CLI change. Ledger D9 respected: lib.rs keeps exactly five commands.
- Codex report `partial` for two non-defects: it did not stack on S7af (S7ag has no dependency on it; the spec's §4 mention of S7af's index.test.ts was the orchestrator's over-reach, and Codex added the named regression test itself) and it could not write the shared cargo target from the sandbox (used a temp dir; the orchestrator's cargo check/test above use the shared warm target).
- Diff read: lib.rs holds both reader JoinHandles and joins them before emitting Exit; admission and insertion of a child happen under one lock with MAX_CHILDREN = 8 ("too many pending terum-skills processes (8); wait for one to finish"); cli_kill closes stdin, polls try_wait for 1,500 ms, then on Unix SIGTERMs the process group (children are spawned as group leaders via process_group(0)), waits 500 ms and SIGKILLs the group; Windows falls back to Child::kill (documented: does not terminate descendants). New Unix-only dependency `libc = "0.2"` (check_decision: no standing conflict). bridge.ts's spawn returns the unlisten function instead of unlistening inside the exit event; run.ts calls it after exit or cancel, keeps a late stdout line as a diagnostic instead of dropping it, and settles Cancelled on a result that arrives after cancel. No suppressions, no skipped tests; run.test.ts gained five lifecycle tests and a strengthened cancellation test.
- Not reproduced here: the exit-before-last-line race, the orphaned git clone and the cap under load need the packaged app (Step 4 of the launch runbook records what this Mac can observe). The 500 ms SIGKILL escalation on a group whose leader already exited is harmless (ESRCH) but recorded as the least-confident claim.
- Follow-up for S7ad (stacks on this branch): `cli_spawn` gains the `path` argument; nothing else in lib.rs should move.
