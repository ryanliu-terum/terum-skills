# m7-S7g notes

- Rows RM-39, RM-40, RM-44 (the subset: D-BM-6, no `sharedState`), AD-23. CLI + app. Batch 5 of tranche 1 in the queue, built off main d5b0408 (S7q) with S7f merged, then merged main through S7ad (1be2c6c) and S7d (5c25233).
- Codex (gpt-6-astra, high) reported `complete` with two open questions. Ruled here: (1) "how should the full Settings model expose the local subset" — moot after the merge: S7k had already merged and serves the full `Settings` from `status` + `ls --local`, so the pre-S7k `LocalSettings` variant, `LocalSettingsContent.tsx` and `PlacementTable.tsx` Codex built were dropped in the fold and S7k's `settingsModel` reads the typed rows instead (commit e8ff535); (2) the adapter README's "Settings entirely unavailable" sentence was already rewritten by S7k.
- **CLI half:** every `ls --local` row carries `tracked`, `shared {id, team}[]`, `placement {id, team, version} | null` and `health` (`up-to-date | update-available | local-changed | both | gone-from-repo | untracked | unknown`) from three fingerprints (ledger, placed folder, the clone's current tree for that id) with no git call; a rejected inspection (top-level symlink) gets `unknown` and is never snapshotted; a ledger placement whose folder is gone produces no row and is named in the section's `problems`. The printed sentence is byte-identical (pinned by `ls.test.ts`). `LocalEntry.placementFingerprint` is the one internal addition.
- **App half:** one strict `cliLocalRow` schema serves both `ls --local` readers (S7f's `cliLs` for Library/Skill, S7k's `cliLocal` for status/settings; sections also carry `problems`); Library/Skill `installed`, `path`, `version` and `version_full` join by ledger team + id, never by name or prose; `settingsModel` PLACEMENTS: state from `health`, version = the drawn 12 characters (`null` renders "tracking"), PINNED_N = ledger placements with a version; a ledger placement with no row shows `folder missing` when the CLI's `problems` name its path.
- **Spec error (orchestrator's, recorded for Ryan):** the S7g spec named the state words "In sync", "Edited here", "Removed from the team", which no board draws. The design fixture's PLACEMENTS vocabulary is `up to date`, `update available`, `edited locally`, `pinned`, so the fold maps `up-to-date` → `up to date`, `update-available` → `update available`, `local-changed` → `edited locally`, `both` → `edited locally · update available`, `gone-from-repo` → `removed from the team` (no drawn word exists; the CLI's fact in the board's register), `unknown`/`untracked` → `—`. Codex implemented the spec's words faithfully; the fold corrected them. `pinned` as a state is not derived (a pinned placement still shows its health; the version column carries the pin).
- Frames: re-recorded from the merged CLI (fixture.sh beside this file, fixture root under the session scratchpad, cwd = the fixture's seed repo so the project section is the fixture's, not this checkout's). `replay.test.ts`, `replay-screens.test.tsx` and `index.test.ts`'s status replay read this batch's frames; the "older status is refused" test keeps S7f's recording, which is the older schema it needs; S7d's `usage-error`/`decline` frames stay in S7d's directory.
- What lights: Settings ▸ This machine's placement table shows the real state and version of every placed skill; Library and Skill detail read `installed` and the version from the ledger. Follow-ups: `SHARED` rows keep state `—` until `sharedState` ships (D-BM-6); `placed` stays `—` (no date in the payload); the CLI's `problems` have no drawn place beyond the `folder missing` state.
