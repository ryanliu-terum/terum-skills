# m7-S7f notes

- Rows RM-49, RM-10, BM-08, RM-30, RM-02, RM-03, RM-38, RM-01, BM-01, BM-04, AD-21, AD-22. CLI + app. Batch 4 of tranche 1; built on main 2d6f247 (S7af merged while the run was live; the branch tip was S7af's, so main is a fast-forward).
- Codex (gpt-6-astra, high) reported `partial` with every gate green: seven open questions, three deviations, no bug logs. Self-report: root 72 files / 1147 tests; desktop 46 files / 549 passed + 88 oracle-skipped. Its `implementation-notes.md` is kept beside this file.
- Orchestrator rulings on the open questions:
  - Team selection (§2 Argv): the adapter bootstraps with one unscoped `status` (which never prompts), accepts exactly one configured team, refuses ambiguity with `Select a team explicitly to read its skills.`, and passes `--team` on every later read. Accepted; the screens gain an optional `team` and S7k's `status()` read model will supply the selected team.
  - Raw frontmatter (AD-22): the wire carries the body only; the real SKILL.md tab renders the body as Markdown (react-markdown, already pinned) and draws no frontmatter block. Accepted: synthesising frontmatter bytes would be a fabrication.
  - `skillVersions` (RM-10): git prints the same missing-object text for an invalid ref and a valid ref without `skills/`; the wrapper verifies the ref with one extra read-only child only on that path. Accepted; the normal listing is one child, the concurrency test bounds the peak at nine.
  - Installed state before S7g: only the explicit `placement recorded from <team>` sentence in `ls --local` marks a card installed; untracked or other-team folders never do. Accepted (S7g replaces the sentence match with structured fields).
  - Recently updated: dates are carried, no sort (design fork, recorded for Ryan/Teddy). Install scope selector: the real adapter serves `installScopes: []`, the dialog draws no radios and says the CLI picks the scope; the mock keeps the four drawn rows via the DTO. Accepted.
  - Maintainer files: `desktop/src/backend/tauri/README.md` still calls Library and Skill detail unavailable; GAPS.md needs its Library/Skill lines retired. Both updated by the orchestrator in this PR.
- Codex also stopped `SkillScreen.execute('install')` from pre-answering the CLI's `Approve these tools for <ref>?` question (the consent now arrives as the CLI's own dialog after the install dialog closes; `library-skill.test.tsx` clicks it). Orchestrator ruling: accepted. The app must not answer a consent question on the user's behalf; the CLI's question names the normalised grants it will record. FOLLOW-UP: `InboxScreen.tsx` still pre-answers the same question for Inbox installs; align it in the Inbox batch (S7n) or earlier.
- Orchestrator fixes on top of the Codex diff (all mechanical): `inventoryDetail.root` is always `'Global'` (a not-installed team skill used to send Back to the hidden Marketplace); the overview note pluralises `teammate`; the Library header title takes the real project registry's spelling when the payload carries it (a real project `terum` no longer renders as the design's `Terum`); the `(rev N)` placeholder dropped from the §6 amendment.
- Real-data proof: Codex rebuilt the CLI and re-recorded `status`, `ls`, `ls --local`, `ls project terum`, `ls member mira`, `search ''` and `validate deploy-check` from the fixture team through its own copy of `fixture.sh` (identical apart from the `CLI=` line, kept beside this file). `replay.test.ts` replays them through `library()` (Global, project and installed scopes) and `skill()`, asserting the fixture's description, grants hash, installers, ISO date, body and project registry from the wire; `replay-screens.test.tsx` mounts the real `App` over the same frames and asserts the rendered Library and Skill screens show the recorded data with no favorite control, no fabricated frontmatter and no sample provenance.
- What lights: Library ▸ Global, Library ▸ project/<name>, Library ▸ installed and Skill detail (SKILL.md, Quality with the validate caption; Evals stays "Not evaluated"; Activity empty) from real team data. Sidebar project rows come from the registry. `surfaces.library` and `surfaces.skill` are true on the real adapter.
