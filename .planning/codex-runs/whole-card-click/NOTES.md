# whole-card-click notes

- Branch `fix/whole-card-click` off origin/main 279830d, worktree `skill-management-software-wt-card`. Codex `gpt-6-astra`, effort high, `--sandbox workspace-write`, report schema `.claude/skills/codex-implement/report.schema.json`. Round 1 ~4 min; round 2 (PROMPT-r2.md, effort medium) removed one Codex-invented assertion, `expect(toggle.tagName).toBe('BUTTON')` — Base UI's Switch renders `span[role=switch]`; the spec asked only that the click not change `location.hash`.
- Codex reported `partial` both rounds for the same non-defect: vitest cannot run inside the sandbox (EPERM on `desktop/node_modules/.vite-temp` through the symlinked node_modules), and a browser will not launch there. Every gate number in `verify.log` is the orchestrator's own run.
- Diff read: `SkillCard.css` gains the overlay, the two `position:relative` raises, `-webkit-user-drag:none`, `a:hover{text-decoration:none}` (the one addition to the locked spec: `app.css:51` underlines every `a:hover`, and with the overlay the title would underline whenever the pointer is anywhere on the card, against the canvas's plain-text title in the hover state) and the `@supports selector(:has(a))` block; no `z-index`. `marketplace.css` mirrors it for `.market-person-card`; PersonCard keeps a plain `<div>` around the avatar so `.market-card-head>span:not(...)` cannot restyle a bare `span.board-avatar`. `OnboardingBasics.tsx` preview is `inert` + `aria-label`. `SkillCard.tsx` untouched. Existing tests unchanged; four appended.
- Harness traps: (1) a symlinked `desktop/node_modules` makes Vite refuse the Inter/JetBrains woff2 files ("outside of Vite serving allow list") and every Playwright test fails at `prepare()`'s `document.fonts.check` — both worktrees got a real copy of the lockfile-identical `wt-m7/desktop/node_modules`. (2) `~/Downloads/terum-teammate-bundle/design-canvas-terum-skills/.shots` is byte-identical to `.shots-linux-teddy-2026-09-08` (Main.png cmp): 86/88 fail there, Main at 3,702 px — the Linux-raster signature. The oracle is `/Users/ryanliu/Documents/Terum/design-canvas-terum-skills/.shots` (re-rendered 2026-09-08 17:23).
- Fidelity: 88/88 passed on the fix; the same gate on untouched origin/main gives identical per-board diff-pixel counts on all 88 boards (rows compared one by one). MarketplacePeople, MarketplacePerson, MarketplacePersonNotInstalled, Main, Light at 0 px; Library 18 px; OnboardingManage 1,779 px on both trees.
- Fail-before: `card-click.spec.ts` alone on origin/main (spec file copied into a detached scratch worktree): 4 failed (the three body clicks and the card focus ring) / 6 passed (the controls, which already did not navigate). After: 101/101 across e2e/routes.
- Not done: nothing from the spec. The GAPS/README line went to `desktop/README.md` (orchestrator-written; desktop/AGENTS.md forbids an implementing agent from editing README/GAPS).
