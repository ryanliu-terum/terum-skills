# m7-S7af notes

- Rows AD-01..AD-10 (adapter plumbing and the real-data rule). No CLI change. First batch of tranche 1; every later app-half batch stacks on it.
- Pass 1 (partial): Codex kept the Inbox/Share/Marketplace sidebar COUNTS literals because the mock's status() returned Global '0' for every route in the `empty` scenario while ShareEmpty and InboxEmpty draw their own counts. Orchestrator's ruling: the board knowledge belongs in the MOCK (it already reads the URL hash for the scenario), never in a screen. Pass 2 (complete): `src/backend/mock/status-counts.ts` maps route family × scenario → counts (Library empty: Global '0'; Inbox empty: Pushes/Updates/Alerts omitted; Share and everything else: the design COUNTS); the four literals are gone; screens pass `counts` undefined (status decides) or null while loading/error.
- Seam additions: `Backend.surfaces()` (ten booleans; mock all true, real adapter all false today), `ReadOptions { signal }` on every read model, `SearchHit` widened with nullable wire fields, `team?` on the mutating args, `PublishResult.changed` and `version: string | null`. `read()` is now an exported consumer over a `Run` (collects print lines, cancels on ask, honours an AbortSignal). `invalidation.ts` maps ChangeSource → query-key prefixes; providers.tsx subscribes once (pickBackend is a module singleton, so the effect deps are stable). Query defaults: staleTime 30 s, refetchOnWindowFocus true, refetchOnReconnect false, refetchOnMount 'always' (ledger D9: refresh on own actions and focus; no timer).
- Merge: origin/main (S7ag) merged into the branch; `fake-bridge.ts` took S7ag's shape (spawn returns an unlisten spy; exposes unlisten and emit); `index.test.ts` keeps S7af's tests plus S7ag's spawn-rejection describe. Gates re-run after the fix.
- Maintainer edits in this PR: GAPS.md re-anchored at b7be3c5 with the M7 status paragraph and the PF-08 line.
- What lights: nothing yet by design. The real adapter still serves no read model, so the real app now shows Library ▸ Global only (every gap surface hidden), which is the North Star's honest state until S7f/S7k/S7b serve data.
