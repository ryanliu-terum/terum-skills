Follow-up to your previous run in this same working tree (your changes are still here, unstaged). Read AGENTS.md at the repo root and desktop/AGENTS.md first. Then finish the ONE open item from your report; everything else you did stays as is.

THE OPEN ITEM (spec §3, AD-03): the four hard-coded sidebar COUNTS literals must go so a count comes only from `status()` or is omitted. You removed Library's and kept Inbox's, Share's and Marketplace's because the mock's `status()` returns `Global:'0'` in the `empty` scenario regardless of route, while the locked boards expect: ShareEmpty → the design COUNTS unchanged (a team of one still has 30 global skills); InboxEmpty → the design COUNTS with Pushes/Updates/Alerts omitted (zero items); LibraryEmpty → COUNTS with Global '0'; MarketplaceEmpty is unreachable in the shell (its literal is dead code).

THE ANSWER (decided by the orchestrator, the maintainer): the board knowledge belongs in the MOCK, not in the screens. `readScenario()` already reads `location.hash`, which carries the route. Make the mock's `status()` derive its `counts` from the route family AND the scenario: default scenario → design COUNTS; `empty` on `#/library/*` → COUNTS with Global '0'; `empty` on `#/inbox*` → COUNTS without the Pushes/Updates/Alerts keys (omitted, not '0'); `empty` on `#/share*` and everywhere else → COUNTS unchanged. Implement that as one small pure function in `src/backend/mock/` with a unit test (route × scenario → counts), then DELETE the three remaining screen literals (InboxScreen, ShareScreen, MarketplaceScreen) so every screen passes `counts={undefined}` (let the Shell's status decide) or `null` while loading/error exactly as the other branches do. Screens never branch on the mock scenario for counts. The real adapter is untouched by this (its `status()` is still the typed gap).

Constraints as before: no git, no npm install, no network, no Playwright; never edit GAPS.md, FIDELITY.md, AGENTS.md, README.md, package.json, design.json, tokens.css, e2e/**, tools/**; never weaken a test (update the affected screen tests' expectations to the new source of counts and declare each in testsModified); gates from desktop/: `npm run typecheck && npm run lint && NODE_OPTIONS=--no-experimental-webstorage npm test` with real counts. Do not commit. Verify each write by reading it back. Report status `complete` only if the three literals are gone and the gates ran green; put the GAPS.md re-anchor note in openQuestions again (maintainer file).
