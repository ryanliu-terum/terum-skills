# m7-S7ad notes

- Rows BM-11, BM-12, AD-19, AD-18. CLI + shell + adapter. Batch 7 of tranche 1, built off main 2d6f247 (S7af and S7ag both merged), then merged main again for S7f (no conflicts) and for S7q.
- Codex (gpt-6-astra, high) reported `complete`, no open questions, two deviations: it compiled Rust into a temp target because the sandbox refused the shared warm target's lock (harmless), and its root test gate was red on the unchanged `runner.test.ts` sleeping-child cleanup case. The orchestrator's own root run outside the sandbox passed all 1133 tests, so that failure was the sandbox blocking the process-group kill, not a regression.
- Ledger D4 is applied as decided: `app.json` records the writing process's PATH verbatim (`string | null`), `cli_spawn` replays a non-empty recorded PATH into the child and otherwise inherits the shell environment; no login-shell probe. `target` is written only by a `setup` hand-off and cleared by the next plain `app` run (rename-over-the-file). `writtenAt` is now REQUIRED on the adapter's schema: a pre-S7ad file without it fails parse with the NO_STATE guidance, and one `terum-skills app` run rewrites it.
- AD-18: `bridge.readAppState` wraps both a JSON parse failure and a schema failure in the NO_STATE sentence plus the detail; the adapter no longer memoises a failed or null read (retried on the next call), while a successful read stays memoised and concurrent first reads share one promise. `Backend.launchTarget()` exposes `{ target, writtenAt }` or null; the consume-once routing and its preference are S7r's.
- `lib.rs` keeps exactly five commands (D9). `app` stays outside `FRAME_VERBS`; `hello.protocol` stays 1 (`app.json` is not a frame).
- Real-data proof: Codex rebuilt the CLI and captured three `run/app.json` files under the fixture HOME through `prove-app-state.mjs` (the `open: false` knob, no download, no native launch): `app.json` (a plain launch: the shell's PATH, no target), `setup-app.json` (the real setup → app hand-off: same PATH plus `target: acme/team`), `plain-after-setup-app.json` (the next plain launch clears the target). All three are in this directory. Not verifiable here: the Rust replay into a real child under the Dock's environment (Step 4).
- Orchestrator gates: see `verify.log`. The first battery (23:52-00:05 UTC, load average 50-110 with three Codex runs live) was green everywhere except desktop vitest (18 red: 16 five-second timeouts and two `findBy` misses, all in screen tests this batch does not touch) and `export:check` (the canvas md5 moved with patch AL; fixed by taking S7q's regenerated fixture). Fidelity passed 88/88 against the relabelled oracle even though this branch still drew "New skill": the Library boards' tolerance (0.0030 of the frame) is wider than that label change, so the gate cannot see the A3 relabel either way. Re-gated in full after merging main.
- What lights: nothing on screen. What changes for a real teammate: a Dock launch finds `git`, `gh` and the right `node` because the CLI children run with the PATH the CLI saw; a joiner's `setup` hand-off carries the team target into the app for S7r's onboarding route.
