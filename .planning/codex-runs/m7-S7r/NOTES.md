# m7-S7r notes

- Rows AC-01/02/03/05/10/12/13/14/15/16, MC-02/03/08, IB-01/04/09, CP-21/25/41, RM-06/12/17/33/34/35, and RM-24 (paired in by the launch handoff so a joiner's first run works in tranche 1). App-only, no CLI change. Batch 12 of tranche 1, built off main 14ccc45 (S7d), then merged main through S7c (6189500; eight conflicts folded).
- Codex (gpt-6-astra, high) reported `partial` for one reason: its recorded `setup` run closed stdin at the first ask, which the CLI reports as an ordinary failure (`Input ended before "Use this identity?" was answered.`), so the "declined at the first ask" proof the spec asked for was not producible. Orchestrator finding: a typed decline is NOT reachable in `setup`: "Use this identity?" answered no re-prompts for a new identity (text asks), and the hook / Claude-skill offers answered no are `skipped` steps on a successful result, not cancellations. The spec's proof sentence was wrong about `setup`; the real proof recorded here is a complete offline join (`frames/setup-join.jsonl`: identity confirmed, invitation blank, both offers declined, `ok: true` with every step reported), replayed through the driver in `setup-replay.test.tsx` (the card settles "Setup finished", no alert, the launch timestamp consumed once). The failure recording (`frames/setup.jsonl`, stdin closed at the ask) stays as the failure-path proof, and the typed-decline path is covered by the driver unit test with a synthetic frame. Five other open questions ruled conservatively as Codex chose: no clearing of the Boot captions beyond what `SetupResult.steps` substantiates; the native record admits chrome keys only while the browser mock keeps its simulated action state; no new permanent Sync controls on healthy Marketplace/Share boards (a design call; the existing Settings ▸ Sync control and the drawn shortcut drive it); vitest routing coverage instead of Playwright edits (e2e/** is maintainer-owned); hostname stays `—` (no CLI payload, no sixth command per D9).
- **What it does:** one app-owned preference store (`desktop/src/backend/prefs.ts`, native `tauri/prefs.ts` in the app config dir through `@tauri-apps/plugin-store`, chrome keys only, a one-time migration from the old webview keys, an unreadable store yields the drawn defaults); the window background follows the theme (`tauri.conf.json` `backgroundColor` + `setBackgroundColor`, permission `core:window:allow-set-background-color`); `LaunchRoute` sends a fresh `app.json` target to `#/onboarding/boot`, where `SetupBoot` drives `backend.setup({target, offerConnect:true})` through the Prompter (asks become the drawn dialog, never pre-answered), renders print/progress frames, and consumes `writtenAt` only after the run settles ok or cancelled; the Inbox nav, bell and routes appear only when `surfaces.inbox === true` (false on the real adapter until IB-01's feed is real); Sync runs only from a user action (`useSyncAction`, team-scoped), with a test asserting no timer or polling refetch exists; relative times come from one module over ISO instants; status counts and PINNED_N derive from the ledger; the placement table's `placed` column carries the real `placed_at`.
- **Fold onto main:** S7g's PLACEMENTS (drawn vocabulary, 12-char version, `folder missing`) with S7r's real `placed_at` in the placed column (S7g's `—` expectations updated to the fixture's instant); S7e's `UpdateAdvice` beside S7r's `PrefStore` lifecycle (`ready`, `flush`, `subscribe`); S7c's identity-saved popup beside S7r's sync popup and its team-scoped Sync test; S7r's Settings inbox-section guard on main's readiness expression; S7c's `setIdentity` needs `zod` in the mock, restored after S7r moved the prefs code out.
- **Maintainer review of the risky bits:** `tauri/prefs.ts` serialises writes behind one promise and reports a failed save through `flush()` (the Boot step surfaces it as an alert); `capabilities/default.json` was reformatted by Codex (multi-line; same grants plus the one new permission).
- What lights: a joiner who launched from `setup` lands on a real Boot step that runs the wizard's remaining questions inside the app; theme and window chrome persist per machine; Sync now on Settings ▸ Sync; the Inbox stays hidden on the real adapter and the PR body says so. Follow-ups: the Boot card's four captions are setup outcomes, not the sync facts PR #61 drew (no evidence for them yet); `machine.name`/hostname stay `—`; Sync now on Marketplace/Share awaits a drawn control.
