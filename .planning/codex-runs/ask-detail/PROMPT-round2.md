Follow-up to your previous run in this same worktree (feat/ask-detail). The orchestrator ran every gate outside the sandbox: root lint/typecheck/build green, desktop typecheck/lint green, desktop vitest 970 passed / 2 failed (only the two S7r replays waiting on the re-recorded frames, as expected), root vitest 1272 passed / 1 failed. Two focused fixes; touch nothing else. Same rules as before: no git commands, no npm install, no edits to recordings, leave changes unstaged, finish with the same structured report.

1. src/commands/__tests__/install.test.ts — the test "shows malformed allowed-tools verbatim, requires consent, and leaves no durable intent when declined" (around line 160-170) still asserts `expect(io.lines.join('\n')).toContain('{"Bash":"*"}')`. Under Part A.C the raw value now travels in the confirm's detail, so this assertion fails on the new tree. Re-anchor it: `io.lines` no longer contains the raw value; `io.details['Install sample despite malformed allowed-tools?']` is a one-element array whose only line starts with 'allowed-tools for sample could not be parsed: ' and contains '{"Bash":"*"}'. Keep every other assertion in that test. Then grep the whole src/ tree for any other test that still reads a consent or identity line from `io.lines` (patterns: 'requests allowed-tools', 'could not be parsed', 'allowed-tools changed', 'Identity: @') and re-anchor those the same way; list each in testsModified.

2. desktop/src/screens/onboarding/SetupBoot.tsx — you changed the persistence-error line to `<div role={refused?undefined:"alert"}>`. Revert that to the original unconditional `<div role="alert">{state.persistenceError}</div>`: a failed preference flush is a real error whatever the setup outcome, and Part B.5's "no alert styling" applies only to the refusal line itself (which already renders in the role="status" region). If a test you wrote asserts `queryByRole('alert')` is null in the refused case, it stays valid because the refused fixtures have no persistence error; adjust only if it genuinely depended on this.

Run `npm run lint` and `npm run typecheck` at the root and in desktop/ and report real results. Root vitest is blocked by the sandbox EPERM you saw; say so.
