# m7-S7q notes

- Rows AC-06, AC-07, AC-08, IB-08, RM-27, RM-45, RM-47 plus the hello-features wiring (Ledger D6/D1/D2/D9/D10). App-only; no CLI change. Batch 3 of tranche 1, gated after merging `codex/m7-S7f` (S7f merged to main as PR #64 while this branch was under review).
- Codex (gpt-6-astra, high) reported `complete`: 39 files, self-reported desktop typecheck 0 / lint 0 / 50 files 606 tests + 88 oracle-skipped, seven open questions, two deviations, no bug logs. Its `implementation-notes.md` (written to `desktop/` by mistake, moved here) carries the cover notes for AC-07 and CP-14 (both refused: `guardTeam` admits exactly rows c/d/e and `categories` is written once at scaffold time) and the permission notes (public `toggleMaximize` needs `core:window:allow-toggle-maximize`; `opener:default` dropped for the GitHub-scoped `opener:allow-open-url`).
- **Spec error, recorded for Ryan.** The spec's "CP-13" paragraph described the New skill → Connect relabel. The costing's CP-13 is a different row ("'uninstall-skill project/member' writes declined — the consequence dialog is undrawn": the remove confirmations should state, from the same predicate the CLI uses, when a removal will suppress automatic placement, with the two numbers for the bulk forms). That row is NOT implemented here and is carried forward (it is copy on SkillDetailRemove, MarketplaceProject and MarketplacePerson, so it needs a canvas patch as well). The relabel itself is Teddy's A3 (M7 doc, S7z status: "MOOT: Teddy relabelled the New skill CTA to Connect"), so it stands.
- **Pixels moved through the canvas.** Canvas patch `.patches/patch_al.py` (two anchors: `view_header`'s default action label and LibraryEmpty's primary) → `build.py` → `render-mac.mjs --all` (99 boards re-rendered on this Mac) → `npm run export` (design.json/tokens.css change only in their build.py md5 line). Orchestrator kept LibraryEmpty's body sentence as drawn (Codex had reworded it; only the CTA label is ruled).
- Orchestrator rulings on the open questions: the combined Settings alert-kind row hides with `offtargetKind` (no separate off-target row is drawn); the Account sign-in hands off through a popup with a Copy button (`openInEditor` opens files, not commands; RM-45 forbids the app touching credentials); the clone-state copy covers seven cases and the `cloneState`/`readable` fields are optional until S7k serves them; Onboarding Basics ▸ Eval already has no Run eval control; the permission additions are accepted as the installed API requires; GAPS.md:28-32 and `desktop/AGENTS.md` invariant 2 are rewritten by the orchestrator in this PR.
- **Merge with S7f (seven conflicts, resolved by folding both edits):** `types.ts` (S7f's Library shape + S7q's CloneState/TeamStatus), `Shell.tsx`/`Sidebar.tsx` (S7f's registry project rows + S7q's team → Footer and features readiness), `SkillCard.tsx` (the heart shows only when `features.favorites` AND the card carries a favorites count), `LibraryScreen.tsx` (S7f's registry title/provenance + S7q's Connect CTA via the connect picker and `revealPath`), `SkillScreen.tsx` (S7f's nullable slots, DTO `installScopes`, no consent pre-answer + S7q's feature gating, `print` forwarding, deep link through `openUrl`; the install dialog draws radios only when `features.installScope` AND the DTO has scopes, else the static "Global" line at the drawn height), `receipt-rules.test.tsx`. The Repo deep link is `<repo>/tree/<version_full ?? 'HEAD'>/skills/<name>/`: on the real adapter `version_full` is null and the CLI's versions are tree hashes, which GitHub cannot resolve under `/tree/`, so HEAD is the honest target until a commit-ish is served (follow-up under AC-08/RM-46).
- What lights: nothing new from real data (S7f lit Library and Skill detail). What changes for a real teammate: every control the CLI's hello frame does not back is gone (hearts, follow, role menus, job labels, last seen, invite scoping, per-machine switches, lift figures, Run eval, per-case tables, other-machines rows); native chrome no longer draws fake window controls; the Library's primary action is Connect and opens the connect picker.
