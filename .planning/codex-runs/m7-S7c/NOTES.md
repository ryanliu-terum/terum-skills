# m7-S7c notes

- Rows MC-11 (the identity WRITE half), CP-D1 (closed by merits, recorded). CLI + app. Batch 11 of tranche 1 in the queue, built off main d5b0408 (S7q), then merged main through S7d (42e3cd9; six conflicts folded), through S7g (150ff41; one test-file conflict, both sides' tests kept) and through S7e (1e1d749; four conflicts: both sides' mock and cli tests kept, S7e's `UpdateAdvice` import beside `IdentityWrite`/`IdentityArgs`, the identity-saved popup beside S7e's update dialog, one `Dialog` import).
- Codex (gpt-6-astra, high) reported `complete` with three open questions, ruled here: (1) the Account field saves on blur or Enter (the spec named no trigger; a save-per-field with the field disabled while busy is the conservative reading; no new board); (2) bare `login` keeps its shape with `updated: []`, `notice: null`, and the `--set` form returns `gh: null` and the current default handle without probing gh (the spec forbids the probe); (3) repeated `--set` of one key: last assignment wins, `updated` keeps the supplied order. Codex's final test runs used `--maxWorkers=2` under sandbox load; no deadline or assertion changed.
- **CLI half:** repeatable `--set <key>=<value>` on `login`, keys `name` → `display_name`, `email`, `default-handle` → `default_handle`, each validated with the schema `configSchema` already carries for that field; `github` and any other key are refused with the accepted-keys line; the write goes through `ConfigStore.update` (still the only writer) with a new `preserveUnchanged` option that patches only the changed top-level values into the file's existing bytes (the file is fully parsed first; the patcher runs only under that flag, so every other writer is unchanged); the consequence sentence is printed before the write and carried on the result (`LoginResult.updated`, `.notice`). Orchestrator's reading of the spec's "byte-for-byte": Codex met it literally; the mechanism is 35 lines in the ledger's writer and is covered by `login.test.ts` byte-preservation cases, so it stays, recorded here for Ryan's read.
- **App half:** `Backend.setIdentity` → `run(['login', '--set', <pair>…], cliLogin)` with `touches: ['config']` (status/settings invalidate); the mock writes an in-memory copy of S7k's `ME` and notifies `config`; Settings ▸ Account's three fields save through the seam (no `localStorage` identity writes remain) and the returned notice renders in a popup titled "Identity saved" (the spec said "the workflow popup line"; Codex's dialog reuses the drawn popup wrapper, pixel-neutral on the boards). **CP-D1 app half:** `Handle on <team.name>` and the eval "Run trees" row read the real team's display name and key from `status` (the design's `Terum`/`terum` literals are gone).
- **Fold onto main:** S7k's typed `StatusResult`/`Settings`, presence-only GitHub row and mock status/settings shapes win; S7c's `setIdentity` is re-applied on the seam, adapter and mock, and its Identity row replaces the pref-backed fields; S7d's AD-17 argv for install/uninstall-skill wins (`login --set` has no positional). The identity replay test's "S7k surfaces remain gaps" premise was retired: `status-before`/`status-after` now replay through the served `status()` and show `Seed` then `Seed2`.
- Frames: `login-set-name.jsonl`, `status-before.jsonl`, `status-after.jsonl`, `config-before.json`, `config-after.json` and `proof.json` were re-recorded from the merged CLI against the fixture (fixture.sh beside this file; cwd = the seed repo); the real `login --set name=Seed2` changed exactly the `display_name` bytes. `ls-local.jsonl` answers the `ls --local` half of `status()` (S7k reads both); its one placement reports `health: both` only because this batch's fixture still carries the pre-S7g placeholder fingerprint `"fixture"`, not a defect.
- What lights: Settings ▸ Account's Name, Email and Default handle write the real `config.json` through the CLI and the footer/Settings identity re-reads it. Follow-ups: the "Identity saved" popup title is Codex's wording (no board draws a confirmation; Teddy may want the notice inline); the `preserveUnchanged` patcher is the one non-trivial mechanism in this batch for Ryan to read.
