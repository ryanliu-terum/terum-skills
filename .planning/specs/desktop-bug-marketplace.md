# desktop-bug-marketplace — the Marketplace on the real adapter derives every number from the frames it already has

**Status:** locked (Teddy, 2026-09-09: "the mock data should not exist in the real version, it should only be real data; however all of the interaction and fidelity should exist"). Source rows: `~/Projects/SSM/review/2026-09-09-mock-vs-real/sections/marketplace.md` and `verbs-seam.md` (M3, M6–M8, M10, M11, M12–M17, M18, M20–M22, M26, V3, V13), copied under `.planning/codex-runs/desktop-bug-marketplace/` by the orchestrator.

**Base:** `origin/main` after PRs 91 (Share ▸ Invite reads Settings, `InviteResult.already/failed`), 92 (`uninstall-skill` asks once with a preview; `UninstallArgs.kind/member/project`; Marketplace bulk Remove is one run whose ask reaches `PromptDialog`), 93 (app version from the root package, `plural()` with an irregular form, missing-data separators: crumbs drop empty/dash segments, identity sub-lines never render a dangling `·`, Sync's quarantine row omits `· —`) and 94 (run.ts cancel race, frame parsing). Read `git log -5` and those diffs before starting; rows they already fixed are marked below and must not be redone. `plural(n, word, words?)` already lives in `desktop/src/screens/marketplace/market-data.ts` (PR 93 extended it with an irregular-plural argument); import it from there. Do NOT create `desktop/src/lib/plural.ts`.

**Sources:** `desktop/AGENTS.md` (invariants 1, 2, 5, 6, 7), `desktop/src/backend/tauri/README.md`, `src/commands/{uninstall,install,ls}.ts` (read only). Recorded frames: `.planning/codex-runs/mock-vs-real-2026-09-09/frames/*.jsonl` (0.1.7; fixture team `acme`; see the argv → file map in `replay.test.ts`'s `inventoryReplay` and extend it with `ls member --team acme -- <h>` → `ls-member-<h>.jsonl`, `ls project nope` → `ls-project-nope.jsonl`, `ls member … -- nope` → `ls-member-nope.jsonl`). What the frames carry: `ls.jsonl` → per skill `updated` (ISO), `installedBy[{handle,displayName,scope,since}]`, `grants` (`"none"` or newline-separated), `installs`, `endorsement`; `projects[{name,remotes,skills}]` (no description/admin/updated); `roster[{handle,active,role:null,projects:[]}]`; `ls-member-<h>.jsonl` → `member.installed[{id,scope,since}]`, `member.declined`, `member.role:null`, `member.projects:[]`; `status.jsonl` → `repository:"https://github.com/acme/team"`, members with `displayName`. `ls-local.jsonl` → one placed row (`deploy-check`).

**The rule.** The mock backend and the locked boards are the oracle and do not change; every screen edit is pixel-neutral on the mock (identical DOM and strings for the mock's data — the mock's `role` strings are non-empty, its `publishLine`/`teamsLine`/`placeNote` are sentences, its projects carry `desc`/`admin`/`updated`, its `verdictCounts` are numbers). The real adapter serves only values derivable from the frames; a value with no source is `null` and the screen draws its absent state instead of `—`-in-a-sentence fragments. No component branches on which backend answers.

## 1. Adapter — `desktop/src/backend/tauri/index.ts` (`rosterModel`, `catalogModel`, `catalog()`, `inventoryCard`)

| Row | Today | Required |
|---|---|---|
| M6 / M11 | `publishLine: '—'`, `lastPublish: '—'` | From the member's **authored** skills (the `ls member` result's `skills[]`): newest `updated` → `publishLine = \`Published ${name} · ${relative}\``, `lastPublish = \`${relative} · ${name}\`` (the mock's `derive.ts` `publish_line`/`last_publish` shapes; reuse its relative-time helper if it is importable from a shared module, otherwise add `desktop/src/lib/relative-time.ts` and make the mock's derive import it — the mock's output must not change). No authored skill → `publishLine = 'Nothing shared yet'` (the mock's own wording), `lastPublish = '—'` |
| M7 | `teamsLine: member.projects.join(' · ')` → `''` | Empty projects → `'On no project yet'` (the mock's wording, `derive.teams_line`) |
| M8 | `role: member.role ?? ''` on `Member`/`Person` | Keep `null`: change `Member.role` and `Person.role` to `string \| null` in `types.ts` (the fixture types them as `string`; the `Omit<…> &` intersections already exist for `followers` — do the same for `role`). Mock fills the fixture string; real fills `member.role` (null in the frames) |
| M9 | `organization: team.team` | `organization: null` — the slot is the person's project on the board; the team id is not a project. (A separate team slot is a design question, out of scope) |
| M10 | `placeNote: '—'` | Compose from `onDisk`: `${onDisk[0]} of ${onDisk[1]} on this machine · install places the other ${onDisk[1]-onDisk[0]}` when `onDisk[1] > onDisk[0] > 0`; `Install places ${onDisk[1]} skill(s) on this machine` (use `plural`) when `onDisk[0] === 0 && onDisk[1] > 0`; `''` otherwise (the screen's `personStatus` already handles `onDisk[1] === 0` and the fully-installed case) |
| M14 | project `updated: '—'` | `max(updated)` over the project's skills (`inventory.skills` filtered by `project.skills` ids), rendered relative with the same helper as M6; `null` when the project has no skills (type `string \| null`) |
| M12 / M13 / M15 | `desc: ''`, `admin: {handle:'',name:'—',…}`, `evaluated: null` | `desc` stays `''` (no source; screen hides the row). `admin` becomes `Project['admin'] \| null` — real serves `null`, mock serves the fixture object. `evaluated` stays `null` (screen hides the chip/row while null) |
| M16 | `path: null` | Serve the project's placement root when known: the `repoRoot` of the `ls --local` project section whose `rows` contain every skill of the project (the definition `installed` already uses); else `null` |
| M17 | `repository: team.repository ?? null` (full URL) | Owner/repo form via `repoIdentity`/`stripRemote` in `backend/paths.ts` with the leading `github.com/` removed (`acme/team`); `MarketRail` builds the href with `githubUrl`, which accepts that form |
| M18 | `verdictCounts: {PASS:null,NEUTRAL:null,FAIL:null,'Not evaluated':null}` | Count the catalog's cards by `summary?.verdict` (`null` → `'Not evaluated'`). A count of rows is not a derived statistic (invariant 6 untouched) |
| M20 | `bulkInstall: {}` | Per project: `{ total: project.skills.length, asking: <skills whose normalizedGrants is neither null nor 'none'> }`, keyed by `project.key` |
| M21 | **DONE by PR 93** (`inventoryCard` prints `1 install`). Nothing to do |
| M23 | `scanned: … section.scope === 'global' ? '~/.claude/skills' : section.repoRoot ?? section.root` | `abbreviateHome(section.repoRoot ?? section.root, home)` for every section (drop the literal), and skip a section whose `rootState` is `'absent'` (declare `rootState`, `label`, `registered`, `detected`, `counts` as optional keys on the `cliLs.local` section schema — invariant 2). `home` is already awaited in `catalog()`; pass it into `catalogModel` |

Do not change `Person.skills`/`Person.installable`/`buckets`/`onDisk` semantics (M1) — Teddy has not decided what a person page lists. Do not remove the per-member `ls member` loop (M24; a separate spec). Do not touch `library()`.

## 2. Screen — `desktop/src/screens/marketplace/{MarketplaceScreen,market-components,market-data,market-filters}.tsx`

| Row | Today | Required |
|---|---|---|
| ~~M3 / V3~~ | **DONE by PR 92** — do not add a screen-drawn remove dialog; the CLI's own confirm (with `detail`) reaches `PromptDialog`. Row kept only for context: | `remove()` runs `uninstallSkill` per skill with a dead preset `Remove ${skill}?: true`, no dialog; on the real CLI `uninstall-skill` asks nothing and deletes at once | Add a confirm dialog reached by `?dialog=remove` on project and person pages (URL state, invariant 7): title `Remove ${name}'s ${plural(n,'skill')} from this machine?`, a list of the skills that will be removed (name + `paths` from the cards), body `Their placed copies leave this machine; a copy you edited by hand is moved to quarantine instead of deleted.`, `Cancel` / danger `Remove`. The trash button opens the dialog; `Remove` runs the loop with **no preset** (`{}`); an empty set shows `Nothing to remove on this machine.` instead of running. No locked board draws this dialog open, so the closed state is pixel-identical |
| V13 | `install()` presets `Approve these tools for ${ref}?: true` (matches only the mock's single question; the real CLI asks per skill) | Delete the preset (pass `{}`): every tool-approval question reaches `PromptProvider`. Update `marketplace.test.tsx` cases that relied on the silent consent to answer the dialog |
| M5 | `market-components.tsx` `navigate('/skill/' + skill.name + '?__mock=not-installed&dialog=install&root=marketplace')` | `navigate('/skill/' + encodeURIComponent(skill.name) + '?dialog=install&root=marketplace')`. `?__mock=` is never produced by product code. (`SkillCard.tsx` has the same string; it belongs to `desktop-bug-chrome-library` — if that spec has not landed in your tree, fix `SkillCard.tsx:15` here too and say so in `deviations`) |
| M8 (screen) | **DONE by PR 93** (`PersonCard` ident and the `DetailPage` heading already filter empty parts). Only the type change in §1 remains; check that `role: string \| null` typechecks through PR 93's `filter(Boolean)` code |
| M12 | `<span className="market-project-desc">{q.desc}</span>`, heading `<span>{q.desc}</span>` | Render the element only when `desc` is non-empty |
| M13 | `market-project-foot` admin block; rail `Admin` section | Render only when `admin !== null`; the foot's `updated` clause renders `updated ${q.updated}` only when `updated !== null` |
| M15 | `<Chip>{q.evaluated ?? '—'} of {q.skills} evaluated</Chip>`; rail row `Evaluated` | Omit the chip and the rail row when `evaluated === null` |
| M16 | `` `${q.skills} skills placed in ${q.path}` `` | `q.path ? \`${plural(q.skills,'skill')} placed in ${q.path}\` : \`${plural(q.skills,'skill')} placed on this machine\`` |
| M17 | `repository?.replace(/^github\.com\//,'')` | Keep (the adapter now serves owner/repo); also strip an `https://github.com/` prefix defensively |
| M22 | `${c.projects.length} projects`, `${c.teamN} teammates`, `Search ${c.projects.length} projects`, `Search ${c.teamN} people`, `Install ${total} skills`, the three Remove aria-labels PR 92 wrote (`… ${total} skills …`), `<Chip>{q.skills} skills</Chip>`, `Adds the project's {project.skills} skills` | All through `plural()` from `market-data.ts` (PR 93 already did categories, filters and `installs`) |
| M26 | One error board: `Couldn't reach the team repo` / `could not pull terum/team-skills` / `Sync now` for a 404 and for every read failure | Split: the `unknown` case renders `CenteredState` (not `alert`) titled `Not found`, body = the existing `No project named …` sentence, primary `Back to marketplace`; a read failure keeps the drawn board but its body names `catalog?.repository ?? 'the team repo'` instead of the literal `terum/team-skills`. The mock's `?__mock=error` board (fixture repository `terum/team-skills`) must render the same string as today — check `catalogData().repository` |
| M10 | `personStatus` | Unchanged; it now receives a real `placeNote` |
| V13 (note) | PR 92 removed the bulk-remove preset; the bulk-**install** preset `Approve these tools for ${ref}?` is the one this row deletes |

## 3. Tests (all under `desktop/`; declare each changed file in `testsModified`)

- Adapter replay on the recorded frames: `catalog()` yields `repository === 'acme/team'`, `scanned` deep-equals `['~/.claude/skills']` (the `seed` root is `absent`), `verdictCounts['Not evaluated'] === 3` and the other three `0`, `bulkInstall.terum` deep-equals `{total:1, asking:0}`, `projects[0].admin === null`, `projects[0].updated` is a relative string derived from `tdd.updated`, `people.find(mira).publishLine === 'Published deploy-check · <relative>'`, `people.find(ravi).publishLine === 'Published diagnose · <relative>'` (ravi authored `diagnose`), `people[*].role === null`, `organization === null`, `teamsLine === 'On no project yet'`, `mira.placeNote === '1 of 2 on this machine · install places the other 1'`, every card's `installs` matches `/^\d+ installs?$/` and `tdd`'s is `'1 install'`.
- Screen replays: `#/marketplace/people` person cards show `· mira` nowhere (no leading separator), `Published deploy-check`; `#/marketplace/projects` project card shows no description span, no admin foot, no `evaluated` chip; `#/marketplace/projects/terum` rail has no `Evaluated`/`Admin` rows and the Repo link text is `acme/team`; `#/marketplace/projects/nope` renders `Not found` with `No project named nope.`; `#/marketplace/skills?filters=open` shows `Not evaluated 3`.
- Mock parity: `marketplace.test.tsx` keeps passing with only the consent/remove cases updated to answer dialogs; add assertions that a mock person card still prints `founder · ryan` and a mock project card still prints its description and admin foot.
- Unit: the placeNote composer; the relative-time helper against fixed dates.

## 4. Gates

From `desktop/`: `npm run typecheck && npm run lint && npm test`, real counts. Orchestrator: `npm run export:check`, `npx playwright test e2e/routes --workers=2`, `FIDELITY_ALL=1 npx playwright test e2e/fidelity --workers=2` — every locked Marketplace board pixel-identical (four Settings boards drift on main independently).

## 5. Out of scope

M1/M2 semantics (what a person page lists), M4/U10 install destinations and the `workspace` cwd, M19/D9 the filter facets, M24 spawn count, M25 the empty-catalog board, the team slot in the person heading, the project Repo label/href pairing, and every CLI ask (`projects[].description/admin`, `skills[].evaluated`, `member.joined`, a CLI-side `uninstall-skill` confirm). Any change to the mock's data, `GAPS.md`, `FIDELITY.md`, `AGENTS.md`, `README.md`, `package.json`, the generated files or `e2e/fidelity/**`.
