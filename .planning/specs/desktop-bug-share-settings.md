# desktop-bug-share-settings — Members and Settings stop asserting what the CLI never reported

**Status:** locked (Teddy, 2026-09-09: "the mock data should not exist in the real version, it should only be real data; however all of the interaction and fidelity should exist"). Source rows: `~/Projects/SSM/review/2026-09-09-mock-vs-real/sections/share-settings.md` and `verbs-seam.md` (SH1, SH3, SH4, SH5, ST4–ST6, ST8–ST17, ST19, ST21, ST26, V1, V6–V10, V15), copied under `.planning/codex-runs/desktop-bug-share-settings/` by the orchestrator.

**Base:** `origin/main` after PRs 91 (Share ▸ Invite reads Settings, `InviteResult.already/failed`), 95 (Settings ▸ Advanced ▸ Remove is the CLI's ask via `MachineRemovalProvider`; `MachineUninstallResult`; `Backend.quit()`), 92 (`uninstall-skill` asks once with a preview; `UninstallArgs.kind/member/project`; Marketplace bulk Remove is one run whose ask reaches `PromptDialog`), 93 (app version from the root package, `plural()` with an irregular form, missing-data separators: crumbs drop empty/dash segments, identity sub-lines never render a dangling `·`, Sync's quarantine row omits `· —`) and 94 (run.ts cancel race, frame parsing). Read `git log -5` and those diffs before starting; rows they already fixed are marked below and must not be redone. `plural(n, word, words?)` already lives in `desktop/src/screens/marketplace/market-data.ts` (PR 93 extended it with an irregular-plural argument); import it from there. Do NOT create `desktop/src/lib/plural.ts`.

**Sources:** `desktop/AGENTS.md` (invariants 1, 2, 5, 7), `desktop/src/backend/tauri/README.md`, CLI contracts `src/commands/{status,sync,invite,connect,uninstallMachine,update}.ts`, `src/cli.ts` (read only). Recorded frames: `.planning/codex-runs/mock-vs-real-2026-09-09/frames/*.jsonl` (0.1.7). What they carry: `status.jsonl` → `teams[0]` with `members[{handle,displayName,role:null,projects:[]}]`, `clone:{state:'ok',origin}`, `readable:true`, `clonePath:'/Users/teddy/.terum/skills/teams/acme'`, `joinCommand`, `joinBlock` (7 lines), `policy`, `categories`, `syncedAt:null`; `ledger.placements[0]` (`path`, `version` 40 chars, `placed_at:'2026-09-01T00:00:00Z'`), `ledger.shared[0]` (`id` of `tdd`, `source:'/Users/teddy/code/seed/skills/tdd'`), `identity`; `ls-local.jsonl` → the placed row `deploy-check` with `tracked:true`, `health:'both'`, `connected:false`, `shared:[]`, plus `skills[{id,name,grantsHash,grants}]`; `ls.jsonl` → `projects[{name:'terum',…}]`; `update.jsonl` → `running:'0.1.7'`, `latest:'0.1.7'`, `observation:'same'`, `description`, `advice`, `lines`.

**The rule.** The mock and the 91 locked boards are the oracle; every screen edit is pixel-neutral on the mock (the mock's `HOOK`, `STORAGE`, `QUARANTINE`, `SHORTCUTS`, `K`, `CLI_LATEST`, `role` values are all present, so a screen branch on "value is null/absent" never fires there). The real adapter serves only what the frames carry: a plausible constant (`HOOK:{installed:false}`, `cache_n:0`, `invited:[]` presented as `0 invitations`) is a fabrication and must become an honest absence the screen draws as such. No component branches on which backend answers.

## 1. Seam types — `desktop/src/backend/types.ts`

- `Settings.HOOK` becomes `Design['HOOK'] | null` (`null` = unknown). `Settings.STORAGE.cache_n`/`evals_n` become `number | null`; `cache`/`evals`/`quarantine` stay strings (`'—'` when unknown). `Settings.QUARANTINE` stays an array but the adapter serves `null` for "not read" — make it `Design['QUARANTINE'] | null`. `Settings.CLI_LATEST` becomes `string | null`.
- `Member.role` and `Roster['invited']`: `role: string | null`; `Roster.invited` becomes `Design['INVITED'] | null` (`null` = the CLI has no invitation list).
- `SyncResult` becomes `{ placed: number; deferred: string[]; notices: string[]; changed: boolean; teams: { team: string; state: string; message?: string }[] }` (mirror `src/commands/sync.ts` `SyncResult`/`TeamOutcome` field names exactly; read the file). Update the mock's `sync` to return the same shape from its fixture names (`placed: names.length`, `deferred: []`, `notices: []`, `changed: true`, `teams: [{team, state:'synced'}]`) — its existing tests assert `placed` names; rewrite them to the new shape and declare them.
- `InviteResult` — **DONE by PR 91**; leave it.
- `ConnectArgs.keepSource`, `keepRepo`, `relocate`, `forget` become `string | undefined` (skill ids; `relocate` is `id:path` — confirm the CLI's spelling in `src/commands/connect.ts` and `src/cli.ts` and copy it).
- `Result.reason` gains `'unreadable' | 'invalid-config'` (the Settings board branch below).
- `TeamStatus` already has optional `cloneState`/`readable` — no change.

## 2. Adapter — `desktop/src/backend/tauri/index.ts`

| Row | Today | Required |
|---|---|---|
| ST8 | `statusModel`/`settingsModel` copy `clonePath`, `PLACEMENTS[].path`, `SHARED[].source` verbatim | Pass `home` (await `home()` in `readModels`, hand it to both models) and run `clone`, every placement path, every shared source, and `SHARED_SPECIMEN` through `abbreviateHome` |
| ST10 | `teams[]` built without `cloneState`/`readable` | Map `clone` → `cloneState` (`{state:'ok'\|'foreign', origin}`, `{state:'absent'}`, `{state:'incomplete', reason, error?}` — the seam's `CloneState` union; map the CLI's `incomplete.reason` string onto the seam's literal union, defaulting to `'unverifiable'`) and `readable` → `readable` |
| ST9 | `TEAM_POLICY.projects: null` | `settings()` reads `ls --team <team>` (the `cliLs` schema, like `catalogModel`) when exactly one team is configured and fills `projects` with `projects[].name`; on an `ls` failure keep `null` and append the error to the failing result like `readModels` does for `ls --local` |
| ST5 | `PINNED_N = placements.filter(p => p.version !== null).length`; Version column shows the sha for a tracked placement | Pinned = the joined `ls --local` row's `tracked === false`; a placement with no row counts as pinned only if `version !== null`. The Version cell (`PLACEMENTS[][3]`) is `null` (→ the screen prints `tracking`) when the row is tracked, else the 12-char sha |
| ST14 | `SHARED: [id, source, team, '—']`; `LOCAL_UNSHARED: []` | `SHARED` rows are `[name, abbreviated source, team, state]` where `name` comes from `local.skills.find(id)` (fallback: the id), `state` from the `ls --local` row whose `path === source`: `PLACEMENT_STATE[row.health]`, `'—'` when no row. `LOCAL_UNSHARED` = names of `ls --local` rows with `connected !== true && shared.length === 0 && placement === null` (a folder that is yours, not placed by Terum and not connected) — the Sharing card's `Share` button then exists for them |
| ST15 | `SHORTCUTS: []` | Serve the four pairs the app binds (`[['Search','⌘K'],['Settings','⌘,'],['Sync now','⌘R'],['Back · forward','⌘[ · ⌘]']]`) from a constant in `desktop/src/lib/shortcuts.ts`; the mock keeps reading `design.SHORTCUTS` (identical content) |
| ST4 | `STORAGE:{cache:'—',cache_n:0,evals:'—',evals_n:0,quarantine:'—'}` | `cache_n: null`, `evals_n: null` |
| ST1 (adapter half) | `HOOK:{installed:false,file:'',timeout:0}` | `HOOK: null` |
| ST3 (adapter half) | `QUARANTINE: []` | `QUARANTINE: null` |
| ST12 | `CLI_LATEST: '—'` | `CLI_LATEST: null` |
| SH6 | `invited: []` | `invited: null` |
| SH1 | `rosterModel`: `role: member.role ?? ''` | `role: member.role` (null stays null) |
| ~~SH4 / SH5~~ | **DONE by PR 91** (the Invite dialog and Copy join block read `settings().TEAMS[].joinCommand/joinBlock`). Do nothing here; `onboarding()` stays a gap and `surfaces.onboarding` stays false. Original text for context: | Serve `Onboarding` from `status` for the fields the Invite dialog and the Members empty board need: `joinBlock` = `teams[0].joinBlock.join('\n')` (the CLI's exact lines), `team` = the mapped `TeamStatus`, `me` = identity, `teamN` = member count, `INVITEE`/`INVITE_TIP`/`JOIN_BLOCK_NOTE`/`TEAM_REPO`/`ONBOARD_*`/`WELCOME_LINES`/`BASICS_*`/`THEME_OPTIONS`/`LIBRARY_OVERVIEW`/`GLOBAL_SET`/`BOOT_STEPS` are app-owned copy → serve them from `design.json` **only if** a review of `ShareInvite.tsx` and `ShareScreen.tsx` shows the dialog reads nothing but `joinBlock`, `team`, `me`, `teamN`, `INVITEE`, `INVITE_TIP`, `JOIN_BLOCK_NOTE`; if the `Onboarding` DTO forces fixture-only fields (`skill`, `summary`, `arm`, `used_by`, `searchResults`, `bootRows`…), split the DTO: add `Backend.invitation(): Promise<Result<{joinBlock:string; joinCommand:string|null; team:TeamStatus; me:Identity; teamN:number}>>` and make `ShareInvite`/`ShareScreen` read that instead of `onboarding()`; the mock's `invitation()` derives from `onboardingData()`. State which path you took in `deviations`. `surfaces.onboarding` stays `false` |
| ST21 / V6 | `sync` maps `placed: value.deferred.length \|\| value.placed ? [] : []`, `removed: []` | Parse the real shape (`cliSync` widened to `placed`, `deferred`, `notices`, `changed`, `teams`) and map it 1:1 into the new `SyncResult` |
| V7 | **DONE by PR 91** |
| V8 | `connect` argv ignores `keepSource`/`keepRepo`/`relocate`/`forget` | Emit `--keep-source <id>`, `--keep-repo <id>`, `--relocate <spec>`, `--forget <id>` when set (exact flag spellings from `src/cli.ts`) |
| ST26 / V9 | `connect` argv passes `args.path` raw | `await localPath(args.path)` before building argv (the same expansion `revealPath`/`openInEditor` use); `validate --cwd` and `setup --target` do not take home paths from the app, leave them |
| V10 | **DROPPED** — PR 92 locks the opposite contract (a declined frame without a value notifies nothing) with a test in `run.test.ts`. Leave the `onSettled` line byte-identical |
| V15 | `features()` latches `featuresOnce`; `refreshLaunch()` does not clear it | `refreshLaunch()` also resets `featuresOnce = undefined` and `hello = null` when `hello` is still null; add `'features'` and `'capabilities'` to the `config` prefix list in `desktop/src/app/invalidation.ts` |

## 3. Screens

### Share — `desktop/src/screens/share/{ShareScreen,ShareInvite}.tsx`

| Row | Today | Required |
|---|---|---|
| SH1 (screen) | **DONE by PR 93** (`sub()` in `ShareScreen.tsx`). Only make sure `role: string \| null` typechecks through it |
| SH3 | Last-seen header always drawn; cell prints `—` when `lastSeen` is false | Hide the header and the cell (`visibility:'hidden'`, exactly like the Teams column) when `features.lastSeen` is false |
| SH6 | subtitle `${members} members · ${invited.length} invitation(s)` | `plural(members,'member')` + (`invited === null ? '' : \` · ${plural(invited.length,'invitation')}\``); the `empty` predicate treats `invited === null` as no invitations |

### Settings — `desktop/src/screens/settings/{SettingsContent,SettingsDialogs,SettingsScreen,SettingsParts}.tsx`

| Row | Today | Required |
|---|---|---|
| ST16 / V1 | **DONE by PR 95** (`MachineRemovalProvider`, the danger dialog is the CLI's ask, `Backend.quit()`). Do not touch `SettingsDialogs`' remove flow or `uninstallMachine` |
| ST1 (screen half) | Session-hook row reads `d.HOOK.installed` | `HOOK === null` → value `Unknown`, description `terum-skills has not reported whether the session-start hook is installed; check ~/.claude/settings.json.`, and the switch is **not rendered** (hidden, never inert). Non-null keeps today's rendering |
| ST3 (screen half) | Quarantine card, `?dialog=prune`, `Delete ${d.QUARANTINE.length}…` preset | `QUARANTINE === null` → the card shows one line `Quarantine contents are not reported by this terum-skills version.` and no Prune button; `?dialog=prune` renders nothing when null. Delete the count-keyed preset in `SettingsDialogs` (pass `{}` so the CLI's own confirm opens as a dialog with its real count) |
| ST4 | `cache_n` / `evals_n` interpolated as numbers | `null` → `—` in place of the number (e.g. `— versions / —`), no `0` |
| ST5 | Version cell `version ?? 'tracking'` | Unchanged (the adapter now serves `null` for tracked) — verify the Updates section's tracked/pinned counts no longer double-count |
| ST6 | `${d.PLACEMENTS_N} placements`, `${team.members} members` | `plural()` imported from `screens/marketplace/market-data.ts`. `team.members === null` prints `— members` |
| ST9 | Projects row `—` | Renders the served list (already handles the array) |
| ST11 | Updates row renders `report.description` | Compose: `${running ?? '—'} installed · ${observation === 'same' ? 'up to date' : observation === 'newer' && latest ? latest + ' available' : observation === 'older' && latest ? 'newer than the advertised ' + latest : 'latest unknown'}`; keep `description`/`advice` inside the Update-command dialog. Verify the mock (`running 0.1.2`, `latest 0.1.3`, `observation 'newer'`) still prints `0.1.2 installed · 0.1.3 available` |
| ST12 | About: `${d.CLI_LATEST} available` | `CLI_LATEST === null` → the row shows only the installed version and a `Check` hint, no "available" clause |
| ST13 (honest half) | `k = —` sentence; `'—'` a selectable k option; `StatusValue` green check beside `—`; note hard-codes `--k 3 --model sonnet` | `K === null` → the Repetitions row description reads `terum-skills keeps no default k; the CLI decides per run.` and the chooser offers no `—` option (render the chooser only when `K !== null`); the flags note prints `--k ${K} --model sonnet` when `K !== null`, otherwise `The app passes no eval flags; terum-skills uses its own defaults.`; `StatusValue` gains `kind: 'unknown'` (icon `alert`, colour `--tk-text3`) used whenever the value is `—`. Mock output (K = 3, AGENT_CLI set) is unchanged |
| ST15 | Keyboard card empty | Renders the served list (already handles the array) |
| ST17 | Placed / Tool approvals / Quarantine / Shared cards render nothing when empty | One drawn empty line per collection: `Nothing placed on this machine yet.`, `No tool approvals recorded.`, `Quarantine is empty.` (only when `QUARANTINE` is `[]`, not null), `Nothing shared from this machine yet.`; the approvals fallback branches on `APPROVALS.length === 0`, not on `!status.ledger` |
| ST19 | Settings error board asserts `config.json … is not valid JSON` for every failure | Branch on `query.data.reason`: `'invalid-config'` → today's copy; otherwise title `Couldn't read your settings`, body `terum-skills could not read this machine's configuration. Check ~/.terum/skills is readable, then try again.`; the adapter sets `reason:'invalid-config'` when the status error contains `Invalid` and `config.json`, else `'unreadable'`. The mock's `?__mock=error` error string contains both words, so its board is unchanged |
| V6 (screen) | `useSyncAction` popup shows `Sync finished.` | Also render: `${placed} placed`, deferred names as `Waiting for tool review: a, b`, each notice line, and each `teams[]` entry with a non-`synced` state as `${team}: ${state}${message ? ' · ' + message : ''}` |

## 4. Tests (all under `desktop/`; declare each changed file in `testsModified`)

- Adapter replays on the recorded frames: `settings()` → `PLACEMENTS[0][0] === '~/.claude/skills/deploy-check'`, `PLACEMENTS[0][3] === null` (tracked), `PINNED_N === 0`, `SHARED[0]` deep-equals `['tdd','~/code/seed/skills/tdd','acme','—']`, `LOCAL_UNSHARED` deep-equals `[]` for the recorded local frame and `['scratch']` when the test mutates the frame to add an unplaced, unconnected row named `scratch`, `SHORTCUTS.length === 4`, `HOOK === null`, `QUARANTINE === null`, `STORAGE.cache_n === null`, `CLI_LATEST === null`, `TEAM_POLICY.projects` deep-equals `['terum']`; `status()` → `teams[0].clone === '~/.terum/skills/teams/acme'`, `teams[0].cloneState` deep-equals `{state:'ok', origin:'github.com/acme/team'}`, `readable === true`; `roster()` → every `role === null`, `invited === null`; `sync` mapping on a scripted success frame `{placed:2,deferred:['x'],notices:['n'],changed:true,hook:false,teams:[{team:'acme',state:'skipped',message:'m'}]}` round-trips; `connect({keepSource:'id1'})` spawns `--keep-source id1`; `connect({path:'~/.claude/skills/x'})` spawns `/Users/teddy/.claude/skills/x`; `refreshLaunch()` after a NO_STATE first read lets a later `features()` see the hello frame.
- Screen replays: `#/share` rows show `mira` with no trailing `·`, no `Last seen` header text visible, subtitle `3 members`; `#/settings/sync` shows `Unknown` and no switch; `#/settings/machine` shows the quarantine placeholder and `tracking` in the Version cell; `#/settings/updates` shows `0.1.7 installed · up to date`; `#/settings/about` has no `available` text; `#/settings/evals` has no `—` option and no `k = —`; `#/settings/sharing` shows `tdd` (not the UUID), `~/code/seed/skills/tdd`; a scripted status error without `config.json` renders the generic settings error body.
- Mock parity: `settings.test.tsx`, `share.test.tsx` keep passing with only the dialog-answer updates declared; add assertions that the mock still prints `Installed in ~/.claude/settings.json`, `2 folders · 60 KB`, `k = 3`, `0.1.3 available`, `founder` role suffixes.
- Unit: `StatusValue kind='unknown'`.

## 5. Gates

From `desktop/`: `npm run typecheck && npm run lint && npm test`, real counts. Orchestrator: `npm run export:check`, `npx playwright test e2e/routes --workers=2`, `FIDELITY_ALL=1 npx playwright test e2e/fidelity --workers=2`; every locked Share/Settings board pixel-identical except the four already drifting on main (SettingsTeams, SettingsSync, SettingsAdvanced, SettingsLeave).

## 6. Out of scope

SH2 (what the Status cell and your own row show with `roles` off), ST22 (date format), ST13's k/model/judge choosers' existence, ST7 hostname, ST1/ST2 hook probe and verb, ST3 quarantine listing, SH6 pending invitations, ST4 storage sizes, every other CLI ask; the Marketplace and Skill screens; `library()`; the mock's data; `GAPS.md`, `FIDELITY.md`, `AGENTS.md`, `README.md`, `package.json`, generated files, `e2e/fidelity/**`.
