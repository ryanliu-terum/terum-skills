# codex-spec review: 2026-09-04-eval-integration.md

Findings were raised by **OpenAI Codex** (gpt-5.6-sol) and verified by a 2-vote Claude adversarial panel.

7 confirmed, 3 contested, 10 dropped, 9 unverified.

## Verdict

This spec was audited in mirror-image mode: the **finders ran on OpenAI Codex** (gpt-5.6-sol, effort high) and the **adversarial verify panel ran on Claude** — the opposite of a standard `/ultraspec` run, so a reader comparing the two should expect different blind spots and a different hit rate, not a strict superset. Of 29 raised findings, 7 survived a 2-0 panel vote; merging two that describe the same IE4 defect leaves **6 distinct confirmed items — 4 BLOCKER, 2 DRIFT**. The plan is not build-ready as written. The dominant theme is the hygiene tier: IE1 explicitly ships the `eval` verb *before* the fail-closed hygiene gate exists ("skip-with-notice"), IE3 wires hygiene only into `share` while the authoritative eval spec requires `validate`, `publish` and CI, and the automatic sync path mutates shared skill content with no hygiene call at all — three independent routes by which unscanned teammate content reaches a Bash-enabled arm or the team repo. The second theme is IE4: the interim CI invocation (`npm ci` + `node dist/index.js`) cannot produce the binary it runs in either candidate checkout, and the plan names no migration for already-scaffolded team workflows that the guard currently refuses to write. The two DRIFT items are cheaper: the README eval column is left permanently `—` because no README-generator work is scheduled, and §0/§2 still pin eval-engine "rev 5" and carry VE1 as open when the sibling records VE1 closed at rev 6. Recommended sequencing fix: make IE3 a hard prerequisite of IE1, rebase §0/§2 onto the current eval-spec revision, and resolve IE4's artifact path before handing this to `/codex-implement`.

## Severity counts (confirmed only)

| Severity | Count |
|---|---|
| BLOCKER | 4 |
| DRIFT | 2 |
| AMBIGUITY | 0 |
| GAP | 0 |
| NOTE | 0 |
| **Total** | **6** |

Counts exclude the contested and unverified sections below. The 7 confirmed findings collapse to 6 items because `findings[19]` (internal quality) and `findings[26]` (build-readiness) were merged — see the merged entry under Build-readiness.

---

## Cross-spec drift

- [x] *(applied, plan rev 2 — eval side builds `validate` per Ajay)* **BLOCKER — MISSING REFERENCE: IE3 omits required `validate` and CI hygiene wiring** `findings[13]` (vote 2-0; verifier suggests severity GAP)
  - **Location:** Target §0 lines 21–22 and §1, IE3, lines 58–64
  - **Evidence:** Target lines 21–22 enumerate only “Hygiene wired into `share` (and later `publish`)”, and IE3 lines 60–61 say “Wire into `share` before `safeWrite`”. Sibling §6 lines 258–259 says “`validate <path|name>` gains the hygiene tier ... `share` and `publish` invoke it by construction”; sibling §9 lines 368–369 requires “`hygiene.ts`, run by `validate`, `share`, `publish`, and CI.”
  - **Suggested fix:** Add `validate` and CI to IE3’s wiring and acceptance tests, with `share` and `publish` invoking that same validation path.

- [x] *(applied, plan rev 2)* **DRIFT — MISSING REFERENCE: README eval column remains permanently blank after receipt integration** `findings[5]` (vote 2-0; verifier concurs DRIFT)
  - **Location:** Target §1 IE2, lines 45–54 and §3, lines 103–107; sibling §9, line 397
  - **Evidence:** Target commits validated receipts and freezes their lookup rule: `evals/<id>/<hash>/<run>.json`, with the newest receipt selected by lexicographically last filename. Sibling requires an “eval column (`—` until phase 3).” The target’s exhaustive wiring list contains no README-generator work, so the phase-3 integration leaves that column at `—` despite committed results.
  - **Suggested fix:** Add receipt lookup and eval-column rendering to the integration plan and the sibling README contract, with tests for no receipt and newest-receipt selection.

## Spec-vs-code reality

- [x] *(applied, plan rev 2)* **DRIFT — Integration plan still treats VE1 as open and pins the superseded eval spec revision** `findings[16]` (vote 2-0)
  - **Location:** §0 lines 7–8; §2 lines 88–90
  - **Evidence:** The target says the authoritative design is `2026-09-04-eval-engine.md` “(rev 5)” and that “VE1/VE2 … closure not recorded.” The referenced spec is now rev 6 and explicitly records “VE1 closed” (`.planning/specs/2026-09-04-eval-engine.md:3`, `:428-435`). The implementation also contains the resulting pinned-CC behavior: it documents CC 2.1.236 and checks skill membership rather than list equality (`src/lib/evals/execution.ts:214-221`). VE2 remains open in the eval spec at lines 434–435.
  - **Suggested fix:** Update the integration plan’s authoritative revision reference and carry only VE2 as outstanding; do not require VE1 to be re-closed before IE1.

## Internal quality

No standalone confirmed items — the one confirmed internal-quality finding (`findings[19]`) was merged into the IE4 build-readiness entry below.

## Build-readiness

- [x] *(applied, plan rev 2 — hygiene milestone now leads; no skip state)* **BLOCKER — IE1 deliberately bypasses the mandatory fail-closed hygiene gate** `findings[21]` (vote 2-0; verifier suggests severity DRIFT)
  - **Location:** `.planning/specs/2026-09-04-eval-integration.md:31-33`
  - **Evidence:** IE1 says “hygiene (once IE3 lands; skip-with-notice before that) → preflight”. The authoritative eval spec requires the opposite: “hygiene tier runs first and hard-stops on failure” at `.planning/specs/2026-09-04-eval-engine.md:245-248`, and calls every hygiene check “fail-closed” at lines 368-369. Following IE1 would execute unchecked teammate content with Bash-enabled arms.
  - **Suggested fix:** Make IE3 a prerequisite of IE1, or keep the eval command unavailable until hygiene exists. Remove the skip-with-notice state and test that no preflight or agent process starts after a hygiene failure.

- [x] *(applied, plan rev 2 — hygiene gates every skill-content mutation incl. sync reconcile)* **BLOCKER — Automatic sync updates bypass the secret/PII hygiene boundary** `findings[23]` (vote 2-0; verifier suggests severity GAP)
  - **Location:** `.planning/specs/2026-09-04-eval-integration.md:60-64`
  - **Evidence:** IE3 says only “Wire into `share` before `safeWrite` (and later `publish`)”. Phase 1 defines `share` as a one-time operation after which edits flow automatically through sync (`.planning/specs/2026-09-02-phase-1-build.md:12`). That path currently mirrors edited source files through `safeWrite` with action `sync` at `src/commands/share.ts:119-120`, with no hygiene call. A secret added after initial sharing would therefore be committed while an implementation followed IE3 exactly.
  - **Suggested fix:** Define hygiene as a prerequisite for every skill-content mutation, including `reconcileShared`/sync and divergence resolution. Specify failure behavior so the repository and stored baseline remain unchanged, and add a regression test with a secret introduced after initial share.

- [x] *(applied, plan rev 2 — pinned dual checkout + build; `team workflow-update` admin PR for existing repos)* **BLOCKER — IE4 has no executable artifact path and no deployment path for existing team workflows** `findings[26]` + `findings[19]` — **merged** (both 2-0; verifier suggests severity GAP for `findings[19]` and `findings[26]`). *Merge note: `findings[19]` was raised on the internal-quality dimension and `findings[26]` on build-readiness; both identify the same defect — the interim CI step specifies a checkout/install/run sequence that cannot produce `dist/index.js` — with `findings[26]` additionally covering the missing migration for already-scaffolded team repos. Evidence from both is reproduced below.*
  - **Location:** `.planning/specs/2026-09-04-eval-integration.md:68-80` (and §1, IE4, line 77)
  - **Evidence (`findings[26]`):** The interim step says the team-repo job “Runs via checkout + `npm ci` + `node dist/index.js` until npm publish lands.” A generated team repo contains `team.json`, people, empty skills/evals, README, and the workflow—not this product's `package.json`, lockfile, source, or `dist` (`src/commands/team.ts:376-387`)—so a default checkout followed by `npm ci` cannot run. The workflow template itself says it requires published npm artifacts (`src/commands/team.ts:398-415`). Editing that template affects only newly scaffolded teams; the plan names no migration for already-created team repos, while the guard currently refuses `.github/workflows/**` writes (`src/lib/guard.ts:39-45`).
  - **Evidence (`findings[19]`):** Line 77 specifies “checkout + `npm ci` + `node dist/index.js` until npm publish lands.” The scaffolded workflow checks out the team repository (`src/commands/team.ts:431`), which has no CLI package to install or build. Even if the intended checkout is the terum-skills source repository, `dist/` is not tracked (`git ls-files dist` returned no files), and `package.json:20–21` builds it only through `npm run build`/`prepack`; `npm ci` alone does not create `dist/index.js`. Thus the interim job fails before evaluation.
  - **Suggested fix (`findings[26]`):** Specify an exact workflow: either check out this product repository at a pinned revision into a second path, install/build there, and invoke it against the team checkout, or gate IE4 on npm publication. Also define an authorized, reviewed migration mechanism for existing team workflows and its guard implications.
  - **Suggested fix (`findings[19]`):** Specify a second checkout of the terum-skills source at a pinned revision, run `npm ci` and `npm run build`, and invoke that checkout’s `dist/index.js`; otherwise keep CI gated on the published package.

---

## Contested (split verdict — needs human adjudication)

Panel split, one vote from flipping. Not counted above.

- **`findings[7]` (BLOCKER / cross-spec drift) — STALE ECHO — receipt flow reads a retired token field**
  - **Location:** Target §1 IE2; sibling §5.4
  - **Evidence:** Target `.planning/specs/2026-09-04-eval-integration.md:51-54` says `buildReceipt` receives ``config.teams[*].token`` secrets and tests that a team token is absent from committed JSON. Sibling `.planning/specs/2026-09-02-phase-1-build.rev9-draft.md:232` says: “The tool never prompts for, stores, probes, or passes a token, and `teams.<team>.token` is gone from the schema.” Implementing the target literally accesses a removed field and violates the rev-9 credential contract.
  - **Suggestion:** Remove `config.teams[*].token` from the integration plan and its receipt test. Define redaction inputs only from secret sources that still exist under rev 9.

- **`findings[22]` (BLOCKER / build-readiness) — The security-sensitive hygiene predicates are left to the implementer**
  - **Location:** `.planning/specs/2026-09-04-eval-integration.md:58-66`
  - **Evidence:** The step specifies only “unicode trickery, secret/PII scan (share patterns with `receipt.redact`'s list), bundled-script scan, license reconciliation … size cap.” The authoritative list still does not define the mixed-script/confusable algorithm, what counts as a text file, the script-extension allowlist, executable/shebang handling, email exception matching, license normalization, or exact token cap. Moreover, `CREDENTIAL_PATTERNS` is private in `src/lib/evals/receipt.ts:15-22`, so the instructed pattern sharing has no named reusable API. These choices determine whether secrets and dangerous bundled content pass a fail-closed gate.
  - **Suggestion:** Specify a pure exported API such as `scanHygiene(input): HygieneFinding[]`, export or centralize the credential matchers, and freeze the scanned paths, binary/text rule, Unicode tables, script allowlist, license comparison, finding codes, line-number behavior, and exact size calculation. Add adversarial fixtures for every boundary.

- **`findings[3]` (DRIFT / cross-spec drift) — MISSING REFERENCE — integration plan omits setup’s required phase-3 eval action**
  - **Location:** Target §0, lines 14–24 and §1 IE1, lines 29–38; sibling §6.1 line 341 and §13 default 41, line 502
  - **Evidence:** Target calls its table the missing wiring “in dependency order” and includes `commands/eval.ts` plus CLI registration, but contains no `setup` work (`rg -n -i 'setup'` returned no target matches). Sibling says “`eval` is not mentioned until phase 3 ships” and explicitly requires: “phase 3 adds an eval action to step 4.” Following IE1–IE4 therefore ships eval without activating the reserved onboarding action.
  - **Suggestion:** Add setup step-4 wiring and acceptance coverage to the integration sequence, coordinated with the still-unbuilt setup verb.

## Unverified (not adversarially checked)

Beyond the verify cap — raised by Codex, never put to the Claude panel. Not counted above; treat as unvalidated leads.

- **`findings[9]` (DRIFT / cross-spec drift) — STALE ECHO — CLI registration ownership differs between specs**
  - **Location:** Target §0/§1 IE1; sibling §3
  - **Evidence:** Target `.planning/specs/2026-09-04-eval-integration.md:18` calls for “`commands/eval.ts` + `cli.ts` registration,” and lines 37-38 say it is registered in `cli.ts`. Sibling `.planning/specs/2026-09-02-phase-1-build.rev9-draft.md:28-29` assigns commander wiring to `index.ts`; line 51 says “`index.ts` does nothing but map commander to” verb functions. These contracts direct the command registration into different modules.
  - **Suggestion:** Document the landed split consistently—likely `index.ts` as the executable shim and `cli.ts` as command registration—or change the target to register in `index.ts`.

- **`findings[10]` (DRIFT / cross-spec drift) — MISSING REFERENCE — generated team workflow remains README-only**
  - **Location:** Target §1 IE4; sibling §4.1 and §9
  - **Evidence:** Target `.planning/specs/2026-09-04-eval-integration.md:74-80` adds an eval job to PRs touching `skills/**`, with neutral skip when `ANTHROPIC_API_KEY` is absent and a later blocking mode on `publish/<name>`. Sibling `.planning/specs/2026-09-02-phase-1-build.rev9-draft.md:71-73` defines the workflow generated by `team create` only as regenerating README and commenting on publish PRs; lines 399-401 likewise specify only README regeneration/commenting. New team repositories built from the sibling would therefore receive a stale workflow contract.
  - **Suggestion:** Extend the sibling’s scaffold and Action sections with the interim report-only job, secret-absent behavior, and final publish-branch gate, including how existing repositories receive the update.

- **`findings[11]` (DRIFT / cross-spec drift) — MISSING REFERENCE — publish has no hygiene precondition**
  - **Location:** Target §0/§1 IE3; sibling §6 publish
  - **Evidence:** Target `.planning/specs/2026-09-04-eval-integration.md:21-22` explicitly requires hygiene to be wired into `share` “and later `publish`”; lines 58-61 define checks for Unicode trickery, secrets/PII, bundled scripts, license mismatch, and size. Sibling `.planning/specs/2026-09-02-phase-1-build.rev9-draft.md:305` defines the complete `publish` flow as adding an endorsement and running the PR/push policy, with no hygiene or validation gate. A case-insensitive search for `hygiene` in the sibling returns no match.
  - **Suggestion:** Add a publish-time hygiene check before `safeWrite`, so skills shared before IE3 or changed by later reconciliation cannot be endorsed without validation.

- **`findings[15]` (DRIFT / cross-spec drift) — STALE ECHO — VE1 is still listed as open after the engine spec closed it**
  - **Location:** Target §0 line 25 and §2 lines 88–90
  - **Evidence:** Target lines 88–90 says “VE1/VE2 ... closure not recorded — close and stamp in the eval spec before calling IE1 done.” Sibling §13 lines 428–433 says “VE1 (CLOSED 2026-09-04, CC 2.1.236)” and records the contamination findings. The target also names the authoritative sibling as “rev 5” at line 7, while the sibling status line records later rev-6/rev-7 changes.
  - **Suggestion:** Remove VE1 from the outstanding verification debt, retain VE2, and update the target’s sibling-revision reference.
  - *Overlaps confirmed `findings[16]`; not merged because it was never verified.*

- **`findings[28]` (DRIFT / build-readiness) — The integration plan pins a stale eval revision and treats a changed provenance contract as frozen**
  - **Location:** `.planning/specs/2026-09-04-eval-integration.md:7-10,88-90,103-106`
  - **Evidence:** The plan calls eval-engine “rev 5” authoritative, says VE1 still needs closure, and calls the rev-5 receipt contract frozen. The current authoritative file declares rev 6 and enumerates rev-7 requirements, including `model_id` snapshots, at `.planning/specs/2026-09-04-eval-engine.md:3`; it records VE1 as closed at lines 428-433. Landed execution samples already carry `model_id` (`src/lib/evals/execution.ts:146-160`), while `receiptSchema` provenance does not (`src/lib/evals/receipt.ts:71-82`). An IE2 implementer cannot tell whether to preserve rev 5 or propagate the newer comparability field.
  - **Suggestion:** Rebase this plan onto one explicit reviewed eval-spec revision, remove the obsolete VE1 debt, and decide whether per-arm resolved model IDs belong in the receipt schema before declaring that schema frozen.

- **`findings[20]` (GAP / internal quality) — Required hygiene consumers are omitted or left without an owner**
  - **Location:** §0 item 5 and §1 IE3, lines 21 and 56–66
  - **Evidence:** The inventory says hygiene is wired into “`share` (and later `publish`)” (line 21), but IE3 only says “Wire into `share` before `safeWrite`” (line 61). The ownership section assigns IE1–IE3 to the eval side and `publish` to Ryan (lines 99–102), without assigning that later wiring. The authoritative eval spec additionally requires `validate <path|name>` to gain hygiene (`.planning/specs/2026-09-04-eval-engine.md:258`) and says hygiene runs through `validate`, `share`, `publish`, and CI (line 368); this plan never schedules the `validate` surface. Searches of `src/cli.ts` and `src/commands/` also confirm no existing `validate` command that could already satisfy it.
  - **Suggestion:** Add `validate` creation/registration to IE3 and explicitly make hygiene integration an acceptance condition of Ryan’s `publish` implementation and the CI job.

- **`findings[24]` (GAP / build-readiness) — IE1 names an incumbent materializer but not an incumbent-selection algorithm**
  - **Location:** `.planning/specs/2026-09-04-eval-integration.md:35-36`
  - **Evidence:** The step says “incumbent tree materialized via `lib/version.ts`'s archive path,” but materialization starts only after a tree hash has been chosen. The authoritative spec defines the incumbent as “the skill's tree at the last version having a committed receipt, else at `origin/main`'s prior state” (`.planning/specs/2026-09-04-eval-engine.md:277-282`) without defining how “last” or “prior” is selected. `src/lib/version.ts:8-59` exports only `resolveVersion` and `materializeVersion`; neither searches receipts or history.
  - **Suggestion:** Name and specify an incumbent resolver: valid receipt enumeration, deterministic ordering across versions, exclusion of the candidate hash, malformed-receipt behavior, and exact fallback semantics for merge commits, renames, new skills, and deleted history. Name its collocated tests before IE1 implementation.

- **`findings[25]` (GAP / build-readiness) — The required `validate` product surface is absent from the integration plan**
  - **Location:** `.planning/specs/2026-09-04-eval-integration.md:16-24,56-66`
  - **Evidence:** The missing-pieces table and IE3 include `hygiene.ts` and `share` wiring but no validate command. The authoritative eval spec requires “`validate <path|name>` gains the hygiene tier” at `.planning/specs/2026-09-04-eval-engine.md:258-259`. Searches for `command('validate`, `commands/validate`, `validate <path|name>`, and `hygiene` across `src/` found no command or CLI registration; `src/cli.ts` contains no validate verb.
  - **Suggestion:** Add `src/commands/validate.ts`, its argument/result contract, `src/cli.ts` registration, injectable verb entry, output and exit-code rules, and `src/commands/__tests__/validate.test.ts` to IE3—or explicitly remove the surface from the authoritative eval spec.

- **`findings[27]` (GAP / build-readiness) — New orchestration and workflow surfaces lack the mandated collocated Vitest strategy**
  - **Location:** `.planning/specs/2026-09-04-eval-integration.md:40-43,64-66,74-80,99-100`
  - **Evidence:** IE1 exits on “a real … run … on one laptop,” IE3 says only “hygiene suite green,” IE4 gives no automated workflow tests, and §3 merely requires each PR's generic `lint`+`typecheck`+`test` gates to be green. The repository mandate requires collocated Vitest suites, bare-repo fixtures, no network, and adversarial inputs (`.planning/specs/2026-09-02-phase-1-build.md:423-443`). The plan does not name tests for eval orchestration, CLI flag mapping, hygiene integration, or the generated workflow.
  - **Suggestion:** Name at least `src/commands/__tests__/eval.test.ts`, `src/lib/evals/__tests__/hygiene.test.ts`, updates to `src/__tests__/cli.test.ts`, `src/lib/__tests__/guard.test.ts`, and `src/commands/__tests__/create.test.ts`. Define adversarial cases for mutually exclusive modes, `--working --commit`, multi-team resolution, missing/malformed cases, fetch failure, no clone mutation, safeWrite replay, sync hygiene, changed-skill CI detection, absent secrets, and report/block exit codes.

## Triage — 7 confirmed → 0 mechanical · 5 clear · 2 fork · 0 declined

Buckets are derived in code from each finding's ratings (single-fix gate: trivial + low risk + isolated + diff = mechanical; a rule stated in more than one place is never isolated). NOTHING here has been applied to the spec.

### Mechanical — one resolution, trivial / low-risk / stated once, diff supplied; batch-apply on ONE confirmation (0)

_none_

### Clear — one resolution clearly wins, but text must be authored or sections move together; confirm each (5)

- **BLOCKER — The proposed pre-publish CI invocation cannot produce the executable it runs** — findings[19] · /Users/ajaywadhwani/terum-skills/.planning/specs/2026-09-04-eval-integration.md §1 "IE4 — CI, gated on Ryan's `publish` + M4 npm", interim bullet, line 77 (with the dependency claim at §0 table row 7, line 24, and the sibling statement at 2026-09-04-eval-engine.md §11, lines 395-402)
  - Root cause: §1 IE4, "Interim (now)" bullet, line 77 of /Users/ajaywadhwani/terum-skills/.planning/specs/2026-09-04-eval-integration.md: "Runs via checkout + `npm ci` + `node dist/index.js` until npm publish lands."

The sentence is wrong on two independent counts and ambiguous on a third.

(1) Wrong repository. The job it describes is a job in the *team skills* repo — it triggers "on PRs touching `skills/**`" and posts "alongside the existing `readme --pr-comment` publish preview", i.e. it is added to the workflow the CLI scaffolds at src/commands/team.ts:400-447. That checkout (`actions/checkout@v4`, line ~429) is the team repo, which contains skills and no CLI package at all; there is nothing for `npm ci` to install and no `dist/` to run. Every existing step in that scaffold gets the CLI the other way — `npx -y terum-skills@latest readme …` — which is precisely the npm path IE4 says is not available yet.

(2) Wrong build steps even for the right repository. If the intended checkout is the terum-skills source repo, `dist/` is gitignored and untracked (`git ls-files dist` → nothing), and package.json exposes only `"build": "tsc -p tsconfig.build.json"` and `"prepack": "npm run build"` (package.json:19-21). `npm ci` runs neither — `prepack` fires on `npm pack`/`publish`, not on install — so `node dist/index.js` resolves to a missing file. The line omits the `npm run build` step entirely.

(3) Ambiguous, and in tension with the spec's own dependency table. §0 row 7 (line 24) records the CI eval job as depending on "npm publish (register §E, M4)", while line 74 asserts an interim job that runs "now" without it; line 77 is the only text that would explain how, and it does not name which repository is checked out, at what revision, or how that revision is pinned/refreshed. Eval spec §11 (/Users/ajaywadhwani/terum-skills/.planning/specs/2026-09-04-eval-engine.md:395-402) states the same job as `terum-skills eval <name> --k 3 --commit`, i.e. a CLI already on PATH from npm — so no location in either spec describes a runnable pre-npm invocation.
  - Rated: moderate / medium risk / pattern — also stated at: The "how CI gets and runs the CLI" rule is stated in four places that must move together:
1. /Users/ajaywadhwani/terum-skills/.planning/specs/2026-09-04-eval-integration.md:77 — the defective interim invocation ("checkout + `npm ci` + `node dist/index.js`"). Primary edit.
2. /Users/ajaywadhwani/terum-skills/.planning/specs/2026-09-04-eval-integration.md:24 — §0 table row 7 lists the CI eval job as depending on "npm publish (register §E, M4)", which contradicts an interim job that runs now; must be narrowed to the blocking/final gate.
3. /Users/ajaywadhwani/terum-skills/.planning/specs/2026-09-04-eval-engine.md:395-402 — §11 states the same job as `terum-skills eval <name> --k 3 --commit` (bare CLI on PATH = published npm package, and `--commit`, not report-only); it is the authoritative design doc per integration spec line 7 and must acknowledge the interim invocation and the report-only interim posture.
4. /Users/ajaywadhwani/terum-skills/src/commands/team.ts:400-447 — the scaffolded workflow the job is actually added to; every existing step uses `npx -y terum-skills@latest`, and its single `actions/checkout@v4` is the team repo. Not a spec edit, but it is the artifact the new text must be written against, and it is why a same-repo `npm ci` cannot work.
Related convention anchor (read, do not edit): /Users/ajaywadhwani/terum-skills/.planning/specs/2026-09-02-phase-1-build.md:24 — "every printed command uses `npx -y terum-skills@latest …`"; and .planning/reviews/DEFERRED-2026-09-04-phase1-landing.md:31, which defers all of M4 including npm publish and confirms no deferral covers this defect.
  - Why one resolution: The spec has already made the product call: line 74 says the eval CI job runs "Interim (now)", report-only, before npm publish. The defect is purely that the one line explaining *how* the CI machine gets a working CLI is unbuildable — it checks out the wrong repo and skips the build step. Option 1 keeps the decision the spec made and just writes down a recipe that actually works, at the cost of one paragraph. Option 2 would quietly reverse that decision (no eval signal on skill PRs until the package is on npm), which is a bigger change for no benefit unless someone objects to building the CLI inside every team's workflow. So: fix the recipe, don't cancel the interim.
  - **Option 1: Spell out the two-checkout, build-then-run recipe (recommended)** — Depth 3/4 · Cost 1/4. Replace the single sentence at §1 IE4 line 77 with a concrete recipe (3-5 lines): the job keeps the team-repo checkout for `skills/**`, and adds a *second* `actions/checkout@v4` of the terum-skills source at a pinned revision into a side path (e.g. `repository: <org>/terum-skills`, `ref: <pinned sha>`, `path: .terum-cli`), then `npm ci --prefix .terum-cli` + `npm run build --prefix .terum-cli`, and invokes `node .terum-cli/dist/index.js eval <skill> --k 3` — naming `npm run build` explicitly, since `dist/` is untracked and `prepack` does not fire on install. Add one clause saying who bumps the pinned sha and that the whole interim block is deleted when M4 npm publish lands (the job reverts to `npx -y terum-skills@latest`, matching every other step in the scaffolded workflow at src/commands/team.ts:400-447). Then sweep the two places that restate the rule: §0 row 7 (line 24) — narrow "Depends on … npm publish (register §E, M4)" to the *blocking/final* gate only, so the table stops contradicting the interim; and eval spec §11 (2026-09-04-eval-engine.md:395-402) — add a sentence that until npm publish lands the job uses the pinned-source-build invocation and is report-only (§11 currently says `--commit`, which the integration spec's interim contradicts). *Wins if:* the team wants eval feedback on skill PRs before M4 npm publish, which is exactly what §1 IE4 line 74 already commits to ("Interim (now)").
  - Option 2: Delete the interim; keep CI wholly gated on M4 npm publish — Depth 4/4 · Cost 2/4. Drop the "Interim (now)" bullet at §1 IE4 lines 74-77 and keep only the "Final" bullet: the eval job is added to the scaffolded workflow when npm publish lands, invoked as `npx -y terum-skills@latest eval <skill> --k 3` like every other step in that workflow (phase-1 spec line 24 makes `npx -y terum-skills@latest …` the invocation convention). §0 row 7 (line 24) then needs no change — it already lists npm publish as a dependency. §1 line 82-84 ("nothing in IE1–IE3 blocks on him; IE4's blocking gate does") tightens to: all of IE4 blocks on Ryan's `publish` + M4. §3 line 101 stays as written. Eval spec §11 needs no change. *Wins if:* maintaining a hand-pinned CLI revision plus a per-PR TypeScript build inside every team's scaffolded workflow is judged not worth a few weeks of report-only eval output — i.e. the team would rather ship one invocation path (npx) than two.
- **BLOCKER — IE1 deliberately bypasses the mandatory fail-closed hygiene gate** — findings[21] · /Users/ajaywadhwani/terum-skills/.planning/specs/2026-09-04-eval-integration.md:32 (§1 IE1 pipeline clause) — structurally caused by the §0 gap table at lines 14-24 ("in dependency order", row 1 before row 4, row 1's Depends-on cell omitting hygiene) and left unclosed by IE3 at lines 61-64, which wires hygiene into `share` only
  - Root cause: §1 IE1 (line 32) writes the eval command's pipeline as "resolve skill … → hygiene (once IE3 lands; skip-with-notice before that) → preflight (agent.preflight …) → trigger evals … → three-arm execution", i.e. it authorizes shipping and running `eval` with the hygiene tier absent. The authoritative sibling spec this document itself defers to (lines 7-8: "Authoritative designs stay in 2026-09-04-eval-engine.md (rev 5)") states the opposite twice: §6.0 line 247 — "hygiene tier runs first and hard-stops on failure; then preflight (§7.4)" — and §9 lines 368-369 — "Free, no LLM, exit-code gated. Checks, all fail-closed". A skipped fail-closed gate is not a degraded gate, it is no gate: with hygiene skipped, `eval` fetches a teammate's team-repo skill and hands it to Bash-enabled agent arms with no unicode-trickery, secret/PII, bundled-script, or license check having run, which is precisely the threat the tier exists for.

The wording is wrong in three linked places, not one:
1. line 32 — the "skip-with-notice" clause itself (the defect the panel confirmed).
2. §0, lines 14-24 — the gap table is introduced as "Missing, in dependency order" yet lists row 1 `commands/eval.ts` before row 4 `lib/evals/hygiene.ts`, and row 1's "Depends on" cell names only "store/catalog resolution (exists: lib/skills.ts, lib/version.ts)" — it omits hygiene. The table is therefore the structural source of the bad ordering, and it also contradicts the sibling spec's own build order (eval-engine §14 ME1 puts `hygiene.ts` in the pure core, before ME4's `commands/eval.ts`).
3. §1 IE3, lines 61-64 — IE3 builds `lib/evals/hygiene.ts` and wires it "into `share` before `safeWrite`" only; §0 row 5 likewise says "Hygiene wired into `share` (and later `publish`)". Nothing in IE1-IE4 ever wires hygiene into the `eval` command. So the "once IE3 lands" promise in line 32 is unbacked: on the plan as written, `eval` never regains the §6.0 hard-stop even after IE3 ships. (Note the sibling spec is itself thin here: §9 line 368 lists the runners as "validate, share, publish, and CI" and omits `eval`, while §6.0 line 247 requires it — the integration spec appears to have inherited that omission.)

Nothing in the plan states an exit criterion that no preflight/agent subprocess starts after a hygiene failure; IE1's exit (lines 42-43) is only "a real two-arm + triggers run … run tree inspectable".

No recorded deferral covers this: there is no PRODUCT-CONCERNS.md, `.planning/decisions/` holds only two decision-walk files (2026-09-01-team-skill-sharing, 2026-09-04-m1-hardening), there is no `.planning/debug/`, and `.planning/reviews/DEFERRED-2026-09-04-phase1-landing.md` contains no "hygiene" or "eval" entry. Code confirms the gap is live: `src/lib/evals/` has agent/checks/execution/judge/receipt/results/stats/triggers and no `hygiene.ts`, and `src/commands/` has no `eval.ts`.
  - Rated: moderate / medium risk / pattern — also stated at: The ordering rule is stated or implied in five places that must move together (sweep worklist; all paths absolute):

1. /Users/ajaywadhwani/terum-skills/.planning/specs/2026-09-04-eval-integration.md:32 — §1 IE1 pipeline: "hygiene (once IE3 lands; skip-with-notice before that) → preflight". The confirmed defect.
2. …/2026-09-04-eval-integration.md:14-24 — §0 gap table, header "Missing, in dependency order": row 1 `commands/eval.ts` + `cli.ts` registration, row 4 `lib/evals/hygiene.ts` (the one unbuilt engine module) — hygiene listed *after* the command; row 1's "Depends on" cell names only store/catalog resolution and omits hygiene.
3. …/2026-09-04-eval-integration.md:56-66 — §1 IE3: builds hygiene and wires it "into `share` before `safeWrite`" only; combined with §0 row 5 ("Hygiene wired into `share` (and later `publish`)") the plan never wires hygiene into the `eval` command at all. Its *Exit:* (lines 64-66) also lacks any "no preflight/agent process after a hygiene failure" case.
4. …/2026-09-04-eval-integration.md:99-100 — §3 Who: "**Eval side (this plan, IE1→IE3):** sequential PRs" — the ordering label to update if the sections are renumbered.
5. Sibling /Users/ajaywadhwani/terum-skills/.planning/specs/2026-09-04-eval-engine.md — §6.0:247 ("hygiene tier runs first and hard-stops on failure; then preflight (§7.4)"), §9:366-369 ("run by `validate`, `share`, `publish`, and CI … Checks, all fail-closed"), §14:452-463 (ME1 pure core includes `hygiene.ts`; `commands/eval.ts` only at ME4). The sibling is the correct side and needs no ordering change, but §9's runner list omits `eval` while §6.0 requires it — that one-line inconsistency is the likely origin of the integration spec's drift and should be reconciled in the same pass.

Code state confirming the gap is live (not spec-only): `/Users/ajaywadhwani/terum-skills/src/lib/evals/` contains agent.ts, checks.ts, execution.ts, judge.ts, receipt.ts, results.ts, stats.ts, triggers.ts and no hygiene.ts; `/Users/ajaywadhwani/terum-skills/src/commands/` has no eval.ts.

Decline-evidence check (all negative): no PRODUCT-CONCERNS.md in the repo; `.planning/decisions/` holds only 2026-09-01-team-skill-sharing-decision-walk.md and 2026-09-04-m1-hardening-decision-walk.md; no `.planning/debug/` directory; `/Users/ajaywadhwani/terum-skills/.planning/reviews/DEFERRED-2026-09-04-phase1-landing.md` contains no "hygiene" or "eval" entry.
  - Why one resolution: No product decision is at stake — the sibling spec already settled the rule ("hygiene tier runs first and hard-stops on failure", "all fail-closed"), and this document says that sibling is authoritative. The only question is how thoroughly the plan is corrected, and option 1 is the only one that closes all three places the bad ordering lives: the sentence that skips the gate, the dependency table that put the eval command ahead of hygiene in the first place, and the hygiene section that wires the gate into `share` but never into `eval` (so on the plan as written, `eval` never gets its hard-stop back even after hygiene ships). Option 3 fixes the sentence and leaves that last hole open, which is how this class of bug survives a fix. Option 2 reaches the same end state as option 1 by a different route and is a fair second choice if the team prefers not to renumber the PRs.
  - **Option 1: Reorder the sequence: hygiene lands before `eval` runs, and wire it into `eval` (recommended)** — Depth 4/4 · Cost 2/4. Four coordinated edits in /Users/ajaywadhwani/terum-skills/.planning/specs/2026-09-04-eval-integration.md. (1) §0 table lines 16-24: swap the rows so `lib/evals/hygiene.ts` is #1 and `commands/eval.ts` follows, and add hygiene to the eval-command row's "Depends on" cell — the table's own header says "in dependency order", so the row order is the fix. (2) §1: promote the current IE3 hygiene-tier section to IE1 and renumber (hygiene tier → eval runs locally → committed receipts → CI), keeping each section's body intact. (3) The eval-pipeline line (currently line 32): replace "hygiene (once IE3 lands; skip-with-notice before that)" with an unconditional "hygiene (hard-stops the run on any finding; no preflight, no agent subprocess, no LLM call after a hygiene failure)", quoting eval-engine §6.0:247. (4) The hygiene section: extend its wiring sentence from `share` only to "`share` before `safeWrite`, and `eval` as the first step of the run (eval-engine §6.0) — note eval-engine §9's runner list (`validate`, `share`, `publish`, CI) omits `eval` and should be amended to match §6.0"; add to that section's *Exit:* a case asserting no preflight/agent process starts after a hygiene failure. Also update §3's "IE1→IE3" ordering label (line 99) to the new numbering. *Wins if:* always, unless the team specifically wants IE1's on-laptop two-arm validation to be the very first PR
  - Option 2: Keep the PR order, fold `hygiene.ts` into IE1 as a prerequisite deliverable — Depth 4/4 · Cost 2/4. Leave the IE1-IE4 numbering alone. In §1 IE1, add `lib/evals/hygiene.ts` to IE1's deliverables (it is pure, no subprocess, per eval-engine §14 ME1) and replace "hygiene (once IE3 lands; skip-with-notice before that)" with an unconditional hard-stop clause; add the "no preflight/agent process after a hygiene failure" exit case to IE1. Retitle IE3 to "hygiene wired into `share`" and drop the module-build language from it, keeping its `allowed-tools` deferred-register note. Fix the §0 table row-1 "Depends on" cell to name hygiene (row order can stay, since hygiene now ships inside PR 1). *Wins if:* the team wants IE1's real-laptop two-arm run to remain the first merged PR and would rather grow that PR by one pure module than reshuffle the plan's section numbers and the §3 ordering label
  - Option 3: Wording-only: delete the skip-with-notice clause — Depth 2/4 · Cost 0/4. Single edit at line 32: replace "hygiene (once IE3 lands; skip-with-notice before that)" with "hygiene (must pass; IE3 is a prerequisite of IE1)". Leave the §0 table, IE3's share-only wiring, and the exit criteria untouched. *Wins if:* the spec is being frozen for handoff today and the reader is trusted to infer the PR reordering and the eval-side wiring from the one corrected sentence
- **BLOCKER — Automatic sync updates bypass the secret/PII hygiene boundary** — findings[23] · .planning/specs/2026-09-04-eval-integration.md §1 IE3, lines 60-61 (with §0 gap-table row 5, line 22); rule restated in .planning/specs/2026-09-04-eval-engine.md §9 line 368, §6.1 line 265, §2 line 42
  - Root cause: The integration spec states hygiene's attachment point as a single verb — `share` — while the phase-1 verb model makes `share` a one-time act after which content mutations flow through `sync`'s auto-update. Root cause is `.planning/specs/2026-09-04-eval-integration.md` §1 IE3, lines 60-61 ("Wire into `share` before `safeWrite` (extends the existing privilege-rejection gate)"), reinforced by §0 gap-table row 5, line 22 ("Hygiene wired into `share` (and later `publish`)"). The text is wrong because it names a *verb* where the security property needs a *mutation class*: every write of `skills/<name>/**` content out of a member's machine. Phase-1 §6 `share` (line 302) says "Thereafter automatic: `sync` diffs each `shared` source against the repo copy and auto-commits `ajay: update single-fix` — you never re-share", and §6 `sync` (line 312) + §5.3's reconciliation table (lines 185-193) commit source content over the repo copy with no gate. Landed code matches the spec exactly: `src/commands/share.ts:75` `reconcileShared` mirrors the source tree with `action: 'sync'` at lines 108, 120 and again in the `--keep-source`/`--keep-repo` divergence-resolution path at lines 157, 160 — four `safeWrite` call sites, none of which an implementer following IE3 verbatim would gate. Consequence: a secret, PII, bidi payload or license conflict introduced *after* the initial share is committed to the team repo by the next `sync` (including the non-interactive `sync --hook`, which runs unattended at session start), while IE3's exit criterion ("one deliberately-tricky skill … rejected") passes. The same narrow rule is restated in the authoritative sibling — eval spec §9 line 368 ("run by `validate`, `share`, `publish`, and CI"), §6.1 lifecycle table line 265, §2 line 42 — so fixing only IE3 leaves the authoritative statement superseded. Secondary gap named by the finding: IE3 specifies no failure behavior for the sync path (what happens to the repo copy, the stored `baseline`, and the `--hook` stderr/stamp contract when hygiene fails mid-sync), which §5.3's "refuse both ways … place nothing, commit nothing" and §8's deferral convention already supply a template for.
  - Rated: moderate / high risk / pattern — also stated at: The "who runs hygiene" rule is stated in 6 places across 2 specs, plus 2 places in a third that describe the ungated mutation. Sweep worklist: (1) /Users/ajaywadhwani/terum-skills/.planning/specs/2026-09-04-eval-integration.md line 60-61 — IE3 "Wire into `share` before `safeWrite`" (root cause); (2) same file line 22 — §0 gap-table row 5 "Hygiene wired into `share` (and later `publish`)"; (3) same file line 32 — IE1's own hygiene call ("hygiene (once IE3 lands; skip-with-notice before that)"), consistent, no change needed but must not contradict the new wording; (4) /Users/ajaywadhwani/terum-skills/.planning/specs/2026-09-04-eval-engine.md line 368 — §9 "run by `validate`, `share`, `publish`, and CI" (authoritative caller list, must gain the sync auto-update path); (5) same file line 265 — §6.1 lifecycle-gates table, which has a `share` row and a `publish` PR row and no row for the automatic sync update; (6) same file line 42 — §2 summary "the hygiene … tier wired into `share`/`publish`"; (7) /Users/ajaywadhwani/terum-skills/.planning/specs/2026-09-02-phase-1-build.md lines 185-193 — §5.3 reconciliation table, whose "commit source over the repo copy via safeWrite" rows become hygiene-conditional; (8) same file line 302 (`share`: "Thereafter automatic: `sync` … auto-commits") and line 312 (`sync`: "auto-update owned `shared` skills per the §5.3 reconciliation contract") — the statements that make the bypass; also line 437, §12's "one test per row of the §5.3 table", where the new regression case belongs. Corresponding code sites an implementer must gate: /Users/ajaywadhwani/terum-skills/src/commands/share.ts:108 and :120 (`reconcileShared`, action `sync`), :157 and :160 (divergence resolution, action `sync`), reached from /Users/ajaywadhwani/terum-skills/src/commands/sync.ts:76.
  - Why one resolution: One option clearly wins, and no product decision is needed. The spec has already decided that hygiene is fail-closed and that it exists precisely to stop secrets and PII leaving the machine; it has also already decided (phase-1 §5.3, §8) what a refusal looks like — refuse just that skill, write nothing, leave the recorded baseline alone, and under the session hook report on stderr without prompting. So the missing text is determined by rules the spec already states; it just has to be written down against the sync path as well as the share path. Option 2 (fixing only the integration spec) is the same edit minus the sweep, and it deliberately leaves the authoritative sibling saying the old, narrower thing — the exact drift that has already bitten this project once. Option 3 (enforce inside the shared write path) is a genuinely attractive engineering alternative because nobody can add a new writer later and forget the check, but it contradicts a convention the eval spec states in writing — that the write guard checks file shapes, not file contents — so taking it means reopening that convention, which is more disruption than this fix needs.
  - **Option 1: Gate the mutation class, not the verb — and sweep every restatement (recommended)** — Depth 4/4 · Cost 3/4. (1) Rewrite IE3's wiring sentence (integration spec lines 60-61) to define hygiene as a precondition of **every mutation of `skills/<name>/**` content**, naming the four sites explicitly: `share`'s initial commit; `reconcileShared`'s auto-update rows of the §5.3 table (`src/commands/share.ts:108,120`); the `share --keep-source`/`--keep-repo` divergence-resolution writes (`share.ts:157,160`); and, later, `publish`. (2) Add a short failure-behavior paragraph modelled on the conventions the phase-1 spec already sets: hygiene failure on a sync auto-update refuses **that skill only**, commits nothing, leaves the repo copy and the stored `shared[id].baseline` exactly where they were (so the next sync re-tries rather than swallowing the edit), prints one line naming the findings and the skill, and lets the rest of the sync proceed; under `sync --hook` the refusal goes to **stderr** (never stdout, per phase-1 §8) with no prompt, and the run counts as partial so the `run/<team>.stamp` is not advanced. (3) Extend IE3's *Exit* criterion with the regression case the finding asks for: a skill shared clean, then a `ghp_`/PEM/team-token planted in the source, then `sync` — commit refused, repo copy byte-identical, baseline unchanged, and the same case under `--hook`. (4) Fix §0 gap-table row 5 (line 22) to read as the mutation class rather than "wired into `share`". (5) Sweep the sibling authoritative spec so it no longer states the narrower rule: eval spec §9 line 368 caller list, §2 line 42, and add a `sync` (owned-skill auto-update) row to the §6.1 lifecycle-gates table next to the existing `share` row. (6) Add a one-line pointer in the integration spec's §2 "verification debts" or IE3 note that phase-1 §5.3's reconciliation table and §6 `sync` now carry a hygiene precondition, so the phase-1 spec's rev-9 pass picks it up. *Wins if:* baseline — this is the recommended option
  - Option 2: Fix IE3 only, flag the sibling — Depth 3/4 · Cost 1/4. Change only the integration spec: IE3 lines 60-61 gain the mutation-class wording, the failure behavior paragraph, and the post-share-secret exit test; §0 row 5 is reworded. The eval spec's §9 caller list and §6.1 lifecycle table are left as-is, with one sentence in IE3 noting they are superseded and must be updated when the eval spec next revs. *Wins if:* the eval spec is frozen at rev 5 for a reason (it is cited as authoritative for a receipt/card contract Teddy is already building against) and the team would rather carry one explicitly-noted supersession than open the sibling now — accepting the exact failure mode measured on 2026-09-03, where a rule fixed in one place stayed stale in the other.
  - Option 3: Enforce inside the write path instead of at each call site — Depth 4/4 · Cost 3/4. Instead of enumerating callers, state in IE3 (and eval spec §10) that the shared-write path itself refuses a `skills/**` content mutation whose post-image fails hygiene, for actions `share` and `sync` alike — hygiene is pure, so it can run as a mutation precondition next to the existing guard rows. IE3 then names one seam rather than four, and §0 row 5 follows. Requires reconciling with eval §10's stated house rule that "the guard row stays structural (path shape + id existence) per guard house style", and with phase-1 §6.0's description of `safeWrite`, i.e. saying explicitly whether this is a guard row or a pre-guard validation step. *Wins if:* the team wants enforcement no future call site can bypass — a fifth `skills/**` writer added later is gated automatically — and is willing to reopen the "guard rows stay structural" convention in eval §10 and phase-1 §6.0 to get it.
- **DRIFT — MISSING REFERENCE — README eval column remains permanently blank after receipt integration** — findings[5] · /Users/ajaywadhwani/terum-skills/.planning/specs/2026-09-04-eval-integration.md §0 gap table (lines 14–25) and §1 IE2 (lines 45–54); compounded by §3 (lines 103–107) naming only the UI as the receipt-consuming surface
  - Root cause: The integration plan presents itself as the exhaustive list of what must be wired between the landed CLI and the landed eval engine ("Both sides exist; **zero wiring exists.** Missing, in dependency order:" — §0, lines 14–25, an eight-row table), and §1 then breaks that list into IE1–IE4. Neither the table nor any IE step mentions the README generator. Meanwhile the sibling phase-1 spec §9 (line 397) promises an "eval column (`—` until phase 3)", the ledger row D22 (2026-09-01-team-skill-sharing.md:130) promises the same, and the shipped generator hard-codes the cell: `src/lib/readme.ts:70` emits a literal `—` in the `| … | Eval | Install |` row. §3 (lines 103–107) assigns receipt *rendering* exclusively to Teddy's UI ("The UI renders receipts and never re-runs anything"), so once IE2 lands committed receipts at `evals/<id>/<hash>/<run>.json` there is still no owner, no step, and no test for filling the README column — the "until phase 3" promise expires with nothing behind it, and the column stays `—` forever. The text is wrong by omission: a plan that claims to enumerate every missing seam is missing one that a sibling spec and a DECIDED ledger row both depend on.
  - Rated: moderate / medium risk / pattern — also stated at: The "eval column — until phase 3" rule is stated in five places, all of which must stay consistent: (1) /Users/ajaywadhwani/terum-skills/.planning/specs/2026-09-02-phase-1-build.md:397 (§9 README generator — `eval column ("—" until phase 3)`); (2) /Users/ajaywadhwani/terum-skills/.planning/specs/2026-09-02-phase-1-build.rev9-draft.md:399 (identical sentence — the unreviewed rev-9 draft the target's §2 already flags as diverging); (3) /Users/ajaywadhwani/terum-skills/.planning/specs/2026-09-01-team-skill-sharing.md:130 (ledger row D22, status DECIDED, same phrasing); (4) /Users/ajaywadhwani/terum-skills/.planning/specs/2026-09-04-eval-engine.md §12 lines 404–425 (the only stated receipt-display contract — "a skill without a receipt shows '—' (phase-1 D22 behavior)", plus the hard rules "no skill list is ever sorted or ranked by any receipt number" and "no lift-style decimal appears at card level", which bind a README table as much as a UI card, but §12 is written as the UI contract and never names the README); (5) the shipped implementation /Users/ajaywadhwani/terum-skills/src/lib/readme.ts:66 (header `| … | Latest | Eval | Install |`) and :70 (row body emits a hard-coded `—` for that cell) — the code confirms the column exists and is blank today. Within the target spec itself the fix touches §0 lines 14–25 (gap table), §1 IE2 lines 45–54, and §3 lines 103–107 (Teddy/UI ownership).
  - Why one resolution: Plain English: the team repo's README already has an "Eval" column that prints a dash, and two earlier documents promise it fills in "at phase 3". This plan IS phase 3, and it never mentions the README — so the dash would be permanent. Filling it in is not a new product choice: the receipts are already frozen JSON files, the rule for picking the newest one is already written down (last filename wins, per version), and the eval spec already says exactly what a viewer is allowed to show for a skill (a PASS/NEUTRAL/FAIL chip, no scores, no ranking). So there is one sensible resolution — add the README step to the plan and to the PR that lands receipts — and the only judgment left is packaging: one extra paragraph inside the receipts PR (recommended) versus a whole extra PR (option 3). The one substantive sub-question, "what if only an older version of the skill has a receipt?", is already answered by the eval spec: display surfaces show the latest receipt per version, so the cell stays a dash. Option 2 (write down that we are deliberately not doing it) is only right if someone decides the README column is not worth phase-3 effort — that would be a product call, and it would need the sibling spec's "until phase 3" wording changed too.
  - **Option 1: Add the README seam as an explicit step (gap row + IE2 extension + sibling contract note) (recommended)** — Depth 4/4 · Cost 2/4. Three edits in the target spec. (1) §0: add a ninth table row — `| 9 | README eval column reads committed receipts | phase-1 §9 / eval spec §12 | 3 |` — so the exhaustive list is exhaustive. (2) §1 IE2: after the `safeWrite`s sentence, add a short paragraph making the README generator part of the receipt PR: `lib/readme.ts` resolves, per skill, the receipt directory `evals/<id>/<latest-tree-hash>/`, takes the lexicographically last filename as the newest run (§5.4 of the eval spec), and renders the Eval cell as the receipt's verdict chip — greyed/suffixed when `execution_status != "complete"` — falling back to the existing `—` when the skill's *current* version has no receipt (an older version's receipt never fills the cell, per eval spec §5.4 'latest receipt per version'). State that the eval spec §12 hard rules bind here: verdict only, no lift decimals, and the skill table is never sorted by any receipt number. Extend IE2's *Exit:* line with the two tests the finding asks for — no receipt for the current version → `—`; two receipts in the same hash dir → the lexicographically last one is rendered. (3) §3: append one clause to the Teddy bullet noting the README generator is the second receipt-consuming surface and is owned by IE2, not by Teddy — so the two surfaces are visibly distinguished. Also add a line to §2 or the IE2 paragraph naming the sibling text that must change in the same PR: phase-1 §9 line 397 and its rev-9 draft line 399, whose `eval column ("—" until phase 3)` wording becomes the phase-3 rule, and ledger row D22. *Wins if:* always — it is the only option that both closes the gap in this plan and names the sibling/ledger sentences that must move with it
  - Option 2: One-line explicit deferral with a named owner — Depth 2/4 · Cost 1/4. Leave IE1–IE4 alone; add one bullet to §2 'Verification debts carried in' (or a new short §4 'Explicitly out of scope') stating that the README eval column (phase-1 §9, ledger D22) is NOT wired by this plan, naming who owns it and where it is tracked (a new row in `.planning/reviews/DEFERRED-2026-09-04-phase1-landing.md`), so the column stays `—` by decision rather than by omission. *Wins if:* the team wants this document to remain strictly a sequencing note for the three eval PRs, and prefers README work tracked in the deferred register — but note it leaves the sibling's "until phase 3" promise unsatisfied by any dated plan
  - Option 3: Full sweep — new IE sub-section plus sibling and ledger rewrites — Depth 4/4 · Cost 3/4. Everything in option 1, plus promote the README work to its own sub-section `### IE5 — README eval column` in §1 (with its own seams/exit paragraph, sequenced after IE2), and rewrite the rule at every site it is stated: phase-1 §9 line 397 and rev-9 draft line 399 (`eval column ("—" until phase 3)` → the phase-3 rendering rule), ledger row D22 in 2026-09-01-team-skill-sharing.md:130, and add a cross-reference in eval-engine §12 that the README table is a second display surface bound by the same hard rules. *Wins if:* the README column is treated as a separate deliverable with its own review, or the rev-9 draft is being finalized in the same pass anyway — then doing the sweep at once avoids a second edit of three files
- **DRIFT — Integration plan still treats VE1 as open and pins the superseded eval spec revision** — findings[16] · /Users/ajaywadhwani/terum-skills/.planning/specs/2026-09-04-eval-integration.md §0 line 7 (revision pin), §0 gap-table row 8 (line 25), §2 first bullet (lines 88–90); sibling truth at .planning/specs/2026-09-04-eval-engine.md:3, :325–328, :428–435
  - Root cause: The integration plan was written at 19:30 on 2026-09-04 against eval-engine rev 5, and the engine spec moved twice afterwards (commit 419240c "rev 6 — §7.3 contamination check asserts membership, not list equality", commit 9d8e075 "rev 7 variance reducers", file mtime 20:17). Three statements in the integration plan are now false rather than merely dated: (a) §0 line 7 names `2026-09-04-eval-engine.md` (rev 5) as the authoritative design, but the authoritative text is rev 7; (b) §0's gap table row 8 (line 25) lists "VE1/VE2 closure on the pinned CC version (contamination + stream-json fields)" as a missing piece, and (c) §2's first bullet (lines 88–90) says "determinism measurement started but closure not recorded — close and stamp in the eval spec before calling IE1 done." VE1 is closed: the engine spec §13 line 428 reads "**VE1 (CLOSED 2026-09-04, CC 2.1.236):**", the resulting rule is written into §7.3 (engine spec lines 325–328, "not by warning (VE1 closed 2026-09-04, see §13)… List *equality* is not asserted"), and the behaviour is already implemented in src/lib/evals/execution.ts:214–221 (membership check, comment "measured on CC 2.1.236 (VE1)"). Only VE2 (stream-json field names) is still open (engine spec §13 lines 434–435). So the plan imposes an entry condition on IE1 that has already been met, and points an implementer at a superseded revision. A fourth spot, §3 line 104, pins the frozen UI card contract to "eval spec rev 5"; that content is unchanged through rev 7 (receiptSchema has no model_id — rev 7's model_id snapshot lives on ArmSample only), so it is stale-but-true and should be re-pinned in the same sweep. §1 line 49's "(rev 5: receipts are append-only…)" is a historical attribution of when the rule was introduced and stays correct.
  - Rated: moderate / medium risk / pattern — also stated at: Same stale rule appears in four places, all in /Users/ajaywadhwani/terum-skills/.planning/specs/2026-09-04-eval-integration.md — sweep worklist: (1) §0 line 7, "Authoritative designs stay in `2026-09-04-eval-engine.md` (rev 5)" — stale revision pin; (2) §0 gap table row 8, line 25, "| 8 | VE1/VE2 closure on the pinned CC version (contamination + stream-json fields) | eval spec §13 | a laptop, not code |" — VE1 already closed; (3) §2 first bullet, lines 88–90, "**VE1/VE2** … determinism measurement started but closure not recorded — close and stamp in the eval spec before calling IE1 done." — false entry condition on IE1; (4) §3 line 104, "(eval spec rev 5: efficiency on the card, attribution one click deep)" — stale pin, content still true through rev 7. Deliberately NOT edited: §1 line 49 "(rev 5: receipts are append-only…)", a correct attribution of the revision that introduced the rule. Cross-check-only (do not edit in this fix): .planning/specs/2026-09-04-eval-engine.md:3 whose status line says "rev 6" while enumerating rev 7 changes, and :456 whose ME2 exit criterion "VE1/VE2/VE7 closed" is satisfied, not contradicted, by VE1's closure.
  - Why one resolution: No product judgement is needed — the facts are already written down. The eval-engine spec says in plain text that VE1 was closed on 2026-09-04 against Claude Code 2.1.236, the code that resulted from that closure is already shipped, and VE2 is the only check still outstanding. The integration plan simply has not been updated since, so it tells an implementer to go re-do work that is finished and to read an old draft. Option 1 just makes the plan say what is true today. The one wrinkle worth a reviewer's eye: the engine spec's own status line opens with "rev 6" and then goes on to describe rev 7 changes, so it contradicts itself about its own number — the commit history says the latest is rev 7, which is what the integration plan should point at, and the engine spec's header should be tidied separately.
  - **Option 1: Re-pin to rev 7 and carry only VE2 (recommended)** — Depth 4/4 · Cost 1/4. Four coordinated edits in the integration spec. §0 line 7: `(rev 5)` → `(rev 7)`. §0 gap-table row 8 (line 25): retitle the row to VE2 only — "VE2 closure on the pinned CC version (stream-json efficiency field names)" — and note VE1 closed 2026-09-04 on CC 2.1.236 (eval spec §13). §2 first bullet (lines 88–90): rewrite as "**VE2** (stream-json field names for `efficiency` on the pinned CC version): still open — close and stamp in the eval spec before calling IE1 done. VE1 (contamination isolation) closed 2026-09-04 on CC 2.1.236; §7.3 now asserts skill-list *membership*, not equality (eval spec rev 6), and `execution.ts` already implements it — do not re-close it." §3 line 104: `eval spec rev 5:` → `eval spec rev 7:` (the §12 card contract and `receiptSchema` are unchanged through rev 7, so Teddy stays unblocked). Leave §1 line 49's "(rev 5: receipts are append-only…)" alone — that is a correct provenance attribution. *Wins if:* recommended baseline — every false statement is corrected in place, the still-true VE2 debt survives, and the historical rev-5 attribution is preserved
  - Option 2: Drop the revision pin, cite §13 by reference — Depth 3/4 · Cost 1/4. Same VE1/VE2 corrections in §0 row 8 and §2, but instead of re-pinning a number, §0 line 7 becomes "Authoritative designs stay in `2026-09-04-eval-engine.md` (latest revision — see its status line) and `2026-09-02-phase-1-build.md`…", and §2's verification bullet says "outstanding verification debts are whatever the eval spec §13 still lists as open (VE2 at time of writing)" rather than enumerating them. §3 line 104 loses its rev pin too. *Wins if:* the engine spec is expected to keep churning through rev 8+ during IE1–IE3 and the team would rather the integration plan never need a re-pin than know exactly which revision it was written against
  - Option 3: Re-pin plus an explicit rev 6/7 delta note — Depth 4/4 · Cost 2/4. Option 1's edits, plus a new two-to-three-line note at the end of §0 (or a §2 bullet) recording what changed under the plan since it was drafted and that none of it moves IE1–IE4: rev 6 = §7.3 membership assertion (already in `execution.ts:214–221`); rev 7 = judge double-asked in both orderings with `judge-split` ties, HEADLESS_NOTE on every arm, one arm retry in a fresh sandbox, per-arm `model_id` snapshot — all inside the landed library, `receiptSchema` untouched, so the §12 card contract frozen for Teddy still holds. *Wins if:* the plan is handed to an implementer (e.g. /codex-implement) who would otherwise diff engine rev 5→7 themselves to decide whether IE1's three-arm description and Teddy's frozen contract still hold

### Forks — hinges on a product/design decision the spec has not made; run `/decision-walk <this report>` (2)

- **BLOCKER — MISSING REFERENCE — IE3 omits required `validate` and CI hygiene wiring** — findings[13] · /Users/ajaywadhwani/terum-skills/.planning/specs/2026-09-04-eval-integration.md §1 IE3 lines 60-61, restated at §0 gap-table row 5 line 22 (measured against sibling /Users/ajaywadhwani/terum-skills/.planning/specs/2026-09-04-eval-engine.md §9 line 368 and §6 lines 258-259)
  - Root cause: The integration spec enumerates the hygiene tier's call sites in two places and both undercount. §0 gap-table row 5 (line 22) says "Hygiene wired into `share` (and later `publish`)"; §1 IE3 (lines 60-61) says "Wire into `share` before `safeWrite`". The authoritative sibling — `.planning/specs/2026-09-04-eval-engine.md` §9 line 368 — states hygiene.ts is "run by `validate`, `share`, `publish`, and CI", and §6 lines 258-259 add "`validate <path|name>` gains the hygiene tier ... `share` and `publish` invoke it by construction". So the integration plan drops two of four required call sites, and with them the `validate` surface entirely.

Why the omission is a defect rather than a wording nit, on two counts:

(1) `validate` does not exist. There is no `src/commands/validate.ts` and no `.command('validate')` in `src/cli.ts` (the landed verbs are login, team, ls, readme, share, install, uninstall, sync, search, invite). The sibling's verb "gains" the hygiene tier — presupposing a verb that was never built. The only `validate` in the phase-1 spec (`2026-09-02-phase-1-build.md` line 405, V5) refers to the external legacy tool `skillevaluator validate`, not to our CLI. The integration spec's whole job is to name every unbuilt seam (§0: "Both sides exist; **zero wiring exists.** Missing, in dependency order:") and it has an established pattern for exactly this case — row 6, "**`publish` verb — phase-1 scope, never built** (register §E)". `validate` gets no such row, so no PR in IE1-IE4 builds it and nothing flags it as missing.

(2) CI hygiene is unassigned, and the incidental path is disabled by design. One could argue CI gets hygiene free because §6.0 of the sibling says "hygiene tier runs first and hard-stops on failure" inside `eval`, and IE4 puts `eval --k 3` in CI. But IE4 lines 74-77 make that job **report-only** and "absent secret → neutral skip" on `ANTHROPIC_API_KEY`. Hygiene is explicitly "free, no LLM, exit-code gated" (sibling §9 line 368) — a deterministic gate that needs no key. Routing it only through the key-gated, non-blocking eval job means the one CI check that could always run and always block, never blocks, and on a fork PR or an unconfigured repo never runs at all.

Neither omission is covered by a recorded deferral: `.planning/reviews/DEFERRED-2026-09-04-phase1-landing.md` §E lists the phase-1 not-built scope (`publish`, `team leave`, `setup`, M4) and never mentions `validate` or hygiene-in-CI; neither decision-walk ledger mentions them; there is no PRODUCT-CONCERNS.md in `.planning/`.
  - Why it is a fork: Half of this is settled and half needs a human call. Settled: hygiene is a free, no-LLM, deterministic check, and the plan currently gets it into CI only as a side effect of the AI-powered eval job — a job the plan itself makes non-blocking and skippable when the API key is missing. Every option fixes that the same way, and nobody needs to decide anything.

The open question is `validate`. The eval-engine spec talks about a `validate` command as if it already exists ("gains the hygiene tier"), but it does not — it was never built, and the only thing by that name in the older spec is an external tool we no longer use. So somebody has to decide: does this eval-integration work ship a new user-facing `validate` command (Option 1), or is a new command Ryan's territory alongside `publish`, with this plan just recording that it is missing (Option 2)? That is a scope-and-ownership call, not an engineering one. Option 3 is listed only for completeness — it patches one paragraph and leaves the summary table on line 22 still wrong, which is the exact two-places-drift this finding is about.

Recommendation is Option 1 because the authoritative eval spec already says hygiene runs from `validate`, and a hygiene checker with no way to invoke it directly is a check nobody can run before sharing. But if the answer to "who builds new verbs" is Ryan, Option 2 is the right call and costs no more.
  - **Option 1: Full call-site parity: build `validate`, block CI on hygiene (recommended)** — Depth 4/4 · Cost 2/4. Bring the integration spec into line with sibling §9's four call sites. (a) §0 gap table: rewrite row 5 (line 22) to "Hygiene wired into `validate`, `share` (and later `publish`), and CI"; add a new row — modelled on row 6's phrasing for `publish` — reading roughly "`validate <path|name>` verb — eval spec §6 assumes it; never built (no `commands/validate.ts`, not in `cli.ts`)", spec home eval spec §6/§9, depends on 4. (b) §1 IE3 (lines 58-66): after the hygiene.ts checklist, replace "Wire into `share` before `safeWrite`" with the three wirings — a new `commands/validate.ts` + `cli.ts` registration (`validate <path|name>`, pure, exit-code gated, no LLM, no network), `share` calling that same function before `safeWrite` (extends the existing privilege-rejection gate), and a CI hygiene step on PRs touching `skills/**` that is blocking and requires no `ANTHROPIC_API_KEY`. (c) IE3 exit criteria (lines 64-66): add `validate` returning a non-zero exit on the tricky skill, `share` refusing the same skill through the same code path, and the CI job failing the PR. (d) §1 IE4 (lines 73-80): one sentence stating the blocking hygiene check is a separate job from the key-gated, report-only `eval` job, so a missing secret degrades only the LLM tier. (e) §3 Who (lines 99-102): confirm the new verb sits on the eval side, not in Ryan's M3 remainder, so ownership is unambiguous. *Wins if:* always, unless the team decides a user-facing `validate` verb is out of this plan's scope
  - Option 2: CI hygiene now, `validate` recorded as a flagged gap — Depth 3/4 · Cost 2/4. Split the two halves by confidence. Wire only CI in IE3: §0 row 5 (line 22) becomes "Hygiene wired into `share`, and CI (and later `publish`)"; IE3 lines 60-61 gain the blocking, key-free CI hygiene step and its exit test; IE4 gains the sentence separating it from the key-gated eval job. For `validate`, add a §0 gap row in the style of row 6 — "`validate` verb — eval spec §6 assumes it; never built" — and a line in §1 or §3 stating it is not in IE1-IE4 and is flagged to Ryan alongside `publish`, plus a matching bullet under `.planning/reviews/DEFERRED-2026-09-04-phase1-landing.md` §E so the omission is recorded rather than silent. *Wins if:* the team judges that a standalone `validate` verb belongs with Ryan's M3 verb work (or phase 2) rather than in the eval integration PRs — this records the gap without committing this plan to build a new user-facing verb
  - Option 3: IE3-local wording fix — Depth 2/4 · Cost 1/4. Touch only §1 IE3 lines 60-61 and 64-66: change "Wire into `share` before `safeWrite`" to "Wire into `validate`, `share` (and later `publish`), and CI", and add the corresponding exit tests. Leave §0's gap table, IE4 and §3 as they are. *Wins if:* you want the smallest possible edit and treat §0's table as an informal summary an implementer will not build from — note this leaves line 22 still naming only `share`, and leaves the reader to infer that a verb which does not exist must first be created
- **BLOCKER — IE4 has neither an executable artifact path nor a deployment path for existing team workflows** — findings[26] · .planning/specs/2026-09-04-eval-integration.md §1 "IE4 — CI, gated on Ryan's `publish` + M4 npm", line 77 (interim bullet); contradicted by §0 table row 7 (line 24) and §3 (line 101)
  - Root cause: `.planning/specs/2026-09-04-eval-integration.md` §1 IE4, line 77 — the interim bullet "Runs via checkout + `npm ci` + `node dist/index.js` until npm publish lands" describes a build that cannot exist in the repo the job runs in, and IE4 never says how the job reaches the repos it must run in.

Two defects in one bullet:

1. **Not executable.** The job runs in a *team* repo. `team create` scaffolds exactly `team.json`, `people/<handle>.json`, `skills/.gitkeep`, `evals/.gitkeep`, `README.md`, `.github/workflows/terum-skills.yml` (`src/commands/team.ts:376-390`) — no `package.json`, no lockfile, no `src/`, no `dist/`. A default `actions/checkout` followed by `npm ci` fails at the first step, and there is no `dist/index.js` to run. The bullet names no second checkout of the product repo, no ref to pin it at, and no build step for it. The workflow template the job would live in states the opposite of the bullet: "This workflow requires published terum-skills npm artifacts (M4)" and invokes the tool as `npx -y terum-skills@latest …` (`src/commands/team.ts:397-415`).

2. **Not deployable, in either the interim or the final form.** The only place the eval job can be added is the `WORKFLOW` string constant, which is written once at `team create`. Editing it reaches *newly scaffolded* teams only. For team repos that already exist, IE4 names no migration, and none is possible through the tool: `guard()` (`src/lib/guard.ts:37-45`) allows only `README.md`, `people/<handle>.json`, `team.json`, and `skills/<name>/**`, and throws `GuardError` on every other path — `.github/workflows/**` included. So the "Final (when Ryan lands `publish`)" bullet inherits the same hole; publishing to npm fixes the invocation but not the delivery.

The bullet also contradicts a rule this spec already states elsewhere: §0 table row 7 (line 24) records the CI eval job's dependency as "1, 3; npm publish (register §E, M4)", and §3 (line 101) repeats "M4 npm publish → arms IE4's blocking gate". Line 77 invents an escape hatch from a dependency lines 24 and 101 record as binding — and the escape hatch does not work.
  - Why it is a fork: A real fork remains, and half of it is a product call the spec has not made.

The invocation half is settled — whatever else changes, "checkout + npm ci + node dist/index.js" in a team repo is simply wrong and has one obvious correct replacement.

The deployment half is a genuine decision: **is the tool allowed to write GitHub workflow files in a team's repo?** Today it is deliberately not — the write guard permits only the README, a person's own file, `team.json`, and skills, and refuses everything else. So a team repo created last week can never receive the eval CI job through any command; someone has to open a pull request in that repo by hand. The choices are (1) keep it that way and write down "upgrading a team's workflow is a manual, reviewed PR", or (2) open a narrow hole so the tool can replace that one file with a known-good template. Option 2 is more convenient and is a security-surface change — a tool that can rewrite a repo's CI can run anything in that repo. That is a "who is allowed to do what" call for a human, not for a spec-edit.

Second, smaller question for the same human: is pre-M4 eval CI worth a second checkout and a build step in every team repo's workflow (option 1), or should IE4 just wait for npm publish (option 2)? The spec currently says both — line 24 and line 101 say IE4 waits for npm; line 77 says it does not.
  - **Option 1: Name a real invocation and a real deployment path for IE4 (recommended)** — Depth 4/4 · Cost 2/4. Rewrite the IE4 interim bullet (line 77) and add a short third bullet to §1 IE4 (lines 74-84).

(a) Invocation — replace "Runs via checkout + `npm ci` + `node dist/index.js` until npm publish lands" with the actual two-checkout recipe: `actions/checkout@v4` for the team repo (workspace root), a second `actions/checkout@v4` with `repository: <org>/terum-skills`, `ref: <pinned SHA or tag>`, `path: .terum-tool`, then `npm ci && npm run build` **in `.terum-tool`**, then `node .terum-tool/dist/index.js eval <name> --k 3` with the team checkout as working directory. State the access precondition the second checkout needs (product repo public, or a read PAT in the team repo's secrets alongside `ANTHROPIC_API_KEY`) and that the pinned ref is bumped by hand until npm publish lands.

(b) Deployment — add a bullet stating that the eval job lives in the `WORKFLOW` constant (`src/commands/team.ts`), that editing it reaches newly scaffolded teams only, that `.github/workflows/**` is outside every guard row (`src/lib/guard.ts`) so no verb can write it, and name the mechanism for already-created team repos: default to a human-authored, human-reviewed PR per team repo pasting the canonical YAML (kept as a template file in the product repo so it has one source of truth), and note the alternative — a new `GuardAction` + row allowing a byte-exact replacement of `.github/workflows/terum-skills.yml` with that template — as a decision Ryan/§E must make before IE4's blocking form arms.

(c) Consistency — reconcile with §0 row 7 (line 24) and §3 (line 101), which currently record npm publish as a hard dependency, and flag the drift against eval spec §11 (which specifies `--commit`, while IE4 interim is report-only). *Wins if:* this is the recommended option
  - Option 2: Delete the interim; gate IE4 wholly on M4, keep only the deployment fix — Depth 3/4 · Cost 1/4. Delete the interim bullet (lines 74-77) from §1 IE4 entirely and fold IE4 into a single post-M4 item invoked the same way every other job in the template is invoked (`npx -y terum-skills@latest eval …`), which needs no second checkout and no build. Keep the new deployment bullet from option 1(b) verbatim — existing team repos still need a named migration once the eval job exists. Adjust §3's Ryan row to say the whole of IE4, not just its blocking gate, waits on M4. §0 row 7 (line 24) then needs no edit: it already records npm publish as the dependency. *Wins if:* the team accepts no eval CI at all until M4 npm publish lands — then the second-checkout build path is throwaway scaffolding nobody wants to maintain, and deleting it makes IE4 consistent with lines 24 and 101 for one paragraph of cost instead of a sub-section.
  - Option 3: Correct the invocation line only — Depth 1/4 · Cost 0/4. Replace `checkout + npm ci + node dist/index.js` on line 77 with `npx -y github:<org>/terum-skills#<pinned-sha> eval <name> --k 3` (npx installs and builds straight from a git ref, so no second checkout, no build step, no npm publish), and note the private-repo access caveat. Leave the deployment question untouched. *Wins if:* every team repo that exists today is a developer's own test repo whose workflow he edits by hand, so the only thing actually blocking IE4 is that the command as written cannot run — then the migration bullet is text about a population of zero.

### Declined by triage — overrule if you disagree (0)

_none_

### Untriaged — the triage agent returned nothing usable; investigate by hand or resume the run (0)

_none_
