# harden deferrals — m4-ship

Findings the /harden loop settled WITHOUT a fix. The review engines tell their verify and triage
agents to treat entries in `.planning/debug/**/*.deferred.md` as settled deferrals, so a later
round does not spend verify budget re-confirming them. `declined` = a cited reason it is not a
defect; `fork` = parked for `/decision-walk`, NOT resolved. Overrule by deleting the entry.

- [fork] **The hourly stamp is written after a partial (deferred) hook run, rate-limiting the deferred work into an hour of silence — contrary to §8** — src/commands/sync.ts:201 (round 1) — pending /decision-walk; do not re-raise
  reason: Product call for Ryan: What run/<team>.stamp is allowed to mean: the spec says "this team is fully synced"; two close-out tests and search.ts's "may be stale" line read it as "the clone was refreshed". Option 1 (applied as the interim, recommended): per-team completion gate on the one file — a team with undone work is not stamped, so it fetches at every session start until an interactive sync clears the item, and search nags "may be stale" meanwhile. Option 2: a second, completion-only marker (run/<team>.synced) keeps both meanings, at the cost of one more run/ artifact, a leave cleanup line and a §8 sentence. Option 3: a whole-run gate on deferred.length lets one team's deferral withhold another's stamp. INTERIM applied in b2ec572: option 1.
- [declined] **installPushGuard overwrites .git/hooks/pre-push and forces core.hooksPath, silently disabling machine-wide hooks in the clone** — src/lib/teamRepo.ts:349 (round 1)
  reason: Contested 2-1 and declined: ~/.terum/skills/teams/<team> is the tool's own clone, created and regenerated by it, and a machine-wide core.hooksPath (an org pre-commit or secret-scanning convention) would otherwise hide .git/hooks and silently disable the D12 guard in the one repository whose pushes reach the shared team remote. The user's own repositories are untouched. If a team wants machine-wide hooks to run here too, the hook can chain to the global hooks directory — a later enhancement, not a 0.1.0 blocker.
- [fork] **`treeBetween`'s new one-pass read spawns one `git show` per changed skill folder with no concurrency bound, on git's blocking pre-push path** — src/commands/guardPush.ts:110 (round 3) — pending /decision-walk; do not re-raise
  reason: Scope call for Ryan: A scope call: bound the fan-out in the one confirmed place (option 1, applied as the interim: slices of eight), add a shared mapLimit and sweep ls/search's one-git-per-skill fan-outs too (option 2), or revert to the sequential loop (option 3). ls and search have the same shape at lower stakes. INTERIM applied in 2ce4687: option 1 — treeBetween reads its blobs in slices of eight. Pick option 2 if `ls`/`search` in a several-hundred-skill team matter now.
- [fork] **`team leave`'s new §8 mutex only excludes hook syncs; an interactive `sync` still runs teardown over** — src/commands/leave.ts:42 (round 3) — pending /decision-walk; do not re-raise
  reason: Ruling for Ryan (round 3 was the last under the cap, so this behaviour change ships unreviewed if applied now): make EVERY sync take the §8 team mutex, keeping only the hourly stamp hook-only (option 1, recommended — a second concurrent interactive sync then skips that team with a printed reason, the way it already does when another process holds the clone's writer lock; one spec §8 sentence), or make leave's teardown self-healing (option 2: re-sweep placements after the clone lock; a resurrected folder can still remain on disk), or narrow leave's comment and accept the interactive-sync race for 0.1.0 (option 3). Today leave's mutex excludes background hook syncs only; an interactive `terum-skills sync` in another terminal can re-place a folder seconds after leave removed it and leave a ledger row pointing at a departed team.
- [fork] **The `blocked()` completeness rule was applied to the placement loop only; `reconcileShared`'s six undone-work exits still let the team be stamped "fully synced"** — src/commands/sync.ts:48 (round 3) — pending /decision-walk; do not re-raise
  reason: Ruling for Ryan: hand reconcileShared the run's `defer` recorder so its eight report-and-continue exits (missing source, unreadable record, missing repo copy, rename refused, two-sided divergence, privileged content held back, per-entry catch-all) withhold the team's stamp like a blocked placement does (option 1, recommended — they then also count toward the hook's `N skills need review` line), or return the unresolved team set and union it into `incomplete` without touching `deferred` (option 2), or re-plumb one team-attributed report channel for the whole run (option 3, which forces the call on whether one teammate's malformed SKILL.md should withhold everyone's stamp). Today a diverged shared skill prints its remedy once and is then silenced by the hourly stamp.
- [declined] **Credential-scrub test only pins the message, not the git argv the r2 high was about** — src/commands/__tests__/guard-push.test.ts:97 (round 3)
  reason: Contested 2-1 and APPLIED: the credential case now records git's argv through wrapRunner and asserts no token reaches it and that the scrubbed label reached the rev-parse lookup.
- [declined] **Credential assertion in the guard-push refusal test is negative-only and does not pin which refusal ran** — src/commands/__tests__/guard-push.test.ts:97 (round 3)
  reason: Contested 1-2 (majority refuted); the applied argv assertion covers the load-bearing half. Test-only tidy for the next sweep.
- [declined] **Credential-scrub test covers only one of the three remoteLabel interpolation sites in the guard** — src/commands/__tests__/guard-push.test.ts:97 (round 3)
  reason: Contested 1-2 (majority refuted); the applied argv assertion covers the load-bearing half. Test-only tidy for the next sweep.
- [declined] **Mid-run mutation test keys off a global fetch counter instead of the file's existing cwd-keyed runner idiom** — src/commands/__tests__/sync.test.ts:701 (round 3)
  reason: Contested 2-1, declined for now: the fetch-counter idiom is used by three tests in the same describe; switching to the cwd-keyed idiom is a test-only tidy for the next sweep.
