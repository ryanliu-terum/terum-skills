# harden deferrals — phase1-closeout

Findings the /harden loop settled WITHOUT a fix. The review engines tell their verify and triage
agents to treat entries in `.planning/debug/**/*.deferred.md` as settled deferrals, so a later
round does not spend verify budget re-confirming them. `declined` = a cited reason it is not a
defect; `fork` = parked for `/decision-walk`, NOT resolved. Overrule by deleting the entry.

- [declined] **Hoisting the foreign-collision inspect() moves a full config.json read + lstat into the no-op fast path of every sync** — src/commands/sync.ts:130 (round 1)
  reason: Contested 2-1 and declined: the collision reading must precede quarantineDrift (a blocked placement is touched by nothing) and, after the confirmation fix, sits inside the target lock like install's; the per-placement config read is the same one the ledger re-key needs for correctness under a rename earlier in the run. Cost is one lstat and one small JSON parse per placement per sync, and the no-op fast path still exits before any copy. Revisit only if a team with hundreds of placements shows it in the SessionStart hook's wall-clock.
- [fork] **setup now reports success on a machine whose team clone is gone, and never re-clones it** — src/commands/setup.ts:183 (round 1) — pending /decision-walk; do not re-raise
  reason: Product call for Ryan: should a resumed setup re-clone a missing team folder by itself (option 1: network work inside a step the wizard calls skipped, plus restoring the git identity), fail fast with a `team join <remote>` hint (option 2), or route the repair through the join verb and re-ask its identity questions (option 3)? INTERIM applied in the confirmation fix: option 2 — the skipped-team branch now refuses a configured team whose clone lacks team.json and names the repair, so the exit code is honest again; the cosmetic catch stays for a clone that exists but does not parse. Pick option 1 if setup is the 'fix my machine' wizard its banner promises.
- [fork] **canonicalDigest still concatenates path and hash without domain separation, so the collision class the new test targets is only half-closed** — src/lib/skills.ts:73 (round 1) — pending /decision-walk; do not re-raise
  reason: The in-repo half is settled and applied (option 1: the digest record's path field escapes `\`, `:` and newline, so the stream is prefix-free; every real path hashes to the bytes it did before, so no stored baseline moves). Ryan's half: whether to apply the same encoding to the vendored skill-fingerprint aggregate (placements[].fingerprint, the drift authority) at the cost of pushing the borrowed file further from upstream 61aa957 and updating its header, NOTICE and the spec sentence that calls it verbatim.
- [fork] **Hardened install cell blocks the backtick but the same hostile name still renders as a live link in the Skill cell** — src/lib/readme.ts:74 (round 1) — pending /decision-walk; do not re-raise
  reason: Product call for Ryan: may text committed to a team repo (a skill's description, category, author, folder name) render as a clickable Markdown link in the generated README and the Action's PR comment? INTERIM applied: option 3 — brackets are escaped in the Skill-name cell only (a no-op for every name the CLI accepts), so the fix's own demonstration case no longer ships a labelled link; the free-text columns keep the r2 reading that link rendering there is defensible. Option 1 (escape `[`/`]` as the last step of both sanitizers, covering every cell, both headings, the roster line and the PR comment) is ~5 lines if the answer is no.
