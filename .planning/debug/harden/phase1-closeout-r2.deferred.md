# harden deferrals — phase1-closeout-r2

Findings the /harden loop settled WITHOUT a fix. The review engines tell their verify and triage
agents to treat entries in `.planning/debug/**/*.deferred.md` as settled deferrals, so a later
round does not spend verify budget re-confirming them. `declined` = a cited reason it is not a
defect; `fork` = parked for `/decision-walk`, NOT resolved. Overrule by deleting the entry.

- [fork] **search's new batching spawns one unbounded `git rev-parse` child per hit, when an existing helper resolves every skill tree in a single `git ls-tree`** — src/commands/search.ts:41 (round 1) — pending /decision-walk; do not re-raise
  reason: Scope call for Ryan: A timing call: lift teamRepo's skillTrees into an exported latestTrees(runner, clone) — one `git ls-tree` per clone — and adopt it in search, ls and the Action's README path (option 1, recommended; touches shared code used by three commands and needs an empty-`skills/`-tree guard), or cap the per-hit fan-out with a small bounded map at search and ls (option 2; ~10 revert-safe lines, N git processes stay). INTERIM applied in 2eb01ed: search resolves its hits with allSettled in slices of eight (a bounded fan-out and per-row degradation); ls still forks one git per skill; the shared latestTrees helper is the call. Related: the M4 fork on treeBetween's fan-out (m4-ship.deferred.md) — one shared limiter would close both.
- [declined] **place()'s new quarantineRoot is optional, but the undefined branch is reachable only from tests and strands the copy inside the skills root — the invariant the fix cites** — src/lib/placer.ts:89 (round 1)
  reason: Low, declined for now: making place()'s quarantineRoot a required option (the compiler-enforced form of spec invariant 34) edits 14 test call sites; both production callers already pass it, and the misreport half — a non-replace failure naming a copy that was never made — is closed by the displacedExists flag. Next sweep.
- [declined] **Skill-cell bracket escape leaves the raw-HTML anchor vector open, so a crafted folder name still renders as an attacker-labelled link** — src/lib/readme.ts:78 (round 1)
  reason: Contested 2-1 and APPLIED as part of the parked README fork's interim: the Skill cell now also rewrites `<` and `>` to entities, so a crafted folder name renders neither as a Markdown link nor as an HTML anchor (a no-op for every name the CLI accepts). Whether the free-text columns may render Markdown at all is still the parked product call.
- [declined] **Duplicate `rm(temporary)` — the catch-block copy is dead now that a finally does the same** — src/lib/placer.ts:72 (round 1)
  reason: Contested 1-2, declined: the catch-block rm now carries a .catch so a staging-cleanup failure cannot abort the recovery, and the finally repeats it; the duplicate is harmless.
- [fork] **search now returns an untracked, unshared folder as a real hit, unlike the malformed-folder path four lines below which excludes it** — src/commands/search.ts:52 (round 2) — pending /decision-walk; do not re-raise
  reason: Product call for Ryan: A product call: should a skill folder that sits in the clone on this machine but was never committed to the team repo appear in `search` at all? Keep the row and add `unresolved: boolean` to SearchHit so the phase-2 SPA can grey out Install without string-matching `—` (option 1, recommended, Depth 2 / Cost 0); report-and-skip it like a malformed folder (option 2, Depth 3 / Cost 1 — but a rejection is not proof of untrackedness: git not on PATH rejects the same way, so a machine without git would print N reasons then 'No skills found'); or teach latestTree to tell 'not in HEAD' from 'git broke' and skip only the former (option 3, Depth 4 / Cost 2 — do it with the parked latestTrees fork if option 2's rule is wanted). Today the row is returned and printed with `—`, and `install team/<name>` of it succeeds off the clone's disk with team provenance. No interim applied (the marker is additive and waits for the ruling).
- [fork] **place() reports a placement that fully succeeded as a failure when only the displaced-copy cleanup fails, instead of using its own notices channel** — src/lib/placer.ts:82 (round 2) — pending /decision-walk; do not re-raise
  reason: Ruling for Ryan (round 2 was the last under the cap, so this control-flow change ships unreviewed if applied now): once the swap has landed, a failed removal of the displaced copy should quarantine it and RETURN the placement with a notice — `Placed <destination> but the previous <name> could not be removed; it is at <quarantine>` — through the notices channel place() already uses for a failed exclude write and both callers already print (option 1, recommended, Depth 3 / Cost 1: the ledger then records the fingerprint actually on disk, install exits 0, and the next sync no longer quarantines the tool's own copy as drift); or re-stamp the ledger from install/sync after a failed place() (option 2, Depth 1 / Cost 2: stops the phantom quarantine, still reports a successful install as a failure); or message-only (option 3, Depth 0 / Cost 0: the sentence stops being false, everything else stays). Today the swap lands, the old copy goes to quarantine, and place() throws `… could not be restored …` — false on its face — so install exits non-zero with no ledger entry, and sync reports Blocked and keeps the OLD fingerprint; the next sync quarantines the correct copy as drift. The control flow predates the close-out (it changed only the wording); placer.test.ts:164 pins the contradiction and flips to a resolved placement under option 1.
- [fork] **latestTree-per-skill resilience fixed at 1 of 3 call sites; ls still dies whole on the same ghost folder** — src/commands/ls.ts:45 (round 2) — pending /decision-walk; do not re-raise
  reason: Ruling for Ryan: give `ls` the per-row treatment search got in 2eb01ed — a `.catch` on the one `latestTree` await in listSkills that prints `<name>: <reason>` and renders `—` (option 1, recommended, Depth 2 / Cost 1, about three lines, deletable under either outcome of the fan-out fork); or mirror search's slices-of-eight + allSettled in ls (option 2, pre-empts the fan-out fork's option 2); or the shared one-`ls-tree`-per-clone latestTrees helper at all three sites (option 3 = the parked fork's option 1; touches the Action's README path). Today an untracked leftover folder in the clone — the state search's ghost test names — makes `ls` return failure with no roster and no rows while search degrades one row; readReadmeData (the Action) fails closed on the same folder, which may be right for a CI gate. Not applied: ls.ts is outside the reviewed diff and the change is behaviour.
- [fork] **setup's hoisted clone guard reimplements a weaker copy of ensureClone's completeness predicate** — src/commands/setup.ts:134 (round 2) — pending /decision-walk; do not re-raise
  reason: Ruling for Ryan: export the read-only half of ensureClone as `describeClone(clone, normalized, runner)` → absent | incomplete | foreign{origin} | ok{origin} in teamRepo.ts and have both `team join` and setup's guard consume it (option 1, recommended, Depth 4 / Cost 1 — one definition of 'a complete clone'; two setup tests: `.git` gone but team.json present, and a clone of a different remote); or inline `cloneOrigin === null` and the origin comparison into setup's guard only (option 2, Depth 2 / Cost 0; two hand-written copies that can drift again). Today the guard is two `exists` probes: a folder with team.json but no `.git` (an interrupted `team leave`, a dotfile-skipping restore) and a clone of the wrong remote both pass, setup prompts for a share, an invite and the hook and exits ok, and sync/publish/install then fail. Not applied: the weakness predates the close-out (which hoisted the guard) and the fix changes what setup refuses. Pairs with the parked 'should setup re-clone a missing clone itself' call.
