Invoke the `terum-skills` skill with the verb `invite`: run `npx -y terum-skills@latest invite …` from inside the session, following that skill's run-here / hand-off rules. Pass through any arguments after the verb: `invite $ARGUMENTS`
