Invoke the `terum-skills` skill with the verb `update`: run `npx -y terum-skills@latest update …` from inside the session, following that skill's run-here / hand-off rules. Pass through any arguments after the verb: `update $ARGUMENTS`
