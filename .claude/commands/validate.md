Invoke the `terum-skills` skill with the verb `validate`: run `npx -y terum-skills@latest validate …` from inside the session, following that skill's run-here / hand-off rules. Pass through any arguments after the verb: `validate $ARGUMENTS`
