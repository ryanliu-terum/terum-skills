---
name: Bug report
about: Something in the CLI or the desktop app does not do what it says
labels: bug
---

## What happened

What you expected, and what you got instead.

## Version and platform

- CLI version (`terum-skills --version`, or the `@<version>` in the npx command you ran; `npx -y terum-skills@latest --version` reports the newest release, not your copy):
- Operating system and architecture (for example macOS 15 arm64, Windows 11 x64):
- Was the desktop app involved, or was this the CLI alone?

## The command and its output

The exact command you ran, and everything it printed. Paste it as a fenced block rather than a screenshot.

```

```

## Status

The output of `npx -y terum-skills@latest status`, with handles, emails and repository names replaced by placeholders if your team is private.

```

```
