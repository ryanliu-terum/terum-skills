# GAPS.md — drawn affordances the CLI on `main` (re-anchored at b7be3c5, 2026-09-08; first pinned at b5c0507) does not have yet

M7 status (2026-09-08): the real adapter declares which read models it serves (`Backend.surfaces()`, S7af); a surface whose model is still a gap is hidden from the sidebar rather than stubbed, and every line below that names a read model closes in the batch that first serves it from real data (S7f library/skill — served since PR m7-S7f, S7k status/settings — served since PR m7-S7k, S7b roster/catalog, S7n receipts, S7e update, S7r inbox/onboarding). `Surfaces.appUpdate` (2026-09-10, W-01, spec `.planning/specs/2026-09-10-w01-app-update.md`) is served `true` by the real adapter and stays `false` on the mock, so the locked SettingsUpdates board keeps its drawn literal; a CLI whose hello lacks `features.appUpdate` degrades the row to the read-only notice ("the CLI tells you about a release and never installs it for you"). `features.appUpdate` has two consumers by design — the Settings rows and the launch-time check hook (`useAppUpdateCheck`) — recorded here as the one exception to the one-consumer rule in `desktop/AGENTS.md` invariant 2.

One line each: screen or board: affordance or printed string — what changed at b5c0507 or what the CLI would need. Design-side calls (printed strings) are Teddy's; seam-side gaps are asks to Ryan's repo. The app builds every affordance as drawn on the mock backend; the four capability-flagged gaps are listed last.

## CLI drift at b5c0507 (printed strings and seam names; design-side, not patched overnight)

- SettingsSharing: terminal hint prints `npx -y terum-skills@latest share ~/.claude/skills/api-docs` (build.py:3740) and the Frontmatter row says "share adds license, id and author to your SKILL.md" (build.py:3734) — the verb is `connect` at b5c0507 (71e7a0a); bare `share` now exits 1 with "`share` is now `connect`: run `<invocation> connect [<path>]` (same options: --team, --allow-privileged, --keep-source, --keep-repo, --relocate, --forget)." Teddy decides the replacement string.
- OnboardingShare: the Basics ▸ Share tab's terminal hint prints `npx -y terum-skills@latest share` (BASICS_HINT build.py:3908, rendered build.py:4156); the tab label "Share" (build.py:3884) and BASICS_COPY["Share"] (build.py:3904) teach the old verb name — a user copying the line gets exit 1 at CLI 0.1.4+. Highest-value line in this list.
- SkillDetailQuality: caption "Hygiene checks · passed on share, 12 days ago" (build.py:1433) and the validate hint prefix "Runs on share, publish and in CI" (build.py:1438) — the verb is `connect`; validate's own description and after-help were reworded (b5f441f) and now say "connect it first".
- InboxPanes (review report): "{n} of 6 checks passed on share" (build.py:2099) — verb is `connect`; the six codes HYG1–HYG6 are unchanged.
- LibraryEmpty: "Share a skill to see installs here" (build.py:401) — UI copy naming the CLI action; verb is `connect`. Product-noun "Share" (page, sidebar row, dialog, Invite button) is Teddy's and is not part of this line. SUPERSEDED 2026-09-10: the Library no longer draws a Connect entry point at all (see the auto-share section below); sharing global skills is sync's default, not a user action.
- SettingsSharing: the hint shows an unquoted real path `~/.claude/skills/api-docs`; at b5c0507 `invocation()` POSIX-single-quotes real argument values and leaves only `<placeholder>` fragments bare (src/lib/invocation.ts:45-50, a08d692). Cosmetic; Teddy decides whether product copy adopts the quoting.
- Every board printing a command (35 `cli()` call sites through build.py:738-740): the boards hard-code one spelling, `npx -y terum-skills@latest <cmd>`; at b5c0507 every printed CLI hint is launch-aware (`terum-skills <verb>` when the PATH bin is this global install, the npx form otherwise; src/lib/invocation.ts:19-38). Board strings match the npx branch byte-for-byte. Decide whether the app ever mirrors the bare form (needs its own launch/PATH probe) or keeps the single npx spelling as a product choice. The CLI keeps two sites fixed-npx on purpose (uninstallMachine.ts:56, teamRepo.ts:390 push-guard hook).
- (no drawn surface) Launch-aware remedy strings that changed only in form at b5c0507, listed so nobody re-derives them: execute.ts:47 "N skills need review — run `<invocation> sync`" (was bare `terum-skills sync`), hook.ts:173 staleLine, ls.ts:44 "Local skills: …", status.ts:58 clone-repair "Restore it", config.ts:28 "No team is configured. Run `<invocation> team join` first." (was an unpasteable `team join`), install.ts:176/182/206, uninstall.ts:30/38, uninstallMachine.ts:90/100/114/132, guard.ts:95 (`login`), auth.ts:135-136 (`team create <name> --remote <url>`), cli.ts:59 team description, cli.ts:124 uninstall excess-args refusal, setup's "Next, from any terminal:" epilogue (setup.ts:197-203, now advertises `connect` and "endorse a skill already connected to the team"), publish's `npxInvocation` helper deleted. GET_STARTED_LINES (src/lib/hints.ts, deleted) is now getStartedLines(form) in src/lib/invocation.ts:53; no board prints those lines.
- SkillDetailRemove: the dialog's command box prints `npx -y terum-skills@latest uninstall deploy-check` (build.py:1601); the per-skill verb is `uninstall-skill <ref>` and bare `uninstall` with an operand refuses with "To remove a skill, use `<invocation> uninstall-skill <ref>`." PRE-EXISTING at 8bf12a8, not from this delta. The dialog's Remove action calls the seam's `uninstallSkill`, never `uninstallMachine`.
- ShareInvite / OnboardingTeam: the join block (build.py:3155-3157) shows one line, `npx -y terum-skills@latest setup terum/team-skills`; the CLI's slackBlock (invite.ts:49-54) also carries `npm install -g terum-skills` and a "Bare equivalent: … team join …" line, and its heading changed in this delta from "Share this with your teammate:" to "Send this to your teammate:" (boards say "Send them this", build.py:3166, 4176). Teddy decides whether the copied block matches the CLI byte-for-byte.
- SkillDetailQuality / SkillDetailStates: the Size hygiene row reads "SKILL.md 4.2k tokens · cap 5k" (pass, build.py:891) and the states-sheet warn specimen "SKILL.md 4.8k tokens · 200 under the cap" (build.py:1672); at b5c0507 HYG6's size arm is a non-blocking warning (914ebb0, hygiene.ts:95: "SKILL.md is N characters, M over the 20,000-character guideline (~5k tokens). Size alone does not block this operation; …"), HYG6's empty-description arm still blocks, and there is no under-limit warning. `validate` prints "<name>: hygiene passed (N warning(s))." and ValidateResult gained `warnings: number` on both arms. Teddy decides the copy ("cap" vs "guideline") and whether the app invents an approaching-limit warning.
- (seam only) `connect` with no path is now a repeat-until-Done picker at b5c0507 (connect.ts:91-134: select "Connect a local skill folder to team <t>?" with Skip|Done, per-skill confirm "Connect <name>?", decline "Connect was declined."); it returns ConnectBatch {kind:'batch'; shared; declined; refused}; path/option calls return ConnectResult {id; name; reconciled?}. No board draws a connect result panel; the mock models the loop. 2026-09-10: no app surface calls the bare picker any more (the Library CTA is gone); the app's remaining connect calls pass a path (Settings ▸ Sharing) or ride setup's connect step.
- Onboarding (header comment build.py:3871-3872, no rendered string): the premise "`setup` (… invites, a first share, the session hook) has already run in the terminal" is stale — at b5c0507 the wizard's step is `connect`, offered to joiners as well as creators (SetupArgs.offerConnect, default true via `!== false`, setup.ts:191; install's quiet bootstrap passes false), and multi-skill. The drawn 14-board flow is final (Teddy); nothing to add. (Citations in the same block point one revision behind: build.py:3898 says setup.ts:44-45, now 48-49; the strings are still verbatim. build.py:3873-3874 still says the community link is the Issues page while ONBOARD_COMMUNITY at 3895 is the placeholder Slack/Discord link Teddy chose.)
- (not drawn) `receipt-check --cwd --base` is a new hidden CI verb at b5c0507 (4149287; success({endorsed, checked}) or failure "Receipt check failed:\n<name>: <reason>"); no seam method, no screen.
- (not drawn) `team workflow-update --print` is a new print-only subcommand (team.ts: WorkflowUpdateArgs {print?}, WorkflowUpdateResult {workflow}); the seam's TeamArgs.kind stays create|join|remove|leave; no screen.
- (not drawn) `login`, `readme`, `guard-push` exist at b5c0507 and have no seam method: no board invokes them (SettingsAccount's gh row draws a "Sign in" button whose copy says it opens gh's own login in a terminal, build.py:3539-3540; the mock treats it as openInEditor-style hand-off, not a verb).
- InboxPanes (review pane): a publish/ PR now runs two blocking CI jobs in the team-repo workflow template (`hygiene` and `receipt-check`, team.ts:559/578; only newly scaffolded repos get it, existing repos need `team workflow-update`); the drawn review pane (report_review build.py:2090-2103, row fact build.py:1811 "hygiene 6 of 6") predates them. Recorded only; no check-run UI invented.
- (seam only) every verb's args at b5c0507 carries `form?: InvocationForm` (WithForm, 1d5e3f9); the seam's DTOs omit it (a CLI launch detail); `validate` args gained `cwd?: string` (kept in the seam); `setup` args gained `offerConnect?: boolean` (kept, default true).

- Settings ▸ Sharing (PF-08, opt out of publishing install records): HARD; ships as drawn. Install records are the team's one adoption signal (D38) and a per-person opt-out would need a committed people-file field, a sync rule and a UI state; nothing in the CLI supports it and no batch builds it.

## Capability-flagged gaps (Teddy, 2026-09-07: the only affordances behind a flag; the mock reports them as supported)

S7q (2026-09-08): the flags now have consumers. The adapter caches the CLI's `hello` frame from any run and serves `Backend.features()` (the twelve `FRAME_FEATURES` switches plus `memberRole`; a key the frame omits is `false`); `capabilities()` maps `disablePerMachine` straight across and `perCaseEvalTables` from `perCase`, while `inboxEventLog`, `offtargetKind` and `machineRegistry` stay hard `false` on the real adapter (their mechanisms are cut). Every drawn control a `false` switch cannot back is hidden or degraded per the S7q spec table (hearts, follow, role menus, job labels, last seen, invite scoping, per-machine switches, lift figures, Run eval, per-case tables, other-machines rows); the mock reports every switch `true`, so the locked boards are unchanged. A flag flips to `true` for real users only when the CLI's hello frame says so.

- Library / SkillDetail / SkillDetailDisabled (card switch, rail status card): per-machine enable/disable of a placed skill — no verb, no state; placement is the loading mechanism, the only off-states are uninstall and quarantine. Flag `capabilities.disablePerMachine`.
- Inbox (every board), sidebar Pushes / Updates / Alerts: an Inbox event log (the seven kinds with unread and acted-on state) — the CLI has no event log; the only announcement channels are `sync --hook`'s stderr line and interactive sync's prompts. Flag `capabilities.inboxEventLog`; a real adapter derives what it can at sync time.
- InboxAlert (deploy-check fired off-target), Settings ▸ Inbox kind list: the `offtarget` alert kind and its per-session prompts — no collector exists anywhere. Flag `capabilities.offtargetKind`.
- SettingsMachine (This machine), footer machine name, "your other machines": a machine registry (machine identity beyond the local hostname) — no machine identity anywhere in the CLI; cross-machine propagation is `people/<you>.json.installed` + `sync` per machine. Flag `capabilities.machineRegistry`.
- SkillDetailEvals / SkillDetailEvalsReport / InboxEval (per-case tables, per-case rows in the report): per-case eval tables — the receipt has `comparisons`/`arm_scores`/`efficiency` and no `per_case` block. Flag `capabilities.perCaseEvalTables`; the mock's receipts carry `per_case` (an additive ask to Ajay, eval-engine §5.3).

## Other drawn affordances without a CLI counterpart (built as drawn, no flag; recorded for Ryan's batch)

- SkillDetailInstall (scope radios Global / Terum / SSM / MRF): `InstallArgs.scope` is a label the real adapter resolves to a registered checkout root and passes as `install --into <root>` (`tauri/index.ts` `install`), so the radios work; the CLI does not echo the scope back, so `InstalledResult.scope` is the label the app sent. §13 default 6 (no per-install override) and D17 (scope is which `team.json` list references the ID) remain the CLI's model. (Corrected 2026-09-10, W-03/W-04 spec `.planning/specs/2026-09-10-w03w04-skill-routing.md`: this line used to say `InstallArgs` has no `scope`.)
- Library / SkillDetail / MarketplaceProject (favorite heart with team-wide counts), MarketplacePerson (Follow / followers): no fields in `people/<handle>.json` (schema.ts:54-64) and no verbs; D20 forbids app-local state shown team-wide. Ask: §5.2 people-file fields + verbs.
- Share / ShareInvite (invite scoping, last seen): `invite` takes GitHub logins only; `teamSchema` has no roles; last-seen's only artefact is the machine-local stamp. The Status role chip Admin · Member is real since the `roles` flag flipped true: `status` emits per-member `admin: boolean | null` from the host's collaborator permissions via gh (null → the app shows `—`); it is read-only — the CLI has no verb that grants or revokes admin. Joined closed 2026-09-10 (PF-06, Ryan in session): `status` emits per-member `joined` — the day `people/<handle>.json` was first committed, one `git log` pass over `people/` in `readRoster`'s caller. The roster's fourth column is now **Skills**, that member's whole library rather than their installs from this team: nothing in a team repository can observe a teammate's machine, so each `sync` writes its own count into its own people file (`local_skills`, §5.2) and the roster reads it back as `skillsTotal`. Both fields are nullish in the app's schema, so an older CLI — and a member who has not synced since the field shipped — still renders `—`, never 0. Last seen (PF-05) is unchanged and still hidden behind its flag.
- MarketplaceProject / MarketplaceProjects / MarketplacePeople (project members, facepiles, per-project admin, checkout paths): `team.json` projects carry `remotes` + `skills`, never members; checkout paths are machine-local. Checkout registration closed 2026-09-09 by `config.checkouts` (the CLI-owned registry behind `checkout add|remove|list`, #85) and the Library rebuild (#88): the real adapter's Library reads `ls --local` and renders one root per section — Global plus every registered checkout keyed by its canonical path in `?root=`, a detected (unregistered) root with a one-click Add, an absent root with a dash — and team fields enrich cards by `skillId`; registration is not sharing consent (it connects nothing and approves no grant) and `checkout remove` is registry-only. Which folder an install lands in from the app (`install --into` destination radios) is Desktop-2. People-file `projects[]` supplies membership; no project → checkout-path registry is inferred.
- Installed-state discovery excludes `notOffered`: even a rejected folder carrying a team id is never counted as present. Every omission has a `skillId` key; under the locked §2 inspection rule it is null for rejected/failed inspections, so rejected folders do not currently expose an otherwise-readable id.
- MarketplaceFilters (verdict / lift / tokens / installs facets, default 4 active): the CLI `search` is still skills-only substring over the clone, but app-side facet filtering now works: the popover commits its selection to search params (`market-facets.ts`) and the skill lists AND the committed facets with the query; a facet whose datum a card lacks (no receipt, unsized, no author record) never excludes that card, and committing the untouched default selection leaves the list unchanged.
- Top bar ⌘K "Search skills, people, projects": `search` is skills-only; the app-side combined search has no drawn results surface (the `#/search` route is reserved).
- SkillDetailQuality (hygiene `warn` tier, "4.2k tokens · cap 5k"): HYG6's size arm is a non-blocking warning at b5c0507 but there is no advisory tier and no tokenizer in `src/`; the app shows the fixture's pass|warn|fail as drawn. PARTLY CLOSED 2026-09-09 (local-card-fields): there is still no tokenizer and no advisory tier, but the card and detail size chips are no longer a hardcoded dash — `ls` carries `characters` per row and the app renders `~N.Nk tokens` at HYG6's own four-characters-to-a-token ratio. The tilde is required: it is an estimate, not a measurement.
- SkillDetail (Edit button opens the file): placed copies are generated output (sync quarantines edits); only `config.shared[id].source` is editable, and only for your own skills. Edit opens the authored source through `openInEditor`.
- SkillDetailEvalsReport / SkillDetailEvals (12-char version hashes, "Partial run · 7 of 9 rounds scored"): `shortHash` is 8 chars in the README renderer; the app renders 12 as drawn and never normalises.
- OnboardingTeam (Workspace name field, first invites from the app): setup's team-name and invite questions move into the app; the app needs a verb to write the workspace name (create/join bind it once today, auth.ts bindTeam).
- SettingsAccount (gh row Sign in): `login` exists at b5c0507 but has no seam method; the button opens gh's own login in a terminal (openInEditor-style hand-off).
- SettingsAdvanced ("Remove terum-skills from this machine", Remove…): machine-wide `uninstall` exists; the seam is `Backend.uninstallMachine`; the dialog's y/N is the CLI's confirm as a dialog.
- SettingsTeams (Join button, hint `setup <org>/<repo>`): `setup [target]` exists; the seam is `Backend.setup({ target, offerConnect })`; the Onboarding boards never call it (the wizard has already run). Since the one-team-per-machine ruling (2026-09-08) Join drives `Backend.setup({ target })` from the app only on a zero-team machine; with a team configured the row reads "Leave first" and opens the leave dialog instead, and a legacy 2+ machine shows a hint per card.
- (eval-in-app, 2026-09-09) SkillDetailEvals / SkillDetailRunEval: the Evals tab and the run dialog are now served from real data through the CLI's read-only `eval-report` verb (committed receipts by version, this machine's local runs, placed vs team-current trees) and `eval` runs in the app over `--frames` with a `Commit the receipt to the team` checkbox (default on, `capabilities.evalCommitChoice`, real adapter only). Still open: a Commit-later affordance for a local run whose receipt commit failed (the Evals tab shows the local receipt under a `Local run, not committed` banner; re-running with commit is the only path today); `evalCommand` stays the board's npx spelling (the launch-aware line above still applies); the History rail lists committed and local runs, one receipt rendered at a time, without click-to-open of an older receipt; `incumbent.version` and the catalog size are not in the committed receipt (`—` / 0 in the report); per-case rows stay behind `perCaseEvalTables`. Two mock scenarios exist only for tests, `__mock=invalid-newest` and `__mock=version-mismatch`; no board draws them. The per-skill `receipts` read model is served (the gap at tauri/index.ts closed for the per-skill case). Since 2026-09-10 (W-06, spec `.planning/specs/2026-09-10-w06-eval-lock.md`) a person-initiated verb waits up to 75 s for another process's clone writer lock (a hook keeps its 4 s budget and says nothing), prints one waiting line after a second, refuses a lock stamped in the machine's future instead of waiting on it, and a cancelled CLI runs its shutdown hooks so the lock and the eval's agent children are released; the run dialog offers `Run eval again` after a stopped or lock-refused run. A run whose receipt commit failed still has no Commit-later affordance.
- Every long verb (install, sync, connect, publish, eval, invite, team, setup, uninstallMachine): the CLI prints progress through `Prompter.print` and asks through confirm/text/select; there is no `--json` mode, no server and no progress events; a decline is typed since S7d (`declined:true` on the result frame from `Result.cancelled`, never matched from the error text). The seam's `Run<T>` frames (print | ask | progress | result) are what a sidecar must serialise.
- Library / Marketplace / SkillDetail (card and detail description): the real adapter derives `desc`/`desc_long` from the first SKILL.md body paragraph (`src/lib/body-excerpt.ts`: CRLF-normalized, leading H1 dropped, first paragraph or first three list lines flattened to one line, ~200 chars), with the frontmatter `description:` as the fallback (ratified decision, teddyzheng 2026-09-08). `ls --local` rows carry no body, so local-only cards stay on the row's frontmatter `description` (#101). The mock keeps serving the design fixture's `desc` — the drawn boards' text is the fixture's contract — so mock and real deliberately diverge on card description text.
- Library / Marketplace / SkillDetail (install tri-state): `SkillCard.installed` is `'placed' | 'recorded' | 'absent'`. The real adapter reads the identified `ls --local` provenance first, then — only for a skill the scan shows nothing for — the unfiltered `status` ledger placements (so a project-scope placement in an unregistered checkout still counts as placed), then the team people file (`installedBy` containing this team handle). 'recorded' (people file says installed, nothing on this machine) renders "Installed · not on this machine" with a Reinstall action — a state no board draws (boards draw only installed/not-installed; it previously rendered as plain Install). The finer disk split (`placed`/`onDiskOnly` booleans, #100's `unidentifiedLocal`) lives inside the 'placed' state; an unidentified same-named folder still renders "Install state unknown" over the 'recorded' copy. The mock maps its fixture boolean to placed/absent and never serves 'recorded'; a successful mock install flips the fixture state to placed.
- (bulk-remove-confirm, 2026-09-09) CLI bug recorded, not fixed here: `connect` adopting a folder that is already a ledger placement (`src/commands/connect.ts`, the `existing.id` branch) records `shared[id] = { source, team, baseline }` after its confirm but leaves `placements[source]` in place, so the same path is both a connected source and a placement. `uninstall-skill` then removes or quarantines that folder through the placer and leaves `shared[id].source` pointing at a missing folder for `sync`'s orphan rules to sort out. The fix belongs in `connect`: refuse to adopt a ledger path, or clear the placement (and its people-file record) in the same store update, with copy saying which happened. Out of scope for the bulk-remove change.

## Closed 2026-09-09 — local-card-fields

- Library / SkillCard (local rows): `localCard` hardcoded `desc:''`, `project:'local'`, `installs:'—'` and `size:'—'`, so a Library of local folders drew 88 near-identical dash cards. `ls --local` rows and `notOffered` entries now carry `description` and `characters`; the identity line takes the section's root label (Global, or the checkout's basename), installs is the honest `0 installs` for an unshared folder, and the size chip is the `~N.Nk tokens` estimate above. `inventoryCard` (team rows) had the same hardcoded `size:'—'` and takes `characters` from `ls --team`, so one skill never reports two different sizes on two screens. No new filesystem read: `localSkills` and `skillRecords` already read each SKILL.md.
- `inspect()` (src/lib/skill-source.ts) now returns the description a rejected folder's frontmatter parsed to, when it got past the YAML parse. Refusing to *connect* a folder was never a claim that it could not be *read*: on Ryan's machine 84 of 86 Global omissions are `name-mismatch` (the `gsd-*` skills declare `name: gsd:add-backlog` inside a hyphenated folder) with a perfectly good description. Nothing is reconstructed — `no-frontmatter`, `invalid-yaml` and `description-missing` stay null, which is exactly where a blank description is the honest report. `illegal-name` is rejected before the parse and deliberately stays bare rather than reordering a validation ladder that error-message tests pin.
- Library title / Skills tile: `n` is `skills.length` — the cards the grid draws (`index.ts` `const n=skills.length`, "title, tile and placeholder never count a card the grid does not draw"); `visibleSkillFolders` makes that coincide with the CLI's `counts.skillFolders` minus the folders the grid filters, so an older CLI needs no fallback. (Clarified 2026-09-10: the title never reads `counts.skillFolders` directly; it counts the drawn cards.) Since 2026-09-10 (W-07) the subtitle prints `plural(n,'skill')` (`"N skills"`, `"1 skill"`) instead of `"N skill folders in <root.label>"`; the root label is already the header title. Required because the bundled `/terum-skills` wrapper is now excluded from `countable()` — terum-skills listing its own placed component as a broken folder the user should fix was a defect — while the CLI still counts it as a folder on disk (`FRONTMATTER_PROBLEMS` in local-skills.ts). Folder counts and the sidebar badge therefore stay in agreement; only the grid is filtered.
- Version coupling (recorded, not fixed): `cliLocalRow` is `.strict()`, so an app that predates these fields cannot read a newer CLI's rows. The reverse skew is handled — every new field is `nullish`, so a newer app on an older CLI degrades to a dash and an empty description rather than failing the read (`index.test.ts`, "degrades to a dash when the CLI predates the character count"). The app and CLI ship together, so only a hand-mixed pair hits this.

## Changed 2026-09-10 — auto-share is the global default (library-mirror-id-sync)

- Sharing model (ratified override, ajay 2026-09-10, `.planning/specs/2026-09-10-library-mirror-id-sync.md`; overrides per-folder connect consent 52d76c00 and global-as-placement-target 85c4ebd2 — Ryan to review): `sync` now auto-shares the GLOBAL root (`~/.claude/skills`) by ID check — a candidate folder with no `metadata.id`, or an id the team repo does not know, is connected automatically (fresh id minted, license/author stamped, committed); a known id is a no-op (content writes stay author-owned, installs stay people-file work). `config.json` `auto_share: false` is the machine-wide opt-out. Hygiene remains the filter: a folder the connect machinery refuses is skipped and reported by name.
- Library screen: the header Connect CTA and the empty-state Connect primary are removed. **Add project** (the native chooser + `checkout add`, #102) is the manual path for projects — whole-project at a time, never per-skill — and is now also the Library empty state's primary (marketplace link when the CLI predates `checkout add`). Project checkouts are never auto-shared.
- Connect surfaces that remain: Settings ▸ Sharing's per-folder Share (path-connect), the onboarding wizard's connect step, and the CLI verb itself (the manual path, and the machinery the auto-share pass reuses).

## Changed 2026-09-10 — teammates' receipts reach this machine through `refresh` (W-08)

- Marketplace / SkillDetail Evals (staleness): every CLI read verb is fetch-free, so until now a teammate's committed receipt, a new catalog entry or a team-current version stayed invisible until this user's next write verb (`install`, `sync`, `publish`). The CLI's read-only fetch (the `refresh` verb at the time, the fetch-only `sync` since B1) fetches each team clone once and reports per team whether it moved; the real adapter runs it once when the CLI's hello advertises `features.refresh` and on every window focus, at most once a minute, one at a time, never while a foreground write verb runs, and invalidates the Marketplace boards only when a clone actually moved (ledger D9: after the app's own actions and on focus, never on a timer). See the f-auto-sync section below for the current shape. The Marketplace's "synced X ago" staleness is therefore bounded by a minute of focus rather than by the user's last write verb. Spec `.planning/specs/2026-09-10-w08-refresh-receipts.md`.
- Settings ▸ Sync (failed background fetch): **closed 2026-09-13.** A fetch that fails (offline, busy clone, missing clone) is recorded on the adapter (`RefreshPolicy.last()`) and now reaches the screen: it travels as `Settings.lastAutomatic` and draws a second line under Settings ▸ Sync ▸ "Sync now" carrying the CLI's own first error line and its notices (spec DECISION 3.23). `SettingsSync` is `in-progress` rather than locked, and FIDELITY.md records the deviation until the canvas is redrawn. The Evals tab, when the current version has no receipt but earlier versions do, now shows History beside "No receipt for this version" instead of claiming "Not evaluated"; with no history the markup is unchanged, which is what the locked `SkillDetailNoReceipt` board draws.
- Requires the CLI at the same release: `hello.features.refresh` gates the whole behaviour, so an app-only update leaves W-08 open until the CLI is updated too ("update the CLI too" in the release note).
## Changed 2026-09-10 — the SKILL.md tab renders styled Markdown (W-05)

- SkillDetail (SKILL.md tab, real adapter): the document renders through `SkillMarkdown` (`react-markdown` with a components map onto the mock's `md-h2` / `md-p` / `md-list` / `md-code` classes) instead of a bare `<ReactMarkdown>` whose output the preflight reset flattened to plain text (Bugs.pdf W-05, spec `.planning/specs/2026-09-10-w05-skill-markdown.md`). A leading `# <exact skill name>` is still lifted into the header; every other `h1` renders as `h2.md-h1`. (a) Links are inert and print their destination beside the text (`.md-url`): `opener:allow-open-url` covers only `github.com` and `discord.gg`, so a clickable link would work for two hosts and silently do nothing elsewhere — widening it is Teddy's call (spec Q2). (b) Images render as an inert chip naming the source: a relative path cannot resolve from `tauri.localhost`, and a remote one would be fetched under `csp: null`. (c) Closed 2026-09-10 (f-md-parity, spec `.planning/specs/2026-09-10-f-md-parity.md`): the frontmatter block the canvas draws above the body (AD-22) now renders on the real adapter from the file's own text — `parseSkillFrontmatter` returns the fenced text verbatim (key order, quoting and line endings kept), `ls` rows carry `frontmatter` beside `body`, and the adapter maps it into `skillMd.frontmatter` on every detail path; `e2e/routes/markdown-parity.spec.ts` asserts computed-style parity between the raw-md scenario and the mock blocks so the delta cannot silently reopen. (d) A skill whose SKILL.md has no body renders the meta line over an empty document; no board draws an empty-document state, so there is no copy for it.
- Test-only scenarios: `?__mock=raw-md` joins `invalid-newest` and `version-mismatch` as a scenario that exists only for tests. It feeds `RAW_MD` through the real component so `e2e/routes/markdown.spec.ts` can assert computed styles (`vitest` runs with `css:false`). No board draws it, and `readScenario` returns `default` inside the native shell.
## Changed 2026-09-10 — a skill opens from the Library root that holds it (W-03, W-04)

- Library card → SkillDetail (real adapter): clicking a local card looked the skill up by bare name in the team, so an uncommitted local skill answered "Couldn't find <name>" (W-03) and a project skill opened under the Global crumb with Global selected and Install offered (W-04). Cards now carry their checkout root (`root=<path>` in the route), `Backend.skill` takes an optional `at: LibraryScope`, the adapter resolves a scoped read against that root only (Global first by rule when unscoped, never by the order the CLI emitted its sections), and `SkillDetail.owningRoot` drives the crumb, the sidebar selection and the install/remove actions. Spec `.planning/specs/2026-09-10-w03w04-skill-routing.md`.
- Not-found copy now separates the two causes the adapter can tell apart: "No skill named X is shared in team T, and no readable folder of that name is in your Library roots." (`not-found`) and "N team skills in T have an ID starting with X; open the one you want from the marketplace." (`ambiguous-ref`, a new `Result.reason`). Neither string is drawn by a board; both are interim copy for Teddy's design call.
- Still open: (a) showing "also in <other root>" when a bare name exists in two roots is a new design surface no board draws; (b) after a failed read of a project-scoped skill the crumb falls back to the team/Global label rather than the project's (the spec's project-label breadcrumb was not implemented; recorded in the batch report as an open question).

## Changed 2026-09-10 — W-02: independent CLI spawns now overlap (heading corrected 2026-09-11)

- All surfaces (real adapter): each desktop read spawned a fresh node process that loaded 68 unbundled ESM files before doing anything, the skill page chained five spawns one after another, and `ls`/`status` probed remotes and health checks per folder in sequence (Bugs.pdf W-02, spec `.planning/specs/2026-09-10-w02-perf.md`). The CLI now ships as one bundled `dist/index.js` (`scripts/bundle-cli.mjs`, esbuild; package assets resolve through `packageRoot()` so the source and bundled layouts behave the same), `local-skills` scans folders concurrently, `ls` and `status` overlap their probes with bounded, ordered concurrency, and `status` looks up GitHub collaborator permissions only when asked (`status --permissions`, requested by the roster read alone).
- Read cache (real adapter): the per-window read cache is stale-while-revalidate instead of being emptied on focus — focus marks every entry stale and serves the previously true board while the read re-runs in the background (a failed refresh drops the entry and broadcasts); mutations still clear it outright. Independent spawns run together (`skill()` reads team and local inventory at once, then validation and the eval report at once); catalog member reads are bounded to four with one retry when the bridge refuses a ninth child (`desktop/src/backend/tauri/concurrency.ts`); `status` is prefetched at launch. Focus therefore does two things: mark reads stale (W-02) and trigger the throttled `refresh` (W-08).
- SkillDetail dialogs (Move / Publish): `install` emits four `progress` frames through the CLI's one progress seam (`Prompter.progress`, shared with the setup wizard's discover and evals steps); the open dialog renders the current step as one status line (`.skill-dialog-progress`, existing tokens only). `features.progress` is true. No board draws a progress line inside these dialogs; the line is interim copy for the design call.
- Not verifiable on Linux: the Windows timings themselves (falsifier: `Measure-Command { terum-skills ls --local }` before and after; the skill page opening in one round of spawns instead of five).
- **Corrected 2026-09-11.** This section used to be headed "every action is fast on Windows", which nothing in the batch established. The batch's Windows figures were models and the bullet above says so; no measurement on Windows has ever been taken, before or after. Three findings since:
  - The reporter was never running this code. W-02 is in `v0.13.0` alone, and the only Windows installers this repository has ever served are `0.1.9_x64-setup.exe` and `0.1.10_x64-setup.exe`. Every `arm64-setup.exe` has zero downloads at every version, so the running app is 0.1.10 and it is the x64 build on an ARM64 machine.
  - The installer choice came from `process.arch`, which reports the emulated process under Windows emulation rather than the host CPU (fixed in `p-arch`).
  - The parallelism was re-serialised in the shell: `cli_spawn` held the admission mutex across `CreateProcess` and every bridge command ran on the UI thread (fixed in `p-bridge`). W-02 was scoped out of `src-tauri`, so neither was looked at.
- Fixed for reads, still true for writes: reads go through one long-lived `terum-skills --frames serve` child (`p-serve`, spec `.planning/specs/2026-09-11-w02b-subsecond.md`); every mutating verb is still a fresh node process. Measured on Linux (12 CPUs, node 24.15.0, real team fixture), fresh process against warm in the session: `status` 164 ms / 45 ms, `ls --local` 145 / 21, `ls --team` 134 / 15, `eval-report` 109 / 7. Session boot is 77 ms, paid once.
- The burst after a mutation is the one case the session does not win on Linux. `notify()` clears every cached read, so all the mounted boards refetch at once, and `serve` runs requests one at a time (cwd is process-global, so overlapping requests with different `cwd` would race). A seven-read burst is **177 ms through the session against 163 ms as seven concurrent one-shot processes** — the session is marginally worse here, because seven processes genuinely run in parallel across twelve cores. On Windows the balance should invert, since `CreateProcess` is the expensive operation and the one W-02 originally serialised, but that is a prediction, not a measurement. If it needs fixing, the fix is to let `serve` overlap requests that share the current `cwd` and serialise only on a `cwd` change.
- Startup decomposition, same box, `--version`, 15 runs: 86 ms total, of which 23 ms is bare `node -e ''` and ~63 ms is ours. Bundle composition by bytes in the output: zod 759 KB (42%), our `src` 588 KB, yaml 253 KB, commander 106 KB. zod reaches the hot path through `src/lib/schema.ts`, which `config.ts`, `status.ts` and `skills.ts` all import, so deferring it means replacing the validation layer — not a speed change, a robustness change, and not worth making for this.
- **`module.enableCompileCache()` in `src/index.ts` does not speed the shipped bundle, and an earlier note here claiming 80 ms → 60 ms was wrong.** Measured: 86 ms with the call as shipped, 74 ms with `NODE_COMPILE_CACHE` set in the environment instead. The call cannot cache the entry, because the 1.8 MB bundle is already compiled by the time its first line runs; only the environment variable is read before the entry is compiled. It is kept because it does cache the unbundled tree, which is what the tests and `npm link` users run. The clean fix — emit a tiny `dist/index.js` stub that arms the cache and then imports the real bundle as `dist/cli.js` — measures the same 12 ms saving and is **blocked**: `describeLaunch` (`src/lib/launch.ts`) identifies the install kind by matching the entry's realpath against `dist/index.js` in three places, so moving the body to `cli.js` classifies every launch as `unknown` and every `npx …` hint in the app and the CLI goes wrong. Worth 12 ms only alongside restructuring the bin entry into a callable `main(entryUrl)`; not attempted.

## Changed 2026-09-10 — the setup wizard finds skill folders and evaluates every shared skill (setup-discover-evals)

- SetupBoot (real adapter): the CLI `setup` wizard gained two optional steps, `discover` (looks for folders on this machine that hold `.claude/skills` and offers to register each one; the same scan is the verb `checkout discover`) and `evals` (evaluates every shared skill whose current version has no receipt, after one consented preflight, one skill at a time), so `SETUP_STEP_KEYS` is `welcome, app, role, github, team, actions, invite, discover, evals, community, hook, wrapper, done` and the boot screen draws the two rows (spec `.planning/specs/2026-09-10-setup-discover-evals.md`, Teddy 2026-09-10). Both steps default to no; `--no-discover` / `--no-evals` skip the offers and the install bootstrap's quiet mode never offers either. The CLI's first `progress` frames come from the discover scan and the eval batch (`Prompter.progress`, the seam #148 declared); the fallback progress row is suppressed when its label names a drawn row.
- Settings ▸ This machine (real adapter): a "Find skills…" control runs `Backend.checkouts.discover` and offers the candidates; the Checkouts group is hidden on the mock machine board (`surfaces.checkouts` is false there), so no locked board moves and no FIDELITY row changes. `hello.features.discover` gates both the control and the wizard step; a CLI that reports it false hides the control.
- Still open: (a) the discover counter renders on the determinate `ProgressCard` bar because `Frame.progress.total` is required in `types.ts` (an indeterminate variant touches `run.ts`, `drive.ts`, `setup-session.ts`, `ProgressCard`, the mock and their tests); (b) a cancelled eval batch on Windows may leave a `claude` grandchild (`Child::kill` does not terminate descendants; W-06 owns cancellation); (c) the drawn 14-board wizard is untouched — the two steps exist only on the boot screen the real app runs.

## Changed 2026-09-10 — updates are visible and install on close or overnight (f-update-policy)

- TopBar (real adapter): when the launch check reports a newer version, `topbar-right` renders an update chip before the eval chip — `Update ready · {version}` once the release is staged, `Update available · {version}` while it is not. It reads the check already in the cache and never fetches. Clicking it opens Settings ▸ Updates with the app row focused. It is absent when the app is up to date, unsupported, or still checking, and on the mock.
- Settings ▸ Updates (real adapter): the app row's action is `Install now` when staged and `Download` when available but not staged; the download toggle is replaced by an `Install updates` select with `Ask me` / `When I quit` / `Overnight`. The old `updates:app:auto` preference migrates on first read (`false → ask`, `true → on-close`), and the default for a machine with no preference is `When I quit`.
- Two ways an update can install without a click: closing the last window hands the staged version to a detached installer that outlives the WebView, and the overnight policy applies a staged version between 01:00 and 05:00 local while the app is open and has been idle for 30 minutes. Both record a reason in the update marker, so the next launch says `Updated to {v} when you quit` or `overnight` rather than a bare version.
- Still open: (a) the detached installer's native half is compiled by CI only — no Cargo on the Linux box that wrote it, so the Windows and macOS spawn paths are read-reviewed, not exercised; (b) the overnight apply needs the app open, so a machine that is shut down overnight still installs on the next quit; (c) the Settings ▸ Updates board is not redrawn for the select or the two new action labels.
## Changed 2026-09-10 — the wizard prices the eval batch, runs it in parallel, or queues it for the night (f-wizard)
- SetupBoot (real adapter): before the evals question the wizard prints what the batch will cost and how long it will take, measured from the team's own receipts, and the question itself became a four-way choice — Now, In batches, Overnight, Skip — with a one-line description under each. Evals run four at a time; each one's output is kept in its own block so parallel runs stay readable, and only one of them can hold the prompt at a time.
- Overnight queues the batch instead of running it. The app drains the queue between 01:00 and 05:00 while it is open and idle, through the same overnight scheduler the update policy uses, and `eval --drain` runs it by hand at any time. `eval` also gained `--parallel`, `--queue-list`, `--window`, `--max` and `--dequeue`.
- Onboarding (real adapter): the prompt dialog renders a select as a radio list carrying each choice's description, ask frames carry those descriptions, the setup result learned the `queued` and `batched` outcomes, and the evals row counts settled evals while a parallel batch runs.
- Terminal presentation (CLI only): the wizard opens with the mark and a welcome line, each step that prints opens with a bold `> ` header, choices show the default as `› n.`, outcomes print in green or red, and the run ends in a session box. Frame mode, `--quiet`, `NO_COLOR`, `TERM=dumb` and non-TTY output are byte-identical to before, and every question string is unchanged, so the app's step classification is unaffected.
- Still open: (a) the overnight queue needs the app open or a manual drain — nothing runs while the app is closed; (b) estimates come from the current team's receipts only, and the batch reports honestly when there is too little history rather than inventing a figure; (c) the Settings ▸ Evals board is not redrawn for the overnight row, and the onboarding boards are not redrawn for the radio list.
## Changed 2026-09-14 — batch, overnight and pending evals are basic options past setup (eval-options)
- The wizard's four-way evals question (Now, In batches, Overnight, Skip) used to exist only inside setup; afterwards the CLI evaluated one skill per run and the app had no way to ask for several, so the queue the app drains overnight could be filled once and never again. `eval` now takes several skills (`eval a b c`), `--batch n` (n at a time, with the CLI's own "Continue with the next n?" between batches; declining queues the rest for later), `--window overnight|later` (queue instead of run) and `--pending` (every shared skill with no receipt for its current version, what setup offered). One paid preflight probe per run, refs resolved before any paid work, `EvalManyResult {mode, team, skills, ok, failed, queued, stoppedAfter?}`.
- App: `Backend.evalMany` spawns that line with the Settings ▸ Evals flags in front; the dialog is hosted from any route as `?dialog=bulk-eval&ref=a&ref=b` (`&pending=1` for the pending set) — BulkEvalDialog asks Now / In batches (size) / Overnight under the same honest cost sentence as the single-skill dialog and prints the terminal line for the choice; the run streams in BulkEvalRunDialog through the app-wide eval host (one at a time with every other eval, Escape keeps it running, Stop cancels it), and the top-bar chip names it (`Eval running · 3 skills`). Entry points today: Settings ▸ Evals ▸ "Evaluate pending…" (disabled without a team) and "Queue for overnight" beside "Run eval" on a skill's Evals tab; and the Library selection bar's "Evaluate N…" (desktop-ui-polish, 2026-09-14), which hands the drawn selection to the same URL.
- The overnight drain now carries the Settings ▸ Evals flags too (`eval --k … --model … --drain --parallel 4`): until now the receipt made at night used the CLI's own k while Settings promised the person's.
- Mock: `evalMany` resolves the fixture skills, asks the same question between batches, queues the same items and prints the CLI's lines; `__mock=no-team` refuses `--pending` as the CLI does. It never fails an eval, exactly like its single `eval`.
- Still open: the mock has no queue service, so an item the mock queues overnight is never drained there (the real adapter's drainer is unchanged); and `later` exists in the CLI for a manual drain but is not offered in the app, whose drain is deliberately unfiltered (A1) and runs every queued item overnight anyway.

## Changed 2026-09-10, rewritten 2026-09-13 — the app fetches by itself (f-auto-sync)
- The app no longer waits to be told. At the first hello that advertises `features.refresh`, and again whenever the window regains focus, the adapter spawns the CLI's fetch-only `sync` verb — `['sync']`, no flags — in the background: at most once a minute, one at a time, and never while a foreground write verb (publish, install, setup, eval, prune…) is running. A trigger that arrives during one of those is dropped without spending the interval, and the workflow gate's idle callback runs the fetch the moment that verb ends, so the fetch is deferred rather than lost. There is no timer, by construction: `desktop/src/app/refresh-policy.test.tsx` greps production source for `setInterval`/`refetchInterval`. A CLI whose hello does not advertise the feature is never spawned for one at all.
- One fetch covers every configured team, and it is the only thing it does. `sync` fetches each team clone and hard-resets it to `origin/main`, changing nothing else on this machine — no placement, no upload, no edit to a local skill (`docs/frame-protocol.md` §f-sync, which owns the verb's contract). Work recorded in `pending` is drained by re-running the matching `install` or `uninstall-skill`, never by a fetch. The policy is the same code path W-08 opened for the deleted `refresh` verb; B1 replaced `sync --auto --fresh-ms 600000` and its ten-minute cadence with this one.
- Every completed automatic fetch republishes the stamp-driven boards — Status, Settings ▸ Sync and the Inbox. A successful fetch wrote `run/<team>.stamp`, so a board that already drew "Last fetched" is stale even when no clone moved; a failed one has a new outcome for Settings ▸ Sync to render, and waiting for the next fetch to show it would be a minute of silence. A fetch that actually moved a clone republishes the Marketplace boards on top of that. Both go through the adapter's `notify`, so they land only after the reads already in flight have settled, and both empty the read cache exactly as the manual Sync now does.
- Settings ▸ Sync is the only board that speaks about any of this. Its "Sync now" row states the cadence in full, and after an automatic fetch that did not refresh every team it adds a second line carrying the CLI's own first error line and then one line per notice the CLI printed. The outcome reaches the screens as `Settings.lastAutomatic` (`AutoFetchOutcome` in `desktop/src/backend/types.ts`), read off the policy at every settings read; the mock adapter never runs a background fetch and reports `null`.
- Authority: Teddy, 2026-09-10, "Sync should be automatic and much faster for the application (for all skills projects, etc)", overriding the on-demand-only rule recorded for the app. The verb, the cadence and the busy gate are the 2026-09-11 library/marketplace refactor decision walk's rows 21–22 as ratified.
- Still open: (a) no measured before/after numbers — `scripts/bench-sync.mjs` ships but the agent's sandbox forbids git, so the Windows figures in the reporter's falsifier are the real check; (b) the automatic fetch has no surface of its own, so one in flight is invisible until it lands; (c) the Settings ▸ Sync board is not redrawn for the cadence and failure lines (FIDELITY.md holds the deliberate deviation).
- Audit follow-ups (2026-09-14): Settings ▸ Sync's hook card, Settings ▸ Updates' "Tracked installs" and "Release probe" rows and the Quarantine hint now describe the fetch-only sync, the launch-time release probe and the `prune` verb (the copy still promised placement at sync, an hourly hook throttle, a sync-driven probe and `sync --prune`); "Update notices" renders only while `surfaces.inbox` is true. The Sync now dialog says what happened ("Fetched 1 of 2 teams." / "Sync did not fetch any team.") and names each unfetched team's state in words, as an alert. The adapter resets the fetch throttle only when the launch file's write time changed (the coordinator re-reads it on every focus, which used to make the once-a-minute throttle a once-per-focus fetch). The launch update check repeats on focus until it has succeeded and re-reads the CLI's answer after an hour; Settings ▸ Updates offers "Check again" on the up-to-date row, treats a non-launched marker for the running version as the update it evidently was, and never lets a stale marker hide a newer release. `sync --hook` reports a clone fetched within the hour as `fresh` and leaves it alone.

## Changed 2026-09-13 — the skill page scrolls by pane, the Library filters and sorts, search is real (desktop-ui-polish)

Teddy, 2026-09-13, from the Windows app on Marketplace ▸ decision-walk ▸ Evals: "the eval report should scroll independently and actually scroll fully. Multi-select for publish should also happen. Filters, sort, and search do not function." Spec `.planning/specs/2026-09-13-desktop-ui-polish.md`.

- SkillDetail (both adapters): the detail column no longer scrolls as one piece (`.planning/specs/desktop-scroll-panes.md` is superseded for this screen). Each tab BODY scrolls itself with the repo's hidden scrollbar — `.skill-md-blocks`, `.evals-main`, `.quality-tab`, `.activity-tab` — and stops 28px short of the clipped edge; the header, ScoreRow, tab strip, each tab's head row and the History rail stay put, and the rail scrolls on its own. A 240px floor under every tab box keeps the column's own `overflow:auto` reachable in a short window (the app's 960×600 minimum) instead of the pane collapsing to a keyhole. `?full=1` renders the whole document in one piece as before, now for the SKILL.md tab too. The four panes and the rail are focusable named regions (`tabIndex=0`, `role="region"`, `aria-label`), so a keyboard user can Tab to a pane and PageDown through it on WKWebView, which has no keyboard-focusable-scroller heuristic; the ring is the app's accent, drawn inside the box, keyboard focus only. Quality and Activity rows no longer compress when a tab holds more rows than fit. Pixels: 0 differing on every SkillDetail board under the gate comparator; 1–6 anti-aliasing pixels at threshold 0 on ten boards from the first block now rasterising inside a scroll container.
- Library (both adapters): the toolbar's Sort button works and search applies as you type. Sort is a menu (`?sort=` updated | name | category; absent for the default order, which is the backend's own order so the Library boards do not move), typing writes `?q=` with `replace` and the ✕ pushes. The facet popover this branch wired to the Filter button (Verdict / Category / Status, `?filters=open`, `?verdicts=`…) was dropped before merge: #212 (2026-09-14, Ryan) took every facet out of the search rows and removed the Library's Filter button with them, so the search row only searches and sorts. `libraryQueryMatches` (name + description, case-insensitive) lives in `library-query.ts`.
- CLI `ls --local` rows and `LocalEntry` carry `updated`: the folder's SKILL.md mtime as an ISO-8601 instant, `null` when the stat gave no finite time. The printed lines are unchanged; the desktop mirror declares the field optional, so an older CLI still parses and a card's `updated` is the CLI's instant or `null` — never a substitute (research row RM-38 closes).
- Marketplace (both adapters): search is live — a query commits 250 ms after the last keystroke with `replace` (one history step per sentence, not per letter), Enter and ✕ commit at once and push, and a keystroke still in flight lands on top of any URL write made after it rather than undoing it. The `Filter` / `Filter · N` button this branch drew beside every search field (the facet popover had lost its entry point on 2026-09-10) was dropped before merge: #212 (2026-09-14) removed the drawer, the facets and their URL codec entirely, so no marketplace board moved.
- Search (`#/search?q=`, ⌘K / Ctrl+K): a real page instead of the "Search is coming" placeholder. Groups: Skills (`Backend.search`, the CLI's `search <term>` over the team clone), Your library (one `library` read per registered root — the same query keys the Library screen uses, so the cache is shared — for folders the team does not hold), People and Projects (`catalog` unioned with the mock's member/project hits). Every failure is explicit and local: a failed source prints the CLI's own sentence under its group while the others still render; a `no-team` answer replaces only the team-backed groups, so a machine with no team still searches its own folders. ↑/↓/Enter walk the rows and keep the highlight on screen; typing keeps the previous answer on screen (aria-busy) instead of blanking. The shortcut is Ctrl+K on Linux/Windows (`windowChrome==='native'`) and on the browser mock, ⌘K on macOS, read from the seam, never a platform probe; the top-bar chip prints `Ctrl+K` there.
- Real-adapter cost worth knowing: a committed search term spawns `search`, `catalog` and one `library` per root (four on a machine with three projects), each cached by TanStack until the next mutation.
- Library multi-select and bulk publish (both adapters): a `Select` button after Sort enters selection mode (`?select=1`, a link); each card draws a checkbox before ⋯ and the whole-card click toggles instead of navigating; the bar reads `N of M selected` against the cards currently drawn (a card hidden by the query or a facet stays selected but is neither counted nor sent), with Select all, Clear and `Publish N to team…`; Escape leaves the mode unless the filter popover, a dialog or a field has the key. The dialog (`?select=1&dialog=publish`) lists each card as `Ready` or `Skipped · <reason>` (the same `localActionReason` gate as the card menu; skipped rows are never sent), then runs the CLI's single-ref `publish` ONE row at a time — the team clone is write-locked — showing the run's progress label, then the same outcome sentence the skill page shows (shared `publish-outcome.ts`, so the two cannot drift) or `Failed · <CLI error>` verbatim; a failed row does not stop the queue, Cancel ends the active run (`Cancelled`) and leaves the rest `Not started` with finished rows keeping their outcome. Done refetches every Library read and skill page and reports `Published X of N skills` (+ ` · Y failed`) in the Library's one status line. The ref is the card's path for a folder the team has never seen (the CLI accepts a path since #193) and the name otherwise, exactly as the skill page publishes. The bar's `children` slot is the extension point for the auto-update branch's bulk-eval dialog (`?dialog=bulk-eval&ref=…`, "Evaluate N…") once both land. Reviewed 2026-09-14: a settle that carries a value after Cancel (the real adapter's "Cancelled, but publish had already finished; its changes are on disk.") counts as published and shows both sentences; only `cancelled:true` or the adapters' literal 'Cancelled.' reads `Cancelled`, anything else after Cancel is `Failed · <the CLI's sentence>`; a second Cancel on a run that never settles leaves the dialog and the unmount refetches what landed; the status line under the toolbar belongs to the library it reports (a checkout shows none) and takes keyboard focus after Done; Clear works on a selection the query is hiding. Neither bulk action passes `team` — SkillCard carries none — so on a machine with several teams the CLI's own 'ambiguous-team' sentence is what a row shows.
- Library "Evaluate N…" (both adapters, 2026-09-14, after #206 landed): a button after "Publish N skills to team…" in the selection bar, shown when the CLI offers `runEvalInApp` like the card menu's Run eval. It runs nothing itself: it writes `?dialog=bulk-eval&ref=…` (a push, so Back closes the question) for the drawn selection and the app-wide BulkEvalDialog asks Now / In batches / Overnight and streams the run through the eval host; closing the question lands back on `?select=1` with the selection intact, and the host's clone invalidation refetches the Library when the run ends. Only cards the ⋯ menu could evaluate are handed over — the ref rule is the one `localRef` now shared by `publish`, `eval` and both bulk dialogs (a folder the team has never seen by path, a team skill by name) — and a line under the bar names the rest with the menu's own reason ("Left out of the eval · a, b: Install it first — evals run against the copy on your machine."), so the button never counts a skill the CLI would refuse. The question shows the refs it was given, so an untracked folder appears by its path there, exactly as on the terminal line.

## Changed 2026-09-14 — right-click menus, one-click copy, selectable text, host-spelled labels, drop-to-add-project (desktop-qol)

Teddy, 2026-09-14: "Go through all of the settings subcards upon click and make sure all of those are also up to par in fidelity, depth, and quality … crawl through the app in general for any basic quality of life updates (easy copy, click, drag, right click, etc.)". Spec `.planning/specs/2026-09-14-desktop-qol-affordances.md`. No board moved (measured per row at the base commit and on the branch).

- App-wide right-click menus (both adapters): skill cards (the ⋯ rows, then Copy name / Copy path / Show in Finder / Open in editor), the detail title (+ Copy install command), sidebar folder rows (+ Manage projects…), every terminal hint and CLI box (Copy command), Settings rows that name a folder (Show in Finder / Copy path), placement and approval rows (Open <skill>), About rows (Copy all versions), inbox items (Open <skill> / Copy skill name), member rows (Open profile / Copy handle / Copy name). In the Tauri shell the webview's own Back/Reload/Save-as menu is suppressed outside text fields and selections; the browser mock keeps it.
- One-click copy: every mono value in Settings, the locked handle, the About versions and the detail rail's Version copy on click ("Copied <what>" toast). Placement rows open their skill on click and carry the full path in their tooltip.
- Text selection: prose and values are selectable again (`.shell` keeps `user-select:none` for chrome and controls).
- Host labels: Settings ▸ Keyboard spells chords `Ctrl+K` on a native host; every "Show in Finder" reads "Show in Explorer" on Windows and "Show in file manager" on Linux (`src/lib/platform-labels.ts`).
- Seam: `Backend.onFileDrop` (Tauri: `getCurrentWebview().onDragDropEvent`; mock: HTML5 drag events, `text/plain` lines as paths). The Library adds a dropped folder as a project through the same `projects.add` the chooser uses and reports `Added N projects`.
- Settings ▸ Publishing is four groups (Teddy, 2026-09-14, "more comprehensive with more options"; spec §8): Defaults (`Publish to` → `--project`, `Category` → `--category`; read-only receipts row), Shared from this machine (the Global Library read with each folder's state and a Publish… action), Rules (the CLI's own steps, read-only) and Nothing automatic. Both publish dialogs draw the target (and, under "Ask before publishing", a category field on the skill page), so the app always sends `--project` and the CLI's "Which project?" prompt no longer appears. Seam: `PublishArgs.category`. Superseded on the CLI side: the 2026-09-10 "auto-share is the global default" entry above — `sync` no longer connects anything (the CLI's `team migrate` help names "the release removing auto-share").
- Still gaps on the real adapter's Settings, CLI-side: `status` reports no quarantine entries, no cache/evals sizes or counts, no agent CLI version, no latest CLI version, so Machine ▸ Quarantine says "not reported by this terum-skills version", Evals ▸ Agent CLI and Advanced ▸ storage rows show `—`. Canvas-side: `SettingsSharing.png` was never renamed to `SettingsPublishing.png` (the row has no oracle), and the Machine board's prune hint prints `sync --prune` where the CLI's verb is `prune`.

## Ratified 2026-09-11 — the new Settings and onboarding designs stand; the canvas follows them

Teddy, 2026-09-11: "I support the designs for settings and others." This settles the three surfaces the
2026-09-10 batches moved ahead of the locked canvas — Settings ▸ Updates (the three-way install policy and the
Install now / Download actions), Settings ▸ Sync (the automatic-cadence description and the failure line), and
Settings ▸ Evals with the onboarding prompt dialog (the overnight row, and selects drawn as a radio list with a
description under each choice). The app is the reference for all three: the canvas is redrawn to match it, and
none of them should be reverted to the drawn version. The affected boards stay `in-progress` only until that
redraw lands; see the deliberate-deviation entries in FIDELITY.md for the per-board detail.

## 2026-09-13 — the CLI renders boards; `policies.ts` is the desktop's twin

- `src/lib/render/policies.ts` (a leaf: imports nothing) carries the verdict band, half-even lift, receipt summary, orderings and estimate rules the desktop keeps in `desktop/src/backend/mock/derive.ts`, `desktop/src/backend/receipt-summary.ts` and `desktop/src/backend/score-fractions.ts`, with the desktop's own examples as its tests. Follow-up: import it from the desktop and delete the copies, so a rule changes in one place.
