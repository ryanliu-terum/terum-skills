import { create } from 'zustand';
import { persist, createJSONStorage } from 'zustand/middleware';
import type { Theme } from '../backend/types';
import { pickBackend } from '../backend';
import { applyTheme } from '../backend/theme';
export { applyTheme } from '../backend/theme';
interface UiState {theme:Theme;sidebarCollapsed:boolean;collapsedSections:string[];setSidebarCollapsed:(collapsed:boolean)=>void;setCollapsedSections:(sections:string[])=>void;overviewHidden:boolean;railOpen:boolean;lastRoute:string;setTheme:(theme:Theme)=>void;setOverviewHidden:(hidden:boolean)=>void;setRailOpen:(open:boolean)=>void;setLastRoute:(route:string)=>void}
export const useUiStore=create<UiState>()(persist(set=>({theme:'dark',sidebarCollapsed:false,collapsedSections:[],setSidebarCollapsed:sidebarCollapsed=>set({sidebarCollapsed}),setCollapsedSections:sections=>set({collapsedSections:[...new Set(sections)]}),overviewHidden:false,railOpen:true,lastRoute:'/library/global',setTheme:theme=>{applyTheme(theme);set({theme});},setOverviewHidden:overviewHidden=>set({overviewHidden}),setRailOpen:railOpen=>set({railOpen}),setLastRoute:lastRoute=>set({lastRoute})}),{name:'terum-skills-app:ui',merge:(persisted,current)=>{if(!persisted||typeof persisted!=='object')return current;const saved=persisted as Record<string,unknown>;return {...current,theme:saved.theme==='dark'||saved.theme==='light'||saved.theme==='system'?saved.theme:current.theme,sidebarCollapsed:typeof saved.sidebarCollapsed==='boolean'?saved.sidebarCollapsed:current.sidebarCollapsed,collapsedSections:Array.isArray(saved.collapsedSections)&&saved.collapsedSections.every(section=>typeof section==='string')?[...new Set(saved.collapsedSections)]:current.collapsedSections,overviewHidden:typeof saved.overviewHidden==='boolean'?saved.overviewHidden:current.overviewHidden,railOpen:typeof saved.railOpen==='boolean'?saved.railOpen:current.railOpen,lastRoute:typeof saved.lastRoute==='string'&&saved.lastRoute.startsWith('/')?saved.lastRoute:current.lastRoute};},storage:createJSONStorage(()=>({getItem:()=>{const saved=pickBackend().prefs.get<unknown>('ui',{});return JSON.stringify(saved);},setItem:(_key,value)=>{try{pickBackend().prefs.set('ui',JSON.parse(value));}catch{ /* In-memory chrome remains usable if storage is denied. */ }},removeItem:()=>pickBackend().prefs.set('ui',{})}))}));
